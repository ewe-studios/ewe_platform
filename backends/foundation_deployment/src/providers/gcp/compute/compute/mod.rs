//! Auto-generated API module for gcp/compute compute.
//!
//! Generated by `cargo run --bin ewe_platform gen_api`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp_compute_compute `

#![cfg(feature = "gcp_compute_compute")]
#![allow(clippy::too_many_arguments, clippy::type_complexity)]
#![allow(
    clippy::missing_errors_doc,
    clippy::doc_markdown,
    clippy::useless_format
)]
#![allow(unused_imports)]

use foundation_core::valtron::{TaskIterator, TaskIteratorExt};
use foundation_netio::simple_http::client::{ClientRequestBuilder, SimpleHttpClient};
use foundation_macros::JsonHash;
use serde::{Deserialize, Serialize};

// Import shared types used by this module
use super::shared::Operation;

use super::shared::ApiResponse;

// =============================================================================
// TYPE DECLARATIONS
// =============================================================================

/// `GetVersionOperationMetadata` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GetVersionOperationMetadata {
    /// inlineSbomInfo property.
    pub inline_sbom_info: Option<GetVersionOperationMetadataSbomInfo>,
}

/// `BackendServiceHAPolicyLeader` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct BackendServiceHAPolicyLeader {
    /// backendGroup property.
    pub backend_group: Option<String>,
    /// networkEndpoint property.
    pub network_endpoint: Option<BackendServiceHAPolicyLeaderNetworkEndpoint>,
}

/// `LicenseCode` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct LicenseCode {
    /// creationTimestamp property.
    pub creation_timestamp: Option<String>,
    /// description property.
    pub description: Option<String>,
    /// id property.
    pub id: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// licenseAlias property.
    pub license_alias: Option<Vec<LicenseCodeLicenseAlias>>,
    /// name property.
    pub name: Option<String>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// state property.
    pub state: Option<String>,
    /// transferable property.
    pub transferable: Option<bool>,
}

/// `TargetVpnGateway` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct TargetVpnGateway {
    /// creationTimestamp property.
    pub creation_timestamp: Option<String>,
    /// description property.
    pub description: Option<String>,
    /// forwardingRules property.
    pub forwarding_rules: Option<Vec<String>>,
    /// id property.
    pub id: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// labelFingerprint property.
    pub label_fingerprint: Option<String>,
    /// labels property.
    pub labels: Option<serde_json::Value>,
    /// name property.
    pub name: Option<String>,
    /// network property.
    pub network: Option<String>,
    /// params property.
    pub params: Option<TargetVpnGatewayParams>,
    /// region property.
    pub region: Option<String>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// status property.
    pub status: Option<String>,
    /// tunnels property.
    pub tunnels: Option<Vec<String>>,
}

/// `ResourcePolicyWeeklyCycle` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ResourcePolicyWeeklyCycle {
    /// dayOfWeeks property.
    pub day_of_weeks: Option<Vec<ResourcePolicyWeeklyCycleDayOfWeek>>,
}

/// `Image` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Image {
    /// architecture property.
    pub architecture: Option<String>,
    /// archiveSizeBytes property.
    pub archive_size_bytes: Option<String>,
    /// creationTimestamp property.
    pub creation_timestamp: Option<String>,
    /// deprecated property.
    pub deprecated: Option<DeprecationStatus>,
    /// description property.
    pub description: Option<String>,
    /// diskSizeGb property.
    pub disk_size_gb: Option<String>,
    /// enableConfidentialCompute property.
    pub enable_confidential_compute: Option<bool>,
    /// family property.
    pub family: Option<String>,
    /// guestOsFeatures property.
    pub guest_os_features: Option<Vec<GuestOsFeature>>,
    /// id property.
    pub id: Option<String>,
    /// imageEncryptionKey property.
    pub image_encryption_key: Option<CustomerEncryptionKey>,
    /// kind property.
    pub kind: Option<String>,
    /// labelFingerprint property.
    pub label_fingerprint: Option<String>,
    /// labels property.
    pub labels: Option<serde_json::Value>,
    /// licenseCodes property.
    pub license_codes: Option<Vec<String>>,
    /// licenses property.
    pub licenses: Option<Vec<String>>,
    /// name property.
    pub name: Option<String>,
    /// params property.
    pub params: Option<ImageParams>,
    /// rawDisk property.
    pub raw_disk: Option<std::collections::HashMap<String, serde_json::Value>>,
    /// satisfiesPzi property.
    pub satisfies_pzi: Option<bool>,
    /// satisfiesPzs property.
    pub satisfies_pzs: Option<bool>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// shieldedInstanceInitialState property.
    pub shielded_instance_initial_state: Option<InitialStateConfig>,
    /// sourceDisk property.
    pub source_disk: Option<String>,
    /// sourceDiskEncryptionKey property.
    pub source_disk_encryption_key: Option<CustomerEncryptionKey>,
    /// sourceDiskId property.
    pub source_disk_id: Option<String>,
    /// sourceImage property.
    pub source_image: Option<String>,
    /// sourceImageEncryptionKey property.
    pub source_image_encryption_key: Option<CustomerEncryptionKey>,
    /// sourceImageId property.
    pub source_image_id: Option<String>,
    /// sourceSnapshot property.
    pub source_snapshot: Option<String>,
    /// sourceSnapshotEncryptionKey property.
    pub source_snapshot_encryption_key: Option<CustomerEncryptionKey>,
    /// sourceSnapshotId property.
    pub source_snapshot_id: Option<String>,
    /// sourceType property.
    pub source_type: Option<String>,
    /// status property.
    pub status: Option<String>,
    /// storageLocations property.
    pub storage_locations: Option<Vec<String>>,
}

/// `NetworksRequestRemovePeeringRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct NetworksRequestRemovePeeringRequest {
    /// name property.
    pub name: Option<String>,
}

/// `InterconnectGroupsCreateMembersRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct InterconnectGroupsCreateMembersRequest {
    /// request property.
    pub request: Option<InterconnectGroupsCreateMembers>,
}

/// `ReservationSubBlocksReportFaultyRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ReservationSubBlocksReportFaultyRequest {
    /// disruptionSchedule property.
    pub disruption_schedule: Option<String>,
    /// failureComponent property.
    pub failure_component: Option<String>,
    /// faultReasons property.
    pub fault_reasons: Option<Vec<ReservationSubBlocksReportFaultyRequestFaultReason>>,
}

/// `DisksStartAsyncReplicationRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct DisksStartAsyncReplicationRequest {
    /// asyncSecondaryDisk property.
    pub async_secondary_disk: Option<String>,
}

/// `StoragePoolAggregatedList` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct StoragePoolAggregatedList {
    /// etag property.
    pub etag: Option<String>,
    /// id property.
    pub id: Option<String>,
    /// items property.
    pub items: Option<serde_json::Value>,
    /// kind property.
    pub kind: Option<String>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// unreachables property.
    pub unreachables: Option<Vec<String>>,
    /// warning property.
    pub warning: Option<std::collections::HashMap<String, serde_json::Value>>,
}

/// `SecuritySettings` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SecuritySettings {
    /// awsV4Authentication property.
    pub aws_v4_authentication: Option<AWSV4Signature>,
    /// clientTlsPolicy property.
    pub client_tls_policy: Option<String>,
    /// subjectAltNames property.
    pub subject_alt_names: Option<Vec<String>>,
}

/// `BackendServiceAggregatedList` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct BackendServiceAggregatedList {
    /// id property.
    pub id: Option<String>,
    /// items property.
    pub items: Option<serde_json::Value>,
    /// kind property.
    pub kind: Option<String>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// unreachables property.
    pub unreachables: Option<Vec<String>>,
    /// warning property.
    pub warning: Option<std::collections::HashMap<String, serde_json::Value>>,
}

/// `DisplayDevice` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct DisplayDevice {
    /// enableDisplay property.
    pub enable_display: Option<bool>,
}

/// `AllocationAggregateReservation` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AllocationAggregateReservation {
    /// inUseResources property.
    pub in_use_resources: Option<Vec<AllocationAggregateReservationReservedResourceInfo>>,
    /// reservedResources property.
    pub reserved_resources: Option<Vec<AllocationAggregateReservationReservedResourceInfo>>,
    /// vmFamily property.
    pub vm_family: Option<String>,
    /// workloadType property.
    pub workload_type: Option<String>,
}

/// `InterconnectGroupsOperationalStatus` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct InterconnectGroupsOperationalStatus {
    /// configured property.
    pub configured: Option<InterconnectGroupConfigured>,
    /// groupStatus property.
    pub group_status: Option<String>,
    /// intent property.
    pub intent: Option<InterconnectGroupIntent>,
    /// interconnectStatuses property.
    pub interconnect_statuses: Option<Vec<InterconnectGroupsOperationalStatusInterconnectStatus>>,
    /// operational property.
    pub operational: Option<InterconnectGroupConfigured>,
}

/// `HttpRedirectAction` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct HttpRedirectAction {
    /// hostRedirect property.
    pub host_redirect: Option<String>,
    /// httpsRedirect property.
    pub https_redirect: Option<bool>,
    /// pathRedirect property.
    pub path_redirect: Option<String>,
    /// prefixRedirect property.
    pub prefix_redirect: Option<String>,
    /// redirectResponseCode property.
    pub redirect_response_code: Option<String>,
    /// stripQuery property.
    pub strip_query: Option<bool>,
}

/// `HostRule` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct HostRule {
    /// description property.
    pub description: Option<String>,
    /// hosts property.
    pub hosts: Option<Vec<String>>,
    /// pathMatcher property.
    pub path_matcher: Option<String>,
}

/// `InstancesGetEffectiveFirewallsResponseEffectiveFirewallPolicy` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct InstancesGetEffectiveFirewallsResponseEffectiveFirewallPolicy {
    /// displayName property.
    pub display_name: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// packetMirroringRules property.
    pub packet_mirroring_rules: Option<Vec<FirewallPolicyRule>>,
    /// priority property.
    pub priority: Option<i64>,
    /// rules property.
    pub rules: Option<Vec<FirewallPolicyRule>>,
    /// shortName property.
    pub short_name: Option<String>,
    /// type property.
    pub r#type: Option<String>,
}

/// `ImageFamilyView` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ImageFamilyView {
    /// image property.
    pub image: Option<Image>,
}

/// `SnapshotGroupParameters` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SnapshotGroupParameters {
    /// replicaZones property.
    pub replica_zones: Option<Vec<String>>,
    /// sourceSnapshotGroup property.
    pub source_snapshot_group: Option<String>,
    /// type property.
    pub r#type: Option<String>,
}

/// `InstanceGroupManagersResumeInstancesRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct InstanceGroupManagersResumeInstancesRequest {
    /// instances property.
    pub instances: Option<Vec<String>>,
}

/// `AllocationResourceStatus` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AllocationResourceStatus {
    /// healthInfo property.
    pub health_info: Option<AllocationResourceStatusHealthInfo>,
    /// reservationBlockCount property.
    pub reservation_block_count: Option<i64>,
    /// reservationMaintenance property.
    pub reservation_maintenance: Option<GroupMaintenanceInfo>,
    /// specificSkuAllocation property.
    pub specific_sku_allocation: Option<AllocationResourceStatusSpecificSKUAllocation>,
}

/// `InterconnectAttachmentL2Forwarding` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct InterconnectAttachmentL2Forwarding {
    /// applianceMappings property.
    pub appliance_mappings: Option<serde_json::Value>,
    /// defaultApplianceIpAddress property.
    pub default_appliance_ip_address: Option<String>,
    /// geneveHeader property.
    pub geneve_header: Option<InterconnectAttachmentL2ForwardingGeneveHeader>,
    /// network property.
    pub network: Option<String>,
    /// tunnelEndpointIpAddress property.
    pub tunnel_endpoint_ip_address: Option<String>,
}

/// `FutureReservationCommitmentInfo` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct FutureReservationCommitmentInfo {
    /// commitmentName property.
    pub commitment_name: Option<String>,
    /// commitmentPlan property.
    pub commitment_plan: Option<String>,
    /// previousCommitmentTerms property.
    pub previous_commitment_terms: Option<String>,
}

/// `InstanceManagedByIgmErrorInstanceActionDetails` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct InstanceManagedByIgmErrorInstanceActionDetails {
    /// action property.
    pub action: Option<String>,
    /// instance property.
    pub instance: Option<String>,
    /// version property.
    pub version: Option<ManagedInstanceVersion>,
}

/// `NodeType` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct NodeType {
    /// cpuPlatform property.
    pub cpu_platform: Option<String>,
    /// creationTimestamp property.
    pub creation_timestamp: Option<String>,
    /// deprecated property.
    pub deprecated: Option<DeprecationStatus>,
    /// description property.
    pub description: Option<String>,
    /// guestCpus property.
    pub guest_cpus: Option<i64>,
    /// id property.
    pub id: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// localSsdGb property.
    pub local_ssd_gb: Option<i64>,
    /// maxVms property.
    pub max_vms: Option<i64>,
    /// memoryMb property.
    pub memory_mb: Option<i64>,
    /// name property.
    pub name: Option<String>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// zone property.
    pub zone: Option<String>,
}

/// `ServiceAttachmentAggregatedList` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ServiceAttachmentAggregatedList {
    /// id property.
    pub id: Option<String>,
    /// items property.
    pub items: Option<serde_json::Value>,
    /// kind property.
    pub kind: Option<String>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// unreachables property.
    pub unreachables: Option<Vec<String>>,
    /// warning property.
    pub warning: Option<std::collections::HashMap<String, serde_json::Value>>,
}

/// `AuditConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AuditConfig {
    /// auditLogConfigs property.
    pub audit_log_configs: Option<Vec<AuditLogConfig>>,
    /// service property.
    pub service: Option<String>,
}

/// `HelpLink` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct HelpLink {
    /// description property.
    pub description: Option<String>,
    /// url property.
    pub url: Option<String>,
}

/// `VpnGatewayStatusVpnConnection` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct VpnGatewayStatusVpnConnection {
    /// peerExternalGateway property.
    pub peer_external_gateway: Option<String>,
    /// peerGcpGateway property.
    pub peer_gcp_gateway: Option<String>,
    /// state property.
    pub state: Option<VpnGatewayStatusHighAvailabilityRequirementState>,
    /// tunnels property.
    pub tunnels: Option<Vec<VpnGatewayStatusTunnel>>,
}

/// `BackendCustomMetric` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct BackendCustomMetric {
    /// dryRun property.
    pub dry_run: Option<bool>,
    /// maxUtilization property.
    pub max_utilization: Option<f64>,
    /// name property.
    pub name: Option<String>,
}

/// `NetworkPerformanceConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct NetworkPerformanceConfig {
    /// totalEgressBandwidthTier property.
    pub total_egress_bandwidth_tier: Option<String>,
}

/// `HealthCheckServiceAggregatedList` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct HealthCheckServiceAggregatedList {
    /// id property.
    pub id: Option<String>,
    /// items property.
    pub items: Option<serde_json::Value>,
    /// kind property.
    pub kind: Option<String>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// unreachables property.
    pub unreachables: Option<Vec<String>>,
    /// warning property.
    pub warning: Option<std::collections::HashMap<String, serde_json::Value>>,
}

/// `RegionSnapshotUpdateKmsKeyRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct RegionSnapshotUpdateKmsKeyRequest {
    /// kmsKeyName property.
    pub kms_key_name: Option<String>,
}

/// `SecurityPolicyRuleRateLimitOptions` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SecurityPolicyRuleRateLimitOptions {
    /// banDurationSec property.
    pub ban_duration_sec: Option<i64>,
    /// banThreshold property.
    pub ban_threshold: Option<SecurityPolicyRuleRateLimitOptionsThreshold>,
    /// conformAction property.
    pub conform_action: Option<String>,
    /// enforceOnKey property.
    pub enforce_on_key: Option<String>,
    /// enforceOnKeyConfigs property.
    pub enforce_on_key_configs: Option<Vec<SecurityPolicyRuleRateLimitOptionsEnforceOnKeyConfig>>,
    /// enforceOnKeyName property.
    pub enforce_on_key_name: Option<String>,
    /// exceedAction property.
    pub exceed_action: Option<String>,
    /// exceedRedirectOptions property.
    pub exceed_redirect_options: Option<SecurityPolicyRuleRedirectOptions>,
    /// rateLimitThreshold property.
    pub rate_limit_threshold: Option<SecurityPolicyRuleRateLimitOptionsThreshold>,
}

/// `AutoscalerList` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AutoscalerList {
    /// id property.
    pub id: Option<String>,
    /// items property.
    pub items: Option<Vec<Autoscaler>>,
    /// kind property.
    pub kind: Option<String>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// warning property.
    pub warning: Option<std::collections::HashMap<String, serde_json::Value>>,
}

/// `InterconnectLocationList` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct InterconnectLocationList {
    /// id property.
    pub id: Option<String>,
    /// items property.
    pub items: Option<Vec<InterconnectLocation>>,
    /// kind property.
    pub kind: Option<String>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// warning property.
    pub warning: Option<std::collections::HashMap<String, serde_json::Value>>,
}

/// `ReservationBlockPhysicalTopology` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ReservationBlockPhysicalTopology {
    /// block property.
    pub block: Option<String>,
    /// cluster property.
    pub cluster: Option<String>,
    /// instances property.
    pub instances: Option<Vec<ReservationBlockPhysicalTopologyInstance>>,
}

/// `ZoneSetPolicyRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ZoneSetPolicyRequest {
    /// bindings property.
    pub bindings: Option<Vec<Binding>>,
    /// etag property.
    pub etag: Option<String>,
    /// policy property.
    pub policy: Option<Policy>,
}

/// `BackendServiceCdnPolicy` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct BackendServiceCdnPolicy {
    /// bypassCacheOnRequestHeaders property.
    pub bypass_cache_on_request_headers:
        Option<Vec<BackendServiceCdnPolicyBypassCacheOnRequestHeader>>,
    /// cacheKeyPolicy property.
    pub cache_key_policy: Option<CacheKeyPolicy>,
    /// cacheMode property.
    pub cache_mode: Option<String>,
    /// clientTtl property.
    pub client_ttl: Option<i64>,
    /// defaultTtl property.
    pub default_ttl: Option<i64>,
    /// maxTtl property.
    pub max_ttl: Option<i64>,
    /// negativeCaching property.
    pub negative_caching: Option<bool>,
    /// negativeCachingPolicy property.
    pub negative_caching_policy: Option<Vec<BackendServiceCdnPolicyNegativeCachingPolicy>>,
    /// requestCoalescing property.
    pub request_coalescing: Option<bool>,
    /// serveWhileStale property.
    pub serve_while_stale: Option<i64>,
    /// signedUrlCacheMaxAgeSec property.
    pub signed_url_cache_max_age_sec: Option<String>,
    /// signedUrlKeyNames property.
    pub signed_url_key_names: Option<Vec<String>>,
}

/// `NotificationEndpointList` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct NotificationEndpointList {
    /// id property.
    pub id: Option<String>,
    /// items property.
    pub items: Option<Vec<NotificationEndpoint>>,
    /// kind property.
    pub kind: Option<String>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// warning property.
    pub warning: Option<std::collections::HashMap<String, serde_json::Value>>,
}

/// `BackendBucketAggregatedList` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct BackendBucketAggregatedList {
    /// id property.
    pub id: Option<String>,
    /// items property.
    pub items: Option<serde_json::Value>,
    /// kind property.
    pub kind: Option<String>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// warning property.
    pub warning: Option<std::collections::HashMap<String, serde_json::Value>>,
}

/// `InstanceGroupManagerResizeRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct InstanceGroupManagerResizeRequest {
    /// creationTimestamp property.
    pub creation_timestamp: Option<String>,
    /// description property.
    pub description: Option<String>,
    /// id property.
    pub id: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// region property.
    pub region: Option<String>,
    /// requestedRunDuration property.
    pub requested_run_duration: Option<Duration>,
    /// resizeBy property.
    pub resize_by: Option<i64>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// selfLinkWithId property.
    pub self_link_with_id: Option<String>,
    /// state property.
    pub state: Option<String>,
    /// status property.
    pub status: Option<InstanceGroupManagerResizeRequestStatus>,
    /// zone property.
    pub zone: Option<String>,
}

/// `BackendBucketList` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct BackendBucketList {
    /// id property.
    pub id: Option<String>,
    /// items property.
    pub items: Option<Vec<BackendBucket>>,
    /// kind property.
    pub kind: Option<String>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// warning property.
    pub warning: Option<std::collections::HashMap<String, serde_json::Value>>,
}

/// `TargetTcpProxyAggregatedList` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct TargetTcpProxyAggregatedList {
    /// id property.
    pub id: Option<String>,
    /// items property.
    pub items: Option<serde_json::Value>,
    /// kind property.
    pub kind: Option<String>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// unreachables property.
    pub unreachables: Option<Vec<String>>,
    /// warning property.
    pub warning: Option<std::collections::HashMap<String, serde_json::Value>>,
}

/// `PacketMirroringForwardingRuleInfo` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct PacketMirroringForwardingRuleInfo {
    /// canonicalUrl property.
    pub canonical_url: Option<String>,
    /// url property.
    pub url: Option<String>,
}

/// `PublicAdvertisedPrefix` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct PublicAdvertisedPrefix {
    /// byoipApiVersion property.
    pub byoip_api_version: Option<String>,
    /// creationTimestamp property.
    pub creation_timestamp: Option<String>,
    /// description property.
    pub description: Option<String>,
    /// dnsVerificationIp property.
    pub dns_verification_ip: Option<String>,
    /// fingerprint property.
    pub fingerprint: Option<String>,
    /// id property.
    pub id: Option<String>,
    /// ipCidrRange property.
    pub ip_cidr_range: Option<String>,
    /// ipv6AccessType property.
    pub ipv6_access_type: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// pdpScope property.
    pub pdp_scope: Option<String>,
    /// publicDelegatedPrefixs property.
    pub public_delegated_prefixs: Option<Vec<PublicAdvertisedPrefixPublicDelegatedPrefix>>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// sharedSecret property.
    pub shared_secret: Option<String>,
    /// status property.
    pub status: Option<String>,
}

/// `InterconnectAttachmentGroupLogicalStructure` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct InterconnectAttachmentGroupLogicalStructure {
    /// regions property.
    pub regions: Option<Vec<InterconnectAttachmentGroupLogicalStructureRegion>>,
}

/// `TargetPoolsAddInstanceRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct TargetPoolsAddInstanceRequest {
    /// instances property.
    pub instances: Option<Vec<InstanceReference>>,
}

/// `GRPCHealthCheck` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GRPCHealthCheck {
    /// grpcServiceName property.
    pub grpc_service_name: Option<String>,
    /// port property.
    pub port: Option<i64>,
    /// portName property.
    pub port_name: Option<String>,
    /// portSpecification property.
    pub port_specification: Option<String>,
}

/// `RegionInstanceGroupManagersResumeInstancesRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct RegionInstanceGroupManagersResumeInstancesRequest {
    /// instances property.
    pub instances: Option<Vec<String>>,
}

/// `InterconnectLocationCrossSiteInterconnectInfo` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct InterconnectLocationCrossSiteInterconnectInfo {
    /// city property.
    pub city: Option<String>,
}

/// `RegionInstanceGroupList` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct RegionInstanceGroupList {
    /// id property.
    pub id: Option<String>,
    /// items property.
    pub items: Option<Vec<InstanceGroup>>,
    /// kind property.
    pub kind: Option<String>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// warning property.
    pub warning: Option<std::collections::HashMap<String, serde_json::Value>>,
}

/// `ReservationAggregatedList` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ReservationAggregatedList {
    /// id property.
    pub id: Option<String>,
    /// items property.
    pub items: Option<serde_json::Value>,
    /// kind property.
    pub kind: Option<String>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// unreachables property.
    pub unreachables: Option<Vec<String>>,
    /// warning property.
    pub warning: Option<std::collections::HashMap<String, serde_json::Value>>,
}

/// `Interconnect` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Interconnect {
    /// aaiEnabled property.
    pub aai_enabled: Option<bool>,
    /// adminEnabled property.
    pub admin_enabled: Option<bool>,
    /// applicationAwareInterconnect property.
    pub application_aware_interconnect: Option<InterconnectApplicationAwareInterconnect>,
    /// availableFeatures property.
    pub available_features: Option<Vec<String>>,
    /// circuitInfos property.
    pub circuit_infos: Option<Vec<InterconnectCircuitInfo>>,
    /// creationTimestamp property.
    pub creation_timestamp: Option<String>,
    /// customerName property.
    pub customer_name: Option<String>,
    /// description property.
    pub description: Option<String>,
    /// expectedOutages property.
    pub expected_outages: Option<Vec<InterconnectOutageNotification>>,
    /// googleIpAddress property.
    pub google_ip_address: Option<String>,
    /// googleReferenceId property.
    pub google_reference_id: Option<String>,
    /// id property.
    pub id: Option<String>,
    /// interconnectAttachments property.
    pub interconnect_attachments: Option<Vec<String>>,
    /// interconnectGroups property.
    pub interconnect_groups: Option<Vec<String>>,
    /// interconnectType property.
    pub interconnect_type: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// labelFingerprint property.
    pub label_fingerprint: Option<String>,
    /// labels property.
    pub labels: Option<serde_json::Value>,
    /// linkType property.
    pub link_type: Option<String>,
    /// location property.
    pub location: Option<String>,
    /// macsec property.
    pub macsec: Option<InterconnectMacsec>,
    /// macsecEnabled property.
    pub macsec_enabled: Option<bool>,
    /// name property.
    pub name: Option<String>,
    /// nocContactEmail property.
    pub noc_contact_email: Option<String>,
    /// operationalStatus property.
    pub operational_status: Option<String>,
    /// params property.
    pub params: Option<InterconnectParams>,
    /// peerIpAddress property.
    pub peer_ip_address: Option<String>,
    /// provisionedLinkCount property.
    pub provisioned_link_count: Option<i64>,
    /// remoteLocation property.
    pub remote_location: Option<String>,
    /// requestedFeatures property.
    pub requested_features: Option<Vec<String>>,
    /// requestedLinkCount property.
    pub requested_link_count: Option<i64>,
    /// satisfiesPzs property.
    pub satisfies_pzs: Option<bool>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// state property.
    pub state: Option<String>,
    /// subzone property.
    pub subzone: Option<String>,
    /// wireGroups property.
    pub wire_groups: Option<Vec<String>>,
}

/// `HealthSourcesGetHealthResponseSourceInfo` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct HealthSourcesGetHealthResponseSourceInfo {
    /// backends property.
    pub backends: Option<Vec<HealthSourcesGetHealthResponseSourceInfoBackendInfo>>,
    /// forwardingRule property.
    pub forwarding_rule: Option<String>,
    /// source property.
    pub source: Option<String>,
}

/// `AutoscalerStatusDetails` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AutoscalerStatusDetails {
    /// message property.
    pub message: Option<String>,
    /// type property.
    pub r#type: Option<String>,
}

/// `InstanceGroupManagerResourcePolicies` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct InstanceGroupManagerResourcePolicies {
    /// workloadPolicy property.
    pub workload_policy: Option<String>,
}

/// `NetworkPeering` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct NetworkPeering {
    /// autoCreateRoutes property.
    pub auto_create_routes: Option<bool>,
    /// connectionStatus property.
    pub connection_status: Option<NetworkPeeringConnectionStatus>,
    /// exchangeSubnetRoutes property.
    pub exchange_subnet_routes: Option<bool>,
    /// exportCustomRoutes property.
    pub export_custom_routes: Option<bool>,
    /// exportSubnetRoutesWithPublicIp property.
    pub export_subnet_routes_with_public_ip: Option<bool>,
    /// importCustomRoutes property.
    pub import_custom_routes: Option<bool>,
    /// importSubnetRoutesWithPublicIp property.
    pub import_subnet_routes_with_public_ip: Option<bool>,
    /// name property.
    pub name: Option<String>,
    /// network property.
    pub network: Option<String>,
    /// peerMtu property.
    pub peer_mtu: Option<i64>,
    /// stackType property.
    pub stack_type: Option<String>,
    /// state property.
    pub state: Option<String>,
    /// stateDetails property.
    pub state_details: Option<String>,
    /// updateStrategy property.
    pub update_strategy: Option<String>,
}

/// `DiskInstantiationConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct DiskInstantiationConfig {
    /// autoDelete property.
    pub auto_delete: Option<bool>,
    /// customImage property.
    pub custom_image: Option<String>,
    /// deviceName property.
    pub device_name: Option<String>,
    /// instantiateFrom property.
    pub instantiate_from: Option<String>,
}

/// `BackendServiceFailoverPolicy` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct BackendServiceFailoverPolicy {
    /// disableConnectionDrainOnFailover property.
    pub disable_connection_drain_on_failover: Option<bool>,
    /// dropTrafficIfUnhealthy property.
    pub drop_traffic_if_unhealthy: Option<bool>,
    /// failoverRatio property.
    pub failover_ratio: Option<f64>,
}

/// `TargetGrpcProxy` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct TargetGrpcProxy {
    /// creationTimestamp property.
    pub creation_timestamp: Option<String>,
    /// description property.
    pub description: Option<String>,
    /// fingerprint property.
    pub fingerprint: Option<String>,
    /// id property.
    pub id: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// selfLinkWithId property.
    pub self_link_with_id: Option<String>,
    /// urlMap property.
    pub url_map: Option<String>,
    /// validateForProxyless property.
    pub validate_for_proxyless: Option<bool>,
}

/// `InstanceGroupManagerResizeRequestStatus` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct InstanceGroupManagerResizeRequestStatus {
    /// error property.
    pub error: Option<std::collections::HashMap<String, serde_json::Value>>,
    /// lastAttempt property.
    pub last_attempt: Option<InstanceGroupManagerResizeRequestStatusLastAttempt>,
}

/// `BackendBucketCdnPolicy` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct BackendBucketCdnPolicy {
    /// bypassCacheOnRequestHeaders property.
    pub bypass_cache_on_request_headers:
        Option<Vec<BackendBucketCdnPolicyBypassCacheOnRequestHeader>>,
    /// cacheKeyPolicy property.
    pub cache_key_policy: Option<BackendBucketCdnPolicyCacheKeyPolicy>,
    /// cacheMode property.
    pub cache_mode: Option<String>,
    /// clientTtl property.
    pub client_ttl: Option<i64>,
    /// defaultTtl property.
    pub default_ttl: Option<i64>,
    /// maxTtl property.
    pub max_ttl: Option<i64>,
    /// negativeCaching property.
    pub negative_caching: Option<bool>,
    /// negativeCachingPolicy property.
    pub negative_caching_policy: Option<Vec<BackendBucketCdnPolicyNegativeCachingPolicy>>,
    /// requestCoalescing property.
    pub request_coalescing: Option<bool>,
    /// serveWhileStale property.
    pub serve_while_stale: Option<i64>,
    /// signedUrlCacheMaxAgeSec property.
    pub signed_url_cache_max_age_sec: Option<String>,
    /// signedUrlKeyNames property.
    pub signed_url_key_names: Option<Vec<String>>,
}

/// `VmEndpointNatMappingsList` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct VmEndpointNatMappingsList {
    /// id property.
    pub id: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// result property.
    pub result: Option<Vec<VmEndpointNatMappings>>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// warning property.
    pub warning: Option<std::collections::HashMap<String, serde_json::Value>>,
}

/// `PreviewFeatureList` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct PreviewFeatureList {
    /// etag property.
    pub etag: Option<String>,
    /// id property.
    pub id: Option<String>,
    /// items property.
    pub items: Option<Vec<PreviewFeature>>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// unreachables property.
    pub unreachables: Option<Vec<String>>,
    /// warning property.
    pub warning: Option<std::collections::HashMap<String, serde_json::Value>>,
}

/// `AliasIpRange` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AliasIpRange {
    /// ipCidrRange property.
    pub ip_cidr_range: Option<String>,
    /// subnetworkRangeName property.
    pub subnetwork_range_name: Option<String>,
}

/// `TestPermissionsResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct TestPermissionsResponse {
    /// permissions property.
    pub permissions: Option<Vec<String>>,
}

/// `SecurityPolicyRulePreconfiguredWafConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SecurityPolicyRulePreconfiguredWafConfig {
    /// exclusions property.
    pub exclusions: Option<Vec<SecurityPolicyRulePreconfiguredWafConfigExclusion>>,
}

/// `InstanceManagedByIgmError` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct InstanceManagedByIgmError {
    /// error property.
    pub error: Option<InstanceManagedByIgmErrorManagedInstanceError>,
    /// instanceActionDetails property.
    pub instance_action_details: Option<InstanceManagedByIgmErrorInstanceActionDetails>,
    /// timestamp property.
    pub timestamp: Option<String>,
}

/// `PublicDelegatedPrefixList` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct PublicDelegatedPrefixList {
    /// id property.
    pub id: Option<String>,
    /// items property.
    pub items: Option<Vec<PublicDelegatedPrefix>>,
    /// kind property.
    pub kind: Option<String>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// warning property.
    pub warning: Option<std::collections::HashMap<String, serde_json::Value>>,
}

/// `InstanceGroupManagersCreateInstancesRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct InstanceGroupManagersCreateInstancesRequest {
    /// instances property.
    pub instances: Option<Vec<PerInstanceConfig>>,
}

/// `AutoscalingPolicyScaleInControl` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AutoscalingPolicyScaleInControl {
    /// maxScaledInReplicas property.
    pub max_scaled_in_replicas: Option<FixedOrPercent>,
    /// timeWindowSec property.
    pub time_window_sec: Option<i64>,
}

/// `InstancesSetMinCpuPlatformRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct InstancesSetMinCpuPlatformRequest {
    /// minCpuPlatform property.
    pub min_cpu_platform: Option<String>,
}

/// `InstanceGroupManagerStatusAcceleratorTopologyAcceleratorTopologyStateDetails` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct InstanceGroupManagerStatusAcceleratorTopologyAcceleratorTopologyStateDetails {
    /// error property.
    pub error: Option<std::collections::HashMap<String, serde_json::Value>>,
    /// timestamp property.
    pub timestamp: Option<String>,
}

/// `InstantSnapshotGroupResourceStatus` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct InstantSnapshotGroupResourceStatus {
    /// consistencyMembershipResolutionTime property.
    pub consistency_membership_resolution_time: Option<String>,
    /// sourceInfo property.
    pub source_info: Option<InstantSnapshotGroupSourceInfo>,
}

/// `InstanceMoveRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct InstanceMoveRequest {
    /// destinationZone property.
    pub destination_zone: Option<String>,
    /// targetInstance property.
    pub target_instance: Option<String>,
}

/// `RegionInstanceGroupManagersListErrorsResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct RegionInstanceGroupManagersListErrorsResponse {
    /// items property.
    pub items: Option<Vec<InstanceManagedByIgmError>>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
}

/// `NetworkEndpointGroupAppEngine` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct NetworkEndpointGroupAppEngine {
    /// service property.
    pub service: Option<String>,
    /// urlMask property.
    pub url_mask: Option<String>,
    /// version property.
    pub version: Option<String>,
}

/// `VpnGatewayStatus` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct VpnGatewayStatus {
    /// vpnConnections property.
    pub vpn_connections: Option<Vec<VpnGatewayStatusVpnConnection>>,
}

/// `BackendBucketCdnPolicyBypassCacheOnRequestHeader` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct BackendBucketCdnPolicyBypassCacheOnRequestHeader {
    /// headerName property.
    pub header_name: Option<String>,
}

/// `UrlMapTestHeader` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct UrlMapTestHeader {
    /// name property.
    pub name: Option<String>,
    /// value property.
    pub value: Option<String>,
}

/// `ResourcePolicyInstanceSchedulePolicySchedule` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ResourcePolicyInstanceSchedulePolicySchedule {
    /// schedule property.
    pub schedule: Option<String>,
}

/// `NetworkEdgeSecurityServiceAggregatedList` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct NetworkEdgeSecurityServiceAggregatedList {
    /// etag property.
    pub etag: Option<String>,
    /// id property.
    pub id: Option<String>,
    /// items property.
    pub items: Option<serde_json::Value>,
    /// kind property.
    pub kind: Option<String>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// unreachables property.
    pub unreachables: Option<Vec<String>>,
    /// warning property.
    pub warning: Option<std::collections::HashMap<String, serde_json::Value>>,
}

/// `RegionDisksAddResourcePoliciesRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct RegionDisksAddResourcePoliciesRequest {
    /// resourcePolicies property.
    pub resource_policies: Option<Vec<String>>,
}

/// `RouterAggregatedList` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct RouterAggregatedList {
    /// id property.
    pub id: Option<String>,
    /// items property.
    pub items: Option<serde_json::Value>,
    /// kind property.
    pub kind: Option<String>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// unreachables property.
    pub unreachables: Option<Vec<String>>,
    /// warning property.
    pub warning: Option<std::collections::HashMap<String, serde_json::Value>>,
}

/// `AddressAggregatedList` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AddressAggregatedList {
    /// id property.
    pub id: Option<String>,
    /// items property.
    pub items: Option<serde_json::Value>,
    /// kind property.
    pub kind: Option<String>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// unreachables property.
    pub unreachables: Option<Vec<String>>,
    /// warning property.
    pub warning: Option<std::collections::HashMap<String, serde_json::Value>>,
}

/// `SnapshotUpdateKmsKeyRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SnapshotUpdateKmsKeyRequest {
    /// kmsKeyName property.
    pub kms_key_name: Option<String>,
}

/// `GroupMaintenanceInfo` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GroupMaintenanceInfo {
    /// instanceMaintenanceOngoingCount property.
    pub instance_maintenance_ongoing_count: Option<i64>,
    /// instanceMaintenancePendingCount property.
    pub instance_maintenance_pending_count: Option<i64>,
    /// maintenanceOngoingCount property.
    pub maintenance_ongoing_count: Option<i64>,
    /// maintenancePendingCount property.
    pub maintenance_pending_count: Option<i64>,
    /// schedulingType property.
    pub scheduling_type: Option<String>,
    /// subblockInfraMaintenanceOngoingCount property.
    pub subblock_infra_maintenance_ongoing_count: Option<i64>,
    /// subblockInfraMaintenancePendingCount property.
    pub subblock_infra_maintenance_pending_count: Option<i64>,
    /// upcomingGroupMaintenance property.
    pub upcoming_group_maintenance: Option<UpcomingMaintenance>,
}

/// `NetworkEdgeSecurityService` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct NetworkEdgeSecurityService {
    /// creationTimestamp property.
    pub creation_timestamp: Option<String>,
    /// description property.
    pub description: Option<String>,
    /// fingerprint property.
    pub fingerprint: Option<String>,
    /// id property.
    pub id: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// region property.
    pub region: Option<String>,
    /// securityPolicy property.
    pub security_policy: Option<String>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// selfLinkWithId property.
    pub self_link_with_id: Option<String>,
}

/// `WireEndpoint` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct WireEndpoint {
    /// interconnect property.
    pub interconnect: Option<String>,
    /// vlanTag property.
    pub vlan_tag: Option<i64>,
}

/// `SecurityPolicyReference` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SecurityPolicyReference {
    /// securityPolicy property.
    pub security_policy: Option<String>,
}

/// `Subsetting` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Subsetting {
    /// policy property.
    pub policy: Option<String>,
}

/// `InstancesSetMachineResourcesRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct InstancesSetMachineResourcesRequest {
    /// guestAccelerators property.
    pub guest_accelerators: Option<Vec<AcceleratorConfig>>,
}

/// `CommitmentAggregatedList` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct CommitmentAggregatedList {
    /// id property.
    pub id: Option<String>,
    /// items property.
    pub items: Option<serde_json::Value>,
    /// kind property.
    pub kind: Option<String>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// unreachables property.
    pub unreachables: Option<Vec<String>>,
    /// warning property.
    pub warning: Option<std::collections::HashMap<String, serde_json::Value>>,
}

/// `RegionInstanceGroupManagerPatchInstanceConfigReq` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct RegionInstanceGroupManagerPatchInstanceConfigReq {
    /// perInstanceConfigs property.
    pub per_instance_configs: Option<Vec<PerInstanceConfig>>,
}

/// `TargetHttpsProxyAggregatedList` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct TargetHttpsProxyAggregatedList {
    /// id property.
    pub id: Option<String>,
    /// items property.
    pub items: Option<serde_json::Value>,
    /// kind property.
    pub kind: Option<String>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// unreachables property.
    pub unreachables: Option<Vec<String>>,
    /// warning property.
    pub warning: Option<std::collections::HashMap<String, serde_json::Value>>,
}

/// `FutureReservationsListResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct FutureReservationsListResponse {
    /// etag property.
    pub etag: Option<String>,
    /// id property.
    pub id: Option<String>,
    /// items property.
    pub items: Option<Vec<FutureReservation>>,
    /// kind property.
    pub kind: Option<String>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// unreachables property.
    pub unreachables: Option<Vec<String>>,
    /// warning property.
    pub warning: Option<std::collections::HashMap<String, serde_json::Value>>,
}

/// `ManagedInstanceLastAttempt` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ManagedInstanceLastAttempt {
    /// errors property.
    pub errors: Option<std::collections::HashMap<String, serde_json::Value>>,
}

/// `InterconnectAttachmentConfigurationConstraintsBgpPeerASNRange` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct InterconnectAttachmentConfigurationConstraintsBgpPeerASNRange {
    /// max property.
    pub max: Option<i64>,
    /// min property.
    pub min: Option<i64>,
}

/// `DiskParams` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct DiskParams {
    /// resourceManagerTags property.
    pub resource_manager_tags: Option<serde_json::Value>,
}

/// `TargetPoolsAddHealthCheckRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct TargetPoolsAddHealthCheckRequest {
    /// healthChecks property.
    pub health_checks: Option<Vec<HealthCheckReference>>,
}

/// `BackendServiceLogConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct BackendServiceLogConfig {
    /// enable property.
    pub enable: Option<bool>,
    /// optionalFields property.
    pub optional_fields: Option<Vec<String>>,
    /// optionalMode property.
    pub optional_mode: Option<String>,
    /// sampleRate property.
    pub sample_rate: Option<f64>,
}

/// `LocationPolicy` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct LocationPolicy {
    /// locations property.
    pub locations: Option<serde_json::Value>,
    /// targetShape property.
    pub target_shape: Option<String>,
    /// zones property.
    pub zones: Option<Vec<LocationPolicyZoneConfiguration>>,
}

/// `LocationPolicyZoneConfiguration` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct LocationPolicyZoneConfiguration {
    /// zone property.
    pub zone: Option<String>,
}

/// `ResourcePolicyDiskConsistencyGroupPolicy` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ResourcePolicyDiskConsistencyGroupPolicy {}

/// `TargetVpnGatewayList` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct TargetVpnGatewayList {
    /// id property.
    pub id: Option<String>,
    /// items property.
    pub items: Option<Vec<TargetVpnGateway>>,
    /// kind property.
    pub kind: Option<String>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// warning property.
    pub warning: Option<std::collections::HashMap<String, serde_json::Value>>,
}

/// `InterconnectAttachmentGroupLogicalStructureRegionMetro` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct InterconnectAttachmentGroupLogicalStructureRegionMetro {
    /// facilities property.
    pub facilities: Option<Vec<InterconnectAttachmentGroupLogicalStructureRegionMetroFacility>>,
    /// metro property.
    pub metro: Option<String>,
}

/// `RouterNatSubnetworkToNat` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct RouterNatSubnetworkToNat {
    /// name property.
    pub name: Option<String>,
    /// secondaryIpRangeNames property.
    pub secondary_ip_range_names: Option<Vec<String>>,
    /// sourceIpRangesToNat property.
    pub source_ip_ranges_to_nat: Option<Vec<String>>,
}

/// `NodeGroupsAddNodesRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct NodeGroupsAddNodesRequest {
    /// additionalNodeCount property.
    pub additional_node_count: Option<i64>,
}

/// `RegionInstanceGroupManagersApplyUpdatesRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct RegionInstanceGroupManagersApplyUpdatesRequest {
    /// allInstances property.
    pub all_instances: Option<bool>,
    /// instances property.
    pub instances: Option<Vec<String>>,
    /// minimalAction property.
    pub minimal_action: Option<String>,
    /// mostDisruptiveAllowedAction property.
    pub most_disruptive_allowed_action: Option<String>,
}

/// `AllocationSpecificSKUAllocationReservedInstanceProperties` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AllocationSpecificSKUAllocationReservedInstanceProperties {
    /// guestAccelerators property.
    pub guest_accelerators: Option<Vec<AcceleratorConfig>>,
    /// localSsds property.
    pub local_ssds:
        Option<Vec<AllocationSpecificSKUAllocationAllocatedInstancePropertiesReservedDisk>>,
    /// locationHint property.
    pub location_hint: Option<String>,
    /// machineType property.
    pub machine_type: Option<String>,
    /// minCpuPlatform property.
    pub min_cpu_platform: Option<String>,
}

/// `BackendServiceReference` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct BackendServiceReference {
    /// backendService property.
    pub backend_service: Option<String>,
}

/// `InstanceGroupManagerAggregatedList` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct InstanceGroupManagerAggregatedList {
    /// id property.
    pub id: Option<String>,
    /// items property.
    pub items: Option<serde_json::Value>,
    /// kind property.
    pub kind: Option<String>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// unreachables property.
    pub unreachables: Option<Vec<String>>,
    /// warning property.
    pub warning: Option<std::collections::HashMap<String, serde_json::Value>>,
}

/// `ResourcePolicySnapshotSchedulePolicy` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ResourcePolicySnapshotSchedulePolicy {
    /// retentionPolicy property.
    pub retention_policy: Option<ResourcePolicySnapshotSchedulePolicyRetentionPolicy>,
    /// schedule property.
    pub schedule: Option<ResourcePolicySnapshotSchedulePolicySchedule>,
    /// snapshotProperties property.
    pub snapshot_properties: Option<ResourcePolicySnapshotSchedulePolicySnapshotProperties>,
}

/// `InterconnectGroupConfigured` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct InterconnectGroupConfigured {
    /// topologyCapability property.
    pub topology_capability: Option<InterconnectGroupConfiguredTopologyCapability>,
}

/// `InstanceGroupManagerStandbyPolicy` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct InstanceGroupManagerStandbyPolicy {
    /// initialDelaySec property.
    pub initial_delay_sec: Option<i64>,
    /// mode property.
    pub mode: Option<String>,
}

/// `SourceInstanceParams` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SourceInstanceParams {
    /// diskConfigs property.
    pub disk_configs: Option<Vec<DiskInstantiationConfig>>,
}

/// `NetworkProfile` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct NetworkProfile {
    /// creationTimestamp property.
    pub creation_timestamp: Option<String>,
    /// description property.
    pub description: Option<String>,
    /// features property.
    pub features: Option<NetworkProfileNetworkFeatures>,
    /// id property.
    pub id: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// location property.
    pub location: Option<NetworkProfileLocation>,
    /// name property.
    pub name: Option<String>,
    /// profileType property.
    pub profile_type: Option<NetworkProfileProfileType>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// selfLinkWithId property.
    pub self_link_with_id: Option<String>,
}

/// `SourceDiskEncryptionKey` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SourceDiskEncryptionKey {
    /// diskEncryptionKey property.
    pub disk_encryption_key: Option<CustomerEncryptionKey>,
    /// sourceDisk property.
    pub source_disk: Option<String>,
}

/// `RegionNetworkEndpointGroupsDetachEndpointsRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct RegionNetworkEndpointGroupsDetachEndpointsRequest {
    /// networkEndpoints property.
    pub network_endpoints: Option<Vec<NetworkEndpoint>>,
}

/// `NetworkEndpoint` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct NetworkEndpoint {
    /// annotations property.
    pub annotations: Option<serde_json::Value>,
    /// clientDestinationPort property.
    pub client_destination_port: Option<i64>,
    /// fqdn property.
    pub fqdn: Option<String>,
    /// instance property.
    pub instance: Option<String>,
    /// ipAddress property.
    pub ip_address: Option<String>,
    /// ipv6Address property.
    pub ipv6_address: Option<String>,
    /// port property.
    pub port: Option<i64>,
}

/// `SecurityPolicyRuleNetworkMatcher` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SecurityPolicyRuleNetworkMatcher {
    /// destIpRanges property.
    pub dest_ip_ranges: Option<Vec<String>>,
    /// destPorts property.
    pub dest_ports: Option<Vec<String>>,
    /// ipProtocols property.
    pub ip_protocols: Option<Vec<String>>,
    /// srcAsns property.
    pub src_asns: Option<Vec<i64>>,
    /// srcIpRanges property.
    pub src_ip_ranges: Option<Vec<String>>,
    /// srcPorts property.
    pub src_ports: Option<Vec<String>>,
    /// srcRegionCodes property.
    pub src_region_codes: Option<Vec<String>>,
    /// userDefinedFields property.
    pub user_defined_fields: Option<Vec<SecurityPolicyRuleNetworkMatcherUserDefinedFieldMatch>>,
}

/// `AcceleratorType` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AcceleratorType {
    /// creationTimestamp property.
    pub creation_timestamp: Option<String>,
    /// deprecated property.
    pub deprecated: Option<DeprecationStatus>,
    /// description property.
    pub description: Option<String>,
    /// id property.
    pub id: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// maximumCardsPerInstance property.
    pub maximum_cards_per_instance: Option<i64>,
    /// name property.
    pub name: Option<String>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// zone property.
    pub zone: Option<String>,
}

/// `HealthSourceAggregatedList` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct HealthSourceAggregatedList {
    /// id property.
    pub id: Option<String>,
    /// items property.
    pub items: Option<serde_json::Value>,
    /// kind property.
    pub kind: Option<String>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// unreachables property.
    pub unreachables: Option<Vec<String>>,
    /// warning property.
    pub warning: Option<std::collections::HashMap<String, serde_json::Value>>,
}

/// `InterconnectAttachmentGroupsListResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct InterconnectAttachmentGroupsListResponse {
    /// etag property.
    pub etag: Option<String>,
    /// id property.
    pub id: Option<String>,
    /// items property.
    pub items: Option<Vec<InterconnectAttachmentGroup>>,
    /// kind property.
    pub kind: Option<String>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// unreachables property.
    pub unreachables: Option<Vec<String>>,
    /// warning property.
    pub warning: Option<std::collections::HashMap<String, serde_json::Value>>,
}

/// `ProjectsDisableXpnResourceRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ProjectsDisableXpnResourceRequest {
    /// xpnResource property.
    pub xpn_resource: Option<XpnResourceId>,
}

/// `HealthSourceHealth` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct HealthSourceHealth {
    /// healthState property.
    pub health_state: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// sources property.
    pub sources: Option<Vec<HealthSourcesGetHealthResponseSourceInfo>>,
}

/// `RegionNetworkEndpointGroupsAttachEndpointsRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct RegionNetworkEndpointGroupsAttachEndpointsRequest {
    /// networkEndpoints property.
    pub network_endpoints: Option<Vec<NetworkEndpoint>>,
}

/// `AdvancedMachineFeatures` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AdvancedMachineFeatures {
    /// enableNestedVirtualization property.
    pub enable_nested_virtualization: Option<bool>,
    /// enableUefiNetworking property.
    pub enable_uefi_networking: Option<bool>,
    /// performanceMonitoringUnit property.
    pub performance_monitoring_unit: Option<String>,
    /// threadsPerCore property.
    pub threads_per_core: Option<i64>,
    /// turboMode property.
    pub turbo_mode: Option<String>,
    /// visibleCoreCount property.
    pub visible_core_count: Option<i64>,
}

/// `InstantSnapshot` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct InstantSnapshot {
    /// architecture property.
    pub architecture: Option<String>,
    /// creationTimestamp property.
    pub creation_timestamp: Option<String>,
    /// description property.
    pub description: Option<String>,
    /// diskSizeGb property.
    pub disk_size_gb: Option<String>,
    /// id property.
    pub id: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// labelFingerprint property.
    pub label_fingerprint: Option<String>,
    /// labels property.
    pub labels: Option<serde_json::Value>,
    /// name property.
    pub name: Option<String>,
    /// params property.
    pub params: Option<InstantSnapshotParams>,
    /// region property.
    pub region: Option<String>,
    /// resourceStatus property.
    pub resource_status: Option<InstantSnapshotResourceStatus>,
    /// satisfiesPzi property.
    pub satisfies_pzi: Option<bool>,
    /// satisfiesPzs property.
    pub satisfies_pzs: Option<bool>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// selfLinkWithId property.
    pub self_link_with_id: Option<String>,
    /// sourceDisk property.
    pub source_disk: Option<String>,
    /// sourceDiskId property.
    pub source_disk_id: Option<String>,
    /// sourceInstantSnapshotGroup property.
    pub source_instant_snapshot_group: Option<String>,
    /// sourceInstantSnapshotGroupId property.
    pub source_instant_snapshot_group_id: Option<String>,
    /// status property.
    pub status: Option<String>,
    /// zone property.
    pub zone: Option<String>,
}

/// `BulkInsertDiskResource` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct BulkInsertDiskResource {
    /// instantSnapshotGroupParameters property.
    pub instant_snapshot_group_parameters: Option<InstantSnapshotGroupParameters>,
    /// snapshotGroupParameters property.
    pub snapshot_group_parameters: Option<SnapshotGroupParameters>,
    /// sourceConsistencyGroupPolicy property.
    pub source_consistency_group_policy: Option<String>,
}

/// `VpnTunnelAggregatedList` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct VpnTunnelAggregatedList {
    /// id property.
    pub id: Option<String>,
    /// items property.
    pub items: Option<serde_json::Value>,
    /// kind property.
    pub kind: Option<String>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// unreachables property.
    pub unreachables: Option<Vec<String>>,
    /// warning property.
    pub warning: Option<std::collections::HashMap<String, serde_json::Value>>,
}

/// `ReservationBlockHealthInfo` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ReservationBlockHealthInfo {
    /// degradedSubBlockCount property.
    pub degraded_sub_block_count: Option<i64>,
    /// healthStatus property.
    pub health_status: Option<String>,
    /// healthySubBlockCount property.
    pub healthy_sub_block_count: Option<i64>,
}

/// `MetadataFilter` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct MetadataFilter {
    /// filterLabels property.
    pub filter_labels: Option<Vec<MetadataFilterLabelMatch>>,
    /// filterMatchCriteria property.
    pub filter_match_criteria: Option<String>,
}

/// `SecurityPolicyAdvancedOptionsConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SecurityPolicyAdvancedOptionsConfig {
    /// jsonCustomConfig property.
    pub json_custom_config: Option<SecurityPolicyAdvancedOptionsConfigJsonCustomConfig>,
    /// jsonParsing property.
    pub json_parsing: Option<String>,
    /// logLevel property.
    pub log_level: Option<String>,
    /// requestBodyInspectionSize property.
    pub request_body_inspection_size: Option<String>,
    /// userIpRequestHeaders property.
    pub user_ip_request_headers: Option<Vec<String>>,
}

/// `InstantSnapshotResourceStatus` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct InstantSnapshotResourceStatus {
    /// storageSizeBytes property.
    pub storage_size_bytes: Option<String>,
}

/// `InstanceGroupManagerInstanceLifecyclePolicy` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct InstanceGroupManagerInstanceLifecyclePolicy {
    /// defaultActionOnFailure property.
    pub default_action_on_failure: Option<String>,
    /// forceUpdateOnRepair property.
    pub force_update_on_repair: Option<String>,
    /// onFailedHealthCheck property.
    pub on_failed_health_check: Option<String>,
}

/// `InterconnectAttachmentList` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct InterconnectAttachmentList {
    /// id property.
    pub id: Option<String>,
    /// items property.
    pub items: Option<Vec<InterconnectAttachment>>,
    /// kind property.
    pub kind: Option<String>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// warning property.
    pub warning: Option<std::collections::HashMap<String, serde_json::Value>>,
}

/// `InstanceGroupsListInstances` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct InstanceGroupsListInstances {
    /// id property.
    pub id: Option<String>,
    /// items property.
    pub items: Option<Vec<InstanceWithNamedPorts>>,
    /// kind property.
    pub kind: Option<String>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// warning property.
    pub warning: Option<std::collections::HashMap<String, serde_json::Value>>,
}

/// `RegionDiskUpdateKmsKeyRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct RegionDiskUpdateKmsKeyRequest {
    /// kmsKeyName property.
    pub kms_key_name: Option<String>,
}

/// `ResourcePolicyResourceStatusInstanceSchedulePolicyStatus` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ResourcePolicyResourceStatusInstanceSchedulePolicyStatus {
    /// lastRunStartTime property.
    pub last_run_start_time: Option<String>,
    /// nextRunStartTime property.
    pub next_run_start_time: Option<String>,
}

/// `InterconnectApplicationAwareInterconnectStrictPriorityPolicy` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct InterconnectApplicationAwareInterconnectStrictPriorityPolicy {}

/// `CustomerEncryptionKey` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct CustomerEncryptionKey {
    /// kmsKeyName property.
    pub kms_key_name: Option<String>,
    /// kmsKeyServiceAccount property.
    pub kms_key_service_account: Option<String>,
    /// rawKey property.
    pub raw_key: Option<String>,
    /// rsaEncryptedKey property.
    pub rsa_encrypted_key: Option<String>,
    /// sha256 property.
    pub sha256: Option<String>,
}

/// `NetworkEndpointGroupAggregatedList` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct NetworkEndpointGroupAggregatedList {
    /// id property.
    pub id: Option<String>,
    /// items property.
    pub items: Option<serde_json::Value>,
    /// kind property.
    pub kind: Option<String>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// unreachables property.
    pub unreachables: Option<Vec<String>>,
    /// warning property.
    pub warning: Option<std::collections::HashMap<String, serde_json::Value>>,
}

/// `SecurityPolicyRulePreconfiguredWafConfigExclusion` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SecurityPolicyRulePreconfiguredWafConfigExclusion {
    /// requestCookiesToExclude property.
    pub request_cookies_to_exclude:
        Option<Vec<SecurityPolicyRulePreconfiguredWafConfigExclusionFieldParams>>,
    /// requestHeadersToExclude property.
    pub request_headers_to_exclude:
        Option<Vec<SecurityPolicyRulePreconfiguredWafConfigExclusionFieldParams>>,
    /// requestQueryParamsToExclude property.
    pub request_query_params_to_exclude:
        Option<Vec<SecurityPolicyRulePreconfiguredWafConfigExclusionFieldParams>>,
    /// requestUrisToExclude property.
    pub request_uris_to_exclude:
        Option<Vec<SecurityPolicyRulePreconfiguredWafConfigExclusionFieldParams>>,
    /// targetRuleIds property.
    pub target_rule_ids: Option<Vec<String>>,
    /// targetRuleSet property.
    pub target_rule_set: Option<String>,
}

/// `Route` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Route {
    /// asPaths property.
    pub as_paths: Option<Vec<RouteAsPath>>,
    /// creationTimestamp property.
    pub creation_timestamp: Option<String>,
    /// description property.
    pub description: Option<String>,
    /// destRange property.
    pub dest_range: Option<String>,
    /// id property.
    pub id: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// network property.
    pub network: Option<String>,
    /// nextHopGateway property.
    pub next_hop_gateway: Option<String>,
    /// nextHopHub property.
    pub next_hop_hub: Option<String>,
    /// nextHopIlb property.
    pub next_hop_ilb: Option<String>,
    /// nextHopInstance property.
    pub next_hop_instance: Option<String>,
    /// nextHopInterRegionCost property.
    pub next_hop_inter_region_cost: Option<i64>,
    /// nextHopInterconnectAttachment property.
    pub next_hop_interconnect_attachment: Option<String>,
    /// nextHopIp property.
    pub next_hop_ip: Option<String>,
    /// nextHopMed property.
    pub next_hop_med: Option<i64>,
    /// nextHopNetwork property.
    pub next_hop_network: Option<String>,
    /// nextHopOrigin property.
    pub next_hop_origin: Option<String>,
    /// nextHopPeering property.
    pub next_hop_peering: Option<String>,
    /// nextHopVpnTunnel property.
    pub next_hop_vpn_tunnel: Option<String>,
    /// params property.
    pub params: Option<RouteParams>,
    /// priority property.
    pub priority: Option<i64>,
    /// routeStatus property.
    pub route_status: Option<String>,
    /// routeType property.
    pub route_type: Option<String>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// tags property.
    pub tags: Option<Vec<String>>,
    /// warnings property.
    pub warnings: Option<Vec<std::collections::HashMap<String, serde_json::Value>>>,
}

/// `NetworkRoutingConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct NetworkRoutingConfig {
    /// bgpAlwaysCompareMed property.
    pub bgp_always_compare_med: Option<bool>,
    /// bgpBestPathSelectionMode property.
    pub bgp_best_path_selection_mode: Option<String>,
    /// bgpInterRegionCost property.
    pub bgp_inter_region_cost: Option<String>,
    /// effectiveBgpAlwaysCompareMed property.
    pub effective_bgp_always_compare_med: Option<bool>,
    /// effectiveBgpInterRegionCost property.
    pub effective_bgp_inter_region_cost: Option<String>,
    /// routingMode property.
    pub routing_mode: Option<String>,
}

/// `XpnHostList` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct XpnHostList {
    /// id property.
    pub id: Option<String>,
    /// items property.
    pub items: Option<Vec<Project>>,
    /// kind property.
    pub kind: Option<String>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// warning property.
    pub warning: Option<std::collections::HashMap<String, serde_json::Value>>,
}

/// `BfdPacket` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct BfdPacket {
    /// authenticationPresent property.
    pub authentication_present: Option<bool>,
    /// controlPlaneIndependent property.
    pub control_plane_independent: Option<bool>,
    /// demand property.
    pub demand: Option<bool>,
    /// diagnostic property.
    pub diagnostic: Option<String>,
    /// final property.
    pub r#final: Option<bool>,
    /// length property.
    pub length: Option<i64>,
    /// minEchoRxIntervalMs property.
    pub min_echo_rx_interval_ms: Option<i64>,
    /// minRxIntervalMs property.
    pub min_rx_interval_ms: Option<i64>,
    /// minTxIntervalMs property.
    pub min_tx_interval_ms: Option<i64>,
    /// multiplier property.
    pub multiplier: Option<i64>,
    /// multipoint property.
    pub multipoint: Option<bool>,
    /// myDiscriminator property.
    pub my_discriminator: Option<i64>,
    /// poll property.
    pub poll: Option<bool>,
    /// state property.
    pub state: Option<String>,
    /// version property.
    pub version: Option<i64>,
    /// yourDiscriminator property.
    pub your_discriminator: Option<i64>,
}

/// `NodeTypeAggregatedList` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct NodeTypeAggregatedList {
    /// id property.
    pub id: Option<String>,
    /// items property.
    pub items: Option<serde_json::Value>,
    /// kind property.
    pub kind: Option<String>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// unreachables property.
    pub unreachables: Option<Vec<String>>,
    /// warning property.
    pub warning: Option<std::collections::HashMap<String, serde_json::Value>>,
}

/// `FutureReservationParams` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct FutureReservationParams {
    /// resourceManagerTags property.
    pub resource_manager_tags: Option<serde_json::Value>,
}

/// `ResourceStatusPhysicalHostTopology` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ResourceStatusPhysicalHostTopology {
    /// block property.
    pub block: Option<String>,
    /// cluster property.
    pub cluster: Option<String>,
    /// host property.
    pub host: Option<String>,
    /// subblock property.
    pub subblock: Option<String>,
}

/// `ExternalVpnGatewayList` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ExternalVpnGatewayList {
    /// etag property.
    pub etag: Option<String>,
    /// id property.
    pub id: Option<String>,
    /// items property.
    pub items: Option<Vec<ExternalVpnGateway>>,
    /// kind property.
    pub kind: Option<String>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// warning property.
    pub warning: Option<std::collections::HashMap<String, serde_json::Value>>,
}

/// `UrlMapList` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct UrlMapList {
    /// id property.
    pub id: Option<String>,
    /// items property.
    pub items: Option<Vec<UrlMap>>,
    /// kind property.
    pub kind: Option<String>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// warning property.
    pub warning: Option<std::collections::HashMap<String, serde_json::Value>>,
}

/// `SerialPortOutput` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SerialPortOutput {
    /// contents property.
    pub contents: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// next property.
    pub next: Option<String>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// start property.
    pub start: Option<String>,
}

/// `TargetHttpsProxiesSetQuicOverrideRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct TargetHttpsProxiesSetQuicOverrideRequest {
    /// quicOverride property.
    pub quic_override: Option<String>,
}

/// `InterconnectAttachmentPartnerMetadata` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct InterconnectAttachmentPartnerMetadata {
    /// interconnectName property.
    pub interconnect_name: Option<String>,
    /// partnerName property.
    pub partner_name: Option<String>,
    /// portalUrl property.
    pub portal_url: Option<String>,
}

/// `AcceleratorTypeList` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AcceleratorTypeList {
    /// id property.
    pub id: Option<String>,
    /// items property.
    pub items: Option<Vec<AcceleratorType>>,
    /// kind property.
    pub kind: Option<String>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// warning property.
    pub warning: Option<std::collections::HashMap<String, serde_json::Value>>,
}

/// `RegionNetworkFirewallPoliciesGetEffectiveFirewallsResponseEffectiveFirewallPolicy` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct RegionNetworkFirewallPoliciesGetEffectiveFirewallsResponseEffectiveFirewallPolicy {
    /// displayName property.
    pub display_name: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// packetMirroringRules property.
    pub packet_mirroring_rules: Option<Vec<FirewallPolicyRule>>,
    /// priority property.
    pub priority: Option<i64>,
    /// rules property.
    pub rules: Option<Vec<FirewallPolicyRule>>,
    /// type property.
    pub r#type: Option<String>,
}

/// `InstanceGroupManagerStatusVersionTarget` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct InstanceGroupManagerStatusVersionTarget {
    /// isReached property.
    pub is_reached: Option<bool>,
}

/// `TargetSslProxy` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct TargetSslProxy {
    /// certificateMap property.
    pub certificate_map: Option<String>,
    /// creationTimestamp property.
    pub creation_timestamp: Option<String>,
    /// description property.
    pub description: Option<String>,
    /// id property.
    pub id: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// proxyHeader property.
    pub proxy_header: Option<String>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// service property.
    pub service: Option<String>,
    /// sslCertificates property.
    pub ssl_certificates: Option<Vec<String>>,
    /// sslPolicy property.
    pub ssl_policy: Option<String>,
}

/// `NetworksGetEffectiveFirewallsResponseEffectiveFirewallPolicy` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct NetworksGetEffectiveFirewallsResponseEffectiveFirewallPolicy {
    /// displayName property.
    pub display_name: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// packetMirroringRules property.
    pub packet_mirroring_rules: Option<Vec<FirewallPolicyRule>>,
    /// priority property.
    pub priority: Option<i64>,
    /// rules property.
    pub rules: Option<Vec<FirewallPolicyRule>>,
    /// shortName property.
    pub short_name: Option<String>,
    /// type property.
    pub r#type: Option<String>,
}

/// `FutureReservationStatusLastKnownGoodStateFutureReservationSpecs` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct FutureReservationStatusLastKnownGoodStateFutureReservationSpecs {
    /// shareSettings property.
    pub share_settings: Option<ShareSettings>,
    /// specificSkuProperties property.
    pub specific_sku_properties: Option<FutureReservationSpecificSKUProperties>,
    /// timeWindow property.
    pub time_window: Option<FutureReservationTimeWindow>,
}

/// `InstanceGroupManagersListErrorsResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct InstanceGroupManagersListErrorsResponse {
    /// items property.
    pub items: Option<Vec<InstanceManagedByIgmError>>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
}

/// `ResourcePolicyAggregatedList` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ResourcePolicyAggregatedList {
    /// etag property.
    pub etag: Option<String>,
    /// id property.
    pub id: Option<String>,
    /// items property.
    pub items: Option<serde_json::Value>,
    /// kind property.
    pub kind: Option<String>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// unreachables property.
    pub unreachables: Option<Vec<String>>,
    /// warning property.
    pub warning: Option<std::collections::HashMap<String, serde_json::Value>>,
}

/// `TargetInstance` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct TargetInstance {
    /// creationTimestamp property.
    pub creation_timestamp: Option<String>,
    /// description property.
    pub description: Option<String>,
    /// id property.
    pub id: Option<String>,
    /// instance property.
    pub instance: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// natPolicy property.
    pub nat_policy: Option<String>,
    /// network property.
    pub network: Option<String>,
    /// securityPolicy property.
    pub security_policy: Option<String>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// zone property.
    pub zone: Option<String>,
}

/// `RegionAutoscalerList` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct RegionAutoscalerList {
    /// id property.
    pub id: Option<String>,
    /// items property.
    pub items: Option<Vec<Autoscaler>>,
    /// kind property.
    pub kind: Option<String>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// warning property.
    pub warning: Option<std::collections::HashMap<String, serde_json::Value>>,
}

/// `ShareSettings` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ShareSettings {
    /// projectMap property.
    pub project_map: Option<serde_json::Value>,
    /// shareType property.
    pub share_type: Option<String>,
}

/// `HealthCheckReference` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct HealthCheckReference {
    /// healthCheck property.
    pub health_check: Option<String>,
}

/// `Policy` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Policy {
    /// auditConfigs property.
    pub audit_configs: Option<Vec<AuditConfig>>,
    /// bindings property.
    pub bindings: Option<Vec<Binding>>,
    /// etag property.
    pub etag: Option<String>,
    /// version property.
    pub version: Option<i64>,
}

/// `VmExtensionPolicyList` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct VmExtensionPolicyList {
    /// etag property.
    pub etag: Option<String>,
    /// id property.
    pub id: Option<String>,
    /// items property.
    pub items: Option<Vec<VmExtensionPolicy>>,
    /// kind property.
    pub kind: Option<String>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// unreachables property.
    pub unreachables: Option<Vec<String>>,
    /// warning property.
    pub warning: Option<std::collections::HashMap<String, serde_json::Value>>,
}

/// `ProjectsEnableXpnResourceRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ProjectsEnableXpnResourceRequest {
    /// xpnResource property.
    pub xpn_resource: Option<XpnResourceId>,
}

/// `TargetHttpProxyAggregatedList` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct TargetHttpProxyAggregatedList {
    /// id property.
    pub id: Option<String>,
    /// items property.
    pub items: Option<serde_json::Value>,
    /// kind property.
    pub kind: Option<String>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// unreachables property.
    pub unreachables: Option<Vec<String>>,
}

/// `UrlMapsAggregatedList` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct UrlMapsAggregatedList {
    /// id property.
    pub id: Option<String>,
    /// items property.
    pub items: Option<serde_json::Value>,
    /// kind property.
    pub kind: Option<String>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// unreachables property.
    pub unreachables: Option<Vec<String>>,
    /// warning property.
    pub warning: Option<std::collections::HashMap<String, serde_json::Value>>,
}

/// `HealthCheckLogConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct HealthCheckLogConfig {
    /// enable property.
    pub enable: Option<bool>,
}

/// `VpnTunnelParams` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct VpnTunnelParams {
    /// resourceManagerTags property.
    pub resource_manager_tags: Option<serde_json::Value>,
}

/// `UrlMapTest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct UrlMapTest {
    /// description property.
    pub description: Option<String>,
    /// expectedOutputUrl property.
    pub expected_output_url: Option<String>,
    /// expectedRedirectResponseCode property.
    pub expected_redirect_response_code: Option<i64>,
    /// headers property.
    pub headers: Option<Vec<UrlMapTestHeader>>,
    /// host property.
    pub host: Option<String>,
    /// path property.
    pub path: Option<String>,
    /// service property.
    pub service: Option<String>,
}

/// `DiskList` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct DiskList {
    /// id property.
    pub id: Option<String>,
    /// items property.
    pub items: Option<Vec<Disk>>,
    /// kind property.
    pub kind: Option<String>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// warning property.
    pub warning: Option<std::collections::HashMap<String, serde_json::Value>>,
}

/// `VmExtensionPolicyInstanceSelector` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct VmExtensionPolicyInstanceSelector {
    /// labelSelector property.
    pub label_selector: Option<VmExtensionPolicyLabelSelector>,
}

/// `HealthStatus` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct HealthStatus {
    /// annotations property.
    pub annotations: Option<serde_json::Value>,
    /// forwardingRule property.
    pub forwarding_rule: Option<String>,
    /// forwardingRuleIp property.
    pub forwarding_rule_ip: Option<String>,
    /// healthState property.
    pub health_state: Option<String>,
    /// instance property.
    pub instance: Option<String>,
    /// ipAddress property.
    pub ip_address: Option<String>,
    /// ipv6Address property.
    pub ipv6_address: Option<String>,
    /// ipv6HealthState property.
    pub ipv6_health_state: Option<String>,
    /// port property.
    pub port: Option<i64>,
    /// weight property.
    pub weight: Option<String>,
    /// weightError property.
    pub weight_error: Option<String>,
}

/// `NetworkEndpointGroupCloudFunction` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct NetworkEndpointGroupCloudFunction {
    /// function property.
    pub function: Option<String>,
    /// urlMask property.
    pub url_mask: Option<String>,
}

/// `ManagedInstancePropertiesFromFlexibilityPolicy` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ManagedInstancePropertiesFromFlexibilityPolicy {
    /// machineType property.
    pub machine_type: Option<String>,
}

/// `FutureReservationStatusExistingMatchingUsageInfo` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct FutureReservationStatusExistingMatchingUsageInfo {
    /// count property.
    pub count: Option<String>,
    /// timestamp property.
    pub timestamp: Option<String>,
}

/// `CalendarModeAdviceResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct CalendarModeAdviceResponse {
    /// recommendations property.
    pub recommendations: Option<Vec<CalendarModeRecommendation>>,
}

/// `ResourceStatusScheduling` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ResourceStatusScheduling {
    /// availabilityDomain property.
    pub availability_domain: Option<i64>,
}

/// `InterconnectAttachmentAggregatedList` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct InterconnectAttachmentAggregatedList {
    /// id property.
    pub id: Option<String>,
    /// items property.
    pub items: Option<serde_json::Value>,
    /// kind property.
    pub kind: Option<String>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// unreachables property.
    pub unreachables: Option<Vec<String>>,
    /// warning property.
    pub warning: Option<std::collections::HashMap<String, serde_json::Value>>,
}

/// `InterconnectMacsecPreSharedKey` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct InterconnectMacsecPreSharedKey {
    /// name property.
    pub name: Option<String>,
    /// startTime property.
    pub start_time: Option<String>,
}

/// `SecurityPolicyAdvancedOptionsConfigJsonCustomConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SecurityPolicyAdvancedOptionsConfigJsonCustomConfig {
    /// contentTypes property.
    pub content_types: Option<Vec<String>>,
}

/// `NetworkParams` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct NetworkParams {
    /// resourceManagerTags property.
    pub resource_manager_tags: Option<serde_json::Value>,
}

/// `BgpRoute` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct BgpRoute {
    /// asPaths property.
    pub as_paths: Option<Vec<BgpRouteAsPath>>,
    /// communities property.
    pub communities: Option<Vec<String>>,
    /// destination property.
    pub destination: Option<BgpRouteNetworkLayerReachabilityInformation>,
    /// med property.
    pub med: Option<i64>,
    /// origin property.
    pub origin: Option<String>,
}

/// `PreservedState` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct PreservedState {
    /// disks property.
    pub disks: Option<serde_json::Value>,
    /// externalIPs property.
    pub external_i_ps: Option<serde_json::Value>,
    /// internalIPs property.
    pub internal_i_ps: Option<serde_json::Value>,
    /// metadata property.
    pub metadata: Option<serde_json::Value>,
}

/// `ConsistentHashLoadBalancerSettingsHttpCookie` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ConsistentHashLoadBalancerSettingsHttpCookie {
    /// name property.
    pub name: Option<String>,
    /// path property.
    pub path: Option<String>,
    /// ttl property.
    pub ttl: Option<Duration>,
}

/// `PublicDelegatedPrefix` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct PublicDelegatedPrefix {
    /// allocatablePrefixLength property.
    pub allocatable_prefix_length: Option<i64>,
    /// byoipApiVersion property.
    pub byoip_api_version: Option<String>,
    /// creationTimestamp property.
    pub creation_timestamp: Option<String>,
    /// description property.
    pub description: Option<String>,
    /// enableEnhancedIpv4Allocation property.
    pub enable_enhanced_ipv4_allocation: Option<bool>,
    /// fingerprint property.
    pub fingerprint: Option<String>,
    /// id property.
    pub id: Option<String>,
    /// ipCidrRange property.
    pub ip_cidr_range: Option<String>,
    /// ipv6AccessType property.
    pub ipv6_access_type: Option<String>,
    /// isLiveMigration property.
    pub is_live_migration: Option<bool>,
    /// kind property.
    pub kind: Option<String>,
    /// mode property.
    pub mode: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// parentPrefix property.
    pub parent_prefix: Option<String>,
    /// publicDelegatedSubPrefixs property.
    pub public_delegated_sub_prefixs: Option<Vec<PublicDelegatedPrefixPublicDelegatedSubPrefix>>,
    /// region property.
    pub region: Option<String>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// status property.
    pub status: Option<String>,
}

/// `TargetHttpProxyList` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct TargetHttpProxyList {
    /// id property.
    pub id: Option<String>,
    /// items property.
    pub items: Option<Vec<TargetHttpProxy>>,
    /// kind property.
    pub kind: Option<String>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// warning property.
    pub warning: Option<std::collections::HashMap<String, serde_json::Value>>,
}

/// `VmExtensionPolicy` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct VmExtensionPolicy {
    /// creationTimestamp property.
    pub creation_timestamp: Option<String>,
    /// description property.
    pub description: Option<String>,
    /// extensionPolicies property.
    pub extension_policies: Option<serde_json::Value>,
    /// globalResourceLink property.
    pub global_resource_link: Option<String>,
    /// id property.
    pub id: Option<String>,
    /// instanceSelectors property.
    pub instance_selectors: Option<Vec<VmExtensionPolicyInstanceSelector>>,
    /// kind property.
    pub kind: Option<String>,
    /// managedByGlobal property.
    pub managed_by_global: Option<bool>,
    /// name property.
    pub name: Option<String>,
    /// priority property.
    pub priority: Option<i64>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// selfLinkWithId property.
    pub self_link_with_id: Option<String>,
    /// state property.
    pub state: Option<String>,
    /// updateTimestamp property.
    pub update_timestamp: Option<String>,
}

/// `InterconnectRemoteLocationPermittedConnections` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct InterconnectRemoteLocationPermittedConnections {
    /// interconnectLocation property.
    pub interconnect_location: Option<String>,
}

/// `AutoscalingPolicyCpuUtilization` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AutoscalingPolicyCpuUtilization {
    /// predictiveMethod property.
    pub predictive_method: Option<String>,
    /// utilizationTarget property.
    pub utilization_target: Option<f64>,
}

/// `SecurityPolicyAdaptiveProtectionConfigLayer7DdosDefenseConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SecurityPolicyAdaptiveProtectionConfigLayer7DdosDefenseConfig {
    /// enable property.
    pub enable: Option<bool>,
    /// ruleVisibility property.
    pub rule_visibility: Option<String>,
    /// thresholdConfigs property.
    pub threshold_configs:
        Option<Vec<SecurityPolicyAdaptiveProtectionConfigLayer7DdosDefenseConfigThresholdConfig>>,
}

/// `InstancesReportHostAsFaultyRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct InstancesReportHostAsFaultyRequest {
    /// disruptionSchedule property.
    pub disruption_schedule: Option<String>,
    /// faultReasons property.
    pub fault_reasons: Option<Vec<InstancesReportHostAsFaultyRequestFaultReason>>,
}

/// `ResourcePolicy` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ResourcePolicy {
    /// creationTimestamp property.
    pub creation_timestamp: Option<String>,
    /// description property.
    pub description: Option<String>,
    /// diskConsistencyGroupPolicy property.
    pub disk_consistency_group_policy: Option<ResourcePolicyDiskConsistencyGroupPolicy>,
    /// groupPlacementPolicy property.
    pub group_placement_policy: Option<ResourcePolicyGroupPlacementPolicy>,
    /// id property.
    pub id: Option<String>,
    /// instanceSchedulePolicy property.
    pub instance_schedule_policy: Option<ResourcePolicyInstanceSchedulePolicy>,
    /// kind property.
    pub kind: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// region property.
    pub region: Option<String>,
    /// resourceStatus property.
    pub resource_status: Option<ResourcePolicyResourceStatus>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// snapshotSchedulePolicy property.
    pub snapshot_schedule_policy: Option<ResourcePolicySnapshotSchedulePolicy>,
    /// status property.
    pub status: Option<String>,
    /// workloadPolicy property.
    pub workload_policy: Option<ResourcePolicyWorkloadPolicy>,
}

/// `InterconnectApplicationAwareInterconnectBandwidthPercentage` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct InterconnectApplicationAwareInterconnectBandwidthPercentage {
    /// percentage property.
    pub percentage: Option<i64>,
    /// trafficClass property.
    pub traffic_class: Option<String>,
}

/// `InstancesSetServiceAccountRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct InstancesSetServiceAccountRequest {
    /// email property.
    pub email: Option<String>,
    /// scopes property.
    pub scopes: Option<Vec<String>>,
}

/// `LicenseCodeLicenseAlias` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct LicenseCodeLicenseAlias {
    /// description property.
    pub description: Option<String>,
    /// selfLink property.
    pub self_link: Option<String>,
}

/// `LicenseParams` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct LicenseParams {
    /// resourceManagerTags property.
    pub resource_manager_tags: Option<serde_json::Value>,
}

/// `NetworksAddPeeringRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct NetworksAddPeeringRequest {
    /// autoCreateRoutes property.
    pub auto_create_routes: Option<bool>,
    /// name property.
    pub name: Option<String>,
    /// networkPeering property.
    pub network_peering: Option<NetworkPeering>,
    /// peerNetwork property.
    pub peer_network: Option<String>,
}

/// `RegionInstanceGroupManagersListInstancesResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct RegionInstanceGroupManagersListInstancesResponse {
    /// managedInstances property.
    pub managed_instances: Option<Vec<ManagedInstance>>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
}

/// `HealthChecksAggregatedList` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct HealthChecksAggregatedList {
    /// id property.
    pub id: Option<String>,
    /// items property.
    pub items: Option<serde_json::Value>,
    /// kind property.
    pub kind: Option<String>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// unreachables property.
    pub unreachables: Option<Vec<String>>,
    /// warning property.
    pub warning: Option<std::collections::HashMap<String, serde_json::Value>>,
}

/// `UsableSubnetworksAggregatedList` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct UsableSubnetworksAggregatedList {
    /// id property.
    pub id: Option<String>,
    /// items property.
    pub items: Option<Vec<UsableSubnetwork>>,
    /// kind property.
    pub kind: Option<String>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// scopedWarnings property.
    pub scoped_warnings: Option<Vec<SubnetworksScopedWarning>>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// unreachables property.
    pub unreachables: Option<Vec<String>>,
    /// warning property.
    pub warning: Option<std::collections::HashMap<String, serde_json::Value>>,
}

/// `AllocationResourceStatusSpecificSKUAllocation` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AllocationResourceStatusSpecificSKUAllocation {
    /// sourceInstanceTemplateId property.
    pub source_instance_template_id: Option<String>,
    /// utilizations property.
    pub utilizations: Option<serde_json::Value>,
}

/// `InterconnectGroupIntent` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct InterconnectGroupIntent {
    /// topologyCapability property.
    pub topology_capability: Option<String>,
}

/// `InterconnectsGetDiagnosticsResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct InterconnectsGetDiagnosticsResponse {
    /// result property.
    pub result: Option<InterconnectDiagnostics>,
}

/// `ShieldedInstanceConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ShieldedInstanceConfig {
    /// enableIntegrityMonitoring property.
    pub enable_integrity_monitoring: Option<bool>,
    /// enableSecureBoot property.
    pub enable_secure_boot: Option<bool>,
    /// enableVtpm property.
    pub enable_vtpm: Option<bool>,
}

/// `NotificationEndpointAggregatedList` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct NotificationEndpointAggregatedList {
    /// id property.
    pub id: Option<String>,
    /// items property.
    pub items: Option<serde_json::Value>,
    /// kind property.
    pub kind: Option<String>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// unreachables property.
    pub unreachables: Option<Vec<String>>,
    /// warning property.
    pub warning: Option<std::collections::HashMap<String, serde_json::Value>>,
}

/// `TargetPoolList` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct TargetPoolList {
    /// id property.
    pub id: Option<String>,
    /// items property.
    pub items: Option<Vec<TargetPool>>,
    /// kind property.
    pub kind: Option<String>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// warning property.
    pub warning: Option<std::collections::HashMap<String, serde_json::Value>>,
}

/// `InstanceListReferrers` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct InstanceListReferrers {
    /// id property.
    pub id: Option<String>,
    /// items property.
    pub items: Option<Vec<Reference>>,
    /// kind property.
    pub kind: Option<String>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// warning property.
    pub warning: Option<std::collections::HashMap<String, serde_json::Value>>,
}

/// `InterconnectAttachmentGroupsOperationalStatusAttachmentStatus` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct InterconnectAttachmentGroupsOperationalStatusAttachmentStatus {
    /// adminEnabled property.
    pub admin_enabled: Option<bool>,
    /// attachment property.
    pub attachment: Option<String>,
    /// isActive property.
    pub is_active: Option<String>,
    /// status property.
    pub status: Option<String>,
}

/// `GuestOsFeature` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GuestOsFeature {
    /// type property.
    pub r#type: Option<String>,
}

/// `TargetSslProxiesSetBackendServiceRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct TargetSslProxiesSetBackendServiceRequest {
    /// service property.
    pub service: Option<String>,
}

/// `UrlMap` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct UrlMap {
    /// creationTimestamp property.
    pub creation_timestamp: Option<String>,
    /// defaultCustomErrorResponsePolicy property.
    pub default_custom_error_response_policy: Option<CustomErrorResponsePolicy>,
    /// defaultRouteAction property.
    pub default_route_action: Option<HttpRouteAction>,
    /// defaultService property.
    pub default_service: Option<String>,
    /// defaultUrlRedirect property.
    pub default_url_redirect: Option<HttpRedirectAction>,
    /// description property.
    pub description: Option<String>,
    /// fingerprint property.
    pub fingerprint: Option<String>,
    /// headerAction property.
    pub header_action: Option<HttpHeaderAction>,
    /// hostRules property.
    pub host_rules: Option<Vec<HostRule>>,
    /// id property.
    pub id: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// pathMatchers property.
    pub path_matchers: Option<Vec<PathMatcher>>,
    /// region property.
    pub region: Option<String>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// tests property.
    pub tests: Option<Vec<UrlMapTest>>,
}

/// `GlobalSetLabelsRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GlobalSetLabelsRequest {
    /// labelFingerprint property.
    pub label_fingerprint: Option<String>,
    /// labels property.
    pub labels: Option<serde_json::Value>,
}

/// `NetworkEndpointWithHealthStatus` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct NetworkEndpointWithHealthStatus {
    /// healths property.
    pub healths: Option<Vec<HealthStatusForNetworkEndpoint>>,
    /// networkEndpoint property.
    pub network_endpoint: Option<NetworkEndpoint>,
}

/// `CustomerEncryptionKeyProtectedDisk` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct CustomerEncryptionKeyProtectedDisk {
    /// diskEncryptionKey property.
    pub disk_encryption_key: Option<CustomerEncryptionKey>,
    /// source property.
    pub source: Option<String>,
}

/// `InterconnectAttachmentGroupConfigured` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct InterconnectAttachmentGroupConfigured {
    /// availabilitySla property.
    pub availability_sla: Option<InterconnectAttachmentGroupConfiguredAvailabilitySLA>,
}

/// `WeightedBackendService` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct WeightedBackendService {
    /// backendService property.
    pub backend_service: Option<String>,
    /// headerAction property.
    pub header_action: Option<HttpHeaderAction>,
    /// weight property.
    pub weight: Option<i64>,
}

/// `ForwardingRuleServiceDirectoryRegistration` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ForwardingRuleServiceDirectoryRegistration {
    /// namespace property.
    pub namespace: Option<String>,
    /// service property.
    pub service: Option<String>,
    /// serviceDirectoryRegion property.
    pub service_directory_region: Option<String>,
}

/// `InterconnectList` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct InterconnectList {
    /// id property.
    pub id: Option<String>,
    /// items property.
    pub items: Option<Vec<Interconnect>>,
    /// kind property.
    pub kind: Option<String>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// warning property.
    pub warning: Option<std::collections::HashMap<String, serde_json::Value>>,
}

/// `SslCertificateList` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SslCertificateList {
    /// id property.
    pub id: Option<String>,
    /// items property.
    pub items: Option<Vec<SslCertificate>>,
    /// kind property.
    pub kind: Option<String>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// warning property.
    pub warning: Option<std::collections::HashMap<String, serde_json::Value>>,
}

/// `InstantSnapshotGroupSourceInfo` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct InstantSnapshotGroupSourceInfo {
    /// consistencyGroup property.
    pub consistency_group: Option<String>,
    /// consistencyGroupId property.
    pub consistency_group_id: Option<String>,
}

/// `InstanceGroupManagersRecreateInstancesRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct InstanceGroupManagersRecreateInstancesRequest {
    /// instances property.
    pub instances: Option<Vec<String>>,
}

/// `SnapshotSettingsAccessLocation` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SnapshotSettingsAccessLocation {
    /// locations property.
    pub locations: Option<serde_json::Value>,
    /// policy property.
    pub policy: Option<String>,
}

/// `UsableSubnetwork` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct UsableSubnetwork {
    /// externalIpv6Prefix property.
    pub external_ipv6_prefix: Option<String>,
    /// internalIpv6Prefix property.
    pub internal_ipv6_prefix: Option<String>,
    /// ipCidrRange property.
    pub ip_cidr_range: Option<String>,
    /// ipv6AccessType property.
    pub ipv6_access_type: Option<String>,
    /// network property.
    pub network: Option<String>,
    /// purpose property.
    pub purpose: Option<String>,
    /// role property.
    pub role: Option<String>,
    /// secondaryIpRanges property.
    pub secondary_ip_ranges: Option<Vec<UsableSubnetworkSecondaryRange>>,
    /// stackType property.
    pub stack_type: Option<String>,
    /// subnetwork property.
    pub subnetwork: Option<String>,
}

/// `ManagedInstanceInstanceHealth` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ManagedInstanceInstanceHealth {
    /// detailedHealthState property.
    pub detailed_health_state: Option<String>,
    /// healthCheck property.
    pub health_check: Option<String>,
}

/// `ReservationsResizeRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ReservationsResizeRequest {
    /// specificSkuCount property.
    pub specific_sku_count: Option<String>,
}

/// `AttachedDisk` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AttachedDisk {
    /// architecture property.
    pub architecture: Option<String>,
    /// autoDelete property.
    pub auto_delete: Option<bool>,
    /// boot property.
    pub boot: Option<bool>,
    /// deviceName property.
    pub device_name: Option<String>,
    /// diskEncryptionKey property.
    pub disk_encryption_key: Option<CustomerEncryptionKey>,
    /// diskSizeGb property.
    pub disk_size_gb: Option<String>,
    /// forceAttach property.
    pub force_attach: Option<bool>,
    /// guestOsFeatures property.
    pub guest_os_features: Option<Vec<GuestOsFeature>>,
    /// index property.
    pub index: Option<i64>,
    /// initializeParams property.
    pub initialize_params: Option<AttachedDiskInitializeParams>,
    /// interface property.
    pub interface: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// licenses property.
    pub licenses: Option<Vec<String>>,
    /// mode property.
    pub mode: Option<String>,
    /// savedState property.
    pub saved_state: Option<String>,
    /// shieldedInstanceInitialState property.
    pub shielded_instance_initial_state: Option<InitialStateConfig>,
    /// source property.
    pub source: Option<String>,
    /// type property.
    pub r#type: Option<String>,
}

/// `HTTPSHealthCheck` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct HTTPSHealthCheck {
    /// host property.
    pub host: Option<String>,
    /// port property.
    pub port: Option<i64>,
    /// portName property.
    pub port_name: Option<String>,
    /// portSpecification property.
    pub port_specification: Option<String>,
    /// proxyHeader property.
    pub proxy_header: Option<String>,
    /// requestPath property.
    pub request_path: Option<String>,
    /// response property.
    pub response: Option<String>,
}

/// `AutoscalerAggregatedList` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AutoscalerAggregatedList {
    /// id property.
    pub id: Option<String>,
    /// items property.
    pub items: Option<serde_json::Value>,
    /// kind property.
    pub kind: Option<String>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// unreachables property.
    pub unreachables: Option<Vec<String>>,
    /// warning property.
    pub warning: Option<std::collections::HashMap<String, serde_json::Value>>,
}

/// `InstanceProperties` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct InstanceProperties {
    /// advancedMachineFeatures property.
    pub advanced_machine_features: Option<AdvancedMachineFeatures>,
    /// canIpForward property.
    pub can_ip_forward: Option<bool>,
    /// confidentialInstanceConfig property.
    pub confidential_instance_config: Option<ConfidentialInstanceConfig>,
    /// description property.
    pub description: Option<String>,
    /// disks property.
    pub disks: Option<Vec<AttachedDisk>>,
    /// guestAccelerators property.
    pub guest_accelerators: Option<Vec<AcceleratorConfig>>,
    /// keyRevocationActionType property.
    pub key_revocation_action_type: Option<String>,
    /// labels property.
    pub labels: Option<serde_json::Value>,
    /// machineType property.
    pub machine_type: Option<String>,
    /// metadata property.
    pub metadata: Option<Metadata>,
    /// minCpuPlatform property.
    pub min_cpu_platform: Option<String>,
    /// networkInterfaces property.
    pub network_interfaces: Option<Vec<NetworkInterface>>,
    /// networkPerformanceConfig property.
    pub network_performance_config: Option<NetworkPerformanceConfig>,
    /// privateIpv6GoogleAccess property.
    pub private_ipv6_google_access: Option<String>,
    /// reservationAffinity property.
    pub reservation_affinity: Option<ReservationAffinity>,
    /// resourceManagerTags property.
    pub resource_manager_tags: Option<serde_json::Value>,
    /// resourcePolicies property.
    pub resource_policies: Option<Vec<String>>,
    /// scheduling property.
    pub scheduling: Option<Scheduling>,
    /// serviceAccounts property.
    pub service_accounts: Option<Vec<ServiceAccount>>,
    /// shieldedInstanceConfig property.
    pub shielded_instance_config: Option<ShieldedInstanceConfig>,
    /// tags property.
    pub tags: Option<Tags>,
    /// workloadIdentityConfig property.
    pub workload_identity_config: Option<WorkloadIdentityConfig>,
}

/// `ResourcePolicyWeeklyCycleDayOfWeek` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ResourcePolicyWeeklyCycleDayOfWeek {
    /// day property.
    pub day: Option<String>,
    /// duration property.
    pub duration: Option<String>,
    /// startTime property.
    pub start_time: Option<String>,
}

/// `ResourcePolicySnapshotSchedulePolicySnapshotProperties` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ResourcePolicySnapshotSchedulePolicySnapshotProperties {
    /// chainName property.
    pub chain_name: Option<String>,
    /// guestFlush property.
    pub guest_flush: Option<bool>,
    /// labels property.
    pub labels: Option<serde_json::Value>,
    /// storageLocations property.
    pub storage_locations: Option<Vec<String>>,
}

/// `SubnetworkSecondaryRange` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SubnetworkSecondaryRange {
    /// ipCidrRange property.
    pub ip_cidr_range: Option<String>,
    /// rangeName property.
    pub range_name: Option<String>,
    /// reservedInternalRange property.
    pub reserved_internal_range: Option<String>,
}

/// `FirewallPolicyRuleMatcherLayer4Config` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct FirewallPolicyRuleMatcherLayer4Config {
    /// ipProtocol property.
    pub ip_protocol: Option<String>,
    /// ports property.
    pub ports: Option<Vec<String>>,
}

/// `Binding` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Binding {
    /// condition property.
    pub condition: Option<Expr>,
    /// members property.
    pub members: Option<Vec<String>>,
    /// role property.
    pub role: Option<String>,
}

/// `SecurityPolicyRuleHttpHeaderAction` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SecurityPolicyRuleHttpHeaderAction {
    /// requestHeadersToAdds property.
    pub request_headers_to_adds: Option<Vec<SecurityPolicyRuleHttpHeaderActionHttpHeaderOption>>,
}

/// `TargetVpnGatewayParams` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct TargetVpnGatewayParams {
    /// resourceManagerTags property.
    pub resource_manager_tags: Option<serde_json::Value>,
}

/// `Address` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Address {
    /// address property.
    pub address: Option<String>,
    /// addressType property.
    pub address_type: Option<String>,
    /// creationTimestamp property.
    pub creation_timestamp: Option<String>,
    /// description property.
    pub description: Option<String>,
    /// id property.
    pub id: Option<String>,
    /// ipCollection property.
    pub ip_collection: Option<String>,
    /// ipVersion property.
    pub ip_version: Option<String>,
    /// ipv6EndpointType property.
    pub ipv6_endpoint_type: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// labelFingerprint property.
    pub label_fingerprint: Option<String>,
    /// labels property.
    pub labels: Option<serde_json::Value>,
    /// name property.
    pub name: Option<String>,
    /// network property.
    pub network: Option<String>,
    /// networkTier property.
    pub network_tier: Option<String>,
    /// prefixLength property.
    pub prefix_length: Option<i64>,
    /// purpose property.
    pub purpose: Option<String>,
    /// region property.
    pub region: Option<String>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// status property.
    pub status: Option<String>,
    /// subnetwork property.
    pub subnetwork: Option<String>,
    /// users property.
    pub users: Option<Vec<String>>,
}

/// `AddressList` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AddressList {
    /// id property.
    pub id: Option<String>,
    /// items property.
    pub items: Option<Vec<Address>>,
    /// kind property.
    pub kind: Option<String>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// warning property.
    pub warning: Option<std::collections::HashMap<String, serde_json::Value>>,
}

/// `DisksStopGroupAsyncReplicationResource` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct DisksStopGroupAsyncReplicationResource {
    /// resourcePolicy property.
    pub resource_policy: Option<String>,
}

/// `OutlierDetection` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct OutlierDetection {
    /// baseEjectionTime property.
    pub base_ejection_time: Option<Duration>,
    /// consecutiveErrors property.
    pub consecutive_errors: Option<i64>,
    /// consecutiveGatewayFailure property.
    pub consecutive_gateway_failure: Option<i64>,
    /// enforcingConsecutiveErrors property.
    pub enforcing_consecutive_errors: Option<i64>,
    /// enforcingConsecutiveGatewayFailure property.
    pub enforcing_consecutive_gateway_failure: Option<i64>,
    /// enforcingSuccessRate property.
    pub enforcing_success_rate: Option<i64>,
    /// interval property.
    pub interval: Option<Duration>,
    /// maxEjectionPercent property.
    pub max_ejection_percent: Option<i64>,
    /// successRateMinimumHosts property.
    pub success_rate_minimum_hosts: Option<i64>,
    /// successRateRequestVolume property.
    pub success_rate_request_volume: Option<i64>,
    /// successRateStdevFactor property.
    pub success_rate_stdev_factor: Option<i64>,
}

/// `UrlMapValidationResult` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct UrlMapValidationResult {
    /// loadErrors property.
    pub load_errors: Option<Vec<String>>,
    /// loadSucceeded property.
    pub load_succeeded: Option<bool>,
    /// testFailures property.
    pub test_failures: Option<Vec<TestFailure>>,
    /// testPassed property.
    pub test_passed: Option<bool>,
}

/// `RequestMirrorPolicy` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct RequestMirrorPolicy {
    /// backendService property.
    pub backend_service: Option<String>,
    /// mirrorPercent property.
    pub mirror_percent: Option<f64>,
}

/// `RegionUrlMapsValidateRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct RegionUrlMapsValidateRequest {
    /// resource property.
    pub resource: Option<UrlMap>,
}

/// `NetworkAttachmentList` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct NetworkAttachmentList {
    /// id property.
    pub id: Option<String>,
    /// items property.
    pub items: Option<Vec<NetworkAttachment>>,
    /// kind property.
    pub kind: Option<String>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// warning property.
    pub warning: Option<std::collections::HashMap<String, serde_json::Value>>,
}

/// `NodeTemplateNodeTypeFlexibility` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct NodeTemplateNodeTypeFlexibility {
    /// cpus property.
    pub cpus: Option<String>,
    /// localSsd property.
    pub local_ssd: Option<String>,
    /// memory property.
    pub memory: Option<String>,
}

/// `InstanceParams` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct InstanceParams {
    /// requestValidForDuration property.
    pub request_valid_for_duration: Option<Duration>,
    /// resourceManagerTags property.
    pub resource_manager_tags: Option<serde_json::Value>,
}

/// `InterconnectCircuitInfo` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct InterconnectCircuitInfo {
    /// customerDemarcId property.
    pub customer_demarc_id: Option<String>,
    /// googleCircuitId property.
    pub google_circuit_id: Option<String>,
    /// googleDemarcId property.
    pub google_demarc_id: Option<String>,
}

/// `PacketMirroringList` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct PacketMirroringList {
    /// id property.
    pub id: Option<String>,
    /// items property.
    pub items: Option<Vec<PacketMirroring>>,
    /// kind property.
    pub kind: Option<String>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// warning property.
    pub warning: Option<std::collections::HashMap<String, serde_json::Value>>,
}

/// `HealthCheckService` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct HealthCheckService {
    /// creationTimestamp property.
    pub creation_timestamp: Option<String>,
    /// description property.
    pub description: Option<String>,
    /// fingerprint property.
    pub fingerprint: Option<String>,
    /// healthChecks property.
    pub health_checks: Option<Vec<String>>,
    /// healthStatusAggregationPolicy property.
    pub health_status_aggregation_policy: Option<String>,
    /// id property.
    pub id: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// networkEndpointGroups property.
    pub network_endpoint_groups: Option<Vec<String>>,
    /// notificationEndpoints property.
    pub notification_endpoints: Option<Vec<String>>,
    /// region property.
    pub region: Option<String>,
    /// selfLink property.
    pub self_link: Option<String>,
}

/// `MachineImageParams` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct MachineImageParams {
    /// resourceManagerTags property.
    pub resource_manager_tags: Option<serde_json::Value>,
}

/// `StoragePoolTypeAggregatedList` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct StoragePoolTypeAggregatedList {
    /// id property.
    pub id: Option<String>,
    /// items property.
    pub items: Option<serde_json::Value>,
    /// kind property.
    pub kind: Option<String>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// warning property.
    pub warning: Option<std::collections::HashMap<String, serde_json::Value>>,
}

/// `Expr` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Expr {
    /// description property.
    pub description: Option<String>,
    /// expression property.
    pub expression: Option<String>,
    /// location property.
    pub location: Option<String>,
    /// title property.
    pub title: Option<String>,
}

/// `InstanceGroupManagerStatusStatefulPerInstanceConfigs` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct InstanceGroupManagerStatusStatefulPerInstanceConfigs {
    /// allEffective property.
    pub all_effective: Option<bool>,
}

/// `WafExpressionSet` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct WafExpressionSet {
    /// aliases property.
    pub aliases: Option<Vec<String>>,
    /// expressions property.
    pub expressions: Option<Vec<WafExpressionSetExpression>>,
    /// id property.
    pub id: Option<String>,
}

/// `SecurityPolicyAdaptiveProtectionConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SecurityPolicyAdaptiveProtectionConfig {
    /// layer7DdosDefenseConfig property.
    pub layer7_ddos_defense_config:
        Option<SecurityPolicyAdaptiveProtectionConfigLayer7DdosDefenseConfig>,
}

/// `StoragePoolParams` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct StoragePoolParams {
    /// resourceManagerTags property.
    pub resource_manager_tags: Option<serde_json::Value>,
}

/// `HealthCheckServicesList` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct HealthCheckServicesList {
    /// id property.
    pub id: Option<String>,
    /// items property.
    pub items: Option<Vec<HealthCheckService>>,
    /// kind property.
    pub kind: Option<String>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// warning property.
    pub warning: Option<std::collections::HashMap<String, serde_json::Value>>,
}

/// `NetworkEndpointGroupCloudRun` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct NetworkEndpointGroupCloudRun {
    /// service property.
    pub service: Option<String>,
    /// tag property.
    pub tag: Option<String>,
    /// urlMask property.
    pub url_mask: Option<String>,
}

/// `InterconnectApplicationAwareInterconnectBandwidthPercentagePolicy` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct InterconnectApplicationAwareInterconnectBandwidthPercentagePolicy {
    /// bandwidthPercentages property.
    pub bandwidth_percentages:
        Option<Vec<InterconnectApplicationAwareInterconnectBandwidthPercentage>>,
}

/// `GlobalNetworkEndpointGroupsAttachEndpointsRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GlobalNetworkEndpointGroupsAttachEndpointsRequest {
    /// networkEndpoints property.
    pub network_endpoints: Option<Vec<NetworkEndpoint>>,
}

/// `RoutePolicy` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct RoutePolicy {
    /// description property.
    pub description: Option<String>,
    /// fingerprint property.
    pub fingerprint: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// terms property.
    pub terms: Option<Vec<RoutePolicyPolicyTerm>>,
    /// type property.
    pub r#type: Option<String>,
}

/// `ReservationAdvancedDeploymentControl` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ReservationAdvancedDeploymentControl {
    /// reservationOperationalMode property.
    pub reservation_operational_mode: Option<String>,
}

/// `NetworkEndpointGroupList` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct NetworkEndpointGroupList {
    /// id property.
    pub id: Option<String>,
    /// items property.
    pub items: Option<Vec<NetworkEndpointGroup>>,
    /// kind property.
    pub kind: Option<String>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// warning property.
    pub warning: Option<std::collections::HashMap<String, serde_json::Value>>,
}

/// `VpnGatewayVpnGatewayInterface` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct VpnGatewayVpnGatewayInterface {
    /// id property.
    pub id: Option<i64>,
    /// interconnectAttachment property.
    pub interconnect_attachment: Option<String>,
    /// ipAddress property.
    pub ip_address: Option<String>,
    /// ipv6Address property.
    pub ipv6_address: Option<String>,
}

/// `HttpHeaderMatch` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct HttpHeaderMatch {
    /// exactMatch property.
    pub exact_match: Option<String>,
    /// headerName property.
    pub header_name: Option<String>,
    /// invertMatch property.
    pub invert_match: Option<bool>,
    /// prefixMatch property.
    pub prefix_match: Option<String>,
    /// presentMatch property.
    pub present_match: Option<bool>,
    /// rangeMatch property.
    pub range_match: Option<Int64RangeMatch>,
    /// regexMatch property.
    pub regex_match: Option<String>,
    /// suffixMatch property.
    pub suffix_match: Option<String>,
}

/// `VpnGatewayStatusHighAvailabilityRequirementState` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct VpnGatewayStatusHighAvailabilityRequirementState {
    /// state property.
    pub state: Option<String>,
    /// unsatisfiedReason property.
    pub unsatisfied_reason: Option<String>,
}

/// `TargetTcpProxiesSetProxyHeaderRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct TargetTcpProxiesSetProxyHeaderRequest {
    /// proxyHeader property.
    pub proxy_header: Option<String>,
}

/// `InstanceSettings` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct InstanceSettings {
    /// fingerprint property.
    pub fingerprint: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// metadata property.
    pub metadata: Option<InstanceSettingsMetadata>,
    /// zone property.
    pub zone: Option<String>,
}

/// `NodeTemplateList` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct NodeTemplateList {
    /// id property.
    pub id: Option<String>,
    /// items property.
    pub items: Option<Vec<NodeTemplate>>,
    /// kind property.
    pub kind: Option<String>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// warning property.
    pub warning: Option<std::collections::HashMap<String, serde_json::Value>>,
}

/// `NodeGroup` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct NodeGroup {
    /// autoscalingPolicy property.
    pub autoscaling_policy: Option<NodeGroupAutoscalingPolicy>,
    /// creationTimestamp property.
    pub creation_timestamp: Option<String>,
    /// description property.
    pub description: Option<String>,
    /// fingerprint property.
    pub fingerprint: Option<String>,
    /// id property.
    pub id: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// locationHint property.
    pub location_hint: Option<String>,
    /// maintenanceInterval property.
    pub maintenance_interval: Option<String>,
    /// maintenancePolicy property.
    pub maintenance_policy: Option<String>,
    /// maintenanceWindow property.
    pub maintenance_window: Option<NodeGroupMaintenanceWindow>,
    /// name property.
    pub name: Option<String>,
    /// nodeTemplate property.
    pub node_template: Option<String>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// shareSettings property.
    pub share_settings: Option<ShareSettings>,
    /// size property.
    pub size: Option<i64>,
    /// status property.
    pub status: Option<String>,
    /// zone property.
    pub zone: Option<String>,
}

/// `ErrorInfo` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ErrorInfo {
    /// domain property.
    pub domain: Option<String>,
    /// metadatas property.
    pub metadatas: Option<serde_json::Value>,
    /// reason property.
    pub reason: Option<String>,
}

/// `InterconnectLocationRegionInfo` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct InterconnectLocationRegionInfo {
    /// expectedRttMs property.
    pub expected_rtt_ms: Option<String>,
    /// l2ForwardingEnabled property.
    pub l2_forwarding_enabled: Option<bool>,
    /// locationPresence property.
    pub location_presence: Option<String>,
    /// region property.
    pub region: Option<String>,
}

/// `FirewallLogConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct FirewallLogConfig {
    /// enable property.
    pub enable: Option<bool>,
    /// metadata property.
    pub metadata: Option<String>,
}

/// `InterconnectParams` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct InterconnectParams {
    /// resourceManagerTags property.
    pub resource_manager_tags: Option<serde_json::Value>,
}

/// `BackendServiceNetworkPassThroughLbTrafficPolicy` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct BackendServiceNetworkPassThroughLbTrafficPolicy {
    /// zonalAffinity property.
    pub zonal_affinity: Option<BackendServiceNetworkPassThroughLbTrafficPolicyZonalAffinity>,
}

/// `SecurityPolicyRulePreconfiguredWafConfigExclusionFieldParams` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SecurityPolicyRulePreconfiguredWafConfigExclusionFieldParams {
    /// op property.
    pub op: Option<String>,
    /// val property.
    pub val: Option<String>,
}

/// `SslPolicy` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SslPolicy {
    /// creationTimestamp property.
    pub creation_timestamp: Option<String>,
    /// customFeatures property.
    pub custom_features: Option<Vec<String>>,
    /// description property.
    pub description: Option<String>,
    /// enabledFeatures property.
    pub enabled_features: Option<Vec<String>>,
    /// fingerprint property.
    pub fingerprint: Option<String>,
    /// id property.
    pub id: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// minTlsVersion property.
    pub min_tls_version: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// profile property.
    pub profile: Option<String>,
    /// region property.
    pub region: Option<String>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// warnings property.
    pub warnings: Option<Vec<std::collections::HashMap<String, serde_json::Value>>>,
}

/// `LicenseResourceRequirements` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct LicenseResourceRequirements {
    /// minGuestCpuCount property.
    pub min_guest_cpu_count: Option<i64>,
    /// minMemoryMb property.
    pub min_memory_mb: Option<i64>,
}

/// `GRPCTLSHealthCheck` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GRPCTLSHealthCheck {
    /// grpcServiceName property.
    pub grpc_service_name: Option<String>,
    /// port property.
    pub port: Option<i64>,
    /// portSpecification property.
    pub port_specification: Option<String>,
}

/// `InterconnectRemoteLocation` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct InterconnectRemoteLocation {
    /// address property.
    pub address: Option<String>,
    /// attachmentConfigurationConstraints property.
    pub attachment_configuration_constraints:
        Option<InterconnectAttachmentConfigurationConstraints>,
    /// city property.
    pub city: Option<String>,
    /// constraints property.
    pub constraints: Option<InterconnectRemoteLocationConstraints>,
    /// continent property.
    pub continent: Option<String>,
    /// creationTimestamp property.
    pub creation_timestamp: Option<String>,
    /// description property.
    pub description: Option<String>,
    /// facilityProvider property.
    pub facility_provider: Option<String>,
    /// facilityProviderFacilityId property.
    pub facility_provider_facility_id: Option<String>,
    /// id property.
    pub id: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// lacp property.
    pub lacp: Option<String>,
    /// maxLagSize100Gbps property.
    pub max_lag_size100_gbps: Option<i64>,
    /// maxLagSize10Gbps property.
    pub max_lag_size10_gbps: Option<i64>,
    /// maxLagSize400Gbps property.
    pub max_lag_size400_gbps: Option<i64>,
    /// name property.
    pub name: Option<String>,
    /// peeringdbFacilityId property.
    pub peeringdb_facility_id: Option<String>,
    /// permittedConnections property.
    pub permitted_connections: Option<Vec<InterconnectRemoteLocationPermittedConnections>>,
    /// remoteService property.
    pub remote_service: Option<String>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// status property.
    pub status: Option<String>,
}

/// `ResourcePolicyHourlyCycle` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ResourcePolicyHourlyCycle {
    /// duration property.
    pub duration: Option<String>,
    /// hoursInCycle property.
    pub hours_in_cycle: Option<i64>,
    /// startTime property.
    pub start_time: Option<String>,
}

/// `FixedOrPercent` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct FixedOrPercent {
    /// calculated property.
    pub calculated: Option<i64>,
    /// fixed property.
    pub fixed: Option<i64>,
    /// percent property.
    pub percent: Option<i64>,
}

/// `PublicAdvertisedPrefixPublicDelegatedPrefix` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct PublicAdvertisedPrefixPublicDelegatedPrefix {
    /// ipRange property.
    pub ip_range: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// project property.
    pub project: Option<String>,
    /// region property.
    pub region: Option<String>,
    /// status property.
    pub status: Option<String>,
}

/// `InterconnectGroupPhysicalStructure` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct InterconnectGroupPhysicalStructure {
    /// metros property.
    pub metros: Option<Vec<InterconnectGroupPhysicalStructureMetros>>,
}

/// `StatefulPolicy` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct StatefulPolicy {
    /// preservedState property.
    pub preserved_state: Option<StatefulPolicyPreservedState>,
}

/// `NetworksUpdatePeeringRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct NetworksUpdatePeeringRequest {
    /// networkPeering property.
    pub network_peering: Option<NetworkPeering>,
}

/// `WireGroupTopology` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct WireGroupTopology {
    /// endpoints property.
    pub endpoints: Option<Vec<WireGroupTopologyEndpoint>>,
}

/// `CommitmentList` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct CommitmentList {
    /// id property.
    pub id: Option<String>,
    /// items property.
    pub items: Option<Vec<Commitment>>,
    /// kind property.
    pub kind: Option<String>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// warning property.
    pub warning: Option<std::collections::HashMap<String, serde_json::Value>>,
}

/// `ExchangedPeeringRoute` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ExchangedPeeringRoute {
    /// destRange property.
    pub dest_range: Option<String>,
    /// imported property.
    pub imported: Option<bool>,
    /// nextHopRegion property.
    pub next_hop_region: Option<String>,
    /// priority property.
    pub priority: Option<i64>,
    /// type property.
    pub r#type: Option<String>,
}

/// `GuestAttributesValue` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GuestAttributesValue {
    /// items property.
    pub items: Option<Vec<GuestAttributesEntry>>,
}

/// `NetworkEndpointGroupPscData` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct NetworkEndpointGroupPscData {
    /// consumerPscAddress property.
    pub consumer_psc_address: Option<String>,
    /// producerPort property.
    pub producer_port: Option<i64>,
    /// pscConnectionId property.
    pub psc_connection_id: Option<String>,
    /// pscConnectionStatus property.
    pub psc_connection_status: Option<String>,
}

/// `FutureReservationStatusLastKnownGoodState` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct FutureReservationStatusLastKnownGoodState {
    /// description property.
    pub description: Option<String>,
    /// existingMatchingUsageInfo property.
    pub existing_matching_usage_info: Option<FutureReservationStatusExistingMatchingUsageInfo>,
    /// futureReservationSpecs property.
    pub future_reservation_specs:
        Option<FutureReservationStatusLastKnownGoodStateFutureReservationSpecs>,
    /// lockTime property.
    pub lock_time: Option<String>,
    /// namePrefix property.
    pub name_prefix: Option<String>,
    /// procurementStatus property.
    pub procurement_status: Option<String>,
}

/// `TargetSslProxiesSetSslCertificatesRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct TargetSslProxiesSetSslCertificatesRequest {
    /// sslCertificates property.
    pub ssl_certificates: Option<Vec<String>>,
}

/// `NotificationEndpoint` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct NotificationEndpoint {
    /// creationTimestamp property.
    pub creation_timestamp: Option<String>,
    /// description property.
    pub description: Option<String>,
    /// grpcSettings property.
    pub grpc_settings: Option<NotificationEndpointGrpcSettings>,
    /// id property.
    pub id: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// region property.
    pub region: Option<String>,
    /// selfLink property.
    pub self_link: Option<String>,
}

/// `FileContentBuffer` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct FileContentBuffer {
    /// content property.
    pub content: Option<String>,
    /// fileType property.
    pub file_type: Option<String>,
}

/// `SubnetworkUtilizationDetailsIPV6Utilization` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SubnetworkUtilizationDetailsIPV6Utilization {
    /// totalAllocatedIp property.
    pub total_allocated_ip: Option<Uint128>,
    /// totalFreeIp property.
    pub total_free_ip: Option<Uint128>,
}

/// `RouterStatusResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct RouterStatusResponse {
    /// kind property.
    pub kind: Option<String>,
    /// result property.
    pub result: Option<RouterStatus>,
}

/// `RegionInstanceGroupManagersRecreateRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct RegionInstanceGroupManagersRecreateRequest {
    /// instances property.
    pub instances: Option<Vec<String>>,
}

/// `ServiceAttachment` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ServiceAttachment {
    /// connectedEndpoints property.
    pub connected_endpoints: Option<Vec<ServiceAttachmentConnectedEndpoint>>,
    /// connectionPreference property.
    pub connection_preference: Option<String>,
    /// consumerAcceptLists property.
    pub consumer_accept_lists: Option<Vec<ServiceAttachmentConsumerProjectLimit>>,
    /// consumerRejectLists property.
    pub consumer_reject_lists: Option<Vec<String>>,
    /// creationTimestamp property.
    pub creation_timestamp: Option<String>,
    /// description property.
    pub description: Option<String>,
    /// domainNames property.
    pub domain_names: Option<Vec<String>>,
    /// enableProxyProtocol property.
    pub enable_proxy_protocol: Option<bool>,
    /// fingerprint property.
    pub fingerprint: Option<String>,
    /// id property.
    pub id: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// metadata property.
    pub metadata: Option<serde_json::Value>,
    /// name property.
    pub name: Option<String>,
    /// natSubnets property.
    pub nat_subnets: Option<Vec<String>>,
    /// producerForwardingRule property.
    pub producer_forwarding_rule: Option<String>,
    /// propagatedConnectionLimit property.
    pub propagated_connection_limit: Option<i64>,
    /// pscServiceAttachmentId property.
    pub psc_service_attachment_id: Option<Uint128>,
    /// reconcileConnections property.
    pub reconcile_connections: Option<bool>,
    /// region property.
    pub region: Option<String>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// targetService property.
    pub target_service: Option<String>,
}

/// `AWSV4Signature` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AWSV4Signature {
    /// accessKey property.
    pub access_key: Option<String>,
    /// accessKeyId property.
    pub access_key_id: Option<String>,
    /// accessKeyVersion property.
    pub access_key_version: Option<String>,
    /// originRegion property.
    pub origin_region: Option<String>,
}

/// `RouterInterface` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct RouterInterface {
    /// ipRange property.
    pub ip_range: Option<String>,
    /// ipVersion property.
    pub ip_version: Option<String>,
    /// linkedInterconnectAttachment property.
    pub linked_interconnect_attachment: Option<String>,
    /// linkedVpnTunnel property.
    pub linked_vpn_tunnel: Option<String>,
    /// managementType property.
    pub management_type: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// privateIpAddress property.
    pub private_ip_address: Option<String>,
    /// redundantInterface property.
    pub redundant_interface: Option<String>,
    /// subnetwork property.
    pub subnetwork: Option<String>,
}

/// `SubnetworksSetPrivateIpGoogleAccessRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SubnetworksSetPrivateIpGoogleAccessRequest {
    /// privateIpGoogleAccess property.
    pub private_ip_google_access: Option<bool>,
}

/// `InterconnectMacsecConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct InterconnectMacsecConfig {
    /// preSharedKeys property.
    pub pre_shared_keys: Option<Vec<InterconnectMacsecConfigPreSharedKey>>,
}

/// `TargetVpnGatewayAggregatedList` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct TargetVpnGatewayAggregatedList {
    /// id property.
    pub id: Option<String>,
    /// items property.
    pub items: Option<serde_json::Value>,
    /// kind property.
    pub kind: Option<String>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// unreachables property.
    pub unreachables: Option<Vec<String>>,
    /// warning property.
    pub warning: Option<std::collections::HashMap<String, serde_json::Value>>,
}

/// `ExchangedPeeringRoutesList` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ExchangedPeeringRoutesList {
    /// id property.
    pub id: Option<String>,
    /// items property.
    pub items: Option<Vec<ExchangedPeeringRoute>>,
    /// kind property.
    pub kind: Option<String>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// warning property.
    pub warning: Option<std::collections::HashMap<String, serde_json::Value>>,
}

/// `SnapshotList` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SnapshotList {
    /// id property.
    pub id: Option<String>,
    /// items property.
    pub items: Option<Vec<Snapshot>>,
    /// kind property.
    pub kind: Option<String>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// warning property.
    pub warning: Option<std::collections::HashMap<String, serde_json::Value>>,
}

/// `DistributionPolicy` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct DistributionPolicy {
    /// targetShape property.
    pub target_shape: Option<String>,
    /// zones property.
    pub zones: Option<Vec<DistributionPolicyZoneConfiguration>>,
}

/// `InstancesBulkInsertOperationMetadata` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct InstancesBulkInsertOperationMetadata {
    /// perLocationStatus property.
    pub per_location_status: Option<serde_json::Value>,
}

/// `ImageParams` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ImageParams {
    /// resourceManagerTags property.
    pub resource_manager_tags: Option<serde_json::Value>,
}

/// `InitialStateConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct InitialStateConfig {
    /// dbs property.
    pub dbs: Option<Vec<FileContentBuffer>>,
    /// dbxs property.
    pub dbxs: Option<Vec<FileContentBuffer>>,
    /// keks property.
    pub keks: Option<Vec<FileContentBuffer>>,
    /// pk property.
    pub pk: Option<FileContentBuffer>,
}

/// `ResourceCommitment` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ResourceCommitment {
    /// acceleratorType property.
    pub accelerator_type: Option<String>,
    /// amount property.
    pub amount: Option<String>,
    /// type property.
    pub r#type: Option<String>,
}

/// `InstanceGroupManagerList` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct InstanceGroupManagerList {
    /// id property.
    pub id: Option<String>,
    /// items property.
    pub items: Option<Vec<InstanceGroupManager>>,
    /// kind property.
    pub kind: Option<String>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// warning property.
    pub warning: Option<std::collections::HashMap<String, serde_json::Value>>,
}

/// `SecurityPoliciesListPreconfiguredExpressionSetsResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SecurityPoliciesListPreconfiguredExpressionSetsResponse {
    /// preconfiguredExpressionSets property.
    pub preconfigured_expression_sets: Option<SecurityPoliciesWafConfig>,
}

/// `CompositeHealthCheck` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct CompositeHealthCheck {
    /// creationTimestamp property.
    pub creation_timestamp: Option<String>,
    /// description property.
    pub description: Option<String>,
    /// fingerprint property.
    pub fingerprint: Option<String>,
    /// healthDestination property.
    pub health_destination: Option<String>,
    /// healthSources property.
    pub health_sources: Option<Vec<String>>,
    /// id property.
    pub id: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// region property.
    pub region: Option<String>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// selfLinkWithId property.
    pub self_link_with_id: Option<String>,
}

/// `ReservationSubBlockPhysicalTopology` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ReservationSubBlockPhysicalTopology {
    /// block property.
    pub block: Option<String>,
    /// cluster property.
    pub cluster: Option<String>,
    /// subBlock property.
    pub sub_block: Option<String>,
}

/// `RouteList` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct RouteList {
    /// id property.
    pub id: Option<String>,
    /// items property.
    pub items: Option<Vec<Route>>,
    /// kind property.
    pub kind: Option<String>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// warning property.
    pub warning: Option<std::collections::HashMap<String, serde_json::Value>>,
}

/// `NetworkProfileNetworkFeaturesPredefinedSubnetworkRange` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct NetworkProfileNetworkFeaturesPredefinedSubnetworkRange {
    /// ipv6Range property.
    pub ipv6_range: Option<String>,
    /// namePrefix property.
    pub name_prefix: Option<String>,
}

/// `NetworkAttachmentAggregatedList` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct NetworkAttachmentAggregatedList {
    /// id property.
    pub id: Option<String>,
    /// items property.
    pub items: Option<serde_json::Value>,
    /// kind property.
    pub kind: Option<String>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// warning property.
    pub warning: Option<std::collections::HashMap<String, serde_json::Value>>,
}

/// `AutoscalingPolicyLoadBalancingUtilization` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AutoscalingPolicyLoadBalancingUtilization {
    /// utilizationTarget property.
    pub utilization_target: Option<f64>,
}

/// `MachineTypeList` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct MachineTypeList {
    /// id property.
    pub id: Option<String>,
    /// items property.
    pub items: Option<Vec<MachineType>>,
    /// kind property.
    pub kind: Option<String>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// warning property.
    pub warning: Option<std::collections::HashMap<String, serde_json::Value>>,
}

/// `NetworkFirewallPolicyAggregatedList` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct NetworkFirewallPolicyAggregatedList {
    /// id property.
    pub id: Option<String>,
    /// items property.
    pub items: Option<serde_json::Value>,
    /// kind property.
    pub kind: Option<String>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// unreachables property.
    pub unreachables: Option<Vec<String>>,
    /// warning property.
    pub warning: Option<std::collections::HashMap<String, serde_json::Value>>,
}

/// `RegionInstanceGroupManagersSuspendInstancesRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct RegionInstanceGroupManagersSuspendInstancesRequest {
    /// forceSuspend property.
    pub force_suspend: Option<bool>,
    /// instances property.
    pub instances: Option<Vec<String>>,
}

/// `NetworkPeeringConnectionStatus` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct NetworkPeeringConnectionStatus {
    /// consensusState property.
    pub consensus_state: Option<NetworkPeeringConnectionStatusConsensusState>,
    /// trafficConfiguration property.
    pub traffic_configuration: Option<NetworkPeeringConnectionStatusTrafficConfiguration>,
    /// updateStrategy property.
    pub update_strategy: Option<String>,
}

/// `InstanceWithNamedPorts` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct InstanceWithNamedPorts {
    /// instance property.
    pub instance: Option<String>,
    /// namedPorts property.
    pub named_ports: Option<Vec<NamedPort>>,
    /// status property.
    pub status: Option<String>,
}

/// `InstanceGroupManagersAbandonInstancesRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct InstanceGroupManagersAbandonInstancesRequest {
    /// instances property.
    pub instances: Option<Vec<String>>,
}

/// `AcceleratorTopologiesInfo` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AcceleratorTopologiesInfo {
    /// acceleratorTopologyInfos property.
    pub accelerator_topology_infos: Option<Vec<AcceleratorTopologiesInfoAcceleratorTopologyInfo>>,
}

/// `InstanceGroupManagerResizeRequestsListResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct InstanceGroupManagerResizeRequestsListResponse {
    /// id property.
    pub id: Option<String>,
    /// items property.
    pub items: Option<Vec<InstanceGroupManagerResizeRequest>>,
    /// kind property.
    pub kind: Option<String>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// warning property.
    pub warning: Option<std::collections::HashMap<String, serde_json::Value>>,
}

/// `DiskMoveRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct DiskMoveRequest {
    /// destinationZone property.
    pub destination_zone: Option<String>,
    /// targetDisk property.
    pub target_disk: Option<String>,
}

/// `RegionInstanceGroupManagersAbandonInstancesRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct RegionInstanceGroupManagersAbandonInstancesRequest {
    /// instances property.
    pub instances: Option<Vec<String>>,
}

/// `FutureReservationStatus` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct FutureReservationStatus {
    /// amendmentStatus property.
    pub amendment_status: Option<String>,
    /// autoCreatedReservations property.
    pub auto_created_reservations: Option<Vec<String>>,
    /// existingMatchingUsageInfo property.
    pub existing_matching_usage_info: Option<FutureReservationStatusExistingMatchingUsageInfo>,
    /// fulfilledCount property.
    pub fulfilled_count: Option<String>,
    /// lastKnownGoodState property.
    pub last_known_good_state: Option<FutureReservationStatusLastKnownGoodState>,
    /// lockTime property.
    pub lock_time: Option<String>,
    /// procurementStatus property.
    pub procurement_status: Option<String>,
    /// specificSkuProperties property.
    pub specific_sku_properties: Option<FutureReservationStatusSpecificSKUProperties>,
}

/// `FirewallPoliciesListAssociationsResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct FirewallPoliciesListAssociationsResponse {
    /// associations property.
    pub associations: Option<Vec<FirewallPolicyAssociation>>,
    /// kind property.
    pub kind: Option<String>,
}

/// `InstanceGroupManagersApplyUpdatesRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct InstanceGroupManagersApplyUpdatesRequest {
    /// allInstances property.
    pub all_instances: Option<bool>,
    /// instances property.
    pub instances: Option<Vec<String>>,
    /// minimalAction property.
    pub minimal_action: Option<String>,
    /// mostDisruptiveAllowedAction property.
    pub most_disruptive_allowed_action: Option<String>,
}

/// `Screenshot` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Screenshot {
    /// contents property.
    pub contents: Option<String>,
    /// kind property.
    pub kind: Option<String>,
}

/// `SslPolicyReference` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SslPolicyReference {
    /// sslPolicy property.
    pub ssl_policy: Option<String>,
}

/// `InterconnectGroupConfiguredTopologyCapabilityIntendedCapabilityBlockers` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct InterconnectGroupConfiguredTopologyCapabilityIntendedCapabilityBlockers {
    /// blockerType property.
    pub blocker_type: Option<String>,
    /// documentationLink property.
    pub documentation_link: Option<String>,
    /// explanation property.
    pub explanation: Option<String>,
    /// facilities property.
    pub facilities: Option<Vec<String>>,
    /// interconnects property.
    pub interconnects: Option<Vec<String>>,
    /// metros property.
    pub metros: Option<Vec<String>>,
    /// zones property.
    pub zones: Option<Vec<String>>,
}

/// `BackendBucketCdnPolicyNegativeCachingPolicy` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct BackendBucketCdnPolicyNegativeCachingPolicy {
    /// code property.
    pub code: Option<i64>,
    /// ttl property.
    pub ttl: Option<i64>,
}

/// `ManagedInstance` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ManagedInstance {
    /// currentAction property.
    pub current_action: Option<String>,
    /// id property.
    pub id: Option<String>,
    /// instance property.
    pub instance: Option<String>,
    /// instanceHealth property.
    pub instance_health: Option<Vec<ManagedInstanceInstanceHealth>>,
    /// instanceStatus property.
    pub instance_status: Option<String>,
    /// lastAttempt property.
    pub last_attempt: Option<ManagedInstanceLastAttempt>,
    /// name property.
    pub name: Option<String>,
    /// preservedStateFromConfig property.
    pub preserved_state_from_config: Option<PreservedState>,
    /// preservedStateFromPolicy property.
    pub preserved_state_from_policy: Option<PreservedState>,
    /// propertiesFromFlexibilityPolicy property.
    pub properties_from_flexibility_policy: Option<ManagedInstancePropertiesFromFlexibilityPolicy>,
    /// version property.
    pub version: Option<ManagedInstanceVersion>,
}

/// `InstancesReportHostAsFaultyRequestFaultReason` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct InstancesReportHostAsFaultyRequestFaultReason {
    /// behavior property.
    pub behavior: Option<String>,
    /// description property.
    pub description: Option<String>,
}

/// `ReservationSlotsGetResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ReservationSlotsGetResponse {
    /// resource property.
    pub resource: Option<ReservationSlot>,
}

/// `ManagedInstanceVersion` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ManagedInstanceVersion {
    /// instanceTemplate property.
    pub instance_template: Option<String>,
    /// name property.
    pub name: Option<String>,
}

/// `NetworkAttachment` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct NetworkAttachment {
    /// connectionEndpoints property.
    pub connection_endpoints: Option<Vec<NetworkAttachmentConnectedEndpoint>>,
    /// connectionPreference property.
    pub connection_preference: Option<String>,
    /// creationTimestamp property.
    pub creation_timestamp: Option<String>,
    /// description property.
    pub description: Option<String>,
    /// fingerprint property.
    pub fingerprint: Option<String>,
    /// id property.
    pub id: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// network property.
    pub network: Option<String>,
    /// producerAcceptLists property.
    pub producer_accept_lists: Option<Vec<String>>,
    /// producerRejectLists property.
    pub producer_reject_lists: Option<Vec<String>>,
    /// region property.
    pub region: Option<String>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// selfLinkWithId property.
    pub self_link_with_id: Option<String>,
    /// subnetworks property.
    pub subnetworks: Option<Vec<String>>,
}

/// `VpnGatewaysGetStatusResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct VpnGatewaysGetStatusResponse {
    /// result property.
    pub result: Option<VpnGatewayStatus>,
}

/// `License` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct License {
    /// allowedReplacementLicenses property.
    pub allowed_replacement_licenses: Option<Vec<String>>,
    /// appendableToDisk property.
    pub appendable_to_disk: Option<bool>,
    /// chargesUseFee property.
    pub charges_use_fee: Option<bool>,
    /// creationTimestamp property.
    pub creation_timestamp: Option<String>,
    /// description property.
    pub description: Option<String>,
    /// id property.
    pub id: Option<String>,
    /// incompatibleLicenses property.
    pub incompatible_licenses: Option<Vec<String>>,
    /// kind property.
    pub kind: Option<String>,
    /// licenseCode property.
    pub license_code: Option<String>,
    /// minimumRetention property.
    pub minimum_retention: Option<Duration>,
    /// multiTenantOnly property.
    pub multi_tenant_only: Option<bool>,
    /// name property.
    pub name: Option<String>,
    /// osLicense property.
    pub os_license: Option<bool>,
    /// params property.
    pub params: Option<LicenseParams>,
    /// removableFromDisk property.
    pub removable_from_disk: Option<bool>,
    /// requiredCoattachedLicenses property.
    pub required_coattached_licenses: Option<Vec<String>>,
    /// resourceRequirements property.
    pub resource_requirements: Option<LicenseResourceRequirements>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// selfLinkWithId property.
    pub self_link_with_id: Option<String>,
    /// soleTenantOnly property.
    pub sole_tenant_only: Option<bool>,
    /// transferable property.
    pub transferable: Option<bool>,
    /// updateTimestamp property.
    pub update_timestamp: Option<String>,
}

/// `HTTP2HealthCheck` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct HTTP2HealthCheck {
    /// host property.
    pub host: Option<String>,
    /// port property.
    pub port: Option<i64>,
    /// portName property.
    pub port_name: Option<String>,
    /// portSpecification property.
    pub port_specification: Option<String>,
    /// proxyHeader property.
    pub proxy_header: Option<String>,
    /// requestPath property.
    pub request_path: Option<String>,
    /// response property.
    pub response: Option<String>,
}

/// `NotificationEndpointGrpcSettings` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct NotificationEndpointGrpcSettings {
    /// authority property.
    pub authority: Option<String>,
    /// endpoint property.
    pub endpoint: Option<String>,
    /// payloadName property.
    pub payload_name: Option<String>,
    /// resendInterval property.
    pub resend_interval: Option<Duration>,
    /// retryDurationSec property.
    pub retry_duration_sec: Option<i64>,
}

/// `VpnGatewayAggregatedList` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct VpnGatewayAggregatedList {
    /// id property.
    pub id: Option<String>,
    /// items property.
    pub items: Option<serde_json::Value>,
    /// kind property.
    pub kind: Option<String>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// unreachables property.
    pub unreachables: Option<Vec<String>>,
    /// warning property.
    pub warning: Option<std::collections::HashMap<String, serde_json::Value>>,
}

/// `FirewallPolicy` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct FirewallPolicy {
    /// associations property.
    pub associations: Option<Vec<FirewallPolicyAssociation>>,
    /// creationTimestamp property.
    pub creation_timestamp: Option<String>,
    /// description property.
    pub description: Option<String>,
    /// displayName property.
    pub display_name: Option<String>,
    /// fingerprint property.
    pub fingerprint: Option<String>,
    /// id property.
    pub id: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// packetMirroringRules property.
    pub packet_mirroring_rules: Option<Vec<FirewallPolicyRule>>,
    /// parent property.
    pub parent: Option<String>,
    /// policyType property.
    pub policy_type: Option<String>,
    /// region property.
    pub region: Option<String>,
    /// ruleTupleCount property.
    pub rule_tuple_count: Option<i64>,
    /// rules property.
    pub rules: Option<Vec<FirewallPolicyRule>>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// selfLinkWithId property.
    pub self_link_with_id: Option<String>,
    /// shortName property.
    pub short_name: Option<String>,
}

/// `RoutersListBgpRoutes` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct RoutersListBgpRoutes {
    /// etag property.
    pub etag: Option<String>,
    /// id property.
    pub id: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// result property.
    pub result: Option<Vec<BgpRoute>>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// unreachables property.
    pub unreachables: Option<Vec<String>>,
    /// warning property.
    pub warning: Option<std::collections::HashMap<String, serde_json::Value>>,
}

/// `HttpRouteRuleMatch` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct HttpRouteRuleMatch {
    /// fullPathMatch property.
    pub full_path_match: Option<String>,
    /// headerMatches property.
    pub header_matches: Option<Vec<HttpHeaderMatch>>,
    /// ignoreCase property.
    pub ignore_case: Option<bool>,
    /// metadataFilters property.
    pub metadata_filters: Option<Vec<MetadataFilter>>,
    /// pathTemplateMatch property.
    pub path_template_match: Option<String>,
    /// prefixMatch property.
    pub prefix_match: Option<String>,
    /// queryParameterMatches property.
    pub query_parameter_matches: Option<Vec<HttpQueryParameterMatch>>,
    /// regexMatch property.
    pub regex_match: Option<String>,
}

/// `BackendServiceIAP` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct BackendServiceIAP {
    /// enabled property.
    pub enabled: Option<bool>,
    /// oauth2ClientId property.
    pub oauth2_client_id: Option<String>,
    /// oauth2ClientSecret property.
    pub oauth2_client_secret: Option<String>,
    /// oauth2ClientSecretSha256 property.
    pub oauth2_client_secret_sha256: Option<String>,
}

/// `InstanceGroupManagersPatchPerInstanceConfigsReq` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct InstanceGroupManagersPatchPerInstanceConfigsReq {
    /// perInstanceConfigs property.
    pub per_instance_configs: Option<Vec<PerInstanceConfig>>,
}

/// `NetworkEndpointGroupsAttachEndpointsRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct NetworkEndpointGroupsAttachEndpointsRequest {
    /// networkEndpoints property.
    pub network_endpoints: Option<Vec<NetworkEndpoint>>,
}

/// `LocalDisk` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct LocalDisk {
    /// diskCount property.
    pub disk_count: Option<i64>,
    /// diskSizeGb property.
    pub disk_size_gb: Option<i64>,
    /// diskType property.
    pub disk_type: Option<String>,
}

/// `VmEndpointNatMappingsInterfaceNatMappings` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct VmEndpointNatMappingsInterfaceNatMappings {
    /// drainNatIpPortRanges property.
    pub drain_nat_ip_port_ranges: Option<Vec<String>>,
    /// natIpPortRanges property.
    pub nat_ip_port_ranges: Option<Vec<String>>,
    /// numTotalDrainNatPorts property.
    pub num_total_drain_nat_ports: Option<i64>,
    /// numTotalNatPorts property.
    pub num_total_nat_ports: Option<i64>,
    /// ruleMappings property.
    pub rule_mappings: Option<Vec<VmEndpointNatMappingsInterfaceNatMappingsNatRuleMappings>>,
    /// sourceAliasIpRange property.
    pub source_alias_ip_range: Option<String>,
    /// sourceVirtualIp property.
    pub source_virtual_ip: Option<String>,
}

/// `CorsPolicy` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct CorsPolicy {
    /// allowCredentials property.
    pub allow_credentials: Option<bool>,
    /// allowHeaders property.
    pub allow_headers: Option<Vec<String>>,
    /// allowMethods property.
    pub allow_methods: Option<Vec<String>>,
    /// allowOriginRegexes property.
    pub allow_origin_regexes: Option<Vec<String>>,
    /// allowOrigins property.
    pub allow_origins: Option<Vec<String>>,
    /// disabled property.
    pub disabled: Option<bool>,
    /// exposeHeaders property.
    pub expose_headers: Option<Vec<String>>,
    /// maxAge property.
    pub max_age: Option<i64>,
}

/// `PreviewFeatureRolloutOperation` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct PreviewFeatureRolloutOperation {
    /// rolloutInput property.
    pub rollout_input: Option<PreviewFeatureRolloutOperationRolloutInput>,
}

/// `InstantSnapshotList` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct InstantSnapshotList {
    /// id property.
    pub id: Option<String>,
    /// items property.
    pub items: Option<Vec<InstantSnapshot>>,
    /// kind property.
    pub kind: Option<String>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// warning property.
    pub warning: Option<std::collections::HashMap<String, serde_json::Value>>,
}

/// `SubnetworksScopedWarning` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SubnetworksScopedWarning {
    /// scopeName property.
    pub scope_name: Option<String>,
    /// warning property.
    pub warning: Option<std::collections::HashMap<String, serde_json::Value>>,
}

/// `InterconnectAttachmentPrivateInfo` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct InterconnectAttachmentPrivateInfo {
    /// tag8021q property.
    pub tag8021q: Option<i64>,
}

/// `NetworksGetEffectiveFirewallsResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct NetworksGetEffectiveFirewallsResponse {
    /// firewallPolicys property.
    pub firewall_policys: Option<Vec<NetworksGetEffectiveFirewallsResponseEffectiveFirewallPolicy>>,
    /// firewalls property.
    pub firewalls: Option<Vec<Firewall>>,
}

/// `HealthCheckServiceReference` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct HealthCheckServiceReference {
    /// healthCheckService property.
    pub health_check_service: Option<String>,
}

/// `Backend` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Backend {
    /// balancingMode property.
    pub balancing_mode: Option<String>,
    /// capacityScaler property.
    pub capacity_scaler: Option<f64>,
    /// customMetrics property.
    pub custom_metrics: Option<Vec<BackendCustomMetric>>,
    /// description property.
    pub description: Option<String>,
    /// failover property.
    pub failover: Option<bool>,
    /// group property.
    pub group: Option<String>,
    /// maxConnections property.
    pub max_connections: Option<i64>,
    /// maxConnectionsPerEndpoint property.
    pub max_connections_per_endpoint: Option<i64>,
    /// maxConnectionsPerInstance property.
    pub max_connections_per_instance: Option<i64>,
    /// maxInFlightRequests property.
    pub max_in_flight_requests: Option<i64>,
    /// maxInFlightRequestsPerEndpoint property.
    pub max_in_flight_requests_per_endpoint: Option<i64>,
    /// maxInFlightRequestsPerInstance property.
    pub max_in_flight_requests_per_instance: Option<i64>,
    /// maxRate property.
    pub max_rate: Option<i64>,
    /// maxRatePerEndpoint property.
    pub max_rate_per_endpoint: Option<f64>,
    /// maxRatePerInstance property.
    pub max_rate_per_instance: Option<f64>,
    /// maxUtilization property.
    pub max_utilization: Option<f64>,
    /// orchestrationInfo property.
    pub orchestration_info: Option<BackendBackendOrchestrationInfo>,
    /// preference property.
    pub preference: Option<String>,
    /// trafficDuration property.
    pub traffic_duration: Option<String>,
}

/// `InstanceGroupManagerStatusBulkInstanceOperationLastProgressCheck` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct InstanceGroupManagerStatusBulkInstanceOperationLastProgressCheck {
    /// error property.
    pub error: Option<std::collections::HashMap<String, serde_json::Value>>,
    /// timestamp property.
    pub timestamp: Option<String>,
}

/// `InterconnectApplicationAwareInterconnect` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct InterconnectApplicationAwareInterconnect {
    /// bandwidthPercentagePolicy property.
    pub bandwidth_percentage_policy:
        Option<InterconnectApplicationAwareInterconnectBandwidthPercentagePolicy>,
    /// profileDescription property.
    pub profile_description: Option<String>,
    /// shapeAveragePercentages property.
    pub shape_average_percentages:
        Option<Vec<InterconnectApplicationAwareInterconnectBandwidthPercentage>>,
    /// strictPriorityPolicy property.
    pub strict_priority_policy:
        Option<InterconnectApplicationAwareInterconnectStrictPriorityPolicy>,
}

/// `RegionInstanceGroupManagerDeleteInstanceConfigReq` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct RegionInstanceGroupManagerDeleteInstanceConfigReq {
    /// names property.
    pub names: Option<Vec<String>>,
}

/// `NetworkEndpointGroupsListNetworkEndpoints` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct NetworkEndpointGroupsListNetworkEndpoints {
    /// id property.
    pub id: Option<String>,
    /// items property.
    pub items: Option<Vec<NetworkEndpointWithHealthStatus>>,
    /// kind property.
    pub kind: Option<String>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// warning property.
    pub warning: Option<std::collections::HashMap<String, serde_json::Value>>,
}

/// `TestPermissionsRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct TestPermissionsRequest {
    /// permissions property.
    pub permissions: Option<Vec<String>>,
}

/// `HealthCheck` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct HealthCheck {
    /// checkIntervalSec property.
    pub check_interval_sec: Option<i64>,
    /// creationTimestamp property.
    pub creation_timestamp: Option<String>,
    /// description property.
    pub description: Option<String>,
    /// grpcHealthCheck property.
    pub grpc_health_check: Option<GRPCHealthCheck>,
    /// grpcTlsHealthCheck property.
    pub grpc_tls_health_check: Option<GRPCTLSHealthCheck>,
    /// healthyThreshold property.
    pub healthy_threshold: Option<i64>,
    /// http2HealthCheck property.
    pub http2_health_check: Option<HTTP2HealthCheck>,
    /// httpHealthCheck property.
    pub http_health_check: Option<HTTPHealthCheck>,
    /// httpsHealthCheck property.
    pub https_health_check: Option<HTTPSHealthCheck>,
    /// id property.
    pub id: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// logConfig property.
    pub log_config: Option<HealthCheckLogConfig>,
    /// name property.
    pub name: Option<String>,
    /// region property.
    pub region: Option<String>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// sourceRegions property.
    pub source_regions: Option<Vec<String>>,
    /// sslHealthCheck property.
    pub ssl_health_check: Option<SSLHealthCheck>,
    /// tcpHealthCheck property.
    pub tcp_health_check: Option<TCPHealthCheck>,
    /// timeoutSec property.
    pub timeout_sec: Option<i64>,
    /// type property.
    pub r#type: Option<String>,
    /// unhealthyThreshold property.
    pub unhealthy_threshold: Option<i64>,
}

/// `SecurityPolicyAssociation` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SecurityPolicyAssociation {
    /// attachmentId property.
    pub attachment_id: Option<String>,
    /// displayName property.
    pub display_name: Option<String>,
    /// excludedFolders property.
    pub excluded_folders: Option<Vec<String>>,
    /// excludedProjects property.
    pub excluded_projects: Option<Vec<String>>,
    /// name property.
    pub name: Option<String>,
    /// securityPolicyId property.
    pub security_policy_id: Option<String>,
    /// shortName property.
    pub short_name: Option<String>,
}

/// `PacketMirroringFilter` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct PacketMirroringFilter {
    /// IPProtocols property.
    pub ip_protocols: Option<Vec<String>>,
    /// cidrRanges property.
    pub cidr_ranges: Option<Vec<String>>,
    /// direction property.
    pub direction: Option<String>,
}

/// `InstantSnapshotAggregatedList` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct InstantSnapshotAggregatedList {
    /// id property.
    pub id: Option<String>,
    /// items property.
    pub items: Option<serde_json::Value>,
    /// kind property.
    pub kind: Option<String>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// unreachables property.
    pub unreachables: Option<Vec<String>>,
    /// warning property.
    pub warning: Option<std::collections::HashMap<String, serde_json::Value>>,
}

/// `RoutersPreviewResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct RoutersPreviewResponse {
    /// resource property.
    pub resource: Option<Router>,
}

/// `HTTPHealthCheck` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct HTTPHealthCheck {
    /// host property.
    pub host: Option<String>,
    /// port property.
    pub port: Option<i64>,
    /// portName property.
    pub port_name: Option<String>,
    /// portSpecification property.
    pub port_specification: Option<String>,
    /// proxyHeader property.
    pub proxy_header: Option<String>,
    /// requestPath property.
    pub request_path: Option<String>,
    /// response property.
    pub response: Option<String>,
}

/// `HttpFaultAbort` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct HttpFaultAbort {
    /// httpStatus property.
    pub http_status: Option<i64>,
    /// percentage property.
    pub percentage: Option<f64>,
}

/// `InterconnectDiagnostics` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct InterconnectDiagnostics {
    /// arpCaches property.
    pub arp_caches: Option<Vec<InterconnectDiagnosticsARPEntry>>,
    /// bundleAggregationType property.
    pub bundle_aggregation_type: Option<String>,
    /// bundleOperationalStatus property.
    pub bundle_operational_status: Option<String>,
    /// links property.
    pub links: Option<Vec<InterconnectDiagnosticsLinkStatus>>,
    /// macAddress property.
    pub mac_address: Option<String>,
}

/// `PacketMirroringMirroredResourceInfoInstanceInfo` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct PacketMirroringMirroredResourceInfoInstanceInfo {
    /// canonicalUrl property.
    pub canonical_url: Option<String>,
    /// url property.
    pub url: Option<String>,
}

/// `DiskUpdateKmsKeyRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct DiskUpdateKmsKeyRequest {
    /// kmsKeyName property.
    pub kms_key_name: Option<String>,
}

/// `HealthSource` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct HealthSource {
    /// creationTimestamp property.
    pub creation_timestamp: Option<String>,
    /// description property.
    pub description: Option<String>,
    /// fingerprint property.
    pub fingerprint: Option<String>,
    /// healthAggregationPolicy property.
    pub health_aggregation_policy: Option<String>,
    /// id property.
    pub id: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// region property.
    pub region: Option<String>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// selfLinkWithId property.
    pub self_link_with_id: Option<String>,
    /// sourceType property.
    pub source_type: Option<String>,
    /// sources property.
    pub sources: Option<Vec<String>>,
}

/// `HttpsHealthCheckList` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct HttpsHealthCheckList {
    /// id property.
    pub id: Option<String>,
    /// items property.
    pub items: Option<Vec<HttpsHealthCheck>>,
    /// kind property.
    pub kind: Option<String>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// warning property.
    pub warning: Option<std::collections::HashMap<String, serde_json::Value>>,
}

/// `Router` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Router {
    /// bgp property.
    pub bgp: Option<RouterBgp>,
    /// bgpPeers property.
    pub bgp_peers: Option<Vec<RouterBgpPeer>>,
    /// creationTimestamp property.
    pub creation_timestamp: Option<String>,
    /// description property.
    pub description: Option<String>,
    /// encryptedInterconnectRouter property.
    pub encrypted_interconnect_router: Option<bool>,
    /// id property.
    pub id: Option<String>,
    /// interfaces property.
    pub interfaces: Option<Vec<RouterInterface>>,
    /// kind property.
    pub kind: Option<String>,
    /// md5AuthenticationKeys property.
    pub md5_authentication_keys: Option<Vec<RouterMd5AuthenticationKey>>,
    /// name property.
    pub name: Option<String>,
    /// nats property.
    pub nats: Option<Vec<RouterNat>>,
    /// network property.
    pub network: Option<String>,
    /// params property.
    pub params: Option<RouterParams>,
    /// region property.
    pub region: Option<String>,
    /// selfLink property.
    pub self_link: Option<String>,
}

/// `RouterList` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct RouterList {
    /// id property.
    pub id: Option<String>,
    /// items property.
    pub items: Option<Vec<Router>>,
    /// kind property.
    pub kind: Option<String>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// warning property.
    pub warning: Option<std::collections::HashMap<String, serde_json::Value>>,
}

/// `NodeGroupMaintenanceWindow` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct NodeGroupMaintenanceWindow {
    /// maintenanceDuration property.
    pub maintenance_duration: Option<Duration>,
    /// startTime property.
    pub start_time: Option<String>,
}

/// `PacketMirroring` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct PacketMirroring {
    /// collectorIlb property.
    pub collector_ilb: Option<PacketMirroringForwardingRuleInfo>,
    /// creationTimestamp property.
    pub creation_timestamp: Option<String>,
    /// description property.
    pub description: Option<String>,
    /// enable property.
    pub enable: Option<String>,
    /// filter property.
    pub filter: Option<PacketMirroringFilter>,
    /// id property.
    pub id: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// mirroredResources property.
    pub mirrored_resources: Option<PacketMirroringMirroredResourceInfo>,
    /// name property.
    pub name: Option<String>,
    /// network property.
    pub network: Option<PacketMirroringNetworkInfo>,
    /// priority property.
    pub priority: Option<i64>,
    /// region property.
    pub region: Option<String>,
    /// selfLink property.
    pub self_link: Option<String>,
}

/// `InstanceGroupManagersListPerInstanceConfigsResp` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct InstanceGroupManagersListPerInstanceConfigsResp {
    /// items property.
    pub items: Option<Vec<PerInstanceConfig>>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// warning property.
    pub warning: Option<std::collections::HashMap<String, serde_json::Value>>,
}

/// `AttachedDiskInitializeParams` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AttachedDiskInitializeParams {
    /// architecture property.
    pub architecture: Option<String>,
    /// description property.
    pub description: Option<String>,
    /// diskName property.
    pub disk_name: Option<String>,
    /// diskSizeGb property.
    pub disk_size_gb: Option<String>,
    /// diskType property.
    pub disk_type: Option<String>,
    /// enableConfidentialCompute property.
    pub enable_confidential_compute: Option<bool>,
    /// labels property.
    pub labels: Option<serde_json::Value>,
    /// licenses property.
    pub licenses: Option<Vec<String>>,
    /// onUpdateAction property.
    pub on_update_action: Option<String>,
    /// provisionedIops property.
    pub provisioned_iops: Option<String>,
    /// provisionedThroughput property.
    pub provisioned_throughput: Option<String>,
    /// replicaZones property.
    pub replica_zones: Option<Vec<String>>,
    /// resourceManagerTags property.
    pub resource_manager_tags: Option<serde_json::Value>,
    /// resourcePolicies property.
    pub resource_policies: Option<Vec<String>>,
    /// sourceImage property.
    pub source_image: Option<String>,
    /// sourceImageEncryptionKey property.
    pub source_image_encryption_key: Option<CustomerEncryptionKey>,
    /// sourceSnapshot property.
    pub source_snapshot: Option<String>,
    /// sourceSnapshotEncryptionKey property.
    pub source_snapshot_encryption_key: Option<CustomerEncryptionKey>,
    /// storagePool property.
    pub storage_pool: Option<String>,
}

/// `ProjectsSetDefaultNetworkTierRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ProjectsSetDefaultNetworkTierRequest {
    /// networkTier property.
    pub network_tier: Option<String>,
}

/// `AutoscalingPolicyCustomMetricUtilization` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AutoscalingPolicyCustomMetricUtilization {
    /// filter property.
    pub filter: Option<String>,
    /// metric property.
    pub metric: Option<String>,
    /// singleInstanceAssignment property.
    pub single_instance_assignment: Option<f64>,
    /// utilizationTarget property.
    pub utilization_target: Option<f64>,
    /// utilizationTargetType property.
    pub utilization_target_type: Option<String>,
}

/// `InstanceManagedByIgmErrorManagedInstanceError` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct InstanceManagedByIgmErrorManagedInstanceError {
    /// code property.
    pub code: Option<String>,
    /// message property.
    pub message: Option<String>,
}

/// `RegionInstanceGroupManagersSetTargetPoolsRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct RegionInstanceGroupManagersSetTargetPoolsRequest {
    /// fingerprint property.
    pub fingerprint: Option<String>,
    /// targetPools property.
    pub target_pools: Option<Vec<String>>,
}

/// `InterconnectRemoteLocationConstraintsSubnetLengthRange` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct InterconnectRemoteLocationConstraintsSubnetLengthRange {
    /// max property.
    pub max: Option<i64>,
    /// min property.
    pub min: Option<i64>,
}

/// `FirewallPolicyRuleSecureTag` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct FirewallPolicyRuleSecureTag {
    /// name property.
    pub name: Option<String>,
    /// state property.
    pub state: Option<String>,
}

/// `SchedulingOnInstanceStopAction` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SchedulingOnInstanceStopAction {
    /// discardLocalSsd property.
    pub discard_local_ssd: Option<bool>,
}

/// `NodeGroupAutoscalingPolicy` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct NodeGroupAutoscalingPolicy {
    /// maxNodes property.
    pub max_nodes: Option<i64>,
    /// minNodes property.
    pub min_nodes: Option<i64>,
    /// mode property.
    pub mode: Option<String>,
}

/// `InstanceGroupManagerStatusInstanceStatusSummary` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct InstanceGroupManagerStatusInstanceStatusSummary {
    /// deprovisioning property.
    pub deprovisioning: Option<i64>,
    /// nonExistent property.
    pub non_existent: Option<i64>,
    /// pending property.
    pub pending: Option<i64>,
    /// pendingStop property.
    pub pending_stop: Option<i64>,
    /// provisioning property.
    pub provisioning: Option<i64>,
    /// repairing property.
    pub repairing: Option<i64>,
    /// running property.
    pub running: Option<i64>,
    /// staging property.
    pub staging: Option<i64>,
    /// stopped property.
    pub stopped: Option<i64>,
    /// stopping property.
    pub stopping: Option<i64>,
    /// suspended property.
    pub suspended: Option<i64>,
    /// suspending property.
    pub suspending: Option<i64>,
    /// terminated property.
    pub terminated: Option<i64>,
}

/// `RegionDisksResizeRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct RegionDisksResizeRequest {
    /// sizeGb property.
    pub size_gb: Option<String>,
}

/// `Project` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Project {
    /// cloudArmorTier property.
    pub cloud_armor_tier: Option<String>,
    /// commonInstanceMetadata property.
    pub common_instance_metadata: Option<Metadata>,
    /// creationTimestamp property.
    pub creation_timestamp: Option<String>,
    /// defaultNetworkTier property.
    pub default_network_tier: Option<String>,
    /// defaultServiceAccount property.
    pub default_service_account: Option<String>,
    /// description property.
    pub description: Option<String>,
    /// enabledFeatures property.
    pub enabled_features: Option<Vec<String>>,
    /// id property.
    pub id: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// quotas property.
    pub quotas: Option<Vec<Quota>>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// usageExportLocation property.
    pub usage_export_location: Option<UsageExportLocation>,
    /// vmDnsSetting property.
    pub vm_dns_setting: Option<String>,
    /// xpnProjectStatus property.
    pub xpn_project_status: Option<String>,
}

/// `RoutersListRoutePolicies` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct RoutersListRoutePolicies {
    /// etag property.
    pub etag: Option<String>,
    /// id property.
    pub id: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// result property.
    pub result: Option<Vec<RoutePolicy>>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// unreachables property.
    pub unreachables: Option<Vec<String>>,
    /// warning property.
    pub warning: Option<std::collections::HashMap<String, serde_json::Value>>,
}

/// `ReservationSubBlocksGetResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ReservationSubBlocksGetResponse {
    /// resource property.
    pub resource: Option<ReservationSubBlock>,
}

/// `RegionDiskTypeList` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct RegionDiskTypeList {
    /// id property.
    pub id: Option<String>,
    /// items property.
    pub items: Option<Vec<DiskType>>,
    /// kind property.
    pub kind: Option<String>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// warning property.
    pub warning: Option<std::collections::HashMap<String, serde_json::Value>>,
}

/// `RouterMd5AuthenticationKey` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct RouterMd5AuthenticationKey {
    /// key property.
    pub key: Option<String>,
    /// name property.
    pub name: Option<String>,
}

/// `HealthSourceList` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct HealthSourceList {
    /// id property.
    pub id: Option<String>,
    /// items property.
    pub items: Option<Vec<HealthSource>>,
    /// kind property.
    pub kind: Option<String>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// warning property.
    pub warning: Option<std::collections::HashMap<String, serde_json::Value>>,
}

/// `VpnGateway` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct VpnGateway {
    /// creationTimestamp property.
    pub creation_timestamp: Option<String>,
    /// description property.
    pub description: Option<String>,
    /// gatewayIpVersion property.
    pub gateway_ip_version: Option<String>,
    /// id property.
    pub id: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// labelFingerprint property.
    pub label_fingerprint: Option<String>,
    /// labels property.
    pub labels: Option<serde_json::Value>,
    /// name property.
    pub name: Option<String>,
    /// network property.
    pub network: Option<String>,
    /// params property.
    pub params: Option<VpnGatewayParams>,
    /// region property.
    pub region: Option<String>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// stackType property.
    pub stack_type: Option<String>,
    /// vpnInterfaces property.
    pub vpn_interfaces: Option<Vec<VpnGatewayVpnGatewayInterface>>,
}

/// `InterconnectAttachmentParams` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct InterconnectAttachmentParams {
    /// resourceManagerTags property.
    pub resource_manager_tags: Option<serde_json::Value>,
}

/// `VmEndpointNatMappingsInterfaceNatMappingsNatRuleMappings` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct VmEndpointNatMappingsInterfaceNatMappingsNatRuleMappings {
    /// drainNatIpPortRanges property.
    pub drain_nat_ip_port_ranges: Option<Vec<String>>,
    /// natIpPortRanges property.
    pub nat_ip_port_ranges: Option<Vec<String>>,
    /// numTotalDrainNatPorts property.
    pub num_total_drain_nat_ports: Option<i64>,
    /// numTotalNatPorts property.
    pub num_total_nat_ports: Option<i64>,
    /// ruleNumber property.
    pub rule_number: Option<i64>,
}

/// `StoragePoolResourceStatus` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct StoragePoolResourceStatus {
    /// diskCount property.
    pub disk_count: Option<String>,
    /// exapoolMaxReadIops property.
    pub exapool_max_read_iops: Option<String>,
    /// exapoolMaxReadThroughput property.
    pub exapool_max_read_throughput: Option<String>,
    /// exapoolMaxWriteIops property.
    pub exapool_max_write_iops: Option<String>,
    /// exapoolMaxWriteThroughput property.
    pub exapool_max_write_throughput: Option<String>,
    /// lastResizeTimestamp property.
    pub last_resize_timestamp: Option<String>,
    /// maxTotalProvisionedDiskCapacityGb property.
    pub max_total_provisioned_disk_capacity_gb: Option<String>,
    /// poolUsedCapacityBytes property.
    pub pool_used_capacity_bytes: Option<String>,
    /// poolUsedIops property.
    pub pool_used_iops: Option<String>,
    /// poolUsedThroughput property.
    pub pool_used_throughput: Option<String>,
    /// poolUserWrittenBytes property.
    pub pool_user_written_bytes: Option<String>,
    /// totalProvisionedDiskCapacityGb property.
    pub total_provisioned_disk_capacity_gb: Option<String>,
    /// totalProvisionedDiskIops property.
    pub total_provisioned_disk_iops: Option<String>,
    /// totalProvisionedDiskThroughput property.
    pub total_provisioned_disk_throughput: Option<String>,
}

/// `AllocationSpecificSKUAllocationAllocatedInstancePropertiesReservedDisk` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AllocationSpecificSKUAllocationAllocatedInstancePropertiesReservedDisk {
    /// diskSizeGb property.
    pub disk_size_gb: Option<String>,
    /// interface property.
    pub interface: Option<String>,
}

/// `TargetPoolsRemoveHealthCheckRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct TargetPoolsRemoveHealthCheckRequest {
    /// healthChecks property.
    pub health_checks: Option<Vec<HealthCheckReference>>,
}

/// `VpnTunnelCipherSuite` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct VpnTunnelCipherSuite {
    /// phase1 property.
    pub phase1: Option<VpnTunnelPhase1Algorithms>,
    /// phase2 property.
    pub phase2: Option<VpnTunnelPhase2Algorithms>,
}

/// `InterconnectMacsec` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct InterconnectMacsec {
    /// failOpen property.
    pub fail_open: Option<bool>,
    /// preSharedKeys property.
    pub pre_shared_keys: Option<Vec<InterconnectMacsecPreSharedKey>>,
}

/// `InterconnectGroupPhysicalStructureMetros` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct InterconnectGroupPhysicalStructureMetros {
    /// facilities property.
    pub facilities: Option<Vec<InterconnectGroupPhysicalStructureMetrosFacilities>>,
    /// metro property.
    pub metro: Option<String>,
}

/// `ProjectsGetXpnResources` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ProjectsGetXpnResources {
    /// kind property.
    pub kind: Option<String>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// resources property.
    pub resources: Option<Vec<XpnResourceId>>,
}

/// `ReservationSlotPhysicalTopology` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ReservationSlotPhysicalTopology {
    /// block property.
    pub block: Option<String>,
    /// cluster property.
    pub cluster: Option<String>,
    /// host property.
    pub host: Option<String>,
    /// subBlock property.
    pub sub_block: Option<String>,
}

/// `InstanceGroupManagerStatus` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct InstanceGroupManagerStatus {
    /// allInstancesConfig property.
    pub all_instances_config: Option<InstanceGroupManagerStatusAllInstancesConfig>,
    /// appliedAcceleratorTopologies property.
    pub applied_accelerator_topologies: Option<Vec<InstanceGroupManagerStatusAcceleratorTopology>>,
    /// autoscaler property.
    pub autoscaler: Option<String>,
    /// bulkInstanceOperation property.
    pub bulk_instance_operation: Option<InstanceGroupManagerStatusBulkInstanceOperation>,
    /// currentInstanceStatuses property.
    pub current_instance_statuses: Option<InstanceGroupManagerStatusInstanceStatusSummary>,
    /// isStable property.
    pub is_stable: Option<bool>,
    /// stateful property.
    pub stateful: Option<InstanceGroupManagerStatusStateful>,
    /// versionTarget property.
    pub version_target: Option<InstanceGroupManagerStatusVersionTarget>,
}

/// `GlobalNetworkEndpointGroupsDetachEndpointsRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GlobalNetworkEndpointGroupsDetachEndpointsRequest {
    /// networkEndpoints property.
    pub network_endpoints: Option<Vec<NetworkEndpoint>>,
}

/// `InstanceGroupManagersSetInstanceTemplateRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct InstanceGroupManagersSetInstanceTemplateRequest {
    /// instanceTemplate property.
    pub instance_template: Option<String>,
}

/// `DiskTypeList` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct DiskTypeList {
    /// id property.
    pub id: Option<String>,
    /// items property.
    pub items: Option<Vec<DiskType>>,
    /// kind property.
    pub kind: Option<String>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// warning property.
    pub warning: Option<std::collections::HashMap<String, serde_json::Value>>,
}

/// `Metadata` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Metadata {
    /// fingerprint property.
    pub fingerprint: Option<String>,
    /// items property.
    pub items: Option<Vec<std::collections::HashMap<String, serde_json::Value>>>,
    /// kind property.
    pub kind: Option<String>,
}

/// `InstancesSetSecurityPolicyRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct InstancesSetSecurityPolicyRequest {
    /// networkInterfaces property.
    pub network_interfaces: Option<Vec<String>>,
    /// securityPolicy property.
    pub security_policy: Option<String>,
}

/// `RegionList` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct RegionList {
    /// id property.
    pub id: Option<String>,
    /// items property.
    pub items: Option<Vec<Region>>,
    /// kind property.
    pub kind: Option<String>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// warning property.
    pub warning: Option<std::collections::HashMap<String, serde_json::Value>>,
}

/// `InstancesSetMachineTypeRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct InstancesSetMachineTypeRequest {
    /// machineType property.
    pub machine_type: Option<String>,
}

/// `RouterStatusNatStatusNatRuleStatus` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct RouterStatusNatStatusNatRuleStatus {
    /// activeNatIps property.
    pub active_nat_ips: Option<Vec<String>>,
    /// drainNatIps property.
    pub drain_nat_ips: Option<Vec<String>>,
    /// minExtraIpsNeeded property.
    pub min_extra_ips_needed: Option<i64>,
    /// numVmEndpointsWithNatMappings property.
    pub num_vm_endpoints_with_nat_mappings: Option<i64>,
    /// ruleNumber property.
    pub rule_number: Option<i64>,
}

/// `AllocationReservationSharingPolicy` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AllocationReservationSharingPolicy {
    /// serviceShareType property.
    pub service_share_type: Option<String>,
}

/// `ForwardingRuleList` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ForwardingRuleList {
    /// id property.
    pub id: Option<String>,
    /// items property.
    pub items: Option<Vec<ForwardingRule>>,
    /// kind property.
    pub kind: Option<String>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// warning property.
    pub warning: Option<std::collections::HashMap<String, serde_json::Value>>,
}

/// `SnapshotSettings` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SnapshotSettings {
    /// accessLocation property.
    pub access_location: Option<SnapshotSettingsAccessLocation>,
    /// storageLocation property.
    pub storage_location: Option<SnapshotSettingsStorageLocationSettings>,
}

/// `TargetHttpsProxyList` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct TargetHttpsProxyList {
    /// id property.
    pub id: Option<String>,
    /// items property.
    pub items: Option<Vec<TargetHttpsProxy>>,
    /// kind property.
    pub kind: Option<String>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// warning property.
    pub warning: Option<std::collections::HashMap<String, serde_json::Value>>,
}

/// `SecurityPolicyRuleHttpHeaderActionHttpHeaderOption` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SecurityPolicyRuleHttpHeaderActionHttpHeaderOption {
    /// headerName property.
    pub header_name: Option<String>,
    /// headerValue property.
    pub header_value: Option<String>,
}

/// `InstancesRemoveResourcePoliciesRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct InstancesRemoveResourcePoliciesRequest {
    /// resourcePolicies property.
    pub resource_policies: Option<Vec<String>>,
}

/// `ZoneSetNestedPolicyRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ZoneSetNestedPolicyRequest {
    /// bindings property.
    pub bindings: Option<Vec<Binding>>,
    /// etag property.
    pub etag: Option<String>,
    /// policy property.
    pub policy: Option<Policy>,
}

/// `RouterBgp` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct RouterBgp {
    /// advertiseMode property.
    pub advertise_mode: Option<String>,
    /// advertisedGroups property.
    pub advertised_groups: Option<Vec<String>>,
    /// advertisedIpRanges property.
    pub advertised_ip_ranges: Option<Vec<RouterAdvertisedIpRange>>,
    /// asn property.
    pub asn: Option<i64>,
    /// identifierRange property.
    pub identifier_range: Option<String>,
    /// keepaliveInterval property.
    pub keepalive_interval: Option<i64>,
}

/// `Duration` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Duration {
    /// nanos property.
    pub nanos: Option<i64>,
    /// seconds property.
    pub seconds: Option<String>,
}

/// `CircuitBreakers` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct CircuitBreakers {
    /// maxConnections property.
    pub max_connections: Option<i64>,
    /// maxPendingRequests property.
    pub max_pending_requests: Option<i64>,
    /// maxRequests property.
    pub max_requests: Option<i64>,
    /// maxRequestsPerConnection property.
    pub max_requests_per_connection: Option<i64>,
    /// maxRetries property.
    pub max_retries: Option<i64>,
}

/// `NetworkProfileNetworkFeatures` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct NetworkProfileNetworkFeatures {
    /// addressPurposes property.
    pub address_purposes: Option<Vec<String>>,
    /// allowAddressCreation property.
    pub allow_address_creation: Option<String>,
    /// allowAliasIpRanges property.
    pub allow_alias_ip_ranges: Option<String>,
    /// allowAutoModeSubnet property.
    pub allow_auto_mode_subnet: Option<String>,
    /// allowClassDFirewalls property.
    pub allow_class_d_firewalls: Option<String>,
    /// allowCloudNat property.
    pub allow_cloud_nat: Option<String>,
    /// allowCloudRouter property.
    pub allow_cloud_router: Option<String>,
    /// allowDefaultNicAttachment property.
    pub allow_default_nic_attachment: Option<String>,
    /// allowExternalIpAccess property.
    pub allow_external_ip_access: Option<String>,
    /// allowFirewallPolicy property.
    pub allow_firewall_policy: Option<String>,
    /// allowInterconnect property.
    pub allow_interconnect: Option<String>,
    /// allowIpForwarding property.
    pub allow_ip_forwarding: Option<String>,
    /// allowLoadBalancing property.
    pub allow_load_balancing: Option<String>,
    /// allowMultiNicInSameNetwork property.
    pub allow_multi_nic_in_same_network: Option<String>,
    /// allowMultiNicInSameSubnetwork property.
    pub allow_multi_nic_in_same_subnetwork: Option<String>,
    /// allowMulticast property.
    pub allow_multicast: Option<String>,
    /// allowNcc property.
    pub allow_ncc: Option<String>,
    /// allowNetworkMigration property.
    pub allow_network_migration: Option<String>,
    /// allowPacketMirroring property.
    pub allow_packet_mirroring: Option<String>,
    /// allowPrivateGoogleAccess property.
    pub allow_private_google_access: Option<String>,
    /// allowPsc property.
    pub allow_psc: Option<String>,
    /// allowSameNetworkUnicast property.
    pub allow_same_network_unicast: Option<String>,
    /// allowStaticRoutes property.
    pub allow_static_routes: Option<String>,
    /// allowSubInterfaces property.
    pub allow_sub_interfaces: Option<String>,
    /// allowSubnetworkCreation property.
    pub allow_subnetwork_creation: Option<String>,
    /// allowVpcFirewallRules property.
    pub allow_vpc_firewall_rules: Option<String>,
    /// allowVpcPeering property.
    pub allow_vpc_peering: Option<String>,
    /// allowVpn property.
    pub allow_vpn: Option<String>,
    /// firewallPolicyTypes property.
    pub firewall_policy_types: Option<Vec<String>>,
    /// interfaceTypes property.
    pub interface_types: Option<Vec<String>>,
    /// multicast property.
    pub multicast: Option<String>,
    /// predefinedNetworkInternalIpv6Range property.
    pub predefined_network_internal_ipv6_range: Option<String>,
    /// predefinedSubnetworkRanges property.
    pub predefined_subnetwork_ranges:
        Option<Vec<NetworkProfileNetworkFeaturesPredefinedSubnetworkRange>>,
    /// subnetPurposes property.
    pub subnet_purposes: Option<Vec<String>>,
    /// subnetStackTypes property.
    pub subnet_stack_types: Option<Vec<String>>,
    /// subnetworkPurposes property.
    pub subnetwork_purposes: Option<Vec<String>>,
    /// subnetworkStackTypes property.
    pub subnetwork_stack_types: Option<Vec<String>>,
    /// unicast property.
    pub unicast: Option<String>,
}

/// `FutureReservationTimeWindow` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct FutureReservationTimeWindow {
    /// duration property.
    pub duration: Option<Duration>,
    /// endTime property.
    pub end_time: Option<String>,
    /// startTime property.
    pub start_time: Option<String>,
}

/// `ReservationSubBlocksReportFaultyRequestFaultReason` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ReservationSubBlocksReportFaultyRequestFaultReason {
    /// behavior property.
    pub behavior: Option<String>,
    /// description property.
    pub description: Option<String>,
}

/// `InstanceGroupManagersStopInstancesRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct InstanceGroupManagersStopInstancesRequest {
    /// forceStop property.
    pub force_stop: Option<bool>,
    /// instances property.
    pub instances: Option<Vec<String>>,
}

/// `InstantSnapshotGroup` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct InstantSnapshotGroup {
    /// creationTimestamp property.
    pub creation_timestamp: Option<String>,
    /// description property.
    pub description: Option<String>,
    /// id property.
    pub id: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// region property.
    pub region: Option<String>,
    /// resourceStatus property.
    pub resource_status: Option<InstantSnapshotGroupResourceStatus>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// selfLinkWithId property.
    pub self_link_with_id: Option<String>,
    /// sourceConsistencyGroup property.
    pub source_consistency_group: Option<String>,
    /// status property.
    pub status: Option<String>,
    /// zone property.
    pub zone: Option<String>,
}

/// `PublicAdvertisedPrefixList` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct PublicAdvertisedPrefixList {
    /// id property.
    pub id: Option<String>,
    /// items property.
    pub items: Option<Vec<PublicAdvertisedPrefix>>,
    /// kind property.
    pub kind: Option<String>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// warning property.
    pub warning: Option<std::collections::HashMap<String, serde_json::Value>>,
}

/// `DiskAggregatedList` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct DiskAggregatedList {
    /// id property.
    pub id: Option<String>,
    /// items property.
    pub items: Option<serde_json::Value>,
    /// kind property.
    pub kind: Option<String>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// unreachables property.
    pub unreachables: Option<Vec<String>>,
    /// warning property.
    pub warning: Option<std::collections::HashMap<String, serde_json::Value>>,
}

/// `TargetInstanceAggregatedList` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct TargetInstanceAggregatedList {
    /// id property.
    pub id: Option<String>,
    /// items property.
    pub items: Option<serde_json::Value>,
    /// kind property.
    pub kind: Option<String>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// unreachables property.
    pub unreachables: Option<Vec<String>>,
    /// warning property.
    pub warning: Option<std::collections::HashMap<String, serde_json::Value>>,
}

/// `FirewallPolicyRuleMatcher` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct FirewallPolicyRuleMatcher {
    /// destAddressGroups property.
    pub dest_address_groups: Option<Vec<String>>,
    /// destFqdns property.
    pub dest_fqdns: Option<Vec<String>>,
    /// destIpRanges property.
    pub dest_ip_ranges: Option<Vec<String>>,
    /// destNetworkContext property.
    pub dest_network_context: Option<String>,
    /// destNetworkType property.
    pub dest_network_type: Option<String>,
    /// destRegionCodes property.
    pub dest_region_codes: Option<Vec<String>>,
    /// destThreatIntelligences property.
    pub dest_threat_intelligences: Option<Vec<String>>,
    /// layer4Configs property.
    pub layer4_configs: Option<Vec<FirewallPolicyRuleMatcherLayer4Config>>,
    /// srcAddressGroups property.
    pub src_address_groups: Option<Vec<String>>,
    /// srcFqdns property.
    pub src_fqdns: Option<Vec<String>>,
    /// srcIpRanges property.
    pub src_ip_ranges: Option<Vec<String>>,
    /// srcNetworkContext property.
    pub src_network_context: Option<String>,
    /// srcNetworkType property.
    pub src_network_type: Option<String>,
    /// srcNetworks property.
    pub src_networks: Option<Vec<String>>,
    /// srcRegionCodes property.
    pub src_region_codes: Option<Vec<String>>,
    /// srcSecureTags property.
    pub src_secure_tags: Option<Vec<FirewallPolicyRuleSecureTag>>,
    /// srcThreatIntelligences property.
    pub src_threat_intelligences: Option<Vec<String>>,
}

/// `BackendServiceLocalityLoadBalancingPolicyConfigCustomPolicy` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct BackendServiceLocalityLoadBalancingPolicyConfigCustomPolicy {
    /// data property.
    pub data: Option<String>,
    /// name property.
    pub name: Option<String>,
}

/// `NodeGroupsListNodes` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct NodeGroupsListNodes {
    /// id property.
    pub id: Option<String>,
    /// items property.
    pub items: Option<Vec<NodeGroupNode>>,
    /// kind property.
    pub kind: Option<String>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// warning property.
    pub warning: Option<std::collections::HashMap<String, serde_json::Value>>,
}

/// `ServiceAccount` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ServiceAccount {
    /// email property.
    pub email: Option<String>,
    /// scopes property.
    pub scopes: Option<Vec<String>>,
}

/// `Reference` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Reference {
    /// kind property.
    pub kind: Option<String>,
    /// referenceType property.
    pub reference_type: Option<String>,
    /// referrer property.
    pub referrer: Option<String>,
    /// target property.
    pub target: Option<String>,
}

/// `TargetInstanceList` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct TargetInstanceList {
    /// id property.
    pub id: Option<String>,
    /// items property.
    pub items: Option<Vec<TargetInstance>>,
    /// kind property.
    pub kind: Option<String>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// warning property.
    pub warning: Option<std::collections::HashMap<String, serde_json::Value>>,
}

/// `StoragePoolDisk` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct StoragePoolDisk {
    /// attachedInstances property.
    pub attached_instances: Option<Vec<String>>,
    /// creationTimestamp property.
    pub creation_timestamp: Option<String>,
    /// disk property.
    pub disk: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// provisionedIops property.
    pub provisioned_iops: Option<String>,
    /// provisionedThroughput property.
    pub provisioned_throughput: Option<String>,
    /// resourcePolicies property.
    pub resource_policies: Option<Vec<String>>,
    /// sizeGb property.
    pub size_gb: Option<String>,
    /// status property.
    pub status: Option<String>,
    /// type property.
    pub r#type: Option<String>,
    /// usedBytes property.
    pub used_bytes: Option<String>,
}

/// `FirewallList` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct FirewallList {
    /// id property.
    pub id: Option<String>,
    /// items property.
    pub items: Option<Vec<Firewall>>,
    /// kind property.
    pub kind: Option<String>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// warning property.
    pub warning: Option<std::collections::HashMap<String, serde_json::Value>>,
}

/// `Wire` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Wire {
    /// adminEnabled property.
    pub admin_enabled: Option<bool>,
    /// endpoints property.
    pub endpoints: Option<Vec<WireEndpoint>>,
    /// label property.
    pub label: Option<String>,
    /// wireProperties property.
    pub wire_properties: Option<WireProperties>,
}

/// `CommitmentResourceStatus` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct CommitmentResourceStatus {
    /// customTermEligibilityEndTimestamp property.
    pub custom_term_eligibility_end_timestamp: Option<String>,
}

/// `VmEndpointNatMappings` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct VmEndpointNatMappings {
    /// instanceName property.
    pub instance_name: Option<String>,
    /// interfaceNatMappings property.
    pub interface_nat_mappings: Option<Vec<VmEndpointNatMappingsInterfaceNatMappings>>,
}

/// `FutureReservation` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct FutureReservation {
    /// aggregateReservation property.
    pub aggregate_reservation: Option<AllocationAggregateReservation>,
    /// autoCreatedReservationsDeleteTime property.
    pub auto_created_reservations_delete_time: Option<String>,
    /// autoCreatedReservationsDuration property.
    pub auto_created_reservations_duration: Option<Duration>,
    /// autoDeleteAutoCreatedReservations property.
    pub auto_delete_auto_created_reservations: Option<bool>,
    /// commitmentInfo property.
    pub commitment_info: Option<FutureReservationCommitmentInfo>,
    /// confidentialComputeType property.
    pub confidential_compute_type: Option<String>,
    /// creationTimestamp property.
    pub creation_timestamp: Option<String>,
    /// deploymentType property.
    pub deployment_type: Option<String>,
    /// description property.
    pub description: Option<String>,
    /// enableEmergentMaintenance property.
    pub enable_emergent_maintenance: Option<bool>,
    /// id property.
    pub id: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// namePrefix property.
    pub name_prefix: Option<String>,
    /// params property.
    pub params: Option<FutureReservationParams>,
    /// planningStatus property.
    pub planning_status: Option<String>,
    /// reservationMode property.
    pub reservation_mode: Option<String>,
    /// reservationName property.
    pub reservation_name: Option<String>,
    /// schedulingType property.
    pub scheduling_type: Option<String>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// selfLinkWithId property.
    pub self_link_with_id: Option<String>,
    /// shareSettings property.
    pub share_settings: Option<ShareSettings>,
    /// specificReservationRequired property.
    pub specific_reservation_required: Option<bool>,
    /// specificSkuProperties property.
    pub specific_sku_properties: Option<FutureReservationSpecificSKUProperties>,
    /// status property.
    pub status: Option<FutureReservationStatus>,
    /// timeWindow property.
    pub time_window: Option<FutureReservationTimeWindow>,
    /// zone property.
    pub zone: Option<String>,
}

/// `AllocationAggregateReservationReservedResourceInfoAccelerator` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AllocationAggregateReservationReservedResourceInfoAccelerator {
    /// acceleratorCount property.
    pub accelerator_count: Option<i64>,
    /// acceleratorType property.
    pub accelerator_type: Option<String>,
}

/// `AcceleratorTopologiesInfoAcceleratorTopologyInfo` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AcceleratorTopologiesInfoAcceleratorTopologyInfo {
    /// acceleratorTopology property.
    pub accelerator_topology: Option<String>,
    /// infoPerTopologyStates property.
    pub info_per_topology_states:
        Option<Vec<AcceleratorTopologiesInfoAcceleratorTopologyInfoInfoPerTopologyState>>,
}

/// `ResourcePolicyWorkloadPolicy` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ResourcePolicyWorkloadPolicy {
    /// acceleratorTopology property.
    pub accelerator_topology: Option<String>,
    /// acceleratorTopologyMode property.
    pub accelerator_topology_mode: Option<String>,
    /// maxTopologyDistance property.
    pub max_topology_distance: Option<String>,
    /// type property.
    pub r#type: Option<String>,
}

/// `TargetGrpcProxyList` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct TargetGrpcProxyList {
    /// id property.
    pub id: Option<String>,
    /// items property.
    pub items: Option<Vec<TargetGrpcProxy>>,
    /// kind property.
    pub kind: Option<String>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// warning property.
    pub warning: Option<std::collections::HashMap<String, serde_json::Value>>,
}

/// `InterconnectGroup` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct InterconnectGroup {
    /// configured property.
    pub configured: Option<InterconnectGroupConfigured>,
    /// creationTimestamp property.
    pub creation_timestamp: Option<String>,
    /// description property.
    pub description: Option<String>,
    /// etag property.
    pub etag: Option<String>,
    /// id property.
    pub id: Option<String>,
    /// intent property.
    pub intent: Option<InterconnectGroupIntent>,
    /// interconnects property.
    pub interconnects: Option<serde_json::Value>,
    /// kind property.
    pub kind: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// physicalStructure property.
    pub physical_structure: Option<InterconnectGroupPhysicalStructure>,
    /// selfLink property.
    pub self_link: Option<String>,
}

/// `LicensesListResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct LicensesListResponse {
    /// id property.
    pub id: Option<String>,
    /// items property.
    pub items: Option<Vec<License>>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// warning property.
    pub warning: Option<std::collections::HashMap<String, serde_json::Value>>,
}

/// `MachineImage` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct MachineImage {
    /// creationTimestamp property.
    pub creation_timestamp: Option<String>,
    /// description property.
    pub description: Option<String>,
    /// guestFlush property.
    pub guest_flush: Option<bool>,
    /// id property.
    pub id: Option<String>,
    /// instanceProperties property.
    pub instance_properties: Option<InstanceProperties>,
    /// kind property.
    pub kind: Option<String>,
    /// labelFingerprint property.
    pub label_fingerprint: Option<String>,
    /// labels property.
    pub labels: Option<serde_json::Value>,
    /// machineImageEncryptionKey property.
    pub machine_image_encryption_key: Option<CustomerEncryptionKey>,
    /// name property.
    pub name: Option<String>,
    /// params property.
    pub params: Option<MachineImageParams>,
    /// satisfiesPzi property.
    pub satisfies_pzi: Option<bool>,
    /// satisfiesPzs property.
    pub satisfies_pzs: Option<bool>,
    /// savedDisks property.
    pub saved_disks: Option<Vec<SavedDisk>>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// sourceDiskEncryptionKeys property.
    pub source_disk_encryption_keys: Option<Vec<SourceDiskEncryptionKey>>,
    /// sourceInstance property.
    pub source_instance: Option<String>,
    /// sourceInstanceProperties property.
    pub source_instance_properties: Option<SourceInstanceProperties>,
    /// status property.
    pub status: Option<String>,
    /// storageLocations property.
    pub storage_locations: Option<Vec<String>>,
    /// totalStorageBytes property.
    pub total_storage_bytes: Option<String>,
}

/// `NodeTypeList` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct NodeTypeList {
    /// id property.
    pub id: Option<String>,
    /// items property.
    pub items: Option<Vec<NodeType>>,
    /// kind property.
    pub kind: Option<String>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// warning property.
    pub warning: Option<std::collections::HashMap<String, serde_json::Value>>,
}

/// `RegionInstanceGroupManagersListInstanceConfigsResp` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct RegionInstanceGroupManagersListInstanceConfigsResp {
    /// items property.
    pub items: Option<Vec<PerInstanceConfig>>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// warning property.
    pub warning: Option<std::collections::HashMap<String, serde_json::Value>>,
}

/// `RouterNatRuleAction` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct RouterNatRuleAction {
    /// sourceNatActiveIps property.
    pub source_nat_active_ips: Option<Vec<String>>,
    /// sourceNatActiveRanges property.
    pub source_nat_active_ranges: Option<Vec<String>>,
    /// sourceNatDrainIps property.
    pub source_nat_drain_ips: Option<Vec<String>>,
    /// sourceNatDrainRanges property.
    pub source_nat_drain_ranges: Option<Vec<String>>,
}

/// `BackendServiceCdnPolicyBypassCacheOnRequestHeader` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct BackendServiceCdnPolicyBypassCacheOnRequestHeader {
    /// headerName property.
    pub header_name: Option<String>,
}

/// `SignedUrlKey` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SignedUrlKey {
    /// keyName property.
    pub key_name: Option<String>,
    /// keyValue property.
    pub key_value: Option<String>,
}

/// `TargetReference` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct TargetReference {
    /// target property.
    pub target: Option<String>,
}

/// `ListInstantSnapshotGroups` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ListInstantSnapshotGroups {
    /// etag property.
    pub etag: Option<String>,
    /// id property.
    pub id: Option<String>,
    /// items property.
    pub items: Option<Vec<InstantSnapshotGroup>>,
    /// kind property.
    pub kind: Option<String>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// unreachables property.
    pub unreachables: Option<Vec<String>>,
    /// warning property.
    pub warning: Option<std::collections::HashMap<String, serde_json::Value>>,
}

/// `Network` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Network {
    /// IPv4Range property.
    pub i_pv4_range: Option<String>,
    /// autoCreateSubnetworks property.
    pub auto_create_subnetworks: Option<bool>,
    /// creationTimestamp property.
    pub creation_timestamp: Option<String>,
    /// description property.
    pub description: Option<String>,
    /// enableUlaInternalIpv6 property.
    pub enable_ula_internal_ipv6: Option<bool>,
    /// firewallPolicy property.
    pub firewall_policy: Option<String>,
    /// gatewayIPv4 property.
    pub gateway_i_pv4: Option<String>,
    /// id property.
    pub id: Option<String>,
    /// internalIpv6Range property.
    pub internal_ipv6_range: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// mtu property.
    pub mtu: Option<i64>,
    /// name property.
    pub name: Option<String>,
    /// networkFirewallPolicyEnforcementOrder property.
    pub network_firewall_policy_enforcement_order: Option<String>,
    /// networkProfile property.
    pub network_profile: Option<String>,
    /// params property.
    pub params: Option<NetworkParams>,
    /// peerings property.
    pub peerings: Option<Vec<NetworkPeering>>,
    /// routingConfig property.
    pub routing_config: Option<NetworkRoutingConfig>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// selfLinkWithId property.
    pub self_link_with_id: Option<String>,
    /// subnetworks property.
    pub subnetworks: Option<Vec<String>>,
}

/// `AllocationAggregateReservationReservedResourceInfo` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AllocationAggregateReservationReservedResourceInfo {
    /// accelerator property.
    pub accelerator: Option<AllocationAggregateReservationReservedResourceInfoAccelerator>,
}

/// `InstanceAggregatedList` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct InstanceAggregatedList {
    /// id property.
    pub id: Option<String>,
    /// items property.
    pub items: Option<serde_json::Value>,
    /// kind property.
    pub kind: Option<String>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// unreachables property.
    pub unreachables: Option<Vec<String>>,
    /// warning property.
    pub warning: Option<std::collections::HashMap<String, serde_json::Value>>,
}

/// `SecurityPolicyRule` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SecurityPolicyRule {
    /// action property.
    pub action: Option<String>,
    /// description property.
    pub description: Option<String>,
    /// headerAction property.
    pub header_action: Option<SecurityPolicyRuleHttpHeaderAction>,
    /// kind property.
    pub kind: Option<String>,
    /// match property.
    pub r#match: Option<SecurityPolicyRuleMatcher>,
    /// networkMatch property.
    pub network_match: Option<SecurityPolicyRuleNetworkMatcher>,
    /// preconfiguredWafConfig property.
    pub preconfigured_waf_config: Option<SecurityPolicyRulePreconfiguredWafConfig>,
    /// preview property.
    pub preview: Option<bool>,
    /// priority property.
    pub priority: Option<i64>,
    /// rateLimitOptions property.
    pub rate_limit_options: Option<SecurityPolicyRuleRateLimitOptions>,
    /// redirectOptions property.
    pub redirect_options: Option<SecurityPolicyRuleRedirectOptions>,
}

/// `VpnGatewayStatusTunnel` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct VpnGatewayStatusTunnel {
    /// localGatewayInterface property.
    pub local_gateway_interface: Option<i64>,
    /// peerGatewayInterface property.
    pub peer_gateway_interface: Option<i64>,
    /// tunnelUrl property.
    pub tunnel_url: Option<String>,
}

/// `NetworkAttachmentConnectedEndpoint` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct NetworkAttachmentConnectedEndpoint {
    /// ipAddress property.
    pub ip_address: Option<String>,
    /// ipv6Address property.
    pub ipv6_address: Option<String>,
    /// projectIdOrNum property.
    pub project_id_or_num: Option<String>,
    /// secondaryIpCidrRanges property.
    pub secondary_ip_cidr_ranges: Option<Vec<String>>,
    /// serviceClassId property.
    pub service_class_id: Option<String>,
    /// status property.
    pub status: Option<String>,
    /// subnetwork property.
    pub subnetwork: Option<String>,
    /// subnetworkCidrRange property.
    pub subnetwork_cidr_range: Option<String>,
}

/// `ShieldedInstanceIdentityEntry` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ShieldedInstanceIdentityEntry {
    /// ekCert property.
    pub ek_cert: Option<String>,
    /// ekPub property.
    pub ek_pub: Option<String>,
}

/// `BackendServiceCdnPolicyNegativeCachingPolicy` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct BackendServiceCdnPolicyNegativeCachingPolicy {
    /// code property.
    pub code: Option<i64>,
    /// ttl property.
    pub ttl: Option<i64>,
}

/// `VpnGatewayParams` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct VpnGatewayParams {
    /// resourceManagerTags property.
    pub resource_manager_tags: Option<serde_json::Value>,
}

/// `ServerBinding` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ServerBinding {
    /// type property.
    pub r#type: Option<String>,
}

/// `InstanceFlexibilityPolicy` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct InstanceFlexibilityPolicy {
    /// instanceSelections property.
    pub instance_selections: Option<serde_json::Value>,
}

/// `NatIpInfoResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct NatIpInfoResponse {
    /// result property.
    pub result: Option<Vec<NatIpInfo>>,
}

/// `InstanceGroupList` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct InstanceGroupList {
    /// id property.
    pub id: Option<String>,
    /// items property.
    pub items: Option<Vec<InstanceGroup>>,
    /// kind property.
    pub kind: Option<String>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// warning property.
    pub warning: Option<std::collections::HashMap<String, serde_json::Value>>,
}

/// `HttpQueryParameterMatch` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct HttpQueryParameterMatch {
    /// exactMatch property.
    pub exact_match: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// presentMatch property.
    pub present_match: Option<bool>,
    /// regexMatch property.
    pub regex_match: Option<String>,
}

/// `InstanceGroupManager` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct InstanceGroupManager {
    /// allInstancesConfig property.
    pub all_instances_config: Option<InstanceGroupManagerAllInstancesConfig>,
    /// autoHealingPolicies property.
    pub auto_healing_policies: Option<Vec<InstanceGroupManagerAutoHealingPolicy>>,
    /// baseInstanceName property.
    pub base_instance_name: Option<String>,
    /// creationTimestamp property.
    pub creation_timestamp: Option<String>,
    /// currentActions property.
    pub current_actions: Option<InstanceGroupManagerActionsSummary>,
    /// description property.
    pub description: Option<String>,
    /// distributionPolicy property.
    pub distribution_policy: Option<DistributionPolicy>,
    /// fingerprint property.
    pub fingerprint: Option<String>,
    /// id property.
    pub id: Option<String>,
    /// instanceFlexibilityPolicy property.
    pub instance_flexibility_policy: Option<InstanceGroupManagerInstanceFlexibilityPolicy>,
    /// instanceGroup property.
    pub instance_group: Option<String>,
    /// instanceLifecyclePolicy property.
    pub instance_lifecycle_policy: Option<InstanceGroupManagerInstanceLifecyclePolicy>,
    /// instanceTemplate property.
    pub instance_template: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// listManagedInstancesResults property.
    pub list_managed_instances_results: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// namedPorts property.
    pub named_ports: Option<Vec<NamedPort>>,
    /// region property.
    pub region: Option<String>,
    /// resourcePolicies property.
    pub resource_policies: Option<InstanceGroupManagerResourcePolicies>,
    /// satisfiesPzi property.
    pub satisfies_pzi: Option<bool>,
    /// satisfiesPzs property.
    pub satisfies_pzs: Option<bool>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// standbyPolicy property.
    pub standby_policy: Option<InstanceGroupManagerStandbyPolicy>,
    /// statefulPolicy property.
    pub stateful_policy: Option<StatefulPolicy>,
    /// status property.
    pub status: Option<InstanceGroupManagerStatus>,
    /// targetPools property.
    pub target_pools: Option<Vec<String>>,
    /// targetSize property.
    pub target_size: Option<i64>,
    /// targetSizePolicy property.
    pub target_size_policy: Option<InstanceGroupManagerTargetSizePolicy>,
    /// targetStoppedSize property.
    pub target_stopped_size: Option<i64>,
    /// targetSuspendedSize property.
    pub target_suspended_size: Option<i64>,
    /// updatePolicy property.
    pub update_policy: Option<InstanceGroupManagerUpdatePolicy>,
    /// versions property.
    pub versions: Option<Vec<InstanceGroupManagerVersion>>,
    /// zone property.
    pub zone: Option<String>,
}

/// `UrlRewrite` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct UrlRewrite {
    /// hostRewrite property.
    pub host_rewrite: Option<String>,
    /// pathPrefixRewrite property.
    pub path_prefix_rewrite: Option<String>,
    /// pathTemplateRewrite property.
    pub path_template_rewrite: Option<String>,
}

/// `ResourcePolicySnapshotSchedulePolicyRetentionPolicy` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ResourcePolicySnapshotSchedulePolicyRetentionPolicy {
    /// maxRetentionDays property.
    pub max_retention_days: Option<i64>,
    /// onSourceDiskDelete property.
    pub on_source_disk_delete: Option<String>,
}

/// `UpcomingMaintenance` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct UpcomingMaintenance {
    /// canReschedule property.
    pub can_reschedule: Option<bool>,
    /// latestWindowStartTime property.
    pub latest_window_start_time: Option<String>,
    /// maintenanceOnShutdown property.
    pub maintenance_on_shutdown: Option<bool>,
    /// maintenanceReasons property.
    pub maintenance_reasons: Option<Vec<String>>,
    /// maintenanceStatus property.
    pub maintenance_status: Option<String>,
    /// type property.
    pub r#type: Option<String>,
    /// windowEndTime property.
    pub window_end_time: Option<String>,
    /// windowStartTime property.
    pub window_start_time: Option<String>,
}

/// `ConsistentHashLoadBalancerSettings` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ConsistentHashLoadBalancerSettings {
    /// httpCookie property.
    pub http_cookie: Option<ConsistentHashLoadBalancerSettingsHttpCookie>,
    /// httpHeaderName property.
    pub http_header_name: Option<String>,
    /// minimumRingSize property.
    pub minimum_ring_size: Option<String>,
}

/// `AuditLogConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AuditLogConfig {
    /// exemptedMembers property.
    pub exempted_members: Option<Vec<String>>,
    /// logType property.
    pub log_type: Option<String>,
}

/// `FutureReservationSpecificSKUProperties` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct FutureReservationSpecificSKUProperties {
    /// instanceProperties property.
    pub instance_properties: Option<AllocationSpecificSKUAllocationReservedInstanceProperties>,
    /// sourceInstanceTemplate property.
    pub source_instance_template: Option<String>,
    /// totalCount property.
    pub total_count: Option<String>,
}

/// `CalendarModeAdviceRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct CalendarModeAdviceRequest {
    /// futureResourcesSpecs property.
    pub future_resources_specs: Option<serde_json::Value>,
}

/// `SslCertificateAggregatedList` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SslCertificateAggregatedList {
    /// id property.
    pub id: Option<String>,
    /// items property.
    pub items: Option<serde_json::Value>,
    /// kind property.
    pub kind: Option<String>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// unreachables property.
    pub unreachables: Option<Vec<String>>,
    /// warning property.
    pub warning: Option<std::collections::HashMap<String, serde_json::Value>>,
}

/// `PerInstanceConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct PerInstanceConfig {
    /// fingerprint property.
    pub fingerprint: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// preservedState property.
    pub preserved_state: Option<PreservedState>,
    /// status property.
    pub status: Option<String>,
}

/// `DiskResourceStatusAsyncReplicationStatus` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct DiskResourceStatusAsyncReplicationStatus {
    /// state property.
    pub state: Option<String>,
}

/// `TargetHttpProxy` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct TargetHttpProxy {
    /// creationTimestamp property.
    pub creation_timestamp: Option<String>,
    /// description property.
    pub description: Option<String>,
    /// fingerprint property.
    pub fingerprint: Option<String>,
    /// httpKeepAliveTimeoutSec property.
    pub http_keep_alive_timeout_sec: Option<i64>,
    /// id property.
    pub id: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// proxyBind property.
    pub proxy_bind: Option<bool>,
    /// region property.
    pub region: Option<String>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// urlMap property.
    pub url_map: Option<String>,
}

/// `PublicDelegatedPrefixAggregatedList` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct PublicDelegatedPrefixAggregatedList {
    /// id property.
    pub id: Option<String>,
    /// items property.
    pub items: Option<serde_json::Value>,
    /// kind property.
    pub kind: Option<String>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// unreachables property.
    pub unreachables: Option<Vec<String>>,
    /// warning property.
    pub warning: Option<std::collections::HashMap<String, serde_json::Value>>,
}

/// `CustomErrorResponsePolicyCustomErrorResponseRule` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct CustomErrorResponsePolicyCustomErrorResponseRule {
    /// matchResponseCodes property.
    pub match_response_codes: Option<Vec<String>>,
    /// overrideResponseCode property.
    pub override_response_code: Option<i64>,
    /// path property.
    pub path: Option<String>,
}

/// `InterconnectsGetMacsecConfigResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct InterconnectsGetMacsecConfigResponse {
    /// etag property.
    pub etag: Option<String>,
    /// result property.
    pub result: Option<InterconnectMacsecConfig>,
}

/// `Subnetwork` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Subnetwork {
    /// allowSubnetCidrRoutesOverlap property.
    pub allow_subnet_cidr_routes_overlap: Option<bool>,
    /// creationTimestamp property.
    pub creation_timestamp: Option<String>,
    /// description property.
    pub description: Option<String>,
    /// enableFlowLogs property.
    pub enable_flow_logs: Option<bool>,
    /// externalIpv6Prefix property.
    pub external_ipv6_prefix: Option<String>,
    /// fingerprint property.
    pub fingerprint: Option<String>,
    /// gatewayAddress property.
    pub gateway_address: Option<String>,
    /// id property.
    pub id: Option<String>,
    /// internalIpv6Prefix property.
    pub internal_ipv6_prefix: Option<String>,
    /// ipCidrRange property.
    pub ip_cidr_range: Option<String>,
    /// ipCollection property.
    pub ip_collection: Option<String>,
    /// ipv6AccessType property.
    pub ipv6_access_type: Option<String>,
    /// ipv6CidrRange property.
    pub ipv6_cidr_range: Option<String>,
    /// ipv6GceEndpoint property.
    pub ipv6_gce_endpoint: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// logConfig property.
    pub log_config: Option<SubnetworkLogConfig>,
    /// name property.
    pub name: Option<String>,
    /// network property.
    pub network: Option<String>,
    /// params property.
    pub params: Option<SubnetworkParams>,
    /// privateIpGoogleAccess property.
    pub private_ip_google_access: Option<bool>,
    /// privateIpv6GoogleAccess property.
    pub private_ipv6_google_access: Option<String>,
    /// purpose property.
    pub purpose: Option<String>,
    /// region property.
    pub region: Option<String>,
    /// reservedInternalRange property.
    pub reserved_internal_range: Option<String>,
    /// resolveSubnetMask property.
    pub resolve_subnet_mask: Option<String>,
    /// role property.
    pub role: Option<String>,
    /// secondaryIpRanges property.
    pub secondary_ip_ranges: Option<Vec<SubnetworkSecondaryRange>>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// stackType property.
    pub stack_type: Option<String>,
    /// state property.
    pub state: Option<String>,
    /// systemReservedExternalIpv6Ranges property.
    pub system_reserved_external_ipv6_ranges: Option<Vec<String>>,
    /// systemReservedInternalIpv6Ranges property.
    pub system_reserved_internal_ipv6_ranges: Option<Vec<String>>,
    /// utilizationDetails property.
    pub utilization_details: Option<SubnetworkUtilizationDetails>,
}

/// `HttpRouteRule` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct HttpRouteRule {
    /// customErrorResponsePolicy property.
    pub custom_error_response_policy: Option<CustomErrorResponsePolicy>,
    /// description property.
    pub description: Option<String>,
    /// headerAction property.
    pub header_action: Option<HttpHeaderAction>,
    /// matchRules property.
    pub match_rules: Option<Vec<HttpRouteRuleMatch>>,
    /// priority property.
    pub priority: Option<i64>,
    /// routeAction property.
    pub route_action: Option<HttpRouteAction>,
    /// service property.
    pub service: Option<String>,
    /// urlRedirect property.
    pub url_redirect: Option<HttpRedirectAction>,
}

/// `SecurityPolicyRuleNetworkMatcherUserDefinedFieldMatch` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SecurityPolicyRuleNetworkMatcherUserDefinedFieldMatch {
    /// name property.
    pub name: Option<String>,
    /// values property.
    pub values: Option<Vec<String>>,
}

/// `AcceleratorConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AcceleratorConfig {
    /// acceleratorCount property.
    pub accelerator_count: Option<i64>,
    /// acceleratorType property.
    pub accelerator_type: Option<String>,
}

/// `LicenseResourceCommitment` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct LicenseResourceCommitment {
    /// amount property.
    pub amount: Option<String>,
    /// coresPerLicense property.
    pub cores_per_license: Option<String>,
    /// license property.
    pub license: Option<String>,
}

/// `Uint128` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Uint128 {
    /// high property.
    pub high: Option<String>,
    /// low property.
    pub low: Option<String>,
}

/// `TargetPool` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct TargetPool {
    /// backupPool property.
    pub backup_pool: Option<String>,
    /// creationTimestamp property.
    pub creation_timestamp: Option<String>,
    /// description property.
    pub description: Option<String>,
    /// failoverRatio property.
    pub failover_ratio: Option<f64>,
    /// healthChecks property.
    pub health_checks: Option<Vec<String>>,
    /// id property.
    pub id: Option<String>,
    /// instances property.
    pub instances: Option<Vec<String>>,
    /// kind property.
    pub kind: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// region property.
    pub region: Option<String>,
    /// securityPolicy property.
    pub security_policy: Option<String>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// sessionAffinity property.
    pub session_affinity: Option<String>,
}

/// `NetworksRemovePeeringRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct NetworksRemovePeeringRequest {
    /// name property.
    pub name: Option<String>,
}

/// `ProjectsListXpnHostsRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ProjectsListXpnHostsRequest {
    /// organization property.
    pub organization: Option<String>,
}

/// `InterconnectAttachmentGroupConfiguredAvailabilitySLA` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct InterconnectAttachmentGroupConfiguredAvailabilitySLA {
    /// effectiveSla property.
    pub effective_sla: Option<String>,
    /// intendedSlaBlockers property.
    pub intended_sla_blockers:
        Option<Vec<InterconnectAttachmentGroupConfiguredAvailabilitySLAIntendedSlaBlockers>>,
}

/// `OrganizationSecurityPoliciesListAssociationsResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct OrganizationSecurityPoliciesListAssociationsResponse {
    /// associations property.
    pub associations: Option<Vec<SecurityPolicyAssociation>>,
    /// kind property.
    pub kind: Option<String>,
}

/// `RouterStatusBgpPeerStatus` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct RouterStatusBgpPeerStatus {
    /// advertisedRoutes property.
    pub advertised_routes: Option<Vec<Route>>,
    /// bfdStatus property.
    pub bfd_status: Option<BfdStatus>,
    /// enableIpv4 property.
    pub enable_ipv4: Option<bool>,
    /// enableIpv6 property.
    pub enable_ipv6: Option<bool>,
    /// ipAddress property.
    pub ip_address: Option<String>,
    /// ipv4NexthopAddress property.
    pub ipv4_nexthop_address: Option<String>,
    /// ipv6NexthopAddress property.
    pub ipv6_nexthop_address: Option<String>,
    /// linkedVpnTunnel property.
    pub linked_vpn_tunnel: Option<String>,
    /// md5AuthEnabled property.
    pub md5_auth_enabled: Option<bool>,
    /// name property.
    pub name: Option<String>,
    /// numLearnedRoutes property.
    pub num_learned_routes: Option<i64>,
    /// peerIpAddress property.
    pub peer_ip_address: Option<String>,
    /// peerIpv4NexthopAddress property.
    pub peer_ipv4_nexthop_address: Option<String>,
    /// peerIpv6NexthopAddress property.
    pub peer_ipv6_nexthop_address: Option<String>,
    /// routerApplianceInstance property.
    pub router_appliance_instance: Option<String>,
    /// state property.
    pub state: Option<String>,
    /// status property.
    pub status: Option<String>,
    /// statusReason property.
    pub status_reason: Option<String>,
    /// uptime property.
    pub uptime: Option<String>,
    /// uptimeSeconds property.
    pub uptime_seconds: Option<String>,
}

/// `ReservationAffinity` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ReservationAffinity {
    /// consumeReservationType property.
    pub consume_reservation_type: Option<String>,
    /// key property.
    pub key: Option<String>,
    /// values property.
    pub values: Option<Vec<String>>,
}

/// `PathMatcher` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct PathMatcher {
    /// defaultCustomErrorResponsePolicy property.
    pub default_custom_error_response_policy: Option<CustomErrorResponsePolicy>,
    /// defaultRouteAction property.
    pub default_route_action: Option<HttpRouteAction>,
    /// defaultService property.
    pub default_service: Option<String>,
    /// defaultUrlRedirect property.
    pub default_url_redirect: Option<HttpRedirectAction>,
    /// description property.
    pub description: Option<String>,
    /// headerAction property.
    pub header_action: Option<HttpHeaderAction>,
    /// name property.
    pub name: Option<String>,
    /// pathRules property.
    pub path_rules: Option<Vec<PathRule>>,
    /// routeRules property.
    pub route_rules: Option<Vec<HttpRouteRule>>,
}

/// `ReservationList` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ReservationList {
    /// id property.
    pub id: Option<String>,
    /// items property.
    pub items: Option<Vec<Reservation>>,
    /// kind property.
    pub kind: Option<String>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// warning property.
    pub warning: Option<std::collections::HashMap<String, serde_json::Value>>,
}

/// `NetworkProfileLocation` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct NetworkProfileLocation {
    /// name property.
    pub name: Option<String>,
    /// scope property.
    pub scope: Option<String>,
}

/// `PacketMirroringMirroredResourceInfo` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct PacketMirroringMirroredResourceInfo {
    /// instances property.
    pub instances: Option<Vec<PacketMirroringMirroredResourceInfoInstanceInfo>>,
    /// subnetworks property.
    pub subnetworks: Option<Vec<PacketMirroringMirroredResourceInfoSubnetInfo>>,
    /// tags property.
    pub tags: Option<Vec<String>>,
}

/// `Scheduling` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Scheduling {
    /// automaticRestart property.
    pub automatic_restart: Option<bool>,
    /// availabilityDomain property.
    pub availability_domain: Option<i64>,
    /// hostErrorTimeoutSeconds property.
    pub host_error_timeout_seconds: Option<i64>,
    /// instanceTerminationAction property.
    pub instance_termination_action: Option<String>,
    /// localSsdRecoveryTimeout property.
    pub local_ssd_recovery_timeout: Option<Duration>,
    /// locationHint property.
    pub location_hint: Option<String>,
    /// maxRunDuration property.
    pub max_run_duration: Option<Duration>,
    /// minNodeCpus property.
    pub min_node_cpus: Option<i64>,
    /// nodeAffinities property.
    pub node_affinities: Option<Vec<SchedulingNodeAffinity>>,
    /// onHostMaintenance property.
    pub on_host_maintenance: Option<String>,
    /// onInstanceStopAction property.
    pub on_instance_stop_action: Option<SchedulingOnInstanceStopAction>,
    /// preemptible property.
    pub preemptible: Option<bool>,
    /// provisioningModel property.
    pub provisioning_model: Option<String>,
    /// skipGuestOsShutdown property.
    pub skip_guest_os_shutdown: Option<bool>,
    /// terminationTime property.
    pub termination_time: Option<String>,
}

/// `PreviewFeatureStatus` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct PreviewFeatureStatus {
    /// description property.
    pub description: Option<String>,
    /// helpLink property.
    pub help_link: Option<String>,
    /// releaseStatus property.
    pub release_status: Option<PreviewFeatureStatusReleaseStatus>,
}

/// `NatIpInfo` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct NatIpInfo {
    /// natIpInfoMappings property.
    pub nat_ip_info_mappings: Option<Vec<NatIpInfoNatIpInfoMapping>>,
    /// natName property.
    pub nat_name: Option<String>,
}

/// `GlobalAddressesMoveRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GlobalAddressesMoveRequest {
    /// description property.
    pub description: Option<String>,
    /// destinationAddress property.
    pub destination_address: Option<String>,
}

/// `SecurityPolicyRuleMatcher` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SecurityPolicyRuleMatcher {
    /// config property.
    pub config: Option<SecurityPolicyRuleMatcherConfig>,
    /// expr property.
    pub expr: Option<Expr>,
    /// exprOptions property.
    pub expr_options: Option<SecurityPolicyRuleMatcherExprOptions>,
    /// versionedExpr property.
    pub versioned_expr: Option<String>,
}

/// `BulkZoneSetLabelsRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct BulkZoneSetLabelsRequest {
    /// requests property.
    pub requests: Option<Vec<BulkSetLabelsRequest>>,
}

/// `UsageExportLocation` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct UsageExportLocation {
    /// bucketName property.
    pub bucket_name: Option<String>,
    /// reportNamePrefix property.
    pub report_name_prefix: Option<String>,
}

/// `InterconnectAttachment` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct InterconnectAttachment {
    /// adminEnabled property.
    pub admin_enabled: Option<bool>,
    /// attachmentGroup property.
    pub attachment_group: Option<String>,
    /// bandwidth property.
    pub bandwidth: Option<String>,
    /// candidateCloudRouterIpAddress property.
    pub candidate_cloud_router_ip_address: Option<String>,
    /// candidateCloudRouterIpv6Address property.
    pub candidate_cloud_router_ipv6_address: Option<String>,
    /// candidateCustomerRouterIpAddress property.
    pub candidate_customer_router_ip_address: Option<String>,
    /// candidateCustomerRouterIpv6Address property.
    pub candidate_customer_router_ipv6_address: Option<String>,
    /// candidateIpv6Subnets property.
    pub candidate_ipv6_subnets: Option<Vec<String>>,
    /// candidateSubnets property.
    pub candidate_subnets: Option<Vec<String>>,
    /// cloudRouterIpAddress property.
    pub cloud_router_ip_address: Option<String>,
    /// cloudRouterIpv6Address property.
    pub cloud_router_ipv6_address: Option<String>,
    /// cloudRouterIpv6InterfaceId property.
    pub cloud_router_ipv6_interface_id: Option<String>,
    /// configurationConstraints property.
    pub configuration_constraints: Option<InterconnectAttachmentConfigurationConstraints>,
    /// creationTimestamp property.
    pub creation_timestamp: Option<String>,
    /// customerRouterIpAddress property.
    pub customer_router_ip_address: Option<String>,
    /// customerRouterIpv6Address property.
    pub customer_router_ipv6_address: Option<String>,
    /// customerRouterIpv6InterfaceId property.
    pub customer_router_ipv6_interface_id: Option<String>,
    /// dataplaneVersion property.
    pub dataplane_version: Option<i64>,
    /// description property.
    pub description: Option<String>,
    /// edgeAvailabilityDomain property.
    pub edge_availability_domain: Option<String>,
    /// encryption property.
    pub encryption: Option<String>,
    /// googleReferenceId property.
    pub google_reference_id: Option<String>,
    /// id property.
    pub id: Option<String>,
    /// interconnect property.
    pub interconnect: Option<String>,
    /// ipsecInternalAddresses property.
    pub ipsec_internal_addresses: Option<Vec<String>>,
    /// kind property.
    pub kind: Option<String>,
    /// l2Forwarding property.
    pub l2_forwarding: Option<InterconnectAttachmentL2Forwarding>,
    /// labelFingerprint property.
    pub label_fingerprint: Option<String>,
    /// labels property.
    pub labels: Option<serde_json::Value>,
    /// mtu property.
    pub mtu: Option<i64>,
    /// name property.
    pub name: Option<String>,
    /// operationalStatus property.
    pub operational_status: Option<String>,
    /// pairingKey property.
    pub pairing_key: Option<String>,
    /// params property.
    pub params: Option<InterconnectAttachmentParams>,
    /// partnerAsn property.
    pub partner_asn: Option<String>,
    /// partnerMetadata property.
    pub partner_metadata: Option<InterconnectAttachmentPartnerMetadata>,
    /// privateInterconnectInfo property.
    pub private_interconnect_info: Option<InterconnectAttachmentPrivateInfo>,
    /// region property.
    pub region: Option<String>,
    /// remoteService property.
    pub remote_service: Option<String>,
    /// router property.
    pub router: Option<String>,
    /// satisfiesPzs property.
    pub satisfies_pzs: Option<bool>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// stackType property.
    pub stack_type: Option<String>,
    /// state property.
    pub state: Option<String>,
    /// subnetLength property.
    pub subnet_length: Option<i64>,
    /// type property.
    pub r#type: Option<String>,
    /// vlanTag8021q property.
    pub vlan_tag8021q: Option<i64>,
}

/// `HealthAggregationPolicy` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct HealthAggregationPolicy {
    /// creationTimestamp property.
    pub creation_timestamp: Option<String>,
    /// description property.
    pub description: Option<String>,
    /// fingerprint property.
    pub fingerprint: Option<String>,
    /// healthyPercentThreshold property.
    pub healthy_percent_threshold: Option<i64>,
    /// id property.
    pub id: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// minHealthyThreshold property.
    pub min_healthy_threshold: Option<i64>,
    /// name property.
    pub name: Option<String>,
    /// policyType property.
    pub policy_type: Option<String>,
    /// region property.
    pub region: Option<String>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// selfLinkWithId property.
    pub self_link_with_id: Option<String>,
}

/// `Commitment` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Commitment {
    /// autoRenew property.
    pub auto_renew: Option<bool>,
    /// category property.
    pub category: Option<String>,
    /// creationTimestamp property.
    pub creation_timestamp: Option<String>,
    /// customEndTimestamp property.
    pub custom_end_timestamp: Option<String>,
    /// description property.
    pub description: Option<String>,
    /// endTimestamp property.
    pub end_timestamp: Option<String>,
    /// existingReservations property.
    pub existing_reservations: Option<Vec<String>>,
    /// id property.
    pub id: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// licenseResource property.
    pub license_resource: Option<LicenseResourceCommitment>,
    /// mergeSourceCommitments property.
    pub merge_source_commitments: Option<Vec<String>>,
    /// name property.
    pub name: Option<String>,
    /// params property.
    pub params: Option<CommitmentParams>,
    /// plan property.
    pub plan: Option<String>,
    /// region property.
    pub region: Option<String>,
    /// reservations property.
    pub reservations: Option<Vec<Reservation>>,
    /// resourceStatus property.
    pub resource_status: Option<CommitmentResourceStatus>,
    /// resources property.
    pub resources: Option<Vec<ResourceCommitment>>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// splitSourceCommitment property.
    pub split_source_commitment: Option<String>,
    /// startTimestamp property.
    pub start_timestamp: Option<String>,
    /// status property.
    pub status: Option<String>,
    /// statusMessage property.
    pub status_message: Option<String>,
    /// type property.
    pub r#type: Option<String>,
}

/// `RegionInstanceGroupManagersStopInstancesRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct RegionInstanceGroupManagersStopInstancesRequest {
    /// forceStop property.
    pub force_stop: Option<bool>,
    /// instances property.
    pub instances: Option<Vec<String>>,
}

/// `NetworkEndpointGroup` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct NetworkEndpointGroup {
    /// annotations property.
    pub annotations: Option<serde_json::Value>,
    /// appEngine property.
    pub app_engine: Option<NetworkEndpointGroupAppEngine>,
    /// cloudFunction property.
    pub cloud_function: Option<NetworkEndpointGroupCloudFunction>,
    /// cloudRun property.
    pub cloud_run: Option<NetworkEndpointGroupCloudRun>,
    /// creationTimestamp property.
    pub creation_timestamp: Option<String>,
    /// defaultPort property.
    pub default_port: Option<i64>,
    /// description property.
    pub description: Option<String>,
    /// id property.
    pub id: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// network property.
    pub network: Option<String>,
    /// networkEndpointType property.
    pub network_endpoint_type: Option<String>,
    /// pscData property.
    pub psc_data: Option<NetworkEndpointGroupPscData>,
    /// pscTargetService property.
    pub psc_target_service: Option<String>,
    /// region property.
    pub region: Option<String>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// size property.
    pub size: Option<i64>,
    /// subnetwork property.
    pub subnetwork: Option<String>,
    /// zone property.
    pub zone: Option<String>,
}

/// `SslPoliciesListAvailableFeaturesResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SslPoliciesListAvailableFeaturesResponse {
    /// features property.
    pub features: Option<Vec<String>>,
}

/// `RegionSetPolicyRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct RegionSetPolicyRequest {
    /// bindings property.
    pub bindings: Option<Vec<Binding>>,
    /// etag property.
    pub etag: Option<String>,
    /// policy property.
    pub policy: Option<Policy>,
}

/// `TargetTcpProxy` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct TargetTcpProxy {
    /// creationTimestamp property.
    pub creation_timestamp: Option<String>,
    /// description property.
    pub description: Option<String>,
    /// id property.
    pub id: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// proxyBind property.
    pub proxy_bind: Option<bool>,
    /// proxyHeader property.
    pub proxy_header: Option<String>,
    /// region property.
    pub region: Option<String>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// service property.
    pub service: Option<String>,
}

/// `InstanceGroupManagersSuspendInstancesRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct InstanceGroupManagersSuspendInstancesRequest {
    /// forceSuspend property.
    pub force_suspend: Option<bool>,
    /// instances property.
    pub instances: Option<Vec<String>>,
}

/// `BackendServiceHAPolicyLeaderNetworkEndpoint` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct BackendServiceHAPolicyLeaderNetworkEndpoint {
    /// instance property.
    pub instance: Option<String>,
}

/// `SecurityPoliciesWafConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SecurityPoliciesWafConfig {
    /// wafRules property.
    pub waf_rules: Option<PreconfiguredWafSet>,
}

/// `NetworkPeeringConnectionStatusConsensusState` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct NetworkPeeringConnectionStatusConsensusState {
    /// deleteStatus property.
    pub delete_status: Option<String>,
    /// updateStatus property.
    pub update_status: Option<String>,
}

/// `RegionInstanceGroupManagersStartInstancesRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct RegionInstanceGroupManagersStartInstancesRequest {
    /// instances property.
    pub instances: Option<Vec<String>>,
}

/// `NodeGroupsDeleteNodesRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct NodeGroupsDeleteNodesRequest {
    /// nodes property.
    pub nodes: Option<Vec<String>>,
}

/// `WireGroupList` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct WireGroupList {
    /// etag property.
    pub etag: Option<String>,
    /// id property.
    pub id: Option<String>,
    /// items property.
    pub items: Option<Vec<WireGroup>>,
    /// kind property.
    pub kind: Option<String>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// unreachables property.
    pub unreachables: Option<Vec<String>>,
    /// warning property.
    pub warning: Option<std::collections::HashMap<String, serde_json::Value>>,
}

/// `RouterNatRule` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct RouterNatRule {
    /// action property.
    pub action: Option<RouterNatRuleAction>,
    /// description property.
    pub description: Option<String>,
    /// match property.
    pub r#match: Option<String>,
    /// ruleNumber property.
    pub rule_number: Option<i64>,
}

/// `InterconnectGroupConfiguredTopologyCapability` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct InterconnectGroupConfiguredTopologyCapability {
    /// intendedCapabilityBlockers property.
    pub intended_capability_blockers:
        Option<Vec<InterconnectGroupConfiguredTopologyCapabilityIntendedCapabilityBlockers>>,
    /// supportedSla property.
    pub supported_sla: Option<String>,
}

/// `InstanceGroupManagersDeletePerInstanceConfigsReq` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct InstanceGroupManagersDeletePerInstanceConfigsReq {
    /// names property.
    pub names: Option<Vec<String>>,
}

/// `GuestAttributesEntry` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GuestAttributesEntry {
    /// key property.
    pub key: Option<String>,
    /// namespace property.
    pub namespace: Option<String>,
    /// value property.
    pub value: Option<String>,
}

/// `OperationAggregatedList` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct OperationAggregatedList {
    /// id property.
    pub id: Option<String>,
    /// items property.
    pub items: Option<serde_json::Value>,
    /// kind property.
    pub kind: Option<String>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// unreachables property.
    pub unreachables: Option<Vec<String>>,
    /// warning property.
    pub warning: Option<std::collections::HashMap<String, serde_json::Value>>,
}

/// `PreviewFeatureRolloutOperationRolloutInput` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct PreviewFeatureRolloutOperationRolloutInput {
    /// name property.
    pub name: Option<String>,
    /// predefinedRolloutPlan property.
    pub predefined_rollout_plan: Option<String>,
}

/// `InstanceGroupManagerStatusAcceleratorTopology` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct InstanceGroupManagerStatusAcceleratorTopology {
    /// acceleratorTopology property.
    pub accelerator_topology: Option<String>,
    /// state property.
    pub state: Option<String>,
    /// stateDetails property.
    pub state_details:
        Option<InstanceGroupManagerStatusAcceleratorTopologyAcceleratorTopologyStateDetails>,
}

/// `NodeGroupNode` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct NodeGroupNode {
    /// accelerators property.
    pub accelerators: Option<Vec<AcceleratorConfig>>,
    /// consumedResources property.
    pub consumed_resources: Option<InstanceConsumptionInfo>,
    /// cpuOvercommitType property.
    pub cpu_overcommit_type: Option<String>,
    /// disks property.
    pub disks: Option<Vec<LocalDisk>>,
    /// instanceConsumptionData property.
    pub instance_consumption_data: Option<Vec<InstanceConsumptionData>>,
    /// instances property.
    pub instances: Option<Vec<String>>,
    /// name property.
    pub name: Option<String>,
    /// nodeType property.
    pub node_type: Option<String>,
    /// satisfiesPzs property.
    pub satisfies_pzs: Option<bool>,
    /// serverBinding property.
    pub server_binding: Option<ServerBinding>,
    /// serverId property.
    pub server_id: Option<String>,
    /// status property.
    pub status: Option<String>,
    /// totalResources property.
    pub total_resources: Option<InstanceConsumptionInfo>,
    /// upcomingMaintenance property.
    pub upcoming_maintenance: Option<UpcomingMaintenance>,
}

/// `TCPHealthCheck` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct TCPHealthCheck {
    /// port property.
    pub port: Option<i64>,
    /// portName property.
    pub port_name: Option<String>,
    /// portSpecification property.
    pub port_specification: Option<String>,
    /// proxyHeader property.
    pub proxy_header: Option<String>,
    /// request property.
    pub request: Option<String>,
    /// response property.
    pub response: Option<String>,
}

/// `InstanceGroupManagerStatusStateful` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct InstanceGroupManagerStatusStateful {
    /// hasStatefulConfig property.
    pub has_stateful_config: Option<bool>,
    /// perInstanceConfigs property.
    pub per_instance_configs: Option<InstanceGroupManagerStatusStatefulPerInstanceConfigs>,
}

/// `DeprecationStatus` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct DeprecationStatus {
    /// deleted property.
    pub deleted: Option<String>,
    /// deprecated property.
    pub deprecated: Option<String>,
    /// obsolete property.
    pub obsolete: Option<String>,
    /// replacement property.
    pub replacement: Option<String>,
    /// state property.
    pub state: Option<String>,
}

/// `DisksRemoveResourcePoliciesRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct DisksRemoveResourcePoliciesRequest {
    /// resourcePolicies property.
    pub resource_policies: Option<Vec<String>>,
}

/// `InstancesSetLabelsRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct InstancesSetLabelsRequest {
    /// labelFingerprint property.
    pub label_fingerprint: Option<String>,
    /// labels property.
    pub labels: Option<serde_json::Value>,
}

/// `InterconnectGroupsListResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct InterconnectGroupsListResponse {
    /// etag property.
    pub etag: Option<String>,
    /// id property.
    pub id: Option<String>,
    /// items property.
    pub items: Option<Vec<InterconnectGroup>>,
    /// kind property.
    pub kind: Option<String>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// unreachables property.
    pub unreachables: Option<Vec<String>>,
    /// warning property.
    pub warning: Option<std::collections::HashMap<String, serde_json::Value>>,
}

/// `InterconnectDiagnosticsLinkStatus` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct InterconnectDiagnosticsLinkStatus {
    /// arpCaches property.
    pub arp_caches: Option<Vec<InterconnectDiagnosticsARPEntry>>,
    /// circuitId property.
    pub circuit_id: Option<String>,
    /// googleDemarc property.
    pub google_demarc: Option<String>,
    /// lacpStatus property.
    pub lacp_status: Option<InterconnectDiagnosticsLinkLACPStatus>,
    /// macsec property.
    pub macsec: Option<InterconnectDiagnosticsMacsecStatus>,
    /// operationalStatus property.
    pub operational_status: Option<String>,
    /// receivingOpticalPower property.
    pub receiving_optical_power: Option<InterconnectDiagnosticsLinkOpticalPower>,
    /// transmittingOpticalPower property.
    pub transmitting_optical_power: Option<InterconnectDiagnosticsLinkOpticalPower>,
}

/// `BackendServiceLocalityLoadBalancingPolicyConfigPolicy` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct BackendServiceLocalityLoadBalancingPolicyConfigPolicy {
    /// name property.
    pub name: Option<String>,
}

/// `AcceleratorTypeAggregatedList` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AcceleratorTypeAggregatedList {
    /// id property.
    pub id: Option<String>,
    /// items property.
    pub items: Option<serde_json::Value>,
    /// kind property.
    pub kind: Option<String>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// unreachables property.
    pub unreachables: Option<Vec<String>>,
    /// warning property.
    pub warning: Option<std::collections::HashMap<String, serde_json::Value>>,
}

/// `HealthAggregationPolicyList` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct HealthAggregationPolicyList {
    /// id property.
    pub id: Option<String>,
    /// items property.
    pub items: Option<Vec<HealthAggregationPolicy>>,
    /// kind property.
    pub kind: Option<String>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// warning property.
    pub warning: Option<std::collections::HashMap<String, serde_json::Value>>,
}

/// `Int64RangeMatch` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Int64RangeMatch {
    /// rangeEnd property.
    pub range_end: Option<String>,
    /// rangeStart property.
    pub range_start: Option<String>,
}

/// `InterconnectDiagnosticsLinkLACPStatus` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct InterconnectDiagnosticsLinkLACPStatus {
    /// googleSystemId property.
    pub google_system_id: Option<String>,
    /// neighborSystemId property.
    pub neighbor_system_id: Option<String>,
    /// state property.
    pub state: Option<String>,
}

/// `SubnetworkUtilizationDetails` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SubnetworkUtilizationDetails {
    /// externalIpv6InstanceUtilization property.
    pub external_ipv6_instance_utilization: Option<SubnetworkUtilizationDetailsIPV6Utilization>,
    /// externalIpv6LbUtilization property.
    pub external_ipv6_lb_utilization: Option<SubnetworkUtilizationDetailsIPV6Utilization>,
    /// internalIpv6Utilization property.
    pub internal_ipv6_utilization: Option<SubnetworkUtilizationDetailsIPV6Utilization>,
    /// ipv4Utilizations property.
    pub ipv4_utilizations: Option<Vec<SubnetworkUtilizationDetailsIPV4Utilization>>,
}

/// `RouterParams` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct RouterParams {
    /// resourceManagerTags property.
    pub resource_manager_tags: Option<serde_json::Value>,
}

/// `CacheKeyPolicy` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct CacheKeyPolicy {
    /// includeHost property.
    pub include_host: Option<bool>,
    /// includeHttpHeaders property.
    pub include_http_headers: Option<Vec<String>>,
    /// includeNamedCookies property.
    pub include_named_cookies: Option<Vec<String>>,
    /// includeProtocol property.
    pub include_protocol: Option<bool>,
    /// includeQueryString property.
    pub include_query_string: Option<bool>,
    /// queryStringBlacklist property.
    pub query_string_blacklist: Option<Vec<String>>,
    /// queryStringWhitelist property.
    pub query_string_whitelist: Option<Vec<String>>,
}

/// `DiskAsyncReplication` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct DiskAsyncReplication {
    /// consistencyGroupPolicy property.
    pub consistency_group_policy: Option<String>,
    /// consistencyGroupPolicyId property.
    pub consistency_group_policy_id: Option<String>,
    /// disk property.
    pub disk: Option<String>,
    /// diskId property.
    pub disk_id: Option<String>,
}

/// `CrossSiteNetworkList` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct CrossSiteNetworkList {
    /// etag property.
    pub etag: Option<String>,
    /// id property.
    pub id: Option<String>,
    /// items property.
    pub items: Option<Vec<CrossSiteNetwork>>,
    /// kind property.
    pub kind: Option<String>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// unreachables property.
    pub unreachables: Option<Vec<String>>,
    /// warning property.
    pub warning: Option<std::collections::HashMap<String, serde_json::Value>>,
}

/// `NetworkList` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct NetworkList {
    /// id property.
    pub id: Option<String>,
    /// items property.
    pub items: Option<Vec<Network>>,
    /// kind property.
    pub kind: Option<String>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// warning property.
    pub warning: Option<std::collections::HashMap<String, serde_json::Value>>,
}

/// `ConnectionDraining` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ConnectionDraining {
    /// drainingTimeoutSec property.
    pub draining_timeout_sec: Option<i64>,
}

/// `RouterStatusNatStatus` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct RouterStatusNatStatus {
    /// autoAllocatedNatIps property.
    pub auto_allocated_nat_ips: Option<Vec<String>>,
    /// drainAutoAllocatedNatIps property.
    pub drain_auto_allocated_nat_ips: Option<Vec<String>>,
    /// drainUserAllocatedNatIps property.
    pub drain_user_allocated_nat_ips: Option<Vec<String>>,
    /// minExtraNatIpsNeeded property.
    pub min_extra_nat_ips_needed: Option<i64>,
    /// name property.
    pub name: Option<String>,
    /// numVmEndpointsWithNatMappings property.
    pub num_vm_endpoints_with_nat_mappings: Option<i64>,
    /// ruleStatus property.
    pub rule_status: Option<Vec<RouterStatusNatStatusNatRuleStatus>>,
    /// userAllocatedNatIpResources property.
    pub user_allocated_nat_ip_resources: Option<Vec<String>>,
    /// userAllocatedNatIps property.
    pub user_allocated_nat_ips: Option<Vec<String>>,
}

/// `Instance` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Instance {
    /// advancedMachineFeatures property.
    pub advanced_machine_features: Option<AdvancedMachineFeatures>,
    /// canIpForward property.
    pub can_ip_forward: Option<bool>,
    /// confidentialInstanceConfig property.
    pub confidential_instance_config: Option<ConfidentialInstanceConfig>,
    /// cpuPlatform property.
    pub cpu_platform: Option<String>,
    /// creationTimestamp property.
    pub creation_timestamp: Option<String>,
    /// deletionProtection property.
    pub deletion_protection: Option<bool>,
    /// description property.
    pub description: Option<String>,
    /// disks property.
    pub disks: Option<Vec<AttachedDisk>>,
    /// displayDevice property.
    pub display_device: Option<DisplayDevice>,
    /// fingerprint property.
    pub fingerprint: Option<String>,
    /// guestAccelerators property.
    pub guest_accelerators: Option<Vec<AcceleratorConfig>>,
    /// hostname property.
    pub hostname: Option<String>,
    /// id property.
    pub id: Option<String>,
    /// instanceEncryptionKey property.
    pub instance_encryption_key: Option<CustomerEncryptionKey>,
    /// keyRevocationActionType property.
    pub key_revocation_action_type: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// labelFingerprint property.
    pub label_fingerprint: Option<String>,
    /// labels property.
    pub labels: Option<serde_json::Value>,
    /// lastStartTimestamp property.
    pub last_start_timestamp: Option<String>,
    /// lastStopTimestamp property.
    pub last_stop_timestamp: Option<String>,
    /// lastSuspendedTimestamp property.
    pub last_suspended_timestamp: Option<String>,
    /// machineType property.
    pub machine_type: Option<String>,
    /// metadata property.
    pub metadata: Option<Metadata>,
    /// minCpuPlatform property.
    pub min_cpu_platform: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// networkInterfaces property.
    pub network_interfaces: Option<Vec<NetworkInterface>>,
    /// networkPerformanceConfig property.
    pub network_performance_config: Option<NetworkPerformanceConfig>,
    /// params property.
    pub params: Option<InstanceParams>,
    /// privateIpv6GoogleAccess property.
    pub private_ipv6_google_access: Option<String>,
    /// reservationAffinity property.
    pub reservation_affinity: Option<ReservationAffinity>,
    /// resourcePolicies property.
    pub resource_policies: Option<Vec<String>>,
    /// resourceStatus property.
    pub resource_status: Option<ResourceStatus>,
    /// satisfiesPzi property.
    pub satisfies_pzi: Option<bool>,
    /// satisfiesPzs property.
    pub satisfies_pzs: Option<bool>,
    /// scheduling property.
    pub scheduling: Option<Scheduling>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// serviceAccounts property.
    pub service_accounts: Option<Vec<ServiceAccount>>,
    /// shieldedInstanceConfig property.
    pub shielded_instance_config: Option<ShieldedInstanceConfig>,
    /// shieldedInstanceIntegrityPolicy property.
    pub shielded_instance_integrity_policy: Option<ShieldedInstanceIntegrityPolicy>,
    /// sourceMachineImage property.
    pub source_machine_image: Option<String>,
    /// sourceMachineImageEncryptionKey property.
    pub source_machine_image_encryption_key: Option<CustomerEncryptionKey>,
    /// startRestricted property.
    pub start_restricted: Option<bool>,
    /// status property.
    pub status: Option<String>,
    /// statusMessage property.
    pub status_message: Option<String>,
    /// tags property.
    pub tags: Option<Tags>,
    /// workloadIdentityConfig property.
    pub workload_identity_config: Option<WorkloadIdentityConfig>,
    /// zone property.
    pub zone: Option<String>,
}

/// `RegionInstanceGroupsListInstances` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct RegionInstanceGroupsListInstances {
    /// id property.
    pub id: Option<String>,
    /// items property.
    pub items: Option<Vec<InstanceWithNamedPorts>>,
    /// kind property.
    pub kind: Option<String>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// warning property.
    pub warning: Option<std::collections::HashMap<String, serde_json::Value>>,
}

/// `InstanceGroupManagerStatusAllInstancesConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct InstanceGroupManagerStatusAllInstancesConfig {
    /// currentRevision property.
    pub current_revision: Option<String>,
    /// effective property.
    pub effective: Option<bool>,
}

/// `SecurityPolicyRuleMatcherExprOptionsRecaptchaOptions` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SecurityPolicyRuleMatcherExprOptionsRecaptchaOptions {
    /// actionTokenSiteKeys property.
    pub action_token_site_keys: Option<Vec<String>>,
    /// sessionTokenSiteKeys property.
    pub session_token_site_keys: Option<Vec<String>>,
}

/// `ReservationSlotsGetVersionRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ReservationSlotsGetVersionRequest {
    /// sbomSelections property.
    pub sbom_selections: Option<Vec<String>>,
}

/// `InstanceGroupManagerResizeRequestStatusLastAttempt` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct InstanceGroupManagerResizeRequestStatusLastAttempt {
    /// error property.
    pub error: Option<std::collections::HashMap<String, serde_json::Value>>,
}

/// `NetworkEndpointGroupsListEndpointsRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct NetworkEndpointGroupsListEndpointsRequest {
    /// healthStatus property.
    pub health_status: Option<String>,
}

/// `StoragePoolTypeList` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct StoragePoolTypeList {
    /// id property.
    pub id: Option<String>,
    /// items property.
    pub items: Option<Vec<StoragePoolType>>,
    /// kind property.
    pub kind: Option<String>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// warning property.
    pub warning: Option<std::collections::HashMap<String, serde_json::Value>>,
}

/// `TargetPoolInstanceHealth` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct TargetPoolInstanceHealth {
    /// healthStatus property.
    pub health_status: Option<Vec<HealthStatus>>,
    /// kind property.
    pub kind: Option<String>,
}

/// `InterconnectRemoteLocationConstraints` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct InterconnectRemoteLocationConstraints {
    /// portPairRemoteLocation property.
    pub port_pair_remote_location: Option<String>,
    /// portPairVlan property.
    pub port_pair_vlan: Option<String>,
    /// subnetLengthRange property.
    pub subnet_length_range: Option<InterconnectRemoteLocationConstraintsSubnetLengthRange>,
}

/// `AllocationResourceStatusHealthInfo` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AllocationResourceStatusHealthInfo {
    /// degradedBlockCount property.
    pub degraded_block_count: Option<i64>,
    /// healthStatus property.
    pub health_status: Option<String>,
    /// healthyBlockCount property.
    pub healthy_block_count: Option<i64>,
}

/// `InterconnectAttachmentGroup` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct InterconnectAttachmentGroup {
    /// attachments property.
    pub attachments: Option<serde_json::Value>,
    /// configured property.
    pub configured: Option<InterconnectAttachmentGroupConfigured>,
    /// creationTimestamp property.
    pub creation_timestamp: Option<String>,
    /// description property.
    pub description: Option<String>,
    /// etag property.
    pub etag: Option<String>,
    /// id property.
    pub id: Option<String>,
    /// intent property.
    pub intent: Option<InterconnectAttachmentGroupIntent>,
    /// interconnectGroup property.
    pub interconnect_group: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// logicalStructure property.
    pub logical_structure: Option<InterconnectAttachmentGroupLogicalStructure>,
    /// name property.
    pub name: Option<String>,
    /// selfLink property.
    pub self_link: Option<String>,
}

/// `SubnetworkAggregatedList` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SubnetworkAggregatedList {
    /// id property.
    pub id: Option<String>,
    /// items property.
    pub items: Option<serde_json::Value>,
    /// kind property.
    pub kind: Option<String>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// unreachables property.
    pub unreachables: Option<Vec<String>>,
    /// warning property.
    pub warning: Option<std::collections::HashMap<String, serde_json::Value>>,
}

/// `ResourcePolicyGroupPlacementPolicy` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ResourcePolicyGroupPlacementPolicy {
    /// acceleratorTopologyMode property.
    pub accelerator_topology_mode: Option<String>,
    /// availabilityDomainCount property.
    pub availability_domain_count: Option<i64>,
    /// collocation property.
    pub collocation: Option<String>,
    /// gpuTopology property.
    pub gpu_topology: Option<String>,
    /// vmCount property.
    pub vm_count: Option<i64>,
}

/// `InstancesGetEffectiveFirewallsResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct InstancesGetEffectiveFirewallsResponse {
    /// firewallPolicys property.
    pub firewall_policys:
        Option<Vec<InstancesGetEffectiveFirewallsResponseEffectiveFirewallPolicy>>,
    /// firewalls property.
    pub firewalls: Option<Vec<Firewall>>,
}

/// `InstanceGroupsAddInstancesRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct InstanceGroupsAddInstancesRequest {
    /// instances property.
    pub instances: Option<Vec<InstanceReference>>,
}

/// `SecurityPolicyList` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SecurityPolicyList {
    /// id property.
    pub id: Option<String>,
    /// items property.
    pub items: Option<Vec<SecurityPolicy>>,
    /// kind property.
    pub kind: Option<String>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// warning property.
    pub warning: Option<std::collections::HashMap<String, serde_json::Value>>,
}

/// `RoutersGetRoutePolicyResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct RoutersGetRoutePolicyResponse {
    /// resource property.
    pub resource: Option<RoutePolicy>,
}

/// `HttpHeaderAction` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct HttpHeaderAction {
    /// requestHeadersToAdd property.
    pub request_headers_to_add: Option<Vec<HttpHeaderOption>>,
    /// requestHeadersToRemove property.
    pub request_headers_to_remove: Option<Vec<String>>,
    /// responseHeadersToAdd property.
    pub response_headers_to_add: Option<Vec<HttpHeaderOption>>,
    /// responseHeadersToRemove property.
    pub response_headers_to_remove: Option<Vec<String>>,
}

/// `RegionAddressesMoveRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct RegionAddressesMoveRequest {
    /// description property.
    pub description: Option<String>,
    /// destinationAddress property.
    pub destination_address: Option<String>,
}

/// `InstanceGroupAggregatedList` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct InstanceGroupAggregatedList {
    /// id property.
    pub id: Option<String>,
    /// items property.
    pub items: Option<serde_json::Value>,
    /// kind property.
    pub kind: Option<String>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// unreachables property.
    pub unreachables: Option<Vec<String>>,
    /// warning property.
    pub warning: Option<std::collections::HashMap<String, serde_json::Value>>,
}

/// `InstanceGroupsListInstancesRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct InstanceGroupsListInstancesRequest {
    /// instanceState property.
    pub instance_state: Option<String>,
}

/// `PreviewFeature` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct PreviewFeature {
    /// activationStatus property.
    pub activation_status: Option<String>,
    /// creationTimestamp property.
    pub creation_timestamp: Option<String>,
    /// description property.
    pub description: Option<String>,
    /// id property.
    pub id: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// rolloutOperation property.
    pub rollout_operation: Option<PreviewFeatureRolloutOperation>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// status property.
    pub status: Option<PreviewFeatureStatus>,
}

/// `TargetPoolAggregatedList` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct TargetPoolAggregatedList {
    /// id property.
    pub id: Option<String>,
    /// items property.
    pub items: Option<serde_json::Value>,
    /// kind property.
    pub kind: Option<String>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// unreachables property.
    pub unreachables: Option<Vec<String>>,
    /// warning property.
    pub warning: Option<std::collections::HashMap<String, serde_json::Value>>,
}

/// `ServiceAttachmentList` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ServiceAttachmentList {
    /// id property.
    pub id: Option<String>,
    /// items property.
    pub items: Option<Vec<ServiceAttachment>>,
    /// kind property.
    pub kind: Option<String>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// warning property.
    pub warning: Option<std::collections::HashMap<String, serde_json::Value>>,
}

/// `HttpRouteAction` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct HttpRouteAction {
    /// corsPolicy property.
    pub cors_policy: Option<CorsPolicy>,
    /// faultInjectionPolicy property.
    pub fault_injection_policy: Option<HttpFaultInjection>,
    /// maxStreamDuration property.
    pub max_stream_duration: Option<Duration>,
    /// requestMirrorPolicy property.
    pub request_mirror_policy: Option<RequestMirrorPolicy>,
    /// retryPolicy property.
    pub retry_policy: Option<HttpRetryPolicy>,
    /// timeout property.
    pub timeout: Option<Duration>,
    /// urlRewrite property.
    pub url_rewrite: Option<UrlRewrite>,
    /// weightedBackendServices property.
    pub weighted_backend_services: Option<Vec<WeightedBackendService>>,
}

/// `SecurityPolicyAdaptiveProtectionConfigLayer7DdosDefenseConfigThresholdConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SecurityPolicyAdaptiveProtectionConfigLayer7DdosDefenseConfigThresholdConfig {
    /// autoDeployConfidenceThreshold property.
    pub auto_deploy_confidence_threshold: Option<f64>,
    /// autoDeployExpirationSec property.
    pub auto_deploy_expiration_sec: Option<i64>,
    /// autoDeployImpactedBaselineThreshold property.
    pub auto_deploy_impacted_baseline_threshold: Option<f64>,
    /// autoDeployLoadThreshold property.
    pub auto_deploy_load_threshold: Option<f64>,
    /// detectionAbsoluteQps property.
    pub detection_absolute_qps: Option<f64>,
    /// detectionLoadThreshold property.
    pub detection_load_threshold: Option<f64>,
    /// detectionRelativeToBaselineQps property.
    pub detection_relative_to_baseline_qps: Option<f64>,
    /// name property.
    pub name: Option<String>,
    /// trafficGranularityConfigs property.
    pub traffic_granularity_configs: Option<Vec<SecurityPolicyAdaptiveProtectionConfigLayer7DdosDefenseConfigThresholdConfigTrafficGranularityConfig>>,
}

/// `SecurityPoliciesAggregatedList` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SecurityPoliciesAggregatedList {
    /// etag property.
    pub etag: Option<String>,
    /// id property.
    pub id: Option<String>,
    /// items property.
    pub items: Option<serde_json::Value>,
    /// kind property.
    pub kind: Option<String>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// unreachables property.
    pub unreachables: Option<Vec<String>>,
    /// warning property.
    pub warning: Option<std::collections::HashMap<String, serde_json::Value>>,
}

/// `BackendBackendOrchestrationInfo` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct BackendBackendOrchestrationInfo {
    /// resourceUri property.
    pub resource_uri: Option<String>,
}

/// `ImageList` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ImageList {
    /// id property.
    pub id: Option<String>,
    /// items property.
    pub items: Option<Vec<Image>>,
    /// kind property.
    pub kind: Option<String>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// warning property.
    pub warning: Option<std::collections::HashMap<String, serde_json::Value>>,
}

/// `SslCertificateManagedSslCertificate` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SslCertificateManagedSslCertificate {
    /// domainStatus property.
    pub domain_status: Option<serde_json::Value>,
    /// domains property.
    pub domains: Option<Vec<String>>,
    /// status property.
    pub status: Option<String>,
}

/// `ReservationBlocksGetResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ReservationBlocksGetResponse {
    /// resource property.
    pub resource: Option<ReservationBlock>,
}

/// `InstanceGroupManagersStartInstancesRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct InstanceGroupManagersStartInstancesRequest {
    /// instances property.
    pub instances: Option<Vec<String>>,
}

/// `ReservationSlotsListResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ReservationSlotsListResponse {
    /// id property.
    pub id: Option<String>,
    /// items property.
    pub items: Option<Vec<ReservationSlot>>,
    /// kind property.
    pub kind: Option<String>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// warning property.
    pub warning: Option<std::collections::HashMap<String, serde_json::Value>>,
}

/// `InterconnectGroupsOperationalStatusInterconnectStatus` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct InterconnectGroupsOperationalStatusInterconnectStatus {
    /// adminEnabled property.
    pub admin_enabled: Option<bool>,
    /// diagnostics property.
    pub diagnostics: Option<InterconnectDiagnostics>,
    /// interconnect property.
    pub interconnect: Option<String>,
    /// isActive property.
    pub is_active: Option<String>,
}

/// `CompositeHealthChecksGetHealthResponseHealthSourceHealth` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct CompositeHealthChecksGetHealthResponseHealthSourceHealth {
    /// healthState property.
    pub health_state: Option<String>,
    /// source property.
    pub source: Option<String>,
}

/// `BackendServiceGroupHealth` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct BackendServiceGroupHealth {
    /// annotations property.
    pub annotations: Option<serde_json::Value>,
    /// healthStatus property.
    pub health_status: Option<Vec<HealthStatus>>,
    /// kind property.
    pub kind: Option<String>,
}

/// `NodeGroupsSimulateMaintenanceEventRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct NodeGroupsSimulateMaintenanceEventRequest {
    /// nodes property.
    pub nodes: Option<Vec<String>>,
}

/// `BackendBucketParams` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct BackendBucketParams {
    /// resourceManagerTags property.
    pub resource_manager_tags: Option<serde_json::Value>,
}

/// `ResourcePolicyResourceStatus` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ResourcePolicyResourceStatus {
    /// instanceSchedulePolicy property.
    pub instance_schedule_policy: Option<ResourcePolicyResourceStatusInstanceSchedulePolicyStatus>,
}

/// `NetworkProfileProfileType` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct NetworkProfileProfileType {
    /// networkType property.
    pub network_type: Option<String>,
    /// rdmaSubtype property.
    pub rdma_subtype: Option<String>,
    /// ullSubtype property.
    pub ull_subtype: Option<String>,
    /// vpcSubtype property.
    pub vpc_subtype: Option<String>,
}

/// `SchedulingNodeAffinity` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SchedulingNodeAffinity {
    /// key property.
    pub key: Option<String>,
    /// operator property.
    pub operator: Option<String>,
    /// values property.
    pub values: Option<Vec<String>>,
}

/// `DisksAddResourcePoliciesRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct DisksAddResourcePoliciesRequest {
    /// resourcePolicies property.
    pub resource_policies: Option<Vec<String>>,
}

/// `SslCertificate` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SslCertificate {
    /// certificate property.
    pub certificate: Option<String>,
    /// creationTimestamp property.
    pub creation_timestamp: Option<String>,
    /// description property.
    pub description: Option<String>,
    /// expireTime property.
    pub expire_time: Option<String>,
    /// id property.
    pub id: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// managed property.
    pub managed: Option<SslCertificateManagedSslCertificate>,
    /// name property.
    pub name: Option<String>,
    /// privateKey property.
    pub private_key: Option<String>,
    /// region property.
    pub region: Option<String>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// selfManaged property.
    pub self_managed: Option<SslCertificateSelfManagedSslCertificate>,
    /// subjectAlternativeNames property.
    pub subject_alternative_names: Option<Vec<String>>,
    /// type property.
    pub r#type: Option<String>,
}

/// `SubnetworkParams` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SubnetworkParams {
    /// resourceManagerTags property.
    pub resource_manager_tags: Option<serde_json::Value>,
}

/// `InstanceGroupManagerInstanceFlexibilityPolicy` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct InstanceGroupManagerInstanceFlexibilityPolicy {
    /// instanceSelections property.
    pub instance_selections: Option<serde_json::Value>,
}

/// `ExternalVpnGatewayInterface` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ExternalVpnGatewayInterface {
    /// id property.
    pub id: Option<i64>,
    /// ipAddress property.
    pub ip_address: Option<String>,
    /// ipv6Address property.
    pub ipv6_address: Option<String>,
}

/// `ForwardingRule` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ForwardingRule {
    /// IPAddress property.
    pub ip_address: Option<String>,
    /// IPProtocol property.
    pub ip_protocol: Option<String>,
    /// allPorts property.
    pub all_ports: Option<bool>,
    /// allowGlobalAccess property.
    pub allow_global_access: Option<bool>,
    /// allowPscGlobalAccess property.
    pub allow_psc_global_access: Option<bool>,
    /// backendService property.
    pub backend_service: Option<String>,
    /// baseForwardingRule property.
    pub base_forwarding_rule: Option<String>,
    /// creationTimestamp property.
    pub creation_timestamp: Option<String>,
    /// description property.
    pub description: Option<String>,
    /// externalManagedBackendBucketMigrationState property.
    pub external_managed_backend_bucket_migration_state: Option<String>,
    /// externalManagedBackendBucketMigrationTestingPercentage property.
    pub external_managed_backend_bucket_migration_testing_percentage: Option<f64>,
    /// fingerprint property.
    pub fingerprint: Option<String>,
    /// id property.
    pub id: Option<String>,
    /// ipCollection property.
    pub ip_collection: Option<String>,
    /// ipVersion property.
    pub ip_version: Option<String>,
    /// isMirroringCollector property.
    pub is_mirroring_collector: Option<bool>,
    /// kind property.
    pub kind: Option<String>,
    /// labelFingerprint property.
    pub label_fingerprint: Option<String>,
    /// labels property.
    pub labels: Option<serde_json::Value>,
    /// loadBalancingScheme property.
    pub load_balancing_scheme: Option<String>,
    /// metadataFilters property.
    pub metadata_filters: Option<Vec<MetadataFilter>>,
    /// name property.
    pub name: Option<String>,
    /// network property.
    pub network: Option<String>,
    /// networkTier property.
    pub network_tier: Option<String>,
    /// noAutomateDnsZone property.
    pub no_automate_dns_zone: Option<bool>,
    /// portRange property.
    pub port_range: Option<String>,
    /// ports property.
    pub ports: Option<Vec<String>>,
    /// pscConnectionId property.
    pub psc_connection_id: Option<String>,
    /// pscConnectionStatus property.
    pub psc_connection_status: Option<String>,
    /// region property.
    pub region: Option<String>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// selfLinkWithId property.
    pub self_link_with_id: Option<String>,
    /// serviceDirectoryRegistrations property.
    pub service_directory_registrations: Option<Vec<ForwardingRuleServiceDirectoryRegistration>>,
    /// serviceLabel property.
    pub service_label: Option<String>,
    /// serviceName property.
    pub service_name: Option<String>,
    /// sourceIpRanges property.
    pub source_ip_ranges: Option<Vec<String>>,
    /// subnetwork property.
    pub subnetwork: Option<String>,
    /// target property.
    pub target: Option<String>,
}

/// `RegionInstanceGroupManagersSetTemplateRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct RegionInstanceGroupManagersSetTemplateRequest {
    /// instanceTemplate property.
    pub instance_template: Option<String>,
}

/// `RegionTargetHttpsProxiesSetSslCertificatesRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct RegionTargetHttpsProxiesSetSslCertificatesRequest {
    /// sslCertificates property.
    pub ssl_certificates: Option<Vec<String>>,
}

/// `TargetPoolsRemoveInstanceRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct TargetPoolsRemoveInstanceRequest {
    /// instances property.
    pub instances: Option<Vec<InstanceReference>>,
}

/// `ReservationSlot` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ReservationSlot {
    /// creationTimestamp property.
    pub creation_timestamp: Option<String>,
    /// id property.
    pub id: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// physicalTopology property.
    pub physical_topology: Option<ReservationSlotPhysicalTopology>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// selfLinkWithId property.
    pub self_link_with_id: Option<String>,
    /// shareSettings property.
    pub share_settings: Option<ShareSettings>,
    /// state property.
    pub state: Option<String>,
    /// status property.
    pub status: Option<ReservationSlotStatus>,
    /// zone property.
    pub zone: Option<String>,
}

/// `InterconnectRemoteLocationList` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct InterconnectRemoteLocationList {
    /// id property.
    pub id: Option<String>,
    /// items property.
    pub items: Option<Vec<InterconnectRemoteLocation>>,
    /// kind property.
    pub kind: Option<String>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// warning property.
    pub warning: Option<std::collections::HashMap<String, serde_json::Value>>,
}

/// `RouterAdvertisedIpRange` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct RouterAdvertisedIpRange {
    /// description property.
    pub description: Option<String>,
    /// range property.
    pub range: Option<String>,
}

/// `InterconnectAttachmentConfigurationConstraints` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct InterconnectAttachmentConfigurationConstraints {
    /// bgpMd5 property.
    pub bgp_md5: Option<String>,
    /// bgpPeerAsnRanges property.
    pub bgp_peer_asn_ranges:
        Option<Vec<InterconnectAttachmentConfigurationConstraintsBgpPeerASNRange>>,
}

/// `InterconnectAttachmentGroupLogicalStructureRegionMetroFacility` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct InterconnectAttachmentGroupLogicalStructureRegionMetroFacility {
    /// facility property.
    pub facility: Option<String>,
    /// zones property.
    pub zones: Option<Vec<InterconnectAttachmentGroupLogicalStructureRegionMetroFacilityZone>>,
}

/// `GlobalSetPolicyRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GlobalSetPolicyRequest {
    /// bindings property.
    pub bindings: Option<Vec<Binding>>,
    /// etag property.
    pub etag: Option<String>,
    /// policy property.
    pub policy: Option<Policy>,
}

/// `ReservationSubBlocksListResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ReservationSubBlocksListResponse {
    /// id property.
    pub id: Option<String>,
    /// items property.
    pub items: Option<Vec<ReservationSubBlock>>,
    /// kind property.
    pub kind: Option<String>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// warning property.
    pub warning: Option<std::collections::HashMap<String, serde_json::Value>>,
}

/// `SourceInstanceProperties` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SourceInstanceProperties {
    /// canIpForward property.
    pub can_ip_forward: Option<bool>,
    /// deletionProtection property.
    pub deletion_protection: Option<bool>,
    /// description property.
    pub description: Option<String>,
    /// disks property.
    pub disks: Option<Vec<SavedAttachedDisk>>,
    /// guestAccelerators property.
    pub guest_accelerators: Option<Vec<AcceleratorConfig>>,
    /// keyRevocationActionType property.
    pub key_revocation_action_type: Option<String>,
    /// labels property.
    pub labels: Option<serde_json::Value>,
    /// machineType property.
    pub machine_type: Option<String>,
    /// metadata property.
    pub metadata: Option<Metadata>,
    /// minCpuPlatform property.
    pub min_cpu_platform: Option<String>,
    /// networkInterfaces property.
    pub network_interfaces: Option<Vec<NetworkInterface>>,
    /// postKeyRevocationActionType property.
    pub post_key_revocation_action_type: Option<String>,
    /// scheduling property.
    pub scheduling: Option<Scheduling>,
    /// serviceAccounts property.
    pub service_accounts: Option<Vec<ServiceAccount>>,
    /// tags property.
    pub tags: Option<Tags>,
}

/// `InterconnectGroupPhysicalStructureMetrosFacilities` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct InterconnectGroupPhysicalStructureMetrosFacilities {
    /// facility property.
    pub facility: Option<String>,
    /// zones property.
    pub zones: Option<Vec<InterconnectGroupPhysicalStructureMetrosFacilitiesZones>>,
}

/// `NodeGroupAggregatedList` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct NodeGroupAggregatedList {
    /// id property.
    pub id: Option<String>,
    /// items property.
    pub items: Option<serde_json::Value>,
    /// kind property.
    pub kind: Option<String>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// unreachables property.
    pub unreachables: Option<Vec<String>>,
    /// warning property.
    pub warning: Option<std::collections::HashMap<String, serde_json::Value>>,
}

/// `SnapshotSettingsStorageLocationSettings` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SnapshotSettingsStorageLocationSettings {
    /// locations property.
    pub locations: Option<serde_json::Value>,
    /// policy property.
    pub policy: Option<String>,
}

/// `ZoneSetLabelsRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ZoneSetLabelsRequest {
    /// labelFingerprint property.
    pub label_fingerprint: Option<String>,
    /// labels property.
    pub labels: Option<serde_json::Value>,
}

/// `RegionDisksRemoveResourcePoliciesRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct RegionDisksRemoveResourcePoliciesRequest {
    /// resourcePolicies property.
    pub resource_policies: Option<Vec<String>>,
}

/// `HealthAggregationPolicyAggregatedList` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct HealthAggregationPolicyAggregatedList {
    /// id property.
    pub id: Option<String>,
    /// items property.
    pub items: Option<serde_json::Value>,
    /// kind property.
    pub kind: Option<String>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// unreachables property.
    pub unreachables: Option<Vec<String>>,
    /// warning property.
    pub warning: Option<std::collections::HashMap<String, serde_json::Value>>,
}

/// `ResourcePolicyList` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ResourcePolicyList {
    /// etag property.
    pub etag: Option<String>,
    /// id property.
    pub id: Option<String>,
    /// items property.
    pub items: Option<Vec<ResourcePolicy>>,
    /// kind property.
    pub kind: Option<String>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// warning property.
    pub warning: Option<std::collections::HashMap<String, serde_json::Value>>,
}

/// `InstanceGroup` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct InstanceGroup {
    /// creationTimestamp property.
    pub creation_timestamp: Option<String>,
    /// description property.
    pub description: Option<String>,
    /// fingerprint property.
    pub fingerprint: Option<String>,
    /// id property.
    pub id: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// namedPorts property.
    pub named_ports: Option<Vec<NamedPort>>,
    /// network property.
    pub network: Option<String>,
    /// region property.
    pub region: Option<String>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// size property.
    pub size: Option<i64>,
    /// subnetwork property.
    pub subnetwork: Option<String>,
    /// zone property.
    pub zone: Option<String>,
}

/// `RouterNat` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct RouterNat {
    /// autoNetworkTier property.
    pub auto_network_tier: Option<String>,
    /// drainNatIps property.
    pub drain_nat_ips: Option<Vec<String>>,
    /// enableDynamicPortAllocation property.
    pub enable_dynamic_port_allocation: Option<bool>,
    /// enableEndpointIndependentMapping property.
    pub enable_endpoint_independent_mapping: Option<bool>,
    /// endpointTypes property.
    pub endpoint_types: Option<Vec<String>>,
    /// icmpIdleTimeoutSec property.
    pub icmp_idle_timeout_sec: Option<i64>,
    /// logConfig property.
    pub log_config: Option<RouterNatLogConfig>,
    /// maxPortsPerVm property.
    pub max_ports_per_vm: Option<i64>,
    /// minPortsPerVm property.
    pub min_ports_per_vm: Option<i64>,
    /// name property.
    pub name: Option<String>,
    /// nat64Subnetworks property.
    pub nat64_subnetworks: Option<Vec<RouterNatSubnetworkToNat64>>,
    /// natIpAllocateOption property.
    pub nat_ip_allocate_option: Option<String>,
    /// natIps property.
    pub nat_ips: Option<Vec<String>>,
    /// rules property.
    pub rules: Option<Vec<RouterNatRule>>,
    /// sourceSubnetworkIpRangesToNat property.
    pub source_subnetwork_ip_ranges_to_nat: Option<String>,
    /// sourceSubnetworkIpRangesToNat64 property.
    pub source_subnetwork_ip_ranges_to_nat64: Option<String>,
    /// subnetworks property.
    pub subnetworks: Option<Vec<RouterNatSubnetworkToNat>>,
    /// tcpEstablishedIdleTimeoutSec property.
    pub tcp_established_idle_timeout_sec: Option<i64>,
    /// tcpTimeWaitTimeoutSec property.
    pub tcp_time_wait_timeout_sec: Option<i64>,
    /// tcpTransitoryIdleTimeoutSec property.
    pub tcp_transitory_idle_timeout_sec: Option<i64>,
    /// type property.
    pub r#type: Option<String>,
    /// udpIdleTimeoutSec property.
    pub udp_idle_timeout_sec: Option<i64>,
}

/// `InstanceGroupManagerUpdatePolicy` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct InstanceGroupManagerUpdatePolicy {
    /// instanceRedistributionType property.
    pub instance_redistribution_type: Option<String>,
    /// maxSurge property.
    pub max_surge: Option<FixedOrPercent>,
    /// maxUnavailable property.
    pub max_unavailable: Option<FixedOrPercent>,
    /// minimalAction property.
    pub minimal_action: Option<String>,
    /// mostDisruptiveAllowedAction property.
    pub most_disruptive_allowed_action: Option<String>,
    /// replacementMethod property.
    pub replacement_method: Option<String>,
    /// type property.
    pub r#type: Option<String>,
}

/// `SecurityPolicyRuleMatcherExprOptions` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SecurityPolicyRuleMatcherExprOptions {
    /// recaptchaOptions property.
    pub recaptcha_options: Option<SecurityPolicyRuleMatcherExprOptionsRecaptchaOptions>,
}

/// `InstanceGroupManagerVersion` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct InstanceGroupManagerVersion {
    /// instanceTemplate property.
    pub instance_template: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// targetSize property.
    pub target_size: Option<FixedOrPercent>,
}

/// `Firewall` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Firewall {
    /// allowed property.
    pub allowed: Option<Vec<std::collections::HashMap<String, serde_json::Value>>>,
    /// creationTimestamp property.
    pub creation_timestamp: Option<String>,
    /// denied property.
    pub denied: Option<Vec<std::collections::HashMap<String, serde_json::Value>>>,
    /// description property.
    pub description: Option<String>,
    /// destinationRanges property.
    pub destination_ranges: Option<Vec<String>>,
    /// direction property.
    pub direction: Option<String>,
    /// disabled property.
    pub disabled: Option<bool>,
    /// id property.
    pub id: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// logConfig property.
    pub log_config: Option<FirewallLogConfig>,
    /// name property.
    pub name: Option<String>,
    /// network property.
    pub network: Option<String>,
    /// params property.
    pub params: Option<FirewallParams>,
    /// priority property.
    pub priority: Option<i64>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// sourceRanges property.
    pub source_ranges: Option<Vec<String>>,
    /// sourceServiceAccounts property.
    pub source_service_accounts: Option<Vec<String>>,
    /// sourceTags property.
    pub source_tags: Option<Vec<String>>,
    /// targetServiceAccounts property.
    pub target_service_accounts: Option<Vec<String>>,
    /// targetTags property.
    pub target_tags: Option<Vec<String>>,
}

/// `InterconnectDiagnosticsLinkOpticalPower` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct InterconnectDiagnosticsLinkOpticalPower {
    /// state property.
    pub state: Option<String>,
    /// value property.
    pub value: Option<f64>,
}

/// `InstanceGroupManagerAutoHealingPolicy` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct InstanceGroupManagerAutoHealingPolicy {
    /// healthCheck property.
    pub health_check: Option<String>,
    /// initialDelaySec property.
    pub initial_delay_sec: Option<i64>,
}

/// `ResourceGroupReference` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ResourceGroupReference {
    /// group property.
    pub group: Option<String>,
}

/// `BackendServiceList` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct BackendServiceList {
    /// id property.
    pub id: Option<String>,
    /// items property.
    pub items: Option<Vec<BackendService>>,
    /// kind property.
    pub kind: Option<String>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// warning property.
    pub warning: Option<std::collections::HashMap<String, serde_json::Value>>,
}

/// `NodeTemplateAggregatedList` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct NodeTemplateAggregatedList {
    /// id property.
    pub id: Option<String>,
    /// items property.
    pub items: Option<serde_json::Value>,
    /// kind property.
    pub kind: Option<String>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// unreachables property.
    pub unreachables: Option<Vec<String>>,
    /// warning property.
    pub warning: Option<std::collections::HashMap<String, serde_json::Value>>,
}

/// `RegionInstanceGroupManagerUpdateInstanceConfigReq` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct RegionInstanceGroupManagerUpdateInstanceConfigReq {
    /// perInstanceConfigs property.
    pub per_instance_configs: Option<Vec<PerInstanceConfig>>,
}

/// `TargetSslProxiesSetCertificateMapRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct TargetSslProxiesSetCertificateMapRequest {
    /// certificateMap property.
    pub certificate_map: Option<String>,
}

/// `Zone` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Zone {
    /// availableCpuPlatforms property.
    pub available_cpu_platforms: Option<Vec<String>>,
    /// creationTimestamp property.
    pub creation_timestamp: Option<String>,
    /// deprecated property.
    pub deprecated: Option<DeprecationStatus>,
    /// description property.
    pub description: Option<String>,
    /// id property.
    pub id: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// region property.
    pub region: Option<String>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// status property.
    pub status: Option<String>,
    /// supportsPzs property.
    pub supports_pzs: Option<bool>,
}

/// `DiskType` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct DiskType {
    /// creationTimestamp property.
    pub creation_timestamp: Option<String>,
    /// defaultDiskSizeGb property.
    pub default_disk_size_gb: Option<String>,
    /// deprecated property.
    pub deprecated: Option<DeprecationStatus>,
    /// description property.
    pub description: Option<String>,
    /// id property.
    pub id: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// region property.
    pub region: Option<String>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// validDiskSize property.
    pub valid_disk_size: Option<String>,
    /// zone property.
    pub zone: Option<String>,
}

/// `RouterBgpPeerBfd` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct RouterBgpPeerBfd {
    /// minReceiveInterval property.
    pub min_receive_interval: Option<i64>,
    /// minTransmitInterval property.
    pub min_transmit_interval: Option<i64>,
    /// multiplier property.
    pub multiplier: Option<i64>,
    /// sessionInitializationMode property.
    pub session_initialization_mode: Option<String>,
}

/// `MetadataFilterLabelMatch` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct MetadataFilterLabelMatch {
    /// name property.
    pub name: Option<String>,
    /// value property.
    pub value: Option<String>,
}

/// `InterconnectMacsecConfigPreSharedKey` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct InterconnectMacsecConfigPreSharedKey {
    /// cak property.
    pub cak: Option<String>,
    /// ckn property.
    pub ckn: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// startTime property.
    pub start_time: Option<String>,
}

/// `SecurityPolicyRuleMatcherConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SecurityPolicyRuleMatcherConfig {
    /// srcIpRanges property.
    pub src_ip_ranges: Option<Vec<String>>,
}

/// `InstanceGroupManagersUpdatePerInstanceConfigsReq` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct InstanceGroupManagersUpdatePerInstanceConfigsReq {
    /// perInstanceConfigs property.
    pub per_instance_configs: Option<Vec<PerInstanceConfig>>,
}

/// `SecurityPolicy` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SecurityPolicy {
    /// adaptiveProtectionConfig property.
    pub adaptive_protection_config: Option<SecurityPolicyAdaptiveProtectionConfig>,
    /// advancedOptionsConfig property.
    pub advanced_options_config: Option<SecurityPolicyAdvancedOptionsConfig>,
    /// associations property.
    pub associations: Option<Vec<SecurityPolicyAssociation>>,
    /// creationTimestamp property.
    pub creation_timestamp: Option<String>,
    /// ddosProtectionConfig property.
    pub ddos_protection_config: Option<SecurityPolicyDdosProtectionConfig>,
    /// description property.
    pub description: Option<String>,
    /// fingerprint property.
    pub fingerprint: Option<String>,
    /// id property.
    pub id: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// labelFingerprint property.
    pub label_fingerprint: Option<String>,
    /// labels property.
    pub labels: Option<serde_json::Value>,
    /// name property.
    pub name: Option<String>,
    /// parent property.
    pub parent: Option<String>,
    /// recaptchaOptionsConfig property.
    pub recaptcha_options_config: Option<SecurityPolicyRecaptchaOptionsConfig>,
    /// region property.
    pub region: Option<String>,
    /// rules property.
    pub rules: Option<Vec<SecurityPolicyRule>>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// shortName property.
    pub short_name: Option<String>,
    /// type property.
    pub r#type: Option<String>,
    /// userDefinedFields property.
    pub user_defined_fields: Option<Vec<SecurityPolicyUserDefinedField>>,
}

/// `CompositeHealthCheckAggregatedList` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct CompositeHealthCheckAggregatedList {
    /// id property.
    pub id: Option<String>,
    /// items property.
    pub items: Option<serde_json::Value>,
    /// kind property.
    pub kind: Option<String>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// unreachables property.
    pub unreachables: Option<Vec<String>>,
    /// warning property.
    pub warning: Option<std::collections::HashMap<String, serde_json::Value>>,
}

/// `CompositeHealthCheckList` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct CompositeHealthCheckList {
    /// id property.
    pub id: Option<String>,
    /// items property.
    pub items: Option<Vec<CompositeHealthCheck>>,
    /// kind property.
    pub kind: Option<String>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// warning property.
    pub warning: Option<std::collections::HashMap<String, serde_json::Value>>,
}

/// `TargetHttpsProxiesSetSslCertificatesRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct TargetHttpsProxiesSetSslCertificatesRequest {
    /// sslCertificates property.
    pub ssl_certificates: Option<Vec<String>>,
}

/// `VpnTunnelList` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct VpnTunnelList {
    /// id property.
    pub id: Option<String>,
    /// items property.
    pub items: Option<Vec<VpnTunnel>>,
    /// kind property.
    pub kind: Option<String>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// warning property.
    pub warning: Option<std::collections::HashMap<String, serde_json::Value>>,
}

/// `InstanceTemplateAggregatedList` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct InstanceTemplateAggregatedList {
    /// id property.
    pub id: Option<String>,
    /// items property.
    pub items: Option<serde_json::Value>,
    /// kind property.
    pub kind: Option<String>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// warning property.
    pub warning: Option<std::collections::HashMap<String, serde_json::Value>>,
}

/// `BackendBucketListUsable` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct BackendBucketListUsable {
    /// id property.
    pub id: Option<String>,
    /// items property.
    pub items: Option<Vec<BackendBucket>>,
    /// kind property.
    pub kind: Option<String>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// warning property.
    pub warning: Option<std::collections::HashMap<String, serde_json::Value>>,
}

/// `RegionNetworkFirewallPoliciesGetEffectiveFirewallsResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct RegionNetworkFirewallPoliciesGetEffectiveFirewallsResponse {
    /// firewallPolicys property.
    pub firewall_policys: Option<
        Vec<RegionNetworkFirewallPoliciesGetEffectiveFirewallsResponseEffectiveFirewallPolicy>,
    >,
    /// firewalls property.
    pub firewalls: Option<Vec<Firewall>>,
}

/// `PacketMirroringMirroredResourceInfoSubnetInfo` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct PacketMirroringMirroredResourceInfoSubnetInfo {
    /// canonicalUrl property.
    pub canonical_url: Option<String>,
    /// url property.
    pub url: Option<String>,
}

/// `DiskResourceStatus` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct DiskResourceStatus {
    /// asyncPrimaryDisk property.
    pub async_primary_disk: Option<DiskResourceStatusAsyncReplicationStatus>,
    /// asyncSecondaryDisks property.
    pub async_secondary_disks: Option<serde_json::Value>,
}

/// `Snapshot` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Snapshot {
    /// architecture property.
    pub architecture: Option<String>,
    /// autoCreated property.
    pub auto_created: Option<bool>,
    /// chainName property.
    pub chain_name: Option<String>,
    /// creationSizeBytes property.
    pub creation_size_bytes: Option<String>,
    /// creationTimestamp property.
    pub creation_timestamp: Option<String>,
    /// description property.
    pub description: Option<String>,
    /// diskSizeGb property.
    pub disk_size_gb: Option<String>,
    /// downloadBytes property.
    pub download_bytes: Option<String>,
    /// enableConfidentialCompute property.
    pub enable_confidential_compute: Option<bool>,
    /// guestFlush property.
    pub guest_flush: Option<bool>,
    /// guestOsFeatures property.
    pub guest_os_features: Option<Vec<GuestOsFeature>>,
    /// id property.
    pub id: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// labelFingerprint property.
    pub label_fingerprint: Option<String>,
    /// labels property.
    pub labels: Option<serde_json::Value>,
    /// licenseCodes property.
    pub license_codes: Option<Vec<String>>,
    /// licenses property.
    pub licenses: Option<Vec<String>>,
    /// locationHint property.
    pub location_hint: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// params property.
    pub params: Option<SnapshotParams>,
    /// region property.
    pub region: Option<String>,
    /// satisfiesPzi property.
    pub satisfies_pzi: Option<bool>,
    /// satisfiesPzs property.
    pub satisfies_pzs: Option<bool>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// snapshotEncryptionKey property.
    pub snapshot_encryption_key: Option<CustomerEncryptionKey>,
    /// snapshotGroupId property.
    pub snapshot_group_id: Option<String>,
    /// snapshotGroupName property.
    pub snapshot_group_name: Option<String>,
    /// snapshotType property.
    pub snapshot_type: Option<String>,
    /// sourceDisk property.
    pub source_disk: Option<String>,
    /// sourceDiskEncryptionKey property.
    pub source_disk_encryption_key: Option<CustomerEncryptionKey>,
    /// sourceDiskForRecoveryCheckpoint property.
    pub source_disk_for_recovery_checkpoint: Option<String>,
    /// sourceDiskId property.
    pub source_disk_id: Option<String>,
    /// sourceInstantSnapshot property.
    pub source_instant_snapshot: Option<String>,
    /// sourceInstantSnapshotEncryptionKey property.
    pub source_instant_snapshot_encryption_key: Option<CustomerEncryptionKey>,
    /// sourceInstantSnapshotId property.
    pub source_instant_snapshot_id: Option<String>,
    /// sourceSnapshotSchedulePolicy property.
    pub source_snapshot_schedule_policy: Option<String>,
    /// sourceSnapshotSchedulePolicyId property.
    pub source_snapshot_schedule_policy_id: Option<String>,
    /// status property.
    pub status: Option<String>,
    /// storageBytes property.
    pub storage_bytes: Option<String>,
    /// storageBytesStatus property.
    pub storage_bytes_status: Option<String>,
    /// storageLocations property.
    pub storage_locations: Option<Vec<String>>,
}

/// `NetworkEndpointGroupsDetachEndpointsRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct NetworkEndpointGroupsDetachEndpointsRequest {
    /// networkEndpoints property.
    pub network_endpoints: Option<Vec<NetworkEndpoint>>,
}

/// `BackendServiceCustomMetric` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct BackendServiceCustomMetric {
    /// dryRun property.
    pub dry_run: Option<bool>,
    /// name property.
    pub name: Option<String>,
}

/// `BackendServiceHttpCookie` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct BackendServiceHttpCookie {
    /// name property.
    pub name: Option<String>,
    /// path property.
    pub path: Option<String>,
    /// ttl property.
    pub ttl: Option<Duration>,
}

/// `BackendServiceUsedBy` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct BackendServiceUsedBy {
    /// reference property.
    pub reference: Option<String>,
}

/// `OperationList` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct OperationList {
    /// id property.
    pub id: Option<String>,
    /// items property.
    pub items: Option<Vec<Operation>>,
    /// kind property.
    pub kind: Option<String>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// warning property.
    pub warning: Option<std::collections::HashMap<String, serde_json::Value>>,
}

/// `SecurityPolicyDdosProtectionConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SecurityPolicyDdosProtectionConfig {
    /// ddosProtection property.
    pub ddos_protection: Option<String>,
}

/// `PreviewFeatureStatusReleaseStatus` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct PreviewFeatureStatusReleaseStatus {
    /// stage property.
    pub stage: Option<String>,
    /// updateDate property.
    pub update_date: Option<Date>,
}

/// `WafExpressionSetExpression` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct WafExpressionSetExpression {
    /// id property.
    pub id: Option<String>,
    /// sensitivity property.
    pub sensitivity: Option<i64>,
}

/// `HealthSourcesGetHealthResponseSourceInfoBackendInfo` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct HealthSourcesGetHealthResponseSourceInfoBackendInfo {
    /// endpointCount property.
    pub endpoint_count: Option<i64>,
    /// group property.
    pub group: Option<String>,
    /// healthyEndpointCount property.
    pub healthy_endpoint_count: Option<i64>,
}

/// `BackendBucket` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct BackendBucket {
    /// bucketName property.
    pub bucket_name: Option<String>,
    /// cdnPolicy property.
    pub cdn_policy: Option<BackendBucketCdnPolicy>,
    /// compressionMode property.
    pub compression_mode: Option<String>,
    /// creationTimestamp property.
    pub creation_timestamp: Option<String>,
    /// customResponseHeaders property.
    pub custom_response_headers: Option<Vec<String>>,
    /// description property.
    pub description: Option<String>,
    /// edgeSecurityPolicy property.
    pub edge_security_policy: Option<String>,
    /// enableCdn property.
    pub enable_cdn: Option<bool>,
    /// id property.
    pub id: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// loadBalancingScheme property.
    pub load_balancing_scheme: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// params property.
    pub params: Option<BackendBucketParams>,
    /// region property.
    pub region: Option<String>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// usedBy property.
    pub used_by: Option<Vec<BackendBucketUsedBy>>,
}

/// `Date` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Date {
    /// day property.
    pub day: Option<i64>,
    /// month property.
    pub month: Option<i64>,
    /// year property.
    pub year: Option<i64>,
}

/// `InstanceGroupManagersSetTargetPoolsRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct InstanceGroupManagersSetTargetPoolsRequest {
    /// fingerprint property.
    pub fingerprint: Option<String>,
    /// targetPools property.
    pub target_pools: Option<Vec<String>>,
}

/// `NodeGroupsPerformMaintenanceRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct NodeGroupsPerformMaintenanceRequest {
    /// nodes property.
    pub nodes: Option<Vec<String>>,
    /// startTime property.
    pub start_time: Option<String>,
}

/// `SslPoliciesAggregatedList` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SslPoliciesAggregatedList {
    /// etag property.
    pub etag: Option<String>,
    /// id property.
    pub id: Option<String>,
    /// items property.
    pub items: Option<serde_json::Value>,
    /// kind property.
    pub kind: Option<String>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// unreachables property.
    pub unreachables: Option<Vec<String>>,
    /// warning property.
    pub warning: Option<std::collections::HashMap<String, serde_json::Value>>,
}

/// `RouterStatus` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct RouterStatus {
    /// bestRoutes property.
    pub best_routes: Option<Vec<Route>>,
    /// bestRoutesForRouter property.
    pub best_routes_for_router: Option<Vec<Route>>,
    /// bgpPeerStatus property.
    pub bgp_peer_status: Option<Vec<RouterStatusBgpPeerStatus>>,
    /// natStatus property.
    pub nat_status: Option<Vec<RouterStatusNatStatus>>,
    /// network property.
    pub network: Option<String>,
}

/// `HealthCheckList` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct HealthCheckList {
    /// id property.
    pub id: Option<String>,
    /// items property.
    pub items: Option<Vec<HealthCheck>>,
    /// kind property.
    pub kind: Option<String>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// warning property.
    pub warning: Option<std::collections::HashMap<String, serde_json::Value>>,
}

/// `VpnTunnel` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct VpnTunnel {
    /// cipherSuite property.
    pub cipher_suite: Option<VpnTunnelCipherSuite>,
    /// creationTimestamp property.
    pub creation_timestamp: Option<String>,
    /// description property.
    pub description: Option<String>,
    /// detailedStatus property.
    pub detailed_status: Option<String>,
    /// id property.
    pub id: Option<String>,
    /// ikeVersion property.
    pub ike_version: Option<i64>,
    /// kind property.
    pub kind: Option<String>,
    /// labelFingerprint property.
    pub label_fingerprint: Option<String>,
    /// labels property.
    pub labels: Option<serde_json::Value>,
    /// localTrafficSelector property.
    pub local_traffic_selector: Option<Vec<String>>,
    /// name property.
    pub name: Option<String>,
    /// params property.
    pub params: Option<VpnTunnelParams>,
    /// peerExternalGateway property.
    pub peer_external_gateway: Option<String>,
    /// peerExternalGatewayInterface property.
    pub peer_external_gateway_interface: Option<i64>,
    /// peerGcpGateway property.
    pub peer_gcp_gateway: Option<String>,
    /// peerIp property.
    pub peer_ip: Option<String>,
    /// region property.
    pub region: Option<String>,
    /// remoteTrafficSelector property.
    pub remote_traffic_selector: Option<Vec<String>>,
    /// router property.
    pub router: Option<String>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// sharedSecret property.
    pub shared_secret: Option<String>,
    /// sharedSecretHash property.
    pub shared_secret_hash: Option<String>,
    /// status property.
    pub status: Option<String>,
    /// targetVpnGateway property.
    pub target_vpn_gateway: Option<String>,
    /// vpnGateway property.
    pub vpn_gateway: Option<String>,
    /// vpnGatewayInterface property.
    pub vpn_gateway_interface: Option<i64>,
}

/// `HttpHealthCheckList` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct HttpHealthCheckList {
    /// id property.
    pub id: Option<String>,
    /// items property.
    pub items: Option<Vec<HttpHealthCheck>>,
    /// kind property.
    pub kind: Option<String>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// warning property.
    pub warning: Option<std::collections::HashMap<String, serde_json::Value>>,
}

/// `InstanceGroupManagersDeleteInstancesRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct InstanceGroupManagersDeleteInstancesRequest {
    /// instances property.
    pub instances: Option<Vec<String>>,
    /// skipInstancesOnValidationError property.
    pub skip_instances_on_validation_error: Option<bool>,
}

/// `ZoneList` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ZoneList {
    /// id property.
    pub id: Option<String>,
    /// items property.
    pub items: Option<Vec<Zone>>,
    /// kind property.
    pub kind: Option<String>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// warning property.
    pub warning: Option<std::collections::HashMap<String, serde_json::Value>>,
}

/// `StoragePoolListDisks` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct StoragePoolListDisks {
    /// etag property.
    pub etag: Option<String>,
    /// id property.
    pub id: Option<String>,
    /// items property.
    pub items: Option<Vec<StoragePoolDisk>>,
    /// kind property.
    pub kind: Option<String>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// unreachables property.
    pub unreachables: Option<Vec<String>>,
    /// warning property.
    pub warning: Option<std::collections::HashMap<String, serde_json::Value>>,
}

/// `LocalizedMessage` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct LocalizedMessage {
    /// locale property.
    pub locale: Option<String>,
    /// message property.
    pub message: Option<String>,
}

/// `Autoscaler` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Autoscaler {
    /// autoscalingPolicy property.
    pub autoscaling_policy: Option<AutoscalingPolicy>,
    /// creationTimestamp property.
    pub creation_timestamp: Option<String>,
    /// description property.
    pub description: Option<String>,
    /// id property.
    pub id: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// recommendedSize property.
    pub recommended_size: Option<i64>,
    /// region property.
    pub region: Option<String>,
    /// scalingScheduleStatus property.
    pub scaling_schedule_status: Option<serde_json::Value>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// status property.
    pub status: Option<String>,
    /// statusDetails property.
    pub status_details: Option<Vec<AutoscalerStatusDetails>>,
    /// target property.
    pub target: Option<String>,
    /// zone property.
    pub zone: Option<String>,
}

/// `RegionInstanceGroupsListInstancesRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct RegionInstanceGroupsListInstancesRequest {
    /// instanceState property.
    pub instance_state: Option<String>,
    /// portName property.
    pub port_name: Option<String>,
}

/// `Tags` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Tags {
    /// fingerprint property.
    pub fingerprint: Option<String>,
    /// items property.
    pub items: Option<Vec<String>>,
}

/// `CustomErrorResponsePolicy` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct CustomErrorResponsePolicy {
    /// errorResponseRules property.
    pub error_response_rules: Option<Vec<CustomErrorResponsePolicyCustomErrorResponseRule>>,
    /// errorService property.
    pub error_service: Option<String>,
}

/// `BackendServiceConnectionTrackingPolicy` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct BackendServiceConnectionTrackingPolicy {
    /// connectionPersistenceOnUnhealthyBackends property.
    pub connection_persistence_on_unhealthy_backends: Option<String>,
    /// enableStrongAffinity property.
    pub enable_strong_affinity: Option<bool>,
    /// idleTimeoutSec property.
    pub idle_timeout_sec: Option<i64>,
    /// trackingMode property.
    pub tracking_mode: Option<String>,
}

/// `CalendarModeRecommendation` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct CalendarModeRecommendation {
    /// recommendationsPerSpec property.
    pub recommendations_per_spec: Option<serde_json::Value>,
}

/// `SetCommonInstanceMetadataOperationMetadata` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SetCommonInstanceMetadataOperationMetadata {
    /// clientOperationId property.
    pub client_operation_id: Option<String>,
    /// perLocationOperations property.
    pub per_location_operations: Option<serde_json::Value>,
}

/// `DistributionPolicyZoneConfiguration` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct DistributionPolicyZoneConfiguration {
    /// zone property.
    pub zone: Option<String>,
}

/// `TargetHttpsProxiesSetCertificateMapRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct TargetHttpsProxiesSetCertificateMapRequest {
    /// certificateMap property.
    pub certificate_map: Option<String>,
}

/// `TargetTcpProxyList` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct TargetTcpProxyList {
    /// id property.
    pub id: Option<String>,
    /// items property.
    pub items: Option<Vec<TargetTcpProxy>>,
    /// kind property.
    pub kind: Option<String>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// warning property.
    pub warning: Option<std::collections::HashMap<String, serde_json::Value>>,
}

/// `RouterBgpPeerCustomLearnedIpRange` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct RouterBgpPeerCustomLearnedIpRange {
    /// range property.
    pub range: Option<String>,
}

/// `PacketMirroringAggregatedList` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct PacketMirroringAggregatedList {
    /// id property.
    pub id: Option<String>,
    /// items property.
    pub items: Option<serde_json::Value>,
    /// kind property.
    pub kind: Option<String>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// unreachables property.
    pub unreachables: Option<Vec<String>>,
    /// warning property.
    pub warning: Option<std::collections::HashMap<String, serde_json::Value>>,
}

/// `FirewallPolicyAssociation` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct FirewallPolicyAssociation {
    /// attachmentTarget property.
    pub attachment_target: Option<String>,
    /// displayName property.
    pub display_name: Option<String>,
    /// firewallPolicyId property.
    pub firewall_policy_id: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// shortName property.
    pub short_name: Option<String>,
}

/// `WireProperties` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct WireProperties {
    /// bandwidthAllocation property.
    pub bandwidth_allocation: Option<String>,
    /// bandwidthUnmetered property.
    pub bandwidth_unmetered: Option<String>,
    /// faultResponse property.
    pub fault_response: Option<String>,
}

/// `BackendServiceLocalityLoadBalancingPolicyConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct BackendServiceLocalityLoadBalancingPolicyConfig {
    /// customPolicy property.
    pub custom_policy: Option<BackendServiceLocalityLoadBalancingPolicyConfigCustomPolicy>,
    /// policy property.
    pub policy: Option<BackendServiceLocalityLoadBalancingPolicyConfigPolicy>,
}

/// `BfdStatus` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct BfdStatus {
    /// bfdSessionInitializationMode property.
    pub bfd_session_initialization_mode: Option<String>,
    /// configUpdateTimestampMicros property.
    pub config_update_timestamp_micros: Option<String>,
    /// controlPacketCounts property.
    pub control_packet_counts: Option<BfdStatusPacketCounts>,
    /// controlPacketIntervals property.
    pub control_packet_intervals: Option<Vec<PacketIntervals>>,
    /// localDiagnostic property.
    pub local_diagnostic: Option<String>,
    /// localState property.
    pub local_state: Option<String>,
    /// negotiatedLocalControlTxIntervalMs property.
    pub negotiated_local_control_tx_interval_ms: Option<i64>,
    /// rxPacket property.
    pub rx_packet: Option<BfdPacket>,
    /// txPacket property.
    pub tx_packet: Option<BfdPacket>,
    /// uptimeMs property.
    pub uptime_ms: Option<String>,
}

/// `VpnGatewayList` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct VpnGatewayList {
    /// id property.
    pub id: Option<String>,
    /// items property.
    pub items: Option<Vec<VpnGateway>>,
    /// kind property.
    pub kind: Option<String>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// warning property.
    pub warning: Option<std::collections::HashMap<String, serde_json::Value>>,
}

/// `ReservationSlotStatus` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ReservationSlotStatus {
    /// physicalTopology property.
    pub physical_topology: Option<ReservationSlotPhysicalTopology>,
    /// rdmaIpAddresses property.
    pub rdma_ip_addresses: Option<Vec<String>>,
    /// runningInstances property.
    pub running_instances: Option<Vec<String>>,
}

/// `XpnResourceId` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct XpnResourceId {
    /// id property.
    pub id: Option<String>,
    /// type property.
    pub r#type: Option<String>,
}

/// `HttpFaultDelay` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct HttpFaultDelay {
    /// fixedDelay property.
    pub fixed_delay: Option<Duration>,
    /// percentage property.
    pub percentage: Option<f64>,
}

/// `FutureReservationStatusSpecificSKUProperties` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct FutureReservationStatusSpecificSKUProperties {
    /// sourceInstanceTemplateId property.
    pub source_instance_template_id: Option<String>,
}

/// `ReservationsPerformMaintenanceRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ReservationsPerformMaintenanceRequest {
    /// maintenanceScope property.
    pub maintenance_scope: Option<String>,
}

/// `ReservationBlockPhysicalTopologyInstance` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ReservationBlockPhysicalTopologyInstance {
    /// instanceId property.
    pub instance_id: Option<String>,
    /// physicalHostTopology property.
    pub physical_host_topology:
        Option<ReservationBlockPhysicalTopologyInstancePhysicalHostTopology>,
    /// projectId property.
    pub project_id: Option<String>,
}

/// `BackendServiceHAPolicy` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct BackendServiceHAPolicy {
    /// fastIPMove property.
    pub fast_ip_move: Option<String>,
    /// leader property.
    pub leader: Option<BackendServiceHAPolicyLeader>,
}

/// `BackendServiceTlsSettingsSubjectAltName` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct BackendServiceTlsSettingsSubjectAltName {
    /// dnsName property.
    pub dns_name: Option<String>,
    /// uniformResourceIdentifier property.
    pub uniform_resource_identifier: Option<String>,
}

/// `FirewallParams` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct FirewallParams {
    /// resourceManagerTags property.
    pub resource_manager_tags: Option<serde_json::Value>,
}

/// `StoragePoolType` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct StoragePoolType {
    /// creationTimestamp property.
    pub creation_timestamp: Option<String>,
    /// deprecated property.
    pub deprecated: Option<DeprecationStatus>,
    /// description property.
    pub description: Option<String>,
    /// id property.
    pub id: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// maxPoolProvisionedCapacityGb property.
    pub max_pool_provisioned_capacity_gb: Option<String>,
    /// maxPoolProvisionedIops property.
    pub max_pool_provisioned_iops: Option<String>,
    /// maxPoolProvisionedThroughput property.
    pub max_pool_provisioned_throughput: Option<String>,
    /// minPoolProvisionedCapacityGb property.
    pub min_pool_provisioned_capacity_gb: Option<String>,
    /// minPoolProvisionedIops property.
    pub min_pool_provisioned_iops: Option<String>,
    /// minPoolProvisionedThroughput property.
    pub min_pool_provisioned_throughput: Option<String>,
    /// minSizeGb property.
    pub min_size_gb: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// selfLinkWithId property.
    pub self_link_with_id: Option<String>,
    /// supportedDiskTypes property.
    pub supported_disk_types: Option<Vec<String>>,
    /// zone property.
    pub zone: Option<String>,
}

/// `InstantSnapshotGroupParameters` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct InstantSnapshotGroupParameters {
    /// sourceInstantSnapshotGroup property.
    pub source_instant_snapshot_group: Option<String>,
}

/// `NetworkProfilesListResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct NetworkProfilesListResponse {
    /// etag property.
    pub etag: Option<String>,
    /// id property.
    pub id: Option<String>,
    /// items property.
    pub items: Option<Vec<NetworkProfile>>,
    /// kind property.
    pub kind: Option<String>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// unreachables property.
    pub unreachables: Option<Vec<String>>,
    /// warning property.
    pub warning: Option<std::collections::HashMap<String, serde_json::Value>>,
}

/// `InstancesSetNameRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct InstancesSetNameRequest {
    /// currentName property.
    pub current_name: Option<String>,
    /// name property.
    pub name: Option<String>,
}

/// `BgpRouteNetworkLayerReachabilityInformation` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct BgpRouteNetworkLayerReachabilityInformation {
    /// pathId property.
    pub path_id: Option<i64>,
    /// prefix property.
    pub prefix: Option<String>,
}

/// `InterconnectAttachmentL2ForwardingGeneveHeader` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct InterconnectAttachmentL2ForwardingGeneveHeader {
    /// vni property.
    pub vni: Option<i64>,
}

/// `SavedDisk` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SavedDisk {
    /// architecture property.
    pub architecture: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// sourceDisk property.
    pub source_disk: Option<String>,
    /// storageBytes property.
    pub storage_bytes: Option<String>,
    /// storageBytesStatus property.
    pub storage_bytes_status: Option<String>,
}

/// `InstanceGroupsRemoveInstancesRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct InstanceGroupsRemoveInstancesRequest {
    /// instances property.
    pub instances: Option<Vec<InstanceReference>>,
}

/// `SSLHealthCheck` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SSLHealthCheck {
    /// port property.
    pub port: Option<i64>,
    /// portName property.
    pub port_name: Option<String>,
    /// portSpecification property.
    pub port_specification: Option<String>,
    /// proxyHeader property.
    pub proxy_header: Option<String>,
    /// request property.
    pub request: Option<String>,
    /// response property.
    pub response: Option<String>,
}

/// `SubnetworkLogConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SubnetworkLogConfig {
    /// aggregationInterval property.
    pub aggregation_interval: Option<String>,
    /// enable property.
    pub enable: Option<bool>,
    /// filterExpr property.
    pub filter_expr: Option<String>,
    /// flowSampling property.
    pub flow_sampling: Option<f64>,
    /// metadata property.
    pub metadata: Option<String>,
    /// metadataFields property.
    pub metadata_fields: Option<Vec<String>>,
}

/// `SecurityPolicyRecaptchaOptionsConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SecurityPolicyRecaptchaOptionsConfig {
    /// redirectSiteKey property.
    pub redirect_site_key: Option<String>,
}

/// `GlobalOrganizationSetPolicyRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GlobalOrganizationSetPolicyRequest {
    /// bindings property.
    pub bindings: Option<Vec<Binding>>,
    /// etag property.
    pub etag: Option<String>,
    /// policy property.
    pub policy: Option<Policy>,
}

/// `Region` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Region {
    /// creationTimestamp property.
    pub creation_timestamp: Option<String>,
    /// deprecated property.
    pub deprecated: Option<DeprecationStatus>,
    /// description property.
    pub description: Option<String>,
    /// id property.
    pub id: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// quotaStatusWarning property.
    pub quota_status_warning: Option<std::collections::HashMap<String, serde_json::Value>>,
    /// quotas property.
    pub quotas: Option<Vec<Quota>>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// status property.
    pub status: Option<String>,
    /// supportsPzs property.
    pub supports_pzs: Option<bool>,
    /// zones property.
    pub zones: Option<Vec<String>>,
}

/// `ForwardingRuleReference` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ForwardingRuleReference {
    /// forwardingRule property.
    pub forwarding_rule: Option<String>,
}

/// `MachineType` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct MachineType {
    /// accelerators property.
    pub accelerators: Option<Vec<std::collections::HashMap<String, serde_json::Value>>>,
    /// architecture property.
    pub architecture: Option<String>,
    /// bundledLocalSsds property.
    pub bundled_local_ssds: Option<BundledLocalSsds>,
    /// creationTimestamp property.
    pub creation_timestamp: Option<String>,
    /// deprecated property.
    pub deprecated: Option<DeprecationStatus>,
    /// description property.
    pub description: Option<String>,
    /// guestCpus property.
    pub guest_cpus: Option<i64>,
    /// id property.
    pub id: Option<String>,
    /// imageSpaceGb property.
    pub image_space_gb: Option<i64>,
    /// isSharedCpu property.
    pub is_shared_cpu: Option<bool>,
    /// kind property.
    pub kind: Option<String>,
    /// maximumPersistentDisks property.
    pub maximum_persistent_disks: Option<i64>,
    /// maximumPersistentDisksSizeGb property.
    pub maximum_persistent_disks_size_gb: Option<String>,
    /// memoryMb property.
    pub memory_mb: Option<i64>,
    /// name property.
    pub name: Option<String>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// zone property.
    pub zone: Option<String>,
}

/// `ShieldedInstanceIntegrityPolicy` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ShieldedInstanceIntegrityPolicy {
    /// updateAutoLearnPolicy property.
    pub update_auto_learn_policy: Option<bool>,
}

/// `BfdStatusPacketCounts` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct BfdStatusPacketCounts {
    /// numRx property.
    pub num_rx: Option<i64>,
    /// numRxRejected property.
    pub num_rx_rejected: Option<i64>,
    /// numRxSuccessful property.
    pub num_rx_successful: Option<i64>,
    /// numTx property.
    pub num_tx: Option<i64>,
}

/// `InterconnectGroupsCreateMembers` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct InterconnectGroupsCreateMembers {
    /// intentMismatchBehavior property.
    pub intent_mismatch_behavior: Option<String>,
    /// interconnects property.
    pub interconnects: Option<Vec<InterconnectGroupsCreateMembersInterconnectInput>>,
    /// templateInterconnect property.
    pub template_interconnect: Option<InterconnectGroupsCreateMembersInterconnectInput>,
}

/// `RegionInstanceGroupManagersDeleteInstancesRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct RegionInstanceGroupManagersDeleteInstancesRequest {
    /// instances property.
    pub instances: Option<Vec<String>>,
    /// skipInstancesOnValidationError property.
    pub skip_instances_on_validation_error: Option<bool>,
}

/// `InterconnectOutageNotification` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct InterconnectOutageNotification {
    /// affectedCircuits property.
    pub affected_circuits: Option<Vec<String>>,
    /// description property.
    pub description: Option<String>,
    /// endTime property.
    pub end_time: Option<String>,
    /// issueType property.
    pub issue_type: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// source property.
    pub source: Option<String>,
    /// startTime property.
    pub start_time: Option<String>,
    /// state property.
    pub state: Option<String>,
}

/// `UsableSubnetworkSecondaryRange` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct UsableSubnetworkSecondaryRange {
    /// ipCidrRange property.
    pub ip_cidr_range: Option<String>,
    /// rangeName property.
    pub range_name: Option<String>,
}

/// `NetworkInterface` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct NetworkInterface {
    /// accessConfigs property.
    pub access_configs: Option<Vec<AccessConfig>>,
    /// aliasIpRanges property.
    pub alias_ip_ranges: Option<Vec<AliasIpRange>>,
    /// enableVpcScopedDns property.
    pub enable_vpc_scoped_dns: Option<bool>,
    /// fingerprint property.
    pub fingerprint: Option<String>,
    /// igmpQuery property.
    pub igmp_query: Option<String>,
    /// internalIpv6PrefixLength property.
    pub internal_ipv6_prefix_length: Option<i64>,
    /// ipv6AccessConfigs property.
    pub ipv6_access_configs: Option<Vec<AccessConfig>>,
    /// ipv6AccessType property.
    pub ipv6_access_type: Option<String>,
    /// ipv6Address property.
    pub ipv6_address: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// network property.
    pub network: Option<String>,
    /// networkAttachment property.
    pub network_attachment: Option<String>,
    /// networkIP property.
    pub network_ip: Option<String>,
    /// nicType property.
    pub nic_type: Option<String>,
    /// parentNicName property.
    pub parent_nic_name: Option<String>,
    /// queueCount property.
    pub queue_count: Option<i64>,
    /// serviceClassId property.
    pub service_class_id: Option<String>,
    /// stackType property.
    pub stack_type: Option<String>,
    /// subnetwork property.
    pub subnetwork: Option<String>,
    /// vlan property.
    pub vlan: Option<i64>,
}

/// `StoragePool` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct StoragePool {
    /// capacityProvisioningType property.
    pub capacity_provisioning_type: Option<String>,
    /// creationTimestamp property.
    pub creation_timestamp: Option<String>,
    /// description property.
    pub description: Option<String>,
    /// exapoolProvisionedCapacityGb property.
    pub exapool_provisioned_capacity_gb: Option<StoragePoolExapoolProvisionedCapacityGb>,
    /// id property.
    pub id: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// labelFingerprint property.
    pub label_fingerprint: Option<String>,
    /// labels property.
    pub labels: Option<serde_json::Value>,
    /// name property.
    pub name: Option<String>,
    /// params property.
    pub params: Option<StoragePoolParams>,
    /// performanceProvisioningType property.
    pub performance_provisioning_type: Option<String>,
    /// poolProvisionedCapacityGb property.
    pub pool_provisioned_capacity_gb: Option<String>,
    /// poolProvisionedIops property.
    pub pool_provisioned_iops: Option<String>,
    /// poolProvisionedThroughput property.
    pub pool_provisioned_throughput: Option<String>,
    /// resourceStatus property.
    pub resource_status: Option<StoragePoolResourceStatus>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// selfLinkWithId property.
    pub self_link_with_id: Option<String>,
    /// state property.
    pub state: Option<String>,
    /// status property.
    pub status: Option<StoragePoolResourceStatus>,
    /// storagePoolType property.
    pub storage_pool_type: Option<String>,
    /// zone property.
    pub zone: Option<String>,
}

/// `ReservationParams` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ReservationParams {
    /// resourceManagerTags property.
    pub resource_manager_tags: Option<serde_json::Value>,
}

/// `InterconnectAttachmentGroupConfiguredAvailabilitySLAIntendedSlaBlockers` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct InterconnectAttachmentGroupConfiguredAvailabilitySLAIntendedSlaBlockers {
    /// attachments property.
    pub attachments: Option<Vec<String>>,
    /// blockerType property.
    pub blocker_type: Option<String>,
    /// documentationLink property.
    pub documentation_link: Option<String>,
    /// explanation property.
    pub explanation: Option<String>,
    /// metros property.
    pub metros: Option<Vec<String>>,
    /// regions property.
    pub regions: Option<Vec<String>>,
    /// zones property.
    pub zones: Option<Vec<String>>,
}

/// `ExternalVpnGatewayParams` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ExternalVpnGatewayParams {
    /// resourceManagerTags property.
    pub resource_manager_tags: Option<serde_json::Value>,
}

/// `RouterNatLogConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct RouterNatLogConfig {
    /// enable property.
    pub enable: Option<bool>,
    /// filter property.
    pub filter: Option<String>,
}

/// `BackendServiceTlsSettings` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct BackendServiceTlsSettings {
    /// authenticationConfig property.
    pub authentication_config: Option<String>,
    /// sni property.
    pub sni: Option<String>,
    /// subjectAltNames property.
    pub subject_alt_names: Option<Vec<BackendServiceTlsSettingsSubjectAltName>>,
}

/// `ServiceAttachmentConsumerProjectLimit` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ServiceAttachmentConsumerProjectLimit {
    /// connectionLimit property.
    pub connection_limit: Option<i64>,
    /// endpointUrl property.
    pub endpoint_url: Option<String>,
    /// networkUrl property.
    pub network_url: Option<String>,
    /// projectIdOrNum property.
    pub project_id_or_num: Option<String>,
}

/// `RegionSetLabelsRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct RegionSetLabelsRequest {
    /// labelFingerprint property.
    pub label_fingerprint: Option<String>,
    /// labels property.
    pub labels: Option<serde_json::Value>,
}

/// `BulkInsertInstanceResource` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct BulkInsertInstanceResource {
    /// count property.
    pub count: Option<String>,
    /// instanceFlexibilityPolicy property.
    pub instance_flexibility_policy: Option<InstanceFlexibilityPolicy>,
    /// instanceProperties property.
    pub instance_properties: Option<InstanceProperties>,
    /// locationPolicy property.
    pub location_policy: Option<LocationPolicy>,
    /// minCount property.
    pub min_count: Option<String>,
    /// namePattern property.
    pub name_pattern: Option<String>,
    /// perInstanceProperties property.
    pub per_instance_properties: Option<serde_json::Value>,
    /// sourceInstanceTemplate property.
    pub source_instance_template: Option<String>,
}

/// `MachineImageList` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct MachineImageList {
    /// id property.
    pub id: Option<String>,
    /// items property.
    pub items: Option<Vec<MachineImage>>,
    /// kind property.
    pub kind: Option<String>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// warning property.
    pub warning: Option<std::collections::HashMap<String, serde_json::Value>>,
}

/// `InstancesAddResourcePoliciesRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct InstancesAddResourcePoliciesRequest {
    /// resourcePolicies property.
    pub resource_policies: Option<Vec<String>>,
}

/// `ReservationSubBlocksGetVersionRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ReservationSubBlocksGetVersionRequest {
    /// sbomSelections property.
    pub sbom_selections: Option<Vec<String>>,
}

/// `AccessConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AccessConfig {
    /// externalIpv6 property.
    pub external_ipv6: Option<String>,
    /// externalIpv6PrefixLength property.
    pub external_ipv6_prefix_length: Option<i64>,
    /// kind property.
    pub kind: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// natIP property.
    pub nat_ip: Option<String>,
    /// networkTier property.
    pub network_tier: Option<String>,
    /// publicPtrDomainName property.
    pub public_ptr_domain_name: Option<String>,
    /// securityPolicy property.
    pub security_policy: Option<String>,
    /// setPublicPtr property.
    pub set_public_ptr: Option<bool>,
    /// type property.
    pub r#type: Option<String>,
}

/// `RegionDisksStartAsyncReplicationRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct RegionDisksStartAsyncReplicationRequest {
    /// asyncSecondaryDisk property.
    pub async_secondary_disk: Option<String>,
}

/// `SubnetworksExpandIpCidrRangeRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SubnetworksExpandIpCidrRangeRequest {
    /// ipCidrRange property.
    pub ip_cidr_range: Option<String>,
}

/// `Quota` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Quota {
    /// limit property.
    pub limit: Option<f64>,
    /// metric property.
    pub metric: Option<String>,
    /// owner property.
    pub owner: Option<String>,
    /// usage property.
    pub usage: Option<f64>,
}

/// `PacketMirroringNetworkInfo` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct PacketMirroringNetworkInfo {
    /// canonicalUrl property.
    pub canonical_url: Option<String>,
    /// url property.
    pub url: Option<String>,
}

/// `NamedPort` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct NamedPort {
    /// name property.
    pub name: Option<String>,
    /// port property.
    pub port: Option<i64>,
}

/// `GuestAttributes` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GuestAttributes {
    /// kind property.
    pub kind: Option<String>,
    /// queryPath property.
    pub query_path: Option<String>,
    /// queryValue property.
    pub query_value: Option<GuestAttributesValue>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// variableKey property.
    pub variable_key: Option<String>,
    /// variableValue property.
    pub variable_value: Option<String>,
}

/// `WireGroupTopologyEndpoint` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct WireGroupTopologyEndpoint {
    /// city property.
    pub city: Option<String>,
    /// label property.
    pub label: Option<String>,
}

/// `BackendServiceOrchestrationInfo` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct BackendServiceOrchestrationInfo {
    /// resourceUri property.
    pub resource_uri: Option<String>,
}

/// `TargetSslProxiesSetProxyHeaderRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct TargetSslProxiesSetProxyHeaderRequest {
    /// proxyHeader property.
    pub proxy_header: Option<String>,
}

/// `HttpHealthCheck` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct HttpHealthCheck {
    /// checkIntervalSec property.
    pub check_interval_sec: Option<i64>,
    /// creationTimestamp property.
    pub creation_timestamp: Option<String>,
    /// description property.
    pub description: Option<String>,
    /// healthyThreshold property.
    pub healthy_threshold: Option<i64>,
    /// host property.
    pub host: Option<String>,
    /// id property.
    pub id: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// port property.
    pub port: Option<i64>,
    /// requestPath property.
    pub request_path: Option<String>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// timeoutSec property.
    pub timeout_sec: Option<i64>,
    /// unhealthyThreshold property.
    pub unhealthy_threshold: Option<i64>,
}

/// `RoutePolicyPolicyTerm` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct RoutePolicyPolicyTerm {
    /// actions property.
    pub actions: Option<Vec<Expr>>,
    /// match property.
    pub r#match: Option<Expr>,
    /// priority property.
    pub priority: Option<i64>,
}

/// `InstanceReference` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct InstanceReference {
    /// instance property.
    pub instance: Option<String>,
}

/// `RegionInstanceGroupManagerResizeRequestsListResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct RegionInstanceGroupManagerResizeRequestsListResponse {
    /// etag property.
    pub etag: Option<String>,
    /// id property.
    pub id: Option<String>,
    /// items property.
    pub items: Option<Vec<InstanceGroupManagerResizeRequest>>,
    /// kind property.
    pub kind: Option<String>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// unreachables property.
    pub unreachables: Option<Vec<String>>,
    /// warning property.
    pub warning: Option<std::collections::HashMap<String, serde_json::Value>>,
}

/// `TargetSslProxyList` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct TargetSslProxyList {
    /// id property.
    pub id: Option<String>,
    /// items property.
    pub items: Option<Vec<TargetSslProxy>>,
    /// kind property.
    pub kind: Option<String>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// warning property.
    pub warning: Option<std::collections::HashMap<String, serde_json::Value>>,
}

/// `InstanceTemplate` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct InstanceTemplate {
    /// creationTimestamp property.
    pub creation_timestamp: Option<String>,
    /// description property.
    pub description: Option<String>,
    /// id property.
    pub id: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// properties property.
    pub properties: Option<InstanceProperties>,
    /// region property.
    pub region: Option<String>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// sourceInstance property.
    pub source_instance: Option<String>,
    /// sourceInstanceParams property.
    pub source_instance_params: Option<SourceInstanceParams>,
}

/// `ReservationSubBlockHealthInfo` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ReservationSubBlockHealthInfo {
    /// degradedHostCount property.
    pub degraded_host_count: Option<i64>,
    /// degradedInfraCount property.
    pub degraded_infra_count: Option<i64>,
    /// healthStatus property.
    pub health_status: Option<String>,
    /// healthyHostCount property.
    pub healthy_host_count: Option<i64>,
    /// healthyInfraCount property.
    pub healthy_infra_count: Option<i64>,
}

/// `AllocationSpecificSKUReservation` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AllocationSpecificSKUReservation {
    /// assuredCount property.
    pub assured_count: Option<String>,
    /// count property.
    pub count: Option<String>,
    /// inUseCount property.
    pub in_use_count: Option<String>,
    /// instanceProperties property.
    pub instance_properties: Option<AllocationSpecificSKUAllocationReservedInstanceProperties>,
    /// sourceInstanceTemplate property.
    pub source_instance_template: Option<String>,
}

/// `Help` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Help {
    /// links property.
    pub links: Option<Vec<HelpLink>>,
}

/// `FutureReservationsAggregatedListResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct FutureReservationsAggregatedListResponse {
    /// etag property.
    pub etag: Option<String>,
    /// id property.
    pub id: Option<String>,
    /// items property.
    pub items: Option<serde_json::Value>,
    /// kind property.
    pub kind: Option<String>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// unreachables property.
    pub unreachables: Option<Vec<String>>,
    /// warning property.
    pub warning: Option<std::collections::HashMap<String, serde_json::Value>>,
}

/// `ReservationSubBlock` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ReservationSubBlock {
    /// acceleratorTopologiesInfo property.
    pub accelerator_topologies_info: Option<AcceleratorTopologiesInfo>,
    /// count property.
    pub count: Option<i64>,
    /// creationTimestamp property.
    pub creation_timestamp: Option<String>,
    /// healthInfo property.
    pub health_info: Option<ReservationSubBlockHealthInfo>,
    /// id property.
    pub id: Option<String>,
    /// inUseCount property.
    pub in_use_count: Option<i64>,
    /// inUseHostCount property.
    pub in_use_host_count: Option<i64>,
    /// kind property.
    pub kind: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// physicalTopology property.
    pub physical_topology: Option<ReservationSubBlockPhysicalTopology>,
    /// reservationSubBlockMaintenance property.
    pub reservation_sub_block_maintenance: Option<GroupMaintenanceInfo>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// selfLinkWithId property.
    pub self_link_with_id: Option<String>,
    /// status property.
    pub status: Option<String>,
    /// zone property.
    pub zone: Option<String>,
}

/// `NodeGroupsSetNodeTemplateRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct NodeGroupsSetNodeTemplateRequest {
    /// nodeTemplate property.
    pub node_template: Option<String>,
}

/// `BackendServiceNetworkPassThroughLbTrafficPolicyZonalAffinity` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct BackendServiceNetworkPassThroughLbTrafficPolicyZonalAffinity {
    /// spillover property.
    pub spillover: Option<String>,
    /// spilloverRatio property.
    pub spillover_ratio: Option<f64>,
}

/// `UrlMapsValidateResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct UrlMapsValidateResponse {
    /// result property.
    pub result: Option<UrlMapValidationResult>,
}

/// `WorkloadIdentityConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct WorkloadIdentityConfig {
    /// identity property.
    pub identity: Option<String>,
    /// identityCertificateEnabled property.
    pub identity_certificate_enabled: Option<bool>,
}

/// `PathRule` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct PathRule {
    /// customErrorResponsePolicy property.
    pub custom_error_response_policy: Option<CustomErrorResponsePolicy>,
    /// paths property.
    pub paths: Option<Vec<String>>,
    /// routeAction property.
    pub route_action: Option<HttpRouteAction>,
    /// service property.
    pub service: Option<String>,
    /// urlRedirect property.
    pub url_redirect: Option<HttpRedirectAction>,
}

/// `InterconnectLocation` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct InterconnectLocation {
    /// address property.
    pub address: Option<String>,
    /// availabilityZone property.
    pub availability_zone: Option<String>,
    /// availableFeatures property.
    pub available_features: Option<Vec<String>>,
    /// availableLinkTypes property.
    pub available_link_types: Option<Vec<String>>,
    /// city property.
    pub city: Option<String>,
    /// continent property.
    pub continent: Option<String>,
    /// creationTimestamp property.
    pub creation_timestamp: Option<String>,
    /// crossSiteInterconnectInfos property.
    pub cross_site_interconnect_infos: Option<Vec<InterconnectLocationCrossSiteInterconnectInfo>>,
    /// description property.
    pub description: Option<String>,
    /// facilityProvider property.
    pub facility_provider: Option<String>,
    /// facilityProviderFacilityId property.
    pub facility_provider_facility_id: Option<String>,
    /// id property.
    pub id: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// peeringdbFacilityId property.
    pub peeringdb_facility_id: Option<String>,
    /// regionInfos property.
    pub region_infos: Option<Vec<InterconnectLocationRegionInfo>>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// singleRegionProductionCriticalPeerLocations property.
    pub single_region_production_critical_peer_locations: Option<Vec<String>>,
    /// status property.
    pub status: Option<String>,
    /// supportsPzs property.
    pub supports_pzs: Option<bool>,
}

/// `HttpFaultInjection` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct HttpFaultInjection {
    /// abort property.
    pub abort: Option<HttpFaultAbort>,
    /// delay property.
    pub delay: Option<HttpFaultDelay>,
}

/// `PreconfiguredWafSet` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct PreconfiguredWafSet {
    /// expressionSets property.
    pub expression_sets: Option<Vec<WafExpressionSet>>,
}

/// `ReservationsBlocksPerformMaintenanceRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ReservationsBlocksPerformMaintenanceRequest {
    /// maintenanceScope property.
    pub maintenance_scope: Option<String>,
}

/// `BackendServiceParams` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct BackendServiceParams {
    /// resourceManagerTags property.
    pub resource_manager_tags: Option<serde_json::Value>,
}

/// `SecurityPolicyUserDefinedField` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SecurityPolicyUserDefinedField {
    /// base property.
    pub base: Option<String>,
    /// mask property.
    pub mask: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// offset property.
    pub offset: Option<i64>,
    /// size property.
    pub size: Option<i64>,
}

/// `SslCertificateSelfManagedSslCertificate` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SslCertificateSelfManagedSslCertificate {
    /// certificate property.
    pub certificate: Option<String>,
    /// privateKey property.
    pub private_key: Option<String>,
}

/// `NodeTemplate` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct NodeTemplate {
    /// accelerators property.
    pub accelerators: Option<Vec<AcceleratorConfig>>,
    /// cpuOvercommitType property.
    pub cpu_overcommit_type: Option<String>,
    /// creationTimestamp property.
    pub creation_timestamp: Option<String>,
    /// description property.
    pub description: Option<String>,
    /// disks property.
    pub disks: Option<Vec<LocalDisk>>,
    /// id property.
    pub id: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// nodeAffinityLabels property.
    pub node_affinity_labels: Option<serde_json::Value>,
    /// nodeType property.
    pub node_type: Option<String>,
    /// nodeTypeFlexibility property.
    pub node_type_flexibility: Option<NodeTemplateNodeTypeFlexibility>,
    /// region property.
    pub region: Option<String>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// serverBinding property.
    pub server_binding: Option<ServerBinding>,
    /// status property.
    pub status: Option<String>,
    /// statusMessage property.
    pub status_message: Option<String>,
}

/// `InstantSnapshotParams` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct InstantSnapshotParams {
    /// resourceManagerTags property.
    pub resource_manager_tags: Option<serde_json::Value>,
}

/// `RouterBgpPeer` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct RouterBgpPeer {
    /// advertiseMode property.
    pub advertise_mode: Option<String>,
    /// advertisedGroups property.
    pub advertised_groups: Option<Vec<String>>,
    /// advertisedIpRanges property.
    pub advertised_ip_ranges: Option<Vec<RouterAdvertisedIpRange>>,
    /// advertisedRoutePriority property.
    pub advertised_route_priority: Option<i64>,
    /// bfd property.
    pub bfd: Option<RouterBgpPeerBfd>,
    /// customLearnedIpRanges property.
    pub custom_learned_ip_ranges: Option<Vec<RouterBgpPeerCustomLearnedIpRange>>,
    /// customLearnedRoutePriority property.
    pub custom_learned_route_priority: Option<i64>,
    /// enable property.
    pub enable: Option<String>,
    /// enableIpv4 property.
    pub enable_ipv4: Option<bool>,
    /// enableIpv6 property.
    pub enable_ipv6: Option<bool>,
    /// exportPolicies property.
    pub export_policies: Option<Vec<String>>,
    /// importPolicies property.
    pub import_policies: Option<Vec<String>>,
    /// interfaceName property.
    pub interface_name: Option<String>,
    /// ipAddress property.
    pub ip_address: Option<String>,
    /// ipv4NexthopAddress property.
    pub ipv4_nexthop_address: Option<String>,
    /// ipv6NexthopAddress property.
    pub ipv6_nexthop_address: Option<String>,
    /// managementType property.
    pub management_type: Option<String>,
    /// md5AuthenticationKeyName property.
    pub md5_authentication_key_name: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// peerAsn property.
    pub peer_asn: Option<i64>,
    /// peerIpAddress property.
    pub peer_ip_address: Option<String>,
    /// peerIpv4NexthopAddress property.
    pub peer_ipv4_nexthop_address: Option<String>,
    /// peerIpv6NexthopAddress property.
    pub peer_ipv6_nexthop_address: Option<String>,
    /// routerApplianceInstance property.
    pub router_appliance_instance: Option<String>,
}

/// `ShieldedInstanceIdentity` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ShieldedInstanceIdentity {
    /// eccP256EncryptionKey property.
    pub ecc_p256_encryption_key: Option<ShieldedInstanceIdentityEntry>,
    /// eccP256SigningKey property.
    pub ecc_p256_signing_key: Option<ShieldedInstanceIdentityEntry>,
    /// encryptionKey property.
    pub encryption_key: Option<ShieldedInstanceIdentityEntry>,
    /// kind property.
    pub kind: Option<String>,
    /// signingKey property.
    pub signing_key: Option<ShieldedInstanceIdentityEntry>,
}

/// `RouteParams` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct RouteParams {
    /// resourceManagerTags property.
    pub resource_manager_tags: Option<serde_json::Value>,
}

/// `ResourceStatus` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ResourceStatus {
    /// effectiveInstanceMetadata property.
    pub effective_instance_metadata: Option<ResourceStatusEffectiveInstanceMetadata>,
    /// physicalHost property.
    pub physical_host: Option<String>,
    /// physicalHostTopology property.
    pub physical_host_topology: Option<ResourceStatusPhysicalHostTopology>,
    /// reservationConsumptionInfo property.
    pub reservation_consumption_info: Option<ResourceStatusReservationConsumptionInfo>,
    /// scheduling property.
    pub scheduling: Option<ResourceStatusScheduling>,
    /// upcomingMaintenance property.
    pub upcoming_maintenance: Option<UpcomingMaintenance>,
}

/// `RegionInstanceGroupsSetNamedPortsRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct RegionInstanceGroupsSetNamedPortsRequest {
    /// fingerprint property.
    pub fingerprint: Option<String>,
    /// namedPorts property.
    pub named_ports: Option<Vec<NamedPort>>,
}

/// `CommitmentParams` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct CommitmentParams {
    /// resourceManagerTags property.
    pub resource_manager_tags: Option<serde_json::Value>,
}

/// `VmExtensionPolicyLabelSelector` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct VmExtensionPolicyLabelSelector {
    /// inclusionLabels property.
    pub inclusion_labels: Option<serde_json::Value>,
}

/// `BackendService` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct BackendService {
    /// affinityCookieTtlSec property.
    pub affinity_cookie_ttl_sec: Option<i64>,
    /// backends property.
    pub backends: Option<Vec<Backend>>,
    /// cdnPolicy property.
    pub cdn_policy: Option<BackendServiceCdnPolicy>,
    /// circuitBreakers property.
    pub circuit_breakers: Option<CircuitBreakers>,
    /// compressionMode property.
    pub compression_mode: Option<String>,
    /// connectionDraining property.
    pub connection_draining: Option<ConnectionDraining>,
    /// connectionTrackingPolicy property.
    pub connection_tracking_policy: Option<BackendServiceConnectionTrackingPolicy>,
    /// consistentHash property.
    pub consistent_hash: Option<ConsistentHashLoadBalancerSettings>,
    /// creationTimestamp property.
    pub creation_timestamp: Option<String>,
    /// customMetrics property.
    pub custom_metrics: Option<Vec<BackendServiceCustomMetric>>,
    /// customRequestHeaders property.
    pub custom_request_headers: Option<Vec<String>>,
    /// customResponseHeaders property.
    pub custom_response_headers: Option<Vec<String>>,
    /// description property.
    pub description: Option<String>,
    /// edgeSecurityPolicy property.
    pub edge_security_policy: Option<String>,
    /// enableCDN property.
    pub enable_cdn: Option<bool>,
    /// externalManagedMigrationState property.
    pub external_managed_migration_state: Option<String>,
    /// externalManagedMigrationTestingPercentage property.
    pub external_managed_migration_testing_percentage: Option<f64>,
    /// failoverPolicy property.
    pub failover_policy: Option<BackendServiceFailoverPolicy>,
    /// fingerprint property.
    pub fingerprint: Option<String>,
    /// haPolicy property.
    pub ha_policy: Option<BackendServiceHAPolicy>,
    /// healthChecks property.
    pub health_checks: Option<Vec<String>>,
    /// iap property.
    pub iap: Option<BackendServiceIAP>,
    /// id property.
    pub id: Option<String>,
    /// ipAddressSelectionPolicy property.
    pub ip_address_selection_policy: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// loadBalancingScheme property.
    pub load_balancing_scheme: Option<String>,
    /// localityLbPolicies property.
    pub locality_lb_policies: Option<Vec<BackendServiceLocalityLoadBalancingPolicyConfig>>,
    /// localityLbPolicy property.
    pub locality_lb_policy: Option<String>,
    /// logConfig property.
    pub log_config: Option<BackendServiceLogConfig>,
    /// maxStreamDuration property.
    pub max_stream_duration: Option<Duration>,
    /// metadatas property.
    pub metadatas: Option<serde_json::Value>,
    /// name property.
    pub name: Option<String>,
    /// network property.
    pub network: Option<String>,
    /// networkPassThroughLbTrafficPolicy property.
    pub network_pass_through_lb_traffic_policy:
        Option<BackendServiceNetworkPassThroughLbTrafficPolicy>,
    /// orchestrationInfo property.
    pub orchestration_info: Option<BackendServiceOrchestrationInfo>,
    /// outlierDetection property.
    pub outlier_detection: Option<OutlierDetection>,
    /// params property.
    pub params: Option<BackendServiceParams>,
    /// port property.
    pub port: Option<i64>,
    /// portName property.
    pub port_name: Option<String>,
    /// protocol property.
    pub protocol: Option<String>,
    /// region property.
    pub region: Option<String>,
    /// securityPolicy property.
    pub security_policy: Option<String>,
    /// securitySettings property.
    pub security_settings: Option<SecuritySettings>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// serviceBindings property.
    pub service_bindings: Option<Vec<String>>,
    /// serviceLbPolicy property.
    pub service_lb_policy: Option<String>,
    /// sessionAffinity property.
    pub session_affinity: Option<String>,
    /// strongSessionAffinityCookie property.
    pub strong_session_affinity_cookie: Option<BackendServiceHttpCookie>,
    /// subsetting property.
    pub subsetting: Option<Subsetting>,
    /// timeoutSec property.
    pub timeout_sec: Option<i64>,
    /// tlsSettings property.
    pub tls_settings: Option<BackendServiceTlsSettings>,
    /// usedBy property.
    pub used_by: Option<Vec<BackendServiceUsedBy>>,
}

/// `SubnetworkUtilizationDetailsIPV4Utilization` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SubnetworkUtilizationDetailsIPV4Utilization {
    /// rangeName property.
    pub range_name: Option<String>,
    /// totalAllocatedIp property.
    pub total_allocated_ip: Option<String>,
    /// totalFreeIp property.
    pub total_free_ip: Option<String>,
}

/// `InstanceGroupManagersListManagedInstancesResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct InstanceGroupManagersListManagedInstancesResponse {
    /// managedInstances property.
    pub managed_instances: Option<Vec<ManagedInstance>>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
}

/// `Disk` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Disk {
    /// accessMode property.
    pub access_mode: Option<String>,
    /// architecture property.
    pub architecture: Option<String>,
    /// asyncPrimaryDisk property.
    pub async_primary_disk: Option<DiskAsyncReplication>,
    /// asyncSecondaryDisks property.
    pub async_secondary_disks: Option<serde_json::Value>,
    /// creationTimestamp property.
    pub creation_timestamp: Option<String>,
    /// description property.
    pub description: Option<String>,
    /// diskEncryptionKey property.
    pub disk_encryption_key: Option<CustomerEncryptionKey>,
    /// enableConfidentialCompute property.
    pub enable_confidential_compute: Option<bool>,
    /// guestOsFeatures property.
    pub guest_os_features: Option<Vec<GuestOsFeature>>,
    /// id property.
    pub id: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// labelFingerprint property.
    pub label_fingerprint: Option<String>,
    /// labels property.
    pub labels: Option<serde_json::Value>,
    /// lastAttachTimestamp property.
    pub last_attach_timestamp: Option<String>,
    /// lastDetachTimestamp property.
    pub last_detach_timestamp: Option<String>,
    /// licenseCodes property.
    pub license_codes: Option<Vec<String>>,
    /// licenses property.
    pub licenses: Option<Vec<String>>,
    /// locationHint property.
    pub location_hint: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// options property.
    pub options: Option<String>,
    /// params property.
    pub params: Option<DiskParams>,
    /// physicalBlockSizeBytes property.
    pub physical_block_size_bytes: Option<String>,
    /// provisionedIops property.
    pub provisioned_iops: Option<String>,
    /// provisionedThroughput property.
    pub provisioned_throughput: Option<String>,
    /// region property.
    pub region: Option<String>,
    /// replicaZones property.
    pub replica_zones: Option<Vec<String>>,
    /// resourcePolicies property.
    pub resource_policies: Option<Vec<String>>,
    /// resourceStatus property.
    pub resource_status: Option<DiskResourceStatus>,
    /// satisfiesPzi property.
    pub satisfies_pzi: Option<bool>,
    /// satisfiesPzs property.
    pub satisfies_pzs: Option<bool>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// sizeGb property.
    pub size_gb: Option<String>,
    /// sourceConsistencyGroupPolicy property.
    pub source_consistency_group_policy: Option<String>,
    /// sourceConsistencyGroupPolicyId property.
    pub source_consistency_group_policy_id: Option<String>,
    /// sourceDisk property.
    pub source_disk: Option<String>,
    /// sourceDiskId property.
    pub source_disk_id: Option<String>,
    /// sourceImage property.
    pub source_image: Option<String>,
    /// sourceImageEncryptionKey property.
    pub source_image_encryption_key: Option<CustomerEncryptionKey>,
    /// sourceImageId property.
    pub source_image_id: Option<String>,
    /// sourceInstantSnapshot property.
    pub source_instant_snapshot: Option<String>,
    /// sourceInstantSnapshotId property.
    pub source_instant_snapshot_id: Option<String>,
    /// sourceSnapshot property.
    pub source_snapshot: Option<String>,
    /// sourceSnapshotEncryptionKey property.
    pub source_snapshot_encryption_key: Option<CustomerEncryptionKey>,
    /// sourceSnapshotId property.
    pub source_snapshot_id: Option<String>,
    /// sourceStorageObject property.
    pub source_storage_object: Option<String>,
    /// status property.
    pub status: Option<String>,
    /// storagePool property.
    pub storage_pool: Option<String>,
    /// type property.
    pub r#type: Option<String>,
    /// users property.
    pub users: Option<Vec<String>>,
    /// zone property.
    pub zone: Option<String>,
}

/// `VpnTunnelPhase2Algorithms` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct VpnTunnelPhase2Algorithms {
    /// encryption property.
    pub encryption: Option<Vec<String>>,
    /// integrity property.
    pub integrity: Option<Vec<String>>,
    /// pfs property.
    pub pfs: Option<Vec<String>>,
}

/// `BackendServiceListUsable` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct BackendServiceListUsable {
    /// id property.
    pub id: Option<String>,
    /// items property.
    pub items: Option<Vec<BackendService>>,
    /// kind property.
    pub kind: Option<String>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// warning property.
    pub warning: Option<std::collections::HashMap<String, serde_json::Value>>,
}

/// `HttpHeaderOption` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct HttpHeaderOption {
    /// headerName property.
    pub header_name: Option<String>,
    /// headerValue property.
    pub header_value: Option<String>,
    /// replace property.
    pub replace: Option<bool>,
}

/// `SecurityPolicyAdaptiveProtectionConfigLayer7DdosDefenseConfigThresholdConfigTrafficGranularityConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SecurityPolicyAdaptiveProtectionConfigLayer7DdosDefenseConfigThresholdConfigTrafficGranularityConfig
{
    /// enableEachUniqueValue property.
    pub enable_each_unique_value: Option<bool>,
    /// type property.
    pub r#type: Option<String>,
    /// value property.
    pub value: Option<String>,
}

/// `AutoscalingPolicy` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AutoscalingPolicy {
    /// coolDownPeriodSec property.
    pub cool_down_period_sec: Option<i64>,
    /// cpuUtilization property.
    pub cpu_utilization: Option<AutoscalingPolicyCpuUtilization>,
    /// customMetricUtilizations property.
    pub custom_metric_utilizations: Option<Vec<AutoscalingPolicyCustomMetricUtilization>>,
    /// loadBalancingUtilization property.
    pub load_balancing_utilization: Option<AutoscalingPolicyLoadBalancingUtilization>,
    /// maxNumReplicas property.
    pub max_num_replicas: Option<i64>,
    /// minNumReplicas property.
    pub min_num_replicas: Option<i64>,
    /// mode property.
    pub mode: Option<String>,
    /// scaleInControl property.
    pub scale_in_control: Option<AutoscalingPolicyScaleInControl>,
    /// scalingSchedules property.
    pub scaling_schedules: Option<serde_json::Value>,
}

/// `ForwardingRuleAggregatedList` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ForwardingRuleAggregatedList {
    /// id property.
    pub id: Option<String>,
    /// items property.
    pub items: Option<serde_json::Value>,
    /// kind property.
    pub kind: Option<String>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// unreachables property.
    pub unreachables: Option<Vec<String>>,
    /// warning property.
    pub warning: Option<std::collections::HashMap<String, serde_json::Value>>,
}

/// `ConfidentialInstanceConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ConfidentialInstanceConfig {
    /// confidentialInstanceType property.
    pub confidential_instance_type: Option<String>,
    /// enableConfidentialCompute property.
    pub enable_confidential_compute: Option<bool>,
}

/// `InterconnectAttachmentGroupLogicalStructureRegionMetroFacilityZone` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct InterconnectAttachmentGroupLogicalStructureRegionMetroFacilityZone {
    /// attachments property.
    pub attachments: Option<Vec<String>>,
    /// zone property.
    pub zone: Option<String>,
}

/// `TestFailure` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct TestFailure {
    /// actualOutputUrl property.
    pub actual_output_url: Option<String>,
    /// actualRedirectResponseCode property.
    pub actual_redirect_response_code: Option<i64>,
    /// actualService property.
    pub actual_service: Option<String>,
    /// expectedOutputUrl property.
    pub expected_output_url: Option<String>,
    /// expectedRedirectResponseCode property.
    pub expected_redirect_response_code: Option<i64>,
    /// expectedService property.
    pub expected_service: Option<String>,
    /// headers property.
    pub headers: Option<Vec<UrlMapTestHeader>>,
    /// host property.
    pub host: Option<String>,
    /// path property.
    pub path: Option<String>,
}

/// `HttpRetryPolicy` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct HttpRetryPolicy {
    /// numRetries property.
    pub num_retries: Option<i64>,
    /// perTryTimeout property.
    pub per_try_timeout: Option<Duration>,
    /// retryConditions property.
    pub retry_conditions: Option<Vec<String>>,
}

/// `ResourceStatusReservationConsumptionInfo` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ResourceStatusReservationConsumptionInfo {
    /// consumedReservation property.
    pub consumed_reservation: Option<String>,
}

/// `SubnetworkList` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SubnetworkList {
    /// id property.
    pub id: Option<String>,
    /// items property.
    pub items: Option<Vec<Subnetwork>>,
    /// kind property.
    pub kind: Option<String>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// warning property.
    pub warning: Option<std::collections::HashMap<String, serde_json::Value>>,
}

/// `SnapshotParams` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SnapshotParams {
    /// resourceManagerTags property.
    pub resource_manager_tags: Option<serde_json::Value>,
}

/// `InstanceGroupManagerActionsSummary` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct InstanceGroupManagerActionsSummary {
    /// abandoning property.
    pub abandoning: Option<i64>,
    /// creating property.
    pub creating: Option<i64>,
    /// creatingWithoutRetries property.
    pub creating_without_retries: Option<i64>,
    /// deleting property.
    pub deleting: Option<i64>,
    /// none property.
    pub none: Option<i64>,
    /// recreating property.
    pub recreating: Option<i64>,
    /// refreshing property.
    pub refreshing: Option<i64>,
    /// restarting property.
    pub restarting: Option<i64>,
    /// resuming property.
    pub resuming: Option<i64>,
    /// starting property.
    pub starting: Option<i64>,
    /// stopping property.
    pub stopping: Option<i64>,
    /// suspending property.
    pub suspending: Option<i64>,
    /// verifying property.
    pub verifying: Option<i64>,
}

/// `InstanceGroupManagerStatusBulkInstanceOperation` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct InstanceGroupManagerStatusBulkInstanceOperation {
    /// inProgress property.
    pub in_progress: Option<bool>,
    /// lastProgressCheck property.
    pub last_progress_check:
        Option<InstanceGroupManagerStatusBulkInstanceOperationLastProgressCheck>,
}

/// `InstancePropertiesPatch` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct InstancePropertiesPatch {
    /// labels property.
    pub labels: Option<serde_json::Value>,
    /// metadata property.
    pub metadata: Option<serde_json::Value>,
}

/// `SecurityPolicyRuleRedirectOptions` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SecurityPolicyRuleRedirectOptions {
    /// target property.
    pub target: Option<String>,
    /// type property.
    pub r#type: Option<String>,
}

/// `ReservationBlocksListResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ReservationBlocksListResponse {
    /// id property.
    pub id: Option<String>,
    /// items property.
    pub items: Option<Vec<ReservationBlock>>,
    /// kind property.
    pub kind: Option<String>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// warning property.
    pub warning: Option<std::collections::HashMap<String, serde_json::Value>>,
}

/// `HttpsHealthCheck` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct HttpsHealthCheck {
    /// checkIntervalSec property.
    pub check_interval_sec: Option<i64>,
    /// creationTimestamp property.
    pub creation_timestamp: Option<String>,
    /// description property.
    pub description: Option<String>,
    /// healthyThreshold property.
    pub healthy_threshold: Option<i64>,
    /// host property.
    pub host: Option<String>,
    /// id property.
    pub id: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// port property.
    pub port: Option<i64>,
    /// requestPath property.
    pub request_path: Option<String>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// timeoutSec property.
    pub timeout_sec: Option<i64>,
    /// unhealthyThreshold property.
    pub unhealthy_threshold: Option<i64>,
}

/// `ResourcePolicySnapshotSchedulePolicySchedule` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ResourcePolicySnapshotSchedulePolicySchedule {
    /// dailySchedule property.
    pub daily_schedule: Option<ResourcePolicyDailyCycle>,
    /// hourlySchedule property.
    pub hourly_schedule: Option<ResourcePolicyHourlyCycle>,
    /// weeklySchedule property.
    pub weekly_schedule: Option<ResourcePolicyWeeklyCycle>,
}

/// `FirewallPolicyRule` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct FirewallPolicyRule {
    /// action property.
    pub action: Option<String>,
    /// description property.
    pub description: Option<String>,
    /// direction property.
    pub direction: Option<String>,
    /// disabled property.
    pub disabled: Option<bool>,
    /// enableLogging property.
    pub enable_logging: Option<bool>,
    /// kind property.
    pub kind: Option<String>,
    /// match property.
    pub r#match: Option<FirewallPolicyRuleMatcher>,
    /// priority property.
    pub priority: Option<i64>,
    /// ruleName property.
    pub rule_name: Option<String>,
    /// ruleTupleCount property.
    pub rule_tuple_count: Option<i64>,
    /// securityProfileGroup property.
    pub security_profile_group: Option<String>,
    /// targetResources property.
    pub target_resources: Option<Vec<String>>,
    /// targetSecureTags property.
    pub target_secure_tags: Option<Vec<FirewallPolicyRuleSecureTag>>,
    /// targetServiceAccounts property.
    pub target_service_accounts: Option<Vec<String>>,
    /// tlsInspect property.
    pub tls_inspect: Option<bool>,
}

/// `CacheInvalidationRule` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct CacheInvalidationRule {
    /// cacheTags property.
    pub cache_tags: Option<Vec<String>>,
    /// host property.
    pub host: Option<String>,
    /// path property.
    pub path: Option<String>,
}

/// `CompositeHealthCheckHealth` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct CompositeHealthCheckHealth {
    /// healthSources property.
    pub health_sources: Option<Vec<CompositeHealthChecksGetHealthResponseHealthSourceHealth>>,
    /// healthState property.
    pub health_state: Option<String>,
    /// kind property.
    pub kind: Option<String>,
}

/// `BackendBucketCdnPolicyCacheKeyPolicy` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct BackendBucketCdnPolicyCacheKeyPolicy {
    /// includeHttpHeaders property.
    pub include_http_headers: Option<Vec<String>>,
    /// queryStringWhitelist property.
    pub query_string_whitelist: Option<Vec<String>>,
}

/// `ExternalVpnGateway` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ExternalVpnGateway {
    /// creationTimestamp property.
    pub creation_timestamp: Option<String>,
    /// description property.
    pub description: Option<String>,
    /// id property.
    pub id: Option<String>,
    /// interfaces property.
    pub interfaces: Option<Vec<ExternalVpnGatewayInterface>>,
    /// kind property.
    pub kind: Option<String>,
    /// labelFingerprint property.
    pub label_fingerprint: Option<String>,
    /// labels property.
    pub labels: Option<serde_json::Value>,
    /// name property.
    pub name: Option<String>,
    /// params property.
    pub params: Option<ExternalVpnGatewayParams>,
    /// redundancyType property.
    pub redundancy_type: Option<String>,
    /// selfLink property.
    pub self_link: Option<String>,
}

/// `ResourcePolicyDailyCycle` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ResourcePolicyDailyCycle {
    /// daysInCycle property.
    pub days_in_cycle: Option<i64>,
    /// duration property.
    pub duration: Option<String>,
    /// startTime property.
    pub start_time: Option<String>,
}

/// `InstanceList` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct InstanceList {
    /// id property.
    pub id: Option<String>,
    /// items property.
    pub items: Option<Vec<Instance>>,
    /// kind property.
    pub kind: Option<String>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// warning property.
    pub warning: Option<std::collections::HashMap<String, serde_json::Value>>,
}

/// `StatefulPolicyPreservedState` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct StatefulPolicyPreservedState {
    /// disks property.
    pub disks: Option<serde_json::Value>,
    /// externalIPs property.
    pub external_i_ps: Option<serde_json::Value>,
    /// internalIPs property.
    pub internal_i_ps: Option<serde_json::Value>,
}

/// `NatIpInfoNatIpInfoMapping` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct NatIpInfoNatIpInfoMapping {
    /// mode property.
    pub mode: Option<String>,
    /// natIp property.
    pub nat_ip: Option<String>,
    /// usage property.
    pub usage: Option<String>,
}

/// `ProjectsSetCloudArmorTierRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ProjectsSetCloudArmorTierRequest {
    /// cloudArmorTier property.
    pub cloud_armor_tier: Option<String>,
}

/// `UrlMapsValidateRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct UrlMapsValidateRequest {
    /// loadBalancingSchemes property.
    pub load_balancing_schemes: Option<Vec<String>>,
    /// resource property.
    pub resource: Option<UrlMap>,
}

/// `AcceleratorTopologiesInfoAcceleratorTopologyInfoInfoPerTopologyState` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AcceleratorTopologiesInfoAcceleratorTopologyInfoInfoPerTopologyState {
    /// count property.
    pub count: Option<i64>,
    /// state property.
    pub state: Option<String>,
}

/// `ReservationBlockPhysicalTopologyInstancePhysicalHostTopology` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ReservationBlockPhysicalTopologyInstancePhysicalHostTopology {
    /// host property.
    pub host: Option<String>,
    /// subBlock property.
    pub sub_block: Option<String>,
}

/// `PublicDelegatedPrefixPublicDelegatedSubPrefix` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct PublicDelegatedPrefixPublicDelegatedSubPrefix {
    /// allocatablePrefixLength property.
    pub allocatable_prefix_length: Option<i64>,
    /// delegateeProject property.
    pub delegatee_project: Option<String>,
    /// description property.
    pub description: Option<String>,
    /// enableEnhancedIpv4Allocation property.
    pub enable_enhanced_ipv4_allocation: Option<bool>,
    /// ipCidrRange property.
    pub ip_cidr_range: Option<String>,
    /// ipv6AccessType property.
    pub ipv6_access_type: Option<String>,
    /// isAddress property.
    pub is_address: Option<bool>,
    /// mode property.
    pub mode: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// region property.
    pub region: Option<String>,
    /// status property.
    pub status: Option<String>,
}

/// `FirewallPolicyList` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct FirewallPolicyList {
    /// id property.
    pub id: Option<String>,
    /// items property.
    pub items: Option<Vec<FirewallPolicy>>,
    /// kind property.
    pub kind: Option<String>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// warning property.
    pub warning: Option<std::collections::HashMap<String, serde_json::Value>>,
}

/// `BgpRouteAsPath` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct BgpRouteAsPath {
    /// asns property.
    pub asns: Option<Vec<i64>>,
    /// asns32 property.
    pub asns32: Option<Vec<i64>>,
    /// type property.
    pub r#type: Option<String>,
}

/// `GetVersionOperationMetadataSbomInfo` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GetVersionOperationMetadataSbomInfo {
    /// currentComponentVersions property.
    pub current_component_versions: Option<serde_json::Value>,
    /// targetComponentVersions property.
    pub target_component_versions: Option<serde_json::Value>,
}

/// `SavedAttachedDisk` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SavedAttachedDisk {
    /// autoDelete property.
    pub auto_delete: Option<bool>,
    /// boot property.
    pub boot: Option<bool>,
    /// deviceName property.
    pub device_name: Option<String>,
    /// diskEncryptionKey property.
    pub disk_encryption_key: Option<CustomerEncryptionKey>,
    /// diskSizeGb property.
    pub disk_size_gb: Option<String>,
    /// diskType property.
    pub disk_type: Option<String>,
    /// guestOsFeatures property.
    pub guest_os_features: Option<Vec<GuestOsFeature>>,
    /// index property.
    pub index: Option<i64>,
    /// interface property.
    pub interface: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// licenses property.
    pub licenses: Option<Vec<String>>,
    /// mode property.
    pub mode: Option<String>,
    /// source property.
    pub source: Option<String>,
    /// storageBytes property.
    pub storage_bytes: Option<String>,
    /// storageBytesStatus property.
    pub storage_bytes_status: Option<String>,
    /// type property.
    pub r#type: Option<String>,
}

/// `InterconnectGroupsCreateMembersInterconnectInput` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct InterconnectGroupsCreateMembersInterconnectInput {
    /// adminEnabled property.
    pub admin_enabled: Option<bool>,
    /// customerName property.
    pub customer_name: Option<String>,
    /// description property.
    pub description: Option<String>,
    /// facility property.
    pub facility: Option<String>,
    /// interconnectType property.
    pub interconnect_type: Option<String>,
    /// linkType property.
    pub link_type: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// nocContactEmail property.
    pub noc_contact_email: Option<String>,
    /// remoteLocation property.
    pub remote_location: Option<String>,
    /// requestedFeatures property.
    pub requested_features: Option<Vec<String>>,
    /// requestedLinkCount property.
    pub requested_link_count: Option<i64>,
}

/// `HealthStatusForNetworkEndpoint` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct HealthStatusForNetworkEndpoint {
    /// backendService property.
    pub backend_service: Option<BackendServiceReference>,
    /// forwardingRule property.
    pub forwarding_rule: Option<ForwardingRuleReference>,
    /// healthCheck property.
    pub health_check: Option<HealthCheckReference>,
    /// healthCheckService property.
    pub health_check_service: Option<HealthCheckServiceReference>,
    /// healthState property.
    pub health_state: Option<String>,
    /// ipv6HealthState property.
    pub ipv6_health_state: Option<String>,
}

/// `InstanceConsumptionData` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct InstanceConsumptionData {
    /// consumptionInfo property.
    pub consumption_info: Option<InstanceConsumptionInfo>,
    /// instance property.
    pub instance: Option<String>,
}

/// `InterconnectGroupPhysicalStructureMetrosFacilitiesZones` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct InterconnectGroupPhysicalStructureMetrosFacilitiesZones {
    /// interconnects property.
    pub interconnects: Option<Vec<String>>,
    /// zone property.
    pub zone: Option<String>,
}

/// `WireGroup` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct WireGroup {
    /// adminEnabled property.
    pub admin_enabled: Option<bool>,
    /// creationTimestamp property.
    pub creation_timestamp: Option<String>,
    /// description property.
    pub description: Option<String>,
    /// endpoints property.
    pub endpoints: Option<serde_json::Value>,
    /// id property.
    pub id: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// reconciling property.
    pub reconciling: Option<bool>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// topology property.
    pub topology: Option<WireGroupTopology>,
    /// wireProperties property.
    pub wire_properties: Option<WireProperties>,
    /// wires property.
    pub wires: Option<Vec<Wire>>,
}

/// `InterconnectAttachmentGroupsGetOperationalStatusResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct InterconnectAttachmentGroupsGetOperationalStatusResponse {
    /// etag property.
    pub etag: Option<String>,
    /// result property.
    pub result: Option<InterconnectAttachmentGroupsOperationalStatus>,
}

/// `ReservationBlock` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ReservationBlock {
    /// count property.
    pub count: Option<i64>,
    /// creationTimestamp property.
    pub creation_timestamp: Option<String>,
    /// healthInfo property.
    pub health_info: Option<ReservationBlockHealthInfo>,
    /// id property.
    pub id: Option<String>,
    /// inUseCount property.
    pub in_use_count: Option<i64>,
    /// inUseHostCount property.
    pub in_use_host_count: Option<i64>,
    /// kind property.
    pub kind: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// physicalTopology property.
    pub physical_topology: Option<ReservationBlockPhysicalTopology>,
    /// reservationMaintenance property.
    pub reservation_maintenance: Option<GroupMaintenanceInfo>,
    /// reservationSubBlockCount property.
    pub reservation_sub_block_count: Option<i64>,
    /// reservationSubBlockInUseCount property.
    pub reservation_sub_block_in_use_count: Option<i64>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// selfLinkWithId property.
    pub self_link_with_id: Option<String>,
    /// status property.
    pub status: Option<String>,
    /// zone property.
    pub zone: Option<String>,
}

/// `InterconnectGroupsGetOperationalStatusResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct InterconnectGroupsGetOperationalStatusResponse {
    /// etag property.
    pub etag: Option<String>,
    /// result property.
    pub result: Option<InterconnectGroupsOperationalStatus>,
}

/// `CrossSiteNetwork` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct CrossSiteNetwork {
    /// creationTimestamp property.
    pub creation_timestamp: Option<String>,
    /// description property.
    pub description: Option<String>,
    /// id property.
    pub id: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// selfLink property.
    pub self_link: Option<String>,
}

/// `DisksResizeRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct DisksResizeRequest {
    /// sizeGb property.
    pub size_gb: Option<String>,
}

/// `SslPoliciesList` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SslPoliciesList {
    /// id property.
    pub id: Option<String>,
    /// items property.
    pub items: Option<Vec<SslPolicy>>,
    /// kind property.
    pub kind: Option<String>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// warning property.
    pub warning: Option<std::collections::HashMap<String, serde_json::Value>>,
}

/// `UrlMapReference` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct UrlMapReference {
    /// urlMap property.
    pub url_map: Option<String>,
}

/// `Reservation` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Reservation {
    /// advancedDeploymentControl property.
    pub advanced_deployment_control: Option<ReservationAdvancedDeploymentControl>,
    /// aggregateReservation property.
    pub aggregate_reservation: Option<AllocationAggregateReservation>,
    /// commitment property.
    pub commitment: Option<String>,
    /// confidentialComputeType property.
    pub confidential_compute_type: Option<String>,
    /// creationTimestamp property.
    pub creation_timestamp: Option<String>,
    /// deleteAfterDuration property.
    pub delete_after_duration: Option<Duration>,
    /// deleteAtTime property.
    pub delete_at_time: Option<String>,
    /// deploymentType property.
    pub deployment_type: Option<String>,
    /// description property.
    pub description: Option<String>,
    /// earlyAccessMaintenance property.
    pub early_access_maintenance: Option<String>,
    /// enableEmergentMaintenance property.
    pub enable_emergent_maintenance: Option<bool>,
    /// id property.
    pub id: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// linkedCommitments property.
    pub linked_commitments: Option<Vec<String>>,
    /// name property.
    pub name: Option<String>,
    /// params property.
    pub params: Option<ReservationParams>,
    /// protectionTier property.
    pub protection_tier: Option<String>,
    /// reservationSharingPolicy property.
    pub reservation_sharing_policy: Option<AllocationReservationSharingPolicy>,
    /// resourcePolicies property.
    pub resource_policies: Option<serde_json::Value>,
    /// resourceStatus property.
    pub resource_status: Option<AllocationResourceStatus>,
    /// satisfiesPzs property.
    pub satisfies_pzs: Option<bool>,
    /// schedulingType property.
    pub scheduling_type: Option<String>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// shareSettings property.
    pub share_settings: Option<ShareSettings>,
    /// specificReservation property.
    pub specific_reservation: Option<AllocationSpecificSKUReservation>,
    /// specificReservationRequired property.
    pub specific_reservation_required: Option<bool>,
    /// status property.
    pub status: Option<String>,
    /// zone property.
    pub zone: Option<String>,
}

/// `StoragePoolList` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct StoragePoolList {
    /// etag property.
    pub etag: Option<String>,
    /// id property.
    pub id: Option<String>,
    /// items property.
    pub items: Option<Vec<StoragePool>>,
    /// kind property.
    pub kind: Option<String>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// unreachables property.
    pub unreachables: Option<Vec<String>>,
    /// warning property.
    pub warning: Option<std::collections::HashMap<String, serde_json::Value>>,
}

/// `InstanceGroupsSetNamedPortsRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct InstanceGroupsSetNamedPortsRequest {
    /// fingerprint property.
    pub fingerprint: Option<String>,
    /// namedPorts property.
    pub named_ports: Option<Vec<NamedPort>>,
}

/// `TargetTcpProxiesSetBackendServiceRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct TargetTcpProxiesSetBackendServiceRequest {
    /// service property.
    pub service: Option<String>,
}

/// `RouterNatSubnetworkToNat64` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct RouterNatSubnetworkToNat64 {
    /// name property.
    pub name: Option<String>,
}

/// `RegionInstanceGroupManagerList` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct RegionInstanceGroupManagerList {
    /// id property.
    pub id: Option<String>,
    /// items property.
    pub items: Option<Vec<InstanceGroupManager>>,
    /// kind property.
    pub kind: Option<String>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// warning property.
    pub warning: Option<std::collections::HashMap<String, serde_json::Value>>,
}

/// `InterconnectDiagnosticsMacsecStatus` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct InterconnectDiagnosticsMacsecStatus {
    /// ckn property.
    pub ckn: Option<String>,
    /// operational property.
    pub operational: Option<bool>,
}

/// `NetworkPeeringConnectionStatusTrafficConfiguration` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct NetworkPeeringConnectionStatusTrafficConfiguration {
    /// exportCustomRoutesToPeer property.
    pub export_custom_routes_to_peer: Option<bool>,
    /// exportSubnetRoutesWithPublicIpToPeer property.
    pub export_subnet_routes_with_public_ip_to_peer: Option<bool>,
    /// importCustomRoutesFromPeer property.
    pub import_custom_routes_from_peer: Option<bool>,
    /// importSubnetRoutesWithPublicIpFromPeer property.
    pub import_subnet_routes_with_public_ip_from_peer: Option<bool>,
    /// stackType property.
    pub stack_type: Option<String>,
}

/// `VpnTunnelPhase1Algorithms` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct VpnTunnelPhase1Algorithms {
    /// dh property.
    pub dh: Option<Vec<String>>,
    /// encryption property.
    pub encryption: Option<Vec<String>>,
    /// integrity property.
    pub integrity: Option<Vec<String>>,
    /// prf property.
    pub prf: Option<Vec<String>>,
}

/// `InterconnectAttachmentGroupLogicalStructureRegion` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct InterconnectAttachmentGroupLogicalStructureRegion {
    /// metros property.
    pub metros: Option<Vec<InterconnectAttachmentGroupLogicalStructureRegionMetro>>,
    /// region property.
    pub region: Option<String>,
}

/// `RegionInstanceGroupManagersCreateInstancesRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct RegionInstanceGroupManagersCreateInstancesRequest {
    /// instances property.
    pub instances: Option<Vec<PerInstanceConfig>>,
}

/// `SecurityPolicyRuleRateLimitOptionsEnforceOnKeyConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SecurityPolicyRuleRateLimitOptionsEnforceOnKeyConfig {
    /// enforceOnKeyName property.
    pub enforce_on_key_name: Option<String>,
    /// enforceOnKeyType property.
    pub enforce_on_key_type: Option<String>,
}

/// `InstanceConsumptionInfo` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct InstanceConsumptionInfo {
    /// guestCpus property.
    pub guest_cpus: Option<i64>,
    /// localSsdGb property.
    pub local_ssd_gb: Option<i64>,
    /// memoryMb property.
    pub memory_mb: Option<i64>,
    /// minNodeCpus property.
    pub min_node_cpus: Option<i64>,
}

/// `InstancesStartWithEncryptionKeyRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct InstancesStartWithEncryptionKeyRequest {
    /// disks property.
    pub disks: Option<Vec<CustomerEncryptionKeyProtectedDisk>>,
}

/// `InstanceTemplateList` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct InstanceTemplateList {
    /// id property.
    pub id: Option<String>,
    /// items property.
    pub items: Option<Vec<InstanceTemplate>>,
    /// kind property.
    pub kind: Option<String>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// warning property.
    pub warning: Option<std::collections::HashMap<String, serde_json::Value>>,
}

/// `DiskTypeAggregatedList` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct DiskTypeAggregatedList {
    /// id property.
    pub id: Option<String>,
    /// items property.
    pub items: Option<serde_json::Value>,
    /// kind property.
    pub kind: Option<String>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// unreachables property.
    pub unreachables: Option<Vec<String>>,
    /// warning property.
    pub warning: Option<std::collections::HashMap<String, serde_json::Value>>,
}

/// `InterconnectDiagnosticsARPEntry` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct InterconnectDiagnosticsARPEntry {
    /// ipAddress property.
    pub ip_address: Option<String>,
    /// macAddress property.
    pub mac_address: Option<String>,
}

/// `ResourceStatusEffectiveInstanceMetadata` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ResourceStatusEffectiveInstanceMetadata {
    /// blockProjectSshKeysMetadataValue property.
    pub block_project_ssh_keys_metadata_value: Option<bool>,
    /// enableGuestAttributesMetadataValue property.
    pub enable_guest_attributes_metadata_value: Option<bool>,
    /// enableOsInventoryMetadataValue property.
    pub enable_os_inventory_metadata_value: Option<bool>,
    /// enableOsconfigMetadataValue property.
    pub enable_osconfig_metadata_value: Option<bool>,
    /// enableOsloginMetadataValue property.
    pub enable_oslogin_metadata_value: Option<bool>,
    /// gceContainerDeclarationMetadataValue property.
    pub gce_container_declaration_metadata_value: Option<bool>,
    /// serialPortEnableMetadataValue property.
    pub serial_port_enable_metadata_value: Option<bool>,
    /// serialPortLoggingEnableMetadataValue property.
    pub serial_port_logging_enable_metadata_value: Option<bool>,
    /// vmDnsSettingMetadataValue property.
    pub vm_dns_setting_metadata_value: Option<String>,
}

/// `ServiceAttachmentConnectedEndpoint` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ServiceAttachmentConnectedEndpoint {
    /// consumerNetwork property.
    pub consumer_network: Option<String>,
    /// endpoint property.
    pub endpoint: Option<String>,
    /// endpointWithId property.
    pub endpoint_with_id: Option<String>,
    /// natIps property.
    pub nat_ips: Option<Vec<String>>,
    /// propagatedConnectionCount property.
    pub propagated_connection_count: Option<i64>,
    /// pscConnectionId property.
    pub psc_connection_id: Option<String>,
    /// status property.
    pub status: Option<String>,
}

/// `TargetHttpsProxy` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct TargetHttpsProxy {
    /// authorizationPolicy property.
    pub authorization_policy: Option<String>,
    /// certificateMap property.
    pub certificate_map: Option<String>,
    /// creationTimestamp property.
    pub creation_timestamp: Option<String>,
    /// description property.
    pub description: Option<String>,
    /// fingerprint property.
    pub fingerprint: Option<String>,
    /// httpKeepAliveTimeoutSec property.
    pub http_keep_alive_timeout_sec: Option<i64>,
    /// id property.
    pub id: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// proxyBind property.
    pub proxy_bind: Option<bool>,
    /// quicOverride property.
    pub quic_override: Option<String>,
    /// region property.
    pub region: Option<String>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// serverTlsPolicy property.
    pub server_tls_policy: Option<String>,
    /// sslCertificates property.
    pub ssl_certificates: Option<Vec<String>>,
    /// sslPolicy property.
    pub ssl_policy: Option<String>,
    /// tlsEarlyData property.
    pub tls_early_data: Option<String>,
    /// urlMap property.
    pub url_map: Option<String>,
}

/// `NodeGroupList` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct NodeGroupList {
    /// id property.
    pub id: Option<String>,
    /// items property.
    pub items: Option<Vec<NodeGroup>>,
    /// kind property.
    pub kind: Option<String>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// warning property.
    pub warning: Option<std::collections::HashMap<String, serde_json::Value>>,
}

/// `QuotaExceededInfo` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct QuotaExceededInfo {
    /// dimensions property.
    pub dimensions: Option<serde_json::Value>,
    /// futureLimit property.
    pub future_limit: Option<f64>,
    /// limit property.
    pub limit: Option<f64>,
    /// limitName property.
    pub limit_name: Option<String>,
    /// metricName property.
    pub metric_name: Option<String>,
    /// rolloutStatus property.
    pub rollout_status: Option<String>,
}

/// `RouteAsPath` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct RouteAsPath {
    /// asLists property.
    pub as_lists: Option<Vec<i64>>,
    /// pathSegmentType property.
    pub path_segment_type: Option<String>,
}

/// `InstanceGroupManagerTargetSizePolicy` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct InstanceGroupManagerTargetSizePolicy {
    /// mode property.
    pub mode: Option<String>,
}

/// `SecurityPolicyRuleRateLimitOptionsThreshold` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SecurityPolicyRuleRateLimitOptionsThreshold {
    /// count property.
    pub count: Option<i64>,
    /// intervalSec property.
    pub interval_sec: Option<i64>,
}

/// `InstanceSettingsMetadata` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct InstanceSettingsMetadata {
    /// items property.
    pub items: Option<serde_json::Value>,
    /// kind property.
    pub kind: Option<String>,
}

/// `InterconnectAttachmentGroupsOperationalStatus` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct InterconnectAttachmentGroupsOperationalStatus {
    /// attachmentStatuses property.
    pub attachment_statuses:
        Option<Vec<InterconnectAttachmentGroupsOperationalStatusAttachmentStatus>>,
    /// configured property.
    pub configured: Option<InterconnectAttachmentGroupConfigured>,
    /// groupStatus property.
    pub group_status: Option<String>,
    /// intent property.
    pub intent: Option<InterconnectAttachmentGroupIntent>,
    /// operational property.
    pub operational: Option<InterconnectAttachmentGroupConfigured>,
}

/// `InterconnectAttachmentGroupIntent` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct InterconnectAttachmentGroupIntent {
    /// availabilitySla property.
    pub availability_sla: Option<String>,
}

/// `PacketIntervals` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct PacketIntervals {
    /// avgMs property.
    pub avg_ms: Option<String>,
    /// duration property.
    pub duration: Option<String>,
    /// maxMs property.
    pub max_ms: Option<String>,
    /// minMs property.
    pub min_ms: Option<String>,
    /// numIntervals property.
    pub num_intervals: Option<String>,
    /// type property.
    pub r#type: Option<String>,
}

/// `ResourcePolicyInstanceSchedulePolicy` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ResourcePolicyInstanceSchedulePolicy {
    /// expirationTime property.
    pub expiration_time: Option<String>,
    /// startTime property.
    pub start_time: Option<String>,
    /// timeZone property.
    pub time_zone: Option<String>,
    /// vmStartSchedule property.
    pub vm_start_schedule: Option<ResourcePolicyInstanceSchedulePolicySchedule>,
    /// vmStopSchedule property.
    pub vm_stop_schedule: Option<ResourcePolicyInstanceSchedulePolicySchedule>,
}

/// `BundledLocalSsds` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct BundledLocalSsds {
    /// defaultInterface property.
    pub default_interface: Option<String>,
    /// partitionCount property.
    pub partition_count: Option<i64>,
}

/// `BackendBucketUsedBy` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct BackendBucketUsedBy {
    /// reference property.
    pub reference: Option<String>,
}

/// `BulkSetLabelsRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct BulkSetLabelsRequest {
    /// labelFingerprint property.
    pub label_fingerprint: Option<String>,
    /// labels property.
    pub labels: Option<serde_json::Value>,
}

/// `InstanceGroupManagerAllInstancesConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct InstanceGroupManagerAllInstancesConfig {
    /// properties property.
    pub properties: Option<InstancePropertiesPatch>,
}

/// `StoragePoolExapoolProvisionedCapacityGb` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct StoragePoolExapoolProvisionedCapacityGb {
    /// capacityOptimized property.
    pub capacity_optimized: Option<String>,
    /// readOptimized property.
    pub read_optimized: Option<String>,
    /// writeOptimized property.
    pub write_optimized: Option<String>,
}

/// `MachineTypeAggregatedList` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct MachineTypeAggregatedList {
    /// id property.
    pub id: Option<String>,
    /// items property.
    pub items: Option<serde_json::Value>,
    /// kind property.
    pub kind: Option<String>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// unreachables property.
    pub unreachables: Option<Vec<String>>,
    /// warning property.
    pub warning: Option<std::collections::HashMap<String, serde_json::Value>>,
}

// =============================================================================
// ARGS TYPES (per-endpoint)
// =============================================================================

/// Arguments for [`compute.acceleratorTypes.aggregatedList_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeAcceleratorTypesAggregatedListArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `includeAllScopes`.
    pub include_all_scopes: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `returnPartialSuccess`.
    pub return_partial_success: Option<String>,
    /// Query parameter: `serviceProjectNumber`.
    pub service_project_number: Option<String>,
}

/// Arguments for [`compute.acceleratorTypes.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeAcceleratorTypesGetArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Path parameter: `acceleratorType`.
    pub accelerator_type: String,
}

/// Arguments for [`compute.acceleratorTypes.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeAcceleratorTypesListArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `returnPartialSuccess`.
    pub return_partial_success: Option<String>,
}

/// Arguments for [`compute.addresses.aggregatedList_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeAddressesAggregatedListArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `includeAllScopes`.
    pub include_all_scopes: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `returnPartialSuccess`.
    pub return_partial_success: Option<String>,
    /// Query parameter: `serviceProjectNumber`.
    pub service_project_number: Option<String>,
}

/// Arguments for [`compute.addresses.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeAddressesDeleteArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `address`.
    pub address: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
}

/// Arguments for [`compute.addresses.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeAddressesGetArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `address`.
    pub address: String,
}

/// Arguments for [`compute.addresses.insert_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeAddressesInsertArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: Address,
}

/// Arguments for [`compute.addresses.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeAddressesListArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `returnPartialSuccess`.
    pub return_partial_success: Option<String>,
}

/// Arguments for [`compute.addresses.move_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeAddressesMoveArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `address`.
    pub address: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: RegionAddressesMoveRequest,
}

/// Arguments for [`compute.addresses.setLabels_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeAddressesSetLabelsArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `resource`.
    pub resource: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: RegionSetLabelsRequest,
}

/// Arguments for [`compute.addresses.testIamPermissions_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeAddressesTestIamPermissionsArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `resource`.
    pub resource: String,
    /// Request body.
    pub body: TestPermissionsRequest,
}

/// Arguments for [`compute.advice.calendarMode_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeAdviceCalendarModeArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Request body.
    pub body: CalendarModeAdviceRequest,
}

/// Arguments for [`compute.autoscalers.aggregatedList_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeAutoscalersAggregatedListArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `includeAllScopes`.
    pub include_all_scopes: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `returnPartialSuccess`.
    pub return_partial_success: Option<String>,
    /// Query parameter: `serviceProjectNumber`.
    pub service_project_number: Option<String>,
}

/// Arguments for [`compute.autoscalers.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeAutoscalersDeleteArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Path parameter: `autoscaler`.
    pub autoscaler: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
}

/// Arguments for [`compute.autoscalers.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeAutoscalersGetArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Path parameter: `autoscaler`.
    pub autoscaler: String,
}

/// Arguments for [`compute.autoscalers.insert_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeAutoscalersInsertArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: Autoscaler,
}

/// Arguments for [`compute.autoscalers.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeAutoscalersListArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `returnPartialSuccess`.
    pub return_partial_success: Option<String>,
}

/// Arguments for [`compute.autoscalers.patch_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeAutoscalersPatchArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Query parameter: `autoscaler`.
    pub autoscaler: Option<String>,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: Autoscaler,
}

/// Arguments for [`compute.autoscalers.testIamPermissions_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeAutoscalersTestIamPermissionsArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Path parameter: `resource`.
    pub resource: String,
    /// Request body.
    pub body: TestPermissionsRequest,
}

/// Arguments for [`compute.autoscalers.update_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeAutoscalersUpdateArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Query parameter: `autoscaler`.
    pub autoscaler: Option<String>,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: Autoscaler,
}

/// Arguments for [`compute.backendBuckets.addSignedUrlKey_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeBackendBucketsAddSignedUrlKeyArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `backendBucket`.
    pub backend_bucket: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: SignedUrlKey,
}

/// Arguments for [`compute.backendBuckets.aggregatedList_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeBackendBucketsAggregatedListArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `includeAllScopes`.
    pub include_all_scopes: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `returnPartialSuccess`.
    pub return_partial_success: Option<String>,
    /// Query parameter: `serviceProjectNumber`.
    pub service_project_number: Option<String>,
}

/// Arguments for [`compute.backendBuckets.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeBackendBucketsDeleteArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `backendBucket`.
    pub backend_bucket: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
}

/// Arguments for [`compute.backendBuckets.deleteSignedUrlKey_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeBackendBucketsDeleteSignedUrlKeyArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `backendBucket`.
    pub backend_bucket: String,
    /// Query parameter: `keyName`.
    pub key_name: Option<String>,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
}

/// Arguments for [`compute.backendBuckets.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeBackendBucketsGetArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `backendBucket`.
    pub backend_bucket: String,
}

/// Arguments for [`compute.backendBuckets.getIamPolicy_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeBackendBucketsGetIamPolicyArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `resource`.
    pub resource: String,
    /// Query parameter: `optionsRequestedPolicyVersion`.
    pub options_requested_policy_version: Option<String>,
}

/// Arguments for [`compute.backendBuckets.insert_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeBackendBucketsInsertArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: BackendBucket,
}

/// Arguments for [`compute.backendBuckets.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeBackendBucketsListArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `returnPartialSuccess`.
    pub return_partial_success: Option<String>,
}

/// Arguments for [`compute.backendBuckets.listUsable_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeBackendBucketsListUsableArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `returnPartialSuccess`.
    pub return_partial_success: Option<String>,
}

/// Arguments for [`compute.backendBuckets.patch_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeBackendBucketsPatchArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `backendBucket`.
    pub backend_bucket: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: BackendBucket,
}

/// Arguments for [`compute.backendBuckets.setEdgeSecurityPolicy_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeBackendBucketsSetEdgeSecurityPolicyArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `backendBucket`.
    pub backend_bucket: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: SecurityPolicyReference,
}

/// Arguments for [`compute.backendBuckets.setIamPolicy_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeBackendBucketsSetIamPolicyArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `resource`.
    pub resource: String,
    /// Request body.
    pub body: GlobalSetPolicyRequest,
}

/// Arguments for [`compute.backendBuckets.testIamPermissions_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeBackendBucketsTestIamPermissionsArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `resource`.
    pub resource: String,
    /// Request body.
    pub body: TestPermissionsRequest,
}

/// Arguments for [`compute.backendBuckets.update_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeBackendBucketsUpdateArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `backendBucket`.
    pub backend_bucket: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: BackendBucket,
}

/// Arguments for [`compute.backendServices.addSignedUrlKey_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeBackendServicesAddSignedUrlKeyArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `backendService`.
    pub backend_service: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: SignedUrlKey,
}

/// Arguments for [`compute.backendServices.aggregatedList_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeBackendServicesAggregatedListArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `includeAllScopes`.
    pub include_all_scopes: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `returnPartialSuccess`.
    pub return_partial_success: Option<String>,
    /// Query parameter: `serviceProjectNumber`.
    pub service_project_number: Option<String>,
}

/// Arguments for [`compute.backendServices.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeBackendServicesDeleteArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `backendService`.
    pub backend_service: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
}

/// Arguments for [`compute.backendServices.deleteSignedUrlKey_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeBackendServicesDeleteSignedUrlKeyArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `backendService`.
    pub backend_service: String,
    /// Query parameter: `keyName`.
    pub key_name: Option<String>,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
}

/// Arguments for [`compute.backendServices.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeBackendServicesGetArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `backendService`.
    pub backend_service: String,
}

/// Arguments for [`compute.backendServices.getEffectiveSecurityPolicies_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeBackendServicesGetEffectiveSecurityPoliciesArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `backendService`.
    pub backend_service: String,
}

/// Arguments for [`compute.backendServices.getHealth_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeBackendServicesGetHealthArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `backendService`.
    pub backend_service: String,
    /// Request body.
    pub body: ResourceGroupReference,
}

/// Arguments for [`compute.backendServices.getIamPolicy_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeBackendServicesGetIamPolicyArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `resource`.
    pub resource: String,
    /// Query parameter: `optionsRequestedPolicyVersion`.
    pub options_requested_policy_version: Option<String>,
}

/// Arguments for [`compute.backendServices.insert_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeBackendServicesInsertArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: BackendService,
}

/// Arguments for [`compute.backendServices.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeBackendServicesListArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `returnPartialSuccess`.
    pub return_partial_success: Option<String>,
}

/// Arguments for [`compute.backendServices.listUsable_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeBackendServicesListUsableArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `returnPartialSuccess`.
    pub return_partial_success: Option<String>,
}

/// Arguments for [`compute.backendServices.patch_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeBackendServicesPatchArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `backendService`.
    pub backend_service: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: BackendService,
}

/// Arguments for [`compute.backendServices.setEdgeSecurityPolicy_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeBackendServicesSetEdgeSecurityPolicyArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `backendService`.
    pub backend_service: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: SecurityPolicyReference,
}

/// Arguments for [`compute.backendServices.setIamPolicy_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeBackendServicesSetIamPolicyArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `resource`.
    pub resource: String,
    /// Request body.
    pub body: GlobalSetPolicyRequest,
}

/// Arguments for [`compute.backendServices.setSecurityPolicy_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeBackendServicesSetSecurityPolicyArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `backendService`.
    pub backend_service: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: SecurityPolicyReference,
}

/// Arguments for [`compute.backendServices.testIamPermissions_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeBackendServicesTestIamPermissionsArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `resource`.
    pub resource: String,
    /// Request body.
    pub body: TestPermissionsRequest,
}

/// Arguments for [`compute.backendServices.update_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeBackendServicesUpdateArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `backendService`.
    pub backend_service: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: BackendService,
}

/// Arguments for [`compute.crossSiteNetworks.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeCrossSiteNetworksDeleteArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `crossSiteNetwork`.
    pub cross_site_network: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
}

/// Arguments for [`compute.crossSiteNetworks.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeCrossSiteNetworksGetArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `crossSiteNetwork`.
    pub cross_site_network: String,
}

/// Arguments for [`compute.crossSiteNetworks.insert_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeCrossSiteNetworksInsertArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Query parameter: `validateOnly`.
    pub validate_only: Option<String>,
    /// Request body.
    pub body: CrossSiteNetwork,
}

/// Arguments for [`compute.crossSiteNetworks.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeCrossSiteNetworksListArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `returnPartialSuccess`.
    pub return_partial_success: Option<String>,
}

/// Arguments for [`compute.crossSiteNetworks.patch_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeCrossSiteNetworksPatchArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `crossSiteNetwork`.
    pub cross_site_network: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Query parameter: `updateMask`.
    pub update_mask: Option<String>,
    /// Query parameter: `validateOnly`.
    pub validate_only: Option<String>,
    /// Request body.
    pub body: CrossSiteNetwork,
}

/// Arguments for [`compute.diskTypes.aggregatedList_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeDiskTypesAggregatedListArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `includeAllScopes`.
    pub include_all_scopes: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `returnPartialSuccess`.
    pub return_partial_success: Option<String>,
    /// Query parameter: `serviceProjectNumber`.
    pub service_project_number: Option<String>,
}

/// Arguments for [`compute.diskTypes.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeDiskTypesGetArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Path parameter: `diskType`.
    pub disk_type: String,
}

/// Arguments for [`compute.diskTypes.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeDiskTypesListArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `returnPartialSuccess`.
    pub return_partial_success: Option<String>,
}

/// Arguments for [`compute.disks.addResourcePolicies_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeDisksAddResourcePoliciesArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Path parameter: `disk`.
    pub disk: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: DisksAddResourcePoliciesRequest,
}

/// Arguments for [`compute.disks.aggregatedList_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeDisksAggregatedListArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `includeAllScopes`.
    pub include_all_scopes: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `returnPartialSuccess`.
    pub return_partial_success: Option<String>,
    /// Query parameter: `serviceProjectNumber`.
    pub service_project_number: Option<String>,
}

/// Arguments for [`compute.disks.bulkInsert_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeDisksBulkInsertArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: BulkInsertDiskResource,
}

/// Arguments for [`compute.disks.bulkSetLabels_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeDisksBulkSetLabelsArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Query parameter: `resource`.
    pub resource: Option<String>,
    /// Request body.
    pub body: BulkZoneSetLabelsRequest,
}

/// Arguments for [`compute.disks.createSnapshot_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeDisksCreateSnapshotArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Path parameter: `disk`.
    pub disk: String,
    /// Query parameter: `guestFlush`.
    pub guest_flush: Option<String>,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: Snapshot,
}

/// Arguments for [`compute.disks.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeDisksDeleteArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Path parameter: `disk`.
    pub disk: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
}

/// Arguments for [`compute.disks.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeDisksGetArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Path parameter: `disk`.
    pub disk: String,
}

/// Arguments for [`compute.disks.getIamPolicy_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeDisksGetIamPolicyArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Path parameter: `resource`.
    pub resource: String,
    /// Query parameter: `optionsRequestedPolicyVersion`.
    pub options_requested_policy_version: Option<String>,
}

/// Arguments for [`compute.disks.insert_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeDisksInsertArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Query parameter: `sourceImage`.
    pub source_image: Option<String>,
    /// Request body.
    pub body: Disk,
}

/// Arguments for [`compute.disks.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeDisksListArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `returnPartialSuccess`.
    pub return_partial_success: Option<String>,
}

/// Arguments for [`compute.disks.removeResourcePolicies_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeDisksRemoveResourcePoliciesArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Path parameter: `disk`.
    pub disk: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: DisksRemoveResourcePoliciesRequest,
}

/// Arguments for [`compute.disks.resize_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeDisksResizeArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Path parameter: `disk`.
    pub disk: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: DisksResizeRequest,
}

/// Arguments for [`compute.disks.setIamPolicy_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeDisksSetIamPolicyArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Path parameter: `resource`.
    pub resource: String,
    /// Request body.
    pub body: ZoneSetPolicyRequest,
}

/// Arguments for [`compute.disks.setLabels_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeDisksSetLabelsArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Path parameter: `resource`.
    pub resource: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: ZoneSetLabelsRequest,
}

/// Arguments for [`compute.disks.startAsyncReplication_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeDisksStartAsyncReplicationArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Path parameter: `disk`.
    pub disk: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: DisksStartAsyncReplicationRequest,
}

/// Arguments for [`compute.disks.stopAsyncReplication_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeDisksStopAsyncReplicationArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Path parameter: `disk`.
    pub disk: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
}

/// Arguments for [`compute.disks.stopGroupAsyncReplication_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeDisksStopGroupAsyncReplicationArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: DisksStopGroupAsyncReplicationResource,
}

/// Arguments for [`compute.disks.testIamPermissions_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeDisksTestIamPermissionsArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Path parameter: `resource`.
    pub resource: String,
    /// Request body.
    pub body: TestPermissionsRequest,
}

/// Arguments for [`compute.disks.update_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeDisksUpdateArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Path parameter: `disk`.
    pub disk: String,
    /// Query parameter: `paths`.
    pub paths: Option<String>,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Query parameter: `updateMask`.
    pub update_mask: Option<String>,
    /// Request body.
    pub body: Disk,
}

/// Arguments for [`compute.disks.updateKmsKey_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeDisksUpdateKmsKeyArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Path parameter: `disk`.
    pub disk: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: DiskUpdateKmsKeyRequest,
}

/// Arguments for [`compute.externalVpnGateways.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeExternalVpnGatewaysDeleteArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `externalVpnGateway`.
    pub external_vpn_gateway: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
}

/// Arguments for [`compute.externalVpnGateways.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeExternalVpnGatewaysGetArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `externalVpnGateway`.
    pub external_vpn_gateway: String,
}

/// Arguments for [`compute.externalVpnGateways.insert_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeExternalVpnGatewaysInsertArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: ExternalVpnGateway,
}

/// Arguments for [`compute.externalVpnGateways.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeExternalVpnGatewaysListArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `returnPartialSuccess`.
    pub return_partial_success: Option<String>,
}

/// Arguments for [`compute.externalVpnGateways.setLabels_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeExternalVpnGatewaysSetLabelsArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `resource`.
    pub resource: String,
    /// Request body.
    pub body: GlobalSetLabelsRequest,
}

/// Arguments for [`compute.externalVpnGateways.testIamPermissions_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeExternalVpnGatewaysTestIamPermissionsArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `resource`.
    pub resource: String,
    /// Request body.
    pub body: TestPermissionsRequest,
}

/// Arguments for [`compute.firewallPolicies.addAssociation_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeFirewallPoliciesAddAssociationArgs {
    /// Path parameter: `firewallPolicy`.
    pub firewall_policy: String,
    /// Query parameter: `replaceExistingAssociation`.
    pub replace_existing_association: Option<String>,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: FirewallPolicyAssociation,
}

/// Arguments for [`compute.firewallPolicies.addRule_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeFirewallPoliciesAddRuleArgs {
    /// Path parameter: `firewallPolicy`.
    pub firewall_policy: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: FirewallPolicyRule,
}

/// Arguments for [`compute.firewallPolicies.cloneRules_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeFirewallPoliciesCloneRulesArgs {
    /// Path parameter: `firewallPolicy`.
    pub firewall_policy: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Query parameter: `sourceFirewallPolicy`.
    pub source_firewall_policy: Option<String>,
}

/// Arguments for [`compute.firewallPolicies.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeFirewallPoliciesDeleteArgs {
    /// Path parameter: `firewallPolicy`.
    pub firewall_policy: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
}

/// Arguments for [`compute.firewallPolicies.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeFirewallPoliciesGetArgs {
    /// Path parameter: `firewallPolicy`.
    pub firewall_policy: String,
}

/// Arguments for [`compute.firewallPolicies.getAssociation_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeFirewallPoliciesGetAssociationArgs {
    /// Path parameter: `firewallPolicy`.
    pub firewall_policy: String,
    /// Query parameter: `name`.
    pub name: Option<String>,
}

/// Arguments for [`compute.firewallPolicies.getIamPolicy_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeFirewallPoliciesGetIamPolicyArgs {
    /// Path parameter: `resource`.
    pub resource: String,
    /// Query parameter: `optionsRequestedPolicyVersion`.
    pub options_requested_policy_version: Option<String>,
}

/// Arguments for [`compute.firewallPolicies.getRule_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeFirewallPoliciesGetRuleArgs {
    /// Path parameter: `firewallPolicy`.
    pub firewall_policy: String,
    /// Query parameter: `priority`.
    pub priority: Option<String>,
}

/// Arguments for [`compute.firewallPolicies.insert_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeFirewallPoliciesInsertArgs {
    /// Query parameter: `parentId`.
    pub parent_id: Option<String>,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: FirewallPolicy,
}

/// Arguments for [`compute.firewallPolicies.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeFirewallPoliciesListArgs {
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `parentId`.
    pub parent_id: Option<String>,
    /// Query parameter: `returnPartialSuccess`.
    pub return_partial_success: Option<String>,
}

/// Arguments for [`compute.firewallPolicies.listAssociations_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeFirewallPoliciesListAssociationsArgs {
    /// Query parameter: `includeInheritedPolicies`.
    pub include_inherited_policies: Option<String>,
    /// Query parameter: `targetResource`.
    pub target_resource: Option<String>,
}

/// Arguments for [`compute.firewallPolicies.move_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeFirewallPoliciesMoveArgs {
    /// Path parameter: `firewallPolicy`.
    pub firewall_policy: String,
    /// Query parameter: `parentId`.
    pub parent_id: Option<String>,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
}

/// Arguments for [`compute.firewallPolicies.patch_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeFirewallPoliciesPatchArgs {
    /// Path parameter: `firewallPolicy`.
    pub firewall_policy: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: FirewallPolicy,
}

/// Arguments for [`compute.firewallPolicies.patchRule_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeFirewallPoliciesPatchRuleArgs {
    /// Path parameter: `firewallPolicy`.
    pub firewall_policy: String,
    /// Query parameter: `priority`.
    pub priority: Option<String>,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: FirewallPolicyRule,
}

/// Arguments for [`compute.firewallPolicies.removeAssociation_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeFirewallPoliciesRemoveAssociationArgs {
    /// Path parameter: `firewallPolicy`.
    pub firewall_policy: String,
    /// Query parameter: `name`.
    pub name: Option<String>,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
}

/// Arguments for [`compute.firewallPolicies.removeRule_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeFirewallPoliciesRemoveRuleArgs {
    /// Path parameter: `firewallPolicy`.
    pub firewall_policy: String,
    /// Query parameter: `priority`.
    pub priority: Option<String>,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
}

/// Arguments for [`compute.firewallPolicies.setIamPolicy_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeFirewallPoliciesSetIamPolicyArgs {
    /// Path parameter: `resource`.
    pub resource: String,
    /// Request body.
    pub body: GlobalOrganizationSetPolicyRequest,
}

/// Arguments for [`compute.firewallPolicies.testIamPermissions_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeFirewallPoliciesTestIamPermissionsArgs {
    /// Path parameter: `resource`.
    pub resource: String,
    /// Request body.
    pub body: TestPermissionsRequest,
}

/// Arguments for [`compute.firewalls.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeFirewallsDeleteArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `firewall`.
    pub firewall: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
}

/// Arguments for [`compute.firewalls.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeFirewallsGetArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `firewall`.
    pub firewall: String,
}

/// Arguments for [`compute.firewalls.insert_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeFirewallsInsertArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: Firewall,
}

/// Arguments for [`compute.firewalls.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeFirewallsListArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `returnPartialSuccess`.
    pub return_partial_success: Option<String>,
}

/// Arguments for [`compute.firewalls.patch_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeFirewallsPatchArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `firewall`.
    pub firewall: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: Firewall,
}

/// Arguments for [`compute.firewalls.testIamPermissions_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeFirewallsTestIamPermissionsArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `resource`.
    pub resource: String,
    /// Request body.
    pub body: TestPermissionsRequest,
}

/// Arguments for [`compute.firewalls.update_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeFirewallsUpdateArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `firewall`.
    pub firewall: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: Firewall,
}

/// Arguments for [`compute.forwardingRules.aggregatedList_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeForwardingRulesAggregatedListArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `includeAllScopes`.
    pub include_all_scopes: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `returnPartialSuccess`.
    pub return_partial_success: Option<String>,
    /// Query parameter: `serviceProjectNumber`.
    pub service_project_number: Option<String>,
}

/// Arguments for [`compute.forwardingRules.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeForwardingRulesDeleteArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `forwardingRule`.
    pub forwarding_rule: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
}

/// Arguments for [`compute.forwardingRules.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeForwardingRulesGetArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `forwardingRule`.
    pub forwarding_rule: String,
}

/// Arguments for [`compute.forwardingRules.insert_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeForwardingRulesInsertArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: ForwardingRule,
}

/// Arguments for [`compute.forwardingRules.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeForwardingRulesListArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `returnPartialSuccess`.
    pub return_partial_success: Option<String>,
}

/// Arguments for [`compute.forwardingRules.patch_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeForwardingRulesPatchArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `forwardingRule`.
    pub forwarding_rule: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: ForwardingRule,
}

/// Arguments for [`compute.forwardingRules.setLabels_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeForwardingRulesSetLabelsArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `resource`.
    pub resource: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: RegionSetLabelsRequest,
}

/// Arguments for [`compute.forwardingRules.setTarget_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeForwardingRulesSetTargetArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `forwardingRule`.
    pub forwarding_rule: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: TargetReference,
}

/// Arguments for [`compute.futureReservations.aggregatedList_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeFutureReservationsAggregatedListArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `includeAllScopes`.
    pub include_all_scopes: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `returnPartialSuccess`.
    pub return_partial_success: Option<String>,
    /// Query parameter: `serviceProjectNumber`.
    pub service_project_number: Option<String>,
}

/// Arguments for [`compute.futureReservations.cancel_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeFutureReservationsCancelArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Path parameter: `futureReservation`.
    pub future_reservation: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
}

/// Arguments for [`compute.futureReservations.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeFutureReservationsDeleteArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Path parameter: `futureReservation`.
    pub future_reservation: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
}

/// Arguments for [`compute.futureReservations.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeFutureReservationsGetArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Path parameter: `futureReservation`.
    pub future_reservation: String,
}

/// Arguments for [`compute.futureReservations.insert_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeFutureReservationsInsertArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: FutureReservation,
}

/// Arguments for [`compute.futureReservations.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeFutureReservationsListArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `returnPartialSuccess`.
    pub return_partial_success: Option<String>,
}

/// Arguments for [`compute.futureReservations.update_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeFutureReservationsUpdateArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Path parameter: `futureReservation`.
    pub future_reservation: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Query parameter: `updateMask`.
    pub update_mask: Option<String>,
    /// Request body.
    pub body: FutureReservation,
}

/// Arguments for [`compute.globalAddresses.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeGlobalAddressesDeleteArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `address`.
    pub address: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
}

/// Arguments for [`compute.globalAddresses.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeGlobalAddressesGetArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `address`.
    pub address: String,
}

/// Arguments for [`compute.globalAddresses.insert_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeGlobalAddressesInsertArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: Address,
}

/// Arguments for [`compute.globalAddresses.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeGlobalAddressesListArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `returnPartialSuccess`.
    pub return_partial_success: Option<String>,
}

/// Arguments for [`compute.globalAddresses.move_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeGlobalAddressesMoveArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `address`.
    pub address: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: GlobalAddressesMoveRequest,
}

/// Arguments for [`compute.globalAddresses.setLabels_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeGlobalAddressesSetLabelsArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `resource`.
    pub resource: String,
    /// Request body.
    pub body: GlobalSetLabelsRequest,
}

/// Arguments for [`compute.globalAddresses.testIamPermissions_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeGlobalAddressesTestIamPermissionsArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `resource`.
    pub resource: String,
    /// Request body.
    pub body: TestPermissionsRequest,
}

/// Arguments for [`compute.globalForwardingRules.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeGlobalForwardingRulesDeleteArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `forwardingRule`.
    pub forwarding_rule: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
}

/// Arguments for [`compute.globalForwardingRules.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeGlobalForwardingRulesGetArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `forwardingRule`.
    pub forwarding_rule: String,
}

/// Arguments for [`compute.globalForwardingRules.insert_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeGlobalForwardingRulesInsertArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: ForwardingRule,
}

/// Arguments for [`compute.globalForwardingRules.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeGlobalForwardingRulesListArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `returnPartialSuccess`.
    pub return_partial_success: Option<String>,
}

/// Arguments for [`compute.globalForwardingRules.patch_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeGlobalForwardingRulesPatchArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `forwardingRule`.
    pub forwarding_rule: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: ForwardingRule,
}

/// Arguments for [`compute.globalForwardingRules.setLabels_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeGlobalForwardingRulesSetLabelsArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `resource`.
    pub resource: String,
    /// Request body.
    pub body: GlobalSetLabelsRequest,
}

/// Arguments for [`compute.globalForwardingRules.setTarget_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeGlobalForwardingRulesSetTargetArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `forwardingRule`.
    pub forwarding_rule: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: TargetReference,
}

/// Arguments for [`compute.globalNetworkEndpointGroups.attachNetworkEndpoints_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeGlobalNetworkEndpointGroupsAttachNetworkEndpointsArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `networkEndpointGroup`.
    pub network_endpoint_group: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: GlobalNetworkEndpointGroupsAttachEndpointsRequest,
}

/// Arguments for [`compute.globalNetworkEndpointGroups.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeGlobalNetworkEndpointGroupsDeleteArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `networkEndpointGroup`.
    pub network_endpoint_group: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
}

/// Arguments for [`compute.globalNetworkEndpointGroups.detachNetworkEndpoints_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeGlobalNetworkEndpointGroupsDetachNetworkEndpointsArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `networkEndpointGroup`.
    pub network_endpoint_group: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: GlobalNetworkEndpointGroupsDetachEndpointsRequest,
}

/// Arguments for [`compute.globalNetworkEndpointGroups.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeGlobalNetworkEndpointGroupsGetArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `networkEndpointGroup`.
    pub network_endpoint_group: String,
}

/// Arguments for [`compute.globalNetworkEndpointGroups.insert_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeGlobalNetworkEndpointGroupsInsertArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: NetworkEndpointGroup,
}

/// Arguments for [`compute.globalNetworkEndpointGroups.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeGlobalNetworkEndpointGroupsListArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `returnPartialSuccess`.
    pub return_partial_success: Option<String>,
}

/// Arguments for [`compute.globalNetworkEndpointGroups.listNetworkEndpoints_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeGlobalNetworkEndpointGroupsListNetworkEndpointsArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `networkEndpointGroup`.
    pub network_endpoint_group: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `returnPartialSuccess`.
    pub return_partial_success: Option<String>,
}

/// Arguments for [`compute.globalOperations.aggregatedList_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeGlobalOperationsAggregatedListArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `includeAllScopes`.
    pub include_all_scopes: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `returnPartialSuccess`.
    pub return_partial_success: Option<String>,
    /// Query parameter: `serviceProjectNumber`.
    pub service_project_number: Option<String>,
}

/// Arguments for [`compute.globalOperations.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeGlobalOperationsDeleteArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `operation`.
    pub operation: String,
}

/// Arguments for [`compute.globalOperations.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeGlobalOperationsGetArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `operation`.
    pub operation: String,
}

/// Arguments for [`compute.globalOperations.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeGlobalOperationsListArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `returnPartialSuccess`.
    pub return_partial_success: Option<String>,
}

/// Arguments for [`compute.globalOperations.wait_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeGlobalOperationsWaitArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `operation`.
    pub operation: String,
}

/// Arguments for [`compute.globalOrganizationOperations.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeGlobalOrganizationOperationsDeleteArgs {
    /// Path parameter: `operation`.
    pub operation: String,
    /// Query parameter: `parentId`.
    pub parent_id: Option<String>,
}

/// Arguments for [`compute.globalOrganizationOperations.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeGlobalOrganizationOperationsGetArgs {
    /// Path parameter: `operation`.
    pub operation: String,
    /// Query parameter: `parentId`.
    pub parent_id: Option<String>,
}

/// Arguments for [`compute.globalOrganizationOperations.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeGlobalOrganizationOperationsListArgs {
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `parentId`.
    pub parent_id: Option<String>,
    /// Query parameter: `returnPartialSuccess`.
    pub return_partial_success: Option<String>,
}

/// Arguments for [`compute.globalPublicDelegatedPrefixes.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeGlobalPublicDelegatedPrefixesDeleteArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `publicDelegatedPrefix`.
    pub public_delegated_prefix: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
}

/// Arguments for [`compute.globalPublicDelegatedPrefixes.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeGlobalPublicDelegatedPrefixesGetArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `publicDelegatedPrefix`.
    pub public_delegated_prefix: String,
}

/// Arguments for [`compute.globalPublicDelegatedPrefixes.insert_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeGlobalPublicDelegatedPrefixesInsertArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: PublicDelegatedPrefix,
}

/// Arguments for [`compute.globalPublicDelegatedPrefixes.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeGlobalPublicDelegatedPrefixesListArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `returnPartialSuccess`.
    pub return_partial_success: Option<String>,
}

/// Arguments for [`compute.globalPublicDelegatedPrefixes.patch_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeGlobalPublicDelegatedPrefixesPatchArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `publicDelegatedPrefix`.
    pub public_delegated_prefix: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: PublicDelegatedPrefix,
}

/// Arguments for [`compute.healthChecks.aggregatedList_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeHealthChecksAggregatedListArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `includeAllScopes`.
    pub include_all_scopes: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `returnPartialSuccess`.
    pub return_partial_success: Option<String>,
    /// Query parameter: `serviceProjectNumber`.
    pub service_project_number: Option<String>,
}

/// Arguments for [`compute.healthChecks.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeHealthChecksDeleteArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `healthCheck`.
    pub health_check: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
}

/// Arguments for [`compute.healthChecks.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeHealthChecksGetArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `healthCheck`.
    pub health_check: String,
}

/// Arguments for [`compute.healthChecks.insert_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeHealthChecksInsertArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: HealthCheck,
}

/// Arguments for [`compute.healthChecks.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeHealthChecksListArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `returnPartialSuccess`.
    pub return_partial_success: Option<String>,
}

/// Arguments for [`compute.healthChecks.patch_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeHealthChecksPatchArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `healthCheck`.
    pub health_check: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: HealthCheck,
}

/// Arguments for [`compute.healthChecks.testIamPermissions_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeHealthChecksTestIamPermissionsArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `resource`.
    pub resource: String,
    /// Request body.
    pub body: TestPermissionsRequest,
}

/// Arguments for [`compute.healthChecks.update_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeHealthChecksUpdateArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `healthCheck`.
    pub health_check: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: HealthCheck,
}

/// Arguments for [`compute.httpHealthChecks.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeHttpHealthChecksDeleteArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `httpHealthCheck`.
    pub http_health_check: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
}

/// Arguments for [`compute.httpHealthChecks.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeHttpHealthChecksGetArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `httpHealthCheck`.
    pub http_health_check: String,
}

/// Arguments for [`compute.httpHealthChecks.insert_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeHttpHealthChecksInsertArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: HttpHealthCheck,
}

/// Arguments for [`compute.httpHealthChecks.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeHttpHealthChecksListArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `returnPartialSuccess`.
    pub return_partial_success: Option<String>,
}

/// Arguments for [`compute.httpHealthChecks.patch_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeHttpHealthChecksPatchArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `httpHealthCheck`.
    pub http_health_check: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: HttpHealthCheck,
}

/// Arguments for [`compute.httpHealthChecks.testIamPermissions_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeHttpHealthChecksTestIamPermissionsArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `resource`.
    pub resource: String,
    /// Request body.
    pub body: TestPermissionsRequest,
}

/// Arguments for [`compute.httpHealthChecks.update_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeHttpHealthChecksUpdateArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `httpHealthCheck`.
    pub http_health_check: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: HttpHealthCheck,
}

/// Arguments for [`compute.httpsHealthChecks.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeHttpsHealthChecksDeleteArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `httpsHealthCheck`.
    pub https_health_check: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
}

/// Arguments for [`compute.httpsHealthChecks.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeHttpsHealthChecksGetArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `httpsHealthCheck`.
    pub https_health_check: String,
}

/// Arguments for [`compute.httpsHealthChecks.insert_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeHttpsHealthChecksInsertArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: HttpsHealthCheck,
}

/// Arguments for [`compute.httpsHealthChecks.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeHttpsHealthChecksListArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `returnPartialSuccess`.
    pub return_partial_success: Option<String>,
}

/// Arguments for [`compute.httpsHealthChecks.patch_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeHttpsHealthChecksPatchArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `httpsHealthCheck`.
    pub https_health_check: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: HttpsHealthCheck,
}

/// Arguments for [`compute.httpsHealthChecks.testIamPermissions_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeHttpsHealthChecksTestIamPermissionsArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `resource`.
    pub resource: String,
    /// Request body.
    pub body: TestPermissionsRequest,
}

/// Arguments for [`compute.httpsHealthChecks.update_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeHttpsHealthChecksUpdateArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `httpsHealthCheck`.
    pub https_health_check: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: HttpsHealthCheck,
}

/// Arguments for [`compute.imageFamilyViews.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeImageFamilyViewsGetArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Path parameter: `family`.
    pub family: String,
}

/// Arguments for [`compute.images.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeImagesDeleteArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `image`.
    pub image: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
}

/// Arguments for [`compute.images.deprecate_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeImagesDeprecateArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `image`.
    pub image: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: DeprecationStatus,
}

/// Arguments for [`compute.images.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeImagesGetArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `image`.
    pub image: String,
}

/// Arguments for [`compute.images.getFromFamily_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeImagesGetFromFamilyArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `family`.
    pub family: String,
}

/// Arguments for [`compute.images.getIamPolicy_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeImagesGetIamPolicyArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `resource`.
    pub resource: String,
    /// Query parameter: `optionsRequestedPolicyVersion`.
    pub options_requested_policy_version: Option<String>,
}

/// Arguments for [`compute.images.insert_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeImagesInsertArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Query parameter: `forceCreate`.
    pub force_create: Option<String>,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: Image,
}

/// Arguments for [`compute.images.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeImagesListArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `returnPartialSuccess`.
    pub return_partial_success: Option<String>,
}

/// Arguments for [`compute.images.patch_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeImagesPatchArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `image`.
    pub image: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: Image,
}

/// Arguments for [`compute.images.setIamPolicy_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeImagesSetIamPolicyArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `resource`.
    pub resource: String,
    /// Request body.
    pub body: GlobalSetPolicyRequest,
}

/// Arguments for [`compute.images.setLabels_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeImagesSetLabelsArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `resource`.
    pub resource: String,
    /// Request body.
    pub body: GlobalSetLabelsRequest,
}

/// Arguments for [`compute.images.testIamPermissions_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeImagesTestIamPermissionsArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `resource`.
    pub resource: String,
    /// Request body.
    pub body: TestPermissionsRequest,
}

/// Arguments for [`compute.instanceGroupManagerResizeRequests.cancel_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeInstanceGroupManagerResizeRequestsCancelArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Path parameter: `instanceGroupManager`.
    pub instance_group_manager: String,
    /// Path parameter: `resizeRequest`.
    pub resize_request: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
}

/// Arguments for [`compute.instanceGroupManagerResizeRequests.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeInstanceGroupManagerResizeRequestsDeleteArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Path parameter: `instanceGroupManager`.
    pub instance_group_manager: String,
    /// Path parameter: `resizeRequest`.
    pub resize_request: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
}

/// Arguments for [`compute.instanceGroupManagerResizeRequests.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeInstanceGroupManagerResizeRequestsGetArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Path parameter: `instanceGroupManager`.
    pub instance_group_manager: String,
    /// Path parameter: `resizeRequest`.
    pub resize_request: String,
}

/// Arguments for [`compute.instanceGroupManagerResizeRequests.insert_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeInstanceGroupManagerResizeRequestsInsertArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Path parameter: `instanceGroupManager`.
    pub instance_group_manager: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: InstanceGroupManagerResizeRequest,
}

/// Arguments for [`compute.instanceGroupManagerResizeRequests.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeInstanceGroupManagerResizeRequestsListArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Path parameter: `instanceGroupManager`.
    pub instance_group_manager: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `returnPartialSuccess`.
    pub return_partial_success: Option<String>,
}

/// Arguments for [`compute.instanceGroupManagers.abandonInstances_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeInstanceGroupManagersAbandonInstancesArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Path parameter: `instanceGroupManager`.
    pub instance_group_manager: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: InstanceGroupManagersAbandonInstancesRequest,
}

/// Arguments for [`compute.instanceGroupManagers.aggregatedList_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeInstanceGroupManagersAggregatedListArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `includeAllScopes`.
    pub include_all_scopes: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `returnPartialSuccess`.
    pub return_partial_success: Option<String>,
    /// Query parameter: `serviceProjectNumber`.
    pub service_project_number: Option<String>,
}

/// Arguments for [`compute.instanceGroupManagers.applyUpdatesToInstances_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeInstanceGroupManagersApplyUpdatesToInstancesArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Path parameter: `instanceGroupManager`.
    pub instance_group_manager: String,
    /// Request body.
    pub body: InstanceGroupManagersApplyUpdatesRequest,
}

/// Arguments for [`compute.instanceGroupManagers.createInstances_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeInstanceGroupManagersCreateInstancesArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Path parameter: `instanceGroupManager`.
    pub instance_group_manager: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: InstanceGroupManagersCreateInstancesRequest,
}

/// Arguments for [`compute.instanceGroupManagers.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeInstanceGroupManagersDeleteArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Path parameter: `instanceGroupManager`.
    pub instance_group_manager: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
}

/// Arguments for [`compute.instanceGroupManagers.deleteInstances_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeInstanceGroupManagersDeleteInstancesArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Path parameter: `instanceGroupManager`.
    pub instance_group_manager: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: InstanceGroupManagersDeleteInstancesRequest,
}

/// Arguments for [`compute.instanceGroupManagers.deletePerInstanceConfigs_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeInstanceGroupManagersDeletePerInstanceConfigsArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Path parameter: `instanceGroupManager`.
    pub instance_group_manager: String,
    /// Request body.
    pub body: InstanceGroupManagersDeletePerInstanceConfigsReq,
}

/// Arguments for [`compute.instanceGroupManagers.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeInstanceGroupManagersGetArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Path parameter: `instanceGroupManager`.
    pub instance_group_manager: String,
}

/// Arguments for [`compute.instanceGroupManagers.insert_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeInstanceGroupManagersInsertArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: InstanceGroupManager,
}

/// Arguments for [`compute.instanceGroupManagers.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeInstanceGroupManagersListArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `returnPartialSuccess`.
    pub return_partial_success: Option<String>,
}

/// Arguments for [`compute.instanceGroupManagers.listErrors_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeInstanceGroupManagersListErrorsArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Path parameter: `instanceGroupManager`.
    pub instance_group_manager: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `returnPartialSuccess`.
    pub return_partial_success: Option<String>,
}

/// Arguments for [`compute.instanceGroupManagers.listManagedInstances_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeInstanceGroupManagersListManagedInstancesArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Path parameter: `instanceGroupManager`.
    pub instance_group_manager: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `returnPartialSuccess`.
    pub return_partial_success: Option<String>,
}

/// Arguments for [`compute.instanceGroupManagers.listPerInstanceConfigs_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeInstanceGroupManagersListPerInstanceConfigsArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Path parameter: `instanceGroupManager`.
    pub instance_group_manager: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `returnPartialSuccess`.
    pub return_partial_success: Option<String>,
}

/// Arguments for [`compute.instanceGroupManagers.patch_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeInstanceGroupManagersPatchArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Path parameter: `instanceGroupManager`.
    pub instance_group_manager: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: InstanceGroupManager,
}

/// Arguments for [`compute.instanceGroupManagers.patchPerInstanceConfigs_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeInstanceGroupManagersPatchPerInstanceConfigsArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Path parameter: `instanceGroupManager`.
    pub instance_group_manager: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: InstanceGroupManagersPatchPerInstanceConfigsReq,
}

/// Arguments for [`compute.instanceGroupManagers.recreateInstances_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeInstanceGroupManagersRecreateInstancesArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Path parameter: `instanceGroupManager`.
    pub instance_group_manager: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: InstanceGroupManagersRecreateInstancesRequest,
}

/// Arguments for [`compute.instanceGroupManagers.resize_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeInstanceGroupManagersResizeArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Path parameter: `instanceGroupManager`.
    pub instance_group_manager: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Query parameter: `size`.
    pub size: Option<String>,
}

/// Arguments for [`compute.instanceGroupManagers.resumeInstances_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeInstanceGroupManagersResumeInstancesArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Path parameter: `instanceGroupManager`.
    pub instance_group_manager: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: InstanceGroupManagersResumeInstancesRequest,
}

/// Arguments for [`compute.instanceGroupManagers.setInstanceTemplate_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeInstanceGroupManagersSetInstanceTemplateArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Path parameter: `instanceGroupManager`.
    pub instance_group_manager: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: InstanceGroupManagersSetInstanceTemplateRequest,
}

/// Arguments for [`compute.instanceGroupManagers.setTargetPools_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeInstanceGroupManagersSetTargetPoolsArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Path parameter: `instanceGroupManager`.
    pub instance_group_manager: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: InstanceGroupManagersSetTargetPoolsRequest,
}

/// Arguments for [`compute.instanceGroupManagers.startInstances_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeInstanceGroupManagersStartInstancesArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Path parameter: `instanceGroupManager`.
    pub instance_group_manager: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: InstanceGroupManagersStartInstancesRequest,
}

/// Arguments for [`compute.instanceGroupManagers.stopInstances_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeInstanceGroupManagersStopInstancesArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Path parameter: `instanceGroupManager`.
    pub instance_group_manager: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: InstanceGroupManagersStopInstancesRequest,
}

/// Arguments for [`compute.instanceGroupManagers.suspendInstances_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeInstanceGroupManagersSuspendInstancesArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Path parameter: `instanceGroupManager`.
    pub instance_group_manager: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: InstanceGroupManagersSuspendInstancesRequest,
}

/// Arguments for [`compute.instanceGroupManagers.updatePerInstanceConfigs_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeInstanceGroupManagersUpdatePerInstanceConfigsArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Path parameter: `instanceGroupManager`.
    pub instance_group_manager: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: InstanceGroupManagersUpdatePerInstanceConfigsReq,
}

/// Arguments for [`compute.instanceGroups.addInstances_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeInstanceGroupsAddInstancesArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Path parameter: `instanceGroup`.
    pub instance_group: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: InstanceGroupsAddInstancesRequest,
}

/// Arguments for [`compute.instanceGroups.aggregatedList_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeInstanceGroupsAggregatedListArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `includeAllScopes`.
    pub include_all_scopes: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `returnPartialSuccess`.
    pub return_partial_success: Option<String>,
    /// Query parameter: `serviceProjectNumber`.
    pub service_project_number: Option<String>,
}

/// Arguments for [`compute.instanceGroups.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeInstanceGroupsDeleteArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Path parameter: `instanceGroup`.
    pub instance_group: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
}

/// Arguments for [`compute.instanceGroups.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeInstanceGroupsGetArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Path parameter: `instanceGroup`.
    pub instance_group: String,
}

/// Arguments for [`compute.instanceGroups.insert_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeInstanceGroupsInsertArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: InstanceGroup,
}

/// Arguments for [`compute.instanceGroups.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeInstanceGroupsListArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `returnPartialSuccess`.
    pub return_partial_success: Option<String>,
}

/// Arguments for [`compute.instanceGroups.listInstances_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeInstanceGroupsListInstancesArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Path parameter: `instanceGroup`.
    pub instance_group: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `returnPartialSuccess`.
    pub return_partial_success: Option<String>,
    /// Request body.
    pub body: InstanceGroupsListInstancesRequest,
}

/// Arguments for [`compute.instanceGroups.removeInstances_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeInstanceGroupsRemoveInstancesArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Path parameter: `instanceGroup`.
    pub instance_group: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: InstanceGroupsRemoveInstancesRequest,
}

/// Arguments for [`compute.instanceGroups.setNamedPorts_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeInstanceGroupsSetNamedPortsArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Path parameter: `instanceGroup`.
    pub instance_group: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: InstanceGroupsSetNamedPortsRequest,
}

/// Arguments for [`compute.instanceGroups.testIamPermissions_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeInstanceGroupsTestIamPermissionsArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Path parameter: `resource`.
    pub resource: String,
    /// Request body.
    pub body: TestPermissionsRequest,
}

/// Arguments for [`compute.instanceSettings.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeInstanceSettingsGetArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `zone`.
    pub zone: String,
}

/// Arguments for [`compute.instanceSettings.patch_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeInstanceSettingsPatchArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Query parameter: `updateMask`.
    pub update_mask: Option<String>,
    /// Request body.
    pub body: InstanceSettings,
}

/// Arguments for [`compute.instanceTemplates.aggregatedList_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeInstanceTemplatesAggregatedListArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `includeAllScopes`.
    pub include_all_scopes: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `returnPartialSuccess`.
    pub return_partial_success: Option<String>,
    /// Query parameter: `serviceProjectNumber`.
    pub service_project_number: Option<String>,
}

/// Arguments for [`compute.instanceTemplates.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeInstanceTemplatesDeleteArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `instanceTemplate`.
    pub instance_template: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
}

/// Arguments for [`compute.instanceTemplates.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeInstanceTemplatesGetArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `instanceTemplate`.
    pub instance_template: String,
}

/// Arguments for [`compute.instanceTemplates.getIamPolicy_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeInstanceTemplatesGetIamPolicyArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `resource`.
    pub resource: String,
    /// Query parameter: `optionsRequestedPolicyVersion`.
    pub options_requested_policy_version: Option<String>,
}

/// Arguments for [`compute.instanceTemplates.insert_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeInstanceTemplatesInsertArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: InstanceTemplate,
}

/// Arguments for [`compute.instanceTemplates.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeInstanceTemplatesListArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `returnPartialSuccess`.
    pub return_partial_success: Option<String>,
}

/// Arguments for [`compute.instanceTemplates.setIamPolicy_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeInstanceTemplatesSetIamPolicyArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `resource`.
    pub resource: String,
    /// Request body.
    pub body: GlobalSetPolicyRequest,
}

/// Arguments for [`compute.instanceTemplates.testIamPermissions_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeInstanceTemplatesTestIamPermissionsArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `resource`.
    pub resource: String,
    /// Request body.
    pub body: TestPermissionsRequest,
}

/// Arguments for [`compute.instances.addAccessConfig_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeInstancesAddAccessConfigArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Path parameter: `instance`.
    pub instance: String,
    /// Query parameter: `networkInterface`.
    pub network_interface: Option<String>,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: AccessConfig,
}

/// Arguments for [`compute.instances.addNetworkInterface_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeInstancesAddNetworkInterfaceArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Path parameter: `instance`.
    pub instance: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: NetworkInterface,
}

/// Arguments for [`compute.instances.addResourcePolicies_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeInstancesAddResourcePoliciesArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Path parameter: `instance`.
    pub instance: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: InstancesAddResourcePoliciesRequest,
}

/// Arguments for [`compute.instances.aggregatedList_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeInstancesAggregatedListArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `includeAllScopes`.
    pub include_all_scopes: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `returnPartialSuccess`.
    pub return_partial_success: Option<String>,
    /// Query parameter: `serviceProjectNumber`.
    pub service_project_number: Option<String>,
}

/// Arguments for [`compute.instances.attachDisk_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeInstancesAttachDiskArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Path parameter: `instance`.
    pub instance: String,
    /// Query parameter: `forceAttach`.
    pub force_attach: Option<String>,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: AttachedDisk,
}

/// Arguments for [`compute.instances.bulkInsert_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeInstancesBulkInsertArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: BulkInsertInstanceResource,
}

/// Arguments for [`compute.instances.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeInstancesDeleteArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Path parameter: `instance`.
    pub instance: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
}

/// Arguments for [`compute.instances.deleteAccessConfig_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeInstancesDeleteAccessConfigArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Path parameter: `instance`.
    pub instance: String,
    /// Query parameter: `accessConfig`.
    pub access_config: Option<String>,
    /// Query parameter: `networkInterface`.
    pub network_interface: Option<String>,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
}

/// Arguments for [`compute.instances.deleteNetworkInterface_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeInstancesDeleteNetworkInterfaceArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Path parameter: `instance`.
    pub instance: String,
    /// Query parameter: `networkInterfaceName`.
    pub network_interface_name: Option<String>,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
}

/// Arguments for [`compute.instances.detachDisk_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeInstancesDetachDiskArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Path parameter: `instance`.
    pub instance: String,
    /// Query parameter: `deviceName`.
    pub device_name: Option<String>,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
}

/// Arguments for [`compute.instances.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeInstancesGetArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Path parameter: `instance`.
    pub instance: String,
}

/// Arguments for [`compute.instances.getEffectiveFirewalls_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeInstancesGetEffectiveFirewallsArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Path parameter: `instance`.
    pub instance: String,
    /// Query parameter: `networkInterface`.
    pub network_interface: Option<String>,
}

/// Arguments for [`compute.instances.getGuestAttributes_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeInstancesGetGuestAttributesArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Path parameter: `instance`.
    pub instance: String,
    /// Query parameter: `queryPath`.
    pub query_path: Option<String>,
    /// Query parameter: `variableKey`.
    pub variable_key: Option<String>,
}

/// Arguments for [`compute.instances.getIamPolicy_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeInstancesGetIamPolicyArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Path parameter: `resource`.
    pub resource: String,
    /// Query parameter: `optionsRequestedPolicyVersion`.
    pub options_requested_policy_version: Option<String>,
}

/// Arguments for [`compute.instances.getScreenshot_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeInstancesGetScreenshotArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Path parameter: `instance`.
    pub instance: String,
}

/// Arguments for [`compute.instances.getSerialPortOutput_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeInstancesGetSerialPortOutputArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Path parameter: `instance`.
    pub instance: String,
    /// Query parameter: `port`.
    pub port: Option<String>,
    /// Query parameter: `start`.
    pub start: Option<String>,
}

/// Arguments for [`compute.instances.getShieldedInstanceIdentity_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeInstancesGetShieldedInstanceIdentityArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Path parameter: `instance`.
    pub instance: String,
}

/// Arguments for [`compute.instances.insert_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeInstancesInsertArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Query parameter: `sourceInstanceTemplate`.
    pub source_instance_template: Option<String>,
    /// Query parameter: `sourceMachineImage`.
    pub source_machine_image: Option<String>,
    /// Request body.
    pub body: Instance,
}

/// Arguments for [`compute.instances.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeInstancesListArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `returnPartialSuccess`.
    pub return_partial_success: Option<String>,
}

/// Arguments for [`compute.instances.listReferrers_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeInstancesListReferrersArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Path parameter: `instance`.
    pub instance: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `returnPartialSuccess`.
    pub return_partial_success: Option<String>,
}

/// Arguments for [`compute.instances.performMaintenance_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeInstancesPerformMaintenanceArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Path parameter: `instance`.
    pub instance: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
}

/// Arguments for [`compute.instances.removeResourcePolicies_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeInstancesRemoveResourcePoliciesArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Path parameter: `instance`.
    pub instance: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: InstancesRemoveResourcePoliciesRequest,
}

/// Arguments for [`compute.instances.reportHostAsFaulty_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeInstancesReportHostAsFaultyArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Path parameter: `instance`.
    pub instance: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: InstancesReportHostAsFaultyRequest,
}

/// Arguments for [`compute.instances.reset_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeInstancesResetArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Path parameter: `instance`.
    pub instance: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
}

/// Arguments for [`compute.instances.resume_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeInstancesResumeArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Path parameter: `instance`.
    pub instance: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
}

/// Arguments for [`compute.instances.sendDiagnosticInterrupt_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeInstancesSendDiagnosticInterruptArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Path parameter: `instance`.
    pub instance: String,
}

/// Arguments for [`compute.instances.setDeletionProtection_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeInstancesSetDeletionProtectionArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Path parameter: `resource`.
    pub resource: String,
    /// Query parameter: `deletionProtection`.
    pub deletion_protection: Option<String>,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
}

/// Arguments for [`compute.instances.setDiskAutoDelete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeInstancesSetDiskAutoDeleteArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Path parameter: `instance`.
    pub instance: String,
    /// Query parameter: `autoDelete`.
    pub auto_delete: Option<String>,
    /// Query parameter: `deviceName`.
    pub device_name: Option<String>,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
}

/// Arguments for [`compute.instances.setIamPolicy_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeInstancesSetIamPolicyArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Path parameter: `resource`.
    pub resource: String,
    /// Request body.
    pub body: ZoneSetPolicyRequest,
}

/// Arguments for [`compute.instances.setLabels_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeInstancesSetLabelsArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Path parameter: `instance`.
    pub instance: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: InstancesSetLabelsRequest,
}

/// Arguments for [`compute.instances.setMachineResources_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeInstancesSetMachineResourcesArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Path parameter: `instance`.
    pub instance: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: InstancesSetMachineResourcesRequest,
}

/// Arguments for [`compute.instances.setMachineType_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeInstancesSetMachineTypeArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Path parameter: `instance`.
    pub instance: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: InstancesSetMachineTypeRequest,
}

/// Arguments for [`compute.instances.setMetadata_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeInstancesSetMetadataArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Path parameter: `instance`.
    pub instance: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: Metadata,
}

/// Arguments for [`compute.instances.setMinCpuPlatform_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeInstancesSetMinCpuPlatformArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Path parameter: `instance`.
    pub instance: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: InstancesSetMinCpuPlatformRequest,
}

/// Arguments for [`compute.instances.setName_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeInstancesSetNameArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Path parameter: `instance`.
    pub instance: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: InstancesSetNameRequest,
}

/// Arguments for [`compute.instances.setScheduling_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeInstancesSetSchedulingArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Path parameter: `instance`.
    pub instance: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: Scheduling,
}

/// Arguments for [`compute.instances.setSecurityPolicy_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeInstancesSetSecurityPolicyArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Path parameter: `instance`.
    pub instance: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: InstancesSetSecurityPolicyRequest,
}

/// Arguments for [`compute.instances.setServiceAccount_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeInstancesSetServiceAccountArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Path parameter: `instance`.
    pub instance: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: InstancesSetServiceAccountRequest,
}

/// Arguments for [`compute.instances.setShieldedInstanceIntegrityPolicy_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeInstancesSetShieldedInstanceIntegrityPolicyArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Path parameter: `instance`.
    pub instance: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: ShieldedInstanceIntegrityPolicy,
}

/// Arguments for [`compute.instances.setTags_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeInstancesSetTagsArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Path parameter: `instance`.
    pub instance: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: Tags,
}

/// Arguments for [`compute.instances.simulateMaintenanceEvent_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeInstancesSimulateMaintenanceEventArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Path parameter: `instance`.
    pub instance: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Query parameter: `withExtendedNotifications`.
    pub with_extended_notifications: Option<String>,
}

/// Arguments for [`compute.instances.start_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeInstancesStartArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Path parameter: `instance`.
    pub instance: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
}

/// Arguments for [`compute.instances.startWithEncryptionKey_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeInstancesStartWithEncryptionKeyArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Path parameter: `instance`.
    pub instance: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: InstancesStartWithEncryptionKeyRequest,
}

/// Arguments for [`compute.instances.stop_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeInstancesStopArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Path parameter: `instance`.
    pub instance: String,
    /// Query parameter: `discardLocalSsd`.
    pub discard_local_ssd: Option<String>,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
}

/// Arguments for [`compute.instances.suspend_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeInstancesSuspendArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Path parameter: `instance`.
    pub instance: String,
    /// Query parameter: `discardLocalSsd`.
    pub discard_local_ssd: Option<String>,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
}

/// Arguments for [`compute.instances.testIamPermissions_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeInstancesTestIamPermissionsArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Path parameter: `resource`.
    pub resource: String,
    /// Request body.
    pub body: TestPermissionsRequest,
}

/// Arguments for [`compute.instances.update_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeInstancesUpdateArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Path parameter: `instance`.
    pub instance: String,
    /// Query parameter: `discardLocalSsd`.
    pub discard_local_ssd: Option<String>,
    /// Query parameter: `minimalAction`.
    pub minimal_action: Option<String>,
    /// Query parameter: `mostDisruptiveAllowedAction`.
    pub most_disruptive_allowed_action: Option<String>,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: Instance,
}

/// Arguments for [`compute.instances.updateAccessConfig_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeInstancesUpdateAccessConfigArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Path parameter: `instance`.
    pub instance: String,
    /// Query parameter: `networkInterface`.
    pub network_interface: Option<String>,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: AccessConfig,
}

/// Arguments for [`compute.instances.updateDisplayDevice_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeInstancesUpdateDisplayDeviceArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Path parameter: `instance`.
    pub instance: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: DisplayDevice,
}

/// Arguments for [`compute.instances.updateNetworkInterface_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeInstancesUpdateNetworkInterfaceArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Path parameter: `instance`.
    pub instance: String,
    /// Query parameter: `networkInterface`.
    pub network_interface: Option<String>,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: NetworkInterface,
}

/// Arguments for [`compute.instances.updateShieldedInstanceConfig_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeInstancesUpdateShieldedInstanceConfigArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Path parameter: `instance`.
    pub instance: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: ShieldedInstanceConfig,
}

/// Arguments for [`compute.instantSnapshotGroups.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeInstantSnapshotGroupsDeleteArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Path parameter: `instantSnapshotGroup`.
    pub instant_snapshot_group: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
}

/// Arguments for [`compute.instantSnapshotGroups.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeInstantSnapshotGroupsGetArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Path parameter: `instantSnapshotGroup`.
    pub instant_snapshot_group: String,
}

/// Arguments for [`compute.instantSnapshotGroups.getIamPolicy_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeInstantSnapshotGroupsGetIamPolicyArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Path parameter: `resource`.
    pub resource: String,
    /// Query parameter: `optionsRequestedPolicyVersion`.
    pub options_requested_policy_version: Option<String>,
}

/// Arguments for [`compute.instantSnapshotGroups.insert_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeInstantSnapshotGroupsInsertArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Query parameter: `sourceConsistencyGroup`.
    pub source_consistency_group: Option<String>,
    /// Request body.
    pub body: InstantSnapshotGroup,
}

/// Arguments for [`compute.instantSnapshotGroups.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeInstantSnapshotGroupsListArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `returnPartialSuccess`.
    pub return_partial_success: Option<String>,
}

/// Arguments for [`compute.instantSnapshotGroups.setIamPolicy_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeInstantSnapshotGroupsSetIamPolicyArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Path parameter: `resource`.
    pub resource: String,
    /// Request body.
    pub body: ZoneSetPolicyRequest,
}

/// Arguments for [`compute.instantSnapshotGroups.testIamPermissions_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeInstantSnapshotGroupsTestIamPermissionsArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Path parameter: `resource`.
    pub resource: String,
    /// Request body.
    pub body: TestPermissionsRequest,
}

/// Arguments for [`compute.instantSnapshots.aggregatedList_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeInstantSnapshotsAggregatedListArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `includeAllScopes`.
    pub include_all_scopes: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `returnPartialSuccess`.
    pub return_partial_success: Option<String>,
    /// Query parameter: `serviceProjectNumber`.
    pub service_project_number: Option<String>,
}

/// Arguments for [`compute.instantSnapshots.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeInstantSnapshotsDeleteArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Path parameter: `instantSnapshot`.
    pub instant_snapshot: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
}

/// Arguments for [`compute.instantSnapshots.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeInstantSnapshotsGetArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Path parameter: `instantSnapshot`.
    pub instant_snapshot: String,
}

/// Arguments for [`compute.instantSnapshots.getIamPolicy_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeInstantSnapshotsGetIamPolicyArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Path parameter: `resource`.
    pub resource: String,
    /// Query parameter: `optionsRequestedPolicyVersion`.
    pub options_requested_policy_version: Option<String>,
}

/// Arguments for [`compute.instantSnapshots.insert_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeInstantSnapshotsInsertArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: InstantSnapshot,
}

/// Arguments for [`compute.instantSnapshots.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeInstantSnapshotsListArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `returnPartialSuccess`.
    pub return_partial_success: Option<String>,
}

/// Arguments for [`compute.instantSnapshots.setIamPolicy_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeInstantSnapshotsSetIamPolicyArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Path parameter: `resource`.
    pub resource: String,
    /// Request body.
    pub body: ZoneSetPolicyRequest,
}

/// Arguments for [`compute.instantSnapshots.setLabels_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeInstantSnapshotsSetLabelsArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Path parameter: `resource`.
    pub resource: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: ZoneSetLabelsRequest,
}

/// Arguments for [`compute.instantSnapshots.testIamPermissions_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeInstantSnapshotsTestIamPermissionsArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Path parameter: `resource`.
    pub resource: String,
    /// Request body.
    pub body: TestPermissionsRequest,
}

/// Arguments for [`compute.interconnectAttachmentGroups.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeInterconnectAttachmentGroupsDeleteArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `interconnectAttachmentGroup`.
    pub interconnect_attachment_group: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
}

/// Arguments for [`compute.interconnectAttachmentGroups.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeInterconnectAttachmentGroupsGetArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `interconnectAttachmentGroup`.
    pub interconnect_attachment_group: String,
}

/// Arguments for [`compute.interconnectAttachmentGroups.getIamPolicy_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeInterconnectAttachmentGroupsGetIamPolicyArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `resource`.
    pub resource: String,
    /// Query parameter: `optionsRequestedPolicyVersion`.
    pub options_requested_policy_version: Option<String>,
}

/// Arguments for [`compute.interconnectAttachmentGroups.getOperationalStatus_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeInterconnectAttachmentGroupsGetOperationalStatusArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `interconnectAttachmentGroup`.
    pub interconnect_attachment_group: String,
}

/// Arguments for [`compute.interconnectAttachmentGroups.insert_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeInterconnectAttachmentGroupsInsertArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: InterconnectAttachmentGroup,
}

/// Arguments for [`compute.interconnectAttachmentGroups.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeInterconnectAttachmentGroupsListArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `returnPartialSuccess`.
    pub return_partial_success: Option<String>,
}

/// Arguments for [`compute.interconnectAttachmentGroups.patch_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeInterconnectAttachmentGroupsPatchArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `interconnectAttachmentGroup`.
    pub interconnect_attachment_group: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Query parameter: `updateMask`.
    pub update_mask: Option<String>,
    /// Request body.
    pub body: InterconnectAttachmentGroup,
}

/// Arguments for [`compute.interconnectAttachmentGroups.setIamPolicy_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeInterconnectAttachmentGroupsSetIamPolicyArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `resource`.
    pub resource: String,
    /// Request body.
    pub body: GlobalSetPolicyRequest,
}

/// Arguments for [`compute.interconnectAttachmentGroups.testIamPermissions_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeInterconnectAttachmentGroupsTestIamPermissionsArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `resource`.
    pub resource: String,
    /// Request body.
    pub body: TestPermissionsRequest,
}

/// Arguments for [`compute.interconnectAttachments.aggregatedList_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeInterconnectAttachmentsAggregatedListArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `includeAllScopes`.
    pub include_all_scopes: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `returnPartialSuccess`.
    pub return_partial_success: Option<String>,
    /// Query parameter: `serviceProjectNumber`.
    pub service_project_number: Option<String>,
}

/// Arguments for [`compute.interconnectAttachments.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeInterconnectAttachmentsDeleteArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `interconnectAttachment`.
    pub interconnect_attachment: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
}

/// Arguments for [`compute.interconnectAttachments.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeInterconnectAttachmentsGetArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `interconnectAttachment`.
    pub interconnect_attachment: String,
}

/// Arguments for [`compute.interconnectAttachments.insert_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeInterconnectAttachmentsInsertArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Query parameter: `validateOnly`.
    pub validate_only: Option<String>,
    /// Request body.
    pub body: InterconnectAttachment,
}

/// Arguments for [`compute.interconnectAttachments.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeInterconnectAttachmentsListArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `returnPartialSuccess`.
    pub return_partial_success: Option<String>,
}

/// Arguments for [`compute.interconnectAttachments.patch_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeInterconnectAttachmentsPatchArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `interconnectAttachment`.
    pub interconnect_attachment: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: InterconnectAttachment,
}

/// Arguments for [`compute.interconnectAttachments.setLabels_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeInterconnectAttachmentsSetLabelsArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `resource`.
    pub resource: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: RegionSetLabelsRequest,
}

/// Arguments for [`compute.interconnectGroups.createMembers_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeInterconnectGroupsCreateMembersArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `interconnectGroup`.
    pub interconnect_group: String,
    /// Request body.
    pub body: InterconnectGroupsCreateMembersRequest,
}

/// Arguments for [`compute.interconnectGroups.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeInterconnectGroupsDeleteArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `interconnectGroup`.
    pub interconnect_group: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
}

/// Arguments for [`compute.interconnectGroups.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeInterconnectGroupsGetArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `interconnectGroup`.
    pub interconnect_group: String,
}

/// Arguments for [`compute.interconnectGroups.getIamPolicy_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeInterconnectGroupsGetIamPolicyArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `resource`.
    pub resource: String,
    /// Query parameter: `optionsRequestedPolicyVersion`.
    pub options_requested_policy_version: Option<String>,
}

/// Arguments for [`compute.interconnectGroups.getOperationalStatus_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeInterconnectGroupsGetOperationalStatusArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `interconnectGroup`.
    pub interconnect_group: String,
}

/// Arguments for [`compute.interconnectGroups.insert_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeInterconnectGroupsInsertArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: InterconnectGroup,
}

/// Arguments for [`compute.interconnectGroups.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeInterconnectGroupsListArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `returnPartialSuccess`.
    pub return_partial_success: Option<String>,
}

/// Arguments for [`compute.interconnectGroups.patch_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeInterconnectGroupsPatchArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `interconnectGroup`.
    pub interconnect_group: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Query parameter: `updateMask`.
    pub update_mask: Option<String>,
    /// Request body.
    pub body: InterconnectGroup,
}

/// Arguments for [`compute.interconnectGroups.setIamPolicy_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeInterconnectGroupsSetIamPolicyArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `resource`.
    pub resource: String,
    /// Request body.
    pub body: GlobalSetPolicyRequest,
}

/// Arguments for [`compute.interconnectGroups.testIamPermissions_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeInterconnectGroupsTestIamPermissionsArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `resource`.
    pub resource: String,
    /// Request body.
    pub body: TestPermissionsRequest,
}

/// Arguments for [`compute.interconnectLocations.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeInterconnectLocationsGetArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `interconnectLocation`.
    pub interconnect_location: String,
}

/// Arguments for [`compute.interconnectLocations.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeInterconnectLocationsListArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `returnPartialSuccess`.
    pub return_partial_success: Option<String>,
}

/// Arguments for [`compute.interconnectRemoteLocations.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeInterconnectRemoteLocationsGetArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `interconnectRemoteLocation`.
    pub interconnect_remote_location: String,
}

/// Arguments for [`compute.interconnectRemoteLocations.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeInterconnectRemoteLocationsListArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `returnPartialSuccess`.
    pub return_partial_success: Option<String>,
}

/// Arguments for [`compute.interconnects.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeInterconnectsDeleteArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `interconnect`.
    pub interconnect: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
}

/// Arguments for [`compute.interconnects.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeInterconnectsGetArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `interconnect`.
    pub interconnect: String,
}

/// Arguments for [`compute.interconnects.getDiagnostics_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeInterconnectsGetDiagnosticsArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `interconnect`.
    pub interconnect: String,
}

/// Arguments for [`compute.interconnects.getMacsecConfig_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeInterconnectsGetMacsecConfigArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `interconnect`.
    pub interconnect: String,
}

/// Arguments for [`compute.interconnects.insert_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeInterconnectsInsertArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: Interconnect,
}

/// Arguments for [`compute.interconnects.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeInterconnectsListArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `returnPartialSuccess`.
    pub return_partial_success: Option<String>,
}

/// Arguments for [`compute.interconnects.patch_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeInterconnectsPatchArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `interconnect`.
    pub interconnect: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: Interconnect,
}

/// Arguments for [`compute.interconnects.setLabels_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeInterconnectsSetLabelsArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `resource`.
    pub resource: String,
    /// Request body.
    pub body: GlobalSetLabelsRequest,
}

/// Arguments for [`compute.licenseCodes.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeLicenseCodesGetArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `licenseCode`.
    pub license_code: String,
}

/// Arguments for [`compute.licenseCodes.testIamPermissions_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeLicenseCodesTestIamPermissionsArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `resource`.
    pub resource: String,
    /// Request body.
    pub body: TestPermissionsRequest,
}

/// Arguments for [`compute.licenses.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeLicensesDeleteArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `license`.
    pub license: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
}

/// Arguments for [`compute.licenses.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeLicensesGetArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `license`.
    pub license: String,
}

/// Arguments for [`compute.licenses.getIamPolicy_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeLicensesGetIamPolicyArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `resource`.
    pub resource: String,
    /// Query parameter: `optionsRequestedPolicyVersion`.
    pub options_requested_policy_version: Option<String>,
}

/// Arguments for [`compute.licenses.insert_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeLicensesInsertArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: License,
}

/// Arguments for [`compute.licenses.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeLicensesListArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `returnPartialSuccess`.
    pub return_partial_success: Option<String>,
}

/// Arguments for [`compute.licenses.setIamPolicy_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeLicensesSetIamPolicyArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `resource`.
    pub resource: String,
    /// Request body.
    pub body: GlobalSetPolicyRequest,
}

/// Arguments for [`compute.licenses.testIamPermissions_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeLicensesTestIamPermissionsArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `resource`.
    pub resource: String,
    /// Request body.
    pub body: TestPermissionsRequest,
}

/// Arguments for [`compute.licenses.update_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeLicensesUpdateArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `license`.
    pub license: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Query parameter: `updateMask`.
    pub update_mask: Option<String>,
    /// Request body.
    pub body: License,
}

/// Arguments for [`compute.machineImages.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeMachineImagesDeleteArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `machineImage`.
    pub machine_image: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
}

/// Arguments for [`compute.machineImages.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeMachineImagesGetArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `machineImage`.
    pub machine_image: String,
}

/// Arguments for [`compute.machineImages.getIamPolicy_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeMachineImagesGetIamPolicyArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `resource`.
    pub resource: String,
    /// Query parameter: `optionsRequestedPolicyVersion`.
    pub options_requested_policy_version: Option<String>,
}

/// Arguments for [`compute.machineImages.insert_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeMachineImagesInsertArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Query parameter: `sourceInstance`.
    pub source_instance: Option<String>,
    /// Request body.
    pub body: MachineImage,
}

/// Arguments for [`compute.machineImages.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeMachineImagesListArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `returnPartialSuccess`.
    pub return_partial_success: Option<String>,
}

/// Arguments for [`compute.machineImages.setIamPolicy_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeMachineImagesSetIamPolicyArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `resource`.
    pub resource: String,
    /// Request body.
    pub body: GlobalSetPolicyRequest,
}

/// Arguments for [`compute.machineImages.setLabels_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeMachineImagesSetLabelsArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `resource`.
    pub resource: String,
    /// Request body.
    pub body: GlobalSetLabelsRequest,
}

/// Arguments for [`compute.machineImages.testIamPermissions_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeMachineImagesTestIamPermissionsArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `resource`.
    pub resource: String,
    /// Request body.
    pub body: TestPermissionsRequest,
}

/// Arguments for [`compute.machineTypes.aggregatedList_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeMachineTypesAggregatedListArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `includeAllScopes`.
    pub include_all_scopes: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `returnPartialSuccess`.
    pub return_partial_success: Option<String>,
    /// Query parameter: `serviceProjectNumber`.
    pub service_project_number: Option<String>,
}

/// Arguments for [`compute.machineTypes.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeMachineTypesGetArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Path parameter: `machineType`.
    pub machine_type: String,
}

/// Arguments for [`compute.machineTypes.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeMachineTypesListArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `returnPartialSuccess`.
    pub return_partial_success: Option<String>,
}

/// Arguments for [`compute.networkAttachments.aggregatedList_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeNetworkAttachmentsAggregatedListArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `includeAllScopes`.
    pub include_all_scopes: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `returnPartialSuccess`.
    pub return_partial_success: Option<String>,
    /// Query parameter: `serviceProjectNumber`.
    pub service_project_number: Option<String>,
}

/// Arguments for [`compute.networkAttachments.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeNetworkAttachmentsDeleteArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `networkAttachment`.
    pub network_attachment: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
}

/// Arguments for [`compute.networkAttachments.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeNetworkAttachmentsGetArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `networkAttachment`.
    pub network_attachment: String,
}

/// Arguments for [`compute.networkAttachments.getIamPolicy_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeNetworkAttachmentsGetIamPolicyArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `resource`.
    pub resource: String,
    /// Query parameter: `optionsRequestedPolicyVersion`.
    pub options_requested_policy_version: Option<String>,
}

/// Arguments for [`compute.networkAttachments.insert_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeNetworkAttachmentsInsertArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: NetworkAttachment,
}

/// Arguments for [`compute.networkAttachments.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeNetworkAttachmentsListArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `returnPartialSuccess`.
    pub return_partial_success: Option<String>,
}

/// Arguments for [`compute.networkAttachments.patch_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeNetworkAttachmentsPatchArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `networkAttachment`.
    pub network_attachment: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: NetworkAttachment,
}

/// Arguments for [`compute.networkAttachments.setIamPolicy_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeNetworkAttachmentsSetIamPolicyArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `resource`.
    pub resource: String,
    /// Request body.
    pub body: RegionSetPolicyRequest,
}

/// Arguments for [`compute.networkAttachments.testIamPermissions_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeNetworkAttachmentsTestIamPermissionsArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `resource`.
    pub resource: String,
    /// Request body.
    pub body: TestPermissionsRequest,
}

/// Arguments for [`compute.networkEdgeSecurityServices.aggregatedList_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeNetworkEdgeSecurityServicesAggregatedListArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `includeAllScopes`.
    pub include_all_scopes: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `returnPartialSuccess`.
    pub return_partial_success: Option<String>,
    /// Query parameter: `serviceProjectNumber`.
    pub service_project_number: Option<String>,
}

/// Arguments for [`compute.networkEdgeSecurityServices.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeNetworkEdgeSecurityServicesDeleteArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `networkEdgeSecurityService`.
    pub network_edge_security_service: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
}

/// Arguments for [`compute.networkEdgeSecurityServices.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeNetworkEdgeSecurityServicesGetArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `networkEdgeSecurityService`.
    pub network_edge_security_service: String,
}

/// Arguments for [`compute.networkEdgeSecurityServices.insert_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeNetworkEdgeSecurityServicesInsertArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Query parameter: `validateOnly`.
    pub validate_only: Option<String>,
    /// Request body.
    pub body: NetworkEdgeSecurityService,
}

/// Arguments for [`compute.networkEdgeSecurityServices.patch_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeNetworkEdgeSecurityServicesPatchArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `networkEdgeSecurityService`.
    pub network_edge_security_service: String,
    /// Query parameter: `paths`.
    pub paths: Option<String>,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Query parameter: `updateMask`.
    pub update_mask: Option<String>,
    /// Request body.
    pub body: NetworkEdgeSecurityService,
}

/// Arguments for [`compute.networkEndpointGroups.aggregatedList_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeNetworkEndpointGroupsAggregatedListArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `includeAllScopes`.
    pub include_all_scopes: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `returnPartialSuccess`.
    pub return_partial_success: Option<String>,
    /// Query parameter: `serviceProjectNumber`.
    pub service_project_number: Option<String>,
}

/// Arguments for [`compute.networkEndpointGroups.attachNetworkEndpoints_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeNetworkEndpointGroupsAttachNetworkEndpointsArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Path parameter: `networkEndpointGroup`.
    pub network_endpoint_group: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: NetworkEndpointGroupsAttachEndpointsRequest,
}

/// Arguments for [`compute.networkEndpointGroups.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeNetworkEndpointGroupsDeleteArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Path parameter: `networkEndpointGroup`.
    pub network_endpoint_group: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
}

/// Arguments for [`compute.networkEndpointGroups.detachNetworkEndpoints_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeNetworkEndpointGroupsDetachNetworkEndpointsArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Path parameter: `networkEndpointGroup`.
    pub network_endpoint_group: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: NetworkEndpointGroupsDetachEndpointsRequest,
}

/// Arguments for [`compute.networkEndpointGroups.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeNetworkEndpointGroupsGetArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Path parameter: `networkEndpointGroup`.
    pub network_endpoint_group: String,
}

/// Arguments for [`compute.networkEndpointGroups.insert_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeNetworkEndpointGroupsInsertArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: NetworkEndpointGroup,
}

/// Arguments for [`compute.networkEndpointGroups.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeNetworkEndpointGroupsListArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `returnPartialSuccess`.
    pub return_partial_success: Option<String>,
}

/// Arguments for [`compute.networkEndpointGroups.listNetworkEndpoints_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeNetworkEndpointGroupsListNetworkEndpointsArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Path parameter: `networkEndpointGroup`.
    pub network_endpoint_group: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `returnPartialSuccess`.
    pub return_partial_success: Option<String>,
    /// Request body.
    pub body: NetworkEndpointGroupsListEndpointsRequest,
}

/// Arguments for [`compute.networkEndpointGroups.testIamPermissions_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeNetworkEndpointGroupsTestIamPermissionsArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Path parameter: `resource`.
    pub resource: String,
    /// Request body.
    pub body: TestPermissionsRequest,
}

/// Arguments for [`compute.networkFirewallPolicies.addAssociation_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeNetworkFirewallPoliciesAddAssociationArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `firewallPolicy`.
    pub firewall_policy: String,
    /// Query parameter: `replaceExistingAssociation`.
    pub replace_existing_association: Option<String>,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: FirewallPolicyAssociation,
}

/// Arguments for [`compute.networkFirewallPolicies.addPacketMirroringRule_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeNetworkFirewallPoliciesAddPacketMirroringRuleArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `firewallPolicy`.
    pub firewall_policy: String,
    /// Query parameter: `maxPriority`.
    pub max_priority: Option<String>,
    /// Query parameter: `minPriority`.
    pub min_priority: Option<String>,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: FirewallPolicyRule,
}

/// Arguments for [`compute.networkFirewallPolicies.addRule_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeNetworkFirewallPoliciesAddRuleArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `firewallPolicy`.
    pub firewall_policy: String,
    /// Query parameter: `maxPriority`.
    pub max_priority: Option<String>,
    /// Query parameter: `minPriority`.
    pub min_priority: Option<String>,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: FirewallPolicyRule,
}

/// Arguments for [`compute.networkFirewallPolicies.aggregatedList_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeNetworkFirewallPoliciesAggregatedListArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `includeAllScopes`.
    pub include_all_scopes: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `returnPartialSuccess`.
    pub return_partial_success: Option<String>,
    /// Query parameter: `serviceProjectNumber`.
    pub service_project_number: Option<String>,
}

/// Arguments for [`compute.networkFirewallPolicies.cloneRules_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeNetworkFirewallPoliciesCloneRulesArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `firewallPolicy`.
    pub firewall_policy: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Query parameter: `sourceFirewallPolicy`.
    pub source_firewall_policy: Option<String>,
}

/// Arguments for [`compute.networkFirewallPolicies.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeNetworkFirewallPoliciesDeleteArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `firewallPolicy`.
    pub firewall_policy: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
}

/// Arguments for [`compute.networkFirewallPolicies.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeNetworkFirewallPoliciesGetArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `firewallPolicy`.
    pub firewall_policy: String,
}

/// Arguments for [`compute.networkFirewallPolicies.getAssociation_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeNetworkFirewallPoliciesGetAssociationArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `firewallPolicy`.
    pub firewall_policy: String,
    /// Query parameter: `name`.
    pub name: Option<String>,
}

/// Arguments for [`compute.networkFirewallPolicies.getIamPolicy_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeNetworkFirewallPoliciesGetIamPolicyArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `resource`.
    pub resource: String,
    /// Query parameter: `optionsRequestedPolicyVersion`.
    pub options_requested_policy_version: Option<String>,
}

/// Arguments for [`compute.networkFirewallPolicies.getPacketMirroringRule_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeNetworkFirewallPoliciesGetPacketMirroringRuleArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `firewallPolicy`.
    pub firewall_policy: String,
    /// Query parameter: `priority`.
    pub priority: Option<String>,
}

/// Arguments for [`compute.networkFirewallPolicies.getRule_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeNetworkFirewallPoliciesGetRuleArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `firewallPolicy`.
    pub firewall_policy: String,
    /// Query parameter: `priority`.
    pub priority: Option<String>,
}

/// Arguments for [`compute.networkFirewallPolicies.insert_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeNetworkFirewallPoliciesInsertArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: FirewallPolicy,
}

/// Arguments for [`compute.networkFirewallPolicies.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeNetworkFirewallPoliciesListArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `returnPartialSuccess`.
    pub return_partial_success: Option<String>,
}

/// Arguments for [`compute.networkFirewallPolicies.patch_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeNetworkFirewallPoliciesPatchArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `firewallPolicy`.
    pub firewall_policy: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: FirewallPolicy,
}

/// Arguments for [`compute.networkFirewallPolicies.patchPacketMirroringRule_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeNetworkFirewallPoliciesPatchPacketMirroringRuleArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `firewallPolicy`.
    pub firewall_policy: String,
    /// Query parameter: `priority`.
    pub priority: Option<String>,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: FirewallPolicyRule,
}

/// Arguments for [`compute.networkFirewallPolicies.patchRule_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeNetworkFirewallPoliciesPatchRuleArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `firewallPolicy`.
    pub firewall_policy: String,
    /// Query parameter: `priority`.
    pub priority: Option<String>,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: FirewallPolicyRule,
}

/// Arguments for [`compute.networkFirewallPolicies.removeAssociation_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeNetworkFirewallPoliciesRemoveAssociationArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `firewallPolicy`.
    pub firewall_policy: String,
    /// Query parameter: `name`.
    pub name: Option<String>,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
}

/// Arguments for [`compute.networkFirewallPolicies.removePacketMirroringRule_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeNetworkFirewallPoliciesRemovePacketMirroringRuleArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `firewallPolicy`.
    pub firewall_policy: String,
    /// Query parameter: `priority`.
    pub priority: Option<String>,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
}

/// Arguments for [`compute.networkFirewallPolicies.removeRule_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeNetworkFirewallPoliciesRemoveRuleArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `firewallPolicy`.
    pub firewall_policy: String,
    /// Query parameter: `priority`.
    pub priority: Option<String>,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
}

/// Arguments for [`compute.networkFirewallPolicies.setIamPolicy_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeNetworkFirewallPoliciesSetIamPolicyArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `resource`.
    pub resource: String,
    /// Request body.
    pub body: GlobalSetPolicyRequest,
}

/// Arguments for [`compute.networkFirewallPolicies.testIamPermissions_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeNetworkFirewallPoliciesTestIamPermissionsArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `resource`.
    pub resource: String,
    /// Request body.
    pub body: TestPermissionsRequest,
}

/// Arguments for [`compute.networkProfiles.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeNetworkProfilesGetArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `networkProfile`.
    pub network_profile: String,
}

/// Arguments for [`compute.networkProfiles.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeNetworkProfilesListArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `returnPartialSuccess`.
    pub return_partial_success: Option<String>,
}

/// Arguments for [`compute.networks.addPeering_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeNetworksAddPeeringArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `network`.
    pub network: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: NetworksAddPeeringRequest,
}

/// Arguments for [`compute.networks.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeNetworksDeleteArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `network`.
    pub network: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
}

/// Arguments for [`compute.networks.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeNetworksGetArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `network`.
    pub network: String,
}

/// Arguments for [`compute.networks.getEffectiveFirewalls_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeNetworksGetEffectiveFirewallsArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `network`.
    pub network: String,
}

/// Arguments for [`compute.networks.insert_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeNetworksInsertArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: Network,
}

/// Arguments for [`compute.networks.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeNetworksListArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `returnPartialSuccess`.
    pub return_partial_success: Option<String>,
}

/// Arguments for [`compute.networks.listPeeringRoutes_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeNetworksListPeeringRoutesArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `network`.
    pub network: String,
    /// Query parameter: `direction`.
    pub direction: Option<String>,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `peeringName`.
    pub peering_name: Option<String>,
    /// Query parameter: `region`.
    pub region: Option<String>,
    /// Query parameter: `returnPartialSuccess`.
    pub return_partial_success: Option<String>,
}

/// Arguments for [`compute.networks.patch_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeNetworksPatchArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `network`.
    pub network: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: Network,
}

/// Arguments for [`compute.networks.removePeering_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeNetworksRemovePeeringArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `network`.
    pub network: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: NetworksRemovePeeringRequest,
}

/// Arguments for [`compute.networks.requestRemovePeering_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeNetworksRequestRemovePeeringArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `network`.
    pub network: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: NetworksRequestRemovePeeringRequest,
}

/// Arguments for [`compute.networks.switchToCustomMode_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeNetworksSwitchToCustomModeArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `network`.
    pub network: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
}

/// Arguments for [`compute.networks.updatePeering_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeNetworksUpdatePeeringArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `network`.
    pub network: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: NetworksUpdatePeeringRequest,
}

/// Arguments for [`compute.nodeGroups.addNodes_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeNodeGroupsAddNodesArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Path parameter: `nodeGroup`.
    pub node_group: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: NodeGroupsAddNodesRequest,
}

/// Arguments for [`compute.nodeGroups.aggregatedList_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeNodeGroupsAggregatedListArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `includeAllScopes`.
    pub include_all_scopes: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `returnPartialSuccess`.
    pub return_partial_success: Option<String>,
    /// Query parameter: `serviceProjectNumber`.
    pub service_project_number: Option<String>,
}

/// Arguments for [`compute.nodeGroups.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeNodeGroupsDeleteArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Path parameter: `nodeGroup`.
    pub node_group: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
}

/// Arguments for [`compute.nodeGroups.deleteNodes_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeNodeGroupsDeleteNodesArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Path parameter: `nodeGroup`.
    pub node_group: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: NodeGroupsDeleteNodesRequest,
}

/// Arguments for [`compute.nodeGroups.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeNodeGroupsGetArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Path parameter: `nodeGroup`.
    pub node_group: String,
}

/// Arguments for [`compute.nodeGroups.getIamPolicy_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeNodeGroupsGetIamPolicyArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Path parameter: `resource`.
    pub resource: String,
    /// Query parameter: `optionsRequestedPolicyVersion`.
    pub options_requested_policy_version: Option<String>,
}

/// Arguments for [`compute.nodeGroups.insert_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeNodeGroupsInsertArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Query parameter: `initialNodeCount`.
    pub initial_node_count: Option<String>,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: NodeGroup,
}

/// Arguments for [`compute.nodeGroups.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeNodeGroupsListArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `returnPartialSuccess`.
    pub return_partial_success: Option<String>,
}

/// Arguments for [`compute.nodeGroups.listNodes_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeNodeGroupsListNodesArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Path parameter: `nodeGroup`.
    pub node_group: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `returnPartialSuccess`.
    pub return_partial_success: Option<String>,
}

/// Arguments for [`compute.nodeGroups.patch_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeNodeGroupsPatchArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Path parameter: `nodeGroup`.
    pub node_group: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: NodeGroup,
}

/// Arguments for [`compute.nodeGroups.performMaintenance_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeNodeGroupsPerformMaintenanceArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Path parameter: `nodeGroup`.
    pub node_group: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: NodeGroupsPerformMaintenanceRequest,
}

/// Arguments for [`compute.nodeGroups.setIamPolicy_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeNodeGroupsSetIamPolicyArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Path parameter: `resource`.
    pub resource: String,
    /// Request body.
    pub body: ZoneSetPolicyRequest,
}

/// Arguments for [`compute.nodeGroups.setNodeTemplate_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeNodeGroupsSetNodeTemplateArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Path parameter: `nodeGroup`.
    pub node_group: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: NodeGroupsSetNodeTemplateRequest,
}

/// Arguments for [`compute.nodeGroups.simulateMaintenanceEvent_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeNodeGroupsSimulateMaintenanceEventArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Path parameter: `nodeGroup`.
    pub node_group: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: NodeGroupsSimulateMaintenanceEventRequest,
}

/// Arguments for [`compute.nodeGroups.testIamPermissions_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeNodeGroupsTestIamPermissionsArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Path parameter: `resource`.
    pub resource: String,
    /// Request body.
    pub body: TestPermissionsRequest,
}

/// Arguments for [`compute.nodeTemplates.aggregatedList_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeNodeTemplatesAggregatedListArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `includeAllScopes`.
    pub include_all_scopes: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `returnPartialSuccess`.
    pub return_partial_success: Option<String>,
    /// Query parameter: `serviceProjectNumber`.
    pub service_project_number: Option<String>,
}

/// Arguments for [`compute.nodeTemplates.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeNodeTemplatesDeleteArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `nodeTemplate`.
    pub node_template: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
}

/// Arguments for [`compute.nodeTemplates.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeNodeTemplatesGetArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `nodeTemplate`.
    pub node_template: String,
}

/// Arguments for [`compute.nodeTemplates.getIamPolicy_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeNodeTemplatesGetIamPolicyArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `resource`.
    pub resource: String,
    /// Query parameter: `optionsRequestedPolicyVersion`.
    pub options_requested_policy_version: Option<String>,
}

/// Arguments for [`compute.nodeTemplates.insert_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeNodeTemplatesInsertArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: NodeTemplate,
}

/// Arguments for [`compute.nodeTemplates.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeNodeTemplatesListArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `returnPartialSuccess`.
    pub return_partial_success: Option<String>,
}

/// Arguments for [`compute.nodeTemplates.setIamPolicy_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeNodeTemplatesSetIamPolicyArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `resource`.
    pub resource: String,
    /// Request body.
    pub body: RegionSetPolicyRequest,
}

/// Arguments for [`compute.nodeTemplates.testIamPermissions_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeNodeTemplatesTestIamPermissionsArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `resource`.
    pub resource: String,
    /// Request body.
    pub body: TestPermissionsRequest,
}

/// Arguments for [`compute.nodeTypes.aggregatedList_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeNodeTypesAggregatedListArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `includeAllScopes`.
    pub include_all_scopes: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `returnPartialSuccess`.
    pub return_partial_success: Option<String>,
    /// Query parameter: `serviceProjectNumber`.
    pub service_project_number: Option<String>,
}

/// Arguments for [`compute.nodeTypes.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeNodeTypesGetArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Path parameter: `nodeType`.
    pub node_type: String,
}

/// Arguments for [`compute.nodeTypes.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeNodeTypesListArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `returnPartialSuccess`.
    pub return_partial_success: Option<String>,
}

/// Arguments for [`compute.organizationSecurityPolicies.addAssociation_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeOrganizationSecurityPoliciesAddAssociationArgs {
    /// Path parameter: `securityPolicy`.
    pub security_policy: String,
    /// Query parameter: `replaceExistingAssociation`.
    pub replace_existing_association: Option<String>,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: SecurityPolicyAssociation,
}

/// Arguments for [`compute.organizationSecurityPolicies.addRule_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeOrganizationSecurityPoliciesAddRuleArgs {
    /// Path parameter: `securityPolicy`.
    pub security_policy: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: SecurityPolicyRule,
}

/// Arguments for [`compute.organizationSecurityPolicies.copyRules_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeOrganizationSecurityPoliciesCopyRulesArgs {
    /// Path parameter: `securityPolicy`.
    pub security_policy: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Query parameter: `sourceSecurityPolicy`.
    pub source_security_policy: Option<String>,
}

/// Arguments for [`compute.organizationSecurityPolicies.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeOrganizationSecurityPoliciesDeleteArgs {
    /// Path parameter: `securityPolicy`.
    pub security_policy: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
}

/// Arguments for [`compute.organizationSecurityPolicies.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeOrganizationSecurityPoliciesGetArgs {
    /// Path parameter: `securityPolicy`.
    pub security_policy: String,
}

/// Arguments for [`compute.organizationSecurityPolicies.getAssociation_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeOrganizationSecurityPoliciesGetAssociationArgs {
    /// Path parameter: `securityPolicy`.
    pub security_policy: String,
    /// Query parameter: `name`.
    pub name: Option<String>,
}

/// Arguments for [`compute.organizationSecurityPolicies.getRule_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeOrganizationSecurityPoliciesGetRuleArgs {
    /// Path parameter: `securityPolicy`.
    pub security_policy: String,
    /// Query parameter: `priority`.
    pub priority: Option<String>,
}

/// Arguments for [`compute.organizationSecurityPolicies.insert_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeOrganizationSecurityPoliciesInsertArgs {
    /// Query parameter: `parentId`.
    pub parent_id: Option<String>,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: SecurityPolicy,
}

/// Arguments for [`compute.organizationSecurityPolicies.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeOrganizationSecurityPoliciesListArgs {
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `parentId`.
    pub parent_id: Option<String>,
    /// Query parameter: `returnPartialSuccess`.
    pub return_partial_success: Option<String>,
}

/// Arguments for [`compute.organizationSecurityPolicies.listAssociations_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeOrganizationSecurityPoliciesListAssociationsArgs {
    /// Query parameter: `targetResource`.
    pub target_resource: Option<String>,
}

/// Arguments for [`compute.organizationSecurityPolicies.listPreconfiguredExpressionSets_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeOrganizationSecurityPoliciesListPreconfiguredExpressionSetsArgs {
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `parentId`.
    pub parent_id: Option<String>,
    /// Query parameter: `returnPartialSuccess`.
    pub return_partial_success: Option<String>,
}

/// Arguments for [`compute.organizationSecurityPolicies.move_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeOrganizationSecurityPoliciesMoveArgs {
    /// Path parameter: `securityPolicy`.
    pub security_policy: String,
    /// Query parameter: `parentId`.
    pub parent_id: Option<String>,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
}

/// Arguments for [`compute.organizationSecurityPolicies.patch_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeOrganizationSecurityPoliciesPatchArgs {
    /// Path parameter: `securityPolicy`.
    pub security_policy: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: SecurityPolicy,
}

/// Arguments for [`compute.organizationSecurityPolicies.patchRule_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeOrganizationSecurityPoliciesPatchRuleArgs {
    /// Path parameter: `securityPolicy`.
    pub security_policy: String,
    /// Query parameter: `priority`.
    pub priority: Option<String>,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: SecurityPolicyRule,
}

/// Arguments for [`compute.organizationSecurityPolicies.removeAssociation_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeOrganizationSecurityPoliciesRemoveAssociationArgs {
    /// Path parameter: `securityPolicy`.
    pub security_policy: String,
    /// Query parameter: `name`.
    pub name: Option<String>,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
}

/// Arguments for [`compute.organizationSecurityPolicies.removeRule_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeOrganizationSecurityPoliciesRemoveRuleArgs {
    /// Path parameter: `securityPolicy`.
    pub security_policy: String,
    /// Query parameter: `priority`.
    pub priority: Option<String>,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
}

/// Arguments for [`compute.packetMirrorings.aggregatedList_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputePacketMirroringsAggregatedListArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `includeAllScopes`.
    pub include_all_scopes: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `returnPartialSuccess`.
    pub return_partial_success: Option<String>,
    /// Query parameter: `serviceProjectNumber`.
    pub service_project_number: Option<String>,
}

/// Arguments for [`compute.packetMirrorings.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputePacketMirroringsDeleteArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `packetMirroring`.
    pub packet_mirroring: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
}

/// Arguments for [`compute.packetMirrorings.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputePacketMirroringsGetArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `packetMirroring`.
    pub packet_mirroring: String,
}

/// Arguments for [`compute.packetMirrorings.insert_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputePacketMirroringsInsertArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: PacketMirroring,
}

/// Arguments for [`compute.packetMirrorings.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputePacketMirroringsListArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `returnPartialSuccess`.
    pub return_partial_success: Option<String>,
}

/// Arguments for [`compute.packetMirrorings.patch_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputePacketMirroringsPatchArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `packetMirroring`.
    pub packet_mirroring: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: PacketMirroring,
}

/// Arguments for [`compute.packetMirrorings.testIamPermissions_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputePacketMirroringsTestIamPermissionsArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `resource`.
    pub resource: String,
    /// Request body.
    pub body: TestPermissionsRequest,
}

/// Arguments for [`compute.previewFeatures.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputePreviewFeaturesGetArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `previewFeature`.
    pub preview_feature: String,
}

/// Arguments for [`compute.previewFeatures.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputePreviewFeaturesListArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `returnPartialSuccess`.
    pub return_partial_success: Option<String>,
}

/// Arguments for [`compute.previewFeatures.update_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputePreviewFeaturesUpdateArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `previewFeature`.
    pub preview_feature: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: PreviewFeature,
}

/// Arguments for [`compute.projects.disableXpnHost_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeProjectsDisableXpnHostArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
}

/// Arguments for [`compute.projects.disableXpnResource_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeProjectsDisableXpnResourceArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: ProjectsDisableXpnResourceRequest,
}

/// Arguments for [`compute.projects.enableXpnHost_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeProjectsEnableXpnHostArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
}

/// Arguments for [`compute.projects.enableXpnResource_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeProjectsEnableXpnResourceArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: ProjectsEnableXpnResourceRequest,
}

/// Arguments for [`compute.projects.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeProjectsGetArgs {
    /// Path parameter: `project`.
    pub project: String,
}

/// Arguments for [`compute.projects.getXpnHost_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeProjectsGetXpnHostArgs {
    /// Path parameter: `project`.
    pub project: String,
}

/// Arguments for [`compute.projects.getXpnResources_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeProjectsGetXpnResourcesArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `returnPartialSuccess`.
    pub return_partial_success: Option<String>,
}

/// Arguments for [`compute.projects.listXpnHosts_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeProjectsListXpnHostsArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `returnPartialSuccess`.
    pub return_partial_success: Option<String>,
    /// Request body.
    pub body: ProjectsListXpnHostsRequest,
}

/// Arguments for [`compute.projects.moveDisk_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeProjectsMoveDiskArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: DiskMoveRequest,
}

/// Arguments for [`compute.projects.moveInstance_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeProjectsMoveInstanceArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: InstanceMoveRequest,
}

/// Arguments for [`compute.projects.setCloudArmorTier_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeProjectsSetCloudArmorTierArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: ProjectsSetCloudArmorTierRequest,
}

/// Arguments for [`compute.projects.setCommonInstanceMetadata_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeProjectsSetCommonInstanceMetadataArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: Metadata,
}

/// Arguments for [`compute.projects.setDefaultNetworkTier_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeProjectsSetDefaultNetworkTierArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: ProjectsSetDefaultNetworkTierRequest,
}

/// Arguments for [`compute.projects.setUsageExportBucket_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeProjectsSetUsageExportBucketArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: UsageExportLocation,
}

/// Arguments for [`compute.publicAdvertisedPrefixes.announce_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputePublicAdvertisedPrefixesAnnounceArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `publicAdvertisedPrefix`.
    pub public_advertised_prefix: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
}

/// Arguments for [`compute.publicAdvertisedPrefixes.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputePublicAdvertisedPrefixesDeleteArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `publicAdvertisedPrefix`.
    pub public_advertised_prefix: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
}

/// Arguments for [`compute.publicAdvertisedPrefixes.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputePublicAdvertisedPrefixesGetArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `publicAdvertisedPrefix`.
    pub public_advertised_prefix: String,
}

/// Arguments for [`compute.publicAdvertisedPrefixes.insert_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputePublicAdvertisedPrefixesInsertArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: PublicAdvertisedPrefix,
}

/// Arguments for [`compute.publicAdvertisedPrefixes.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputePublicAdvertisedPrefixesListArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `returnPartialSuccess`.
    pub return_partial_success: Option<String>,
}

/// Arguments for [`compute.publicAdvertisedPrefixes.patch_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputePublicAdvertisedPrefixesPatchArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `publicAdvertisedPrefix`.
    pub public_advertised_prefix: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: PublicAdvertisedPrefix,
}

/// Arguments for [`compute.publicAdvertisedPrefixes.withdraw_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputePublicAdvertisedPrefixesWithdrawArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `publicAdvertisedPrefix`.
    pub public_advertised_prefix: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
}

/// Arguments for [`compute.publicDelegatedPrefixes.aggregatedList_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputePublicDelegatedPrefixesAggregatedListArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `includeAllScopes`.
    pub include_all_scopes: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `returnPartialSuccess`.
    pub return_partial_success: Option<String>,
    /// Query parameter: `serviceProjectNumber`.
    pub service_project_number: Option<String>,
}

/// Arguments for [`compute.publicDelegatedPrefixes.announce_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputePublicDelegatedPrefixesAnnounceArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `publicDelegatedPrefix`.
    pub public_delegated_prefix: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
}

/// Arguments for [`compute.publicDelegatedPrefixes.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputePublicDelegatedPrefixesDeleteArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `publicDelegatedPrefix`.
    pub public_delegated_prefix: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
}

/// Arguments for [`compute.publicDelegatedPrefixes.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputePublicDelegatedPrefixesGetArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `publicDelegatedPrefix`.
    pub public_delegated_prefix: String,
}

/// Arguments for [`compute.publicDelegatedPrefixes.insert_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputePublicDelegatedPrefixesInsertArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: PublicDelegatedPrefix,
}

/// Arguments for [`compute.publicDelegatedPrefixes.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputePublicDelegatedPrefixesListArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `returnPartialSuccess`.
    pub return_partial_success: Option<String>,
}

/// Arguments for [`compute.publicDelegatedPrefixes.patch_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputePublicDelegatedPrefixesPatchArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `publicDelegatedPrefix`.
    pub public_delegated_prefix: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: PublicDelegatedPrefix,
}

/// Arguments for [`compute.publicDelegatedPrefixes.withdraw_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputePublicDelegatedPrefixesWithdrawArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `publicDelegatedPrefix`.
    pub public_delegated_prefix: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
}

/// Arguments for [`compute.regionAutoscalers.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionAutoscalersDeleteArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `autoscaler`.
    pub autoscaler: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
}

/// Arguments for [`compute.regionAutoscalers.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionAutoscalersGetArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `autoscaler`.
    pub autoscaler: String,
}

/// Arguments for [`compute.regionAutoscalers.insert_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionAutoscalersInsertArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: Autoscaler,
}

/// Arguments for [`compute.regionAutoscalers.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionAutoscalersListArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `returnPartialSuccess`.
    pub return_partial_success: Option<String>,
}

/// Arguments for [`compute.regionAutoscalers.patch_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionAutoscalersPatchArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Query parameter: `autoscaler`.
    pub autoscaler: Option<String>,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: Autoscaler,
}

/// Arguments for [`compute.regionAutoscalers.testIamPermissions_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionAutoscalersTestIamPermissionsArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `resource`.
    pub resource: String,
    /// Request body.
    pub body: TestPermissionsRequest,
}

/// Arguments for [`compute.regionAutoscalers.update_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionAutoscalersUpdateArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Query parameter: `autoscaler`.
    pub autoscaler: Option<String>,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: Autoscaler,
}

/// Arguments for [`compute.regionBackendBuckets.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionBackendBucketsDeleteArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `backendBucket`.
    pub backend_bucket: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
}

/// Arguments for [`compute.regionBackendBuckets.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionBackendBucketsGetArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `backendBucket`.
    pub backend_bucket: String,
}

/// Arguments for [`compute.regionBackendBuckets.getIamPolicy_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionBackendBucketsGetIamPolicyArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `resource`.
    pub resource: String,
    /// Query parameter: `optionsRequestedPolicyVersion`.
    pub options_requested_policy_version: Option<String>,
}

/// Arguments for [`compute.regionBackendBuckets.insert_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionBackendBucketsInsertArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: BackendBucket,
}

/// Arguments for [`compute.regionBackendBuckets.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionBackendBucketsListArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `returnPartialSuccess`.
    pub return_partial_success: Option<String>,
}

/// Arguments for [`compute.regionBackendBuckets.listUsable_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionBackendBucketsListUsableArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `returnPartialSuccess`.
    pub return_partial_success: Option<String>,
}

/// Arguments for [`compute.regionBackendBuckets.patch_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionBackendBucketsPatchArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `backendBucket`.
    pub backend_bucket: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: BackendBucket,
}

/// Arguments for [`compute.regionBackendBuckets.setIamPolicy_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionBackendBucketsSetIamPolicyArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `resource`.
    pub resource: String,
    /// Request body.
    pub body: RegionSetPolicyRequest,
}

/// Arguments for [`compute.regionBackendBuckets.testIamPermissions_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionBackendBucketsTestIamPermissionsArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `resource`.
    pub resource: String,
    /// Request body.
    pub body: TestPermissionsRequest,
}

/// Arguments for [`compute.regionBackendServices.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionBackendServicesDeleteArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `backendService`.
    pub backend_service: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
}

/// Arguments for [`compute.regionBackendServices.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionBackendServicesGetArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `backendService`.
    pub backend_service: String,
}

/// Arguments for [`compute.regionBackendServices.getHealth_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionBackendServicesGetHealthArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `backendService`.
    pub backend_service: String,
    /// Request body.
    pub body: ResourceGroupReference,
}

/// Arguments for [`compute.regionBackendServices.getIamPolicy_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionBackendServicesGetIamPolicyArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `resource`.
    pub resource: String,
    /// Query parameter: `optionsRequestedPolicyVersion`.
    pub options_requested_policy_version: Option<String>,
}

/// Arguments for [`compute.regionBackendServices.insert_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionBackendServicesInsertArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: BackendService,
}

/// Arguments for [`compute.regionBackendServices.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionBackendServicesListArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `returnPartialSuccess`.
    pub return_partial_success: Option<String>,
}

/// Arguments for [`compute.regionBackendServices.listUsable_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionBackendServicesListUsableArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `returnPartialSuccess`.
    pub return_partial_success: Option<String>,
}

/// Arguments for [`compute.regionBackendServices.patch_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionBackendServicesPatchArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `backendService`.
    pub backend_service: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: BackendService,
}

/// Arguments for [`compute.regionBackendServices.setIamPolicy_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionBackendServicesSetIamPolicyArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `resource`.
    pub resource: String,
    /// Request body.
    pub body: RegionSetPolicyRequest,
}

/// Arguments for [`compute.regionBackendServices.setSecurityPolicy_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionBackendServicesSetSecurityPolicyArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `backendService`.
    pub backend_service: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: SecurityPolicyReference,
}

/// Arguments for [`compute.regionBackendServices.testIamPermissions_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionBackendServicesTestIamPermissionsArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `resource`.
    pub resource: String,
    /// Request body.
    pub body: TestPermissionsRequest,
}

/// Arguments for [`compute.regionBackendServices.update_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionBackendServicesUpdateArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `backendService`.
    pub backend_service: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: BackendService,
}

/// Arguments for [`compute.regionCommitments.aggregatedList_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionCommitmentsAggregatedListArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `includeAllScopes`.
    pub include_all_scopes: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `returnPartialSuccess`.
    pub return_partial_success: Option<String>,
    /// Query parameter: `serviceProjectNumber`.
    pub service_project_number: Option<String>,
}

/// Arguments for [`compute.regionCommitments.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionCommitmentsGetArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `commitment`.
    pub commitment: String,
}

/// Arguments for [`compute.regionCommitments.insert_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionCommitmentsInsertArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: Commitment,
}

/// Arguments for [`compute.regionCommitments.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionCommitmentsListArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `returnPartialSuccess`.
    pub return_partial_success: Option<String>,
}

/// Arguments for [`compute.regionCommitments.update_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionCommitmentsUpdateArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `commitment`.
    pub commitment: String,
    /// Query parameter: `paths`.
    pub paths: Option<String>,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Query parameter: `updateMask`.
    pub update_mask: Option<String>,
    /// Request body.
    pub body: Commitment,
}

/// Arguments for [`compute.regionCompositeHealthChecks.aggregatedList_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionCompositeHealthChecksAggregatedListArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `includeAllScopes`.
    pub include_all_scopes: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `returnPartialSuccess`.
    pub return_partial_success: Option<String>,
    /// Query parameter: `serviceProjectNumber`.
    pub service_project_number: Option<String>,
}

/// Arguments for [`compute.regionCompositeHealthChecks.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionCompositeHealthChecksDeleteArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `compositeHealthCheck`.
    pub composite_health_check: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
}

/// Arguments for [`compute.regionCompositeHealthChecks.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionCompositeHealthChecksGetArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `compositeHealthCheck`.
    pub composite_health_check: String,
}

/// Arguments for [`compute.regionCompositeHealthChecks.getHealth_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionCompositeHealthChecksGetHealthArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `compositeHealthCheck`.
    pub composite_health_check: String,
}

/// Arguments for [`compute.regionCompositeHealthChecks.insert_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionCompositeHealthChecksInsertArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: CompositeHealthCheck,
}

/// Arguments for [`compute.regionCompositeHealthChecks.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionCompositeHealthChecksListArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `returnPartialSuccess`.
    pub return_partial_success: Option<String>,
}

/// Arguments for [`compute.regionCompositeHealthChecks.patch_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionCompositeHealthChecksPatchArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `compositeHealthCheck`.
    pub composite_health_check: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: CompositeHealthCheck,
}

/// Arguments for [`compute.regionCompositeHealthChecks.testIamPermissions_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionCompositeHealthChecksTestIamPermissionsArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `resource`.
    pub resource: String,
    /// Request body.
    pub body: TestPermissionsRequest,
}

/// Arguments for [`compute.regionDiskTypes.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionDiskTypesGetArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `diskType`.
    pub disk_type: String,
}

/// Arguments for [`compute.regionDiskTypes.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionDiskTypesListArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `returnPartialSuccess`.
    pub return_partial_success: Option<String>,
}

/// Arguments for [`compute.regionDisks.addResourcePolicies_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionDisksAddResourcePoliciesArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `disk`.
    pub disk: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: RegionDisksAddResourcePoliciesRequest,
}

/// Arguments for [`compute.regionDisks.bulkInsert_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionDisksBulkInsertArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: BulkInsertDiskResource,
}

/// Arguments for [`compute.regionDisks.createSnapshot_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionDisksCreateSnapshotArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `disk`.
    pub disk: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: Snapshot,
}

/// Arguments for [`compute.regionDisks.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionDisksDeleteArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `disk`.
    pub disk: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
}

/// Arguments for [`compute.regionDisks.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionDisksGetArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `disk`.
    pub disk: String,
}

/// Arguments for [`compute.regionDisks.getIamPolicy_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionDisksGetIamPolicyArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `resource`.
    pub resource: String,
    /// Query parameter: `optionsRequestedPolicyVersion`.
    pub options_requested_policy_version: Option<String>,
}

/// Arguments for [`compute.regionDisks.insert_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionDisksInsertArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Query parameter: `sourceImage`.
    pub source_image: Option<String>,
    /// Request body.
    pub body: Disk,
}

/// Arguments for [`compute.regionDisks.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionDisksListArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `returnPartialSuccess`.
    pub return_partial_success: Option<String>,
}

/// Arguments for [`compute.regionDisks.removeResourcePolicies_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionDisksRemoveResourcePoliciesArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `disk`.
    pub disk: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: RegionDisksRemoveResourcePoliciesRequest,
}

/// Arguments for [`compute.regionDisks.resize_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionDisksResizeArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `disk`.
    pub disk: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: RegionDisksResizeRequest,
}

/// Arguments for [`compute.regionDisks.setIamPolicy_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionDisksSetIamPolicyArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `resource`.
    pub resource: String,
    /// Request body.
    pub body: RegionSetPolicyRequest,
}

/// Arguments for [`compute.regionDisks.setLabels_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionDisksSetLabelsArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `resource`.
    pub resource: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: RegionSetLabelsRequest,
}

/// Arguments for [`compute.regionDisks.startAsyncReplication_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionDisksStartAsyncReplicationArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `disk`.
    pub disk: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: RegionDisksStartAsyncReplicationRequest,
}

/// Arguments for [`compute.regionDisks.stopAsyncReplication_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionDisksStopAsyncReplicationArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `disk`.
    pub disk: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
}

/// Arguments for [`compute.regionDisks.stopGroupAsyncReplication_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionDisksStopGroupAsyncReplicationArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: DisksStopGroupAsyncReplicationResource,
}

/// Arguments for [`compute.regionDisks.testIamPermissions_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionDisksTestIamPermissionsArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `resource`.
    pub resource: String,
    /// Request body.
    pub body: TestPermissionsRequest,
}

/// Arguments for [`compute.regionDisks.update_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionDisksUpdateArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `disk`.
    pub disk: String,
    /// Query parameter: `paths`.
    pub paths: Option<String>,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Query parameter: `updateMask`.
    pub update_mask: Option<String>,
    /// Request body.
    pub body: Disk,
}

/// Arguments for [`compute.regionDisks.updateKmsKey_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionDisksUpdateKmsKeyArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `disk`.
    pub disk: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: RegionDiskUpdateKmsKeyRequest,
}

/// Arguments for [`compute.regionHealthAggregationPolicies.aggregatedList_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionHealthAggregationPoliciesAggregatedListArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `includeAllScopes`.
    pub include_all_scopes: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `returnPartialSuccess`.
    pub return_partial_success: Option<String>,
    /// Query parameter: `serviceProjectNumber`.
    pub service_project_number: Option<String>,
}

/// Arguments for [`compute.regionHealthAggregationPolicies.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionHealthAggregationPoliciesDeleteArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `healthAggregationPolicy`.
    pub health_aggregation_policy: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
}

/// Arguments for [`compute.regionHealthAggregationPolicies.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionHealthAggregationPoliciesGetArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `healthAggregationPolicy`.
    pub health_aggregation_policy: String,
}

/// Arguments for [`compute.regionHealthAggregationPolicies.insert_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionHealthAggregationPoliciesInsertArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: HealthAggregationPolicy,
}

/// Arguments for [`compute.regionHealthAggregationPolicies.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionHealthAggregationPoliciesListArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `returnPartialSuccess`.
    pub return_partial_success: Option<String>,
}

/// Arguments for [`compute.regionHealthAggregationPolicies.patch_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionHealthAggregationPoliciesPatchArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `healthAggregationPolicy`.
    pub health_aggregation_policy: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: HealthAggregationPolicy,
}

/// Arguments for [`compute.regionHealthAggregationPolicies.testIamPermissions_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionHealthAggregationPoliciesTestIamPermissionsArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `resource`.
    pub resource: String,
    /// Request body.
    pub body: TestPermissionsRequest,
}

/// Arguments for [`compute.regionHealthCheckServices.aggregatedList_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionHealthCheckServicesAggregatedListArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `includeAllScopes`.
    pub include_all_scopes: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `returnPartialSuccess`.
    pub return_partial_success: Option<String>,
    /// Query parameter: `serviceProjectNumber`.
    pub service_project_number: Option<String>,
}

/// Arguments for [`compute.regionHealthCheckServices.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionHealthCheckServicesDeleteArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `healthCheckService`.
    pub health_check_service: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
}

/// Arguments for [`compute.regionHealthCheckServices.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionHealthCheckServicesGetArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `healthCheckService`.
    pub health_check_service: String,
}

/// Arguments for [`compute.regionHealthCheckServices.insert_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionHealthCheckServicesInsertArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: HealthCheckService,
}

/// Arguments for [`compute.regionHealthCheckServices.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionHealthCheckServicesListArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `returnPartialSuccess`.
    pub return_partial_success: Option<String>,
}

/// Arguments for [`compute.regionHealthCheckServices.patch_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionHealthCheckServicesPatchArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `healthCheckService`.
    pub health_check_service: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: HealthCheckService,
}

/// Arguments for [`compute.regionHealthCheckServices.testIamPermissions_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionHealthCheckServicesTestIamPermissionsArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `resource`.
    pub resource: String,
    /// Request body.
    pub body: TestPermissionsRequest,
}

/// Arguments for [`compute.regionHealthChecks.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionHealthChecksDeleteArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `healthCheck`.
    pub health_check: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
}

/// Arguments for [`compute.regionHealthChecks.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionHealthChecksGetArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `healthCheck`.
    pub health_check: String,
}

/// Arguments for [`compute.regionHealthChecks.insert_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionHealthChecksInsertArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: HealthCheck,
}

/// Arguments for [`compute.regionHealthChecks.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionHealthChecksListArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `returnPartialSuccess`.
    pub return_partial_success: Option<String>,
}

/// Arguments for [`compute.regionHealthChecks.patch_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionHealthChecksPatchArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `healthCheck`.
    pub health_check: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: HealthCheck,
}

/// Arguments for [`compute.regionHealthChecks.testIamPermissions_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionHealthChecksTestIamPermissionsArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `resource`.
    pub resource: String,
    /// Request body.
    pub body: TestPermissionsRequest,
}

/// Arguments for [`compute.regionHealthChecks.update_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionHealthChecksUpdateArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `healthCheck`.
    pub health_check: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: HealthCheck,
}

/// Arguments for [`compute.regionHealthSources.aggregatedList_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionHealthSourcesAggregatedListArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `includeAllScopes`.
    pub include_all_scopes: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `returnPartialSuccess`.
    pub return_partial_success: Option<String>,
    /// Query parameter: `serviceProjectNumber`.
    pub service_project_number: Option<String>,
}

/// Arguments for [`compute.regionHealthSources.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionHealthSourcesDeleteArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `healthSource`.
    pub health_source: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
}

/// Arguments for [`compute.regionHealthSources.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionHealthSourcesGetArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `healthSource`.
    pub health_source: String,
}

/// Arguments for [`compute.regionHealthSources.getHealth_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionHealthSourcesGetHealthArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `healthSource`.
    pub health_source: String,
}

/// Arguments for [`compute.regionHealthSources.insert_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionHealthSourcesInsertArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: HealthSource,
}

/// Arguments for [`compute.regionHealthSources.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionHealthSourcesListArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `returnPartialSuccess`.
    pub return_partial_success: Option<String>,
}

/// Arguments for [`compute.regionHealthSources.patch_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionHealthSourcesPatchArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `healthSource`.
    pub health_source: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: HealthSource,
}

/// Arguments for [`compute.regionHealthSources.testIamPermissions_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionHealthSourcesTestIamPermissionsArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `resource`.
    pub resource: String,
    /// Request body.
    pub body: TestPermissionsRequest,
}

/// Arguments for [`compute.regionInstanceGroupManagerResizeRequests.cancel_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionInstanceGroupManagerResizeRequestsCancelArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `instanceGroupManager`.
    pub instance_group_manager: String,
    /// Path parameter: `resizeRequest`.
    pub resize_request: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
}

/// Arguments for [`compute.regionInstanceGroupManagerResizeRequests.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionInstanceGroupManagerResizeRequestsDeleteArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `instanceGroupManager`.
    pub instance_group_manager: String,
    /// Path parameter: `resizeRequest`.
    pub resize_request: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
}

/// Arguments for [`compute.regionInstanceGroupManagerResizeRequests.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionInstanceGroupManagerResizeRequestsGetArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `instanceGroupManager`.
    pub instance_group_manager: String,
    /// Path parameter: `resizeRequest`.
    pub resize_request: String,
}

/// Arguments for [`compute.regionInstanceGroupManagerResizeRequests.insert_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionInstanceGroupManagerResizeRequestsInsertArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `instanceGroupManager`.
    pub instance_group_manager: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: InstanceGroupManagerResizeRequest,
}

/// Arguments for [`compute.regionInstanceGroupManagerResizeRequests.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionInstanceGroupManagerResizeRequestsListArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `instanceGroupManager`.
    pub instance_group_manager: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `returnPartialSuccess`.
    pub return_partial_success: Option<String>,
}

/// Arguments for [`compute.regionInstanceGroupManagers.abandonInstances_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionInstanceGroupManagersAbandonInstancesArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `instanceGroupManager`.
    pub instance_group_manager: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: RegionInstanceGroupManagersAbandonInstancesRequest,
}

/// Arguments for [`compute.regionInstanceGroupManagers.applyUpdatesToInstances_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionInstanceGroupManagersApplyUpdatesToInstancesArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `instanceGroupManager`.
    pub instance_group_manager: String,
    /// Request body.
    pub body: RegionInstanceGroupManagersApplyUpdatesRequest,
}

/// Arguments for [`compute.regionInstanceGroupManagers.createInstances_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionInstanceGroupManagersCreateInstancesArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `instanceGroupManager`.
    pub instance_group_manager: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: RegionInstanceGroupManagersCreateInstancesRequest,
}

/// Arguments for [`compute.regionInstanceGroupManagers.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionInstanceGroupManagersDeleteArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `instanceGroupManager`.
    pub instance_group_manager: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
}

/// Arguments for [`compute.regionInstanceGroupManagers.deleteInstances_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionInstanceGroupManagersDeleteInstancesArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `instanceGroupManager`.
    pub instance_group_manager: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: RegionInstanceGroupManagersDeleteInstancesRequest,
}

/// Arguments for [`compute.regionInstanceGroupManagers.deletePerInstanceConfigs_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionInstanceGroupManagersDeletePerInstanceConfigsArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `instanceGroupManager`.
    pub instance_group_manager: String,
    /// Request body.
    pub body: RegionInstanceGroupManagerDeleteInstanceConfigReq,
}

/// Arguments for [`compute.regionInstanceGroupManagers.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionInstanceGroupManagersGetArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `instanceGroupManager`.
    pub instance_group_manager: String,
}

/// Arguments for [`compute.regionInstanceGroupManagers.insert_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionInstanceGroupManagersInsertArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: InstanceGroupManager,
}

/// Arguments for [`compute.regionInstanceGroupManagers.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionInstanceGroupManagersListArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `returnPartialSuccess`.
    pub return_partial_success: Option<String>,
}

/// Arguments for [`compute.regionInstanceGroupManagers.listErrors_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionInstanceGroupManagersListErrorsArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `instanceGroupManager`.
    pub instance_group_manager: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `returnPartialSuccess`.
    pub return_partial_success: Option<String>,
}

/// Arguments for [`compute.regionInstanceGroupManagers.listManagedInstances_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionInstanceGroupManagersListManagedInstancesArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `instanceGroupManager`.
    pub instance_group_manager: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `returnPartialSuccess`.
    pub return_partial_success: Option<String>,
}

/// Arguments for [`compute.regionInstanceGroupManagers.listPerInstanceConfigs_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionInstanceGroupManagersListPerInstanceConfigsArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `instanceGroupManager`.
    pub instance_group_manager: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `returnPartialSuccess`.
    pub return_partial_success: Option<String>,
}

/// Arguments for [`compute.regionInstanceGroupManagers.patch_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionInstanceGroupManagersPatchArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `instanceGroupManager`.
    pub instance_group_manager: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: InstanceGroupManager,
}

/// Arguments for [`compute.regionInstanceGroupManagers.patchPerInstanceConfigs_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionInstanceGroupManagersPatchPerInstanceConfigsArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `instanceGroupManager`.
    pub instance_group_manager: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: RegionInstanceGroupManagerPatchInstanceConfigReq,
}

/// Arguments for [`compute.regionInstanceGroupManagers.recreateInstances_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionInstanceGroupManagersRecreateInstancesArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `instanceGroupManager`.
    pub instance_group_manager: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: RegionInstanceGroupManagersRecreateRequest,
}

/// Arguments for [`compute.regionInstanceGroupManagers.resize_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionInstanceGroupManagersResizeArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `instanceGroupManager`.
    pub instance_group_manager: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Query parameter: `size`.
    pub size: Option<String>,
}

/// Arguments for [`compute.regionInstanceGroupManagers.resumeInstances_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionInstanceGroupManagersResumeInstancesArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `instanceGroupManager`.
    pub instance_group_manager: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: RegionInstanceGroupManagersResumeInstancesRequest,
}

/// Arguments for [`compute.regionInstanceGroupManagers.setInstanceTemplate_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionInstanceGroupManagersSetInstanceTemplateArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `instanceGroupManager`.
    pub instance_group_manager: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: RegionInstanceGroupManagersSetTemplateRequest,
}

/// Arguments for [`compute.regionInstanceGroupManagers.setTargetPools_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionInstanceGroupManagersSetTargetPoolsArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `instanceGroupManager`.
    pub instance_group_manager: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: RegionInstanceGroupManagersSetTargetPoolsRequest,
}

/// Arguments for [`compute.regionInstanceGroupManagers.startInstances_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionInstanceGroupManagersStartInstancesArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `instanceGroupManager`.
    pub instance_group_manager: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: RegionInstanceGroupManagersStartInstancesRequest,
}

/// Arguments for [`compute.regionInstanceGroupManagers.stopInstances_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionInstanceGroupManagersStopInstancesArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `instanceGroupManager`.
    pub instance_group_manager: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: RegionInstanceGroupManagersStopInstancesRequest,
}

/// Arguments for [`compute.regionInstanceGroupManagers.suspendInstances_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionInstanceGroupManagersSuspendInstancesArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `instanceGroupManager`.
    pub instance_group_manager: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: RegionInstanceGroupManagersSuspendInstancesRequest,
}

/// Arguments for [`compute.regionInstanceGroupManagers.updatePerInstanceConfigs_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionInstanceGroupManagersUpdatePerInstanceConfigsArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `instanceGroupManager`.
    pub instance_group_manager: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: RegionInstanceGroupManagerUpdateInstanceConfigReq,
}

/// Arguments for [`compute.regionInstanceGroups.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionInstanceGroupsGetArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `instanceGroup`.
    pub instance_group: String,
}

/// Arguments for [`compute.regionInstanceGroups.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionInstanceGroupsListArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `returnPartialSuccess`.
    pub return_partial_success: Option<String>,
}

/// Arguments for [`compute.regionInstanceGroups.listInstances_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionInstanceGroupsListInstancesArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `instanceGroup`.
    pub instance_group: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `returnPartialSuccess`.
    pub return_partial_success: Option<String>,
    /// Request body.
    pub body: RegionInstanceGroupsListInstancesRequest,
}

/// Arguments for [`compute.regionInstanceGroups.setNamedPorts_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionInstanceGroupsSetNamedPortsArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `instanceGroup`.
    pub instance_group: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: RegionInstanceGroupsSetNamedPortsRequest,
}

/// Arguments for [`compute.regionInstanceGroups.testIamPermissions_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionInstanceGroupsTestIamPermissionsArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `resource`.
    pub resource: String,
    /// Request body.
    pub body: TestPermissionsRequest,
}

/// Arguments for [`compute.regionInstanceTemplates.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionInstanceTemplatesDeleteArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `instanceTemplate`.
    pub instance_template: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
}

/// Arguments for [`compute.regionInstanceTemplates.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionInstanceTemplatesGetArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `instanceTemplate`.
    pub instance_template: String,
}

/// Arguments for [`compute.regionInstanceTemplates.insert_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionInstanceTemplatesInsertArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: InstanceTemplate,
}

/// Arguments for [`compute.regionInstanceTemplates.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionInstanceTemplatesListArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `returnPartialSuccess`.
    pub return_partial_success: Option<String>,
}

/// Arguments for [`compute.regionInstances.bulkInsert_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionInstancesBulkInsertArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: BulkInsertInstanceResource,
}

/// Arguments for [`compute.regionInstantSnapshotGroups.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionInstantSnapshotGroupsDeleteArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `instantSnapshotGroup`.
    pub instant_snapshot_group: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
}

/// Arguments for [`compute.regionInstantSnapshotGroups.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionInstantSnapshotGroupsGetArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `instantSnapshotGroup`.
    pub instant_snapshot_group: String,
}

/// Arguments for [`compute.regionInstantSnapshotGroups.getIamPolicy_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionInstantSnapshotGroupsGetIamPolicyArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `resource`.
    pub resource: String,
    /// Query parameter: `optionsRequestedPolicyVersion`.
    pub options_requested_policy_version: Option<String>,
}

/// Arguments for [`compute.regionInstantSnapshotGroups.insert_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionInstantSnapshotGroupsInsertArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Query parameter: `sourceConsistencyGroup`.
    pub source_consistency_group: Option<String>,
    /// Request body.
    pub body: InstantSnapshotGroup,
}

/// Arguments for [`compute.regionInstantSnapshotGroups.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionInstantSnapshotGroupsListArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `returnPartialSuccess`.
    pub return_partial_success: Option<String>,
}

/// Arguments for [`compute.regionInstantSnapshotGroups.setIamPolicy_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionInstantSnapshotGroupsSetIamPolicyArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `resource`.
    pub resource: String,
    /// Request body.
    pub body: RegionSetPolicyRequest,
}

/// Arguments for [`compute.regionInstantSnapshotGroups.testIamPermissions_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionInstantSnapshotGroupsTestIamPermissionsArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `resource`.
    pub resource: String,
    /// Request body.
    pub body: TestPermissionsRequest,
}

/// Arguments for [`compute.regionInstantSnapshots.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionInstantSnapshotsDeleteArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `instantSnapshot`.
    pub instant_snapshot: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
}

/// Arguments for [`compute.regionInstantSnapshots.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionInstantSnapshotsGetArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `instantSnapshot`.
    pub instant_snapshot: String,
}

/// Arguments for [`compute.regionInstantSnapshots.getIamPolicy_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionInstantSnapshotsGetIamPolicyArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `resource`.
    pub resource: String,
    /// Query parameter: `optionsRequestedPolicyVersion`.
    pub options_requested_policy_version: Option<String>,
}

/// Arguments for [`compute.regionInstantSnapshots.insert_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionInstantSnapshotsInsertArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: InstantSnapshot,
}

/// Arguments for [`compute.regionInstantSnapshots.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionInstantSnapshotsListArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `returnPartialSuccess`.
    pub return_partial_success: Option<String>,
}

/// Arguments for [`compute.regionInstantSnapshots.setIamPolicy_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionInstantSnapshotsSetIamPolicyArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `resource`.
    pub resource: String,
    /// Request body.
    pub body: RegionSetPolicyRequest,
}

/// Arguments for [`compute.regionInstantSnapshots.setLabels_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionInstantSnapshotsSetLabelsArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `resource`.
    pub resource: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: RegionSetLabelsRequest,
}

/// Arguments for [`compute.regionInstantSnapshots.testIamPermissions_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionInstantSnapshotsTestIamPermissionsArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `resource`.
    pub resource: String,
    /// Request body.
    pub body: TestPermissionsRequest,
}

/// Arguments for [`compute.regionNetworkEndpointGroups.attachNetworkEndpoints_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionNetworkEndpointGroupsAttachNetworkEndpointsArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `networkEndpointGroup`.
    pub network_endpoint_group: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: RegionNetworkEndpointGroupsAttachEndpointsRequest,
}

/// Arguments for [`compute.regionNetworkEndpointGroups.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionNetworkEndpointGroupsDeleteArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `networkEndpointGroup`.
    pub network_endpoint_group: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
}

/// Arguments for [`compute.regionNetworkEndpointGroups.detachNetworkEndpoints_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionNetworkEndpointGroupsDetachNetworkEndpointsArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `networkEndpointGroup`.
    pub network_endpoint_group: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: RegionNetworkEndpointGroupsDetachEndpointsRequest,
}

/// Arguments for [`compute.regionNetworkEndpointGroups.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionNetworkEndpointGroupsGetArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `networkEndpointGroup`.
    pub network_endpoint_group: String,
}

/// Arguments for [`compute.regionNetworkEndpointGroups.insert_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionNetworkEndpointGroupsInsertArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: NetworkEndpointGroup,
}

/// Arguments for [`compute.regionNetworkEndpointGroups.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionNetworkEndpointGroupsListArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `returnPartialSuccess`.
    pub return_partial_success: Option<String>,
}

/// Arguments for [`compute.regionNetworkEndpointGroups.listNetworkEndpoints_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionNetworkEndpointGroupsListNetworkEndpointsArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `networkEndpointGroup`.
    pub network_endpoint_group: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `returnPartialSuccess`.
    pub return_partial_success: Option<String>,
}

/// Arguments for [`compute.regionNetworkFirewallPolicies.addAssociation_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionNetworkFirewallPoliciesAddAssociationArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `firewallPolicy`.
    pub firewall_policy: String,
    /// Query parameter: `replaceExistingAssociation`.
    pub replace_existing_association: Option<String>,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: FirewallPolicyAssociation,
}

/// Arguments for [`compute.regionNetworkFirewallPolicies.addRule_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionNetworkFirewallPoliciesAddRuleArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `firewallPolicy`.
    pub firewall_policy: String,
    /// Query parameter: `maxPriority`.
    pub max_priority: Option<String>,
    /// Query parameter: `minPriority`.
    pub min_priority: Option<String>,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: FirewallPolicyRule,
}

/// Arguments for [`compute.regionNetworkFirewallPolicies.cloneRules_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionNetworkFirewallPoliciesCloneRulesArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `firewallPolicy`.
    pub firewall_policy: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Query parameter: `sourceFirewallPolicy`.
    pub source_firewall_policy: Option<String>,
}

/// Arguments for [`compute.regionNetworkFirewallPolicies.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionNetworkFirewallPoliciesDeleteArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `firewallPolicy`.
    pub firewall_policy: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
}

/// Arguments for [`compute.regionNetworkFirewallPolicies.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionNetworkFirewallPoliciesGetArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `firewallPolicy`.
    pub firewall_policy: String,
}

/// Arguments for [`compute.regionNetworkFirewallPolicies.getAssociation_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionNetworkFirewallPoliciesGetAssociationArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `firewallPolicy`.
    pub firewall_policy: String,
    /// Query parameter: `name`.
    pub name: Option<String>,
}

/// Arguments for [`compute.regionNetworkFirewallPolicies.getEffectiveFirewalls_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionNetworkFirewallPoliciesGetEffectiveFirewallsArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Query parameter: `network`.
    pub network: Option<String>,
}

/// Arguments for [`compute.regionNetworkFirewallPolicies.getIamPolicy_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionNetworkFirewallPoliciesGetIamPolicyArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `resource`.
    pub resource: String,
    /// Query parameter: `optionsRequestedPolicyVersion`.
    pub options_requested_policy_version: Option<String>,
}

/// Arguments for [`compute.regionNetworkFirewallPolicies.getRule_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionNetworkFirewallPoliciesGetRuleArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `firewallPolicy`.
    pub firewall_policy: String,
    /// Query parameter: `priority`.
    pub priority: Option<String>,
}

/// Arguments for [`compute.regionNetworkFirewallPolicies.insert_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionNetworkFirewallPoliciesInsertArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: FirewallPolicy,
}

/// Arguments for [`compute.regionNetworkFirewallPolicies.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionNetworkFirewallPoliciesListArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `returnPartialSuccess`.
    pub return_partial_success: Option<String>,
}

/// Arguments for [`compute.regionNetworkFirewallPolicies.patch_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionNetworkFirewallPoliciesPatchArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `firewallPolicy`.
    pub firewall_policy: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: FirewallPolicy,
}

/// Arguments for [`compute.regionNetworkFirewallPolicies.patchRule_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionNetworkFirewallPoliciesPatchRuleArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `firewallPolicy`.
    pub firewall_policy: String,
    /// Query parameter: `priority`.
    pub priority: Option<String>,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: FirewallPolicyRule,
}

/// Arguments for [`compute.regionNetworkFirewallPolicies.removeAssociation_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionNetworkFirewallPoliciesRemoveAssociationArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `firewallPolicy`.
    pub firewall_policy: String,
    /// Query parameter: `name`.
    pub name: Option<String>,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
}

/// Arguments for [`compute.regionNetworkFirewallPolicies.removeRule_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionNetworkFirewallPoliciesRemoveRuleArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `firewallPolicy`.
    pub firewall_policy: String,
    /// Query parameter: `priority`.
    pub priority: Option<String>,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
}

/// Arguments for [`compute.regionNetworkFirewallPolicies.setIamPolicy_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionNetworkFirewallPoliciesSetIamPolicyArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `resource`.
    pub resource: String,
    /// Request body.
    pub body: RegionSetPolicyRequest,
}

/// Arguments for [`compute.regionNetworkFirewallPolicies.testIamPermissions_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionNetworkFirewallPoliciesTestIamPermissionsArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `resource`.
    pub resource: String,
    /// Request body.
    pub body: TestPermissionsRequest,
}

/// Arguments for [`compute.regionNotificationEndpoints.aggregatedList_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionNotificationEndpointsAggregatedListArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `includeAllScopes`.
    pub include_all_scopes: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `returnPartialSuccess`.
    pub return_partial_success: Option<String>,
    /// Query parameter: `serviceProjectNumber`.
    pub service_project_number: Option<String>,
}

/// Arguments for [`compute.regionNotificationEndpoints.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionNotificationEndpointsDeleteArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `notificationEndpoint`.
    pub notification_endpoint: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
}

/// Arguments for [`compute.regionNotificationEndpoints.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionNotificationEndpointsGetArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `notificationEndpoint`.
    pub notification_endpoint: String,
}

/// Arguments for [`compute.regionNotificationEndpoints.insert_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionNotificationEndpointsInsertArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: NotificationEndpoint,
}

/// Arguments for [`compute.regionNotificationEndpoints.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionNotificationEndpointsListArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `returnPartialSuccess`.
    pub return_partial_success: Option<String>,
}

/// Arguments for [`compute.regionNotificationEndpoints.testIamPermissions_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionNotificationEndpointsTestIamPermissionsArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `resource`.
    pub resource: String,
    /// Request body.
    pub body: TestPermissionsRequest,
}

/// Arguments for [`compute.regionOperations.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionOperationsDeleteArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `operation`.
    pub operation: String,
}

/// Arguments for [`compute.regionOperations.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionOperationsGetArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `operation`.
    pub operation: String,
}

/// Arguments for [`compute.regionOperations.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionOperationsListArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `returnPartialSuccess`.
    pub return_partial_success: Option<String>,
}

/// Arguments for [`compute.regionOperations.wait_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionOperationsWaitArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `operation`.
    pub operation: String,
}

/// Arguments for [`compute.regionSecurityPolicies.addRule_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionSecurityPoliciesAddRuleArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `securityPolicy`.
    pub security_policy: String,
    /// Query parameter: `validateOnly`.
    pub validate_only: Option<String>,
    /// Request body.
    pub body: SecurityPolicyRule,
}

/// Arguments for [`compute.regionSecurityPolicies.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionSecurityPoliciesDeleteArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `securityPolicy`.
    pub security_policy: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
}

/// Arguments for [`compute.regionSecurityPolicies.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionSecurityPoliciesGetArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `securityPolicy`.
    pub security_policy: String,
}

/// Arguments for [`compute.regionSecurityPolicies.getRule_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionSecurityPoliciesGetRuleArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `securityPolicy`.
    pub security_policy: String,
    /// Query parameter: `priority`.
    pub priority: Option<String>,
}

/// Arguments for [`compute.regionSecurityPolicies.insert_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionSecurityPoliciesInsertArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Query parameter: `validateOnly`.
    pub validate_only: Option<String>,
    /// Request body.
    pub body: SecurityPolicy,
}

/// Arguments for [`compute.regionSecurityPolicies.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionSecurityPoliciesListArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `returnPartialSuccess`.
    pub return_partial_success: Option<String>,
}

/// Arguments for [`compute.regionSecurityPolicies.patch_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionSecurityPoliciesPatchArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `securityPolicy`.
    pub security_policy: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Query parameter: `updateMask`.
    pub update_mask: Option<String>,
    /// Request body.
    pub body: SecurityPolicy,
}

/// Arguments for [`compute.regionSecurityPolicies.patchRule_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionSecurityPoliciesPatchRuleArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `securityPolicy`.
    pub security_policy: String,
    /// Query parameter: `priority`.
    pub priority: Option<String>,
    /// Query parameter: `updateMask`.
    pub update_mask: Option<String>,
    /// Query parameter: `validateOnly`.
    pub validate_only: Option<String>,
    /// Request body.
    pub body: SecurityPolicyRule,
}

/// Arguments for [`compute.regionSecurityPolicies.removeRule_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionSecurityPoliciesRemoveRuleArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `securityPolicy`.
    pub security_policy: String,
    /// Query parameter: `priority`.
    pub priority: Option<String>,
}

/// Arguments for [`compute.regionSecurityPolicies.setLabels_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionSecurityPoliciesSetLabelsArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `resource`.
    pub resource: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: RegionSetLabelsRequest,
}

/// Arguments for [`compute.regionSnapshotSettings.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionSnapshotSettingsGetArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
}

/// Arguments for [`compute.regionSnapshotSettings.patch_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionSnapshotSettingsPatchArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Query parameter: `updateMask`.
    pub update_mask: Option<String>,
    /// Request body.
    pub body: SnapshotSettings,
}

/// Arguments for [`compute.regionSnapshots.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionSnapshotsDeleteArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `snapshot`.
    pub snapshot: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
}

/// Arguments for [`compute.regionSnapshots.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionSnapshotsGetArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `snapshot`.
    pub snapshot: String,
}

/// Arguments for [`compute.regionSnapshots.getIamPolicy_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionSnapshotsGetIamPolicyArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `resource`.
    pub resource: String,
    /// Query parameter: `optionsRequestedPolicyVersion`.
    pub options_requested_policy_version: Option<String>,
}

/// Arguments for [`compute.regionSnapshots.insert_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionSnapshotsInsertArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: Snapshot,
}

/// Arguments for [`compute.regionSnapshots.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionSnapshotsListArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `returnPartialSuccess`.
    pub return_partial_success: Option<String>,
}

/// Arguments for [`compute.regionSnapshots.setIamPolicy_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionSnapshotsSetIamPolicyArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `resource`.
    pub resource: String,
    /// Request body.
    pub body: RegionSetPolicyRequest,
}

/// Arguments for [`compute.regionSnapshots.setLabels_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionSnapshotsSetLabelsArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `resource`.
    pub resource: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: RegionSetLabelsRequest,
}

/// Arguments for [`compute.regionSnapshots.testIamPermissions_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionSnapshotsTestIamPermissionsArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `resource`.
    pub resource: String,
    /// Request body.
    pub body: TestPermissionsRequest,
}

/// Arguments for [`compute.regionSnapshots.updateKmsKey_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionSnapshotsUpdateKmsKeyArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `snapshot`.
    pub snapshot: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: RegionSnapshotUpdateKmsKeyRequest,
}

/// Arguments for [`compute.regionSslCertificates.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionSslCertificatesDeleteArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `sslCertificate`.
    pub ssl_certificate: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
}

/// Arguments for [`compute.regionSslCertificates.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionSslCertificatesGetArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `sslCertificate`.
    pub ssl_certificate: String,
}

/// Arguments for [`compute.regionSslCertificates.insert_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionSslCertificatesInsertArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: SslCertificate,
}

/// Arguments for [`compute.regionSslCertificates.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionSslCertificatesListArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `returnPartialSuccess`.
    pub return_partial_success: Option<String>,
}

/// Arguments for [`compute.regionSslPolicies.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionSslPoliciesDeleteArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `sslPolicy`.
    pub ssl_policy: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
}

/// Arguments for [`compute.regionSslPolicies.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionSslPoliciesGetArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `sslPolicy`.
    pub ssl_policy: String,
}

/// Arguments for [`compute.regionSslPolicies.insert_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionSslPoliciesInsertArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: SslPolicy,
}

/// Arguments for [`compute.regionSslPolicies.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionSslPoliciesListArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `returnPartialSuccess`.
    pub return_partial_success: Option<String>,
}

/// Arguments for [`compute.regionSslPolicies.listAvailableFeatures_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionSslPoliciesListAvailableFeaturesArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `returnPartialSuccess`.
    pub return_partial_success: Option<String>,
}

/// Arguments for [`compute.regionSslPolicies.patch_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionSslPoliciesPatchArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `sslPolicy`.
    pub ssl_policy: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: SslPolicy,
}

/// Arguments for [`compute.regionTargetHttpProxies.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionTargetHttpProxiesDeleteArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `targetHttpProxy`.
    pub target_http_proxy: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
}

/// Arguments for [`compute.regionTargetHttpProxies.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionTargetHttpProxiesGetArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `targetHttpProxy`.
    pub target_http_proxy: String,
}

/// Arguments for [`compute.regionTargetHttpProxies.insert_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionTargetHttpProxiesInsertArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: TargetHttpProxy,
}

/// Arguments for [`compute.regionTargetHttpProxies.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionTargetHttpProxiesListArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `returnPartialSuccess`.
    pub return_partial_success: Option<String>,
}

/// Arguments for [`compute.regionTargetHttpProxies.setUrlMap_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionTargetHttpProxiesSetUrlMapArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `targetHttpProxy`.
    pub target_http_proxy: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: UrlMapReference,
}

/// Arguments for [`compute.regionTargetHttpsProxies.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionTargetHttpsProxiesDeleteArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `targetHttpsProxy`.
    pub target_https_proxy: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
}

/// Arguments for [`compute.regionTargetHttpsProxies.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionTargetHttpsProxiesGetArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `targetHttpsProxy`.
    pub target_https_proxy: String,
}

/// Arguments for [`compute.regionTargetHttpsProxies.insert_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionTargetHttpsProxiesInsertArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: TargetHttpsProxy,
}

/// Arguments for [`compute.regionTargetHttpsProxies.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionTargetHttpsProxiesListArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `returnPartialSuccess`.
    pub return_partial_success: Option<String>,
}

/// Arguments for [`compute.regionTargetHttpsProxies.patch_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionTargetHttpsProxiesPatchArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `targetHttpsProxy`.
    pub target_https_proxy: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: TargetHttpsProxy,
}

/// Arguments for [`compute.regionTargetHttpsProxies.setSslCertificates_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionTargetHttpsProxiesSetSslCertificatesArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `targetHttpsProxy`.
    pub target_https_proxy: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: RegionTargetHttpsProxiesSetSslCertificatesRequest,
}

/// Arguments for [`compute.regionTargetHttpsProxies.setUrlMap_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionTargetHttpsProxiesSetUrlMapArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `targetHttpsProxy`.
    pub target_https_proxy: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: UrlMapReference,
}

/// Arguments for [`compute.regionTargetTcpProxies.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionTargetTcpProxiesDeleteArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `targetTcpProxy`.
    pub target_tcp_proxy: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
}

/// Arguments for [`compute.regionTargetTcpProxies.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionTargetTcpProxiesGetArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `targetTcpProxy`.
    pub target_tcp_proxy: String,
}

/// Arguments for [`compute.regionTargetTcpProxies.insert_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionTargetTcpProxiesInsertArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: TargetTcpProxy,
}

/// Arguments for [`compute.regionTargetTcpProxies.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionTargetTcpProxiesListArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `returnPartialSuccess`.
    pub return_partial_success: Option<String>,
}

/// Arguments for [`compute.regionUrlMaps.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionUrlMapsDeleteArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `urlMap`.
    pub url_map: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
}

/// Arguments for [`compute.regionUrlMaps.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionUrlMapsGetArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `urlMap`.
    pub url_map: String,
}

/// Arguments for [`compute.regionUrlMaps.insert_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionUrlMapsInsertArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: UrlMap,
}

/// Arguments for [`compute.regionUrlMaps.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionUrlMapsListArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `returnPartialSuccess`.
    pub return_partial_success: Option<String>,
}

/// Arguments for [`compute.regionUrlMaps.patch_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionUrlMapsPatchArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `urlMap`.
    pub url_map: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: UrlMap,
}

/// Arguments for [`compute.regionUrlMaps.update_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionUrlMapsUpdateArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `urlMap`.
    pub url_map: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: UrlMap,
}

/// Arguments for [`compute.regionUrlMaps.validate_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionUrlMapsValidateArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `urlMap`.
    pub url_map: String,
    /// Request body.
    pub body: RegionUrlMapsValidateRequest,
}

/// Arguments for [`compute.regionZones.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionZonesListArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `returnPartialSuccess`.
    pub return_partial_success: Option<String>,
}

/// Arguments for [`compute.regions.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionsGetArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
}

/// Arguments for [`compute.regions.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRegionsListArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `returnPartialSuccess`.
    pub return_partial_success: Option<String>,
}

/// Arguments for [`compute.reservationBlocks.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeReservationBlocksGetArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Path parameter: `reservation`.
    pub reservation: String,
    /// Path parameter: `reservationBlock`.
    pub reservation_block: String,
    /// Query parameter: `view`.
    pub view: Option<String>,
}

/// Arguments for [`compute.reservationBlocks.getIamPolicy_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeReservationBlocksGetIamPolicyArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Path parameter: `parentResource`.
    pub parent_resource: String,
    /// Path parameter: `resource`.
    pub resource: String,
    /// Query parameter: `optionsRequestedPolicyVersion`.
    pub options_requested_policy_version: Option<String>,
}

/// Arguments for [`compute.reservationBlocks.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeReservationBlocksListArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Path parameter: `reservation`.
    pub reservation: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `returnPartialSuccess`.
    pub return_partial_success: Option<String>,
}

/// Arguments for [`compute.reservationBlocks.performMaintenance_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeReservationBlocksPerformMaintenanceArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Path parameter: `reservation`.
    pub reservation: String,
    /// Path parameter: `reservationBlock`.
    pub reservation_block: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: ReservationsBlocksPerformMaintenanceRequest,
}

/// Arguments for [`compute.reservationBlocks.setIamPolicy_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeReservationBlocksSetIamPolicyArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Path parameter: `parentResource`.
    pub parent_resource: String,
    /// Path parameter: `resource`.
    pub resource: String,
    /// Request body.
    pub body: ZoneSetNestedPolicyRequest,
}

/// Arguments for [`compute.reservationBlocks.testIamPermissions_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeReservationBlocksTestIamPermissionsArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Path parameter: `parentResource`.
    pub parent_resource: String,
    /// Path parameter: `resource`.
    pub resource: String,
    /// Request body.
    pub body: TestPermissionsRequest,
}

/// Arguments for [`compute.reservationSlots.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeReservationSlotsGetArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Path parameter: `parentName`.
    pub parent_name: String,
    /// Path parameter: `reservationSlot`.
    pub reservation_slot: String,
}

/// Arguments for [`compute.reservationSlots.getVersion_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeReservationSlotsGetVersionArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Path parameter: `parentName`.
    pub parent_name: String,
    /// Path parameter: `reservationSlot`.
    pub reservation_slot: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: ReservationSlotsGetVersionRequest,
}

/// Arguments for [`compute.reservationSlots.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeReservationSlotsListArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Path parameter: `parentName`.
    pub parent_name: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `returnPartialSuccess`.
    pub return_partial_success: Option<String>,
}

/// Arguments for [`compute.reservationSlots.update_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeReservationSlotsUpdateArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Path parameter: `parentName`.
    pub parent_name: String,
    /// Path parameter: `reservationSlot`.
    pub reservation_slot: String,
    /// Query parameter: `updateMask`.
    pub update_mask: Option<String>,
    /// Request body.
    pub body: ReservationSlot,
}

/// Arguments for [`compute.reservationSubBlocks.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeReservationSubBlocksGetArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Path parameter: `parentName`.
    pub parent_name: String,
    /// Path parameter: `reservationSubBlock`.
    pub reservation_sub_block: String,
    /// Query parameter: `view`.
    pub view: Option<String>,
}

/// Arguments for [`compute.reservationSubBlocks.getIamPolicy_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeReservationSubBlocksGetIamPolicyArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Path parameter: `parentResource`.
    pub parent_resource: String,
    /// Path parameter: `resource`.
    pub resource: String,
    /// Query parameter: `optionsRequestedPolicyVersion`.
    pub options_requested_policy_version: Option<String>,
}

/// Arguments for [`compute.reservationSubBlocks.getVersion_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeReservationSubBlocksGetVersionArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Path parameter: `parentName`.
    pub parent_name: String,
    /// Path parameter: `reservationSubBlock`.
    pub reservation_sub_block: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: ReservationSubBlocksGetVersionRequest,
}

/// Arguments for [`compute.reservationSubBlocks.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeReservationSubBlocksListArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Path parameter: `parentName`.
    pub parent_name: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `returnPartialSuccess`.
    pub return_partial_success: Option<String>,
}

/// Arguments for [`compute.reservationSubBlocks.performMaintenance_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeReservationSubBlocksPerformMaintenanceArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Path parameter: `parentName`.
    pub parent_name: String,
    /// Path parameter: `reservationSubBlock`.
    pub reservation_sub_block: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
}

/// Arguments for [`compute.reservationSubBlocks.reportFaulty_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeReservationSubBlocksReportFaultyArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Path parameter: `parentName`.
    pub parent_name: String,
    /// Path parameter: `reservationSubBlock`.
    pub reservation_sub_block: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: ReservationSubBlocksReportFaultyRequest,
}

/// Arguments for [`compute.reservationSubBlocks.setIamPolicy_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeReservationSubBlocksSetIamPolicyArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Path parameter: `parentResource`.
    pub parent_resource: String,
    /// Path parameter: `resource`.
    pub resource: String,
    /// Request body.
    pub body: ZoneSetNestedPolicyRequest,
}

/// Arguments for [`compute.reservationSubBlocks.testIamPermissions_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeReservationSubBlocksTestIamPermissionsArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Path parameter: `parentResource`.
    pub parent_resource: String,
    /// Path parameter: `resource`.
    pub resource: String,
    /// Request body.
    pub body: TestPermissionsRequest,
}

/// Arguments for [`compute.reservations.aggregatedList_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeReservationsAggregatedListArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `includeAllScopes`.
    pub include_all_scopes: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `returnPartialSuccess`.
    pub return_partial_success: Option<String>,
    /// Query parameter: `serviceProjectNumber`.
    pub service_project_number: Option<String>,
}

/// Arguments for [`compute.reservations.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeReservationsDeleteArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Path parameter: `reservation`.
    pub reservation: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
}

/// Arguments for [`compute.reservations.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeReservationsGetArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Path parameter: `reservation`.
    pub reservation: String,
}

/// Arguments for [`compute.reservations.getIamPolicy_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeReservationsGetIamPolicyArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Path parameter: `resource`.
    pub resource: String,
    /// Query parameter: `optionsRequestedPolicyVersion`.
    pub options_requested_policy_version: Option<String>,
}

/// Arguments for [`compute.reservations.insert_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeReservationsInsertArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: Reservation,
}

/// Arguments for [`compute.reservations.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeReservationsListArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `returnPartialSuccess`.
    pub return_partial_success: Option<String>,
}

/// Arguments for [`compute.reservations.performMaintenance_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeReservationsPerformMaintenanceArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Path parameter: `reservation`.
    pub reservation: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: ReservationsPerformMaintenanceRequest,
}

/// Arguments for [`compute.reservations.resize_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeReservationsResizeArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Path parameter: `reservation`.
    pub reservation: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: ReservationsResizeRequest,
}

/// Arguments for [`compute.reservations.setIamPolicy_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeReservationsSetIamPolicyArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Path parameter: `resource`.
    pub resource: String,
    /// Request body.
    pub body: ZoneSetPolicyRequest,
}

/// Arguments for [`compute.reservations.testIamPermissions_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeReservationsTestIamPermissionsArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Path parameter: `resource`.
    pub resource: String,
    /// Request body.
    pub body: TestPermissionsRequest,
}

/// Arguments for [`compute.reservations.update_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeReservationsUpdateArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Path parameter: `reservation`.
    pub reservation: String,
    /// Query parameter: `paths`.
    pub paths: Option<String>,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Query parameter: `updateMask`.
    pub update_mask: Option<String>,
    /// Request body.
    pub body: Reservation,
}

/// Arguments for [`compute.resourcePolicies.aggregatedList_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeResourcePoliciesAggregatedListArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `includeAllScopes`.
    pub include_all_scopes: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `returnPartialSuccess`.
    pub return_partial_success: Option<String>,
    /// Query parameter: `serviceProjectNumber`.
    pub service_project_number: Option<String>,
}

/// Arguments for [`compute.resourcePolicies.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeResourcePoliciesDeleteArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `resourcePolicy`.
    pub resource_policy: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
}

/// Arguments for [`compute.resourcePolicies.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeResourcePoliciesGetArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `resourcePolicy`.
    pub resource_policy: String,
}

/// Arguments for [`compute.resourcePolicies.getIamPolicy_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeResourcePoliciesGetIamPolicyArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `resource`.
    pub resource: String,
    /// Query parameter: `optionsRequestedPolicyVersion`.
    pub options_requested_policy_version: Option<String>,
}

/// Arguments for [`compute.resourcePolicies.insert_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeResourcePoliciesInsertArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: ResourcePolicy,
}

/// Arguments for [`compute.resourcePolicies.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeResourcePoliciesListArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `returnPartialSuccess`.
    pub return_partial_success: Option<String>,
}

/// Arguments for [`compute.resourcePolicies.patch_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeResourcePoliciesPatchArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `resourcePolicy`.
    pub resource_policy: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Query parameter: `updateMask`.
    pub update_mask: Option<String>,
    /// Request body.
    pub body: ResourcePolicy,
}

/// Arguments for [`compute.resourcePolicies.setIamPolicy_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeResourcePoliciesSetIamPolicyArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `resource`.
    pub resource: String,
    /// Request body.
    pub body: RegionSetPolicyRequest,
}

/// Arguments for [`compute.resourcePolicies.testIamPermissions_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeResourcePoliciesTestIamPermissionsArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `resource`.
    pub resource: String,
    /// Request body.
    pub body: TestPermissionsRequest,
}

/// Arguments for [`compute.routers.aggregatedList_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRoutersAggregatedListArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `includeAllScopes`.
    pub include_all_scopes: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `returnPartialSuccess`.
    pub return_partial_success: Option<String>,
    /// Query parameter: `serviceProjectNumber`.
    pub service_project_number: Option<String>,
}

/// Arguments for [`compute.routers.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRoutersDeleteArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `router`.
    pub router: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
}

/// Arguments for [`compute.routers.deleteRoutePolicy_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRoutersDeleteRoutePolicyArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `router`.
    pub router: String,
    /// Query parameter: `policy`.
    pub policy: Option<String>,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
}

/// Arguments for [`compute.routers.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRoutersGetArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `router`.
    pub router: String,
}

/// Arguments for [`compute.routers.getNatIpInfo_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRoutersGetNatIpInfoArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `router`.
    pub router: String,
    /// Query parameter: `natName`.
    pub nat_name: Option<String>,
}

/// Arguments for [`compute.routers.getNatMappingInfo_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRoutersGetNatMappingInfoArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `router`.
    pub router: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `natName`.
    pub nat_name: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `returnPartialSuccess`.
    pub return_partial_success: Option<String>,
}

/// Arguments for [`compute.routers.getRoutePolicy_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRoutersGetRoutePolicyArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `router`.
    pub router: String,
    /// Query parameter: `policy`.
    pub policy: Option<String>,
}

/// Arguments for [`compute.routers.getRouterStatus_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRoutersGetRouterStatusArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `router`.
    pub router: String,
}

/// Arguments for [`compute.routers.insert_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRoutersInsertArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: Router,
}

/// Arguments for [`compute.routers.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRoutersListArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `returnPartialSuccess`.
    pub return_partial_success: Option<String>,
}

/// Arguments for [`compute.routers.listBgpRoutes_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRoutersListBgpRoutesArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `router`.
    pub router: String,
    /// Query parameter: `addressFamily`.
    pub address_family: Option<String>,
    /// Query parameter: `destinationPrefix`.
    pub destination_prefix: Option<String>,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `peer`.
    pub peer: Option<String>,
    /// Query parameter: `policyApplied`.
    pub policy_applied: Option<String>,
    /// Query parameter: `returnPartialSuccess`.
    pub return_partial_success: Option<String>,
    /// Query parameter: `routeType`.
    pub route_type: Option<String>,
}

/// Arguments for [`compute.routers.listRoutePolicies_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRoutersListRoutePoliciesArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `router`.
    pub router: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `returnPartialSuccess`.
    pub return_partial_success: Option<String>,
}

/// Arguments for [`compute.routers.patch_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRoutersPatchArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `router`.
    pub router: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: Router,
}

/// Arguments for [`compute.routers.patchRoutePolicy_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRoutersPatchRoutePolicyArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `router`.
    pub router: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: RoutePolicy,
}

/// Arguments for [`compute.routers.preview_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRoutersPreviewArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `router`.
    pub router: String,
    /// Request body.
    pub body: Router,
}

/// Arguments for [`compute.routers.update_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRoutersUpdateArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `router`.
    pub router: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: Router,
}

/// Arguments for [`compute.routers.updateRoutePolicy_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRoutersUpdateRoutePolicyArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `router`.
    pub router: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: RoutePolicy,
}

/// Arguments for [`compute.routes.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRoutesDeleteArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `route`.
    pub route: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
}

/// Arguments for [`compute.routes.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRoutesGetArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `route`.
    pub route: String,
}

/// Arguments for [`compute.routes.insert_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRoutesInsertArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: Route,
}

/// Arguments for [`compute.routes.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRoutesListArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `returnPartialSuccess`.
    pub return_partial_success: Option<String>,
}

/// Arguments for [`compute.routes.testIamPermissions_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeRoutesTestIamPermissionsArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `resource`.
    pub resource: String,
    /// Request body.
    pub body: TestPermissionsRequest,
}

/// Arguments for [`compute.securityPolicies.addRule_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeSecurityPoliciesAddRuleArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `securityPolicy`.
    pub security_policy: String,
    /// Query parameter: `validateOnly`.
    pub validate_only: Option<String>,
    /// Request body.
    pub body: SecurityPolicyRule,
}

/// Arguments for [`compute.securityPolicies.aggregatedList_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeSecurityPoliciesAggregatedListArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `includeAllScopes`.
    pub include_all_scopes: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `returnPartialSuccess`.
    pub return_partial_success: Option<String>,
    /// Query parameter: `serviceProjectNumber`.
    pub service_project_number: Option<String>,
}

/// Arguments for [`compute.securityPolicies.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeSecurityPoliciesDeleteArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `securityPolicy`.
    pub security_policy: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
}

/// Arguments for [`compute.securityPolicies.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeSecurityPoliciesGetArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `securityPolicy`.
    pub security_policy: String,
}

/// Arguments for [`compute.securityPolicies.getRule_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeSecurityPoliciesGetRuleArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `securityPolicy`.
    pub security_policy: String,
    /// Query parameter: `priority`.
    pub priority: Option<String>,
}

/// Arguments for [`compute.securityPolicies.insert_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeSecurityPoliciesInsertArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Query parameter: `validateOnly`.
    pub validate_only: Option<String>,
    /// Request body.
    pub body: SecurityPolicy,
}

/// Arguments for [`compute.securityPolicies.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeSecurityPoliciesListArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `returnPartialSuccess`.
    pub return_partial_success: Option<String>,
}

/// Arguments for [`compute.securityPolicies.listPreconfiguredExpressionSets_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeSecurityPoliciesListPreconfiguredExpressionSetsArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `returnPartialSuccess`.
    pub return_partial_success: Option<String>,
}

/// Arguments for [`compute.securityPolicies.patch_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeSecurityPoliciesPatchArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `securityPolicy`.
    pub security_policy: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Query parameter: `updateMask`.
    pub update_mask: Option<String>,
    /// Request body.
    pub body: SecurityPolicy,
}

/// Arguments for [`compute.securityPolicies.patchRule_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeSecurityPoliciesPatchRuleArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `securityPolicy`.
    pub security_policy: String,
    /// Query parameter: `priority`.
    pub priority: Option<String>,
    /// Query parameter: `updateMask`.
    pub update_mask: Option<String>,
    /// Query parameter: `validateOnly`.
    pub validate_only: Option<String>,
    /// Request body.
    pub body: SecurityPolicyRule,
}

/// Arguments for [`compute.securityPolicies.removeRule_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeSecurityPoliciesRemoveRuleArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `securityPolicy`.
    pub security_policy: String,
    /// Query parameter: `priority`.
    pub priority: Option<String>,
}

/// Arguments for [`compute.securityPolicies.setLabels_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeSecurityPoliciesSetLabelsArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `resource`.
    pub resource: String,
    /// Request body.
    pub body: GlobalSetLabelsRequest,
}

/// Arguments for [`compute.serviceAttachments.aggregatedList_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeServiceAttachmentsAggregatedListArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `includeAllScopes`.
    pub include_all_scopes: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `returnPartialSuccess`.
    pub return_partial_success: Option<String>,
    /// Query parameter: `serviceProjectNumber`.
    pub service_project_number: Option<String>,
}

/// Arguments for [`compute.serviceAttachments.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeServiceAttachmentsDeleteArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `serviceAttachment`.
    pub service_attachment: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
}

/// Arguments for [`compute.serviceAttachments.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeServiceAttachmentsGetArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `serviceAttachment`.
    pub service_attachment: String,
    /// Query parameter: `showNatIps`.
    pub show_nat_ips: Option<String>,
}

/// Arguments for [`compute.serviceAttachments.getIamPolicy_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeServiceAttachmentsGetIamPolicyArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `resource`.
    pub resource: String,
    /// Query parameter: `optionsRequestedPolicyVersion`.
    pub options_requested_policy_version: Option<String>,
}

/// Arguments for [`compute.serviceAttachments.insert_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeServiceAttachmentsInsertArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: ServiceAttachment,
}

/// Arguments for [`compute.serviceAttachments.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeServiceAttachmentsListArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `returnPartialSuccess`.
    pub return_partial_success: Option<String>,
}

/// Arguments for [`compute.serviceAttachments.patch_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeServiceAttachmentsPatchArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `serviceAttachment`.
    pub service_attachment: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: ServiceAttachment,
}

/// Arguments for [`compute.serviceAttachments.setIamPolicy_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeServiceAttachmentsSetIamPolicyArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `resource`.
    pub resource: String,
    /// Request body.
    pub body: RegionSetPolicyRequest,
}

/// Arguments for [`compute.serviceAttachments.testIamPermissions_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeServiceAttachmentsTestIamPermissionsArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `resource`.
    pub resource: String,
    /// Request body.
    pub body: TestPermissionsRequest,
}

/// Arguments for [`compute.snapshotSettings.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeSnapshotSettingsGetArgs {
    /// Path parameter: `project`.
    pub project: String,
}

/// Arguments for [`compute.snapshotSettings.patch_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeSnapshotSettingsPatchArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Query parameter: `updateMask`.
    pub update_mask: Option<String>,
    /// Request body.
    pub body: SnapshotSettings,
}

/// Arguments for [`compute.snapshots.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeSnapshotsDeleteArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `snapshot`.
    pub snapshot: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
}

/// Arguments for [`compute.snapshots.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeSnapshotsGetArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `snapshot`.
    pub snapshot: String,
}

/// Arguments for [`compute.snapshots.getIamPolicy_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeSnapshotsGetIamPolicyArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `resource`.
    pub resource: String,
    /// Query parameter: `optionsRequestedPolicyVersion`.
    pub options_requested_policy_version: Option<String>,
}

/// Arguments for [`compute.snapshots.insert_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeSnapshotsInsertArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: Snapshot,
}

/// Arguments for [`compute.snapshots.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeSnapshotsListArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `returnPartialSuccess`.
    pub return_partial_success: Option<String>,
}

/// Arguments for [`compute.snapshots.setIamPolicy_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeSnapshotsSetIamPolicyArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `resource`.
    pub resource: String,
    /// Request body.
    pub body: GlobalSetPolicyRequest,
}

/// Arguments for [`compute.snapshots.setLabels_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeSnapshotsSetLabelsArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `resource`.
    pub resource: String,
    /// Request body.
    pub body: GlobalSetLabelsRequest,
}

/// Arguments for [`compute.snapshots.testIamPermissions_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeSnapshotsTestIamPermissionsArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `resource`.
    pub resource: String,
    /// Request body.
    pub body: TestPermissionsRequest,
}

/// Arguments for [`compute.snapshots.updateKmsKey_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeSnapshotsUpdateKmsKeyArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `snapshot`.
    pub snapshot: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: SnapshotUpdateKmsKeyRequest,
}

/// Arguments for [`compute.sslCertificates.aggregatedList_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeSslCertificatesAggregatedListArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `includeAllScopes`.
    pub include_all_scopes: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `returnPartialSuccess`.
    pub return_partial_success: Option<String>,
    /// Query parameter: `serviceProjectNumber`.
    pub service_project_number: Option<String>,
}

/// Arguments for [`compute.sslCertificates.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeSslCertificatesDeleteArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `sslCertificate`.
    pub ssl_certificate: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
}

/// Arguments for [`compute.sslCertificates.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeSslCertificatesGetArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `sslCertificate`.
    pub ssl_certificate: String,
}

/// Arguments for [`compute.sslCertificates.insert_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeSslCertificatesInsertArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: SslCertificate,
}

/// Arguments for [`compute.sslCertificates.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeSslCertificatesListArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `returnPartialSuccess`.
    pub return_partial_success: Option<String>,
}

/// Arguments for [`compute.sslPolicies.aggregatedList_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeSslPoliciesAggregatedListArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `includeAllScopes`.
    pub include_all_scopes: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `returnPartialSuccess`.
    pub return_partial_success: Option<String>,
    /// Query parameter: `serviceProjectNumber`.
    pub service_project_number: Option<String>,
}

/// Arguments for [`compute.sslPolicies.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeSslPoliciesDeleteArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `sslPolicy`.
    pub ssl_policy: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
}

/// Arguments for [`compute.sslPolicies.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeSslPoliciesGetArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `sslPolicy`.
    pub ssl_policy: String,
}

/// Arguments for [`compute.sslPolicies.insert_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeSslPoliciesInsertArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: SslPolicy,
}

/// Arguments for [`compute.sslPolicies.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeSslPoliciesListArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `returnPartialSuccess`.
    pub return_partial_success: Option<String>,
}

/// Arguments for [`compute.sslPolicies.listAvailableFeatures_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeSslPoliciesListAvailableFeaturesArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `returnPartialSuccess`.
    pub return_partial_success: Option<String>,
}

/// Arguments for [`compute.sslPolicies.patch_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeSslPoliciesPatchArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `sslPolicy`.
    pub ssl_policy: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: SslPolicy,
}

/// Arguments for [`compute.storagePoolTypes.aggregatedList_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeStoragePoolTypesAggregatedListArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `includeAllScopes`.
    pub include_all_scopes: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `returnPartialSuccess`.
    pub return_partial_success: Option<String>,
    /// Query parameter: `serviceProjectNumber`.
    pub service_project_number: Option<String>,
}

/// Arguments for [`compute.storagePoolTypes.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeStoragePoolTypesGetArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Path parameter: `storagePoolType`.
    pub storage_pool_type: String,
}

/// Arguments for [`compute.storagePoolTypes.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeStoragePoolTypesListArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `returnPartialSuccess`.
    pub return_partial_success: Option<String>,
}

/// Arguments for [`compute.storagePools.aggregatedList_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeStoragePoolsAggregatedListArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `includeAllScopes`.
    pub include_all_scopes: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `returnPartialSuccess`.
    pub return_partial_success: Option<String>,
    /// Query parameter: `serviceProjectNumber`.
    pub service_project_number: Option<String>,
}

/// Arguments for [`compute.storagePools.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeStoragePoolsDeleteArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Path parameter: `storagePool`.
    pub storage_pool: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
}

/// Arguments for [`compute.storagePools.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeStoragePoolsGetArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Path parameter: `storagePool`.
    pub storage_pool: String,
}

/// Arguments for [`compute.storagePools.getIamPolicy_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeStoragePoolsGetIamPolicyArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Path parameter: `resource`.
    pub resource: String,
    /// Query parameter: `optionsRequestedPolicyVersion`.
    pub options_requested_policy_version: Option<String>,
}

/// Arguments for [`compute.storagePools.insert_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeStoragePoolsInsertArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: StoragePool,
}

/// Arguments for [`compute.storagePools.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeStoragePoolsListArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `returnPartialSuccess`.
    pub return_partial_success: Option<String>,
}

/// Arguments for [`compute.storagePools.listDisks_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeStoragePoolsListDisksArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Path parameter: `storagePool`.
    pub storage_pool: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `returnPartialSuccess`.
    pub return_partial_success: Option<String>,
}

/// Arguments for [`compute.storagePools.setIamPolicy_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeStoragePoolsSetIamPolicyArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Path parameter: `resource`.
    pub resource: String,
    /// Request body.
    pub body: ZoneSetPolicyRequest,
}

/// Arguments for [`compute.storagePools.testIamPermissions_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeStoragePoolsTestIamPermissionsArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Path parameter: `resource`.
    pub resource: String,
    /// Request body.
    pub body: TestPermissionsRequest,
}

/// Arguments for [`compute.storagePools.update_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeStoragePoolsUpdateArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Path parameter: `storagePool`.
    pub storage_pool: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Query parameter: `updateMask`.
    pub update_mask: Option<String>,
    /// Request body.
    pub body: StoragePool,
}

/// Arguments for [`compute.subnetworks.aggregatedList_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeSubnetworksAggregatedListArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `includeAllScopes`.
    pub include_all_scopes: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `returnPartialSuccess`.
    pub return_partial_success: Option<String>,
    /// Query parameter: `serviceProjectNumber`.
    pub service_project_number: Option<String>,
    /// Query parameter: `views`.
    pub views: Option<String>,
}

/// Arguments for [`compute.subnetworks.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeSubnetworksDeleteArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `subnetwork`.
    pub subnetwork: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
}

/// Arguments for [`compute.subnetworks.expandIpCidrRange_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeSubnetworksExpandIpCidrRangeArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `subnetwork`.
    pub subnetwork: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: SubnetworksExpandIpCidrRangeRequest,
}

/// Arguments for [`compute.subnetworks.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeSubnetworksGetArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `subnetwork`.
    pub subnetwork: String,
    /// Query parameter: `views`.
    pub views: Option<String>,
}

/// Arguments for [`compute.subnetworks.getIamPolicy_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeSubnetworksGetIamPolicyArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `resource`.
    pub resource: String,
    /// Query parameter: `optionsRequestedPolicyVersion`.
    pub options_requested_policy_version: Option<String>,
}

/// Arguments for [`compute.subnetworks.insert_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeSubnetworksInsertArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: Subnetwork,
}

/// Arguments for [`compute.subnetworks.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeSubnetworksListArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `returnPartialSuccess`.
    pub return_partial_success: Option<String>,
    /// Query parameter: `views`.
    pub views: Option<String>,
}

/// Arguments for [`compute.subnetworks.listUsable_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeSubnetworksListUsableArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `returnPartialSuccess`.
    pub return_partial_success: Option<String>,
    /// Query parameter: `serviceProject`.
    pub service_project: Option<String>,
}

/// Arguments for [`compute.subnetworks.patch_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeSubnetworksPatchArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `subnetwork`.
    pub subnetwork: String,
    /// Query parameter: `drainTimeoutSeconds`.
    pub drain_timeout_seconds: Option<String>,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: Subnetwork,
}

/// Arguments for [`compute.subnetworks.setIamPolicy_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeSubnetworksSetIamPolicyArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `resource`.
    pub resource: String,
    /// Request body.
    pub body: RegionSetPolicyRequest,
}

/// Arguments for [`compute.subnetworks.setPrivateIpGoogleAccess_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeSubnetworksSetPrivateIpGoogleAccessArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `subnetwork`.
    pub subnetwork: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: SubnetworksSetPrivateIpGoogleAccessRequest,
}

/// Arguments for [`compute.subnetworks.testIamPermissions_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeSubnetworksTestIamPermissionsArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `resource`.
    pub resource: String,
    /// Request body.
    pub body: TestPermissionsRequest,
}

/// Arguments for [`compute.targetGrpcProxies.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeTargetGrpcProxiesDeleteArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `targetGrpcProxy`.
    pub target_grpc_proxy: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
}

/// Arguments for [`compute.targetGrpcProxies.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeTargetGrpcProxiesGetArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `targetGrpcProxy`.
    pub target_grpc_proxy: String,
}

/// Arguments for [`compute.targetGrpcProxies.insert_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeTargetGrpcProxiesInsertArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: TargetGrpcProxy,
}

/// Arguments for [`compute.targetGrpcProxies.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeTargetGrpcProxiesListArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `returnPartialSuccess`.
    pub return_partial_success: Option<String>,
}

/// Arguments for [`compute.targetGrpcProxies.patch_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeTargetGrpcProxiesPatchArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `targetGrpcProxy`.
    pub target_grpc_proxy: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: TargetGrpcProxy,
}

/// Arguments for [`compute.targetHttpProxies.aggregatedList_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeTargetHttpProxiesAggregatedListArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `includeAllScopes`.
    pub include_all_scopes: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `returnPartialSuccess`.
    pub return_partial_success: Option<String>,
    /// Query parameter: `serviceProjectNumber`.
    pub service_project_number: Option<String>,
}

/// Arguments for [`compute.targetHttpProxies.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeTargetHttpProxiesDeleteArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `targetHttpProxy`.
    pub target_http_proxy: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
}

/// Arguments for [`compute.targetHttpProxies.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeTargetHttpProxiesGetArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `targetHttpProxy`.
    pub target_http_proxy: String,
}

/// Arguments for [`compute.targetHttpProxies.insert_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeTargetHttpProxiesInsertArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: TargetHttpProxy,
}

/// Arguments for [`compute.targetHttpProxies.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeTargetHttpProxiesListArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `returnPartialSuccess`.
    pub return_partial_success: Option<String>,
}

/// Arguments for [`compute.targetHttpProxies.patch_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeTargetHttpProxiesPatchArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `targetHttpProxy`.
    pub target_http_proxy: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: TargetHttpProxy,
}

/// Arguments for [`compute.targetHttpProxies.setUrlMap_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeTargetHttpProxiesSetUrlMapArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `targetHttpProxy`.
    pub target_http_proxy: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: UrlMapReference,
}

/// Arguments for [`compute.targetHttpsProxies.aggregatedList_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeTargetHttpsProxiesAggregatedListArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `includeAllScopes`.
    pub include_all_scopes: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `returnPartialSuccess`.
    pub return_partial_success: Option<String>,
    /// Query parameter: `serviceProjectNumber`.
    pub service_project_number: Option<String>,
}

/// Arguments for [`compute.targetHttpsProxies.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeTargetHttpsProxiesDeleteArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `targetHttpsProxy`.
    pub target_https_proxy: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
}

/// Arguments for [`compute.targetHttpsProxies.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeTargetHttpsProxiesGetArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `targetHttpsProxy`.
    pub target_https_proxy: String,
}

/// Arguments for [`compute.targetHttpsProxies.insert_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeTargetHttpsProxiesInsertArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: TargetHttpsProxy,
}

/// Arguments for [`compute.targetHttpsProxies.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeTargetHttpsProxiesListArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `returnPartialSuccess`.
    pub return_partial_success: Option<String>,
}

/// Arguments for [`compute.targetHttpsProxies.patch_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeTargetHttpsProxiesPatchArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `targetHttpsProxy`.
    pub target_https_proxy: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: TargetHttpsProxy,
}

/// Arguments for [`compute.targetHttpsProxies.setCertificateMap_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeTargetHttpsProxiesSetCertificateMapArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `targetHttpsProxy`.
    pub target_https_proxy: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: TargetHttpsProxiesSetCertificateMapRequest,
}

/// Arguments for [`compute.targetHttpsProxies.setQuicOverride_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeTargetHttpsProxiesSetQuicOverrideArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `targetHttpsProxy`.
    pub target_https_proxy: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: TargetHttpsProxiesSetQuicOverrideRequest,
}

/// Arguments for [`compute.targetHttpsProxies.setSslCertificates_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeTargetHttpsProxiesSetSslCertificatesArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `targetHttpsProxy`.
    pub target_https_proxy: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: TargetHttpsProxiesSetSslCertificatesRequest,
}

/// Arguments for [`compute.targetHttpsProxies.setSslPolicy_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeTargetHttpsProxiesSetSslPolicyArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `targetHttpsProxy`.
    pub target_https_proxy: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: SslPolicyReference,
}

/// Arguments for [`compute.targetHttpsProxies.setUrlMap_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeTargetHttpsProxiesSetUrlMapArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `targetHttpsProxy`.
    pub target_https_proxy: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: UrlMapReference,
}

/// Arguments for [`compute.targetInstances.aggregatedList_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeTargetInstancesAggregatedListArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `includeAllScopes`.
    pub include_all_scopes: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `returnPartialSuccess`.
    pub return_partial_success: Option<String>,
    /// Query parameter: `serviceProjectNumber`.
    pub service_project_number: Option<String>,
}

/// Arguments for [`compute.targetInstances.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeTargetInstancesDeleteArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Path parameter: `targetInstance`.
    pub target_instance: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
}

/// Arguments for [`compute.targetInstances.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeTargetInstancesGetArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Path parameter: `targetInstance`.
    pub target_instance: String,
}

/// Arguments for [`compute.targetInstances.insert_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeTargetInstancesInsertArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: TargetInstance,
}

/// Arguments for [`compute.targetInstances.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeTargetInstancesListArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `returnPartialSuccess`.
    pub return_partial_success: Option<String>,
}

/// Arguments for [`compute.targetInstances.setSecurityPolicy_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeTargetInstancesSetSecurityPolicyArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Path parameter: `targetInstance`.
    pub target_instance: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: SecurityPolicyReference,
}

/// Arguments for [`compute.targetInstances.testIamPermissions_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeTargetInstancesTestIamPermissionsArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Path parameter: `resource`.
    pub resource: String,
    /// Request body.
    pub body: TestPermissionsRequest,
}

/// Arguments for [`compute.targetPools.addHealthCheck_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeTargetPoolsAddHealthCheckArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `targetPool`.
    pub target_pool: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: TargetPoolsAddHealthCheckRequest,
}

/// Arguments for [`compute.targetPools.addInstance_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeTargetPoolsAddInstanceArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `targetPool`.
    pub target_pool: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: TargetPoolsAddInstanceRequest,
}

/// Arguments for [`compute.targetPools.aggregatedList_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeTargetPoolsAggregatedListArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `includeAllScopes`.
    pub include_all_scopes: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `returnPartialSuccess`.
    pub return_partial_success: Option<String>,
    /// Query parameter: `serviceProjectNumber`.
    pub service_project_number: Option<String>,
}

/// Arguments for [`compute.targetPools.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeTargetPoolsDeleteArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `targetPool`.
    pub target_pool: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
}

/// Arguments for [`compute.targetPools.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeTargetPoolsGetArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `targetPool`.
    pub target_pool: String,
}

/// Arguments for [`compute.targetPools.getHealth_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeTargetPoolsGetHealthArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `targetPool`.
    pub target_pool: String,
    /// Request body.
    pub body: InstanceReference,
}

/// Arguments for [`compute.targetPools.insert_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeTargetPoolsInsertArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: TargetPool,
}

/// Arguments for [`compute.targetPools.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeTargetPoolsListArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `returnPartialSuccess`.
    pub return_partial_success: Option<String>,
}

/// Arguments for [`compute.targetPools.removeHealthCheck_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeTargetPoolsRemoveHealthCheckArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `targetPool`.
    pub target_pool: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: TargetPoolsRemoveHealthCheckRequest,
}

/// Arguments for [`compute.targetPools.removeInstance_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeTargetPoolsRemoveInstanceArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `targetPool`.
    pub target_pool: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: TargetPoolsRemoveInstanceRequest,
}

/// Arguments for [`compute.targetPools.setBackup_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeTargetPoolsSetBackupArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `targetPool`.
    pub target_pool: String,
    /// Query parameter: `failoverRatio`.
    pub failover_ratio: Option<String>,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: TargetReference,
}

/// Arguments for [`compute.targetPools.setSecurityPolicy_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeTargetPoolsSetSecurityPolicyArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `targetPool`.
    pub target_pool: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: SecurityPolicyReference,
}

/// Arguments for [`compute.targetPools.testIamPermissions_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeTargetPoolsTestIamPermissionsArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `resource`.
    pub resource: String,
    /// Request body.
    pub body: TestPermissionsRequest,
}

/// Arguments for [`compute.targetSslProxies.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeTargetSslProxiesDeleteArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `targetSslProxy`.
    pub target_ssl_proxy: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
}

/// Arguments for [`compute.targetSslProxies.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeTargetSslProxiesGetArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `targetSslProxy`.
    pub target_ssl_proxy: String,
}

/// Arguments for [`compute.targetSslProxies.insert_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeTargetSslProxiesInsertArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: TargetSslProxy,
}

/// Arguments for [`compute.targetSslProxies.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeTargetSslProxiesListArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `returnPartialSuccess`.
    pub return_partial_success: Option<String>,
}

/// Arguments for [`compute.targetSslProxies.setBackendService_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeTargetSslProxiesSetBackendServiceArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `targetSslProxy`.
    pub target_ssl_proxy: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: TargetSslProxiesSetBackendServiceRequest,
}

/// Arguments for [`compute.targetSslProxies.setCertificateMap_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeTargetSslProxiesSetCertificateMapArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `targetSslProxy`.
    pub target_ssl_proxy: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: TargetSslProxiesSetCertificateMapRequest,
}

/// Arguments for [`compute.targetSslProxies.setProxyHeader_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeTargetSslProxiesSetProxyHeaderArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `targetSslProxy`.
    pub target_ssl_proxy: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: TargetSslProxiesSetProxyHeaderRequest,
}

/// Arguments for [`compute.targetSslProxies.setSslCertificates_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeTargetSslProxiesSetSslCertificatesArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `targetSslProxy`.
    pub target_ssl_proxy: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: TargetSslProxiesSetSslCertificatesRequest,
}

/// Arguments for [`compute.targetSslProxies.setSslPolicy_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeTargetSslProxiesSetSslPolicyArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `targetSslProxy`.
    pub target_ssl_proxy: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: SslPolicyReference,
}

/// Arguments for [`compute.targetSslProxies.testIamPermissions_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeTargetSslProxiesTestIamPermissionsArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `resource`.
    pub resource: String,
    /// Request body.
    pub body: TestPermissionsRequest,
}

/// Arguments for [`compute.targetTcpProxies.aggregatedList_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeTargetTcpProxiesAggregatedListArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `includeAllScopes`.
    pub include_all_scopes: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `returnPartialSuccess`.
    pub return_partial_success: Option<String>,
    /// Query parameter: `serviceProjectNumber`.
    pub service_project_number: Option<String>,
}

/// Arguments for [`compute.targetTcpProxies.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeTargetTcpProxiesDeleteArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `targetTcpProxy`.
    pub target_tcp_proxy: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
}

/// Arguments for [`compute.targetTcpProxies.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeTargetTcpProxiesGetArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `targetTcpProxy`.
    pub target_tcp_proxy: String,
}

/// Arguments for [`compute.targetTcpProxies.insert_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeTargetTcpProxiesInsertArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: TargetTcpProxy,
}

/// Arguments for [`compute.targetTcpProxies.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeTargetTcpProxiesListArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `returnPartialSuccess`.
    pub return_partial_success: Option<String>,
}

/// Arguments for [`compute.targetTcpProxies.setBackendService_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeTargetTcpProxiesSetBackendServiceArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `targetTcpProxy`.
    pub target_tcp_proxy: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: TargetTcpProxiesSetBackendServiceRequest,
}

/// Arguments for [`compute.targetTcpProxies.setProxyHeader_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeTargetTcpProxiesSetProxyHeaderArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `targetTcpProxy`.
    pub target_tcp_proxy: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: TargetTcpProxiesSetProxyHeaderRequest,
}

/// Arguments for [`compute.targetTcpProxies.testIamPermissions_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeTargetTcpProxiesTestIamPermissionsArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `resource`.
    pub resource: String,
    /// Request body.
    pub body: TestPermissionsRequest,
}

/// Arguments for [`compute.targetVpnGateways.aggregatedList_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeTargetVpnGatewaysAggregatedListArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `includeAllScopes`.
    pub include_all_scopes: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `returnPartialSuccess`.
    pub return_partial_success: Option<String>,
    /// Query parameter: `serviceProjectNumber`.
    pub service_project_number: Option<String>,
}

/// Arguments for [`compute.targetVpnGateways.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeTargetVpnGatewaysDeleteArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `targetVpnGateway`.
    pub target_vpn_gateway: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
}

/// Arguments for [`compute.targetVpnGateways.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeTargetVpnGatewaysGetArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `targetVpnGateway`.
    pub target_vpn_gateway: String,
}

/// Arguments for [`compute.targetVpnGateways.insert_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeTargetVpnGatewaysInsertArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: TargetVpnGateway,
}

/// Arguments for [`compute.targetVpnGateways.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeTargetVpnGatewaysListArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `returnPartialSuccess`.
    pub return_partial_success: Option<String>,
}

/// Arguments for [`compute.targetVpnGateways.setLabels_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeTargetVpnGatewaysSetLabelsArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `resource`.
    pub resource: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: RegionSetLabelsRequest,
}

/// Arguments for [`compute.urlMaps.aggregatedList_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeUrlMapsAggregatedListArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `includeAllScopes`.
    pub include_all_scopes: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `returnPartialSuccess`.
    pub return_partial_success: Option<String>,
    /// Query parameter: `serviceProjectNumber`.
    pub service_project_number: Option<String>,
}

/// Arguments for [`compute.urlMaps.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeUrlMapsDeleteArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `urlMap`.
    pub url_map: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
}

/// Arguments for [`compute.urlMaps.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeUrlMapsGetArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `urlMap`.
    pub url_map: String,
}

/// Arguments for [`compute.urlMaps.insert_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeUrlMapsInsertArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: UrlMap,
}

/// Arguments for [`compute.urlMaps.invalidateCache_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeUrlMapsInvalidateCacheArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `urlMap`.
    pub url_map: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: CacheInvalidationRule,
}

/// Arguments for [`compute.urlMaps.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeUrlMapsListArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `returnPartialSuccess`.
    pub return_partial_success: Option<String>,
}

/// Arguments for [`compute.urlMaps.patch_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeUrlMapsPatchArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `urlMap`.
    pub url_map: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: UrlMap,
}

/// Arguments for [`compute.urlMaps.testIamPermissions_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeUrlMapsTestIamPermissionsArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `resource`.
    pub resource: String,
    /// Request body.
    pub body: TestPermissionsRequest,
}

/// Arguments for [`compute.urlMaps.update_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeUrlMapsUpdateArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `urlMap`.
    pub url_map: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: UrlMap,
}

/// Arguments for [`compute.urlMaps.validate_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeUrlMapsValidateArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `urlMap`.
    pub url_map: String,
    /// Request body.
    pub body: UrlMapsValidateRequest,
}

/// Arguments for [`compute.vpnGateways.aggregatedList_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeVpnGatewaysAggregatedListArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `includeAllScopes`.
    pub include_all_scopes: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `returnPartialSuccess`.
    pub return_partial_success: Option<String>,
    /// Query parameter: `serviceProjectNumber`.
    pub service_project_number: Option<String>,
}

/// Arguments for [`compute.vpnGateways.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeVpnGatewaysDeleteArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `vpnGateway`.
    pub vpn_gateway: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
}

/// Arguments for [`compute.vpnGateways.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeVpnGatewaysGetArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `vpnGateway`.
    pub vpn_gateway: String,
}

/// Arguments for [`compute.vpnGateways.getStatus_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeVpnGatewaysGetStatusArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `vpnGateway`.
    pub vpn_gateway: String,
}

/// Arguments for [`compute.vpnGateways.insert_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeVpnGatewaysInsertArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: VpnGateway,
}

/// Arguments for [`compute.vpnGateways.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeVpnGatewaysListArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `returnPartialSuccess`.
    pub return_partial_success: Option<String>,
}

/// Arguments for [`compute.vpnGateways.setLabels_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeVpnGatewaysSetLabelsArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `resource`.
    pub resource: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: RegionSetLabelsRequest,
}

/// Arguments for [`compute.vpnGateways.testIamPermissions_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeVpnGatewaysTestIamPermissionsArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `resource`.
    pub resource: String,
    /// Request body.
    pub body: TestPermissionsRequest,
}

/// Arguments for [`compute.vpnTunnels.aggregatedList_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeVpnTunnelsAggregatedListArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `includeAllScopes`.
    pub include_all_scopes: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `returnPartialSuccess`.
    pub return_partial_success: Option<String>,
    /// Query parameter: `serviceProjectNumber`.
    pub service_project_number: Option<String>,
}

/// Arguments for [`compute.vpnTunnels.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeVpnTunnelsDeleteArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `vpnTunnel`.
    pub vpn_tunnel: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
}

/// Arguments for [`compute.vpnTunnels.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeVpnTunnelsGetArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `vpnTunnel`.
    pub vpn_tunnel: String,
}

/// Arguments for [`compute.vpnTunnels.insert_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeVpnTunnelsInsertArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: VpnTunnel,
}

/// Arguments for [`compute.vpnTunnels.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeVpnTunnelsListArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `returnPartialSuccess`.
    pub return_partial_success: Option<String>,
}

/// Arguments for [`compute.vpnTunnels.setLabels_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeVpnTunnelsSetLabelsArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `resource`.
    pub resource: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: RegionSetLabelsRequest,
}

/// Arguments for [`compute.wireGroups.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeWireGroupsDeleteArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `crossSiteNetwork`.
    pub cross_site_network: String,
    /// Path parameter: `wireGroup`.
    pub wire_group: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
}

/// Arguments for [`compute.wireGroups.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeWireGroupsGetArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `crossSiteNetwork`.
    pub cross_site_network: String,
    /// Path parameter: `wireGroup`.
    pub wire_group: String,
}

/// Arguments for [`compute.wireGroups.insert_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeWireGroupsInsertArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `crossSiteNetwork`.
    pub cross_site_network: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Query parameter: `validateOnly`.
    pub validate_only: Option<String>,
    /// Request body.
    pub body: WireGroup,
}

/// Arguments for [`compute.wireGroups.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeWireGroupsListArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `crossSiteNetwork`.
    pub cross_site_network: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `returnPartialSuccess`.
    pub return_partial_success: Option<String>,
}

/// Arguments for [`compute.wireGroups.patch_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeWireGroupsPatchArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `crossSiteNetwork`.
    pub cross_site_network: String,
    /// Path parameter: `wireGroup`.
    pub wire_group: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Query parameter: `updateMask`.
    pub update_mask: Option<String>,
    /// Query parameter: `validateOnly`.
    pub validate_only: Option<String>,
    /// Request body.
    pub body: WireGroup,
}

/// Arguments for [`compute.zoneOperations.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeZoneOperationsDeleteArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Path parameter: `operation`.
    pub operation: String,
}

/// Arguments for [`compute.zoneOperations.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeZoneOperationsGetArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Path parameter: `operation`.
    pub operation: String,
}

/// Arguments for [`compute.zoneOperations.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeZoneOperationsListArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `returnPartialSuccess`.
    pub return_partial_success: Option<String>,
}

/// Arguments for [`compute.zoneOperations.wait_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeZoneOperationsWaitArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Path parameter: `operation`.
    pub operation: String,
}

/// Arguments for [`compute.zoneVmExtensionPolicies.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeZoneVmExtensionPoliciesDeleteArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Path parameter: `vmExtensionPolicy`.
    pub vm_extension_policy: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
}

/// Arguments for [`compute.zoneVmExtensionPolicies.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeZoneVmExtensionPoliciesGetArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Path parameter: `vmExtensionPolicy`.
    pub vm_extension_policy: String,
}

/// Arguments for [`compute.zoneVmExtensionPolicies.insert_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeZoneVmExtensionPoliciesInsertArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: VmExtensionPolicy,
}

/// Arguments for [`compute.zoneVmExtensionPolicies.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeZoneVmExtensionPoliciesListArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `returnPartialSuccess`.
    pub return_partial_success: Option<String>,
}

/// Arguments for [`compute.zoneVmExtensionPolicies.update_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeZoneVmExtensionPoliciesUpdateArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Path parameter: `vmExtensionPolicy`.
    pub vm_extension_policy: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: VmExtensionPolicy,
}

/// Arguments for [`compute.zones.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeZonesGetArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `zone`.
    pub zone: String,
}

/// Arguments for [`compute.zones.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ComputeZonesListArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `returnPartialSuccess`.
    pub return_partial_success: Option<String>,
}

// =============================================================================
// CLIENT FUNCTIONS (per-endpoint)
// =============================================================================

// -----------------------------------------------------------------------------
// GET projects/{project}/aggregated/acceleratorTypes
// -----------------------------------------------------------------------------

/// GET projects/{project}/aggregated/acceleratorTypes.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_accelerator_types_aggregated_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_accelerator_types_aggregated_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeAcceleratorTypesAggregatedListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<AcceleratorTypeAggregatedList>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/aggregated/acceleratorTypes",
        args.project,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.include_all_scopes {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("includeAllScopes=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.order_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orderBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.return_partial_success {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("returnPartialSuccess=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.service_project_number {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("serviceProjectNumber=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: AcceleratorTypeAggregatedList =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/zones/{zone}/acceleratorTypes/{acceleratorType}
// -----------------------------------------------------------------------------

/// GET projects/{project}/zones/{zone}/acceleratorTypes/{acceleratorType}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_accelerator_types_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_accelerator_types_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeAcceleratorTypesGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<AcceleratorType>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/acceleratorTypes/{}",
        args.project, args.zone, args.accelerator_type,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: AcceleratorType =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/zones/{zone}/acceleratorTypes
// -----------------------------------------------------------------------------

/// GET projects/{project}/zones/{zone}/acceleratorTypes.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_accelerator_types_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_accelerator_types_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeAcceleratorTypesListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<AcceleratorTypeList>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/acceleratorTypes",
        args.project, args.zone,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.order_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orderBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.return_partial_success {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("returnPartialSuccess=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: AcceleratorTypeList =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/aggregated/addresses
// -----------------------------------------------------------------------------

/// GET projects/{project}/aggregated/addresses.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_addresses_aggregated_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_addresses_aggregated_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeAddressesAggregatedListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<AddressAggregatedList>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/aggregated/addresses",
        args.project,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.include_all_scopes {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("includeAllScopes=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.order_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orderBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.return_partial_success {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("returnPartialSuccess=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.service_project_number {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("serviceProjectNumber=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: AddressAggregatedList =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE projects/{project}/regions/{region}/addresses/{address}
// -----------------------------------------------------------------------------

/// DELETE projects/{project}/regions/{region}/addresses/{address}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_addresses_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_addresses_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeAddressesDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/addresses/{}",
        args.project, args.region, args.address,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/regions/{region}/addresses/{address}
// -----------------------------------------------------------------------------

/// GET projects/{project}/regions/{region}/addresses/{address}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_addresses_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_addresses_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeAddressesGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Address>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/addresses/{}",
        args.project, args.region, args.address,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Address =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/regions/{region}/addresses
// -----------------------------------------------------------------------------

/// POST projects/{project}/regions/{region}/addresses.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_addresses_insert_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_addresses_insert_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeAddressesInsertArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/addresses",
        args.project, args.region,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/regions/{region}/addresses
// -----------------------------------------------------------------------------

/// GET projects/{project}/regions/{region}/addresses.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_addresses_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_addresses_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeAddressesListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<AddressList>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/addresses",
        args.project, args.region,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.order_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orderBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.return_partial_success {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("returnPartialSuccess=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: AddressList =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/regions/{region}/addresses/{address}/move
// -----------------------------------------------------------------------------

/// POST projects/{project}/regions/{region}/addresses/{address}/move.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_addresses_move_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_addresses_move_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeAddressesMoveArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/addresses/{}/move",
        args.project, args.region, args.address,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/regions/{region}/addresses/{resource}/setLabels
// -----------------------------------------------------------------------------

/// POST projects/{project}/regions/{region}/addresses/{resource}/setLabels.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_addresses_set_labels_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_addresses_set_labels_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeAddressesSetLabelsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/addresses/{}/setLabels",
        args.project, args.region, args.resource,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/regions/{region}/addresses/{resource}/testIamPermissions
// -----------------------------------------------------------------------------

/// POST projects/{project}/regions/{region}/addresses/{resource}/testIamPermissions.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_addresses_test_iam_permissions_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_addresses_test_iam_permissions_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeAddressesTestIamPermissionsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TestPermissionsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/addresses/{}/testIamPermissions",
        args.project,
        args.region,
        args.resource,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: TestPermissionsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/regions/{region}/advice/calendarMode
// -----------------------------------------------------------------------------

/// POST projects/{project}/regions/{region}/advice/calendarMode.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_advice_calendar_mode_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_advice_calendar_mode_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeAdviceCalendarModeArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<CalendarModeAdviceResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/advice/calendarMode",
        args.project, args.region,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: CalendarModeAdviceResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/aggregated/autoscalers
// -----------------------------------------------------------------------------

/// GET projects/{project}/aggregated/autoscalers.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_autoscalers_aggregated_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_autoscalers_aggregated_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeAutoscalersAggregatedListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<AutoscalerAggregatedList>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/aggregated/autoscalers",
        args.project,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.include_all_scopes {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("includeAllScopes=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.order_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orderBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.return_partial_success {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("returnPartialSuccess=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.service_project_number {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("serviceProjectNumber=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: AutoscalerAggregatedList =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE projects/{project}/zones/{zone}/autoscalers/{autoscaler}
// -----------------------------------------------------------------------------

/// DELETE projects/{project}/zones/{zone}/autoscalers/{autoscaler}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_autoscalers_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_autoscalers_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeAutoscalersDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/autoscalers/{}",
        args.project, args.zone, args.autoscaler,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/zones/{zone}/autoscalers/{autoscaler}
// -----------------------------------------------------------------------------

/// GET projects/{project}/zones/{zone}/autoscalers/{autoscaler}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_autoscalers_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_autoscalers_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeAutoscalersGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Autoscaler>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/autoscalers/{}",
        args.project, args.zone, args.autoscaler,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Autoscaler =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/zones/{zone}/autoscalers
// -----------------------------------------------------------------------------

/// POST projects/{project}/zones/{zone}/autoscalers.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_autoscalers_insert_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_autoscalers_insert_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeAutoscalersInsertArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/autoscalers",
        args.project, args.zone,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/zones/{zone}/autoscalers
// -----------------------------------------------------------------------------

/// GET projects/{project}/zones/{zone}/autoscalers.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_autoscalers_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_autoscalers_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeAutoscalersListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<AutoscalerList>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/autoscalers",
        args.project, args.zone,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.order_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orderBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.return_partial_success {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("returnPartialSuccess=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: AutoscalerList =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PATCH projects/{project}/zones/{zone}/autoscalers
// -----------------------------------------------------------------------------

/// PATCH projects/{project}/zones/{zone}/autoscalers.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_autoscalers_patch_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_autoscalers_patch_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeAutoscalersPatchArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/autoscalers",
        args.project, args.zone,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.autoscaler {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("autoscaler=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .patch(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/zones/{zone}/autoscalers/{resource}/testIamPermissions
// -----------------------------------------------------------------------------

/// POST projects/{project}/zones/{zone}/autoscalers/{resource}/testIamPermissions.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_autoscalers_test_iam_permissions_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_autoscalers_test_iam_permissions_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeAutoscalersTestIamPermissionsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TestPermissionsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/autoscalers/{}/testIamPermissions",
        args.project,
        args.zone,
        args.resource,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: TestPermissionsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PUT projects/{project}/zones/{zone}/autoscalers
// -----------------------------------------------------------------------------

/// PUT projects/{project}/zones/{zone}/autoscalers.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_autoscalers_update_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_autoscalers_update_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeAutoscalersUpdateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/autoscalers",
        args.project, args.zone,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.autoscaler {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("autoscaler=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .put(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/global/backendBuckets/{backendBucket}/addSignedUrlKey
// -----------------------------------------------------------------------------

/// POST projects/{project}/global/backendBuckets/{backendBucket}/addSignedUrlKey.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_backend_buckets_add_signed_url_key_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_backend_buckets_add_signed_url_key_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeBackendBucketsAddSignedUrlKeyArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/backendBuckets/{}/addSignedUrlKey",
        args.project,
        args.backend_bucket,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/aggregated/backendBuckets
// -----------------------------------------------------------------------------

/// GET projects/{project}/aggregated/backendBuckets.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_backend_buckets_aggregated_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_backend_buckets_aggregated_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeBackendBucketsAggregatedListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<BackendBucketAggregatedList>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/aggregated/backendBuckets",
        args.project,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.include_all_scopes {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("includeAllScopes=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.order_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orderBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.return_partial_success {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("returnPartialSuccess=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.service_project_number {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("serviceProjectNumber=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: BackendBucketAggregatedList =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE projects/{project}/global/backendBuckets/{backendBucket}
// -----------------------------------------------------------------------------

/// DELETE projects/{project}/global/backendBuckets/{backendBucket}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_backend_buckets_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_backend_buckets_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeBackendBucketsDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/backendBuckets/{}",
        args.project, args.backend_bucket,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/global/backendBuckets/{backendBucket}/deleteSignedUrlKey
// -----------------------------------------------------------------------------

/// POST projects/{project}/global/backendBuckets/{backendBucket}/deleteSignedUrlKey.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_backend_buckets_delete_signed_url_key_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_backend_buckets_delete_signed_url_key_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeBackendBucketsDeleteSignedUrlKeyArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/backendBuckets/{}/deleteSignedUrlKey",
        args.project,
        args.backend_bucket,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.key_name {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("keyName=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/global/backendBuckets/{backendBucket}
// -----------------------------------------------------------------------------

/// GET projects/{project}/global/backendBuckets/{backendBucket}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_backend_buckets_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_backend_buckets_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeBackendBucketsGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<BackendBucket>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/backendBuckets/{}",
        args.project, args.backend_bucket,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: BackendBucket =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/global/backendBuckets/{resource}/getIamPolicy
// -----------------------------------------------------------------------------

/// GET projects/{project}/global/backendBuckets/{resource}/getIamPolicy.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_backend_buckets_get_iam_policy_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_backend_buckets_get_iam_policy_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeBackendBucketsGetIamPolicyArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/backendBuckets/{}/getIamPolicy",
        args.project,
        args.resource,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.options_requested_policy_version {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("optionsRequestedPolicyVersion=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Policy =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/global/backendBuckets
// -----------------------------------------------------------------------------

/// POST projects/{project}/global/backendBuckets.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_backend_buckets_insert_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_backend_buckets_insert_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeBackendBucketsInsertArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/backendBuckets",
        args.project,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/global/backendBuckets
// -----------------------------------------------------------------------------

/// GET projects/{project}/global/backendBuckets.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_backend_buckets_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_backend_buckets_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeBackendBucketsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<BackendBucketList>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/backendBuckets",
        args.project,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.order_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orderBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.return_partial_success {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("returnPartialSuccess=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: BackendBucketList =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/global/backendBuckets/listUsable
// -----------------------------------------------------------------------------

/// GET projects/{project}/global/backendBuckets/listUsable.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_backend_buckets_list_usable_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_backend_buckets_list_usable_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeBackendBucketsListUsableArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<BackendBucketListUsable>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/backendBuckets/listUsable",
        args.project,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.order_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orderBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.return_partial_success {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("returnPartialSuccess=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: BackendBucketListUsable =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PATCH projects/{project}/global/backendBuckets/{backendBucket}
// -----------------------------------------------------------------------------

/// PATCH projects/{project}/global/backendBuckets/{backendBucket}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_backend_buckets_patch_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_backend_buckets_patch_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeBackendBucketsPatchArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/backendBuckets/{}",
        args.project, args.backend_bucket,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .patch(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/global/backendBuckets/{backendBucket}/setEdgeSecurityPolicy
// -----------------------------------------------------------------------------

/// POST projects/{project}/global/backendBuckets/{backendBucket}/setEdgeSecurityPolicy.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_backend_buckets_set_edge_security_policy_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_backend_buckets_set_edge_security_policy_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeBackendBucketsSetEdgeSecurityPolicyArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/backendBuckets/{}/setEdgeSecurityPolicy",
        args.project,
        args.backend_bucket,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/global/backendBuckets/{resource}/setIamPolicy
// -----------------------------------------------------------------------------

/// POST projects/{project}/global/backendBuckets/{resource}/setIamPolicy.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_backend_buckets_set_iam_policy_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_backend_buckets_set_iam_policy_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeBackendBucketsSetIamPolicyArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/backendBuckets/{}/setIamPolicy",
        args.project,
        args.resource,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Policy =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/global/backendBuckets/{resource}/testIamPermissions
// -----------------------------------------------------------------------------

/// POST projects/{project}/global/backendBuckets/{resource}/testIamPermissions.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_backend_buckets_test_iam_permissions_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_backend_buckets_test_iam_permissions_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeBackendBucketsTestIamPermissionsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TestPermissionsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/backendBuckets/{}/testIamPermissions",
        args.project,
        args.resource,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: TestPermissionsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PUT projects/{project}/global/backendBuckets/{backendBucket}
// -----------------------------------------------------------------------------

/// PUT projects/{project}/global/backendBuckets/{backendBucket}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_backend_buckets_update_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_backend_buckets_update_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeBackendBucketsUpdateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/backendBuckets/{}",
        args.project, args.backend_bucket,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .put(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/global/backendServices/{backendService}/addSignedUrlKey
// -----------------------------------------------------------------------------

/// POST projects/{project}/global/backendServices/{backendService}/addSignedUrlKey.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_backend_services_add_signed_url_key_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_backend_services_add_signed_url_key_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeBackendServicesAddSignedUrlKeyArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/backendServices/{}/addSignedUrlKey",
        args.project,
        args.backend_service,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/aggregated/backendServices
// -----------------------------------------------------------------------------

/// GET projects/{project}/aggregated/backendServices.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_backend_services_aggregated_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_backend_services_aggregated_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeBackendServicesAggregatedListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<BackendServiceAggregatedList>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/aggregated/backendServices",
        args.project,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.include_all_scopes {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("includeAllScopes=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.order_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orderBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.return_partial_success {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("returnPartialSuccess=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.service_project_number {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("serviceProjectNumber=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: BackendServiceAggregatedList =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE projects/{project}/global/backendServices/{backendService}
// -----------------------------------------------------------------------------

/// DELETE projects/{project}/global/backendServices/{backendService}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_backend_services_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_backend_services_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeBackendServicesDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/backendServices/{}",
        args.project, args.backend_service,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/global/backendServices/{backendService}/deleteSignedUrlKey
// -----------------------------------------------------------------------------

/// POST projects/{project}/global/backendServices/{backendService}/deleteSignedUrlKey.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_backend_services_delete_signed_url_key_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_backend_services_delete_signed_url_key_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeBackendServicesDeleteSignedUrlKeyArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/backendServices/{}/deleteSignedUrlKey",
        args.project,
        args.backend_service,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.key_name {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("keyName=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/global/backendServices/{backendService}
// -----------------------------------------------------------------------------

/// GET projects/{project}/global/backendServices/{backendService}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_backend_services_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_backend_services_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeBackendServicesGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<BackendService>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/backendServices/{}",
        args.project, args.backend_service,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: BackendService =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/global/backendServices/{backendService}/getEffectiveSecurityPolicies
// -----------------------------------------------------------------------------

/// GET projects/{project}/global/backendServices/{backendService}/getEffectiveSecurityPolicies.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_backend_services_get_effective_security_policies_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_backend_services_get_effective_security_policies_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeBackendServicesGetEffectiveSecurityPoliciesArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/backendServices/{}/getEffectiveSecurityPolicies",
        args.project,
        args.backend_service,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream: _,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/global/backendServices/{backendService}/getHealth
// -----------------------------------------------------------------------------

/// POST projects/{project}/global/backendServices/{backendService}/getHealth.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_backend_services_get_health_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_backend_services_get_health_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeBackendServicesGetHealthArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<BackendServiceGroupHealth>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/backendServices/{}/getHealth",
        args.project, args.backend_service,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: BackendServiceGroupHealth =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/global/backendServices/{resource}/getIamPolicy
// -----------------------------------------------------------------------------

/// GET projects/{project}/global/backendServices/{resource}/getIamPolicy.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_backend_services_get_iam_policy_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_backend_services_get_iam_policy_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeBackendServicesGetIamPolicyArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/backendServices/{}/getIamPolicy",
        args.project,
        args.resource,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.options_requested_policy_version {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("optionsRequestedPolicyVersion=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Policy =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/global/backendServices
// -----------------------------------------------------------------------------

/// POST projects/{project}/global/backendServices.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_backend_services_insert_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_backend_services_insert_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeBackendServicesInsertArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/backendServices",
        args.project,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/global/backendServices
// -----------------------------------------------------------------------------

/// GET projects/{project}/global/backendServices.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_backend_services_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_backend_services_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeBackendServicesListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<BackendServiceList>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/backendServices",
        args.project,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.order_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orderBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.return_partial_success {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("returnPartialSuccess=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: BackendServiceList =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/global/backendServices/listUsable
// -----------------------------------------------------------------------------

/// GET projects/{project}/global/backendServices/listUsable.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_backend_services_list_usable_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_backend_services_list_usable_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeBackendServicesListUsableArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<BackendServiceListUsable>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/backendServices/listUsable",
        args.project,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.order_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orderBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.return_partial_success {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("returnPartialSuccess=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: BackendServiceListUsable =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PATCH projects/{project}/global/backendServices/{backendService}
// -----------------------------------------------------------------------------

/// PATCH projects/{project}/global/backendServices/{backendService}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_backend_services_patch_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_backend_services_patch_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeBackendServicesPatchArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/backendServices/{}",
        args.project, args.backend_service,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .patch(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/global/backendServices/{backendService}/setEdgeSecurityPolicy
// -----------------------------------------------------------------------------

/// POST projects/{project}/global/backendServices/{backendService}/setEdgeSecurityPolicy.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_backend_services_set_edge_security_policy_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_backend_services_set_edge_security_policy_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeBackendServicesSetEdgeSecurityPolicyArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/backendServices/{}/setEdgeSecurityPolicy",
        args.project,
        args.backend_service,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/global/backendServices/{resource}/setIamPolicy
// -----------------------------------------------------------------------------

/// POST projects/{project}/global/backendServices/{resource}/setIamPolicy.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_backend_services_set_iam_policy_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_backend_services_set_iam_policy_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeBackendServicesSetIamPolicyArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/backendServices/{}/setIamPolicy",
        args.project,
        args.resource,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Policy =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/global/backendServices/{backendService}/setSecurityPolicy
// -----------------------------------------------------------------------------

/// POST projects/{project}/global/backendServices/{backendService}/setSecurityPolicy.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_backend_services_set_security_policy_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_backend_services_set_security_policy_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeBackendServicesSetSecurityPolicyArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/backendServices/{}/setSecurityPolicy",
        args.project,
        args.backend_service,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/global/backendServices/{resource}/testIamPermissions
// -----------------------------------------------------------------------------

/// POST projects/{project}/global/backendServices/{resource}/testIamPermissions.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_backend_services_test_iam_permissions_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_backend_services_test_iam_permissions_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeBackendServicesTestIamPermissionsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TestPermissionsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/backendServices/{}/testIamPermissions",
        args.project,
        args.resource,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: TestPermissionsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PUT projects/{project}/global/backendServices/{backendService}
// -----------------------------------------------------------------------------

/// PUT projects/{project}/global/backendServices/{backendService}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_backend_services_update_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_backend_services_update_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeBackendServicesUpdateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/backendServices/{}",
        args.project, args.backend_service,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .put(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE projects/{project}/global/crossSiteNetworks/{crossSiteNetwork}
// -----------------------------------------------------------------------------

/// DELETE projects/{project}/global/crossSiteNetworks/{crossSiteNetwork}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_cross_site_networks_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_cross_site_networks_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeCrossSiteNetworksDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/crossSiteNetworks/{}",
        args.project, args.cross_site_network,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/global/crossSiteNetworks/{crossSiteNetwork}
// -----------------------------------------------------------------------------

/// GET projects/{project}/global/crossSiteNetworks/{crossSiteNetwork}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_cross_site_networks_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_cross_site_networks_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeCrossSiteNetworksGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<CrossSiteNetwork>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/crossSiteNetworks/{}",
        args.project, args.cross_site_network,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: CrossSiteNetwork =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/global/crossSiteNetworks
// -----------------------------------------------------------------------------

/// POST projects/{project}/global/crossSiteNetworks.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_cross_site_networks_insert_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_cross_site_networks_insert_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeCrossSiteNetworksInsertArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/crossSiteNetworks",
        args.project,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.validate_only {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("validateOnly=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/global/crossSiteNetworks
// -----------------------------------------------------------------------------

/// GET projects/{project}/global/crossSiteNetworks.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_cross_site_networks_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_cross_site_networks_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeCrossSiteNetworksListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<CrossSiteNetworkList>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/crossSiteNetworks",
        args.project,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.order_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orderBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.return_partial_success {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("returnPartialSuccess=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: CrossSiteNetworkList =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PATCH projects/{project}/global/crossSiteNetworks/{crossSiteNetwork}
// -----------------------------------------------------------------------------

/// PATCH projects/{project}/global/crossSiteNetworks/{crossSiteNetwork}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_cross_site_networks_patch_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_cross_site_networks_patch_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeCrossSiteNetworksPatchArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/crossSiteNetworks/{}",
        args.project, args.cross_site_network,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.update_mask {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("updateMask=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.validate_only {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("validateOnly=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .patch(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/aggregated/diskTypes
// -----------------------------------------------------------------------------

/// GET projects/{project}/aggregated/diskTypes.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_disk_types_aggregated_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_disk_types_aggregated_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeDiskTypesAggregatedListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<DiskTypeAggregatedList>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/aggregated/diskTypes",
        args.project,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.include_all_scopes {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("includeAllScopes=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.order_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orderBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.return_partial_success {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("returnPartialSuccess=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.service_project_number {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("serviceProjectNumber=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: DiskTypeAggregatedList =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/zones/{zone}/diskTypes/{diskType}
// -----------------------------------------------------------------------------

/// GET projects/{project}/zones/{zone}/diskTypes/{diskType}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_disk_types_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_disk_types_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeDiskTypesGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<DiskType>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/diskTypes/{}",
        args.project, args.zone, args.disk_type,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: DiskType =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/zones/{zone}/diskTypes
// -----------------------------------------------------------------------------

/// GET projects/{project}/zones/{zone}/diskTypes.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_disk_types_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_disk_types_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeDiskTypesListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<DiskTypeList>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/diskTypes",
        args.project, args.zone,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.order_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orderBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.return_partial_success {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("returnPartialSuccess=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: DiskTypeList =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/zones/{zone}/disks/{disk}/addResourcePolicies
// -----------------------------------------------------------------------------

/// POST projects/{project}/zones/{zone}/disks/{disk}/addResourcePolicies.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_disks_add_resource_policies_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_disks_add_resource_policies_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeDisksAddResourcePoliciesArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/disks/{}/addResourcePolicies",
        args.project,
        args.zone,
        args.disk,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/aggregated/disks
// -----------------------------------------------------------------------------

/// GET projects/{project}/aggregated/disks.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_disks_aggregated_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_disks_aggregated_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeDisksAggregatedListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<DiskAggregatedList>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/aggregated/disks",
        args.project,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.include_all_scopes {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("includeAllScopes=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.order_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orderBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.return_partial_success {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("returnPartialSuccess=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.service_project_number {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("serviceProjectNumber=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: DiskAggregatedList =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/zones/{zone}/disks/bulkInsert
// -----------------------------------------------------------------------------

/// POST projects/{project}/zones/{zone}/disks/bulkInsert.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_disks_bulk_insert_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_disks_bulk_insert_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeDisksBulkInsertArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/disks/bulkInsert",
        args.project, args.zone,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/zones/{zone}/disks/bulkSetLabels
// -----------------------------------------------------------------------------

/// POST projects/{project}/zones/{zone}/disks/bulkSetLabels.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_disks_bulk_set_labels_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_disks_bulk_set_labels_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeDisksBulkSetLabelsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/disks/bulkSetLabels",
        args.project, args.zone,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.resource {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("resource=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/zones/{zone}/disks/{disk}/createSnapshot
// -----------------------------------------------------------------------------

/// POST projects/{project}/zones/{zone}/disks/{disk}/createSnapshot.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_disks_create_snapshot_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_disks_create_snapshot_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeDisksCreateSnapshotArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/disks/{}/createSnapshot",
        args.project, args.zone, args.disk,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.guest_flush {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("guestFlush=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE projects/{project}/zones/{zone}/disks/{disk}
// -----------------------------------------------------------------------------

/// DELETE projects/{project}/zones/{zone}/disks/{disk}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_disks_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_disks_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeDisksDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/disks/{}",
        args.project, args.zone, args.disk,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/zones/{zone}/disks/{disk}
// -----------------------------------------------------------------------------

/// GET projects/{project}/zones/{zone}/disks/{disk}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_disks_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_disks_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeDisksGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Disk>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/disks/{}",
        args.project, args.zone, args.disk,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Disk =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/zones/{zone}/disks/{resource}/getIamPolicy
// -----------------------------------------------------------------------------

/// GET projects/{project}/zones/{zone}/disks/{resource}/getIamPolicy.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_disks_get_iam_policy_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_disks_get_iam_policy_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeDisksGetIamPolicyArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/disks/{}/getIamPolicy",
        args.project, args.zone, args.resource,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.options_requested_policy_version {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("optionsRequestedPolicyVersion=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Policy =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/zones/{zone}/disks
// -----------------------------------------------------------------------------

/// POST projects/{project}/zones/{zone}/disks.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_disks_insert_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_disks_insert_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeDisksInsertArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/disks",
        args.project, args.zone,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.source_image {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("sourceImage=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/zones/{zone}/disks
// -----------------------------------------------------------------------------

/// GET projects/{project}/zones/{zone}/disks.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_disks_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_disks_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeDisksListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<DiskList>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/disks",
        args.project, args.zone,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.order_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orderBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.return_partial_success {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("returnPartialSuccess=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: DiskList =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/zones/{zone}/disks/{disk}/removeResourcePolicies
// -----------------------------------------------------------------------------

/// POST projects/{project}/zones/{zone}/disks/{disk}/removeResourcePolicies.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_disks_remove_resource_policies_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_disks_remove_resource_policies_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeDisksRemoveResourcePoliciesArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/disks/{}/removeResourcePolicies",
        args.project,
        args.zone,
        args.disk,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/zones/{zone}/disks/{disk}/resize
// -----------------------------------------------------------------------------

/// POST projects/{project}/zones/{zone}/disks/{disk}/resize.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_disks_resize_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_disks_resize_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeDisksResizeArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/disks/{}/resize",
        args.project, args.zone, args.disk,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/zones/{zone}/disks/{resource}/setIamPolicy
// -----------------------------------------------------------------------------

/// POST projects/{project}/zones/{zone}/disks/{resource}/setIamPolicy.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_disks_set_iam_policy_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_disks_set_iam_policy_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeDisksSetIamPolicyArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/disks/{}/setIamPolicy",
        args.project, args.zone, args.resource,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Policy =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/zones/{zone}/disks/{resource}/setLabels
// -----------------------------------------------------------------------------

/// POST projects/{project}/zones/{zone}/disks/{resource}/setLabels.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_disks_set_labels_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_disks_set_labels_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeDisksSetLabelsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/disks/{}/setLabels",
        args.project, args.zone, args.resource,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/zones/{zone}/disks/{disk}/startAsyncReplication
// -----------------------------------------------------------------------------

/// POST projects/{project}/zones/{zone}/disks/{disk}/startAsyncReplication.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_disks_start_async_replication_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_disks_start_async_replication_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeDisksStartAsyncReplicationArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/disks/{}/startAsyncReplication",
        args.project,
        args.zone,
        args.disk,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/zones/{zone}/disks/{disk}/stopAsyncReplication
// -----------------------------------------------------------------------------

/// POST projects/{project}/zones/{zone}/disks/{disk}/stopAsyncReplication.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_disks_stop_async_replication_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_disks_stop_async_replication_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeDisksStopAsyncReplicationArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/disks/{}/stopAsyncReplication",
        args.project,
        args.zone,
        args.disk,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/zones/{zone}/disks/stopGroupAsyncReplication
// -----------------------------------------------------------------------------

/// POST projects/{project}/zones/{zone}/disks/stopGroupAsyncReplication.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_disks_stop_group_async_replication_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_disks_stop_group_async_replication_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeDisksStopGroupAsyncReplicationArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/disks/stopGroupAsyncReplication",
        args.project,
        args.zone,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/zones/{zone}/disks/{resource}/testIamPermissions
// -----------------------------------------------------------------------------

/// POST projects/{project}/zones/{zone}/disks/{resource}/testIamPermissions.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_disks_test_iam_permissions_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_disks_test_iam_permissions_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeDisksTestIamPermissionsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TestPermissionsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/disks/{}/testIamPermissions",
        args.project,
        args.zone,
        args.resource,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: TestPermissionsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PATCH projects/{project}/zones/{zone}/disks/{disk}
// -----------------------------------------------------------------------------

/// PATCH projects/{project}/zones/{zone}/disks/{disk}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_disks_update_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_disks_update_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeDisksUpdateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/disks/{}",
        args.project, args.zone, args.disk,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.paths {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("paths=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.update_mask {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("updateMask=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .patch(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/zones/{zone}/disks/{disk}/updateKmsKey
// -----------------------------------------------------------------------------

/// POST projects/{project}/zones/{zone}/disks/{disk}/updateKmsKey.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_disks_update_kms_key_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_disks_update_kms_key_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeDisksUpdateKmsKeyArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/disks/{}/updateKmsKey",
        args.project, args.zone, args.disk,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE projects/{project}/global/externalVpnGateways/{externalVpnGateway}
// -----------------------------------------------------------------------------

/// DELETE projects/{project}/global/externalVpnGateways/{externalVpnGateway}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_external_vpn_gateways_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_external_vpn_gateways_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeExternalVpnGatewaysDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/externalVpnGateways/{}",
        args.project, args.external_vpn_gateway,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/global/externalVpnGateways/{externalVpnGateway}
// -----------------------------------------------------------------------------

/// GET projects/{project}/global/externalVpnGateways/{externalVpnGateway}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_external_vpn_gateways_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_external_vpn_gateways_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeExternalVpnGatewaysGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ExternalVpnGateway>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/externalVpnGateways/{}",
        args.project, args.external_vpn_gateway,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: ExternalVpnGateway =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/global/externalVpnGateways
// -----------------------------------------------------------------------------

/// POST projects/{project}/global/externalVpnGateways.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_external_vpn_gateways_insert_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_external_vpn_gateways_insert_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeExternalVpnGatewaysInsertArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/externalVpnGateways",
        args.project,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/global/externalVpnGateways
// -----------------------------------------------------------------------------

/// GET projects/{project}/global/externalVpnGateways.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_external_vpn_gateways_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_external_vpn_gateways_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeExternalVpnGatewaysListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ExternalVpnGatewayList>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/externalVpnGateways",
        args.project,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.order_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orderBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.return_partial_success {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("returnPartialSuccess=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: ExternalVpnGatewayList =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/global/externalVpnGateways/{resource}/setLabels
// -----------------------------------------------------------------------------

/// POST projects/{project}/global/externalVpnGateways/{resource}/setLabels.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_external_vpn_gateways_set_labels_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_external_vpn_gateways_set_labels_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeExternalVpnGatewaysSetLabelsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/externalVpnGateways/{}/setLabels",
        args.project,
        args.resource,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/global/externalVpnGateways/{resource}/testIamPermissions
// -----------------------------------------------------------------------------

/// POST projects/{project}/global/externalVpnGateways/{resource}/testIamPermissions.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_external_vpn_gateways_test_iam_permissions_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_external_vpn_gateways_test_iam_permissions_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeExternalVpnGatewaysTestIamPermissionsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TestPermissionsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/externalVpnGateways/{}/testIamPermissions",
        args.project,
        args.resource,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: TestPermissionsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST locations/global/firewallPolicies/{firewallPolicy}/addAssociation
// -----------------------------------------------------------------------------

/// POST locations/global/firewallPolicies/{firewallPolicy}/addAssociation.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_firewall_policies_add_association_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_firewall_policies_add_association_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeFirewallPoliciesAddAssociationArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/locations/global/firewallPolicies/{}/addAssociation",
        args.firewall_policy,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.replace_existing_association {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("replaceExistingAssociation=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST locations/global/firewallPolicies/{firewallPolicy}/addRule
// -----------------------------------------------------------------------------

/// POST locations/global/firewallPolicies/{firewallPolicy}/addRule.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_firewall_policies_add_rule_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_firewall_policies_add_rule_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeFirewallPoliciesAddRuleArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/locations/global/firewallPolicies/{}/addRule",
        args.firewall_policy,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST locations/global/firewallPolicies/{firewallPolicy}/cloneRules
// -----------------------------------------------------------------------------

/// POST locations/global/firewallPolicies/{firewallPolicy}/cloneRules.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_firewall_policies_clone_rules_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_firewall_policies_clone_rules_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeFirewallPoliciesCloneRulesArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/locations/global/firewallPolicies/{}/cloneRules",
        args.firewall_policy,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.source_firewall_policy {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("sourceFirewallPolicy=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE locations/global/firewallPolicies/{firewallPolicy}
// -----------------------------------------------------------------------------

/// DELETE locations/global/firewallPolicies/{firewallPolicy}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_firewall_policies_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_firewall_policies_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeFirewallPoliciesDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/locations/global/firewallPolicies/{}",
        args.firewall_policy,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET locations/global/firewallPolicies/{firewallPolicy}
// -----------------------------------------------------------------------------

/// GET locations/global/firewallPolicies/{firewallPolicy}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_firewall_policies_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_firewall_policies_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeFirewallPoliciesGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<FirewallPolicy>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/locations/global/firewallPolicies/{}",
        args.firewall_policy,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: FirewallPolicy =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET locations/global/firewallPolicies/{firewallPolicy}/getAssociation
// -----------------------------------------------------------------------------

/// GET locations/global/firewallPolicies/{firewallPolicy}/getAssociation.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_firewall_policies_get_association_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_firewall_policies_get_association_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeFirewallPoliciesGetAssociationArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<FirewallPolicyAssociation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/locations/global/firewallPolicies/{}/getAssociation",
        args.firewall_policy,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.name {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("name=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: FirewallPolicyAssociation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET locations/global/firewallPolicies/{resource}/getIamPolicy
// -----------------------------------------------------------------------------

/// GET locations/global/firewallPolicies/{resource}/getIamPolicy.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_firewall_policies_get_iam_policy_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_firewall_policies_get_iam_policy_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeFirewallPoliciesGetIamPolicyArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/locations/global/firewallPolicies/{}/getIamPolicy",
        args.resource,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.options_requested_policy_version {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("optionsRequestedPolicyVersion=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Policy =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET locations/global/firewallPolicies/{firewallPolicy}/getRule
// -----------------------------------------------------------------------------

/// GET locations/global/firewallPolicies/{firewallPolicy}/getRule.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_firewall_policies_get_rule_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_firewall_policies_get_rule_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeFirewallPoliciesGetRuleArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<FirewallPolicyRule>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/locations/global/firewallPolicies/{}/getRule",
        args.firewall_policy,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.priority {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("priority=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: FirewallPolicyRule =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST locations/global/firewallPolicies
// -----------------------------------------------------------------------------

/// POST locations/global/firewallPolicies.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_firewall_policies_insert_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_firewall_policies_insert_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeFirewallPoliciesInsertArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url =
        format!("https://compute.googleapis.com/compute/v1/locations/global/firewallPolicies",);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.parent_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("parentId=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET locations/global/firewallPolicies
// -----------------------------------------------------------------------------

/// GET locations/global/firewallPolicies.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_firewall_policies_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_firewall_policies_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeFirewallPoliciesListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<FirewallPolicyList>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url =
        format!("https://compute.googleapis.com/compute/v1/locations/global/firewallPolicies",);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.order_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orderBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.parent_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("parentId=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.return_partial_success {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("returnPartialSuccess=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: FirewallPolicyList =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET locations/global/firewallPolicies/listAssociations
// -----------------------------------------------------------------------------

/// GET locations/global/firewallPolicies/listAssociations.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_firewall_policies_list_associations_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_firewall_policies_list_associations_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeFirewallPoliciesListAssociationsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<FirewallPoliciesListAssociationsResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/locations/global/firewallPolicies/listAssociations",
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.include_inherited_policies {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("includeInheritedPolicies=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.target_resource {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("targetResource=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: FirewallPoliciesListAssociationsResponse = serde_json::from_str(&body)
                    .map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST locations/global/firewallPolicies/{firewallPolicy}/move
// -----------------------------------------------------------------------------

/// POST locations/global/firewallPolicies/{firewallPolicy}/move.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_firewall_policies_move_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_firewall_policies_move_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeFirewallPoliciesMoveArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/locations/global/firewallPolicies/{}/move",
        args.firewall_policy,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.parent_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("parentId=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PATCH locations/global/firewallPolicies/{firewallPolicy}
// -----------------------------------------------------------------------------

/// PATCH locations/global/firewallPolicies/{firewallPolicy}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_firewall_policies_patch_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_firewall_policies_patch_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeFirewallPoliciesPatchArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/locations/global/firewallPolicies/{}",
        args.firewall_policy,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .patch(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST locations/global/firewallPolicies/{firewallPolicy}/patchRule
// -----------------------------------------------------------------------------

/// POST locations/global/firewallPolicies/{firewallPolicy}/patchRule.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_firewall_policies_patch_rule_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_firewall_policies_patch_rule_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeFirewallPoliciesPatchRuleArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/locations/global/firewallPolicies/{}/patchRule",
        args.firewall_policy,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.priority {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("priority=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST locations/global/firewallPolicies/{firewallPolicy}/removeAssociation
// -----------------------------------------------------------------------------

/// POST locations/global/firewallPolicies/{firewallPolicy}/removeAssociation.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_firewall_policies_remove_association_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_firewall_policies_remove_association_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeFirewallPoliciesRemoveAssociationArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/locations/global/firewallPolicies/{}/removeAssociation",
        args.firewall_policy,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.name {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("name=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST locations/global/firewallPolicies/{firewallPolicy}/removeRule
// -----------------------------------------------------------------------------

/// POST locations/global/firewallPolicies/{firewallPolicy}/removeRule.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_firewall_policies_remove_rule_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_firewall_policies_remove_rule_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeFirewallPoliciesRemoveRuleArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/locations/global/firewallPolicies/{}/removeRule",
        args.firewall_policy,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.priority {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("priority=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST locations/global/firewallPolicies/{resource}/setIamPolicy
// -----------------------------------------------------------------------------

/// POST locations/global/firewallPolicies/{resource}/setIamPolicy.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_firewall_policies_set_iam_policy_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_firewall_policies_set_iam_policy_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeFirewallPoliciesSetIamPolicyArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/locations/global/firewallPolicies/{}/setIamPolicy",
        args.resource,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Policy =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST locations/global/firewallPolicies/{resource}/testIamPermissions
// -----------------------------------------------------------------------------

/// POST locations/global/firewallPolicies/{resource}/testIamPermissions.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_firewall_policies_test_iam_permissions_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_firewall_policies_test_iam_permissions_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeFirewallPoliciesTestIamPermissionsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TestPermissionsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/locations/global/firewallPolicies/{}/testIamPermissions",
        args.resource,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: TestPermissionsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE projects/{project}/global/firewalls/{firewall}
// -----------------------------------------------------------------------------

/// DELETE projects/{project}/global/firewalls/{firewall}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_firewalls_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_firewalls_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeFirewallsDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/firewalls/{}",
        args.project, args.firewall,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/global/firewalls/{firewall}
// -----------------------------------------------------------------------------

/// GET projects/{project}/global/firewalls/{firewall}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_firewalls_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_firewalls_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeFirewallsGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Firewall>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/firewalls/{}",
        args.project, args.firewall,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Firewall =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/global/firewalls
// -----------------------------------------------------------------------------

/// POST projects/{project}/global/firewalls.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_firewalls_insert_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_firewalls_insert_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeFirewallsInsertArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/firewalls",
        args.project,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/global/firewalls
// -----------------------------------------------------------------------------

/// GET projects/{project}/global/firewalls.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_firewalls_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_firewalls_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeFirewallsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<FirewallList>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/firewalls",
        args.project,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.order_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orderBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.return_partial_success {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("returnPartialSuccess=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: FirewallList =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PATCH projects/{project}/global/firewalls/{firewall}
// -----------------------------------------------------------------------------

/// PATCH projects/{project}/global/firewalls/{firewall}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_firewalls_patch_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_firewalls_patch_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeFirewallsPatchArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/firewalls/{}",
        args.project, args.firewall,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .patch(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/global/firewalls/{resource}/testIamPermissions
// -----------------------------------------------------------------------------

/// POST projects/{project}/global/firewalls/{resource}/testIamPermissions.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_firewalls_test_iam_permissions_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_firewalls_test_iam_permissions_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeFirewallsTestIamPermissionsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TestPermissionsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/firewalls/{}/testIamPermissions",
        args.project,
        args.resource,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: TestPermissionsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PUT projects/{project}/global/firewalls/{firewall}
// -----------------------------------------------------------------------------

/// PUT projects/{project}/global/firewalls/{firewall}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_firewalls_update_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_firewalls_update_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeFirewallsUpdateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/firewalls/{}",
        args.project, args.firewall,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .put(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/aggregated/forwardingRules
// -----------------------------------------------------------------------------

/// GET projects/{project}/aggregated/forwardingRules.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_forwarding_rules_aggregated_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_forwarding_rules_aggregated_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeForwardingRulesAggregatedListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ForwardingRuleAggregatedList>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/aggregated/forwardingRules",
        args.project,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.include_all_scopes {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("includeAllScopes=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.order_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orderBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.return_partial_success {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("returnPartialSuccess=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.service_project_number {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("serviceProjectNumber=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: ForwardingRuleAggregatedList =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE projects/{project}/regions/{region}/forwardingRules/{forwardingRule}
// -----------------------------------------------------------------------------

/// DELETE projects/{project}/regions/{region}/forwardingRules/{forwardingRule}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_forwarding_rules_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_forwarding_rules_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeForwardingRulesDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/forwardingRules/{}",
        args.project, args.region, args.forwarding_rule,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/regions/{region}/forwardingRules/{forwardingRule}
// -----------------------------------------------------------------------------

/// GET projects/{project}/regions/{region}/forwardingRules/{forwardingRule}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_forwarding_rules_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_forwarding_rules_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeForwardingRulesGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ForwardingRule>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/forwardingRules/{}",
        args.project, args.region, args.forwarding_rule,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: ForwardingRule =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/regions/{region}/forwardingRules
// -----------------------------------------------------------------------------

/// POST projects/{project}/regions/{region}/forwardingRules.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_forwarding_rules_insert_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_forwarding_rules_insert_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeForwardingRulesInsertArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/forwardingRules",
        args.project, args.region,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/regions/{region}/forwardingRules
// -----------------------------------------------------------------------------

/// GET projects/{project}/regions/{region}/forwardingRules.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_forwarding_rules_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_forwarding_rules_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeForwardingRulesListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ForwardingRuleList>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/forwardingRules",
        args.project, args.region,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.order_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orderBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.return_partial_success {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("returnPartialSuccess=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: ForwardingRuleList =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PATCH projects/{project}/regions/{region}/forwardingRules/{forwardingRule}
// -----------------------------------------------------------------------------

/// PATCH projects/{project}/regions/{region}/forwardingRules/{forwardingRule}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_forwarding_rules_patch_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_forwarding_rules_patch_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeForwardingRulesPatchArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/forwardingRules/{}",
        args.project, args.region, args.forwarding_rule,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .patch(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/regions/{region}/forwardingRules/{resource}/setLabels
// -----------------------------------------------------------------------------

/// POST projects/{project}/regions/{region}/forwardingRules/{resource}/setLabels.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_forwarding_rules_set_labels_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_forwarding_rules_set_labels_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeForwardingRulesSetLabelsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/forwardingRules/{}/setLabels",
        args.project,
        args.region,
        args.resource,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/regions/{region}/forwardingRules/{forwardingRule}/setTarget
// -----------------------------------------------------------------------------

/// POST projects/{project}/regions/{region}/forwardingRules/{forwardingRule}/setTarget.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_forwarding_rules_set_target_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_forwarding_rules_set_target_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeForwardingRulesSetTargetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/forwardingRules/{}/setTarget",
        args.project,
        args.region,
        args.forwarding_rule,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/aggregated/futureReservations
// -----------------------------------------------------------------------------

/// GET projects/{project}/aggregated/futureReservations.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_future_reservations_aggregated_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_future_reservations_aggregated_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeFutureReservationsAggregatedListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<FutureReservationsAggregatedListResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/aggregated/futureReservations",
        args.project,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.include_all_scopes {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("includeAllScopes=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.order_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orderBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.return_partial_success {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("returnPartialSuccess=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.service_project_number {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("serviceProjectNumber=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: FutureReservationsAggregatedListResponse = serde_json::from_str(&body)
                    .map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/zones/{zone}/futureReservations/{futureReservation}/cancel
// -----------------------------------------------------------------------------

/// POST projects/{project}/zones/{zone}/futureReservations/{futureReservation}/cancel.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_future_reservations_cancel_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_future_reservations_cancel_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeFutureReservationsCancelArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/futureReservations/{}/cancel",
        args.project,
        args.zone,
        args.future_reservation,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE projects/{project}/zones/{zone}/futureReservations/{futureReservation}
// -----------------------------------------------------------------------------

/// DELETE projects/{project}/zones/{zone}/futureReservations/{futureReservation}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_future_reservations_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_future_reservations_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeFutureReservationsDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/futureReservations/{}",
        args.project, args.zone, args.future_reservation,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/zones/{zone}/futureReservations/{futureReservation}
// -----------------------------------------------------------------------------

/// GET projects/{project}/zones/{zone}/futureReservations/{futureReservation}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_future_reservations_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_future_reservations_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeFutureReservationsGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<FutureReservation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/futureReservations/{}",
        args.project, args.zone, args.future_reservation,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: FutureReservation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/zones/{zone}/futureReservations
// -----------------------------------------------------------------------------

/// POST projects/{project}/zones/{zone}/futureReservations.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_future_reservations_insert_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_future_reservations_insert_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeFutureReservationsInsertArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/futureReservations",
        args.project, args.zone,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/zones/{zone}/futureReservations
// -----------------------------------------------------------------------------

/// GET projects/{project}/zones/{zone}/futureReservations.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_future_reservations_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_future_reservations_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeFutureReservationsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<FutureReservationsListResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/futureReservations",
        args.project, args.zone,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.order_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orderBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.return_partial_success {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("returnPartialSuccess=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: FutureReservationsListResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PATCH projects/{project}/zones/{zone}/futureReservations/{futureReservation}
// -----------------------------------------------------------------------------

/// PATCH projects/{project}/zones/{zone}/futureReservations/{futureReservation}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_future_reservations_update_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_future_reservations_update_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeFutureReservationsUpdateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/futureReservations/{}",
        args.project, args.zone, args.future_reservation,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.update_mask {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("updateMask=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .patch(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE projects/{project}/global/addresses/{address}
// -----------------------------------------------------------------------------

/// DELETE projects/{project}/global/addresses/{address}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_global_addresses_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_global_addresses_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeGlobalAddressesDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/addresses/{}",
        args.project, args.address,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/global/addresses/{address}
// -----------------------------------------------------------------------------

/// GET projects/{project}/global/addresses/{address}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_global_addresses_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_global_addresses_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeGlobalAddressesGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Address>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/addresses/{}",
        args.project, args.address,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Address =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/global/addresses
// -----------------------------------------------------------------------------

/// POST projects/{project}/global/addresses.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_global_addresses_insert_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_global_addresses_insert_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeGlobalAddressesInsertArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/addresses",
        args.project,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/global/addresses
// -----------------------------------------------------------------------------

/// GET projects/{project}/global/addresses.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_global_addresses_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_global_addresses_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeGlobalAddressesListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<AddressList>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/addresses",
        args.project,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.order_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orderBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.return_partial_success {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("returnPartialSuccess=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: AddressList =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/global/addresses/{address}/move
// -----------------------------------------------------------------------------

/// POST projects/{project}/global/addresses/{address}/move.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_global_addresses_move_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_global_addresses_move_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeGlobalAddressesMoveArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/addresses/{}/move",
        args.project, args.address,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/global/addresses/{resource}/setLabels
// -----------------------------------------------------------------------------

/// POST projects/{project}/global/addresses/{resource}/setLabels.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_global_addresses_set_labels_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_global_addresses_set_labels_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeGlobalAddressesSetLabelsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/addresses/{}/setLabels",
        args.project, args.resource,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/global/addresses/{resource}/testIamPermissions
// -----------------------------------------------------------------------------

/// POST projects/{project}/global/addresses/{resource}/testIamPermissions.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_global_addresses_test_iam_permissions_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_global_addresses_test_iam_permissions_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeGlobalAddressesTestIamPermissionsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TestPermissionsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/addresses/{}/testIamPermissions",
        args.project,
        args.resource,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: TestPermissionsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE projects/{project}/global/forwardingRules/{forwardingRule}
// -----------------------------------------------------------------------------

/// DELETE projects/{project}/global/forwardingRules/{forwardingRule}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_global_forwarding_rules_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_global_forwarding_rules_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeGlobalForwardingRulesDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/forwardingRules/{}",
        args.project, args.forwarding_rule,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/global/forwardingRules/{forwardingRule}
// -----------------------------------------------------------------------------

/// GET projects/{project}/global/forwardingRules/{forwardingRule}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_global_forwarding_rules_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_global_forwarding_rules_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeGlobalForwardingRulesGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ForwardingRule>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/forwardingRules/{}",
        args.project, args.forwarding_rule,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: ForwardingRule =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/global/forwardingRules
// -----------------------------------------------------------------------------

/// POST projects/{project}/global/forwardingRules.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_global_forwarding_rules_insert_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_global_forwarding_rules_insert_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeGlobalForwardingRulesInsertArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/forwardingRules",
        args.project,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/global/forwardingRules
// -----------------------------------------------------------------------------

/// GET projects/{project}/global/forwardingRules.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_global_forwarding_rules_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_global_forwarding_rules_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeGlobalForwardingRulesListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ForwardingRuleList>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/forwardingRules",
        args.project,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.order_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orderBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.return_partial_success {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("returnPartialSuccess=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: ForwardingRuleList =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PATCH projects/{project}/global/forwardingRules/{forwardingRule}
// -----------------------------------------------------------------------------

/// PATCH projects/{project}/global/forwardingRules/{forwardingRule}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_global_forwarding_rules_patch_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_global_forwarding_rules_patch_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeGlobalForwardingRulesPatchArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/forwardingRules/{}",
        args.project, args.forwarding_rule,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .patch(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/global/forwardingRules/{resource}/setLabels
// -----------------------------------------------------------------------------

/// POST projects/{project}/global/forwardingRules/{resource}/setLabels.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_global_forwarding_rules_set_labels_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_global_forwarding_rules_set_labels_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeGlobalForwardingRulesSetLabelsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/forwardingRules/{}/setLabels",
        args.project, args.resource,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/global/forwardingRules/{forwardingRule}/setTarget
// -----------------------------------------------------------------------------

/// POST projects/{project}/global/forwardingRules/{forwardingRule}/setTarget.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_global_forwarding_rules_set_target_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_global_forwarding_rules_set_target_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeGlobalForwardingRulesSetTargetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/forwardingRules/{}/setTarget",
        args.project, args.forwarding_rule,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/global/networkEndpointGroups/{networkEndpointGroup}/attachNetworkEndpoints
// -----------------------------------------------------------------------------

/// POST projects/{project}/global/networkEndpointGroups/{networkEndpointGroup}/attachNetworkEndpoints.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_global_network_endpoint_groups_attach_network_endpoints_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_global_network_endpoint_groups_attach_network_endpoints_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeGlobalNetworkEndpointGroupsAttachNetworkEndpointsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/networkEndpointGroups/{}/attachNetworkEndpoints",
        args.project,
        args.network_endpoint_group,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE projects/{project}/global/networkEndpointGroups/{networkEndpointGroup}
// -----------------------------------------------------------------------------

/// DELETE projects/{project}/global/networkEndpointGroups/{networkEndpointGroup}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_global_network_endpoint_groups_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_global_network_endpoint_groups_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeGlobalNetworkEndpointGroupsDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/networkEndpointGroups/{}",
        args.project, args.network_endpoint_group,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/global/networkEndpointGroups/{networkEndpointGroup}/detachNetworkEndpoints
// -----------------------------------------------------------------------------

/// POST projects/{project}/global/networkEndpointGroups/{networkEndpointGroup}/detachNetworkEndpoints.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_global_network_endpoint_groups_detach_network_endpoints_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_global_network_endpoint_groups_detach_network_endpoints_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeGlobalNetworkEndpointGroupsDetachNetworkEndpointsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/networkEndpointGroups/{}/detachNetworkEndpoints",
        args.project,
        args.network_endpoint_group,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/global/networkEndpointGroups/{networkEndpointGroup}
// -----------------------------------------------------------------------------

/// GET projects/{project}/global/networkEndpointGroups/{networkEndpointGroup}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_global_network_endpoint_groups_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_global_network_endpoint_groups_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeGlobalNetworkEndpointGroupsGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<NetworkEndpointGroup>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/networkEndpointGroups/{}",
        args.project, args.network_endpoint_group,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: NetworkEndpointGroup =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/global/networkEndpointGroups
// -----------------------------------------------------------------------------

/// POST projects/{project}/global/networkEndpointGroups.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_global_network_endpoint_groups_insert_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_global_network_endpoint_groups_insert_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeGlobalNetworkEndpointGroupsInsertArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/networkEndpointGroups",
        args.project,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/global/networkEndpointGroups
// -----------------------------------------------------------------------------

/// GET projects/{project}/global/networkEndpointGroups.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_global_network_endpoint_groups_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_global_network_endpoint_groups_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeGlobalNetworkEndpointGroupsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<NetworkEndpointGroupList>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/networkEndpointGroups",
        args.project,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.order_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orderBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.return_partial_success {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("returnPartialSuccess=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: NetworkEndpointGroupList =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/global/networkEndpointGroups/{networkEndpointGroup}/listNetworkEndpoints
// -----------------------------------------------------------------------------

/// POST projects/{project}/global/networkEndpointGroups/{networkEndpointGroup}/listNetworkEndpoints.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_global_network_endpoint_groups_list_network_endpoints_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_global_network_endpoint_groups_list_network_endpoints_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeGlobalNetworkEndpointGroupsListNetworkEndpointsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<NetworkEndpointGroupsListNetworkEndpoints>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/networkEndpointGroups/{}/listNetworkEndpoints",
        args.project,
        args.network_endpoint_group,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.order_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orderBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.return_partial_success {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("returnPartialSuccess=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: NetworkEndpointGroupsListNetworkEndpoints = serde_json::from_str(&body)
                    .map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/aggregated/operations
// -----------------------------------------------------------------------------

/// GET projects/{project}/aggregated/operations.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_global_operations_aggregated_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_global_operations_aggregated_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeGlobalOperationsAggregatedListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<OperationAggregatedList>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/aggregated/operations",
        args.project,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.include_all_scopes {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("includeAllScopes=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.order_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orderBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.return_partial_success {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("returnPartialSuccess=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.service_project_number {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("serviceProjectNumber=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: OperationAggregatedList =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE projects/{project}/global/operations/{operation}
// -----------------------------------------------------------------------------

/// DELETE projects/{project}/global/operations/{operation}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_global_operations_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_global_operations_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeGlobalOperationsDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/operations/{}",
        args.project, args.operation,
    );

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream: _,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/global/operations/{operation}
// -----------------------------------------------------------------------------

/// GET projects/{project}/global/operations/{operation}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_global_operations_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_global_operations_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeGlobalOperationsGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/operations/{}",
        args.project, args.operation,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/global/operations
// -----------------------------------------------------------------------------

/// GET projects/{project}/global/operations.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_global_operations_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_global_operations_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeGlobalOperationsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<OperationList>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/operations",
        args.project,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.order_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orderBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.return_partial_success {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("returnPartialSuccess=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: OperationList =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/global/operations/{operation}/wait
// -----------------------------------------------------------------------------

/// POST projects/{project}/global/operations/{operation}/wait.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_global_operations_wait_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_global_operations_wait_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeGlobalOperationsWaitArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/operations/{}/wait",
        args.project, args.operation,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE locations/global/operations/{operation}
// -----------------------------------------------------------------------------

/// DELETE locations/global/operations/{operation}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_global_organization_operations_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_global_organization_operations_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeGlobalOrganizationOperationsDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/locations/global/operations/{}",
        args.operation,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.parent_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("parentId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream: _,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET locations/global/operations/{operation}
// -----------------------------------------------------------------------------

/// GET locations/global/operations/{operation}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_global_organization_operations_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_global_organization_operations_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeGlobalOrganizationOperationsGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/locations/global/operations/{}",
        args.operation,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.parent_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("parentId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET locations/global/operations
// -----------------------------------------------------------------------------

/// GET locations/global/operations.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_global_organization_operations_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_global_organization_operations_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeGlobalOrganizationOperationsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<OperationList>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url =
        format!("https://compute.googleapis.com/compute/v1/locations/global/operations",);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.order_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orderBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.parent_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("parentId=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.return_partial_success {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("returnPartialSuccess=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: OperationList =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE projects/{project}/global/publicDelegatedPrefixes/{publicDelegatedPrefix}
// -----------------------------------------------------------------------------

/// DELETE projects/{project}/global/publicDelegatedPrefixes/{publicDelegatedPrefix}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_global_public_delegated_prefixes_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_global_public_delegated_prefixes_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeGlobalPublicDelegatedPrefixesDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/publicDelegatedPrefixes/{}",
        args.project, args.public_delegated_prefix,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/global/publicDelegatedPrefixes/{publicDelegatedPrefix}
// -----------------------------------------------------------------------------

/// GET projects/{project}/global/publicDelegatedPrefixes/{publicDelegatedPrefix}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_global_public_delegated_prefixes_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_global_public_delegated_prefixes_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeGlobalPublicDelegatedPrefixesGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<PublicDelegatedPrefix>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/publicDelegatedPrefixes/{}",
        args.project, args.public_delegated_prefix,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: PublicDelegatedPrefix =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/global/publicDelegatedPrefixes
// -----------------------------------------------------------------------------

/// POST projects/{project}/global/publicDelegatedPrefixes.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_global_public_delegated_prefixes_insert_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_global_public_delegated_prefixes_insert_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeGlobalPublicDelegatedPrefixesInsertArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/publicDelegatedPrefixes",
        args.project,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/global/publicDelegatedPrefixes
// -----------------------------------------------------------------------------

/// GET projects/{project}/global/publicDelegatedPrefixes.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_global_public_delegated_prefixes_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_global_public_delegated_prefixes_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeGlobalPublicDelegatedPrefixesListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<PublicDelegatedPrefixList>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/publicDelegatedPrefixes",
        args.project,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.order_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orderBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.return_partial_success {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("returnPartialSuccess=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: PublicDelegatedPrefixList =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PATCH projects/{project}/global/publicDelegatedPrefixes/{publicDelegatedPrefix}
// -----------------------------------------------------------------------------

/// PATCH projects/{project}/global/publicDelegatedPrefixes/{publicDelegatedPrefix}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_global_public_delegated_prefixes_patch_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_global_public_delegated_prefixes_patch_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeGlobalPublicDelegatedPrefixesPatchArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/publicDelegatedPrefixes/{}",
        args.project, args.public_delegated_prefix,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .patch(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/aggregated/healthChecks
// -----------------------------------------------------------------------------

/// GET projects/{project}/aggregated/healthChecks.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_health_checks_aggregated_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_health_checks_aggregated_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeHealthChecksAggregatedListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<HealthChecksAggregatedList>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/aggregated/healthChecks",
        args.project,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.include_all_scopes {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("includeAllScopes=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.order_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orderBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.return_partial_success {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("returnPartialSuccess=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.service_project_number {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("serviceProjectNumber=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: HealthChecksAggregatedList =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE projects/{project}/global/healthChecks/{healthCheck}
// -----------------------------------------------------------------------------

/// DELETE projects/{project}/global/healthChecks/{healthCheck}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_health_checks_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_health_checks_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeHealthChecksDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/healthChecks/{}",
        args.project, args.health_check,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/global/healthChecks/{healthCheck}
// -----------------------------------------------------------------------------

/// GET projects/{project}/global/healthChecks/{healthCheck}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_health_checks_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_health_checks_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeHealthChecksGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<HealthCheck>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/healthChecks/{}",
        args.project, args.health_check,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: HealthCheck =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/global/healthChecks
// -----------------------------------------------------------------------------

/// POST projects/{project}/global/healthChecks.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_health_checks_insert_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_health_checks_insert_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeHealthChecksInsertArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/healthChecks",
        args.project,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/global/healthChecks
// -----------------------------------------------------------------------------

/// GET projects/{project}/global/healthChecks.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_health_checks_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_health_checks_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeHealthChecksListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<HealthCheckList>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/healthChecks",
        args.project,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.order_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orderBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.return_partial_success {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("returnPartialSuccess=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: HealthCheckList =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PATCH projects/{project}/global/healthChecks/{healthCheck}
// -----------------------------------------------------------------------------

/// PATCH projects/{project}/global/healthChecks/{healthCheck}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_health_checks_patch_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_health_checks_patch_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeHealthChecksPatchArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/healthChecks/{}",
        args.project, args.health_check,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .patch(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/global/healthChecks/{resource}/testIamPermissions
// -----------------------------------------------------------------------------

/// POST projects/{project}/global/healthChecks/{resource}/testIamPermissions.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_health_checks_test_iam_permissions_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_health_checks_test_iam_permissions_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeHealthChecksTestIamPermissionsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TestPermissionsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/healthChecks/{}/testIamPermissions",
        args.project,
        args.resource,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: TestPermissionsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PUT projects/{project}/global/healthChecks/{healthCheck}
// -----------------------------------------------------------------------------

/// PUT projects/{project}/global/healthChecks/{healthCheck}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_health_checks_update_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_health_checks_update_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeHealthChecksUpdateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/healthChecks/{}",
        args.project, args.health_check,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .put(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE projects/{project}/global/httpHealthChecks/{httpHealthCheck}
// -----------------------------------------------------------------------------

/// DELETE projects/{project}/global/httpHealthChecks/{httpHealthCheck}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_http_health_checks_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_http_health_checks_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeHttpHealthChecksDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/httpHealthChecks/{}",
        args.project, args.http_health_check,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/global/httpHealthChecks/{httpHealthCheck}
// -----------------------------------------------------------------------------

/// GET projects/{project}/global/httpHealthChecks/{httpHealthCheck}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_http_health_checks_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_http_health_checks_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeHttpHealthChecksGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<HttpHealthCheck>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/httpHealthChecks/{}",
        args.project, args.http_health_check,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: HttpHealthCheck =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/global/httpHealthChecks
// -----------------------------------------------------------------------------

/// POST projects/{project}/global/httpHealthChecks.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_http_health_checks_insert_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_http_health_checks_insert_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeHttpHealthChecksInsertArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/httpHealthChecks",
        args.project,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/global/httpHealthChecks
// -----------------------------------------------------------------------------

/// GET projects/{project}/global/httpHealthChecks.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_http_health_checks_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_http_health_checks_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeHttpHealthChecksListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<HttpHealthCheckList>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/httpHealthChecks",
        args.project,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.order_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orderBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.return_partial_success {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("returnPartialSuccess=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: HttpHealthCheckList =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PATCH projects/{project}/global/httpHealthChecks/{httpHealthCheck}
// -----------------------------------------------------------------------------

/// PATCH projects/{project}/global/httpHealthChecks/{httpHealthCheck}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_http_health_checks_patch_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_http_health_checks_patch_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeHttpHealthChecksPatchArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/httpHealthChecks/{}",
        args.project, args.http_health_check,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .patch(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/global/httpHealthChecks/{resource}/testIamPermissions
// -----------------------------------------------------------------------------

/// POST projects/{project}/global/httpHealthChecks/{resource}/testIamPermissions.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_http_health_checks_test_iam_permissions_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_http_health_checks_test_iam_permissions_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeHttpHealthChecksTestIamPermissionsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TestPermissionsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/httpHealthChecks/{}/testIamPermissions",
        args.project,
        args.resource,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: TestPermissionsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PUT projects/{project}/global/httpHealthChecks/{httpHealthCheck}
// -----------------------------------------------------------------------------

/// PUT projects/{project}/global/httpHealthChecks/{httpHealthCheck}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_http_health_checks_update_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_http_health_checks_update_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeHttpHealthChecksUpdateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/httpHealthChecks/{}",
        args.project, args.http_health_check,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .put(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE projects/{project}/global/httpsHealthChecks/{httpsHealthCheck}
// -----------------------------------------------------------------------------

/// DELETE projects/{project}/global/httpsHealthChecks/{httpsHealthCheck}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_https_health_checks_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_https_health_checks_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeHttpsHealthChecksDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/httpsHealthChecks/{}",
        args.project, args.https_health_check,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/global/httpsHealthChecks/{httpsHealthCheck}
// -----------------------------------------------------------------------------

/// GET projects/{project}/global/httpsHealthChecks/{httpsHealthCheck}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_https_health_checks_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_https_health_checks_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeHttpsHealthChecksGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<HttpsHealthCheck>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/httpsHealthChecks/{}",
        args.project, args.https_health_check,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: HttpsHealthCheck =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/global/httpsHealthChecks
// -----------------------------------------------------------------------------

/// POST projects/{project}/global/httpsHealthChecks.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_https_health_checks_insert_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_https_health_checks_insert_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeHttpsHealthChecksInsertArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/httpsHealthChecks",
        args.project,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/global/httpsHealthChecks
// -----------------------------------------------------------------------------

/// GET projects/{project}/global/httpsHealthChecks.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_https_health_checks_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_https_health_checks_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeHttpsHealthChecksListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<HttpsHealthCheckList>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/httpsHealthChecks",
        args.project,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.order_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orderBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.return_partial_success {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("returnPartialSuccess=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: HttpsHealthCheckList =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PATCH projects/{project}/global/httpsHealthChecks/{httpsHealthCheck}
// -----------------------------------------------------------------------------

/// PATCH projects/{project}/global/httpsHealthChecks/{httpsHealthCheck}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_https_health_checks_patch_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_https_health_checks_patch_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeHttpsHealthChecksPatchArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/httpsHealthChecks/{}",
        args.project, args.https_health_check,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .patch(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/global/httpsHealthChecks/{resource}/testIamPermissions
// -----------------------------------------------------------------------------

/// POST projects/{project}/global/httpsHealthChecks/{resource}/testIamPermissions.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_https_health_checks_test_iam_permissions_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_https_health_checks_test_iam_permissions_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeHttpsHealthChecksTestIamPermissionsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TestPermissionsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/httpsHealthChecks/{}/testIamPermissions",
        args.project,
        args.resource,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: TestPermissionsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PUT projects/{project}/global/httpsHealthChecks/{httpsHealthCheck}
// -----------------------------------------------------------------------------

/// PUT projects/{project}/global/httpsHealthChecks/{httpsHealthCheck}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_https_health_checks_update_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_https_health_checks_update_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeHttpsHealthChecksUpdateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/httpsHealthChecks/{}",
        args.project, args.https_health_check,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .put(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/zones/{zone}/imageFamilyViews/{family}
// -----------------------------------------------------------------------------

/// GET projects/{project}/zones/{zone}/imageFamilyViews/{family}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_image_family_views_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_image_family_views_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeImageFamilyViewsGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ImageFamilyView>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/imageFamilyViews/{}",
        args.project, args.zone, args.family,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: ImageFamilyView =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE projects/{project}/global/images/{image}
// -----------------------------------------------------------------------------

/// DELETE projects/{project}/global/images/{image}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_images_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_images_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeImagesDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/images/{}",
        args.project, args.image,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/global/images/{image}/deprecate
// -----------------------------------------------------------------------------

/// POST projects/{project}/global/images/{image}/deprecate.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_images_deprecate_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_images_deprecate_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeImagesDeprecateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/images/{}/deprecate",
        args.project, args.image,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/global/images/{image}
// -----------------------------------------------------------------------------

/// GET projects/{project}/global/images/{image}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_images_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_images_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeImagesGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Image>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/images/{}",
        args.project, args.image,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Image =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/global/images/family/{family}
// -----------------------------------------------------------------------------

/// GET projects/{project}/global/images/family/{family}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_images_get_from_family_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_images_get_from_family_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeImagesGetFromFamilyArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Image>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/images/family/{}",
        args.project, args.family,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Image =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/global/images/{resource}/getIamPolicy
// -----------------------------------------------------------------------------

/// GET projects/{project}/global/images/{resource}/getIamPolicy.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_images_get_iam_policy_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_images_get_iam_policy_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeImagesGetIamPolicyArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/images/{}/getIamPolicy",
        args.project, args.resource,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.options_requested_policy_version {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("optionsRequestedPolicyVersion=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Policy =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/global/images
// -----------------------------------------------------------------------------

/// POST projects/{project}/global/images.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_images_insert_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_images_insert_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeImagesInsertArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/images",
        args.project,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.force_create {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("forceCreate=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/global/images
// -----------------------------------------------------------------------------

/// GET projects/{project}/global/images.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_images_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_images_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeImagesListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ImageList>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/images",
        args.project,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.order_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orderBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.return_partial_success {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("returnPartialSuccess=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: ImageList =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PATCH projects/{project}/global/images/{image}
// -----------------------------------------------------------------------------

/// PATCH projects/{project}/global/images/{image}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_images_patch_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_images_patch_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeImagesPatchArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/images/{}",
        args.project, args.image,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .patch(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/global/images/{resource}/setIamPolicy
// -----------------------------------------------------------------------------

/// POST projects/{project}/global/images/{resource}/setIamPolicy.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_images_set_iam_policy_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_images_set_iam_policy_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeImagesSetIamPolicyArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/images/{}/setIamPolicy",
        args.project, args.resource,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Policy =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/global/images/{resource}/setLabels
// -----------------------------------------------------------------------------

/// POST projects/{project}/global/images/{resource}/setLabels.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_images_set_labels_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_images_set_labels_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeImagesSetLabelsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/images/{}/setLabels",
        args.project, args.resource,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/global/images/{resource}/testIamPermissions
// -----------------------------------------------------------------------------

/// POST projects/{project}/global/images/{resource}/testIamPermissions.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_images_test_iam_permissions_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_images_test_iam_permissions_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeImagesTestIamPermissionsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TestPermissionsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/images/{}/testIamPermissions",
        args.project, args.resource,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: TestPermissionsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}/resizeRequests/{resizeRequest}/cancel
// -----------------------------------------------------------------------------

/// POST projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}/resizeRequests/{resizeRequest}/cancel.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_instance_group_manager_resize_requests_cancel_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_instance_group_manager_resize_requests_cancel_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeInstanceGroupManagerResizeRequestsCancelArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/instanceGroupManagers/{}/resizeRequests/{}/cancel",
        args.project,
        args.zone,
        args.instance_group_manager,
        args.resize_request,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}/resizeRequests/{resizeRequest}
// -----------------------------------------------------------------------------

/// DELETE projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}/resizeRequests/{resizeRequest}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_instance_group_manager_resize_requests_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_instance_group_manager_resize_requests_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeInstanceGroupManagerResizeRequestsDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/instanceGroupManagers/{}/resizeRequests/{}",
        args.project,
        args.zone,
        args.instance_group_manager,
        args.resize_request,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}/resizeRequests/{resizeRequest}
// -----------------------------------------------------------------------------

/// GET projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}/resizeRequests/{resizeRequest}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_instance_group_manager_resize_requests_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_instance_group_manager_resize_requests_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeInstanceGroupManagerResizeRequestsGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<InstanceGroupManagerResizeRequest>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/instanceGroupManagers/{}/resizeRequests/{}",
        args.project,
        args.zone,
        args.instance_group_manager,
        args.resize_request,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: InstanceGroupManagerResizeRequest = serde_json::from_str(&body)
                    .map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}/resizeRequests
// -----------------------------------------------------------------------------

/// POST projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}/resizeRequests.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_instance_group_manager_resize_requests_insert_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_instance_group_manager_resize_requests_insert_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeInstanceGroupManagerResizeRequestsInsertArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/instanceGroupManagers/{}/resizeRequests",
        args.project,
        args.zone,
        args.instance_group_manager,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}/resizeRequests
// -----------------------------------------------------------------------------

/// GET projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}/resizeRequests.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_instance_group_manager_resize_requests_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_instance_group_manager_resize_requests_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeInstanceGroupManagerResizeRequestsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<InstanceGroupManagerResizeRequestsListResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/instanceGroupManagers/{}/resizeRequests",
        args.project,
        args.zone,
        args.instance_group_manager,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.order_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orderBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.return_partial_success {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("returnPartialSuccess=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: InstanceGroupManagerResizeRequestsListResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}/abandonInstances
// -----------------------------------------------------------------------------

/// POST projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}/abandonInstances.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_instance_group_managers_abandon_instances_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_instance_group_managers_abandon_instances_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeInstanceGroupManagersAbandonInstancesArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/instanceGroupManagers/{}/abandonInstances",
        args.project,
        args.zone,
        args.instance_group_manager,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/aggregated/instanceGroupManagers
// -----------------------------------------------------------------------------

/// GET projects/{project}/aggregated/instanceGroupManagers.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_instance_group_managers_aggregated_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_instance_group_managers_aggregated_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeInstanceGroupManagersAggregatedListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<InstanceGroupManagerAggregatedList>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/aggregated/instanceGroupManagers",
        args.project,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.include_all_scopes {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("includeAllScopes=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.order_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orderBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.return_partial_success {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("returnPartialSuccess=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.service_project_number {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("serviceProjectNumber=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: InstanceGroupManagerAggregatedList = serde_json::from_str(&body)
                    .map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}/applyUpdatesToInstances
// -----------------------------------------------------------------------------

/// POST projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}/applyUpdatesToInstances.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_instance_group_managers_apply_updates_to_instances_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_instance_group_managers_apply_updates_to_instances_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeInstanceGroupManagersApplyUpdatesToInstancesArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/instanceGroupManagers/{}/applyUpdatesToInstances",
        args.project,
        args.zone,
        args.instance_group_manager,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}/createInstances
// -----------------------------------------------------------------------------

/// POST projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}/createInstances.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_instance_group_managers_create_instances_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_instance_group_managers_create_instances_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeInstanceGroupManagersCreateInstancesArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/instanceGroupManagers/{}/createInstances",
        args.project,
        args.zone,
        args.instance_group_manager,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}
// -----------------------------------------------------------------------------

/// DELETE projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_instance_group_managers_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_instance_group_managers_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeInstanceGroupManagersDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/instanceGroupManagers/{}",
        args.project, args.zone, args.instance_group_manager,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}/deleteInstances
// -----------------------------------------------------------------------------

/// POST projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}/deleteInstances.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_instance_group_managers_delete_instances_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_instance_group_managers_delete_instances_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeInstanceGroupManagersDeleteInstancesArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/instanceGroupManagers/{}/deleteInstances",
        args.project,
        args.zone,
        args.instance_group_manager,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}/deletePerInstanceConfigs
// -----------------------------------------------------------------------------

/// POST projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}/deletePerInstanceConfigs.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_instance_group_managers_delete_per_instance_configs_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_instance_group_managers_delete_per_instance_configs_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeInstanceGroupManagersDeletePerInstanceConfigsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/instanceGroupManagers/{}/deletePerInstanceConfigs",
        args.project,
        args.zone,
        args.instance_group_manager,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}
// -----------------------------------------------------------------------------

/// GET projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_instance_group_managers_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_instance_group_managers_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeInstanceGroupManagersGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<InstanceGroupManager>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/instanceGroupManagers/{}",
        args.project, args.zone, args.instance_group_manager,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: InstanceGroupManager =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/zones/{zone}/instanceGroupManagers
// -----------------------------------------------------------------------------

/// POST projects/{project}/zones/{zone}/instanceGroupManagers.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_instance_group_managers_insert_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_instance_group_managers_insert_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeInstanceGroupManagersInsertArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/instanceGroupManagers",
        args.project, args.zone,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/zones/{zone}/instanceGroupManagers
// -----------------------------------------------------------------------------

/// GET projects/{project}/zones/{zone}/instanceGroupManagers.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_instance_group_managers_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_instance_group_managers_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeInstanceGroupManagersListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<InstanceGroupManagerList>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/instanceGroupManagers",
        args.project, args.zone,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.order_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orderBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.return_partial_success {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("returnPartialSuccess=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: InstanceGroupManagerList =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}/listErrors
// -----------------------------------------------------------------------------

/// GET projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}/listErrors.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_instance_group_managers_list_errors_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_instance_group_managers_list_errors_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeInstanceGroupManagersListErrorsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<InstanceGroupManagersListErrorsResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/instanceGroupManagers/{}/listErrors",
        args.project,
        args.zone,
        args.instance_group_manager,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.order_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orderBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.return_partial_success {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("returnPartialSuccess=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: InstanceGroupManagersListErrorsResponse = serde_json::from_str(&body)
                    .map_err(
                    |e: serde_json::Error| super::shared::ApiError::ParseFailed(e.to_string()),
                )?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}/listManagedInstances
// -----------------------------------------------------------------------------

/// POST projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}/listManagedInstances.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_instance_group_managers_list_managed_instances_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_instance_group_managers_list_managed_instances_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeInstanceGroupManagersListManagedInstancesArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<InstanceGroupManagersListManagedInstancesResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/instanceGroupManagers/{}/listManagedInstances",
        args.project,
        args.zone,
        args.instance_group_manager,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.order_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orderBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.return_partial_success {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("returnPartialSuccess=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: InstanceGroupManagersListManagedInstancesResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}/listPerInstanceConfigs
// -----------------------------------------------------------------------------

/// POST projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}/listPerInstanceConfigs.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_instance_group_managers_list_per_instance_configs_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_instance_group_managers_list_per_instance_configs_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeInstanceGroupManagersListPerInstanceConfigsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<InstanceGroupManagersListPerInstanceConfigsResp>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/instanceGroupManagers/{}/listPerInstanceConfigs",
        args.project,
        args.zone,
        args.instance_group_manager,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.order_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orderBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.return_partial_success {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("returnPartialSuccess=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: InstanceGroupManagersListPerInstanceConfigsResp =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PATCH projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}
// -----------------------------------------------------------------------------

/// PATCH projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_instance_group_managers_patch_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_instance_group_managers_patch_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeInstanceGroupManagersPatchArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/instanceGroupManagers/{}",
        args.project, args.zone, args.instance_group_manager,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .patch(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}/patchPerInstanceConfigs
// -----------------------------------------------------------------------------

/// POST projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}/patchPerInstanceConfigs.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_instance_group_managers_patch_per_instance_configs_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_instance_group_managers_patch_per_instance_configs_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeInstanceGroupManagersPatchPerInstanceConfigsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/instanceGroupManagers/{}/patchPerInstanceConfigs",
        args.project,
        args.zone,
        args.instance_group_manager,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}/recreateInstances
// -----------------------------------------------------------------------------

/// POST projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}/recreateInstances.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_instance_group_managers_recreate_instances_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_instance_group_managers_recreate_instances_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeInstanceGroupManagersRecreateInstancesArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/instanceGroupManagers/{}/recreateInstances",
        args.project,
        args.zone,
        args.instance_group_manager,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}/resize
// -----------------------------------------------------------------------------

/// POST projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}/resize.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_instance_group_managers_resize_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_instance_group_managers_resize_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeInstanceGroupManagersResizeArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/instanceGroupManagers/{}/resize",
        args.project,
        args.zone,
        args.instance_group_manager,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("size=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}/resumeInstances
// -----------------------------------------------------------------------------

/// POST projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}/resumeInstances.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_instance_group_managers_resume_instances_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_instance_group_managers_resume_instances_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeInstanceGroupManagersResumeInstancesArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/instanceGroupManagers/{}/resumeInstances",
        args.project,
        args.zone,
        args.instance_group_manager,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}/setInstanceTemplate
// -----------------------------------------------------------------------------

/// POST projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}/setInstanceTemplate.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_instance_group_managers_set_instance_template_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_instance_group_managers_set_instance_template_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeInstanceGroupManagersSetInstanceTemplateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/instanceGroupManagers/{}/setInstanceTemplate",
        args.project,
        args.zone,
        args.instance_group_manager,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}/setTargetPools
// -----------------------------------------------------------------------------

/// POST projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}/setTargetPools.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_instance_group_managers_set_target_pools_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_instance_group_managers_set_target_pools_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeInstanceGroupManagersSetTargetPoolsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/instanceGroupManagers/{}/setTargetPools",
        args.project,
        args.zone,
        args.instance_group_manager,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}/startInstances
// -----------------------------------------------------------------------------

/// POST projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}/startInstances.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_instance_group_managers_start_instances_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_instance_group_managers_start_instances_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeInstanceGroupManagersStartInstancesArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/instanceGroupManagers/{}/startInstances",
        args.project,
        args.zone,
        args.instance_group_manager,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}/stopInstances
// -----------------------------------------------------------------------------

/// POST projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}/stopInstances.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_instance_group_managers_stop_instances_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_instance_group_managers_stop_instances_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeInstanceGroupManagersStopInstancesArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/instanceGroupManagers/{}/stopInstances",
        args.project,
        args.zone,
        args.instance_group_manager,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}/suspendInstances
// -----------------------------------------------------------------------------

/// POST projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}/suspendInstances.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_instance_group_managers_suspend_instances_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_instance_group_managers_suspend_instances_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeInstanceGroupManagersSuspendInstancesArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/instanceGroupManagers/{}/suspendInstances",
        args.project,
        args.zone,
        args.instance_group_manager,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}/updatePerInstanceConfigs
// -----------------------------------------------------------------------------

/// POST projects/{project}/zones/{zone}/instanceGroupManagers/{instanceGroupManager}/updatePerInstanceConfigs.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_instance_group_managers_update_per_instance_configs_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_instance_group_managers_update_per_instance_configs_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeInstanceGroupManagersUpdatePerInstanceConfigsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/instanceGroupManagers/{}/updatePerInstanceConfigs",
        args.project,
        args.zone,
        args.instance_group_manager,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/zones/{zone}/instanceGroups/{instanceGroup}/addInstances
// -----------------------------------------------------------------------------

/// POST projects/{project}/zones/{zone}/instanceGroups/{instanceGroup}/addInstances.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_instance_groups_add_instances_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_instance_groups_add_instances_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeInstanceGroupsAddInstancesArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/instanceGroups/{}/addInstances",
        args.project,
        args.zone,
        args.instance_group,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/aggregated/instanceGroups
// -----------------------------------------------------------------------------

/// GET projects/{project}/aggregated/instanceGroups.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_instance_groups_aggregated_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_instance_groups_aggregated_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeInstanceGroupsAggregatedListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<InstanceGroupAggregatedList>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/aggregated/instanceGroups",
        args.project,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.include_all_scopes {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("includeAllScopes=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.order_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orderBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.return_partial_success {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("returnPartialSuccess=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.service_project_number {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("serviceProjectNumber=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: InstanceGroupAggregatedList =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE projects/{project}/zones/{zone}/instanceGroups/{instanceGroup}
// -----------------------------------------------------------------------------

/// DELETE projects/{project}/zones/{zone}/instanceGroups/{instanceGroup}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_instance_groups_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_instance_groups_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeInstanceGroupsDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/instanceGroups/{}",
        args.project, args.zone, args.instance_group,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/zones/{zone}/instanceGroups/{instanceGroup}
// -----------------------------------------------------------------------------

/// GET projects/{project}/zones/{zone}/instanceGroups/{instanceGroup}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_instance_groups_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_instance_groups_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeInstanceGroupsGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<InstanceGroup>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/instanceGroups/{}",
        args.project, args.zone, args.instance_group,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: InstanceGroup =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/zones/{zone}/instanceGroups
// -----------------------------------------------------------------------------

/// POST projects/{project}/zones/{zone}/instanceGroups.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_instance_groups_insert_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_instance_groups_insert_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeInstanceGroupsInsertArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/instanceGroups",
        args.project, args.zone,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/zones/{zone}/instanceGroups
// -----------------------------------------------------------------------------

/// GET projects/{project}/zones/{zone}/instanceGroups.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_instance_groups_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_instance_groups_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeInstanceGroupsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<InstanceGroupList>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/instanceGroups",
        args.project, args.zone,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.order_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orderBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.return_partial_success {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("returnPartialSuccess=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: InstanceGroupList =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/zones/{zone}/instanceGroups/{instanceGroup}/listInstances
// -----------------------------------------------------------------------------

/// POST projects/{project}/zones/{zone}/instanceGroups/{instanceGroup}/listInstances.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_instance_groups_list_instances_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_instance_groups_list_instances_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeInstanceGroupsListInstancesArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<InstanceGroupsListInstances>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/instanceGroups/{}/listInstances",
        args.project,
        args.zone,
        args.instance_group,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.order_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orderBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.return_partial_success {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("returnPartialSuccess=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: InstanceGroupsListInstances =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/zones/{zone}/instanceGroups/{instanceGroup}/removeInstances
// -----------------------------------------------------------------------------

/// POST projects/{project}/zones/{zone}/instanceGroups/{instanceGroup}/removeInstances.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_instance_groups_remove_instances_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_instance_groups_remove_instances_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeInstanceGroupsRemoveInstancesArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/instanceGroups/{}/removeInstances",
        args.project,
        args.zone,
        args.instance_group,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/zones/{zone}/instanceGroups/{instanceGroup}/setNamedPorts
// -----------------------------------------------------------------------------

/// POST projects/{project}/zones/{zone}/instanceGroups/{instanceGroup}/setNamedPorts.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_instance_groups_set_named_ports_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_instance_groups_set_named_ports_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeInstanceGroupsSetNamedPortsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/instanceGroups/{}/setNamedPorts",
        args.project,
        args.zone,
        args.instance_group,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/zones/{zone}/instanceGroups/{resource}/testIamPermissions
// -----------------------------------------------------------------------------

/// POST projects/{project}/zones/{zone}/instanceGroups/{resource}/testIamPermissions.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_instance_groups_test_iam_permissions_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_instance_groups_test_iam_permissions_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeInstanceGroupsTestIamPermissionsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TestPermissionsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/instanceGroups/{}/testIamPermissions",
        args.project,
        args.zone,
        args.resource,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: TestPermissionsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/zones/{zone}/instanceSettings
// -----------------------------------------------------------------------------

/// GET projects/{project}/zones/{zone}/instanceSettings.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_instance_settings_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_instance_settings_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeInstanceSettingsGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<InstanceSettings>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/instanceSettings",
        args.project, args.zone,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: InstanceSettings =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PATCH projects/{project}/zones/{zone}/instanceSettings
// -----------------------------------------------------------------------------

/// PATCH projects/{project}/zones/{zone}/instanceSettings.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_instance_settings_patch_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_instance_settings_patch_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeInstanceSettingsPatchArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/instanceSettings",
        args.project, args.zone,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.update_mask {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("updateMask=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .patch(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/aggregated/instanceTemplates
// -----------------------------------------------------------------------------

/// GET projects/{project}/aggregated/instanceTemplates.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_instance_templates_aggregated_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_instance_templates_aggregated_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeInstanceTemplatesAggregatedListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<InstanceTemplateAggregatedList>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/aggregated/instanceTemplates",
        args.project,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.include_all_scopes {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("includeAllScopes=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.order_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orderBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.return_partial_success {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("returnPartialSuccess=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.service_project_number {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("serviceProjectNumber=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: InstanceTemplateAggregatedList =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE projects/{project}/global/instanceTemplates/{instanceTemplate}
// -----------------------------------------------------------------------------

/// DELETE projects/{project}/global/instanceTemplates/{instanceTemplate}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_instance_templates_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_instance_templates_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeInstanceTemplatesDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/instanceTemplates/{}",
        args.project, args.instance_template,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/global/instanceTemplates/{instanceTemplate}
// -----------------------------------------------------------------------------

/// GET projects/{project}/global/instanceTemplates/{instanceTemplate}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_instance_templates_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_instance_templates_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeInstanceTemplatesGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<InstanceTemplate>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/instanceTemplates/{}",
        args.project, args.instance_template,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: InstanceTemplate =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/global/instanceTemplates/{resource}/getIamPolicy
// -----------------------------------------------------------------------------

/// GET projects/{project}/global/instanceTemplates/{resource}/getIamPolicy.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_instance_templates_get_iam_policy_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_instance_templates_get_iam_policy_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeInstanceTemplatesGetIamPolicyArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/instanceTemplates/{}/getIamPolicy",
        args.project,
        args.resource,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.options_requested_policy_version {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("optionsRequestedPolicyVersion=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Policy =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/global/instanceTemplates
// -----------------------------------------------------------------------------

/// POST projects/{project}/global/instanceTemplates.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_instance_templates_insert_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_instance_templates_insert_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeInstanceTemplatesInsertArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/instanceTemplates",
        args.project,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/global/instanceTemplates
// -----------------------------------------------------------------------------

/// GET projects/{project}/global/instanceTemplates.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_instance_templates_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_instance_templates_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeInstanceTemplatesListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<InstanceTemplateList>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/instanceTemplates",
        args.project,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.order_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orderBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.return_partial_success {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("returnPartialSuccess=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: InstanceTemplateList =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/global/instanceTemplates/{resource}/setIamPolicy
// -----------------------------------------------------------------------------

/// POST projects/{project}/global/instanceTemplates/{resource}/setIamPolicy.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_instance_templates_set_iam_policy_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_instance_templates_set_iam_policy_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeInstanceTemplatesSetIamPolicyArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/instanceTemplates/{}/setIamPolicy",
        args.project,
        args.resource,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Policy =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/global/instanceTemplates/{resource}/testIamPermissions
// -----------------------------------------------------------------------------

/// POST projects/{project}/global/instanceTemplates/{resource}/testIamPermissions.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_instance_templates_test_iam_permissions_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_instance_templates_test_iam_permissions_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeInstanceTemplatesTestIamPermissionsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TestPermissionsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/instanceTemplates/{}/testIamPermissions",
        args.project,
        args.resource,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: TestPermissionsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/zones/{zone}/instances/{instance}/addAccessConfig
// -----------------------------------------------------------------------------

/// POST projects/{project}/zones/{zone}/instances/{instance}/addAccessConfig.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_instances_add_access_config_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_instances_add_access_config_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeInstancesAddAccessConfigArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/instances/{}/addAccessConfig",
        args.project,
        args.zone,
        args.instance,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.network_interface {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("networkInterface=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/zones/{zone}/instances/{instance}/addNetworkInterface
// -----------------------------------------------------------------------------

/// POST projects/{project}/zones/{zone}/instances/{instance}/addNetworkInterface.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_instances_add_network_interface_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_instances_add_network_interface_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeInstancesAddNetworkInterfaceArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/instances/{}/addNetworkInterface",
        args.project,
        args.zone,
        args.instance,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/zones/{zone}/instances/{instance}/addResourcePolicies
// -----------------------------------------------------------------------------

/// POST projects/{project}/zones/{zone}/instances/{instance}/addResourcePolicies.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_instances_add_resource_policies_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_instances_add_resource_policies_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeInstancesAddResourcePoliciesArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/instances/{}/addResourcePolicies",
        args.project,
        args.zone,
        args.instance,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/aggregated/instances
// -----------------------------------------------------------------------------

/// GET projects/{project}/aggregated/instances.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_instances_aggregated_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_instances_aggregated_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeInstancesAggregatedListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<InstanceAggregatedList>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/aggregated/instances",
        args.project,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.include_all_scopes {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("includeAllScopes=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.order_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orderBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.return_partial_success {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("returnPartialSuccess=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.service_project_number {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("serviceProjectNumber=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: InstanceAggregatedList =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/zones/{zone}/instances/{instance}/attachDisk
// -----------------------------------------------------------------------------

/// POST projects/{project}/zones/{zone}/instances/{instance}/attachDisk.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_instances_attach_disk_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_instances_attach_disk_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeInstancesAttachDiskArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/instances/{}/attachDisk",
        args.project, args.zone, args.instance,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.force_attach {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("forceAttach=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/zones/{zone}/instances/bulkInsert
// -----------------------------------------------------------------------------

/// POST projects/{project}/zones/{zone}/instances/bulkInsert.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_instances_bulk_insert_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_instances_bulk_insert_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeInstancesBulkInsertArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/instances/bulkInsert",
        args.project, args.zone,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE projects/{project}/zones/{zone}/instances/{instance}
// -----------------------------------------------------------------------------

/// DELETE projects/{project}/zones/{zone}/instances/{instance}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_instances_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_instances_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeInstancesDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/instances/{}",
        args.project, args.zone, args.instance,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/zones/{zone}/instances/{instance}/deleteAccessConfig
// -----------------------------------------------------------------------------

/// POST projects/{project}/zones/{zone}/instances/{instance}/deleteAccessConfig.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_instances_delete_access_config_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_instances_delete_access_config_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeInstancesDeleteAccessConfigArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/instances/{}/deleteAccessConfig",
        args.project,
        args.zone,
        args.instance,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.access_config {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("accessConfig=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.network_interface {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("networkInterface=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/zones/{zone}/instances/{instance}/deleteNetworkInterface
// -----------------------------------------------------------------------------

/// POST projects/{project}/zones/{zone}/instances/{instance}/deleteNetworkInterface.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_instances_delete_network_interface_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_instances_delete_network_interface_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeInstancesDeleteNetworkInterfaceArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/instances/{}/deleteNetworkInterface",
        args.project,
        args.zone,
        args.instance,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.network_interface_name {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("networkInterfaceName=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/zones/{zone}/instances/{instance}/detachDisk
// -----------------------------------------------------------------------------

/// POST projects/{project}/zones/{zone}/instances/{instance}/detachDisk.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_instances_detach_disk_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_instances_detach_disk_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeInstancesDetachDiskArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/instances/{}/detachDisk",
        args.project, args.zone, args.instance,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.device_name {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("deviceName=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/zones/{zone}/instances/{instance}
// -----------------------------------------------------------------------------

/// GET projects/{project}/zones/{zone}/instances/{instance}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_instances_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_instances_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeInstancesGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Instance>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/instances/{}",
        args.project, args.zone, args.instance,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Instance =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/zones/{zone}/instances/{instance}/getEffectiveFirewalls
// -----------------------------------------------------------------------------

/// GET projects/{project}/zones/{zone}/instances/{instance}/getEffectiveFirewalls.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_instances_get_effective_firewalls_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_instances_get_effective_firewalls_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeInstancesGetEffectiveFirewallsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<InstancesGetEffectiveFirewallsResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/instances/{}/getEffectiveFirewalls",
        args.project,
        args.zone,
        args.instance,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.network_interface {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("networkInterface=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: InstancesGetEffectiveFirewallsResponse = serde_json::from_str(&body)
                    .map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/zones/{zone}/instances/{instance}/getGuestAttributes
// -----------------------------------------------------------------------------

/// GET projects/{project}/zones/{zone}/instances/{instance}/getGuestAttributes.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_instances_get_guest_attributes_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_instances_get_guest_attributes_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeInstancesGetGuestAttributesArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GuestAttributes>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/instances/{}/getGuestAttributes",
        args.project,
        args.zone,
        args.instance,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.query_path {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("queryPath=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.variable_key {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("variableKey=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: GuestAttributes =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/zones/{zone}/instances/{resource}/getIamPolicy
// -----------------------------------------------------------------------------

/// GET projects/{project}/zones/{zone}/instances/{resource}/getIamPolicy.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_instances_get_iam_policy_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_instances_get_iam_policy_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeInstancesGetIamPolicyArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/instances/{}/getIamPolicy",
        args.project, args.zone, args.resource,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.options_requested_policy_version {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("optionsRequestedPolicyVersion=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Policy =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/zones/{zone}/instances/{instance}/screenshot
// -----------------------------------------------------------------------------

/// GET projects/{project}/zones/{zone}/instances/{instance}/screenshot.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_instances_get_screenshot_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_instances_get_screenshot_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeInstancesGetScreenshotArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Screenshot>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/instances/{}/screenshot",
        args.project, args.zone, args.instance,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Screenshot =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/zones/{zone}/instances/{instance}/serialPort
// -----------------------------------------------------------------------------

/// GET projects/{project}/zones/{zone}/instances/{instance}/serialPort.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_instances_get_serial_port_output_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_instances_get_serial_port_output_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeInstancesGetSerialPortOutputArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SerialPortOutput>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/instances/{}/serialPort",
        args.project, args.zone, args.instance,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.port {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("port=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.start {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("start=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: SerialPortOutput =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/zones/{zone}/instances/{instance}/getShieldedInstanceIdentity
// -----------------------------------------------------------------------------

/// GET projects/{project}/zones/{zone}/instances/{instance}/getShieldedInstanceIdentity.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_instances_get_shielded_instance_identity_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_instances_get_shielded_instance_identity_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeInstancesGetShieldedInstanceIdentityArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ShieldedInstanceIdentity>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/instances/{}/getShieldedInstanceIdentity",
        args.project,
        args.zone,
        args.instance,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: ShieldedInstanceIdentity =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/zones/{zone}/instances
// -----------------------------------------------------------------------------

/// POST projects/{project}/zones/{zone}/instances.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_instances_insert_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_instances_insert_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeInstancesInsertArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/instances",
        args.project, args.zone,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.source_instance_template {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("sourceInstanceTemplate=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.source_machine_image {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("sourceMachineImage=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/zones/{zone}/instances
// -----------------------------------------------------------------------------

/// GET projects/{project}/zones/{zone}/instances.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_instances_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_instances_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeInstancesListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<InstanceList>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/instances",
        args.project, args.zone,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.order_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orderBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.return_partial_success {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("returnPartialSuccess=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: InstanceList =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/zones/{zone}/instances/{instance}/referrers
// -----------------------------------------------------------------------------

/// GET projects/{project}/zones/{zone}/instances/{instance}/referrers.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_instances_list_referrers_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_instances_list_referrers_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeInstancesListReferrersArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<InstanceListReferrers>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/instances/{}/referrers",
        args.project, args.zone, args.instance,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.order_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orderBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.return_partial_success {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("returnPartialSuccess=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: InstanceListReferrers =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/zones/{zone}/instances/{instance}/performMaintenance
// -----------------------------------------------------------------------------

/// POST projects/{project}/zones/{zone}/instances/{instance}/performMaintenance.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_instances_perform_maintenance_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_instances_perform_maintenance_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeInstancesPerformMaintenanceArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/instances/{}/performMaintenance",
        args.project,
        args.zone,
        args.instance,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/zones/{zone}/instances/{instance}/removeResourcePolicies
// -----------------------------------------------------------------------------

/// POST projects/{project}/zones/{zone}/instances/{instance}/removeResourcePolicies.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_instances_remove_resource_policies_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_instances_remove_resource_policies_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeInstancesRemoveResourcePoliciesArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/instances/{}/removeResourcePolicies",
        args.project,
        args.zone,
        args.instance,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/zones/{zone}/instances/{instance}/reportHostAsFaulty
// -----------------------------------------------------------------------------

/// POST projects/{project}/zones/{zone}/instances/{instance}/reportHostAsFaulty.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_instances_report_host_as_faulty_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_instances_report_host_as_faulty_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeInstancesReportHostAsFaultyArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/instances/{}/reportHostAsFaulty",
        args.project,
        args.zone,
        args.instance,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/zones/{zone}/instances/{instance}/reset
// -----------------------------------------------------------------------------

/// POST projects/{project}/zones/{zone}/instances/{instance}/reset.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_instances_reset_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_instances_reset_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeInstancesResetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/instances/{}/reset",
        args.project, args.zone, args.instance,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/zones/{zone}/instances/{instance}/resume
// -----------------------------------------------------------------------------

/// POST projects/{project}/zones/{zone}/instances/{instance}/resume.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_instances_resume_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_instances_resume_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeInstancesResumeArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/instances/{}/resume",
        args.project, args.zone, args.instance,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/zones/{zone}/instances/{instance}/sendDiagnosticInterrupt
// -----------------------------------------------------------------------------

/// POST projects/{project}/zones/{zone}/instances/{instance}/sendDiagnosticInterrupt.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_instances_send_diagnostic_interrupt_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_instances_send_diagnostic_interrupt_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeInstancesSendDiagnosticInterruptArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/instances/{}/sendDiagnosticInterrupt",
        args.project,
        args.zone,
        args.instance,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream: _,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/zones/{zone}/instances/{resource}/setDeletionProtection
// -----------------------------------------------------------------------------

/// POST projects/{project}/zones/{zone}/instances/{resource}/setDeletionProtection.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_instances_set_deletion_protection_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_instances_set_deletion_protection_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeInstancesSetDeletionProtectionArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/instances/{}/setDeletionProtection",
        args.project,
        args.zone,
        args.resource,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.deletion_protection {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("deletionProtection=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/zones/{zone}/instances/{instance}/setDiskAutoDelete
// -----------------------------------------------------------------------------

/// POST projects/{project}/zones/{zone}/instances/{instance}/setDiskAutoDelete.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_instances_set_disk_auto_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_instances_set_disk_auto_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeInstancesSetDiskAutoDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/instances/{}/setDiskAutoDelete",
        args.project,
        args.zone,
        args.instance,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.auto_delete {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("autoDelete=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.device_name {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("deviceName=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/zones/{zone}/instances/{resource}/setIamPolicy
// -----------------------------------------------------------------------------

/// POST projects/{project}/zones/{zone}/instances/{resource}/setIamPolicy.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_instances_set_iam_policy_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_instances_set_iam_policy_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeInstancesSetIamPolicyArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/instances/{}/setIamPolicy",
        args.project, args.zone, args.resource,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Policy =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/zones/{zone}/instances/{instance}/setLabels
// -----------------------------------------------------------------------------

/// POST projects/{project}/zones/{zone}/instances/{instance}/setLabels.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_instances_set_labels_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_instances_set_labels_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeInstancesSetLabelsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/instances/{}/setLabels",
        args.project, args.zone, args.instance,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/zones/{zone}/instances/{instance}/setMachineResources
// -----------------------------------------------------------------------------

/// POST projects/{project}/zones/{zone}/instances/{instance}/setMachineResources.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_instances_set_machine_resources_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_instances_set_machine_resources_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeInstancesSetMachineResourcesArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/instances/{}/setMachineResources",
        args.project,
        args.zone,
        args.instance,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/zones/{zone}/instances/{instance}/setMachineType
// -----------------------------------------------------------------------------

/// POST projects/{project}/zones/{zone}/instances/{instance}/setMachineType.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_instances_set_machine_type_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_instances_set_machine_type_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeInstancesSetMachineTypeArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/instances/{}/setMachineType",
        args.project,
        args.zone,
        args.instance,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/zones/{zone}/instances/{instance}/setMetadata
// -----------------------------------------------------------------------------

/// POST projects/{project}/zones/{zone}/instances/{instance}/setMetadata.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_instances_set_metadata_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_instances_set_metadata_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeInstancesSetMetadataArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/instances/{}/setMetadata",
        args.project, args.zone, args.instance,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/zones/{zone}/instances/{instance}/setMinCpuPlatform
// -----------------------------------------------------------------------------

/// POST projects/{project}/zones/{zone}/instances/{instance}/setMinCpuPlatform.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_instances_set_min_cpu_platform_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_instances_set_min_cpu_platform_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeInstancesSetMinCpuPlatformArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/instances/{}/setMinCpuPlatform",
        args.project,
        args.zone,
        args.instance,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/zones/{zone}/instances/{instance}/setName
// -----------------------------------------------------------------------------

/// POST projects/{project}/zones/{zone}/instances/{instance}/setName.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_instances_set_name_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_instances_set_name_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeInstancesSetNameArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/instances/{}/setName",
        args.project, args.zone, args.instance,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/zones/{zone}/instances/{instance}/setScheduling
// -----------------------------------------------------------------------------

/// POST projects/{project}/zones/{zone}/instances/{instance}/setScheduling.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_instances_set_scheduling_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_instances_set_scheduling_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeInstancesSetSchedulingArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/instances/{}/setScheduling",
        args.project, args.zone, args.instance,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/zones/{zone}/instances/{instance}/setSecurityPolicy
// -----------------------------------------------------------------------------

/// POST projects/{project}/zones/{zone}/instances/{instance}/setSecurityPolicy.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_instances_set_security_policy_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_instances_set_security_policy_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeInstancesSetSecurityPolicyArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/instances/{}/setSecurityPolicy",
        args.project,
        args.zone,
        args.instance,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/zones/{zone}/instances/{instance}/setServiceAccount
// -----------------------------------------------------------------------------

/// POST projects/{project}/zones/{zone}/instances/{instance}/setServiceAccount.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_instances_set_service_account_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_instances_set_service_account_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeInstancesSetServiceAccountArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/instances/{}/setServiceAccount",
        args.project,
        args.zone,
        args.instance,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PATCH projects/{project}/zones/{zone}/instances/{instance}/setShieldedInstanceIntegrityPolicy
// -----------------------------------------------------------------------------

/// PATCH projects/{project}/zones/{zone}/instances/{instance}/setShieldedInstanceIntegrityPolicy.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_instances_set_shielded_instance_integrity_policy_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_instances_set_shielded_instance_integrity_policy_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeInstancesSetShieldedInstanceIntegrityPolicyArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/instances/{}/setShieldedInstanceIntegrityPolicy",
        args.project,
        args.zone,
        args.instance,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .patch(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/zones/{zone}/instances/{instance}/setTags
// -----------------------------------------------------------------------------

/// POST projects/{project}/zones/{zone}/instances/{instance}/setTags.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_instances_set_tags_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_instances_set_tags_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeInstancesSetTagsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/instances/{}/setTags",
        args.project, args.zone, args.instance,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/zones/{zone}/instances/{instance}/simulateMaintenanceEvent
// -----------------------------------------------------------------------------

/// POST projects/{project}/zones/{zone}/instances/{instance}/simulateMaintenanceEvent.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_instances_simulate_maintenance_event_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_instances_simulate_maintenance_event_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeInstancesSimulateMaintenanceEventArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/instances/{}/simulateMaintenanceEvent",
        args.project,
        args.zone,
        args.instance,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.with_extended_notifications {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("withExtendedNotifications=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/zones/{zone}/instances/{instance}/start
// -----------------------------------------------------------------------------

/// POST projects/{project}/zones/{zone}/instances/{instance}/start.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_instances_start_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_instances_start_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeInstancesStartArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/instances/{}/start",
        args.project, args.zone, args.instance,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/zones/{zone}/instances/{instance}/startWithEncryptionKey
// -----------------------------------------------------------------------------

/// POST projects/{project}/zones/{zone}/instances/{instance}/startWithEncryptionKey.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_instances_start_with_encryption_key_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_instances_start_with_encryption_key_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeInstancesStartWithEncryptionKeyArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/instances/{}/startWithEncryptionKey",
        args.project,
        args.zone,
        args.instance,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/zones/{zone}/instances/{instance}/stop
// -----------------------------------------------------------------------------

/// POST projects/{project}/zones/{zone}/instances/{instance}/stop.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_instances_stop_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_instances_stop_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeInstancesStopArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/instances/{}/stop",
        args.project, args.zone, args.instance,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.discard_local_ssd {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("discardLocalSsd=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/zones/{zone}/instances/{instance}/suspend
// -----------------------------------------------------------------------------

/// POST projects/{project}/zones/{zone}/instances/{instance}/suspend.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_instances_suspend_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_instances_suspend_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeInstancesSuspendArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/instances/{}/suspend",
        args.project, args.zone, args.instance,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.discard_local_ssd {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("discardLocalSsd=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/zones/{zone}/instances/{resource}/testIamPermissions
// -----------------------------------------------------------------------------

/// POST projects/{project}/zones/{zone}/instances/{resource}/testIamPermissions.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_instances_test_iam_permissions_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_instances_test_iam_permissions_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeInstancesTestIamPermissionsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TestPermissionsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/instances/{}/testIamPermissions",
        args.project,
        args.zone,
        args.resource,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: TestPermissionsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PUT projects/{project}/zones/{zone}/instances/{instance}
// -----------------------------------------------------------------------------

/// PUT projects/{project}/zones/{zone}/instances/{instance}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_instances_update_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_instances_update_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeInstancesUpdateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/instances/{}",
        args.project, args.zone, args.instance,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.discard_local_ssd {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("discardLocalSsd=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.minimal_action {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("minimalAction=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.most_disruptive_allowed_action {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("mostDisruptiveAllowedAction=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .put(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/zones/{zone}/instances/{instance}/updateAccessConfig
// -----------------------------------------------------------------------------

/// POST projects/{project}/zones/{zone}/instances/{instance}/updateAccessConfig.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_instances_update_access_config_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_instances_update_access_config_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeInstancesUpdateAccessConfigArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/instances/{}/updateAccessConfig",
        args.project,
        args.zone,
        args.instance,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.network_interface {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("networkInterface=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PATCH projects/{project}/zones/{zone}/instances/{instance}/updateDisplayDevice
// -----------------------------------------------------------------------------

/// PATCH projects/{project}/zones/{zone}/instances/{instance}/updateDisplayDevice.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_instances_update_display_device_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_instances_update_display_device_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeInstancesUpdateDisplayDeviceArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/instances/{}/updateDisplayDevice",
        args.project,
        args.zone,
        args.instance,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .patch(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PATCH projects/{project}/zones/{zone}/instances/{instance}/updateNetworkInterface
// -----------------------------------------------------------------------------

/// PATCH projects/{project}/zones/{zone}/instances/{instance}/updateNetworkInterface.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_instances_update_network_interface_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_instances_update_network_interface_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeInstancesUpdateNetworkInterfaceArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/instances/{}/updateNetworkInterface",
        args.project,
        args.zone,
        args.instance,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.network_interface {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("networkInterface=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .patch(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PATCH projects/{project}/zones/{zone}/instances/{instance}/updateShieldedInstanceConfig
// -----------------------------------------------------------------------------

/// PATCH projects/{project}/zones/{zone}/instances/{instance}/updateShieldedInstanceConfig.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_instances_update_shielded_instance_config_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_instances_update_shielded_instance_config_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeInstancesUpdateShieldedInstanceConfigArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/instances/{}/updateShieldedInstanceConfig",
        args.project,
        args.zone,
        args.instance,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .patch(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE projects/{project}/zones/{zone}/instantSnapshotGroups/{instantSnapshotGroup}
// -----------------------------------------------------------------------------

/// DELETE projects/{project}/zones/{zone}/instantSnapshotGroups/{instantSnapshotGroup}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_instant_snapshot_groups_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_instant_snapshot_groups_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeInstantSnapshotGroupsDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/instantSnapshotGroups/{}",
        args.project, args.zone, args.instant_snapshot_group,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/zones/{zone}/instantSnapshotGroups/{instantSnapshotGroup}
// -----------------------------------------------------------------------------

/// GET projects/{project}/zones/{zone}/instantSnapshotGroups/{instantSnapshotGroup}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_instant_snapshot_groups_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_instant_snapshot_groups_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeInstantSnapshotGroupsGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<InstantSnapshotGroup>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/instantSnapshotGroups/{}",
        args.project, args.zone, args.instant_snapshot_group,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: InstantSnapshotGroup =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/zones/{zone}/instantSnapshotGroups/{resource}/getIamPolicy
// -----------------------------------------------------------------------------

/// GET projects/{project}/zones/{zone}/instantSnapshotGroups/{resource}/getIamPolicy.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_instant_snapshot_groups_get_iam_policy_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_instant_snapshot_groups_get_iam_policy_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeInstantSnapshotGroupsGetIamPolicyArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/instantSnapshotGroups/{}/getIamPolicy",
        args.project,
        args.zone,
        args.resource,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.options_requested_policy_version {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("optionsRequestedPolicyVersion=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Policy =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/zones/{zone}/instantSnapshotGroups
// -----------------------------------------------------------------------------

/// POST projects/{project}/zones/{zone}/instantSnapshotGroups.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_instant_snapshot_groups_insert_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_instant_snapshot_groups_insert_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeInstantSnapshotGroupsInsertArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/instantSnapshotGroups",
        args.project, args.zone,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.source_consistency_group {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("sourceConsistencyGroup=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/zones/{zone}/instantSnapshotGroups
// -----------------------------------------------------------------------------

/// GET projects/{project}/zones/{zone}/instantSnapshotGroups.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_instant_snapshot_groups_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_instant_snapshot_groups_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeInstantSnapshotGroupsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListInstantSnapshotGroups>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/instantSnapshotGroups",
        args.project, args.zone,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.order_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orderBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.return_partial_success {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("returnPartialSuccess=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: ListInstantSnapshotGroups =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/zones/{zone}/instantSnapshotGroups/{resource}/setIamPolicy
// -----------------------------------------------------------------------------

/// POST projects/{project}/zones/{zone}/instantSnapshotGroups/{resource}/setIamPolicy.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_instant_snapshot_groups_set_iam_policy_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_instant_snapshot_groups_set_iam_policy_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeInstantSnapshotGroupsSetIamPolicyArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/instantSnapshotGroups/{}/setIamPolicy",
        args.project,
        args.zone,
        args.resource,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Policy =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/zones/{zone}/instantSnapshotGroups/{resource}/testIamPermissions
// -----------------------------------------------------------------------------

/// POST projects/{project}/zones/{zone}/instantSnapshotGroups/{resource}/testIamPermissions.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_instant_snapshot_groups_test_iam_permissions_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_instant_snapshot_groups_test_iam_permissions_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeInstantSnapshotGroupsTestIamPermissionsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TestPermissionsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/instantSnapshotGroups/{}/testIamPermissions",
        args.project,
        args.zone,
        args.resource,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: TestPermissionsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/aggregated/instantSnapshots
// -----------------------------------------------------------------------------

/// GET projects/{project}/aggregated/instantSnapshots.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_instant_snapshots_aggregated_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_instant_snapshots_aggregated_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeInstantSnapshotsAggregatedListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<InstantSnapshotAggregatedList>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/aggregated/instantSnapshots",
        args.project,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.include_all_scopes {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("includeAllScopes=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.order_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orderBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.return_partial_success {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("returnPartialSuccess=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.service_project_number {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("serviceProjectNumber=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: InstantSnapshotAggregatedList =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE projects/{project}/zones/{zone}/instantSnapshots/{instantSnapshot}
// -----------------------------------------------------------------------------

/// DELETE projects/{project}/zones/{zone}/instantSnapshots/{instantSnapshot}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_instant_snapshots_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_instant_snapshots_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeInstantSnapshotsDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/instantSnapshots/{}",
        args.project, args.zone, args.instant_snapshot,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/zones/{zone}/instantSnapshots/{instantSnapshot}
// -----------------------------------------------------------------------------

/// GET projects/{project}/zones/{zone}/instantSnapshots/{instantSnapshot}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_instant_snapshots_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_instant_snapshots_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeInstantSnapshotsGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<InstantSnapshot>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/instantSnapshots/{}",
        args.project, args.zone, args.instant_snapshot,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: InstantSnapshot =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/zones/{zone}/instantSnapshots/{resource}/getIamPolicy
// -----------------------------------------------------------------------------

/// GET projects/{project}/zones/{zone}/instantSnapshots/{resource}/getIamPolicy.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_instant_snapshots_get_iam_policy_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_instant_snapshots_get_iam_policy_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeInstantSnapshotsGetIamPolicyArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/instantSnapshots/{}/getIamPolicy",
        args.project,
        args.zone,
        args.resource,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.options_requested_policy_version {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("optionsRequestedPolicyVersion=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Policy =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/zones/{zone}/instantSnapshots
// -----------------------------------------------------------------------------

/// POST projects/{project}/zones/{zone}/instantSnapshots.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_instant_snapshots_insert_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_instant_snapshots_insert_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeInstantSnapshotsInsertArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/instantSnapshots",
        args.project, args.zone,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/zones/{zone}/instantSnapshots
// -----------------------------------------------------------------------------

/// GET projects/{project}/zones/{zone}/instantSnapshots.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_instant_snapshots_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_instant_snapshots_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeInstantSnapshotsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<InstantSnapshotList>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/instantSnapshots",
        args.project, args.zone,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.order_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orderBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.return_partial_success {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("returnPartialSuccess=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: InstantSnapshotList =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/zones/{zone}/instantSnapshots/{resource}/setIamPolicy
// -----------------------------------------------------------------------------

/// POST projects/{project}/zones/{zone}/instantSnapshots/{resource}/setIamPolicy.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_instant_snapshots_set_iam_policy_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_instant_snapshots_set_iam_policy_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeInstantSnapshotsSetIamPolicyArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/instantSnapshots/{}/setIamPolicy",
        args.project,
        args.zone,
        args.resource,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Policy =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/zones/{zone}/instantSnapshots/{resource}/setLabels
// -----------------------------------------------------------------------------

/// POST projects/{project}/zones/{zone}/instantSnapshots/{resource}/setLabels.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_instant_snapshots_set_labels_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_instant_snapshots_set_labels_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeInstantSnapshotsSetLabelsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/instantSnapshots/{}/setLabels",
        args.project,
        args.zone,
        args.resource,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/zones/{zone}/instantSnapshots/{resource}/testIamPermissions
// -----------------------------------------------------------------------------

/// POST projects/{project}/zones/{zone}/instantSnapshots/{resource}/testIamPermissions.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_instant_snapshots_test_iam_permissions_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_instant_snapshots_test_iam_permissions_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeInstantSnapshotsTestIamPermissionsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TestPermissionsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/instantSnapshots/{}/testIamPermissions",
        args.project,
        args.zone,
        args.resource,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: TestPermissionsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE projects/{project}/global/interconnectAttachmentGroups/{interconnectAttachmentGroup}
// -----------------------------------------------------------------------------

/// DELETE projects/{project}/global/interconnectAttachmentGroups/{interconnectAttachmentGroup}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_interconnect_attachment_groups_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_interconnect_attachment_groups_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeInterconnectAttachmentGroupsDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/interconnectAttachmentGroups/{}",
        args.project,
        args.interconnect_attachment_group,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/global/interconnectAttachmentGroups/{interconnectAttachmentGroup}
// -----------------------------------------------------------------------------

/// GET projects/{project}/global/interconnectAttachmentGroups/{interconnectAttachmentGroup}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_interconnect_attachment_groups_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_interconnect_attachment_groups_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeInterconnectAttachmentGroupsGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<InterconnectAttachmentGroup>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/interconnectAttachmentGroups/{}",
        args.project,
        args.interconnect_attachment_group,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: InterconnectAttachmentGroup =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/global/interconnectAttachmentGroups/{resource}/getIamPolicy
// -----------------------------------------------------------------------------

/// GET projects/{project}/global/interconnectAttachmentGroups/{resource}/getIamPolicy.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_interconnect_attachment_groups_get_iam_policy_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_interconnect_attachment_groups_get_iam_policy_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeInterconnectAttachmentGroupsGetIamPolicyArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/interconnectAttachmentGroups/{}/getIamPolicy",
        args.project,
        args.resource,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.options_requested_policy_version {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("optionsRequestedPolicyVersion=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Policy =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/global/interconnectAttachmentGroups/{interconnectAttachmentGroup}/getOperationalStatus
// -----------------------------------------------------------------------------

/// GET projects/{project}/global/interconnectAttachmentGroups/{interconnectAttachmentGroup}/getOperationalStatus.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_interconnect_attachment_groups_get_operational_status_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_interconnect_attachment_groups_get_operational_status_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeInterconnectAttachmentGroupsGetOperationalStatusArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<InterconnectAttachmentGroupsGetOperationalStatusResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/interconnectAttachmentGroups/{}/getOperationalStatus",
        args.project,
        args.interconnect_attachment_group,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: InterconnectAttachmentGroupsGetOperationalStatusResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/global/interconnectAttachmentGroups
// -----------------------------------------------------------------------------

/// POST projects/{project}/global/interconnectAttachmentGroups.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_interconnect_attachment_groups_insert_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_interconnect_attachment_groups_insert_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeInterconnectAttachmentGroupsInsertArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/interconnectAttachmentGroups",
        args.project,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/global/interconnectAttachmentGroups
// -----------------------------------------------------------------------------

/// GET projects/{project}/global/interconnectAttachmentGroups.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_interconnect_attachment_groups_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_interconnect_attachment_groups_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeInterconnectAttachmentGroupsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<InterconnectAttachmentGroupsListResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/interconnectAttachmentGroups",
        args.project,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.order_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orderBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.return_partial_success {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("returnPartialSuccess=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: InterconnectAttachmentGroupsListResponse = serde_json::from_str(&body)
                    .map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PATCH projects/{project}/global/interconnectAttachmentGroups/{interconnectAttachmentGroup}
// -----------------------------------------------------------------------------

/// PATCH projects/{project}/global/interconnectAttachmentGroups/{interconnectAttachmentGroup}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_interconnect_attachment_groups_patch_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_interconnect_attachment_groups_patch_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeInterconnectAttachmentGroupsPatchArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/interconnectAttachmentGroups/{}",
        args.project,
        args.interconnect_attachment_group,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.update_mask {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("updateMask=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .patch(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/global/interconnectAttachmentGroups/{resource}/setIamPolicy
// -----------------------------------------------------------------------------

/// POST projects/{project}/global/interconnectAttachmentGroups/{resource}/setIamPolicy.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_interconnect_attachment_groups_set_iam_policy_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_interconnect_attachment_groups_set_iam_policy_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeInterconnectAttachmentGroupsSetIamPolicyArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/interconnectAttachmentGroups/{}/setIamPolicy",
        args.project,
        args.resource,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Policy =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/global/interconnectAttachmentGroups/{resource}/testIamPermissions
// -----------------------------------------------------------------------------

/// POST projects/{project}/global/interconnectAttachmentGroups/{resource}/testIamPermissions.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_interconnect_attachment_groups_test_iam_permissions_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_interconnect_attachment_groups_test_iam_permissions_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeInterconnectAttachmentGroupsTestIamPermissionsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TestPermissionsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/interconnectAttachmentGroups/{}/testIamPermissions",
        args.project,
        args.resource,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: TestPermissionsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/aggregated/interconnectAttachments
// -----------------------------------------------------------------------------

/// GET projects/{project}/aggregated/interconnectAttachments.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_interconnect_attachments_aggregated_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_interconnect_attachments_aggregated_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeInterconnectAttachmentsAggregatedListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<InterconnectAttachmentAggregatedList>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/aggregated/interconnectAttachments",
        args.project,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.include_all_scopes {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("includeAllScopes=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.order_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orderBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.return_partial_success {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("returnPartialSuccess=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.service_project_number {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("serviceProjectNumber=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: InterconnectAttachmentAggregatedList = serde_json::from_str(&body)
                    .map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE projects/{project}/regions/{region}/interconnectAttachments/{interconnectAttachment}
// -----------------------------------------------------------------------------

/// DELETE projects/{project}/regions/{region}/interconnectAttachments/{interconnectAttachment}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_interconnect_attachments_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_interconnect_attachments_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeInterconnectAttachmentsDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/interconnectAttachments/{}",
        args.project,
        args.region,
        args.interconnect_attachment,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/regions/{region}/interconnectAttachments/{interconnectAttachment}
// -----------------------------------------------------------------------------

/// GET projects/{project}/regions/{region}/interconnectAttachments/{interconnectAttachment}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_interconnect_attachments_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_interconnect_attachments_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeInterconnectAttachmentsGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<InterconnectAttachment>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/interconnectAttachments/{}",
        args.project,
        args.region,
        args.interconnect_attachment,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: InterconnectAttachment =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/regions/{region}/interconnectAttachments
// -----------------------------------------------------------------------------

/// POST projects/{project}/regions/{region}/interconnectAttachments.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_interconnect_attachments_insert_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_interconnect_attachments_insert_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeInterconnectAttachmentsInsertArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/interconnectAttachments",
        args.project, args.region,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.validate_only {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("validateOnly=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/regions/{region}/interconnectAttachments
// -----------------------------------------------------------------------------

/// GET projects/{project}/regions/{region}/interconnectAttachments.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_interconnect_attachments_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_interconnect_attachments_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeInterconnectAttachmentsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<InterconnectAttachmentList>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/interconnectAttachments",
        args.project, args.region,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.order_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orderBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.return_partial_success {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("returnPartialSuccess=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: InterconnectAttachmentList =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PATCH projects/{project}/regions/{region}/interconnectAttachments/{interconnectAttachment}
// -----------------------------------------------------------------------------

/// PATCH projects/{project}/regions/{region}/interconnectAttachments/{interconnectAttachment}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_interconnect_attachments_patch_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_interconnect_attachments_patch_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeInterconnectAttachmentsPatchArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/interconnectAttachments/{}",
        args.project,
        args.region,
        args.interconnect_attachment,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .patch(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/regions/{region}/interconnectAttachments/{resource}/setLabels
// -----------------------------------------------------------------------------

/// POST projects/{project}/regions/{region}/interconnectAttachments/{resource}/setLabels.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_interconnect_attachments_set_labels_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_interconnect_attachments_set_labels_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeInterconnectAttachmentsSetLabelsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/interconnectAttachments/{}/setLabels",
        args.project,
        args.region,
        args.resource,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/global/interconnectGroups/{interconnectGroup}/createMembers
// -----------------------------------------------------------------------------

/// POST projects/{project}/global/interconnectGroups/{interconnectGroup}/createMembers.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_interconnect_groups_create_members_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_interconnect_groups_create_members_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeInterconnectGroupsCreateMembersArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/interconnectGroups/{}/createMembers",
        args.project,
        args.interconnect_group,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE projects/{project}/global/interconnectGroups/{interconnectGroup}
// -----------------------------------------------------------------------------

/// DELETE projects/{project}/global/interconnectGroups/{interconnectGroup}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_interconnect_groups_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_interconnect_groups_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeInterconnectGroupsDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/interconnectGroups/{}",
        args.project, args.interconnect_group,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/global/interconnectGroups/{interconnectGroup}
// -----------------------------------------------------------------------------

/// GET projects/{project}/global/interconnectGroups/{interconnectGroup}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_interconnect_groups_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_interconnect_groups_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeInterconnectGroupsGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<InterconnectGroup>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/interconnectGroups/{}",
        args.project, args.interconnect_group,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: InterconnectGroup =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/global/interconnectGroups/{resource}/getIamPolicy
// -----------------------------------------------------------------------------

/// GET projects/{project}/global/interconnectGroups/{resource}/getIamPolicy.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_interconnect_groups_get_iam_policy_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_interconnect_groups_get_iam_policy_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeInterconnectGroupsGetIamPolicyArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/interconnectGroups/{}/getIamPolicy",
        args.project,
        args.resource,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.options_requested_policy_version {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("optionsRequestedPolicyVersion=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Policy =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/global/interconnectGroups/{interconnectGroup}/getOperationalStatus
// -----------------------------------------------------------------------------

/// GET projects/{project}/global/interconnectGroups/{interconnectGroup}/getOperationalStatus.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_interconnect_groups_get_operational_status_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_interconnect_groups_get_operational_status_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeInterconnectGroupsGetOperationalStatusArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<InterconnectGroupsGetOperationalStatusResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/interconnectGroups/{}/getOperationalStatus",
        args.project,
        args.interconnect_group,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: InterconnectGroupsGetOperationalStatusResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/global/interconnectGroups
// -----------------------------------------------------------------------------

/// POST projects/{project}/global/interconnectGroups.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_interconnect_groups_insert_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_interconnect_groups_insert_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeInterconnectGroupsInsertArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/interconnectGroups",
        args.project,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/global/interconnectGroups
// -----------------------------------------------------------------------------

/// GET projects/{project}/global/interconnectGroups.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_interconnect_groups_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_interconnect_groups_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeInterconnectGroupsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<InterconnectGroupsListResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/interconnectGroups",
        args.project,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.order_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orderBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.return_partial_success {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("returnPartialSuccess=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: InterconnectGroupsListResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PATCH projects/{project}/global/interconnectGroups/{interconnectGroup}
// -----------------------------------------------------------------------------

/// PATCH projects/{project}/global/interconnectGroups/{interconnectGroup}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_interconnect_groups_patch_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_interconnect_groups_patch_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeInterconnectGroupsPatchArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/interconnectGroups/{}",
        args.project, args.interconnect_group,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.update_mask {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("updateMask=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .patch(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/global/interconnectGroups/{resource}/setIamPolicy
// -----------------------------------------------------------------------------

/// POST projects/{project}/global/interconnectGroups/{resource}/setIamPolicy.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_interconnect_groups_set_iam_policy_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_interconnect_groups_set_iam_policy_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeInterconnectGroupsSetIamPolicyArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/interconnectGroups/{}/setIamPolicy",
        args.project,
        args.resource,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Policy =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/global/interconnectGroups/{resource}/testIamPermissions
// -----------------------------------------------------------------------------

/// POST projects/{project}/global/interconnectGroups/{resource}/testIamPermissions.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_interconnect_groups_test_iam_permissions_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_interconnect_groups_test_iam_permissions_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeInterconnectGroupsTestIamPermissionsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TestPermissionsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/interconnectGroups/{}/testIamPermissions",
        args.project,
        args.resource,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: TestPermissionsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/global/interconnectLocations/{interconnectLocation}
// -----------------------------------------------------------------------------

/// GET projects/{project}/global/interconnectLocations/{interconnectLocation}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_interconnect_locations_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_interconnect_locations_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeInterconnectLocationsGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<InterconnectLocation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/interconnectLocations/{}",
        args.project, args.interconnect_location,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: InterconnectLocation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/global/interconnectLocations
// -----------------------------------------------------------------------------

/// GET projects/{project}/global/interconnectLocations.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_interconnect_locations_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_interconnect_locations_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeInterconnectLocationsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<InterconnectLocationList>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/interconnectLocations",
        args.project,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.order_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orderBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.return_partial_success {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("returnPartialSuccess=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: InterconnectLocationList =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/global/interconnectRemoteLocations/{interconnectRemoteLocation}
// -----------------------------------------------------------------------------

/// GET projects/{project}/global/interconnectRemoteLocations/{interconnectRemoteLocation}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_interconnect_remote_locations_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_interconnect_remote_locations_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeInterconnectRemoteLocationsGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<InterconnectRemoteLocation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/interconnectRemoteLocations/{}",
        args.project,
        args.interconnect_remote_location,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: InterconnectRemoteLocation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/global/interconnectRemoteLocations
// -----------------------------------------------------------------------------

/// GET projects/{project}/global/interconnectRemoteLocations.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_interconnect_remote_locations_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_interconnect_remote_locations_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeInterconnectRemoteLocationsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<InterconnectRemoteLocationList>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/interconnectRemoteLocations",
        args.project,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.order_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orderBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.return_partial_success {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("returnPartialSuccess=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: InterconnectRemoteLocationList =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE projects/{project}/global/interconnects/{interconnect}
// -----------------------------------------------------------------------------

/// DELETE projects/{project}/global/interconnects/{interconnect}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_interconnects_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_interconnects_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeInterconnectsDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/interconnects/{}",
        args.project, args.interconnect,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/global/interconnects/{interconnect}
// -----------------------------------------------------------------------------

/// GET projects/{project}/global/interconnects/{interconnect}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_interconnects_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_interconnects_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeInterconnectsGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Interconnect>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/interconnects/{}",
        args.project, args.interconnect,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Interconnect =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/global/interconnects/{interconnect}/getDiagnostics
// -----------------------------------------------------------------------------

/// GET projects/{project}/global/interconnects/{interconnect}/getDiagnostics.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_interconnects_get_diagnostics_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_interconnects_get_diagnostics_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeInterconnectsGetDiagnosticsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<InterconnectsGetDiagnosticsResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/interconnects/{}/getDiagnostics",
        args.project,
        args.interconnect,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: InterconnectsGetDiagnosticsResponse = serde_json::from_str(&body)
                    .map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/global/interconnects/{interconnect}/getMacsecConfig
// -----------------------------------------------------------------------------

/// GET projects/{project}/global/interconnects/{interconnect}/getMacsecConfig.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_interconnects_get_macsec_config_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_interconnects_get_macsec_config_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeInterconnectsGetMacsecConfigArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<InterconnectsGetMacsecConfigResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/interconnects/{}/getMacsecConfig",
        args.project,
        args.interconnect,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: InterconnectsGetMacsecConfigResponse = serde_json::from_str(&body)
                    .map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/global/interconnects
// -----------------------------------------------------------------------------

/// POST projects/{project}/global/interconnects.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_interconnects_insert_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_interconnects_insert_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeInterconnectsInsertArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/interconnects",
        args.project,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/global/interconnects
// -----------------------------------------------------------------------------

/// GET projects/{project}/global/interconnects.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_interconnects_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_interconnects_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeInterconnectsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<InterconnectList>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/interconnects",
        args.project,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.order_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orderBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.return_partial_success {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("returnPartialSuccess=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: InterconnectList =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PATCH projects/{project}/global/interconnects/{interconnect}
// -----------------------------------------------------------------------------

/// PATCH projects/{project}/global/interconnects/{interconnect}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_interconnects_patch_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_interconnects_patch_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeInterconnectsPatchArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/interconnects/{}",
        args.project, args.interconnect,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .patch(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/global/interconnects/{resource}/setLabels
// -----------------------------------------------------------------------------

/// POST projects/{project}/global/interconnects/{resource}/setLabels.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_interconnects_set_labels_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_interconnects_set_labels_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeInterconnectsSetLabelsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/interconnects/{}/setLabels",
        args.project, args.resource,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/global/licenseCodes/{licenseCode}
// -----------------------------------------------------------------------------

/// GET projects/{project}/global/licenseCodes/{licenseCode}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_license_codes_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_license_codes_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeLicenseCodesGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<LicenseCode>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/licenseCodes/{}",
        args.project, args.license_code,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: LicenseCode =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/global/licenseCodes/{resource}/testIamPermissions
// -----------------------------------------------------------------------------

/// POST projects/{project}/global/licenseCodes/{resource}/testIamPermissions.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_license_codes_test_iam_permissions_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_license_codes_test_iam_permissions_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeLicenseCodesTestIamPermissionsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TestPermissionsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/licenseCodes/{}/testIamPermissions",
        args.project,
        args.resource,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: TestPermissionsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE projects/{project}/global/licenses/{license}
// -----------------------------------------------------------------------------

/// DELETE projects/{project}/global/licenses/{license}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_licenses_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_licenses_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeLicensesDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/licenses/{}",
        args.project, args.license,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/global/licenses/{license}
// -----------------------------------------------------------------------------

/// GET projects/{project}/global/licenses/{license}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_licenses_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_licenses_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeLicensesGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<License>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/licenses/{}",
        args.project, args.license,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: License =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/global/licenses/{resource}/getIamPolicy
// -----------------------------------------------------------------------------

/// GET projects/{project}/global/licenses/{resource}/getIamPolicy.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_licenses_get_iam_policy_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_licenses_get_iam_policy_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeLicensesGetIamPolicyArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/licenses/{}/getIamPolicy",
        args.project, args.resource,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.options_requested_policy_version {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("optionsRequestedPolicyVersion=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Policy =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/global/licenses
// -----------------------------------------------------------------------------

/// POST projects/{project}/global/licenses.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_licenses_insert_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_licenses_insert_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeLicensesInsertArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/licenses",
        args.project,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/global/licenses
// -----------------------------------------------------------------------------

/// GET projects/{project}/global/licenses.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_licenses_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_licenses_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeLicensesListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<LicensesListResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/licenses",
        args.project,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.order_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orderBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.return_partial_success {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("returnPartialSuccess=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: LicensesListResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/global/licenses/{resource}/setIamPolicy
// -----------------------------------------------------------------------------

/// POST projects/{project}/global/licenses/{resource}/setIamPolicy.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_licenses_set_iam_policy_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_licenses_set_iam_policy_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeLicensesSetIamPolicyArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/licenses/{}/setIamPolicy",
        args.project, args.resource,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Policy =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/global/licenses/{resource}/testIamPermissions
// -----------------------------------------------------------------------------

/// POST projects/{project}/global/licenses/{resource}/testIamPermissions.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_licenses_test_iam_permissions_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_licenses_test_iam_permissions_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeLicensesTestIamPermissionsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TestPermissionsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/licenses/{}/testIamPermissions",
        args.project,
        args.resource,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: TestPermissionsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PATCH projects/{project}/global/licenses/{license}
// -----------------------------------------------------------------------------

/// PATCH projects/{project}/global/licenses/{license}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_licenses_update_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_licenses_update_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeLicensesUpdateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/licenses/{}",
        args.project, args.license,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.update_mask {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("updateMask=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .patch(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE projects/{project}/global/machineImages/{machineImage}
// -----------------------------------------------------------------------------

/// DELETE projects/{project}/global/machineImages/{machineImage}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_machine_images_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_machine_images_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeMachineImagesDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/machineImages/{}",
        args.project, args.machine_image,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/global/machineImages/{machineImage}
// -----------------------------------------------------------------------------

/// GET projects/{project}/global/machineImages/{machineImage}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_machine_images_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_machine_images_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeMachineImagesGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<MachineImage>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/machineImages/{}",
        args.project, args.machine_image,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: MachineImage =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/global/machineImages/{resource}/getIamPolicy
// -----------------------------------------------------------------------------

/// GET projects/{project}/global/machineImages/{resource}/getIamPolicy.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_machine_images_get_iam_policy_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_machine_images_get_iam_policy_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeMachineImagesGetIamPolicyArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/machineImages/{}/getIamPolicy",
        args.project,
        args.resource,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.options_requested_policy_version {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("optionsRequestedPolicyVersion=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Policy =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/global/machineImages
// -----------------------------------------------------------------------------

/// POST projects/{project}/global/machineImages.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_machine_images_insert_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_machine_images_insert_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeMachineImagesInsertArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/machineImages",
        args.project,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.source_instance {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("sourceInstance=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/global/machineImages
// -----------------------------------------------------------------------------

/// GET projects/{project}/global/machineImages.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_machine_images_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_machine_images_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeMachineImagesListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<MachineImageList>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/machineImages",
        args.project,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.order_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orderBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.return_partial_success {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("returnPartialSuccess=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: MachineImageList =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/global/machineImages/{resource}/setIamPolicy
// -----------------------------------------------------------------------------

/// POST projects/{project}/global/machineImages/{resource}/setIamPolicy.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_machine_images_set_iam_policy_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_machine_images_set_iam_policy_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeMachineImagesSetIamPolicyArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/machineImages/{}/setIamPolicy",
        args.project,
        args.resource,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Policy =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/global/machineImages/{resource}/setLabels
// -----------------------------------------------------------------------------

/// POST projects/{project}/global/machineImages/{resource}/setLabels.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_machine_images_set_labels_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_machine_images_set_labels_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeMachineImagesSetLabelsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/machineImages/{}/setLabels",
        args.project, args.resource,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/global/machineImages/{resource}/testIamPermissions
// -----------------------------------------------------------------------------

/// POST projects/{project}/global/machineImages/{resource}/testIamPermissions.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_machine_images_test_iam_permissions_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_machine_images_test_iam_permissions_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeMachineImagesTestIamPermissionsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TestPermissionsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/machineImages/{}/testIamPermissions",
        args.project,
        args.resource,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: TestPermissionsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/aggregated/machineTypes
// -----------------------------------------------------------------------------

/// GET projects/{project}/aggregated/machineTypes.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_machine_types_aggregated_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_machine_types_aggregated_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeMachineTypesAggregatedListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<MachineTypeAggregatedList>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/aggregated/machineTypes",
        args.project,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.include_all_scopes {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("includeAllScopes=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.order_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orderBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.return_partial_success {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("returnPartialSuccess=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.service_project_number {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("serviceProjectNumber=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: MachineTypeAggregatedList =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/zones/{zone}/machineTypes/{machineType}
// -----------------------------------------------------------------------------

/// GET projects/{project}/zones/{zone}/machineTypes/{machineType}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_machine_types_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_machine_types_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeMachineTypesGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<MachineType>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/machineTypes/{}",
        args.project, args.zone, args.machine_type,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: MachineType =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/zones/{zone}/machineTypes
// -----------------------------------------------------------------------------

/// GET projects/{project}/zones/{zone}/machineTypes.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_machine_types_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_machine_types_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeMachineTypesListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<MachineTypeList>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/machineTypes",
        args.project, args.zone,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.order_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orderBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.return_partial_success {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("returnPartialSuccess=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: MachineTypeList =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/aggregated/networkAttachments
// -----------------------------------------------------------------------------

/// GET projects/{project}/aggregated/networkAttachments.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_network_attachments_aggregated_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_network_attachments_aggregated_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeNetworkAttachmentsAggregatedListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<NetworkAttachmentAggregatedList>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/aggregated/networkAttachments",
        args.project,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.include_all_scopes {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("includeAllScopes=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.order_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orderBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.return_partial_success {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("returnPartialSuccess=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.service_project_number {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("serviceProjectNumber=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: NetworkAttachmentAggregatedList =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE projects/{project}/regions/{region}/networkAttachments/{networkAttachment}
// -----------------------------------------------------------------------------

/// DELETE projects/{project}/regions/{region}/networkAttachments/{networkAttachment}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_network_attachments_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_network_attachments_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeNetworkAttachmentsDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/networkAttachments/{}",
        args.project, args.region, args.network_attachment,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/regions/{region}/networkAttachments/{networkAttachment}
// -----------------------------------------------------------------------------

/// GET projects/{project}/regions/{region}/networkAttachments/{networkAttachment}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_network_attachments_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_network_attachments_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeNetworkAttachmentsGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<NetworkAttachment>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/networkAttachments/{}",
        args.project, args.region, args.network_attachment,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: NetworkAttachment =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/regions/{region}/networkAttachments/{resource}/getIamPolicy
// -----------------------------------------------------------------------------

/// GET projects/{project}/regions/{region}/networkAttachments/{resource}/getIamPolicy.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_network_attachments_get_iam_policy_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_network_attachments_get_iam_policy_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeNetworkAttachmentsGetIamPolicyArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/networkAttachments/{}/getIamPolicy",
        args.project,
        args.region,
        args.resource,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.options_requested_policy_version {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("optionsRequestedPolicyVersion=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Policy =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/regions/{region}/networkAttachments
// -----------------------------------------------------------------------------

/// POST projects/{project}/regions/{region}/networkAttachments.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_network_attachments_insert_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_network_attachments_insert_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeNetworkAttachmentsInsertArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/networkAttachments",
        args.project, args.region,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/regions/{region}/networkAttachments
// -----------------------------------------------------------------------------

/// GET projects/{project}/regions/{region}/networkAttachments.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_network_attachments_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_network_attachments_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeNetworkAttachmentsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<NetworkAttachmentList>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/networkAttachments",
        args.project, args.region,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.order_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orderBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.return_partial_success {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("returnPartialSuccess=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: NetworkAttachmentList =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PATCH projects/{project}/regions/{region}/networkAttachments/{networkAttachment}
// -----------------------------------------------------------------------------

/// PATCH projects/{project}/regions/{region}/networkAttachments/{networkAttachment}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_network_attachments_patch_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_network_attachments_patch_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeNetworkAttachmentsPatchArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/networkAttachments/{}",
        args.project, args.region, args.network_attachment,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .patch(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/regions/{region}/networkAttachments/{resource}/setIamPolicy
// -----------------------------------------------------------------------------

/// POST projects/{project}/regions/{region}/networkAttachments/{resource}/setIamPolicy.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_network_attachments_set_iam_policy_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_network_attachments_set_iam_policy_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeNetworkAttachmentsSetIamPolicyArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/networkAttachments/{}/setIamPolicy",
        args.project,
        args.region,
        args.resource,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Policy =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/regions/{region}/networkAttachments/{resource}/testIamPermissions
// -----------------------------------------------------------------------------

/// POST projects/{project}/regions/{region}/networkAttachments/{resource}/testIamPermissions.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_network_attachments_test_iam_permissions_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_network_attachments_test_iam_permissions_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeNetworkAttachmentsTestIamPermissionsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TestPermissionsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/networkAttachments/{}/testIamPermissions",
        args.project,
        args.region,
        args.resource,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: TestPermissionsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/aggregated/networkEdgeSecurityServices
// -----------------------------------------------------------------------------

/// GET projects/{project}/aggregated/networkEdgeSecurityServices.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_network_edge_security_services_aggregated_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_network_edge_security_services_aggregated_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeNetworkEdgeSecurityServicesAggregatedListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<NetworkEdgeSecurityServiceAggregatedList>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/aggregated/networkEdgeSecurityServices",
        args.project,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.include_all_scopes {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("includeAllScopes=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.order_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orderBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.return_partial_success {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("returnPartialSuccess=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.service_project_number {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("serviceProjectNumber=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: NetworkEdgeSecurityServiceAggregatedList = serde_json::from_str(&body)
                    .map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE projects/{project}/regions/{region}/networkEdgeSecurityServices/{networkEdgeSecurityService}
// -----------------------------------------------------------------------------

/// DELETE projects/{project}/regions/{region}/networkEdgeSecurityServices/{networkEdgeSecurityService}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_network_edge_security_services_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_network_edge_security_services_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeNetworkEdgeSecurityServicesDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/networkEdgeSecurityServices/{}",
        args.project,
        args.region,
        args.network_edge_security_service,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/regions/{region}/networkEdgeSecurityServices/{networkEdgeSecurityService}
// -----------------------------------------------------------------------------

/// GET projects/{project}/regions/{region}/networkEdgeSecurityServices/{networkEdgeSecurityService}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_network_edge_security_services_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_network_edge_security_services_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeNetworkEdgeSecurityServicesGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<NetworkEdgeSecurityService>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/networkEdgeSecurityServices/{}",
        args.project,
        args.region,
        args.network_edge_security_service,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: NetworkEdgeSecurityService =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/regions/{region}/networkEdgeSecurityServices
// -----------------------------------------------------------------------------

/// POST projects/{project}/regions/{region}/networkEdgeSecurityServices.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_network_edge_security_services_insert_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_network_edge_security_services_insert_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeNetworkEdgeSecurityServicesInsertArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/networkEdgeSecurityServices",
        args.project,
        args.region,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.validate_only {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("validateOnly=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PATCH projects/{project}/regions/{region}/networkEdgeSecurityServices/{networkEdgeSecurityService}
// -----------------------------------------------------------------------------

/// PATCH projects/{project}/regions/{region}/networkEdgeSecurityServices/{networkEdgeSecurityService}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_network_edge_security_services_patch_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_network_edge_security_services_patch_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeNetworkEdgeSecurityServicesPatchArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/networkEdgeSecurityServices/{}",
        args.project,
        args.region,
        args.network_edge_security_service,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.paths {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("paths=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.update_mask {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("updateMask=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .patch(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/aggregated/networkEndpointGroups
// -----------------------------------------------------------------------------

/// GET projects/{project}/aggregated/networkEndpointGroups.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_network_endpoint_groups_aggregated_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_network_endpoint_groups_aggregated_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeNetworkEndpointGroupsAggregatedListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<NetworkEndpointGroupAggregatedList>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/aggregated/networkEndpointGroups",
        args.project,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.include_all_scopes {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("includeAllScopes=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.order_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orderBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.return_partial_success {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("returnPartialSuccess=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.service_project_number {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("serviceProjectNumber=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: NetworkEndpointGroupAggregatedList = serde_json::from_str(&body)
                    .map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/zones/{zone}/networkEndpointGroups/{networkEndpointGroup}/attachNetworkEndpoints
// -----------------------------------------------------------------------------

/// POST projects/{project}/zones/{zone}/networkEndpointGroups/{networkEndpointGroup}/attachNetworkEndpoints.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_network_endpoint_groups_attach_network_endpoints_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_network_endpoint_groups_attach_network_endpoints_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeNetworkEndpointGroupsAttachNetworkEndpointsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/networkEndpointGroups/{}/attachNetworkEndpoints",
        args.project,
        args.zone,
        args.network_endpoint_group,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE projects/{project}/zones/{zone}/networkEndpointGroups/{networkEndpointGroup}
// -----------------------------------------------------------------------------

/// DELETE projects/{project}/zones/{zone}/networkEndpointGroups/{networkEndpointGroup}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_network_endpoint_groups_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_network_endpoint_groups_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeNetworkEndpointGroupsDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/networkEndpointGroups/{}",
        args.project, args.zone, args.network_endpoint_group,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/zones/{zone}/networkEndpointGroups/{networkEndpointGroup}/detachNetworkEndpoints
// -----------------------------------------------------------------------------

/// POST projects/{project}/zones/{zone}/networkEndpointGroups/{networkEndpointGroup}/detachNetworkEndpoints.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_network_endpoint_groups_detach_network_endpoints_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_network_endpoint_groups_detach_network_endpoints_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeNetworkEndpointGroupsDetachNetworkEndpointsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/networkEndpointGroups/{}/detachNetworkEndpoints",
        args.project,
        args.zone,
        args.network_endpoint_group,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/zones/{zone}/networkEndpointGroups/{networkEndpointGroup}
// -----------------------------------------------------------------------------

/// GET projects/{project}/zones/{zone}/networkEndpointGroups/{networkEndpointGroup}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_network_endpoint_groups_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_network_endpoint_groups_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeNetworkEndpointGroupsGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<NetworkEndpointGroup>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/networkEndpointGroups/{}",
        args.project, args.zone, args.network_endpoint_group,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: NetworkEndpointGroup =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/zones/{zone}/networkEndpointGroups
// -----------------------------------------------------------------------------

/// POST projects/{project}/zones/{zone}/networkEndpointGroups.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_network_endpoint_groups_insert_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_network_endpoint_groups_insert_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeNetworkEndpointGroupsInsertArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/networkEndpointGroups",
        args.project, args.zone,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/zones/{zone}/networkEndpointGroups
// -----------------------------------------------------------------------------

/// GET projects/{project}/zones/{zone}/networkEndpointGroups.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_network_endpoint_groups_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_network_endpoint_groups_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeNetworkEndpointGroupsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<NetworkEndpointGroupList>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/networkEndpointGroups",
        args.project, args.zone,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.order_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orderBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.return_partial_success {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("returnPartialSuccess=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: NetworkEndpointGroupList =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/zones/{zone}/networkEndpointGroups/{networkEndpointGroup}/listNetworkEndpoints
// -----------------------------------------------------------------------------

/// POST projects/{project}/zones/{zone}/networkEndpointGroups/{networkEndpointGroup}/listNetworkEndpoints.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_network_endpoint_groups_list_network_endpoints_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_network_endpoint_groups_list_network_endpoints_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeNetworkEndpointGroupsListNetworkEndpointsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<NetworkEndpointGroupsListNetworkEndpoints>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/networkEndpointGroups/{}/listNetworkEndpoints",
        args.project,
        args.zone,
        args.network_endpoint_group,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.order_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orderBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.return_partial_success {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("returnPartialSuccess=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: NetworkEndpointGroupsListNetworkEndpoints = serde_json::from_str(&body)
                    .map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/zones/{zone}/networkEndpointGroups/{resource}/testIamPermissions
// -----------------------------------------------------------------------------

/// POST projects/{project}/zones/{zone}/networkEndpointGroups/{resource}/testIamPermissions.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_network_endpoint_groups_test_iam_permissions_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_network_endpoint_groups_test_iam_permissions_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeNetworkEndpointGroupsTestIamPermissionsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TestPermissionsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/networkEndpointGroups/{}/testIamPermissions",
        args.project,
        args.zone,
        args.resource,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: TestPermissionsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/global/firewallPolicies/{firewallPolicy}/addAssociation
// -----------------------------------------------------------------------------

/// POST projects/{project}/global/firewallPolicies/{firewallPolicy}/addAssociation.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_network_firewall_policies_add_association_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_network_firewall_policies_add_association_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeNetworkFirewallPoliciesAddAssociationArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/firewallPolicies/{}/addAssociation",
        args.project,
        args.firewall_policy,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.replace_existing_association {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("replaceExistingAssociation=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/global/firewallPolicies/{firewallPolicy}/addPacketMirroringRule
// -----------------------------------------------------------------------------

/// POST projects/{project}/global/firewallPolicies/{firewallPolicy}/addPacketMirroringRule.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_network_firewall_policies_add_packet_mirroring_rule_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_network_firewall_policies_add_packet_mirroring_rule_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeNetworkFirewallPoliciesAddPacketMirroringRuleArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/firewallPolicies/{}/addPacketMirroringRule",
        args.project,
        args.firewall_policy,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.max_priority {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxPriority=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.min_priority {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("minPriority=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/global/firewallPolicies/{firewallPolicy}/addRule
// -----------------------------------------------------------------------------

/// POST projects/{project}/global/firewallPolicies/{firewallPolicy}/addRule.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_network_firewall_policies_add_rule_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_network_firewall_policies_add_rule_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeNetworkFirewallPoliciesAddRuleArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/firewallPolicies/{}/addRule",
        args.project, args.firewall_policy,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.max_priority {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxPriority=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.min_priority {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("minPriority=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/aggregated/firewallPolicies
// -----------------------------------------------------------------------------

/// GET projects/{project}/aggregated/firewallPolicies.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_network_firewall_policies_aggregated_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_network_firewall_policies_aggregated_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeNetworkFirewallPoliciesAggregatedListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<NetworkFirewallPolicyAggregatedList>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/aggregated/firewallPolicies",
        args.project,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.include_all_scopes {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("includeAllScopes=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.order_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orderBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.return_partial_success {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("returnPartialSuccess=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.service_project_number {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("serviceProjectNumber=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: NetworkFirewallPolicyAggregatedList = serde_json::from_str(&body)
                    .map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/global/firewallPolicies/{firewallPolicy}/cloneRules
// -----------------------------------------------------------------------------

/// POST projects/{project}/global/firewallPolicies/{firewallPolicy}/cloneRules.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_network_firewall_policies_clone_rules_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_network_firewall_policies_clone_rules_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeNetworkFirewallPoliciesCloneRulesArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/firewallPolicies/{}/cloneRules",
        args.project,
        args.firewall_policy,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.source_firewall_policy {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("sourceFirewallPolicy=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE projects/{project}/global/firewallPolicies/{firewallPolicy}
// -----------------------------------------------------------------------------

/// DELETE projects/{project}/global/firewallPolicies/{firewallPolicy}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_network_firewall_policies_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_network_firewall_policies_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeNetworkFirewallPoliciesDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/firewallPolicies/{}",
        args.project, args.firewall_policy,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/global/firewallPolicies/{firewallPolicy}
// -----------------------------------------------------------------------------

/// GET projects/{project}/global/firewallPolicies/{firewallPolicy}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_network_firewall_policies_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_network_firewall_policies_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeNetworkFirewallPoliciesGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<FirewallPolicy>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/firewallPolicies/{}",
        args.project, args.firewall_policy,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: FirewallPolicy =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/global/firewallPolicies/{firewallPolicy}/getAssociation
// -----------------------------------------------------------------------------

/// GET projects/{project}/global/firewallPolicies/{firewallPolicy}/getAssociation.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_network_firewall_policies_get_association_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_network_firewall_policies_get_association_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeNetworkFirewallPoliciesGetAssociationArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<FirewallPolicyAssociation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/firewallPolicies/{}/getAssociation",
        args.project,
        args.firewall_policy,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.name {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("name=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: FirewallPolicyAssociation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/global/firewallPolicies/{resource}/getIamPolicy
// -----------------------------------------------------------------------------

/// GET projects/{project}/global/firewallPolicies/{resource}/getIamPolicy.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_network_firewall_policies_get_iam_policy_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_network_firewall_policies_get_iam_policy_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeNetworkFirewallPoliciesGetIamPolicyArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/firewallPolicies/{}/getIamPolicy",
        args.project,
        args.resource,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.options_requested_policy_version {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("optionsRequestedPolicyVersion=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Policy =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/global/firewallPolicies/{firewallPolicy}/getPacketMirroringRule
// -----------------------------------------------------------------------------

/// GET projects/{project}/global/firewallPolicies/{firewallPolicy}/getPacketMirroringRule.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_network_firewall_policies_get_packet_mirroring_rule_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_network_firewall_policies_get_packet_mirroring_rule_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeNetworkFirewallPoliciesGetPacketMirroringRuleArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<FirewallPolicyRule>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/firewallPolicies/{}/getPacketMirroringRule",
        args.project,
        args.firewall_policy,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.priority {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("priority=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: FirewallPolicyRule =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/global/firewallPolicies/{firewallPolicy}/getRule
// -----------------------------------------------------------------------------

/// GET projects/{project}/global/firewallPolicies/{firewallPolicy}/getRule.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_network_firewall_policies_get_rule_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_network_firewall_policies_get_rule_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeNetworkFirewallPoliciesGetRuleArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<FirewallPolicyRule>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/firewallPolicies/{}/getRule",
        args.project, args.firewall_policy,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.priority {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("priority=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: FirewallPolicyRule =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/global/firewallPolicies
// -----------------------------------------------------------------------------

/// POST projects/{project}/global/firewallPolicies.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_network_firewall_policies_insert_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_network_firewall_policies_insert_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeNetworkFirewallPoliciesInsertArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/firewallPolicies",
        args.project,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/global/firewallPolicies
// -----------------------------------------------------------------------------

/// GET projects/{project}/global/firewallPolicies.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_network_firewall_policies_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_network_firewall_policies_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeNetworkFirewallPoliciesListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<FirewallPolicyList>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/firewallPolicies",
        args.project,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.order_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orderBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.return_partial_success {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("returnPartialSuccess=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: FirewallPolicyList =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PATCH projects/{project}/global/firewallPolicies/{firewallPolicy}
// -----------------------------------------------------------------------------

/// PATCH projects/{project}/global/firewallPolicies/{firewallPolicy}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_network_firewall_policies_patch_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_network_firewall_policies_patch_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeNetworkFirewallPoliciesPatchArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/firewallPolicies/{}",
        args.project, args.firewall_policy,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .patch(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/global/firewallPolicies/{firewallPolicy}/patchPacketMirroringRule
// -----------------------------------------------------------------------------

/// POST projects/{project}/global/firewallPolicies/{firewallPolicy}/patchPacketMirroringRule.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_network_firewall_policies_patch_packet_mirroring_rule_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_network_firewall_policies_patch_packet_mirroring_rule_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeNetworkFirewallPoliciesPatchPacketMirroringRuleArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/firewallPolicies/{}/patchPacketMirroringRule",
        args.project,
        args.firewall_policy,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.priority {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("priority=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/global/firewallPolicies/{firewallPolicy}/patchRule
// -----------------------------------------------------------------------------

/// POST projects/{project}/global/firewallPolicies/{firewallPolicy}/patchRule.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_network_firewall_policies_patch_rule_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_network_firewall_policies_patch_rule_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeNetworkFirewallPoliciesPatchRuleArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/firewallPolicies/{}/patchRule",
        args.project,
        args.firewall_policy,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.priority {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("priority=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/global/firewallPolicies/{firewallPolicy}/removeAssociation
// -----------------------------------------------------------------------------

/// POST projects/{project}/global/firewallPolicies/{firewallPolicy}/removeAssociation.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_network_firewall_policies_remove_association_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_network_firewall_policies_remove_association_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeNetworkFirewallPoliciesRemoveAssociationArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/firewallPolicies/{}/removeAssociation",
        args.project,
        args.firewall_policy,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.name {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("name=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/global/firewallPolicies/{firewallPolicy}/removePacketMirroringRule
// -----------------------------------------------------------------------------

/// POST projects/{project}/global/firewallPolicies/{firewallPolicy}/removePacketMirroringRule.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_network_firewall_policies_remove_packet_mirroring_rule_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_network_firewall_policies_remove_packet_mirroring_rule_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeNetworkFirewallPoliciesRemovePacketMirroringRuleArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/firewallPolicies/{}/removePacketMirroringRule",
        args.project,
        args.firewall_policy,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.priority {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("priority=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/global/firewallPolicies/{firewallPolicy}/removeRule
// -----------------------------------------------------------------------------

/// POST projects/{project}/global/firewallPolicies/{firewallPolicy}/removeRule.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_network_firewall_policies_remove_rule_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_network_firewall_policies_remove_rule_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeNetworkFirewallPoliciesRemoveRuleArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/firewallPolicies/{}/removeRule",
        args.project,
        args.firewall_policy,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.priority {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("priority=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/global/firewallPolicies/{resource}/setIamPolicy
// -----------------------------------------------------------------------------

/// POST projects/{project}/global/firewallPolicies/{resource}/setIamPolicy.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_network_firewall_policies_set_iam_policy_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_network_firewall_policies_set_iam_policy_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeNetworkFirewallPoliciesSetIamPolicyArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/firewallPolicies/{}/setIamPolicy",
        args.project,
        args.resource,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Policy =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/global/firewallPolicies/{resource}/testIamPermissions
// -----------------------------------------------------------------------------

/// POST projects/{project}/global/firewallPolicies/{resource}/testIamPermissions.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_network_firewall_policies_test_iam_permissions_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_network_firewall_policies_test_iam_permissions_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeNetworkFirewallPoliciesTestIamPermissionsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TestPermissionsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/firewallPolicies/{}/testIamPermissions",
        args.project,
        args.resource,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: TestPermissionsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/global/networkProfiles/{networkProfile}
// -----------------------------------------------------------------------------

/// GET projects/{project}/global/networkProfiles/{networkProfile}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_network_profiles_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_network_profiles_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeNetworkProfilesGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<NetworkProfile>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/networkProfiles/{}",
        args.project, args.network_profile,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: NetworkProfile =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/global/networkProfiles
// -----------------------------------------------------------------------------

/// GET projects/{project}/global/networkProfiles.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_network_profiles_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_network_profiles_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeNetworkProfilesListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<NetworkProfilesListResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/networkProfiles",
        args.project,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.order_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orderBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.return_partial_success {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("returnPartialSuccess=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: NetworkProfilesListResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/global/networks/{network}/addPeering
// -----------------------------------------------------------------------------

/// POST projects/{project}/global/networks/{network}/addPeering.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_networks_add_peering_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_networks_add_peering_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeNetworksAddPeeringArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/networks/{}/addPeering",
        args.project, args.network,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE projects/{project}/global/networks/{network}
// -----------------------------------------------------------------------------

/// DELETE projects/{project}/global/networks/{network}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_networks_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_networks_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeNetworksDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/networks/{}",
        args.project, args.network,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/global/networks/{network}
// -----------------------------------------------------------------------------

/// GET projects/{project}/global/networks/{network}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_networks_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_networks_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeNetworksGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Network>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/networks/{}",
        args.project, args.network,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Network =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/global/networks/{network}/getEffectiveFirewalls
// -----------------------------------------------------------------------------

/// GET projects/{project}/global/networks/{network}/getEffectiveFirewalls.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_networks_get_effective_firewalls_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_networks_get_effective_firewalls_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeNetworksGetEffectiveFirewallsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<NetworksGetEffectiveFirewallsResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/networks/{}/getEffectiveFirewalls",
        args.project,
        args.network,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: NetworksGetEffectiveFirewallsResponse = serde_json::from_str(&body)
                    .map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/global/networks
// -----------------------------------------------------------------------------

/// POST projects/{project}/global/networks.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_networks_insert_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_networks_insert_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeNetworksInsertArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/networks",
        args.project,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/global/networks
// -----------------------------------------------------------------------------

/// GET projects/{project}/global/networks.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_networks_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_networks_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeNetworksListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<NetworkList>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/networks",
        args.project,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.order_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orderBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.return_partial_success {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("returnPartialSuccess=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: NetworkList =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/global/networks/{network}/listPeeringRoutes
// -----------------------------------------------------------------------------

/// GET projects/{project}/global/networks/{network}/listPeeringRoutes.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_networks_list_peering_routes_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_networks_list_peering_routes_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeNetworksListPeeringRoutesArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ExchangedPeeringRoutesList>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/networks/{}/listPeeringRoutes",
        args.project,
        args.network,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.direction {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("direction=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.order_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orderBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.peering_name {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("peeringName=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.region {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("region=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.return_partial_success {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("returnPartialSuccess=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: ExchangedPeeringRoutesList =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PATCH projects/{project}/global/networks/{network}
// -----------------------------------------------------------------------------

/// PATCH projects/{project}/global/networks/{network}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_networks_patch_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_networks_patch_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeNetworksPatchArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/networks/{}",
        args.project, args.network,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .patch(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/global/networks/{network}/removePeering
// -----------------------------------------------------------------------------

/// POST projects/{project}/global/networks/{network}/removePeering.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_networks_remove_peering_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_networks_remove_peering_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeNetworksRemovePeeringArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/networks/{}/removePeering",
        args.project, args.network,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/global/networks/{network}/requestRemovePeering
// -----------------------------------------------------------------------------

/// POST projects/{project}/global/networks/{network}/requestRemovePeering.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_networks_request_remove_peering_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_networks_request_remove_peering_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeNetworksRequestRemovePeeringArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/networks/{}/requestRemovePeering",
        args.project,
        args.network,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/global/networks/{network}/switchToCustomMode
// -----------------------------------------------------------------------------

/// POST projects/{project}/global/networks/{network}/switchToCustomMode.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_networks_switch_to_custom_mode_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_networks_switch_to_custom_mode_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeNetworksSwitchToCustomModeArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/networks/{}/switchToCustomMode",
        args.project,
        args.network,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PATCH projects/{project}/global/networks/{network}/updatePeering
// -----------------------------------------------------------------------------

/// PATCH projects/{project}/global/networks/{network}/updatePeering.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_networks_update_peering_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_networks_update_peering_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeNetworksUpdatePeeringArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/networks/{}/updatePeering",
        args.project, args.network,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .patch(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/zones/{zone}/nodeGroups/{nodeGroup}/addNodes
// -----------------------------------------------------------------------------

/// POST projects/{project}/zones/{zone}/nodeGroups/{nodeGroup}/addNodes.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_node_groups_add_nodes_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_node_groups_add_nodes_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeNodeGroupsAddNodesArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/nodeGroups/{}/addNodes",
        args.project, args.zone, args.node_group,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/aggregated/nodeGroups
// -----------------------------------------------------------------------------

/// GET projects/{project}/aggregated/nodeGroups.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_node_groups_aggregated_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_node_groups_aggregated_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeNodeGroupsAggregatedListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<NodeGroupAggregatedList>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/aggregated/nodeGroups",
        args.project,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.include_all_scopes {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("includeAllScopes=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.order_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orderBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.return_partial_success {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("returnPartialSuccess=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.service_project_number {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("serviceProjectNumber=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: NodeGroupAggregatedList =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE projects/{project}/zones/{zone}/nodeGroups/{nodeGroup}
// -----------------------------------------------------------------------------

/// DELETE projects/{project}/zones/{zone}/nodeGroups/{nodeGroup}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_node_groups_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_node_groups_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeNodeGroupsDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/nodeGroups/{}",
        args.project, args.zone, args.node_group,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/zones/{zone}/nodeGroups/{nodeGroup}/deleteNodes
// -----------------------------------------------------------------------------

/// POST projects/{project}/zones/{zone}/nodeGroups/{nodeGroup}/deleteNodes.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_node_groups_delete_nodes_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_node_groups_delete_nodes_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeNodeGroupsDeleteNodesArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/nodeGroups/{}/deleteNodes",
        args.project, args.zone, args.node_group,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/zones/{zone}/nodeGroups/{nodeGroup}
// -----------------------------------------------------------------------------

/// GET projects/{project}/zones/{zone}/nodeGroups/{nodeGroup}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_node_groups_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_node_groups_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeNodeGroupsGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<NodeGroup>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/nodeGroups/{}",
        args.project, args.zone, args.node_group,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: NodeGroup =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/zones/{zone}/nodeGroups/{resource}/getIamPolicy
// -----------------------------------------------------------------------------

/// GET projects/{project}/zones/{zone}/nodeGroups/{resource}/getIamPolicy.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_node_groups_get_iam_policy_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_node_groups_get_iam_policy_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeNodeGroupsGetIamPolicyArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/nodeGroups/{}/getIamPolicy",
        args.project, args.zone, args.resource,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.options_requested_policy_version {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("optionsRequestedPolicyVersion=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Policy =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/zones/{zone}/nodeGroups
// -----------------------------------------------------------------------------

/// POST projects/{project}/zones/{zone}/nodeGroups.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_node_groups_insert_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_node_groups_insert_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeNodeGroupsInsertArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/nodeGroups",
        args.project, args.zone,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.initial_node_count {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("initialNodeCount=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/zones/{zone}/nodeGroups
// -----------------------------------------------------------------------------

/// GET projects/{project}/zones/{zone}/nodeGroups.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_node_groups_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_node_groups_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeNodeGroupsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<NodeGroupList>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/nodeGroups",
        args.project, args.zone,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.order_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orderBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.return_partial_success {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("returnPartialSuccess=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: NodeGroupList =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/zones/{zone}/nodeGroups/{nodeGroup}/listNodes
// -----------------------------------------------------------------------------

/// POST projects/{project}/zones/{zone}/nodeGroups/{nodeGroup}/listNodes.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_node_groups_list_nodes_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_node_groups_list_nodes_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeNodeGroupsListNodesArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<NodeGroupsListNodes>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/nodeGroups/{}/listNodes",
        args.project, args.zone, args.node_group,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.order_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orderBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.return_partial_success {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("returnPartialSuccess=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: NodeGroupsListNodes =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PATCH projects/{project}/zones/{zone}/nodeGroups/{nodeGroup}
// -----------------------------------------------------------------------------

/// PATCH projects/{project}/zones/{zone}/nodeGroups/{nodeGroup}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_node_groups_patch_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_node_groups_patch_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeNodeGroupsPatchArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/nodeGroups/{}",
        args.project, args.zone, args.node_group,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .patch(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/zones/{zone}/nodeGroups/{nodeGroup}/performMaintenance
// -----------------------------------------------------------------------------

/// POST projects/{project}/zones/{zone}/nodeGroups/{nodeGroup}/performMaintenance.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_node_groups_perform_maintenance_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_node_groups_perform_maintenance_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeNodeGroupsPerformMaintenanceArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/nodeGroups/{}/performMaintenance",
        args.project,
        args.zone,
        args.node_group,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/zones/{zone}/nodeGroups/{resource}/setIamPolicy
// -----------------------------------------------------------------------------

/// POST projects/{project}/zones/{zone}/nodeGroups/{resource}/setIamPolicy.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_node_groups_set_iam_policy_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_node_groups_set_iam_policy_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeNodeGroupsSetIamPolicyArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/nodeGroups/{}/setIamPolicy",
        args.project, args.zone, args.resource,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Policy =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/zones/{zone}/nodeGroups/{nodeGroup}/setNodeTemplate
// -----------------------------------------------------------------------------

/// POST projects/{project}/zones/{zone}/nodeGroups/{nodeGroup}/setNodeTemplate.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_node_groups_set_node_template_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_node_groups_set_node_template_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeNodeGroupsSetNodeTemplateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/nodeGroups/{}/setNodeTemplate",
        args.project,
        args.zone,
        args.node_group,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/zones/{zone}/nodeGroups/{nodeGroup}/simulateMaintenanceEvent
// -----------------------------------------------------------------------------

/// POST projects/{project}/zones/{zone}/nodeGroups/{nodeGroup}/simulateMaintenanceEvent.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_node_groups_simulate_maintenance_event_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_node_groups_simulate_maintenance_event_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeNodeGroupsSimulateMaintenanceEventArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/nodeGroups/{}/simulateMaintenanceEvent",
        args.project,
        args.zone,
        args.node_group,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/zones/{zone}/nodeGroups/{resource}/testIamPermissions
// -----------------------------------------------------------------------------

/// POST projects/{project}/zones/{zone}/nodeGroups/{resource}/testIamPermissions.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_node_groups_test_iam_permissions_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_node_groups_test_iam_permissions_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeNodeGroupsTestIamPermissionsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TestPermissionsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/nodeGroups/{}/testIamPermissions",
        args.project,
        args.zone,
        args.resource,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: TestPermissionsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/aggregated/nodeTemplates
// -----------------------------------------------------------------------------

/// GET projects/{project}/aggregated/nodeTemplates.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_node_templates_aggregated_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_node_templates_aggregated_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeNodeTemplatesAggregatedListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<NodeTemplateAggregatedList>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/aggregated/nodeTemplates",
        args.project,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.include_all_scopes {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("includeAllScopes=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.order_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orderBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.return_partial_success {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("returnPartialSuccess=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.service_project_number {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("serviceProjectNumber=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: NodeTemplateAggregatedList =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE projects/{project}/regions/{region}/nodeTemplates/{nodeTemplate}
// -----------------------------------------------------------------------------

/// DELETE projects/{project}/regions/{region}/nodeTemplates/{nodeTemplate}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_node_templates_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_node_templates_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeNodeTemplatesDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/nodeTemplates/{}",
        args.project, args.region, args.node_template,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/regions/{region}/nodeTemplates/{nodeTemplate}
// -----------------------------------------------------------------------------

/// GET projects/{project}/regions/{region}/nodeTemplates/{nodeTemplate}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_node_templates_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_node_templates_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeNodeTemplatesGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<NodeTemplate>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/nodeTemplates/{}",
        args.project, args.region, args.node_template,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: NodeTemplate =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/regions/{region}/nodeTemplates/{resource}/getIamPolicy
// -----------------------------------------------------------------------------

/// GET projects/{project}/regions/{region}/nodeTemplates/{resource}/getIamPolicy.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_node_templates_get_iam_policy_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_node_templates_get_iam_policy_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeNodeTemplatesGetIamPolicyArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/nodeTemplates/{}/getIamPolicy",
        args.project,
        args.region,
        args.resource,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.options_requested_policy_version {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("optionsRequestedPolicyVersion=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Policy =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/regions/{region}/nodeTemplates
// -----------------------------------------------------------------------------

/// POST projects/{project}/regions/{region}/nodeTemplates.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_node_templates_insert_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_node_templates_insert_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeNodeTemplatesInsertArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/nodeTemplates",
        args.project, args.region,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/regions/{region}/nodeTemplates
// -----------------------------------------------------------------------------

/// GET projects/{project}/regions/{region}/nodeTemplates.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_node_templates_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_node_templates_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeNodeTemplatesListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<NodeTemplateList>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/nodeTemplates",
        args.project, args.region,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.order_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orderBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.return_partial_success {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("returnPartialSuccess=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: NodeTemplateList =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/regions/{region}/nodeTemplates/{resource}/setIamPolicy
// -----------------------------------------------------------------------------

/// POST projects/{project}/regions/{region}/nodeTemplates/{resource}/setIamPolicy.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_node_templates_set_iam_policy_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_node_templates_set_iam_policy_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeNodeTemplatesSetIamPolicyArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/nodeTemplates/{}/setIamPolicy",
        args.project,
        args.region,
        args.resource,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Policy =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/regions/{region}/nodeTemplates/{resource}/testIamPermissions
// -----------------------------------------------------------------------------

/// POST projects/{project}/regions/{region}/nodeTemplates/{resource}/testIamPermissions.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_node_templates_test_iam_permissions_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_node_templates_test_iam_permissions_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeNodeTemplatesTestIamPermissionsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TestPermissionsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/nodeTemplates/{}/testIamPermissions",
        args.project,
        args.region,
        args.resource,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: TestPermissionsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/aggregated/nodeTypes
// -----------------------------------------------------------------------------

/// GET projects/{project}/aggregated/nodeTypes.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_node_types_aggregated_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_node_types_aggregated_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeNodeTypesAggregatedListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<NodeTypeAggregatedList>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/aggregated/nodeTypes",
        args.project,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.include_all_scopes {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("includeAllScopes=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.order_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orderBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.return_partial_success {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("returnPartialSuccess=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.service_project_number {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("serviceProjectNumber=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: NodeTypeAggregatedList =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/zones/{zone}/nodeTypes/{nodeType}
// -----------------------------------------------------------------------------

/// GET projects/{project}/zones/{zone}/nodeTypes/{nodeType}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_node_types_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_node_types_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeNodeTypesGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<NodeType>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/nodeTypes/{}",
        args.project, args.zone, args.node_type,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: NodeType =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/zones/{zone}/nodeTypes
// -----------------------------------------------------------------------------

/// GET projects/{project}/zones/{zone}/nodeTypes.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_node_types_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_node_types_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeNodeTypesListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<NodeTypeList>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/nodeTypes",
        args.project, args.zone,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.order_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orderBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.return_partial_success {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("returnPartialSuccess=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: NodeTypeList =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST locations/global/securityPolicies/{securityPolicy}/addAssociation
// -----------------------------------------------------------------------------

/// POST locations/global/securityPolicies/{securityPolicy}/addAssociation.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_organization_security_policies_add_association_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_organization_security_policies_add_association_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeOrganizationSecurityPoliciesAddAssociationArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/locations/global/securityPolicies/{}/addAssociation",
        args.security_policy,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.replace_existing_association {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("replaceExistingAssociation=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST locations/global/securityPolicies/{securityPolicy}/addRule
// -----------------------------------------------------------------------------

/// POST locations/global/securityPolicies/{securityPolicy}/addRule.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_organization_security_policies_add_rule_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_organization_security_policies_add_rule_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeOrganizationSecurityPoliciesAddRuleArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/locations/global/securityPolicies/{}/addRule",
        args.security_policy,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST locations/global/securityPolicies/{securityPolicy}/copyRules
// -----------------------------------------------------------------------------

/// POST locations/global/securityPolicies/{securityPolicy}/copyRules.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_organization_security_policies_copy_rules_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_organization_security_policies_copy_rules_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeOrganizationSecurityPoliciesCopyRulesArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/locations/global/securityPolicies/{}/copyRules",
        args.security_policy,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.source_security_policy {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("sourceSecurityPolicy=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE locations/global/securityPolicies/{securityPolicy}
// -----------------------------------------------------------------------------

/// DELETE locations/global/securityPolicies/{securityPolicy}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_organization_security_policies_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_organization_security_policies_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeOrganizationSecurityPoliciesDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/locations/global/securityPolicies/{}",
        args.security_policy,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET locations/global/securityPolicies/{securityPolicy}
// -----------------------------------------------------------------------------

/// GET locations/global/securityPolicies/{securityPolicy}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_organization_security_policies_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_organization_security_policies_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeOrganizationSecurityPoliciesGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SecurityPolicy>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/locations/global/securityPolicies/{}",
        args.security_policy,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: SecurityPolicy =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET locations/global/securityPolicies/{securityPolicy}/getAssociation
// -----------------------------------------------------------------------------

/// GET locations/global/securityPolicies/{securityPolicy}/getAssociation.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_organization_security_policies_get_association_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_organization_security_policies_get_association_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeOrganizationSecurityPoliciesGetAssociationArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SecurityPolicyAssociation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/locations/global/securityPolicies/{}/getAssociation",
        args.security_policy,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.name {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("name=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: SecurityPolicyAssociation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET locations/global/securityPolicies/{securityPolicy}/getRule
// -----------------------------------------------------------------------------

/// GET locations/global/securityPolicies/{securityPolicy}/getRule.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_organization_security_policies_get_rule_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_organization_security_policies_get_rule_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeOrganizationSecurityPoliciesGetRuleArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SecurityPolicyRule>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/locations/global/securityPolicies/{}/getRule",
        args.security_policy,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.priority {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("priority=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: SecurityPolicyRule =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST locations/global/securityPolicies
// -----------------------------------------------------------------------------

/// POST locations/global/securityPolicies.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_organization_security_policies_insert_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_organization_security_policies_insert_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeOrganizationSecurityPoliciesInsertArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url =
        format!("https://compute.googleapis.com/compute/v1/locations/global/securityPolicies",);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.parent_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("parentId=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET locations/global/securityPolicies
// -----------------------------------------------------------------------------

/// GET locations/global/securityPolicies.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_organization_security_policies_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_organization_security_policies_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeOrganizationSecurityPoliciesListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SecurityPolicyList>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url =
        format!("https://compute.googleapis.com/compute/v1/locations/global/securityPolicies",);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.order_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orderBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.parent_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("parentId=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.return_partial_success {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("returnPartialSuccess=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: SecurityPolicyList =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET locations/global/securityPolicies/listAssociations
// -----------------------------------------------------------------------------

/// GET locations/global/securityPolicies/listAssociations.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_organization_security_policies_list_associations_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_organization_security_policies_list_associations_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeOrganizationSecurityPoliciesListAssociationsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<OrganizationSecurityPoliciesListAssociationsResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/locations/global/securityPolicies/listAssociations",
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.target_resource {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("targetResource=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: OrganizationSecurityPoliciesListAssociationsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET locations/global/securityPolicies/listPreconfiguredExpressionSets
// -----------------------------------------------------------------------------

/// GET locations/global/securityPolicies/listPreconfiguredExpressionSets.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_organization_security_policies_list_preconfigured_expression_sets_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_organization_security_policies_list_preconfigured_expression_sets_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeOrganizationSecurityPoliciesListPreconfiguredExpressionSetsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<SecurityPoliciesListPreconfiguredExpressionSetsResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/locations/global/securityPolicies/listPreconfiguredExpressionSets",
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.order_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orderBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.parent_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("parentId=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.return_partial_success {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("returnPartialSuccess=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: SecurityPoliciesListPreconfiguredExpressionSetsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST locations/global/securityPolicies/{securityPolicy}/move
// -----------------------------------------------------------------------------

/// POST locations/global/securityPolicies/{securityPolicy}/move.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_organization_security_policies_move_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_organization_security_policies_move_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeOrganizationSecurityPoliciesMoveArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/locations/global/securityPolicies/{}/move",
        args.security_policy,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.parent_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("parentId=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PATCH locations/global/securityPolicies/{securityPolicy}
// -----------------------------------------------------------------------------

/// PATCH locations/global/securityPolicies/{securityPolicy}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_organization_security_policies_patch_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_organization_security_policies_patch_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeOrganizationSecurityPoliciesPatchArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/locations/global/securityPolicies/{}",
        args.security_policy,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .patch(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST locations/global/securityPolicies/{securityPolicy}/patchRule
// -----------------------------------------------------------------------------

/// POST locations/global/securityPolicies/{securityPolicy}/patchRule.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_organization_security_policies_patch_rule_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_organization_security_policies_patch_rule_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeOrganizationSecurityPoliciesPatchRuleArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/locations/global/securityPolicies/{}/patchRule",
        args.security_policy,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.priority {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("priority=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST locations/global/securityPolicies/{securityPolicy}/removeAssociation
// -----------------------------------------------------------------------------

/// POST locations/global/securityPolicies/{securityPolicy}/removeAssociation.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_organization_security_policies_remove_association_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_organization_security_policies_remove_association_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeOrganizationSecurityPoliciesRemoveAssociationArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/locations/global/securityPolicies/{}/removeAssociation",
        args.security_policy,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.name {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("name=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST locations/global/securityPolicies/{securityPolicy}/removeRule
// -----------------------------------------------------------------------------

/// POST locations/global/securityPolicies/{securityPolicy}/removeRule.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_organization_security_policies_remove_rule_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_organization_security_policies_remove_rule_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeOrganizationSecurityPoliciesRemoveRuleArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/locations/global/securityPolicies/{}/removeRule",
        args.security_policy,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.priority {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("priority=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/aggregated/packetMirrorings
// -----------------------------------------------------------------------------

/// GET projects/{project}/aggregated/packetMirrorings.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_packet_mirrorings_aggregated_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_packet_mirrorings_aggregated_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputePacketMirroringsAggregatedListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<PacketMirroringAggregatedList>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/aggregated/packetMirrorings",
        args.project,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.include_all_scopes {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("includeAllScopes=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.order_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orderBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.return_partial_success {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("returnPartialSuccess=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.service_project_number {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("serviceProjectNumber=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: PacketMirroringAggregatedList =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE projects/{project}/regions/{region}/packetMirrorings/{packetMirroring}
// -----------------------------------------------------------------------------

/// DELETE projects/{project}/regions/{region}/packetMirrorings/{packetMirroring}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_packet_mirrorings_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_packet_mirrorings_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputePacketMirroringsDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/packetMirrorings/{}",
        args.project, args.region, args.packet_mirroring,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/regions/{region}/packetMirrorings/{packetMirroring}
// -----------------------------------------------------------------------------

/// GET projects/{project}/regions/{region}/packetMirrorings/{packetMirroring}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_packet_mirrorings_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_packet_mirrorings_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputePacketMirroringsGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<PacketMirroring>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/packetMirrorings/{}",
        args.project, args.region, args.packet_mirroring,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: PacketMirroring =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/regions/{region}/packetMirrorings
// -----------------------------------------------------------------------------

/// POST projects/{project}/regions/{region}/packetMirrorings.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_packet_mirrorings_insert_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_packet_mirrorings_insert_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputePacketMirroringsInsertArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/packetMirrorings",
        args.project, args.region,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/regions/{region}/packetMirrorings
// -----------------------------------------------------------------------------

/// GET projects/{project}/regions/{region}/packetMirrorings.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_packet_mirrorings_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_packet_mirrorings_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputePacketMirroringsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<PacketMirroringList>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/packetMirrorings",
        args.project, args.region,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.order_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orderBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.return_partial_success {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("returnPartialSuccess=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: PacketMirroringList =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PATCH projects/{project}/regions/{region}/packetMirrorings/{packetMirroring}
// -----------------------------------------------------------------------------

/// PATCH projects/{project}/regions/{region}/packetMirrorings/{packetMirroring}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_packet_mirrorings_patch_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_packet_mirrorings_patch_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputePacketMirroringsPatchArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/packetMirrorings/{}",
        args.project, args.region, args.packet_mirroring,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .patch(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/regions/{region}/packetMirrorings/{resource}/testIamPermissions
// -----------------------------------------------------------------------------

/// POST projects/{project}/regions/{region}/packetMirrorings/{resource}/testIamPermissions.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_packet_mirrorings_test_iam_permissions_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_packet_mirrorings_test_iam_permissions_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputePacketMirroringsTestIamPermissionsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TestPermissionsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/packetMirrorings/{}/testIamPermissions",
        args.project,
        args.region,
        args.resource,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: TestPermissionsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/global/previewFeatures/{previewFeature}
// -----------------------------------------------------------------------------

/// GET projects/{project}/global/previewFeatures/{previewFeature}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_preview_features_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_preview_features_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputePreviewFeaturesGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<PreviewFeature>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/previewFeatures/{}",
        args.project, args.preview_feature,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: PreviewFeature =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/global/previewFeatures
// -----------------------------------------------------------------------------

/// GET projects/{project}/global/previewFeatures.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_preview_features_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_preview_features_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputePreviewFeaturesListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<PreviewFeatureList>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/previewFeatures",
        args.project,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.order_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orderBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.return_partial_success {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("returnPartialSuccess=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: PreviewFeatureList =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PATCH projects/{project}/global/previewFeatures/{previewFeature}
// -----------------------------------------------------------------------------

/// PATCH projects/{project}/global/previewFeatures/{previewFeature}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_preview_features_update_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_preview_features_update_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputePreviewFeaturesUpdateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/previewFeatures/{}",
        args.project, args.preview_feature,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .patch(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/disableXpnHost
// -----------------------------------------------------------------------------

/// POST projects/{project}/disableXpnHost.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_projects_disable_xpn_host_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_projects_disable_xpn_host_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeProjectsDisableXpnHostArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/disableXpnHost",
        args.project,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/disableXpnResource
// -----------------------------------------------------------------------------

/// POST projects/{project}/disableXpnResource.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_projects_disable_xpn_resource_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_projects_disable_xpn_resource_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeProjectsDisableXpnResourceArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/disableXpnResource",
        args.project,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/enableXpnHost
// -----------------------------------------------------------------------------

/// POST projects/{project}/enableXpnHost.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_projects_enable_xpn_host_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_projects_enable_xpn_host_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeProjectsEnableXpnHostArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/enableXpnHost",
        args.project,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/enableXpnResource
// -----------------------------------------------------------------------------

/// POST projects/{project}/enableXpnResource.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_projects_enable_xpn_resource_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_projects_enable_xpn_resource_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeProjectsEnableXpnResourceArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/enableXpnResource",
        args.project,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}
// -----------------------------------------------------------------------------

/// GET projects/{project}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_projects_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_projects_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeProjectsGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Project>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}",
        args.project,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Project =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/getXpnHost
// -----------------------------------------------------------------------------

/// GET projects/{project}/getXpnHost.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_projects_get_xpn_host_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_projects_get_xpn_host_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeProjectsGetXpnHostArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Project>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/getXpnHost",
        args.project,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Project =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/getXpnResources
// -----------------------------------------------------------------------------

/// GET projects/{project}/getXpnResources.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_projects_get_xpn_resources_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_projects_get_xpn_resources_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeProjectsGetXpnResourcesArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ProjectsGetXpnResources>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/getXpnResources",
        args.project,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.order_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orderBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.return_partial_success {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("returnPartialSuccess=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: ProjectsGetXpnResources =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/listXpnHosts
// -----------------------------------------------------------------------------

/// POST projects/{project}/listXpnHosts.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_projects_list_xpn_hosts_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_projects_list_xpn_hosts_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeProjectsListXpnHostsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<XpnHostList>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/listXpnHosts",
        args.project,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.order_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orderBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.return_partial_success {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("returnPartialSuccess=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: XpnHostList =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/moveDisk
// -----------------------------------------------------------------------------

/// POST projects/{project}/moveDisk.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_projects_move_disk_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_projects_move_disk_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeProjectsMoveDiskArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/moveDisk",
        args.project,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/moveInstance
// -----------------------------------------------------------------------------

/// POST projects/{project}/moveInstance.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_projects_move_instance_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_projects_move_instance_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeProjectsMoveInstanceArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/moveInstance",
        args.project,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/setCloudArmorTier
// -----------------------------------------------------------------------------

/// POST projects/{project}/setCloudArmorTier.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_projects_set_cloud_armor_tier_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_projects_set_cloud_armor_tier_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeProjectsSetCloudArmorTierArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/setCloudArmorTier",
        args.project,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/setCommonInstanceMetadata
// -----------------------------------------------------------------------------

/// POST projects/{project}/setCommonInstanceMetadata.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_projects_set_common_instance_metadata_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_projects_set_common_instance_metadata_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeProjectsSetCommonInstanceMetadataArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/setCommonInstanceMetadata",
        args.project,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/setDefaultNetworkTier
// -----------------------------------------------------------------------------

/// POST projects/{project}/setDefaultNetworkTier.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_projects_set_default_network_tier_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_projects_set_default_network_tier_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeProjectsSetDefaultNetworkTierArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/setDefaultNetworkTier",
        args.project,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/setUsageExportBucket
// -----------------------------------------------------------------------------

/// POST projects/{project}/setUsageExportBucket.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_projects_set_usage_export_bucket_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_projects_set_usage_export_bucket_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeProjectsSetUsageExportBucketArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/setUsageExportBucket",
        args.project,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/global/publicAdvertisedPrefixes/{publicAdvertisedPrefix}/announce
// -----------------------------------------------------------------------------

/// POST projects/{project}/global/publicAdvertisedPrefixes/{publicAdvertisedPrefix}/announce.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_public_advertised_prefixes_announce_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_public_advertised_prefixes_announce_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputePublicAdvertisedPrefixesAnnounceArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/publicAdvertisedPrefixes/{}/announce",
        args.project,
        args.public_advertised_prefix,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE projects/{project}/global/publicAdvertisedPrefixes/{publicAdvertisedPrefix}
// -----------------------------------------------------------------------------

/// DELETE projects/{project}/global/publicAdvertisedPrefixes/{publicAdvertisedPrefix}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_public_advertised_prefixes_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_public_advertised_prefixes_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputePublicAdvertisedPrefixesDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/publicAdvertisedPrefixes/{}",
        args.project, args.public_advertised_prefix,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/global/publicAdvertisedPrefixes/{publicAdvertisedPrefix}
// -----------------------------------------------------------------------------

/// GET projects/{project}/global/publicAdvertisedPrefixes/{publicAdvertisedPrefix}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_public_advertised_prefixes_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_public_advertised_prefixes_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputePublicAdvertisedPrefixesGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<PublicAdvertisedPrefix>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/publicAdvertisedPrefixes/{}",
        args.project, args.public_advertised_prefix,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: PublicAdvertisedPrefix =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/global/publicAdvertisedPrefixes
// -----------------------------------------------------------------------------

/// POST projects/{project}/global/publicAdvertisedPrefixes.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_public_advertised_prefixes_insert_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_public_advertised_prefixes_insert_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputePublicAdvertisedPrefixesInsertArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/publicAdvertisedPrefixes",
        args.project,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/global/publicAdvertisedPrefixes
// -----------------------------------------------------------------------------

/// GET projects/{project}/global/publicAdvertisedPrefixes.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_public_advertised_prefixes_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_public_advertised_prefixes_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputePublicAdvertisedPrefixesListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<PublicAdvertisedPrefixList>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/publicAdvertisedPrefixes",
        args.project,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.order_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orderBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.return_partial_success {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("returnPartialSuccess=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: PublicAdvertisedPrefixList =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PATCH projects/{project}/global/publicAdvertisedPrefixes/{publicAdvertisedPrefix}
// -----------------------------------------------------------------------------

/// PATCH projects/{project}/global/publicAdvertisedPrefixes/{publicAdvertisedPrefix}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_public_advertised_prefixes_patch_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_public_advertised_prefixes_patch_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputePublicAdvertisedPrefixesPatchArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/publicAdvertisedPrefixes/{}",
        args.project, args.public_advertised_prefix,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .patch(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/global/publicAdvertisedPrefixes/{publicAdvertisedPrefix}/withdraw
// -----------------------------------------------------------------------------

/// POST projects/{project}/global/publicAdvertisedPrefixes/{publicAdvertisedPrefix}/withdraw.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_public_advertised_prefixes_withdraw_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_public_advertised_prefixes_withdraw_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputePublicAdvertisedPrefixesWithdrawArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/publicAdvertisedPrefixes/{}/withdraw",
        args.project,
        args.public_advertised_prefix,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/aggregated/publicDelegatedPrefixes
// -----------------------------------------------------------------------------

/// GET projects/{project}/aggregated/publicDelegatedPrefixes.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_public_delegated_prefixes_aggregated_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_public_delegated_prefixes_aggregated_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputePublicDelegatedPrefixesAggregatedListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<PublicDelegatedPrefixAggregatedList>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/aggregated/publicDelegatedPrefixes",
        args.project,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.include_all_scopes {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("includeAllScopes=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.order_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orderBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.return_partial_success {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("returnPartialSuccess=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.service_project_number {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("serviceProjectNumber=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: PublicDelegatedPrefixAggregatedList = serde_json::from_str(&body)
                    .map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/regions/{region}/publicDelegatedPrefixes/{publicDelegatedPrefix}/announce
// -----------------------------------------------------------------------------

/// POST projects/{project}/regions/{region}/publicDelegatedPrefixes/{publicDelegatedPrefix}/announce.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_public_delegated_prefixes_announce_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_public_delegated_prefixes_announce_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputePublicDelegatedPrefixesAnnounceArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/publicDelegatedPrefixes/{}/announce",
        args.project,
        args.region,
        args.public_delegated_prefix,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE projects/{project}/regions/{region}/publicDelegatedPrefixes/{publicDelegatedPrefix}
// -----------------------------------------------------------------------------

/// DELETE projects/{project}/regions/{region}/publicDelegatedPrefixes/{publicDelegatedPrefix}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_public_delegated_prefixes_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_public_delegated_prefixes_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputePublicDelegatedPrefixesDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/publicDelegatedPrefixes/{}",
        args.project,
        args.region,
        args.public_delegated_prefix,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/regions/{region}/publicDelegatedPrefixes/{publicDelegatedPrefix}
// -----------------------------------------------------------------------------

/// GET projects/{project}/regions/{region}/publicDelegatedPrefixes/{publicDelegatedPrefix}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_public_delegated_prefixes_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_public_delegated_prefixes_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputePublicDelegatedPrefixesGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<PublicDelegatedPrefix>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/publicDelegatedPrefixes/{}",
        args.project,
        args.region,
        args.public_delegated_prefix,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: PublicDelegatedPrefix =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/regions/{region}/publicDelegatedPrefixes
// -----------------------------------------------------------------------------

/// POST projects/{project}/regions/{region}/publicDelegatedPrefixes.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_public_delegated_prefixes_insert_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_public_delegated_prefixes_insert_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputePublicDelegatedPrefixesInsertArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/publicDelegatedPrefixes",
        args.project, args.region,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/regions/{region}/publicDelegatedPrefixes
// -----------------------------------------------------------------------------

/// GET projects/{project}/regions/{region}/publicDelegatedPrefixes.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_public_delegated_prefixes_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_public_delegated_prefixes_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputePublicDelegatedPrefixesListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<PublicDelegatedPrefixList>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/publicDelegatedPrefixes",
        args.project, args.region,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.order_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orderBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.return_partial_success {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("returnPartialSuccess=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: PublicDelegatedPrefixList =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PATCH projects/{project}/regions/{region}/publicDelegatedPrefixes/{publicDelegatedPrefix}
// -----------------------------------------------------------------------------

/// PATCH projects/{project}/regions/{region}/publicDelegatedPrefixes/{publicDelegatedPrefix}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_public_delegated_prefixes_patch_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_public_delegated_prefixes_patch_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputePublicDelegatedPrefixesPatchArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/publicDelegatedPrefixes/{}",
        args.project,
        args.region,
        args.public_delegated_prefix,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .patch(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/regions/{region}/publicDelegatedPrefixes/{publicDelegatedPrefix}/withdraw
// -----------------------------------------------------------------------------

/// POST projects/{project}/regions/{region}/publicDelegatedPrefixes/{publicDelegatedPrefix}/withdraw.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_public_delegated_prefixes_withdraw_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_public_delegated_prefixes_withdraw_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputePublicDelegatedPrefixesWithdrawArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/publicDelegatedPrefixes/{}/withdraw",
        args.project,
        args.region,
        args.public_delegated_prefix,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE projects/{project}/regions/{region}/autoscalers/{autoscaler}
// -----------------------------------------------------------------------------

/// DELETE projects/{project}/regions/{region}/autoscalers/{autoscaler}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_autoscalers_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_autoscalers_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionAutoscalersDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/autoscalers/{}",
        args.project, args.region, args.autoscaler,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/regions/{region}/autoscalers/{autoscaler}
// -----------------------------------------------------------------------------

/// GET projects/{project}/regions/{region}/autoscalers/{autoscaler}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_autoscalers_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_autoscalers_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionAutoscalersGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Autoscaler>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/autoscalers/{}",
        args.project, args.region, args.autoscaler,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Autoscaler =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/regions/{region}/autoscalers
// -----------------------------------------------------------------------------

/// POST projects/{project}/regions/{region}/autoscalers.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_autoscalers_insert_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_autoscalers_insert_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionAutoscalersInsertArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/autoscalers",
        args.project, args.region,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/regions/{region}/autoscalers
// -----------------------------------------------------------------------------

/// GET projects/{project}/regions/{region}/autoscalers.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_autoscalers_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_autoscalers_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionAutoscalersListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<RegionAutoscalerList>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/autoscalers",
        args.project, args.region,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.order_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orderBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.return_partial_success {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("returnPartialSuccess=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: RegionAutoscalerList =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PATCH projects/{project}/regions/{region}/autoscalers
// -----------------------------------------------------------------------------

/// PATCH projects/{project}/regions/{region}/autoscalers.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_autoscalers_patch_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_autoscalers_patch_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionAutoscalersPatchArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/autoscalers",
        args.project, args.region,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.autoscaler {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("autoscaler=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .patch(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/regions/{region}/autoscalers/{resource}/testIamPermissions
// -----------------------------------------------------------------------------

/// POST projects/{project}/regions/{region}/autoscalers/{resource}/testIamPermissions.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_autoscalers_test_iam_permissions_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_autoscalers_test_iam_permissions_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionAutoscalersTestIamPermissionsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TestPermissionsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/autoscalers/{}/testIamPermissions",
        args.project,
        args.region,
        args.resource,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: TestPermissionsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PUT projects/{project}/regions/{region}/autoscalers
// -----------------------------------------------------------------------------

/// PUT projects/{project}/regions/{region}/autoscalers.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_autoscalers_update_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_autoscalers_update_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionAutoscalersUpdateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/autoscalers",
        args.project, args.region,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.autoscaler {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("autoscaler=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .put(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE projects/{project}/regions/{region}/backendBuckets/{backendBucket}
// -----------------------------------------------------------------------------

/// DELETE projects/{project}/regions/{region}/backendBuckets/{backendBucket}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_backend_buckets_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_backend_buckets_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionBackendBucketsDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/backendBuckets/{}",
        args.project, args.region, args.backend_bucket,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/regions/{region}/backendBuckets/{backendBucket}
// -----------------------------------------------------------------------------

/// GET projects/{project}/regions/{region}/backendBuckets/{backendBucket}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_backend_buckets_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_backend_buckets_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionBackendBucketsGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<BackendBucket>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/backendBuckets/{}",
        args.project, args.region, args.backend_bucket,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: BackendBucket =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/regions/{region}/backendBuckets/{resource}/getIamPolicy
// -----------------------------------------------------------------------------

/// GET projects/{project}/regions/{region}/backendBuckets/{resource}/getIamPolicy.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_backend_buckets_get_iam_policy_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_backend_buckets_get_iam_policy_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionBackendBucketsGetIamPolicyArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/backendBuckets/{}/getIamPolicy",
        args.project,
        args.region,
        args.resource,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.options_requested_policy_version {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("optionsRequestedPolicyVersion=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Policy =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/regions/{region}/backendBuckets
// -----------------------------------------------------------------------------

/// POST projects/{project}/regions/{region}/backendBuckets.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_backend_buckets_insert_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_backend_buckets_insert_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionBackendBucketsInsertArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/backendBuckets",
        args.project, args.region,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/regions/{region}/backendBuckets
// -----------------------------------------------------------------------------

/// GET projects/{project}/regions/{region}/backendBuckets.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_backend_buckets_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_backend_buckets_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionBackendBucketsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<BackendBucketList>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/backendBuckets",
        args.project, args.region,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.order_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orderBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.return_partial_success {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("returnPartialSuccess=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: BackendBucketList =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/regions/{region}/backendBuckets/listUsable
// -----------------------------------------------------------------------------

/// GET projects/{project}/regions/{region}/backendBuckets/listUsable.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_backend_buckets_list_usable_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_backend_buckets_list_usable_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionBackendBucketsListUsableArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<BackendBucketListUsable>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/backendBuckets/listUsable",
        args.project,
        args.region,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.order_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orderBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.return_partial_success {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("returnPartialSuccess=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: BackendBucketListUsable =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PATCH projects/{project}/regions/{region}/backendBuckets/{backendBucket}
// -----------------------------------------------------------------------------

/// PATCH projects/{project}/regions/{region}/backendBuckets/{backendBucket}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_backend_buckets_patch_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_backend_buckets_patch_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionBackendBucketsPatchArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/backendBuckets/{}",
        args.project, args.region, args.backend_bucket,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .patch(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/regions/{region}/backendBuckets/{resource}/setIamPolicy
// -----------------------------------------------------------------------------

/// POST projects/{project}/regions/{region}/backendBuckets/{resource}/setIamPolicy.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_backend_buckets_set_iam_policy_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_backend_buckets_set_iam_policy_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionBackendBucketsSetIamPolicyArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/backendBuckets/{}/setIamPolicy",
        args.project,
        args.region,
        args.resource,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Policy =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/regions/{region}/backendBuckets/{resource}/testIamPermissions
// -----------------------------------------------------------------------------

/// POST projects/{project}/regions/{region}/backendBuckets/{resource}/testIamPermissions.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_backend_buckets_test_iam_permissions_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_backend_buckets_test_iam_permissions_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionBackendBucketsTestIamPermissionsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TestPermissionsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/backendBuckets/{}/testIamPermissions",
        args.project,
        args.region,
        args.resource,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: TestPermissionsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE projects/{project}/regions/{region}/backendServices/{backendService}
// -----------------------------------------------------------------------------

/// DELETE projects/{project}/regions/{region}/backendServices/{backendService}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_backend_services_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_backend_services_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionBackendServicesDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/backendServices/{}",
        args.project, args.region, args.backend_service,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/regions/{region}/backendServices/{backendService}
// -----------------------------------------------------------------------------

/// GET projects/{project}/regions/{region}/backendServices/{backendService}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_backend_services_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_backend_services_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionBackendServicesGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<BackendService>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/backendServices/{}",
        args.project, args.region, args.backend_service,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: BackendService =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/regions/{region}/backendServices/{backendService}/getHealth
// -----------------------------------------------------------------------------

/// POST projects/{project}/regions/{region}/backendServices/{backendService}/getHealth.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_backend_services_get_health_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_backend_services_get_health_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionBackendServicesGetHealthArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<BackendServiceGroupHealth>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/backendServices/{}/getHealth",
        args.project,
        args.region,
        args.backend_service,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: BackendServiceGroupHealth =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/regions/{region}/backendServices/{resource}/getIamPolicy
// -----------------------------------------------------------------------------

/// GET projects/{project}/regions/{region}/backendServices/{resource}/getIamPolicy.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_backend_services_get_iam_policy_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_backend_services_get_iam_policy_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionBackendServicesGetIamPolicyArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/backendServices/{}/getIamPolicy",
        args.project,
        args.region,
        args.resource,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.options_requested_policy_version {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("optionsRequestedPolicyVersion=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Policy =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/regions/{region}/backendServices
// -----------------------------------------------------------------------------

/// POST projects/{project}/regions/{region}/backendServices.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_backend_services_insert_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_backend_services_insert_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionBackendServicesInsertArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/backendServices",
        args.project, args.region,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/regions/{region}/backendServices
// -----------------------------------------------------------------------------

/// GET projects/{project}/regions/{region}/backendServices.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_backend_services_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_backend_services_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionBackendServicesListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<BackendServiceList>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/backendServices",
        args.project, args.region,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.order_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orderBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.return_partial_success {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("returnPartialSuccess=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: BackendServiceList =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/regions/{region}/backendServices/listUsable
// -----------------------------------------------------------------------------

/// GET projects/{project}/regions/{region}/backendServices/listUsable.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_backend_services_list_usable_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_backend_services_list_usable_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionBackendServicesListUsableArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<BackendServiceListUsable>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/backendServices/listUsable",
        args.project,
        args.region,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.order_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orderBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.return_partial_success {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("returnPartialSuccess=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: BackendServiceListUsable =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PATCH projects/{project}/regions/{region}/backendServices/{backendService}
// -----------------------------------------------------------------------------

/// PATCH projects/{project}/regions/{region}/backendServices/{backendService}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_backend_services_patch_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_backend_services_patch_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionBackendServicesPatchArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/backendServices/{}",
        args.project, args.region, args.backend_service,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .patch(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/regions/{region}/backendServices/{resource}/setIamPolicy
// -----------------------------------------------------------------------------

/// POST projects/{project}/regions/{region}/backendServices/{resource}/setIamPolicy.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_backend_services_set_iam_policy_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_backend_services_set_iam_policy_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionBackendServicesSetIamPolicyArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/backendServices/{}/setIamPolicy",
        args.project,
        args.region,
        args.resource,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Policy =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/regions/{region}/backendServices/{backendService}/setSecurityPolicy
// -----------------------------------------------------------------------------

/// POST projects/{project}/regions/{region}/backendServices/{backendService}/setSecurityPolicy.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_backend_services_set_security_policy_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_backend_services_set_security_policy_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionBackendServicesSetSecurityPolicyArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/backendServices/{}/setSecurityPolicy",
        args.project,
        args.region,
        args.backend_service,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/regions/{region}/backendServices/{resource}/testIamPermissions
// -----------------------------------------------------------------------------

/// POST projects/{project}/regions/{region}/backendServices/{resource}/testIamPermissions.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_backend_services_test_iam_permissions_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_backend_services_test_iam_permissions_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionBackendServicesTestIamPermissionsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TestPermissionsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/backendServices/{}/testIamPermissions",
        args.project,
        args.region,
        args.resource,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: TestPermissionsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PUT projects/{project}/regions/{region}/backendServices/{backendService}
// -----------------------------------------------------------------------------

/// PUT projects/{project}/regions/{region}/backendServices/{backendService}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_backend_services_update_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_backend_services_update_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionBackendServicesUpdateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/backendServices/{}",
        args.project, args.region, args.backend_service,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .put(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/aggregated/commitments
// -----------------------------------------------------------------------------

/// GET projects/{project}/aggregated/commitments.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_commitments_aggregated_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_commitments_aggregated_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionCommitmentsAggregatedListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<CommitmentAggregatedList>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/aggregated/commitments",
        args.project,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.include_all_scopes {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("includeAllScopes=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.order_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orderBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.return_partial_success {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("returnPartialSuccess=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.service_project_number {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("serviceProjectNumber=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: CommitmentAggregatedList =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/regions/{region}/commitments/{commitment}
// -----------------------------------------------------------------------------

/// GET projects/{project}/regions/{region}/commitments/{commitment}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_commitments_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_commitments_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionCommitmentsGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Commitment>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/commitments/{}",
        args.project, args.region, args.commitment,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Commitment =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/regions/{region}/commitments
// -----------------------------------------------------------------------------

/// POST projects/{project}/regions/{region}/commitments.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_commitments_insert_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_commitments_insert_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionCommitmentsInsertArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/commitments",
        args.project, args.region,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/regions/{region}/commitments
// -----------------------------------------------------------------------------

/// GET projects/{project}/regions/{region}/commitments.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_commitments_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_commitments_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionCommitmentsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<CommitmentList>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/commitments",
        args.project, args.region,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.order_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orderBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.return_partial_success {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("returnPartialSuccess=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: CommitmentList =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PATCH projects/{project}/regions/{region}/commitments/{commitment}
// -----------------------------------------------------------------------------

/// PATCH projects/{project}/regions/{region}/commitments/{commitment}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_commitments_update_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_commitments_update_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionCommitmentsUpdateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/commitments/{}",
        args.project, args.region, args.commitment,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.paths {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("paths=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.update_mask {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("updateMask=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .patch(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/aggregated/compositeHealthChecks
// -----------------------------------------------------------------------------

/// GET projects/{project}/aggregated/compositeHealthChecks.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_composite_health_checks_aggregated_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_composite_health_checks_aggregated_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionCompositeHealthChecksAggregatedListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<CompositeHealthCheckAggregatedList>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/aggregated/compositeHealthChecks",
        args.project,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.include_all_scopes {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("includeAllScopes=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.order_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orderBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.return_partial_success {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("returnPartialSuccess=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.service_project_number {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("serviceProjectNumber=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: CompositeHealthCheckAggregatedList = serde_json::from_str(&body)
                    .map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE projects/{project}/regions/{region}/compositeHealthChecks/{compositeHealthCheck}
// -----------------------------------------------------------------------------

/// DELETE projects/{project}/regions/{region}/compositeHealthChecks/{compositeHealthCheck}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_composite_health_checks_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_composite_health_checks_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionCompositeHealthChecksDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/compositeHealthChecks/{}",
        args.project, args.region, args.composite_health_check,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/regions/{region}/compositeHealthChecks/{compositeHealthCheck}
// -----------------------------------------------------------------------------

/// GET projects/{project}/regions/{region}/compositeHealthChecks/{compositeHealthCheck}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_composite_health_checks_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_composite_health_checks_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionCompositeHealthChecksGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<CompositeHealthCheck>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/compositeHealthChecks/{}",
        args.project, args.region, args.composite_health_check,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: CompositeHealthCheck =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/regions/{region}/compositeHealthChecks/{compositeHealthCheck}/getHealth
// -----------------------------------------------------------------------------

/// GET projects/{project}/regions/{region}/compositeHealthChecks/{compositeHealthCheck}/getHealth.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_composite_health_checks_get_health_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_composite_health_checks_get_health_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionCompositeHealthChecksGetHealthArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<CompositeHealthCheckHealth>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/compositeHealthChecks/{}/getHealth",
        args.project,
        args.region,
        args.composite_health_check,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: CompositeHealthCheckHealth =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/regions/{region}/compositeHealthChecks
// -----------------------------------------------------------------------------

/// POST projects/{project}/regions/{region}/compositeHealthChecks.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_composite_health_checks_insert_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_composite_health_checks_insert_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionCompositeHealthChecksInsertArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/compositeHealthChecks",
        args.project, args.region,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/regions/{region}/compositeHealthChecks
// -----------------------------------------------------------------------------

/// GET projects/{project}/regions/{region}/compositeHealthChecks.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_composite_health_checks_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_composite_health_checks_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionCompositeHealthChecksListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<CompositeHealthCheckList>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/compositeHealthChecks",
        args.project, args.region,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.order_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orderBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.return_partial_success {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("returnPartialSuccess=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: CompositeHealthCheckList =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PATCH projects/{project}/regions/{region}/compositeHealthChecks/{compositeHealthCheck}
// -----------------------------------------------------------------------------

/// PATCH projects/{project}/regions/{region}/compositeHealthChecks/{compositeHealthCheck}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_composite_health_checks_patch_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_composite_health_checks_patch_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionCompositeHealthChecksPatchArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/compositeHealthChecks/{}",
        args.project, args.region, args.composite_health_check,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .patch(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/regions/{region}/compositeHealthChecks/{resource}/testIamPermissions
// -----------------------------------------------------------------------------

/// POST projects/{project}/regions/{region}/compositeHealthChecks/{resource}/testIamPermissions.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_composite_health_checks_test_iam_permissions_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_composite_health_checks_test_iam_permissions_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionCompositeHealthChecksTestIamPermissionsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TestPermissionsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/compositeHealthChecks/{}/testIamPermissions",
        args.project,
        args.region,
        args.resource,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: TestPermissionsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/regions/{region}/diskTypes/{diskType}
// -----------------------------------------------------------------------------

/// GET projects/{project}/regions/{region}/diskTypes/{diskType}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_disk_types_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_disk_types_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionDiskTypesGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<DiskType>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/diskTypes/{}",
        args.project, args.region, args.disk_type,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: DiskType =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/regions/{region}/diskTypes
// -----------------------------------------------------------------------------

/// GET projects/{project}/regions/{region}/diskTypes.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_disk_types_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_disk_types_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionDiskTypesListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<RegionDiskTypeList>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/diskTypes",
        args.project, args.region,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.order_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orderBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.return_partial_success {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("returnPartialSuccess=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: RegionDiskTypeList =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/regions/{region}/disks/{disk}/addResourcePolicies
// -----------------------------------------------------------------------------

/// POST projects/{project}/regions/{region}/disks/{disk}/addResourcePolicies.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_disks_add_resource_policies_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_disks_add_resource_policies_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionDisksAddResourcePoliciesArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/disks/{}/addResourcePolicies",
        args.project,
        args.region,
        args.disk,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/regions/{region}/disks/bulkInsert
// -----------------------------------------------------------------------------

/// POST projects/{project}/regions/{region}/disks/bulkInsert.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_disks_bulk_insert_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_disks_bulk_insert_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionDisksBulkInsertArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/disks/bulkInsert",
        args.project, args.region,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/regions/{region}/disks/{disk}/createSnapshot
// -----------------------------------------------------------------------------

/// POST projects/{project}/regions/{region}/disks/{disk}/createSnapshot.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_disks_create_snapshot_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_disks_create_snapshot_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionDisksCreateSnapshotArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/disks/{}/createSnapshot",
        args.project, args.region, args.disk,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE projects/{project}/regions/{region}/disks/{disk}
// -----------------------------------------------------------------------------

/// DELETE projects/{project}/regions/{region}/disks/{disk}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_disks_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_disks_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionDisksDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/disks/{}",
        args.project, args.region, args.disk,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/regions/{region}/disks/{disk}
// -----------------------------------------------------------------------------

/// GET projects/{project}/regions/{region}/disks/{disk}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_disks_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_disks_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionDisksGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Disk>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/disks/{}",
        args.project, args.region, args.disk,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Disk =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/regions/{region}/disks/{resource}/getIamPolicy
// -----------------------------------------------------------------------------

/// GET projects/{project}/regions/{region}/disks/{resource}/getIamPolicy.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_disks_get_iam_policy_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_disks_get_iam_policy_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionDisksGetIamPolicyArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/disks/{}/getIamPolicy",
        args.project, args.region, args.resource,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.options_requested_policy_version {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("optionsRequestedPolicyVersion=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Policy =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/regions/{region}/disks
// -----------------------------------------------------------------------------

/// POST projects/{project}/regions/{region}/disks.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_disks_insert_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_disks_insert_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionDisksInsertArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/disks",
        args.project, args.region,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.source_image {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("sourceImage=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/regions/{region}/disks
// -----------------------------------------------------------------------------

/// GET projects/{project}/regions/{region}/disks.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_disks_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_disks_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionDisksListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<DiskList>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/disks",
        args.project, args.region,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.order_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orderBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.return_partial_success {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("returnPartialSuccess=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: DiskList =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/regions/{region}/disks/{disk}/removeResourcePolicies
// -----------------------------------------------------------------------------

/// POST projects/{project}/regions/{region}/disks/{disk}/removeResourcePolicies.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_disks_remove_resource_policies_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_disks_remove_resource_policies_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionDisksRemoveResourcePoliciesArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/disks/{}/removeResourcePolicies",
        args.project,
        args.region,
        args.disk,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/regions/{region}/disks/{disk}/resize
// -----------------------------------------------------------------------------

/// POST projects/{project}/regions/{region}/disks/{disk}/resize.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_disks_resize_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_disks_resize_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionDisksResizeArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/disks/{}/resize",
        args.project, args.region, args.disk,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/regions/{region}/disks/{resource}/setIamPolicy
// -----------------------------------------------------------------------------

/// POST projects/{project}/regions/{region}/disks/{resource}/setIamPolicy.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_disks_set_iam_policy_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_disks_set_iam_policy_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionDisksSetIamPolicyArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/disks/{}/setIamPolicy",
        args.project, args.region, args.resource,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Policy =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/regions/{region}/disks/{resource}/setLabels
// -----------------------------------------------------------------------------

/// POST projects/{project}/regions/{region}/disks/{resource}/setLabels.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_disks_set_labels_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_disks_set_labels_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionDisksSetLabelsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/disks/{}/setLabels",
        args.project, args.region, args.resource,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/regions/{region}/disks/{disk}/startAsyncReplication
// -----------------------------------------------------------------------------

/// POST projects/{project}/regions/{region}/disks/{disk}/startAsyncReplication.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_disks_start_async_replication_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_disks_start_async_replication_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionDisksStartAsyncReplicationArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/disks/{}/startAsyncReplication",
        args.project,
        args.region,
        args.disk,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/regions/{region}/disks/{disk}/stopAsyncReplication
// -----------------------------------------------------------------------------

/// POST projects/{project}/regions/{region}/disks/{disk}/stopAsyncReplication.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_disks_stop_async_replication_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_disks_stop_async_replication_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionDisksStopAsyncReplicationArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/disks/{}/stopAsyncReplication",
        args.project,
        args.region,
        args.disk,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/regions/{region}/disks/stopGroupAsyncReplication
// -----------------------------------------------------------------------------

/// POST projects/{project}/regions/{region}/disks/stopGroupAsyncReplication.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_disks_stop_group_async_replication_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_disks_stop_group_async_replication_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionDisksStopGroupAsyncReplicationArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/disks/stopGroupAsyncReplication",
        args.project,
        args.region,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/regions/{region}/disks/{resource}/testIamPermissions
// -----------------------------------------------------------------------------

/// POST projects/{project}/regions/{region}/disks/{resource}/testIamPermissions.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_disks_test_iam_permissions_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_disks_test_iam_permissions_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionDisksTestIamPermissionsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TestPermissionsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/disks/{}/testIamPermissions",
        args.project,
        args.region,
        args.resource,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: TestPermissionsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PATCH projects/{project}/regions/{region}/disks/{disk}
// -----------------------------------------------------------------------------

/// PATCH projects/{project}/regions/{region}/disks/{disk}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_disks_update_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_disks_update_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionDisksUpdateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/disks/{}",
        args.project, args.region, args.disk,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.paths {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("paths=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.update_mask {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("updateMask=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .patch(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/regions/{region}/disks/{disk}/updateKmsKey
// -----------------------------------------------------------------------------

/// POST projects/{project}/regions/{region}/disks/{disk}/updateKmsKey.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_disks_update_kms_key_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_disks_update_kms_key_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionDisksUpdateKmsKeyArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/disks/{}/updateKmsKey",
        args.project, args.region, args.disk,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/aggregated/healthAggregationPolicies
// -----------------------------------------------------------------------------

/// GET projects/{project}/aggregated/healthAggregationPolicies.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_health_aggregation_policies_aggregated_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_health_aggregation_policies_aggregated_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionHealthAggregationPoliciesAggregatedListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<HealthAggregationPolicyAggregatedList>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/aggregated/healthAggregationPolicies",
        args.project,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.include_all_scopes {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("includeAllScopes=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.order_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orderBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.return_partial_success {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("returnPartialSuccess=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.service_project_number {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("serviceProjectNumber=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: HealthAggregationPolicyAggregatedList = serde_json::from_str(&body)
                    .map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE projects/{project}/regions/{region}/healthAggregationPolicies/{healthAggregationPolicy}
// -----------------------------------------------------------------------------

/// DELETE projects/{project}/regions/{region}/healthAggregationPolicies/{healthAggregationPolicy}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_health_aggregation_policies_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_health_aggregation_policies_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionHealthAggregationPoliciesDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/healthAggregationPolicies/{}",
        args.project,
        args.region,
        args.health_aggregation_policy,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/regions/{region}/healthAggregationPolicies/{healthAggregationPolicy}
// -----------------------------------------------------------------------------

/// GET projects/{project}/regions/{region}/healthAggregationPolicies/{healthAggregationPolicy}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_health_aggregation_policies_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_health_aggregation_policies_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionHealthAggregationPoliciesGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<HealthAggregationPolicy>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/healthAggregationPolicies/{}",
        args.project,
        args.region,
        args.health_aggregation_policy,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: HealthAggregationPolicy =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/regions/{region}/healthAggregationPolicies
// -----------------------------------------------------------------------------

/// POST projects/{project}/regions/{region}/healthAggregationPolicies.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_health_aggregation_policies_insert_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_health_aggregation_policies_insert_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionHealthAggregationPoliciesInsertArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/healthAggregationPolicies",
        args.project,
        args.region,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/regions/{region}/healthAggregationPolicies
// -----------------------------------------------------------------------------

/// GET projects/{project}/regions/{region}/healthAggregationPolicies.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_health_aggregation_policies_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_health_aggregation_policies_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionHealthAggregationPoliciesListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<HealthAggregationPolicyList>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/healthAggregationPolicies",
        args.project,
        args.region,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.order_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orderBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.return_partial_success {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("returnPartialSuccess=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: HealthAggregationPolicyList =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PATCH projects/{project}/regions/{region}/healthAggregationPolicies/{healthAggregationPolicy}
// -----------------------------------------------------------------------------

/// PATCH projects/{project}/regions/{region}/healthAggregationPolicies/{healthAggregationPolicy}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_health_aggregation_policies_patch_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_health_aggregation_policies_patch_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionHealthAggregationPoliciesPatchArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/healthAggregationPolicies/{}",
        args.project,
        args.region,
        args.health_aggregation_policy,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .patch(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/regions/{region}/healthAggregationPolicies/{resource}/testIamPermissions
// -----------------------------------------------------------------------------

/// POST projects/{project}/regions/{region}/healthAggregationPolicies/{resource}/testIamPermissions.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_health_aggregation_policies_test_iam_permissions_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_health_aggregation_policies_test_iam_permissions_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionHealthAggregationPoliciesTestIamPermissionsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TestPermissionsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/healthAggregationPolicies/{}/testIamPermissions",
        args.project,
        args.region,
        args.resource,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: TestPermissionsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/aggregated/healthCheckServices
// -----------------------------------------------------------------------------

/// GET projects/{project}/aggregated/healthCheckServices.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_health_check_services_aggregated_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_health_check_services_aggregated_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionHealthCheckServicesAggregatedListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<HealthCheckServiceAggregatedList>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/aggregated/healthCheckServices",
        args.project,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.include_all_scopes {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("includeAllScopes=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.order_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orderBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.return_partial_success {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("returnPartialSuccess=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.service_project_number {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("serviceProjectNumber=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: HealthCheckServiceAggregatedList = serde_json::from_str(&body)
                    .map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE projects/{project}/regions/{region}/healthCheckServices/{healthCheckService}
// -----------------------------------------------------------------------------

/// DELETE projects/{project}/regions/{region}/healthCheckServices/{healthCheckService}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_health_check_services_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_health_check_services_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionHealthCheckServicesDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/healthCheckServices/{}",
        args.project, args.region, args.health_check_service,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/regions/{region}/healthCheckServices/{healthCheckService}
// -----------------------------------------------------------------------------

/// GET projects/{project}/regions/{region}/healthCheckServices/{healthCheckService}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_health_check_services_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_health_check_services_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionHealthCheckServicesGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<HealthCheckService>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/healthCheckServices/{}",
        args.project, args.region, args.health_check_service,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: HealthCheckService =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/regions/{region}/healthCheckServices
// -----------------------------------------------------------------------------

/// POST projects/{project}/regions/{region}/healthCheckServices.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_health_check_services_insert_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_health_check_services_insert_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionHealthCheckServicesInsertArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/healthCheckServices",
        args.project, args.region,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/regions/{region}/healthCheckServices
// -----------------------------------------------------------------------------

/// GET projects/{project}/regions/{region}/healthCheckServices.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_health_check_services_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_health_check_services_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionHealthCheckServicesListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<HealthCheckServicesList>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/healthCheckServices",
        args.project, args.region,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.order_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orderBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.return_partial_success {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("returnPartialSuccess=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: HealthCheckServicesList =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PATCH projects/{project}/regions/{region}/healthCheckServices/{healthCheckService}
// -----------------------------------------------------------------------------

/// PATCH projects/{project}/regions/{region}/healthCheckServices/{healthCheckService}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_health_check_services_patch_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_health_check_services_patch_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionHealthCheckServicesPatchArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/healthCheckServices/{}",
        args.project, args.region, args.health_check_service,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .patch(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/regions/{region}/healthCheckServices/{resource}/testIamPermissions
// -----------------------------------------------------------------------------

/// POST projects/{project}/regions/{region}/healthCheckServices/{resource}/testIamPermissions.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_health_check_services_test_iam_permissions_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_health_check_services_test_iam_permissions_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionHealthCheckServicesTestIamPermissionsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TestPermissionsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/healthCheckServices/{}/testIamPermissions",
        args.project,
        args.region,
        args.resource,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: TestPermissionsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE projects/{project}/regions/{region}/healthChecks/{healthCheck}
// -----------------------------------------------------------------------------

/// DELETE projects/{project}/regions/{region}/healthChecks/{healthCheck}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_health_checks_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_health_checks_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionHealthChecksDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/healthChecks/{}",
        args.project, args.region, args.health_check,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/regions/{region}/healthChecks/{healthCheck}
// -----------------------------------------------------------------------------

/// GET projects/{project}/regions/{region}/healthChecks/{healthCheck}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_health_checks_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_health_checks_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionHealthChecksGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<HealthCheck>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/healthChecks/{}",
        args.project, args.region, args.health_check,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: HealthCheck =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/regions/{region}/healthChecks
// -----------------------------------------------------------------------------

/// POST projects/{project}/regions/{region}/healthChecks.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_health_checks_insert_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_health_checks_insert_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionHealthChecksInsertArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/healthChecks",
        args.project, args.region,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/regions/{region}/healthChecks
// -----------------------------------------------------------------------------

/// GET projects/{project}/regions/{region}/healthChecks.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_health_checks_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_health_checks_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionHealthChecksListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<HealthCheckList>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/healthChecks",
        args.project, args.region,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.order_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orderBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.return_partial_success {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("returnPartialSuccess=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: HealthCheckList =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PATCH projects/{project}/regions/{region}/healthChecks/{healthCheck}
// -----------------------------------------------------------------------------

/// PATCH projects/{project}/regions/{region}/healthChecks/{healthCheck}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_health_checks_patch_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_health_checks_patch_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionHealthChecksPatchArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/healthChecks/{}",
        args.project, args.region, args.health_check,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .patch(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/regions/{region}/healthChecks/{resource}/testIamPermissions
// -----------------------------------------------------------------------------

/// POST projects/{project}/regions/{region}/healthChecks/{resource}/testIamPermissions.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_health_checks_test_iam_permissions_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_health_checks_test_iam_permissions_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionHealthChecksTestIamPermissionsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TestPermissionsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/healthChecks/{}/testIamPermissions",
        args.project,
        args.region,
        args.resource,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: TestPermissionsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PUT projects/{project}/regions/{region}/healthChecks/{healthCheck}
// -----------------------------------------------------------------------------

/// PUT projects/{project}/regions/{region}/healthChecks/{healthCheck}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_health_checks_update_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_health_checks_update_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionHealthChecksUpdateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/healthChecks/{}",
        args.project, args.region, args.health_check,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .put(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/aggregated/healthSources
// -----------------------------------------------------------------------------

/// GET projects/{project}/aggregated/healthSources.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_health_sources_aggregated_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_health_sources_aggregated_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionHealthSourcesAggregatedListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<HealthSourceAggregatedList>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/aggregated/healthSources",
        args.project,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.include_all_scopes {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("includeAllScopes=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.order_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orderBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.return_partial_success {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("returnPartialSuccess=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.service_project_number {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("serviceProjectNumber=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: HealthSourceAggregatedList =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE projects/{project}/regions/{region}/healthSources/{healthSource}
// -----------------------------------------------------------------------------

/// DELETE projects/{project}/regions/{region}/healthSources/{healthSource}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_health_sources_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_health_sources_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionHealthSourcesDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/healthSources/{}",
        args.project, args.region, args.health_source,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/regions/{region}/healthSources/{healthSource}
// -----------------------------------------------------------------------------

/// GET projects/{project}/regions/{region}/healthSources/{healthSource}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_health_sources_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_health_sources_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionHealthSourcesGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<HealthSource>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/healthSources/{}",
        args.project, args.region, args.health_source,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: HealthSource =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/regions/{region}/healthSources/{healthSource}/getHealth
// -----------------------------------------------------------------------------

/// GET projects/{project}/regions/{region}/healthSources/{healthSource}/getHealth.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_health_sources_get_health_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_health_sources_get_health_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionHealthSourcesGetHealthArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<HealthSourceHealth>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/healthSources/{}/getHealth",
        args.project,
        args.region,
        args.health_source,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: HealthSourceHealth =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/regions/{region}/healthSources
// -----------------------------------------------------------------------------

/// POST projects/{project}/regions/{region}/healthSources.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_health_sources_insert_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_health_sources_insert_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionHealthSourcesInsertArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/healthSources",
        args.project, args.region,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/regions/{region}/healthSources
// -----------------------------------------------------------------------------

/// GET projects/{project}/regions/{region}/healthSources.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_health_sources_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_health_sources_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionHealthSourcesListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<HealthSourceList>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/healthSources",
        args.project, args.region,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.order_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orderBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.return_partial_success {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("returnPartialSuccess=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: HealthSourceList =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PATCH projects/{project}/regions/{region}/healthSources/{healthSource}
// -----------------------------------------------------------------------------

/// PATCH projects/{project}/regions/{region}/healthSources/{healthSource}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_health_sources_patch_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_health_sources_patch_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionHealthSourcesPatchArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/healthSources/{}",
        args.project, args.region, args.health_source,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .patch(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/regions/{region}/healthSources/{resource}/testIamPermissions
// -----------------------------------------------------------------------------

/// POST projects/{project}/regions/{region}/healthSources/{resource}/testIamPermissions.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_health_sources_test_iam_permissions_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_health_sources_test_iam_permissions_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionHealthSourcesTestIamPermissionsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TestPermissionsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/healthSources/{}/testIamPermissions",
        args.project,
        args.region,
        args.resource,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: TestPermissionsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/regions/{region}/instanceGroupManagers/{instanceGroupManager}/resizeRequests/{resizeRequest}/cancel
// -----------------------------------------------------------------------------

/// POST projects/{project}/regions/{region}/instanceGroupManagers/{instanceGroupManager}/resizeRequests/{resizeRequest}/cancel.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_instance_group_manager_resize_requests_cancel_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_instance_group_manager_resize_requests_cancel_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionInstanceGroupManagerResizeRequestsCancelArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/instanceGroupManagers/{}/resizeRequests/{}/cancel",
        args.project,
        args.region,
        args.instance_group_manager,
        args.resize_request,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE projects/{project}/regions/{region}/instanceGroupManagers/{instanceGroupManager}/resizeRequests/{resizeRequest}
// -----------------------------------------------------------------------------

/// DELETE projects/{project}/regions/{region}/instanceGroupManagers/{instanceGroupManager}/resizeRequests/{resizeRequest}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_instance_group_manager_resize_requests_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_instance_group_manager_resize_requests_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionInstanceGroupManagerResizeRequestsDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/instanceGroupManagers/{}/resizeRequests/{}",
        args.project,
        args.region,
        args.instance_group_manager,
        args.resize_request,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/regions/{region}/instanceGroupManagers/{instanceGroupManager}/resizeRequests/{resizeRequest}
// -----------------------------------------------------------------------------

/// GET projects/{project}/regions/{region}/instanceGroupManagers/{instanceGroupManager}/resizeRequests/{resizeRequest}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_instance_group_manager_resize_requests_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_instance_group_manager_resize_requests_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionInstanceGroupManagerResizeRequestsGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<InstanceGroupManagerResizeRequest>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/instanceGroupManagers/{}/resizeRequests/{}",
        args.project,
        args.region,
        args.instance_group_manager,
        args.resize_request,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: InstanceGroupManagerResizeRequest = serde_json::from_str(&body)
                    .map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/regions/{region}/instanceGroupManagers/{instanceGroupManager}/resizeRequests
// -----------------------------------------------------------------------------

/// POST projects/{project}/regions/{region}/instanceGroupManagers/{instanceGroupManager}/resizeRequests.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_instance_group_manager_resize_requests_insert_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_instance_group_manager_resize_requests_insert_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionInstanceGroupManagerResizeRequestsInsertArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/instanceGroupManagers/{}/resizeRequests",
        args.project,
        args.region,
        args.instance_group_manager,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/regions/{region}/instanceGroupManagers/{instanceGroupManager}/resizeRequests
// -----------------------------------------------------------------------------

/// GET projects/{project}/regions/{region}/instanceGroupManagers/{instanceGroupManager}/resizeRequests.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_instance_group_manager_resize_requests_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_instance_group_manager_resize_requests_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionInstanceGroupManagerResizeRequestsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<RegionInstanceGroupManagerResizeRequestsListResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/instanceGroupManagers/{}/resizeRequests",
        args.project,
        args.region,
        args.instance_group_manager,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.order_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orderBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.return_partial_success {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("returnPartialSuccess=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: RegionInstanceGroupManagerResizeRequestsListResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/regions/{region}/instanceGroupManagers/{instanceGroupManager}/abandonInstances
// -----------------------------------------------------------------------------

/// POST projects/{project}/regions/{region}/instanceGroupManagers/{instanceGroupManager}/abandonInstances.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_instance_group_managers_abandon_instances_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_instance_group_managers_abandon_instances_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionInstanceGroupManagersAbandonInstancesArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/instanceGroupManagers/{}/abandonInstances",
        args.project,
        args.region,
        args.instance_group_manager,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/regions/{region}/instanceGroupManagers/{instanceGroupManager}/applyUpdatesToInstances
// -----------------------------------------------------------------------------

/// POST projects/{project}/regions/{region}/instanceGroupManagers/{instanceGroupManager}/applyUpdatesToInstances.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_instance_group_managers_apply_updates_to_instances_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_instance_group_managers_apply_updates_to_instances_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionInstanceGroupManagersApplyUpdatesToInstancesArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/instanceGroupManagers/{}/applyUpdatesToInstances",
        args.project,
        args.region,
        args.instance_group_manager,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/regions/{region}/instanceGroupManagers/{instanceGroupManager}/createInstances
// -----------------------------------------------------------------------------

/// POST projects/{project}/regions/{region}/instanceGroupManagers/{instanceGroupManager}/createInstances.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_instance_group_managers_create_instances_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_instance_group_managers_create_instances_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionInstanceGroupManagersCreateInstancesArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/instanceGroupManagers/{}/createInstances",
        args.project,
        args.region,
        args.instance_group_manager,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE projects/{project}/regions/{region}/instanceGroupManagers/{instanceGroupManager}
// -----------------------------------------------------------------------------

/// DELETE projects/{project}/regions/{region}/instanceGroupManagers/{instanceGroupManager}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_instance_group_managers_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_instance_group_managers_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionInstanceGroupManagersDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/instanceGroupManagers/{}",
        args.project, args.region, args.instance_group_manager,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/regions/{region}/instanceGroupManagers/{instanceGroupManager}/deleteInstances
// -----------------------------------------------------------------------------

/// POST projects/{project}/regions/{region}/instanceGroupManagers/{instanceGroupManager}/deleteInstances.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_instance_group_managers_delete_instances_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_instance_group_managers_delete_instances_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionInstanceGroupManagersDeleteInstancesArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/instanceGroupManagers/{}/deleteInstances",
        args.project,
        args.region,
        args.instance_group_manager,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/regions/{region}/instanceGroupManagers/{instanceGroupManager}/deletePerInstanceConfigs
// -----------------------------------------------------------------------------

/// POST projects/{project}/regions/{region}/instanceGroupManagers/{instanceGroupManager}/deletePerInstanceConfigs.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_instance_group_managers_delete_per_instance_configs_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_instance_group_managers_delete_per_instance_configs_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionInstanceGroupManagersDeletePerInstanceConfigsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/instanceGroupManagers/{}/deletePerInstanceConfigs",
        args.project,
        args.region,
        args.instance_group_manager,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/regions/{region}/instanceGroupManagers/{instanceGroupManager}
// -----------------------------------------------------------------------------

/// GET projects/{project}/regions/{region}/instanceGroupManagers/{instanceGroupManager}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_instance_group_managers_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_instance_group_managers_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionInstanceGroupManagersGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<InstanceGroupManager>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/instanceGroupManagers/{}",
        args.project, args.region, args.instance_group_manager,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: InstanceGroupManager =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/regions/{region}/instanceGroupManagers
// -----------------------------------------------------------------------------

/// POST projects/{project}/regions/{region}/instanceGroupManagers.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_instance_group_managers_insert_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_instance_group_managers_insert_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionInstanceGroupManagersInsertArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/instanceGroupManagers",
        args.project, args.region,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/regions/{region}/instanceGroupManagers
// -----------------------------------------------------------------------------

/// GET projects/{project}/regions/{region}/instanceGroupManagers.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_instance_group_managers_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_instance_group_managers_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionInstanceGroupManagersListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<RegionInstanceGroupManagerList>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/instanceGroupManagers",
        args.project, args.region,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.order_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orderBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.return_partial_success {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("returnPartialSuccess=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: RegionInstanceGroupManagerList =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/regions/{region}/instanceGroupManagers/{instanceGroupManager}/listErrors
// -----------------------------------------------------------------------------

/// GET projects/{project}/regions/{region}/instanceGroupManagers/{instanceGroupManager}/listErrors.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_instance_group_managers_list_errors_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_instance_group_managers_list_errors_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionInstanceGroupManagersListErrorsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<RegionInstanceGroupManagersListErrorsResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/instanceGroupManagers/{}/listErrors",
        args.project,
        args.region,
        args.instance_group_manager,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.order_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orderBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.return_partial_success {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("returnPartialSuccess=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: RegionInstanceGroupManagersListErrorsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/regions/{region}/instanceGroupManagers/{instanceGroupManager}/listManagedInstances
// -----------------------------------------------------------------------------

/// POST projects/{project}/regions/{region}/instanceGroupManagers/{instanceGroupManager}/listManagedInstances.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_instance_group_managers_list_managed_instances_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_instance_group_managers_list_managed_instances_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionInstanceGroupManagersListManagedInstancesArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<RegionInstanceGroupManagersListInstancesResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/instanceGroupManagers/{}/listManagedInstances",
        args.project,
        args.region,
        args.instance_group_manager,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.order_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orderBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.return_partial_success {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("returnPartialSuccess=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: RegionInstanceGroupManagersListInstancesResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/regions/{region}/instanceGroupManagers/{instanceGroupManager}/listPerInstanceConfigs
// -----------------------------------------------------------------------------

/// POST projects/{project}/regions/{region}/instanceGroupManagers/{instanceGroupManager}/listPerInstanceConfigs.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_instance_group_managers_list_per_instance_configs_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_instance_group_managers_list_per_instance_configs_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionInstanceGroupManagersListPerInstanceConfigsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<RegionInstanceGroupManagersListInstanceConfigsResp>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/instanceGroupManagers/{}/listPerInstanceConfigs",
        args.project,
        args.region,
        args.instance_group_manager,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.order_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orderBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.return_partial_success {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("returnPartialSuccess=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: RegionInstanceGroupManagersListInstanceConfigsResp =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PATCH projects/{project}/regions/{region}/instanceGroupManagers/{instanceGroupManager}
// -----------------------------------------------------------------------------

/// PATCH projects/{project}/regions/{region}/instanceGroupManagers/{instanceGroupManager}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_instance_group_managers_patch_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_instance_group_managers_patch_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionInstanceGroupManagersPatchArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/instanceGroupManagers/{}",
        args.project, args.region, args.instance_group_manager,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .patch(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/regions/{region}/instanceGroupManagers/{instanceGroupManager}/patchPerInstanceConfigs
// -----------------------------------------------------------------------------

/// POST projects/{project}/regions/{region}/instanceGroupManagers/{instanceGroupManager}/patchPerInstanceConfigs.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_instance_group_managers_patch_per_instance_configs_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_instance_group_managers_patch_per_instance_configs_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionInstanceGroupManagersPatchPerInstanceConfigsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/instanceGroupManagers/{}/patchPerInstanceConfigs",
        args.project,
        args.region,
        args.instance_group_manager,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/regions/{region}/instanceGroupManagers/{instanceGroupManager}/recreateInstances
// -----------------------------------------------------------------------------

/// POST projects/{project}/regions/{region}/instanceGroupManagers/{instanceGroupManager}/recreateInstances.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_instance_group_managers_recreate_instances_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_instance_group_managers_recreate_instances_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionInstanceGroupManagersRecreateInstancesArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/instanceGroupManagers/{}/recreateInstances",
        args.project,
        args.region,
        args.instance_group_manager,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/regions/{region}/instanceGroupManagers/{instanceGroupManager}/resize
// -----------------------------------------------------------------------------

/// POST projects/{project}/regions/{region}/instanceGroupManagers/{instanceGroupManager}/resize.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_instance_group_managers_resize_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_instance_group_managers_resize_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionInstanceGroupManagersResizeArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/instanceGroupManagers/{}/resize",
        args.project,
        args.region,
        args.instance_group_manager,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("size=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/regions/{region}/instanceGroupManagers/{instanceGroupManager}/resumeInstances
// -----------------------------------------------------------------------------

/// POST projects/{project}/regions/{region}/instanceGroupManagers/{instanceGroupManager}/resumeInstances.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_instance_group_managers_resume_instances_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_instance_group_managers_resume_instances_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionInstanceGroupManagersResumeInstancesArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/instanceGroupManagers/{}/resumeInstances",
        args.project,
        args.region,
        args.instance_group_manager,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/regions/{region}/instanceGroupManagers/{instanceGroupManager}/setInstanceTemplate
// -----------------------------------------------------------------------------

/// POST projects/{project}/regions/{region}/instanceGroupManagers/{instanceGroupManager}/setInstanceTemplate.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_instance_group_managers_set_instance_template_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_instance_group_managers_set_instance_template_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionInstanceGroupManagersSetInstanceTemplateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/instanceGroupManagers/{}/setInstanceTemplate",
        args.project,
        args.region,
        args.instance_group_manager,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/regions/{region}/instanceGroupManagers/{instanceGroupManager}/setTargetPools
// -----------------------------------------------------------------------------

/// POST projects/{project}/regions/{region}/instanceGroupManagers/{instanceGroupManager}/setTargetPools.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_instance_group_managers_set_target_pools_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_instance_group_managers_set_target_pools_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionInstanceGroupManagersSetTargetPoolsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/instanceGroupManagers/{}/setTargetPools",
        args.project,
        args.region,
        args.instance_group_manager,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/regions/{region}/instanceGroupManagers/{instanceGroupManager}/startInstances
// -----------------------------------------------------------------------------

/// POST projects/{project}/regions/{region}/instanceGroupManagers/{instanceGroupManager}/startInstances.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_instance_group_managers_start_instances_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_instance_group_managers_start_instances_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionInstanceGroupManagersStartInstancesArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/instanceGroupManagers/{}/startInstances",
        args.project,
        args.region,
        args.instance_group_manager,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/regions/{region}/instanceGroupManagers/{instanceGroupManager}/stopInstances
// -----------------------------------------------------------------------------

/// POST projects/{project}/regions/{region}/instanceGroupManagers/{instanceGroupManager}/stopInstances.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_instance_group_managers_stop_instances_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_instance_group_managers_stop_instances_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionInstanceGroupManagersStopInstancesArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/instanceGroupManagers/{}/stopInstances",
        args.project,
        args.region,
        args.instance_group_manager,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/regions/{region}/instanceGroupManagers/{instanceGroupManager}/suspendInstances
// -----------------------------------------------------------------------------

/// POST projects/{project}/regions/{region}/instanceGroupManagers/{instanceGroupManager}/suspendInstances.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_instance_group_managers_suspend_instances_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_instance_group_managers_suspend_instances_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionInstanceGroupManagersSuspendInstancesArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/instanceGroupManagers/{}/suspendInstances",
        args.project,
        args.region,
        args.instance_group_manager,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/regions/{region}/instanceGroupManagers/{instanceGroupManager}/updatePerInstanceConfigs
// -----------------------------------------------------------------------------

/// POST projects/{project}/regions/{region}/instanceGroupManagers/{instanceGroupManager}/updatePerInstanceConfigs.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_instance_group_managers_update_per_instance_configs_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_instance_group_managers_update_per_instance_configs_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionInstanceGroupManagersUpdatePerInstanceConfigsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/instanceGroupManagers/{}/updatePerInstanceConfigs",
        args.project,
        args.region,
        args.instance_group_manager,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/regions/{region}/instanceGroups/{instanceGroup}
// -----------------------------------------------------------------------------

/// GET projects/{project}/regions/{region}/instanceGroups/{instanceGroup}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_instance_groups_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_instance_groups_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionInstanceGroupsGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<InstanceGroup>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/instanceGroups/{}",
        args.project, args.region, args.instance_group,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: InstanceGroup =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/regions/{region}/instanceGroups
// -----------------------------------------------------------------------------

/// GET projects/{project}/regions/{region}/instanceGroups.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_instance_groups_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_instance_groups_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionInstanceGroupsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<RegionInstanceGroupList>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/instanceGroups",
        args.project, args.region,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.order_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orderBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.return_partial_success {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("returnPartialSuccess=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: RegionInstanceGroupList =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/regions/{region}/instanceGroups/{instanceGroup}/listInstances
// -----------------------------------------------------------------------------

/// POST projects/{project}/regions/{region}/instanceGroups/{instanceGroup}/listInstances.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_instance_groups_list_instances_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_instance_groups_list_instances_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionInstanceGroupsListInstancesArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<RegionInstanceGroupsListInstances>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/instanceGroups/{}/listInstances",
        args.project,
        args.region,
        args.instance_group,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.order_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orderBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.return_partial_success {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("returnPartialSuccess=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: RegionInstanceGroupsListInstances = serde_json::from_str(&body)
                    .map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/regions/{region}/instanceGroups/{instanceGroup}/setNamedPorts
// -----------------------------------------------------------------------------

/// POST projects/{project}/regions/{region}/instanceGroups/{instanceGroup}/setNamedPorts.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_instance_groups_set_named_ports_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_instance_groups_set_named_ports_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionInstanceGroupsSetNamedPortsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/instanceGroups/{}/setNamedPorts",
        args.project,
        args.region,
        args.instance_group,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/regions/{region}/instanceGroups/{resource}/testIamPermissions
// -----------------------------------------------------------------------------

/// POST projects/{project}/regions/{region}/instanceGroups/{resource}/testIamPermissions.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_instance_groups_test_iam_permissions_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_instance_groups_test_iam_permissions_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionInstanceGroupsTestIamPermissionsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TestPermissionsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/instanceGroups/{}/testIamPermissions",
        args.project,
        args.region,
        args.resource,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: TestPermissionsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE projects/{project}/regions/{region}/instanceTemplates/{instanceTemplate}
// -----------------------------------------------------------------------------

/// DELETE projects/{project}/regions/{region}/instanceTemplates/{instanceTemplate}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_instance_templates_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_instance_templates_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionInstanceTemplatesDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/instanceTemplates/{}",
        args.project, args.region, args.instance_template,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/regions/{region}/instanceTemplates/{instanceTemplate}
// -----------------------------------------------------------------------------

/// GET projects/{project}/regions/{region}/instanceTemplates/{instanceTemplate}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_instance_templates_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_instance_templates_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionInstanceTemplatesGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<InstanceTemplate>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/instanceTemplates/{}",
        args.project, args.region, args.instance_template,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: InstanceTemplate =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/regions/{region}/instanceTemplates
// -----------------------------------------------------------------------------

/// POST projects/{project}/regions/{region}/instanceTemplates.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_instance_templates_insert_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_instance_templates_insert_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionInstanceTemplatesInsertArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/instanceTemplates",
        args.project, args.region,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/regions/{region}/instanceTemplates
// -----------------------------------------------------------------------------

/// GET projects/{project}/regions/{region}/instanceTemplates.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_instance_templates_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_instance_templates_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionInstanceTemplatesListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<InstanceTemplateList>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/instanceTemplates",
        args.project, args.region,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.order_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orderBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.return_partial_success {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("returnPartialSuccess=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: InstanceTemplateList =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/regions/{region}/instances/bulkInsert
// -----------------------------------------------------------------------------

/// POST projects/{project}/regions/{region}/instances/bulkInsert.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_instances_bulk_insert_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_instances_bulk_insert_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionInstancesBulkInsertArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/instances/bulkInsert",
        args.project, args.region,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE projects/{project}/regions/{region}/instantSnapshotGroups/{instantSnapshotGroup}
// -----------------------------------------------------------------------------

/// DELETE projects/{project}/regions/{region}/instantSnapshotGroups/{instantSnapshotGroup}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_instant_snapshot_groups_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_instant_snapshot_groups_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionInstantSnapshotGroupsDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/instantSnapshotGroups/{}",
        args.project, args.region, args.instant_snapshot_group,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/regions/{region}/instantSnapshotGroups/{instantSnapshotGroup}
// -----------------------------------------------------------------------------

/// GET projects/{project}/regions/{region}/instantSnapshotGroups/{instantSnapshotGroup}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_instant_snapshot_groups_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_instant_snapshot_groups_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionInstantSnapshotGroupsGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<InstantSnapshotGroup>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/instantSnapshotGroups/{}",
        args.project, args.region, args.instant_snapshot_group,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: InstantSnapshotGroup =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/regions/{region}/instantSnapshotGroups/{resource}/getIamPolicy
// -----------------------------------------------------------------------------

/// GET projects/{project}/regions/{region}/instantSnapshotGroups/{resource}/getIamPolicy.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_instant_snapshot_groups_get_iam_policy_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_instant_snapshot_groups_get_iam_policy_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionInstantSnapshotGroupsGetIamPolicyArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/instantSnapshotGroups/{}/getIamPolicy",
        args.project,
        args.region,
        args.resource,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.options_requested_policy_version {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("optionsRequestedPolicyVersion=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Policy =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/regions/{region}/instantSnapshotGroups
// -----------------------------------------------------------------------------

/// POST projects/{project}/regions/{region}/instantSnapshotGroups.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_instant_snapshot_groups_insert_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_instant_snapshot_groups_insert_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionInstantSnapshotGroupsInsertArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/instantSnapshotGroups",
        args.project, args.region,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.source_consistency_group {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("sourceConsistencyGroup=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/regions/{region}/instantSnapshotGroups
// -----------------------------------------------------------------------------

/// GET projects/{project}/regions/{region}/instantSnapshotGroups.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_instant_snapshot_groups_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_instant_snapshot_groups_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionInstantSnapshotGroupsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListInstantSnapshotGroups>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/instantSnapshotGroups",
        args.project, args.region,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.order_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orderBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.return_partial_success {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("returnPartialSuccess=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: ListInstantSnapshotGroups =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/regions/{region}/instantSnapshotGroups/{resource}/setIamPolicy
// -----------------------------------------------------------------------------

/// POST projects/{project}/regions/{region}/instantSnapshotGroups/{resource}/setIamPolicy.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_instant_snapshot_groups_set_iam_policy_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_instant_snapshot_groups_set_iam_policy_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionInstantSnapshotGroupsSetIamPolicyArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/instantSnapshotGroups/{}/setIamPolicy",
        args.project,
        args.region,
        args.resource,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Policy =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/regions/{region}/instantSnapshotGroups/{resource}/testIamPermissions
// -----------------------------------------------------------------------------

/// POST projects/{project}/regions/{region}/instantSnapshotGroups/{resource}/testIamPermissions.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_instant_snapshot_groups_test_iam_permissions_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_instant_snapshot_groups_test_iam_permissions_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionInstantSnapshotGroupsTestIamPermissionsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TestPermissionsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/instantSnapshotGroups/{}/testIamPermissions",
        args.project,
        args.region,
        args.resource,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: TestPermissionsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE projects/{project}/regions/{region}/instantSnapshots/{instantSnapshot}
// -----------------------------------------------------------------------------

/// DELETE projects/{project}/regions/{region}/instantSnapshots/{instantSnapshot}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_instant_snapshots_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_instant_snapshots_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionInstantSnapshotsDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/instantSnapshots/{}",
        args.project, args.region, args.instant_snapshot,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/regions/{region}/instantSnapshots/{instantSnapshot}
// -----------------------------------------------------------------------------

/// GET projects/{project}/regions/{region}/instantSnapshots/{instantSnapshot}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_instant_snapshots_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_instant_snapshots_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionInstantSnapshotsGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<InstantSnapshot>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/instantSnapshots/{}",
        args.project, args.region, args.instant_snapshot,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: InstantSnapshot =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/regions/{region}/instantSnapshots/{resource}/getIamPolicy
// -----------------------------------------------------------------------------

/// GET projects/{project}/regions/{region}/instantSnapshots/{resource}/getIamPolicy.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_instant_snapshots_get_iam_policy_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_instant_snapshots_get_iam_policy_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionInstantSnapshotsGetIamPolicyArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/instantSnapshots/{}/getIamPolicy",
        args.project,
        args.region,
        args.resource,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.options_requested_policy_version {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("optionsRequestedPolicyVersion=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Policy =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/regions/{region}/instantSnapshots
// -----------------------------------------------------------------------------

/// POST projects/{project}/regions/{region}/instantSnapshots.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_instant_snapshots_insert_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_instant_snapshots_insert_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionInstantSnapshotsInsertArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/instantSnapshots",
        args.project, args.region,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/regions/{region}/instantSnapshots
// -----------------------------------------------------------------------------

/// GET projects/{project}/regions/{region}/instantSnapshots.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_instant_snapshots_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_instant_snapshots_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionInstantSnapshotsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<InstantSnapshotList>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/instantSnapshots",
        args.project, args.region,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.order_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orderBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.return_partial_success {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("returnPartialSuccess=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: InstantSnapshotList =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/regions/{region}/instantSnapshots/{resource}/setIamPolicy
// -----------------------------------------------------------------------------

/// POST projects/{project}/regions/{region}/instantSnapshots/{resource}/setIamPolicy.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_instant_snapshots_set_iam_policy_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_instant_snapshots_set_iam_policy_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionInstantSnapshotsSetIamPolicyArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/instantSnapshots/{}/setIamPolicy",
        args.project,
        args.region,
        args.resource,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Policy =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/regions/{region}/instantSnapshots/{resource}/setLabels
// -----------------------------------------------------------------------------

/// POST projects/{project}/regions/{region}/instantSnapshots/{resource}/setLabels.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_instant_snapshots_set_labels_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_instant_snapshots_set_labels_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionInstantSnapshotsSetLabelsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/instantSnapshots/{}/setLabels",
        args.project,
        args.region,
        args.resource,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/regions/{region}/instantSnapshots/{resource}/testIamPermissions
// -----------------------------------------------------------------------------

/// POST projects/{project}/regions/{region}/instantSnapshots/{resource}/testIamPermissions.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_instant_snapshots_test_iam_permissions_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_instant_snapshots_test_iam_permissions_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionInstantSnapshotsTestIamPermissionsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TestPermissionsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/instantSnapshots/{}/testIamPermissions",
        args.project,
        args.region,
        args.resource,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: TestPermissionsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/regions/{region}/networkEndpointGroups/{networkEndpointGroup}/attachNetworkEndpoints
// -----------------------------------------------------------------------------

/// POST projects/{project}/regions/{region}/networkEndpointGroups/{networkEndpointGroup}/attachNetworkEndpoints.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_network_endpoint_groups_attach_network_endpoints_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_network_endpoint_groups_attach_network_endpoints_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionNetworkEndpointGroupsAttachNetworkEndpointsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/networkEndpointGroups/{}/attachNetworkEndpoints",
        args.project,
        args.region,
        args.network_endpoint_group,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE projects/{project}/regions/{region}/networkEndpointGroups/{networkEndpointGroup}
// -----------------------------------------------------------------------------

/// DELETE projects/{project}/regions/{region}/networkEndpointGroups/{networkEndpointGroup}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_network_endpoint_groups_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_network_endpoint_groups_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionNetworkEndpointGroupsDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/networkEndpointGroups/{}",
        args.project, args.region, args.network_endpoint_group,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/regions/{region}/networkEndpointGroups/{networkEndpointGroup}/detachNetworkEndpoints
// -----------------------------------------------------------------------------

/// POST projects/{project}/regions/{region}/networkEndpointGroups/{networkEndpointGroup}/detachNetworkEndpoints.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_network_endpoint_groups_detach_network_endpoints_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_network_endpoint_groups_detach_network_endpoints_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionNetworkEndpointGroupsDetachNetworkEndpointsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/networkEndpointGroups/{}/detachNetworkEndpoints",
        args.project,
        args.region,
        args.network_endpoint_group,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/regions/{region}/networkEndpointGroups/{networkEndpointGroup}
// -----------------------------------------------------------------------------

/// GET projects/{project}/regions/{region}/networkEndpointGroups/{networkEndpointGroup}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_network_endpoint_groups_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_network_endpoint_groups_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionNetworkEndpointGroupsGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<NetworkEndpointGroup>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/networkEndpointGroups/{}",
        args.project, args.region, args.network_endpoint_group,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: NetworkEndpointGroup =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/regions/{region}/networkEndpointGroups
// -----------------------------------------------------------------------------

/// POST projects/{project}/regions/{region}/networkEndpointGroups.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_network_endpoint_groups_insert_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_network_endpoint_groups_insert_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionNetworkEndpointGroupsInsertArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/networkEndpointGroups",
        args.project, args.region,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/regions/{region}/networkEndpointGroups
// -----------------------------------------------------------------------------

/// GET projects/{project}/regions/{region}/networkEndpointGroups.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_network_endpoint_groups_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_network_endpoint_groups_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionNetworkEndpointGroupsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<NetworkEndpointGroupList>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/networkEndpointGroups",
        args.project, args.region,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.order_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orderBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.return_partial_success {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("returnPartialSuccess=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: NetworkEndpointGroupList =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/regions/{region}/networkEndpointGroups/{networkEndpointGroup}/listNetworkEndpoints
// -----------------------------------------------------------------------------

/// POST projects/{project}/regions/{region}/networkEndpointGroups/{networkEndpointGroup}/listNetworkEndpoints.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_network_endpoint_groups_list_network_endpoints_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_network_endpoint_groups_list_network_endpoints_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionNetworkEndpointGroupsListNetworkEndpointsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<NetworkEndpointGroupsListNetworkEndpoints>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/networkEndpointGroups/{}/listNetworkEndpoints",
        args.project,
        args.region,
        args.network_endpoint_group,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.order_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orderBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.return_partial_success {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("returnPartialSuccess=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: NetworkEndpointGroupsListNetworkEndpoints = serde_json::from_str(&body)
                    .map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/regions/{region}/firewallPolicies/{firewallPolicy}/addAssociation
// -----------------------------------------------------------------------------

/// POST projects/{project}/regions/{region}/firewallPolicies/{firewallPolicy}/addAssociation.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_network_firewall_policies_add_association_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_network_firewall_policies_add_association_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionNetworkFirewallPoliciesAddAssociationArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/firewallPolicies/{}/addAssociation",
        args.project,
        args.region,
        args.firewall_policy,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.replace_existing_association {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("replaceExistingAssociation=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/regions/{region}/firewallPolicies/{firewallPolicy}/addRule
// -----------------------------------------------------------------------------

/// POST projects/{project}/regions/{region}/firewallPolicies/{firewallPolicy}/addRule.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_network_firewall_policies_add_rule_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_network_firewall_policies_add_rule_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionNetworkFirewallPoliciesAddRuleArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/firewallPolicies/{}/addRule",
        args.project,
        args.region,
        args.firewall_policy,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.max_priority {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxPriority=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.min_priority {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("minPriority=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/regions/{region}/firewallPolicies/{firewallPolicy}/cloneRules
// -----------------------------------------------------------------------------

/// POST projects/{project}/regions/{region}/firewallPolicies/{firewallPolicy}/cloneRules.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_network_firewall_policies_clone_rules_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_network_firewall_policies_clone_rules_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionNetworkFirewallPoliciesCloneRulesArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/firewallPolicies/{}/cloneRules",
        args.project,
        args.region,
        args.firewall_policy,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.source_firewall_policy {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("sourceFirewallPolicy=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE projects/{project}/regions/{region}/firewallPolicies/{firewallPolicy}
// -----------------------------------------------------------------------------

/// DELETE projects/{project}/regions/{region}/firewallPolicies/{firewallPolicy}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_network_firewall_policies_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_network_firewall_policies_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionNetworkFirewallPoliciesDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/firewallPolicies/{}",
        args.project, args.region, args.firewall_policy,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/regions/{region}/firewallPolicies/{firewallPolicy}
// -----------------------------------------------------------------------------

/// GET projects/{project}/regions/{region}/firewallPolicies/{firewallPolicy}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_network_firewall_policies_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_network_firewall_policies_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionNetworkFirewallPoliciesGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<FirewallPolicy>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/firewallPolicies/{}",
        args.project, args.region, args.firewall_policy,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: FirewallPolicy =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/regions/{region}/firewallPolicies/{firewallPolicy}/getAssociation
// -----------------------------------------------------------------------------

/// GET projects/{project}/regions/{region}/firewallPolicies/{firewallPolicy}/getAssociation.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_network_firewall_policies_get_association_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_network_firewall_policies_get_association_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionNetworkFirewallPoliciesGetAssociationArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<FirewallPolicyAssociation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/firewallPolicies/{}/getAssociation",
        args.project,
        args.region,
        args.firewall_policy,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.name {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("name=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: FirewallPolicyAssociation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/regions/{region}/firewallPolicies/getEffectiveFirewalls
// -----------------------------------------------------------------------------

/// GET projects/{project}/regions/{region}/firewallPolicies/getEffectiveFirewalls.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_network_firewall_policies_get_effective_firewalls_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_network_firewall_policies_get_effective_firewalls_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionNetworkFirewallPoliciesGetEffectiveFirewallsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<RegionNetworkFirewallPoliciesGetEffectiveFirewallsResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/firewallPolicies/getEffectiveFirewalls",
        args.project,
        args.region,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.network {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("network=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: RegionNetworkFirewallPoliciesGetEffectiveFirewallsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/regions/{region}/firewallPolicies/{resource}/getIamPolicy
// -----------------------------------------------------------------------------

/// GET projects/{project}/regions/{region}/firewallPolicies/{resource}/getIamPolicy.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_network_firewall_policies_get_iam_policy_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_network_firewall_policies_get_iam_policy_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionNetworkFirewallPoliciesGetIamPolicyArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/firewallPolicies/{}/getIamPolicy",
        args.project,
        args.region,
        args.resource,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.options_requested_policy_version {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("optionsRequestedPolicyVersion=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Policy =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/regions/{region}/firewallPolicies/{firewallPolicy}/getRule
// -----------------------------------------------------------------------------

/// GET projects/{project}/regions/{region}/firewallPolicies/{firewallPolicy}/getRule.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_network_firewall_policies_get_rule_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_network_firewall_policies_get_rule_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionNetworkFirewallPoliciesGetRuleArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<FirewallPolicyRule>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/firewallPolicies/{}/getRule",
        args.project,
        args.region,
        args.firewall_policy,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.priority {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("priority=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: FirewallPolicyRule =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/regions/{region}/firewallPolicies
// -----------------------------------------------------------------------------

/// POST projects/{project}/regions/{region}/firewallPolicies.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_network_firewall_policies_insert_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_network_firewall_policies_insert_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionNetworkFirewallPoliciesInsertArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/firewallPolicies",
        args.project, args.region,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/regions/{region}/firewallPolicies
// -----------------------------------------------------------------------------

/// GET projects/{project}/regions/{region}/firewallPolicies.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_network_firewall_policies_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_network_firewall_policies_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionNetworkFirewallPoliciesListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<FirewallPolicyList>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/firewallPolicies",
        args.project, args.region,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.order_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orderBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.return_partial_success {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("returnPartialSuccess=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: FirewallPolicyList =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PATCH projects/{project}/regions/{region}/firewallPolicies/{firewallPolicy}
// -----------------------------------------------------------------------------

/// PATCH projects/{project}/regions/{region}/firewallPolicies/{firewallPolicy}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_network_firewall_policies_patch_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_network_firewall_policies_patch_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionNetworkFirewallPoliciesPatchArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/firewallPolicies/{}",
        args.project, args.region, args.firewall_policy,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .patch(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/regions/{region}/firewallPolicies/{firewallPolicy}/patchRule
// -----------------------------------------------------------------------------

/// POST projects/{project}/regions/{region}/firewallPolicies/{firewallPolicy}/patchRule.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_network_firewall_policies_patch_rule_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_network_firewall_policies_patch_rule_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionNetworkFirewallPoliciesPatchRuleArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/firewallPolicies/{}/patchRule",
        args.project,
        args.region,
        args.firewall_policy,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.priority {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("priority=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/regions/{region}/firewallPolicies/{firewallPolicy}/removeAssociation
// -----------------------------------------------------------------------------

/// POST projects/{project}/regions/{region}/firewallPolicies/{firewallPolicy}/removeAssociation.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_network_firewall_policies_remove_association_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_network_firewall_policies_remove_association_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionNetworkFirewallPoliciesRemoveAssociationArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/firewallPolicies/{}/removeAssociation",
        args.project,
        args.region,
        args.firewall_policy,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.name {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("name=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/regions/{region}/firewallPolicies/{firewallPolicy}/removeRule
// -----------------------------------------------------------------------------

/// POST projects/{project}/regions/{region}/firewallPolicies/{firewallPolicy}/removeRule.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_network_firewall_policies_remove_rule_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_network_firewall_policies_remove_rule_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionNetworkFirewallPoliciesRemoveRuleArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/firewallPolicies/{}/removeRule",
        args.project,
        args.region,
        args.firewall_policy,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.priority {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("priority=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/regions/{region}/firewallPolicies/{resource}/setIamPolicy
// -----------------------------------------------------------------------------

/// POST projects/{project}/regions/{region}/firewallPolicies/{resource}/setIamPolicy.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_network_firewall_policies_set_iam_policy_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_network_firewall_policies_set_iam_policy_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionNetworkFirewallPoliciesSetIamPolicyArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/firewallPolicies/{}/setIamPolicy",
        args.project,
        args.region,
        args.resource,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Policy =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/regions/{region}/firewallPolicies/{resource}/testIamPermissions
// -----------------------------------------------------------------------------

/// POST projects/{project}/regions/{region}/firewallPolicies/{resource}/testIamPermissions.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_network_firewall_policies_test_iam_permissions_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_network_firewall_policies_test_iam_permissions_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionNetworkFirewallPoliciesTestIamPermissionsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TestPermissionsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/firewallPolicies/{}/testIamPermissions",
        args.project,
        args.region,
        args.resource,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: TestPermissionsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/aggregated/notificationEndpoints
// -----------------------------------------------------------------------------

/// GET projects/{project}/aggregated/notificationEndpoints.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_notification_endpoints_aggregated_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_notification_endpoints_aggregated_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionNotificationEndpointsAggregatedListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<NotificationEndpointAggregatedList>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/aggregated/notificationEndpoints",
        args.project,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.include_all_scopes {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("includeAllScopes=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.order_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orderBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.return_partial_success {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("returnPartialSuccess=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.service_project_number {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("serviceProjectNumber=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: NotificationEndpointAggregatedList = serde_json::from_str(&body)
                    .map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE projects/{project}/regions/{region}/notificationEndpoints/{notificationEndpoint}
// -----------------------------------------------------------------------------

/// DELETE projects/{project}/regions/{region}/notificationEndpoints/{notificationEndpoint}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_notification_endpoints_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_notification_endpoints_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionNotificationEndpointsDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/notificationEndpoints/{}",
        args.project, args.region, args.notification_endpoint,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/regions/{region}/notificationEndpoints/{notificationEndpoint}
// -----------------------------------------------------------------------------

/// GET projects/{project}/regions/{region}/notificationEndpoints/{notificationEndpoint}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_notification_endpoints_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_notification_endpoints_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionNotificationEndpointsGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<NotificationEndpoint>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/notificationEndpoints/{}",
        args.project, args.region, args.notification_endpoint,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: NotificationEndpoint =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/regions/{region}/notificationEndpoints
// -----------------------------------------------------------------------------

/// POST projects/{project}/regions/{region}/notificationEndpoints.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_notification_endpoints_insert_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_notification_endpoints_insert_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionNotificationEndpointsInsertArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/notificationEndpoints",
        args.project, args.region,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/regions/{region}/notificationEndpoints
// -----------------------------------------------------------------------------

/// GET projects/{project}/regions/{region}/notificationEndpoints.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_notification_endpoints_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_notification_endpoints_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionNotificationEndpointsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<NotificationEndpointList>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/notificationEndpoints",
        args.project, args.region,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.order_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orderBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.return_partial_success {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("returnPartialSuccess=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: NotificationEndpointList =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/regions/{region}/notificationEndpoints/{resource}/testIamPermissions
// -----------------------------------------------------------------------------

/// POST projects/{project}/regions/{region}/notificationEndpoints/{resource}/testIamPermissions.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_notification_endpoints_test_iam_permissions_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_notification_endpoints_test_iam_permissions_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionNotificationEndpointsTestIamPermissionsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TestPermissionsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/notificationEndpoints/{}/testIamPermissions",
        args.project,
        args.region,
        args.resource,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: TestPermissionsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE projects/{project}/regions/{region}/operations/{operation}
// -----------------------------------------------------------------------------

/// DELETE projects/{project}/regions/{region}/operations/{operation}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_operations_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_operations_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionOperationsDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/operations/{}",
        args.project, args.region, args.operation,
    );

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream: _,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/regions/{region}/operations/{operation}
// -----------------------------------------------------------------------------

/// GET projects/{project}/regions/{region}/operations/{operation}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_operations_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_operations_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionOperationsGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/operations/{}",
        args.project, args.region, args.operation,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/regions/{region}/operations
// -----------------------------------------------------------------------------

/// GET projects/{project}/regions/{region}/operations.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_operations_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_operations_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionOperationsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<OperationList>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/operations",
        args.project, args.region,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.order_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orderBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.return_partial_success {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("returnPartialSuccess=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: OperationList =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/regions/{region}/operations/{operation}/wait
// -----------------------------------------------------------------------------

/// POST projects/{project}/regions/{region}/operations/{operation}/wait.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_operations_wait_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_operations_wait_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionOperationsWaitArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/operations/{}/wait",
        args.project, args.region, args.operation,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/regions/{region}/securityPolicies/{securityPolicy}/addRule
// -----------------------------------------------------------------------------

/// POST projects/{project}/regions/{region}/securityPolicies/{securityPolicy}/addRule.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_security_policies_add_rule_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_security_policies_add_rule_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionSecurityPoliciesAddRuleArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/securityPolicies/{}/addRule",
        args.project,
        args.region,
        args.security_policy,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.validate_only {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("validateOnly=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE projects/{project}/regions/{region}/securityPolicies/{securityPolicy}
// -----------------------------------------------------------------------------

/// DELETE projects/{project}/regions/{region}/securityPolicies/{securityPolicy}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_security_policies_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_security_policies_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionSecurityPoliciesDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/securityPolicies/{}",
        args.project, args.region, args.security_policy,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/regions/{region}/securityPolicies/{securityPolicy}
// -----------------------------------------------------------------------------

/// GET projects/{project}/regions/{region}/securityPolicies/{securityPolicy}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_security_policies_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_security_policies_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionSecurityPoliciesGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SecurityPolicy>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/securityPolicies/{}",
        args.project, args.region, args.security_policy,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: SecurityPolicy =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/regions/{region}/securityPolicies/{securityPolicy}/getRule
// -----------------------------------------------------------------------------

/// GET projects/{project}/regions/{region}/securityPolicies/{securityPolicy}/getRule.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_security_policies_get_rule_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_security_policies_get_rule_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionSecurityPoliciesGetRuleArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SecurityPolicyRule>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/securityPolicies/{}/getRule",
        args.project,
        args.region,
        args.security_policy,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.priority {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("priority=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: SecurityPolicyRule =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/regions/{region}/securityPolicies
// -----------------------------------------------------------------------------

/// POST projects/{project}/regions/{region}/securityPolicies.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_security_policies_insert_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_security_policies_insert_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionSecurityPoliciesInsertArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/securityPolicies",
        args.project, args.region,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.validate_only {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("validateOnly=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/regions/{region}/securityPolicies
// -----------------------------------------------------------------------------

/// GET projects/{project}/regions/{region}/securityPolicies.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_security_policies_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_security_policies_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionSecurityPoliciesListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SecurityPolicyList>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/securityPolicies",
        args.project, args.region,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.order_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orderBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.return_partial_success {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("returnPartialSuccess=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: SecurityPolicyList =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PATCH projects/{project}/regions/{region}/securityPolicies/{securityPolicy}
// -----------------------------------------------------------------------------

/// PATCH projects/{project}/regions/{region}/securityPolicies/{securityPolicy}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_security_policies_patch_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_security_policies_patch_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionSecurityPoliciesPatchArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/securityPolicies/{}",
        args.project, args.region, args.security_policy,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.update_mask {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("updateMask=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .patch(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/regions/{region}/securityPolicies/{securityPolicy}/patchRule
// -----------------------------------------------------------------------------

/// POST projects/{project}/regions/{region}/securityPolicies/{securityPolicy}/patchRule.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_security_policies_patch_rule_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_security_policies_patch_rule_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionSecurityPoliciesPatchRuleArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/securityPolicies/{}/patchRule",
        args.project,
        args.region,
        args.security_policy,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.priority {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("priority=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.update_mask {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("updateMask=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.validate_only {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("validateOnly=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/regions/{region}/securityPolicies/{securityPolicy}/removeRule
// -----------------------------------------------------------------------------

/// POST projects/{project}/regions/{region}/securityPolicies/{securityPolicy}/removeRule.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_security_policies_remove_rule_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_security_policies_remove_rule_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionSecurityPoliciesRemoveRuleArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/securityPolicies/{}/removeRule",
        args.project,
        args.region,
        args.security_policy,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.priority {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("priority=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/regions/{region}/securityPolicies/{resource}/setLabels
// -----------------------------------------------------------------------------

/// POST projects/{project}/regions/{region}/securityPolicies/{resource}/setLabels.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_security_policies_set_labels_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_security_policies_set_labels_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionSecurityPoliciesSetLabelsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/securityPolicies/{}/setLabels",
        args.project,
        args.region,
        args.resource,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/regions/{region}/snapshotSettings
// -----------------------------------------------------------------------------

/// GET projects/{project}/regions/{region}/snapshotSettings.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_snapshot_settings_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_snapshot_settings_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionSnapshotSettingsGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SnapshotSettings>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/snapshotSettings",
        args.project, args.region,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: SnapshotSettings =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PATCH projects/{project}/regions/{region}/snapshotSettings
// -----------------------------------------------------------------------------

/// PATCH projects/{project}/regions/{region}/snapshotSettings.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_snapshot_settings_patch_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_snapshot_settings_patch_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionSnapshotSettingsPatchArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/snapshotSettings",
        args.project, args.region,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.update_mask {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("updateMask=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .patch(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE projects/{project}/regions/{region}/snapshots/{snapshot}
// -----------------------------------------------------------------------------

/// DELETE projects/{project}/regions/{region}/snapshots/{snapshot}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_snapshots_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_snapshots_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionSnapshotsDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/snapshots/{}",
        args.project, args.region, args.snapshot,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/regions/{region}/snapshots/{snapshot}
// -----------------------------------------------------------------------------

/// GET projects/{project}/regions/{region}/snapshots/{snapshot}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_snapshots_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_snapshots_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionSnapshotsGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Snapshot>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/snapshots/{}",
        args.project, args.region, args.snapshot,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Snapshot =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/regions/{region}/snapshots/{resource}/getIamPolicy
// -----------------------------------------------------------------------------

/// GET projects/{project}/regions/{region}/snapshots/{resource}/getIamPolicy.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_snapshots_get_iam_policy_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_snapshots_get_iam_policy_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionSnapshotsGetIamPolicyArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/snapshots/{}/getIamPolicy",
        args.project,
        args.region,
        args.resource,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.options_requested_policy_version {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("optionsRequestedPolicyVersion=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Policy =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/regions/{region}/snapshots
// -----------------------------------------------------------------------------

/// POST projects/{project}/regions/{region}/snapshots.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_snapshots_insert_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_snapshots_insert_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionSnapshotsInsertArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/snapshots",
        args.project, args.region,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/regions/{region}/snapshots
// -----------------------------------------------------------------------------

/// GET projects/{project}/regions/{region}/snapshots.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_snapshots_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_snapshots_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionSnapshotsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SnapshotList>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/snapshots",
        args.project, args.region,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.order_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orderBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.return_partial_success {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("returnPartialSuccess=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: SnapshotList =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/regions/{region}/snapshots/{resource}/setIamPolicy
// -----------------------------------------------------------------------------

/// POST projects/{project}/regions/{region}/snapshots/{resource}/setIamPolicy.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_snapshots_set_iam_policy_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_snapshots_set_iam_policy_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionSnapshotsSetIamPolicyArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/snapshots/{}/setIamPolicy",
        args.project,
        args.region,
        args.resource,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Policy =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/regions/{region}/snapshots/{resource}/setLabels
// -----------------------------------------------------------------------------

/// POST projects/{project}/regions/{region}/snapshots/{resource}/setLabels.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_snapshots_set_labels_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_snapshots_set_labels_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionSnapshotsSetLabelsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/snapshots/{}/setLabels",
        args.project, args.region, args.resource,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/regions/{region}/snapshots/{resource}/testIamPermissions
// -----------------------------------------------------------------------------

/// POST projects/{project}/regions/{region}/snapshots/{resource}/testIamPermissions.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_snapshots_test_iam_permissions_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_snapshots_test_iam_permissions_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionSnapshotsTestIamPermissionsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TestPermissionsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/snapshots/{}/testIamPermissions",
        args.project,
        args.region,
        args.resource,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: TestPermissionsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/regions/{region}/snapshots/{snapshot}/updateKmsKey
// -----------------------------------------------------------------------------

/// POST projects/{project}/regions/{region}/snapshots/{snapshot}/updateKmsKey.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_snapshots_update_kms_key_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_snapshots_update_kms_key_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionSnapshotsUpdateKmsKeyArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/snapshots/{}/updateKmsKey",
        args.project,
        args.region,
        args.snapshot,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE projects/{project}/regions/{region}/sslCertificates/{sslCertificate}
// -----------------------------------------------------------------------------

/// DELETE projects/{project}/regions/{region}/sslCertificates/{sslCertificate}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_ssl_certificates_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_ssl_certificates_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionSslCertificatesDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/sslCertificates/{}",
        args.project, args.region, args.ssl_certificate,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/regions/{region}/sslCertificates/{sslCertificate}
// -----------------------------------------------------------------------------

/// GET projects/{project}/regions/{region}/sslCertificates/{sslCertificate}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_ssl_certificates_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_ssl_certificates_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionSslCertificatesGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SslCertificate>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/sslCertificates/{}",
        args.project, args.region, args.ssl_certificate,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: SslCertificate =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/regions/{region}/sslCertificates
// -----------------------------------------------------------------------------

/// POST projects/{project}/regions/{region}/sslCertificates.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_ssl_certificates_insert_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_ssl_certificates_insert_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionSslCertificatesInsertArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/sslCertificates",
        args.project, args.region,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/regions/{region}/sslCertificates
// -----------------------------------------------------------------------------

/// GET projects/{project}/regions/{region}/sslCertificates.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_ssl_certificates_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_ssl_certificates_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionSslCertificatesListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SslCertificateList>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/sslCertificates",
        args.project, args.region,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.order_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orderBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.return_partial_success {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("returnPartialSuccess=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: SslCertificateList =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE projects/{project}/regions/{region}/sslPolicies/{sslPolicy}
// -----------------------------------------------------------------------------

/// DELETE projects/{project}/regions/{region}/sslPolicies/{sslPolicy}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_ssl_policies_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_ssl_policies_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionSslPoliciesDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/sslPolicies/{}",
        args.project, args.region, args.ssl_policy,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/regions/{region}/sslPolicies/{sslPolicy}
// -----------------------------------------------------------------------------

/// GET projects/{project}/regions/{region}/sslPolicies/{sslPolicy}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_ssl_policies_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_ssl_policies_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionSslPoliciesGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SslPolicy>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/sslPolicies/{}",
        args.project, args.region, args.ssl_policy,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: SslPolicy =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/regions/{region}/sslPolicies
// -----------------------------------------------------------------------------

/// POST projects/{project}/regions/{region}/sslPolicies.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_ssl_policies_insert_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_ssl_policies_insert_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionSslPoliciesInsertArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/sslPolicies",
        args.project, args.region,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/regions/{region}/sslPolicies
// -----------------------------------------------------------------------------

/// GET projects/{project}/regions/{region}/sslPolicies.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_ssl_policies_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_ssl_policies_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionSslPoliciesListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SslPoliciesList>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/sslPolicies",
        args.project, args.region,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.order_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orderBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.return_partial_success {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("returnPartialSuccess=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: SslPoliciesList =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/regions/{region}/sslPolicies/listAvailableFeatures
// -----------------------------------------------------------------------------

/// GET projects/{project}/regions/{region}/sslPolicies/listAvailableFeatures.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_ssl_policies_list_available_features_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_ssl_policies_list_available_features_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionSslPoliciesListAvailableFeaturesArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<SslPoliciesListAvailableFeaturesResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/sslPolicies/listAvailableFeatures",
        args.project,
        args.region,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.order_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orderBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.return_partial_success {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("returnPartialSuccess=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: SslPoliciesListAvailableFeaturesResponse = serde_json::from_str(&body)
                    .map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PATCH projects/{project}/regions/{region}/sslPolicies/{sslPolicy}
// -----------------------------------------------------------------------------

/// PATCH projects/{project}/regions/{region}/sslPolicies/{sslPolicy}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_ssl_policies_patch_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_ssl_policies_patch_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionSslPoliciesPatchArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/sslPolicies/{}",
        args.project, args.region, args.ssl_policy,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .patch(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE projects/{project}/regions/{region}/targetHttpProxies/{targetHttpProxy}
// -----------------------------------------------------------------------------

/// DELETE projects/{project}/regions/{region}/targetHttpProxies/{targetHttpProxy}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_target_http_proxies_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_target_http_proxies_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionTargetHttpProxiesDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/targetHttpProxies/{}",
        args.project, args.region, args.target_http_proxy,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/regions/{region}/targetHttpProxies/{targetHttpProxy}
// -----------------------------------------------------------------------------

/// GET projects/{project}/regions/{region}/targetHttpProxies/{targetHttpProxy}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_target_http_proxies_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_target_http_proxies_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionTargetHttpProxiesGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TargetHttpProxy>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/targetHttpProxies/{}",
        args.project, args.region, args.target_http_proxy,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: TargetHttpProxy =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/regions/{region}/targetHttpProxies
// -----------------------------------------------------------------------------

/// POST projects/{project}/regions/{region}/targetHttpProxies.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_target_http_proxies_insert_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_target_http_proxies_insert_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionTargetHttpProxiesInsertArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/targetHttpProxies",
        args.project, args.region,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/regions/{region}/targetHttpProxies
// -----------------------------------------------------------------------------

/// GET projects/{project}/regions/{region}/targetHttpProxies.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_target_http_proxies_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_target_http_proxies_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionTargetHttpProxiesListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TargetHttpProxyList>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/targetHttpProxies",
        args.project, args.region,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.order_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orderBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.return_partial_success {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("returnPartialSuccess=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: TargetHttpProxyList =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/regions/{region}/targetHttpProxies/{targetHttpProxy}/setUrlMap
// -----------------------------------------------------------------------------

/// POST projects/{project}/regions/{region}/targetHttpProxies/{targetHttpProxy}/setUrlMap.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_target_http_proxies_set_url_map_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_target_http_proxies_set_url_map_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionTargetHttpProxiesSetUrlMapArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/targetHttpProxies/{}/setUrlMap",
        args.project,
        args.region,
        args.target_http_proxy,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE projects/{project}/regions/{region}/targetHttpsProxies/{targetHttpsProxy}
// -----------------------------------------------------------------------------

/// DELETE projects/{project}/regions/{region}/targetHttpsProxies/{targetHttpsProxy}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_target_https_proxies_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_target_https_proxies_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionTargetHttpsProxiesDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/targetHttpsProxies/{}",
        args.project, args.region, args.target_https_proxy,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/regions/{region}/targetHttpsProxies/{targetHttpsProxy}
// -----------------------------------------------------------------------------

/// GET projects/{project}/regions/{region}/targetHttpsProxies/{targetHttpsProxy}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_target_https_proxies_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_target_https_proxies_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionTargetHttpsProxiesGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TargetHttpsProxy>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/targetHttpsProxies/{}",
        args.project, args.region, args.target_https_proxy,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: TargetHttpsProxy =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/regions/{region}/targetHttpsProxies
// -----------------------------------------------------------------------------

/// POST projects/{project}/regions/{region}/targetHttpsProxies.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_target_https_proxies_insert_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_target_https_proxies_insert_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionTargetHttpsProxiesInsertArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/targetHttpsProxies",
        args.project, args.region,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/regions/{region}/targetHttpsProxies
// -----------------------------------------------------------------------------

/// GET projects/{project}/regions/{region}/targetHttpsProxies.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_target_https_proxies_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_target_https_proxies_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionTargetHttpsProxiesListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TargetHttpsProxyList>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/targetHttpsProxies",
        args.project, args.region,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.order_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orderBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.return_partial_success {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("returnPartialSuccess=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: TargetHttpsProxyList =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PATCH projects/{project}/regions/{region}/targetHttpsProxies/{targetHttpsProxy}
// -----------------------------------------------------------------------------

/// PATCH projects/{project}/regions/{region}/targetHttpsProxies/{targetHttpsProxy}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_target_https_proxies_patch_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_target_https_proxies_patch_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionTargetHttpsProxiesPatchArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/targetHttpsProxies/{}",
        args.project, args.region, args.target_https_proxy,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .patch(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/regions/{region}/targetHttpsProxies/{targetHttpsProxy}/setSslCertificates
// -----------------------------------------------------------------------------

/// POST projects/{project}/regions/{region}/targetHttpsProxies/{targetHttpsProxy}/setSslCertificates.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_target_https_proxies_set_ssl_certificates_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_target_https_proxies_set_ssl_certificates_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionTargetHttpsProxiesSetSslCertificatesArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/targetHttpsProxies/{}/setSslCertificates",
        args.project,
        args.region,
        args.target_https_proxy,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/regions/{region}/targetHttpsProxies/{targetHttpsProxy}/setUrlMap
// -----------------------------------------------------------------------------

/// POST projects/{project}/regions/{region}/targetHttpsProxies/{targetHttpsProxy}/setUrlMap.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_target_https_proxies_set_url_map_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_target_https_proxies_set_url_map_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionTargetHttpsProxiesSetUrlMapArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/targetHttpsProxies/{}/setUrlMap",
        args.project,
        args.region,
        args.target_https_proxy,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE projects/{project}/regions/{region}/targetTcpProxies/{targetTcpProxy}
// -----------------------------------------------------------------------------

/// DELETE projects/{project}/regions/{region}/targetTcpProxies/{targetTcpProxy}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_target_tcp_proxies_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_target_tcp_proxies_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionTargetTcpProxiesDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/targetTcpProxies/{}",
        args.project, args.region, args.target_tcp_proxy,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/regions/{region}/targetTcpProxies/{targetTcpProxy}
// -----------------------------------------------------------------------------

/// GET projects/{project}/regions/{region}/targetTcpProxies/{targetTcpProxy}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_target_tcp_proxies_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_target_tcp_proxies_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionTargetTcpProxiesGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TargetTcpProxy>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/targetTcpProxies/{}",
        args.project, args.region, args.target_tcp_proxy,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: TargetTcpProxy =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/regions/{region}/targetTcpProxies
// -----------------------------------------------------------------------------

/// POST projects/{project}/regions/{region}/targetTcpProxies.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_target_tcp_proxies_insert_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_target_tcp_proxies_insert_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionTargetTcpProxiesInsertArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/targetTcpProxies",
        args.project, args.region,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/regions/{region}/targetTcpProxies
// -----------------------------------------------------------------------------

/// GET projects/{project}/regions/{region}/targetTcpProxies.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_target_tcp_proxies_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_target_tcp_proxies_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionTargetTcpProxiesListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TargetTcpProxyList>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/targetTcpProxies",
        args.project, args.region,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.order_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orderBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.return_partial_success {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("returnPartialSuccess=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: TargetTcpProxyList =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE projects/{project}/regions/{region}/urlMaps/{urlMap}
// -----------------------------------------------------------------------------

/// DELETE projects/{project}/regions/{region}/urlMaps/{urlMap}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_url_maps_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_url_maps_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionUrlMapsDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/urlMaps/{}",
        args.project, args.region, args.url_map,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/regions/{region}/urlMaps/{urlMap}
// -----------------------------------------------------------------------------

/// GET projects/{project}/regions/{region}/urlMaps/{urlMap}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_url_maps_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_url_maps_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionUrlMapsGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<UrlMap>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/urlMaps/{}",
        args.project, args.region, args.url_map,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: UrlMap =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/regions/{region}/urlMaps
// -----------------------------------------------------------------------------

/// POST projects/{project}/regions/{region}/urlMaps.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_url_maps_insert_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_url_maps_insert_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionUrlMapsInsertArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/urlMaps",
        args.project, args.region,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/regions/{region}/urlMaps
// -----------------------------------------------------------------------------

/// GET projects/{project}/regions/{region}/urlMaps.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_url_maps_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_url_maps_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionUrlMapsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<UrlMapList>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/urlMaps",
        args.project, args.region,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.order_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orderBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.return_partial_success {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("returnPartialSuccess=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: UrlMapList =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PATCH projects/{project}/regions/{region}/urlMaps/{urlMap}
// -----------------------------------------------------------------------------

/// PATCH projects/{project}/regions/{region}/urlMaps/{urlMap}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_url_maps_patch_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_url_maps_patch_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionUrlMapsPatchArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/urlMaps/{}",
        args.project, args.region, args.url_map,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .patch(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PUT projects/{project}/regions/{region}/urlMaps/{urlMap}
// -----------------------------------------------------------------------------

/// PUT projects/{project}/regions/{region}/urlMaps/{urlMap}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_url_maps_update_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_url_maps_update_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionUrlMapsUpdateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/urlMaps/{}",
        args.project, args.region, args.url_map,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .put(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/regions/{region}/urlMaps/{urlMap}/validate
// -----------------------------------------------------------------------------

/// POST projects/{project}/regions/{region}/urlMaps/{urlMap}/validate.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_url_maps_validate_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_url_maps_validate_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionUrlMapsValidateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<UrlMapsValidateResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/urlMaps/{}/validate",
        args.project, args.region, args.url_map,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: UrlMapsValidateResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/regions/{region}/zones
// -----------------------------------------------------------------------------

/// GET projects/{project}/regions/{region}/zones.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_zones_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_zones_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionZonesListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ZoneList>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/zones",
        args.project, args.region,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.order_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orderBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.return_partial_success {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("returnPartialSuccess=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: ZoneList =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/regions/{region}
// -----------------------------------------------------------------------------

/// GET projects/{project}/regions/{region}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_regions_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_regions_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionsGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Region>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}",
        args.project, args.region,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Region =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/regions
// -----------------------------------------------------------------------------

/// GET projects/{project}/regions.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_regions_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_regions_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<RegionList>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions",
        args.project,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.order_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orderBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.return_partial_success {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("returnPartialSuccess=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: RegionList =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/zones/{zone}/reservations/{reservation}/reservationBlocks/{reservationBlock}
// -----------------------------------------------------------------------------

/// GET projects/{project}/zones/{zone}/reservations/{reservation}/reservationBlocks/{reservationBlock}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_reservation_blocks_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_reservation_blocks_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeReservationBlocksGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ReservationBlocksGetResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/reservations/{}/reservationBlocks/{}",
        args.project,
        args.zone,
        args.reservation,
        args.reservation_block,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.view {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("view=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: ReservationBlocksGetResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/zones/{zone}/reservations/{parentResource}/reservationBlocks/{resource}/getIamPolicy
// -----------------------------------------------------------------------------

/// GET projects/{project}/zones/{zone}/reservations/{parentResource}/reservationBlocks/{resource}/getIamPolicy.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_reservation_blocks_get_iam_policy_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_reservation_blocks_get_iam_policy_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeReservationBlocksGetIamPolicyArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/reservations/{}/reservationBlocks/{}/getIamPolicy",
        args.project,
        args.zone,
        args.parent_resource,
        args.resource,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.options_requested_policy_version {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("optionsRequestedPolicyVersion=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Policy =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/zones/{zone}/reservations/{reservation}/reservationBlocks
// -----------------------------------------------------------------------------

/// GET projects/{project}/zones/{zone}/reservations/{reservation}/reservationBlocks.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_reservation_blocks_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_reservation_blocks_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeReservationBlocksListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ReservationBlocksListResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/reservations/{}/reservationBlocks",
        args.project,
        args.zone,
        args.reservation,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.order_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orderBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.return_partial_success {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("returnPartialSuccess=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: ReservationBlocksListResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/zones/{zone}/reservations/{reservation}/reservationBlocks/{reservationBlock}/performMaintenance
// -----------------------------------------------------------------------------

/// POST projects/{project}/zones/{zone}/reservations/{reservation}/reservationBlocks/{reservationBlock}/performMaintenance.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_reservation_blocks_perform_maintenance_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_reservation_blocks_perform_maintenance_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeReservationBlocksPerformMaintenanceArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/reservations/{}/reservationBlocks/{}/performMaintenance",
        args.project,
        args.zone,
        args.reservation,
        args.reservation_block,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/zones/{zone}/reservations/{parentResource}/reservationBlocks/{resource}/setIamPolicy
// -----------------------------------------------------------------------------

/// POST projects/{project}/zones/{zone}/reservations/{parentResource}/reservationBlocks/{resource}/setIamPolicy.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_reservation_blocks_set_iam_policy_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_reservation_blocks_set_iam_policy_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeReservationBlocksSetIamPolicyArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/reservations/{}/reservationBlocks/{}/setIamPolicy",
        args.project,
        args.zone,
        args.parent_resource,
        args.resource,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Policy =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/zones/{zone}/reservations/{parentResource}/reservationBlocks/{resource}/testIamPermissions
// -----------------------------------------------------------------------------

/// POST projects/{project}/zones/{zone}/reservations/{parentResource}/reservationBlocks/{resource}/testIamPermissions.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_reservation_blocks_test_iam_permissions_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_reservation_blocks_test_iam_permissions_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeReservationBlocksTestIamPermissionsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TestPermissionsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/reservations/{}/reservationBlocks/{}/testIamPermissions",
        args.project,
        args.zone,
        args.parent_resource,
        args.resource,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: TestPermissionsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/zones/{zone}/{+parentName}/reservationSlots/{reservationSlot}
// -----------------------------------------------------------------------------

/// GET projects/{project}/zones/{zone}/{+parentName}/reservationSlots/{reservationSlot}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_reservation_slots_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_reservation_slots_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeReservationSlotsGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ReservationSlotsGetResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/{}/reservationSlots/{}",
        args.project, args.zone, args.parent_name, args.reservation_slot,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: ReservationSlotsGetResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/zones/{zone}/{+parentName}/reservationSlots/{reservationSlot}/getVersion
// -----------------------------------------------------------------------------

/// POST projects/{project}/zones/{zone}/{+parentName}/reservationSlots/{reservationSlot}/getVersion.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_reservation_slots_get_version_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_reservation_slots_get_version_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeReservationSlotsGetVersionArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/{}/reservationSlots/{}/getVersion",
        args.project,
        args.zone,
        args.parent_name,
        args.reservation_slot,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/zones/{zone}/{+parentName}/reservationSlots
// -----------------------------------------------------------------------------

/// GET projects/{project}/zones/{zone}/{+parentName}/reservationSlots.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_reservation_slots_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_reservation_slots_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeReservationSlotsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ReservationSlotsListResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/{}/reservationSlots",
        args.project, args.zone, args.parent_name,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.order_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orderBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.return_partial_success {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("returnPartialSuccess=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: ReservationSlotsListResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/zones/{zone}/{+parentName}/reservationSlots/{reservationSlot}
// -----------------------------------------------------------------------------

/// POST projects/{project}/zones/{zone}/{+parentName}/reservationSlots/{reservationSlot}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_reservation_slots_update_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_reservation_slots_update_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeReservationSlotsUpdateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/{}/reservationSlots/{}",
        args.project, args.zone, args.parent_name, args.reservation_slot,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.update_mask {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("updateMask=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/zones/{zone}/{+parentName}/reservationSubBlocks/{reservationSubBlock}
// -----------------------------------------------------------------------------

/// GET projects/{project}/zones/{zone}/{+parentName}/reservationSubBlocks/{reservationSubBlock}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_reservation_sub_blocks_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_reservation_sub_blocks_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeReservationSubBlocksGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ReservationSubBlocksGetResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/{}/reservationSubBlocks/{}",
        args.project, args.zone, args.parent_name, args.reservation_sub_block,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.view {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("view=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: ReservationSubBlocksGetResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/zones/{zone}/{+parentResource}/reservationSubBlocks/{resource}/getIamPolicy
// -----------------------------------------------------------------------------

/// GET projects/{project}/zones/{zone}/{+parentResource}/reservationSubBlocks/{resource}/getIamPolicy.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_reservation_sub_blocks_get_iam_policy_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_reservation_sub_blocks_get_iam_policy_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeReservationSubBlocksGetIamPolicyArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/{}/reservationSubBlocks/{}/getIamPolicy",
        args.project,
        args.zone,
        args.parent_resource,
        args.resource,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.options_requested_policy_version {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("optionsRequestedPolicyVersion=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Policy =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/zones/{zone}/{+parentName}/reservationSubBlocks/{reservationSubBlock}/getVersion
// -----------------------------------------------------------------------------

/// POST projects/{project}/zones/{zone}/{+parentName}/reservationSubBlocks/{reservationSubBlock}/getVersion.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_reservation_sub_blocks_get_version_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_reservation_sub_blocks_get_version_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeReservationSubBlocksGetVersionArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/{}/reservationSubBlocks/{}/getVersion",
        args.project,
        args.zone,
        args.parent_name,
        args.reservation_sub_block,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/zones/{zone}/{+parentName}/reservationSubBlocks
// -----------------------------------------------------------------------------

/// GET projects/{project}/zones/{zone}/{+parentName}/reservationSubBlocks.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_reservation_sub_blocks_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_reservation_sub_blocks_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeReservationSubBlocksListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ReservationSubBlocksListResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/{}/reservationSubBlocks",
        args.project, args.zone, args.parent_name,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.order_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orderBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.return_partial_success {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("returnPartialSuccess=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: ReservationSubBlocksListResponse = serde_json::from_str(&body)
                    .map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/zones/{zone}/{+parentName}/reservationSubBlocks/{reservationSubBlock}/performMaintenance
// -----------------------------------------------------------------------------

/// POST projects/{project}/zones/{zone}/{+parentName}/reservationSubBlocks/{reservationSubBlock}/performMaintenance.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_reservation_sub_blocks_perform_maintenance_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_reservation_sub_blocks_perform_maintenance_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeReservationSubBlocksPerformMaintenanceArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/{}/reservationSubBlocks/{}/performMaintenance",
        args.project,
        args.zone,
        args.parent_name,
        args.reservation_sub_block,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/zones/{zone}/{+parentName}/reservationSubBlocks/{reservationSubBlock}/reportFaulty
// -----------------------------------------------------------------------------

/// POST projects/{project}/zones/{zone}/{+parentName}/reservationSubBlocks/{reservationSubBlock}/reportFaulty.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_reservation_sub_blocks_report_faulty_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_reservation_sub_blocks_report_faulty_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeReservationSubBlocksReportFaultyArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/{}/reservationSubBlocks/{}/reportFaulty",
        args.project,
        args.zone,
        args.parent_name,
        args.reservation_sub_block,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/zones/{zone}/{+parentResource}/reservationSubBlocks/{resource}/setIamPolicy
// -----------------------------------------------------------------------------

/// POST projects/{project}/zones/{zone}/{+parentResource}/reservationSubBlocks/{resource}/setIamPolicy.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_reservation_sub_blocks_set_iam_policy_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_reservation_sub_blocks_set_iam_policy_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeReservationSubBlocksSetIamPolicyArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/{}/reservationSubBlocks/{}/setIamPolicy",
        args.project,
        args.zone,
        args.parent_resource,
        args.resource,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Policy =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/zones/{zone}/{+parentResource}/reservationSubBlocks/{resource}/testIamPermissions
// -----------------------------------------------------------------------------

/// POST projects/{project}/zones/{zone}/{+parentResource}/reservationSubBlocks/{resource}/testIamPermissions.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_reservation_sub_blocks_test_iam_permissions_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_reservation_sub_blocks_test_iam_permissions_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeReservationSubBlocksTestIamPermissionsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TestPermissionsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/{}/reservationSubBlocks/{}/testIamPermissions",
        args.project,
        args.zone,
        args.parent_resource,
        args.resource,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: TestPermissionsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/aggregated/reservations
// -----------------------------------------------------------------------------

/// GET projects/{project}/aggregated/reservations.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_reservations_aggregated_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_reservations_aggregated_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeReservationsAggregatedListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ReservationAggregatedList>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/aggregated/reservations",
        args.project,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.include_all_scopes {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("includeAllScopes=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.order_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orderBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.return_partial_success {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("returnPartialSuccess=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.service_project_number {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("serviceProjectNumber=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: ReservationAggregatedList =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE projects/{project}/zones/{zone}/reservations/{reservation}
// -----------------------------------------------------------------------------

/// DELETE projects/{project}/zones/{zone}/reservations/{reservation}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_reservations_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_reservations_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeReservationsDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/reservations/{}",
        args.project, args.zone, args.reservation,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/zones/{zone}/reservations/{reservation}
// -----------------------------------------------------------------------------

/// GET projects/{project}/zones/{zone}/reservations/{reservation}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_reservations_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_reservations_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeReservationsGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Reservation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/reservations/{}",
        args.project, args.zone, args.reservation,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Reservation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/zones/{zone}/reservations/{resource}/getIamPolicy
// -----------------------------------------------------------------------------

/// GET projects/{project}/zones/{zone}/reservations/{resource}/getIamPolicy.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_reservations_get_iam_policy_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_reservations_get_iam_policy_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeReservationsGetIamPolicyArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/reservations/{}/getIamPolicy",
        args.project,
        args.zone,
        args.resource,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.options_requested_policy_version {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("optionsRequestedPolicyVersion=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Policy =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/zones/{zone}/reservations
// -----------------------------------------------------------------------------

/// POST projects/{project}/zones/{zone}/reservations.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_reservations_insert_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_reservations_insert_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeReservationsInsertArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/reservations",
        args.project, args.zone,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/zones/{zone}/reservations
// -----------------------------------------------------------------------------

/// GET projects/{project}/zones/{zone}/reservations.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_reservations_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_reservations_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeReservationsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ReservationList>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/reservations",
        args.project, args.zone,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.order_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orderBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.return_partial_success {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("returnPartialSuccess=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: ReservationList =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/zones/{zone}/reservations/{reservation}/performMaintenance
// -----------------------------------------------------------------------------

/// POST projects/{project}/zones/{zone}/reservations/{reservation}/performMaintenance.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_reservations_perform_maintenance_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_reservations_perform_maintenance_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeReservationsPerformMaintenanceArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/reservations/{}/performMaintenance",
        args.project,
        args.zone,
        args.reservation,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/zones/{zone}/reservations/{reservation}/resize
// -----------------------------------------------------------------------------

/// POST projects/{project}/zones/{zone}/reservations/{reservation}/resize.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_reservations_resize_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_reservations_resize_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeReservationsResizeArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/reservations/{}/resize",
        args.project, args.zone, args.reservation,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/zones/{zone}/reservations/{resource}/setIamPolicy
// -----------------------------------------------------------------------------

/// POST projects/{project}/zones/{zone}/reservations/{resource}/setIamPolicy.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_reservations_set_iam_policy_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_reservations_set_iam_policy_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeReservationsSetIamPolicyArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/reservations/{}/setIamPolicy",
        args.project,
        args.zone,
        args.resource,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Policy =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/zones/{zone}/reservations/{resource}/testIamPermissions
// -----------------------------------------------------------------------------

/// POST projects/{project}/zones/{zone}/reservations/{resource}/testIamPermissions.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_reservations_test_iam_permissions_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_reservations_test_iam_permissions_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeReservationsTestIamPermissionsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TestPermissionsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/reservations/{}/testIamPermissions",
        args.project,
        args.zone,
        args.resource,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: TestPermissionsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PATCH projects/{project}/zones/{zone}/reservations/{reservation}
// -----------------------------------------------------------------------------

/// PATCH projects/{project}/zones/{zone}/reservations/{reservation}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_reservations_update_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_reservations_update_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeReservationsUpdateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/reservations/{}",
        args.project, args.zone, args.reservation,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.paths {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("paths=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.update_mask {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("updateMask=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .patch(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/aggregated/resourcePolicies
// -----------------------------------------------------------------------------

/// GET projects/{project}/aggregated/resourcePolicies.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_resource_policies_aggregated_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_resource_policies_aggregated_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeResourcePoliciesAggregatedListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ResourcePolicyAggregatedList>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/aggregated/resourcePolicies",
        args.project,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.include_all_scopes {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("includeAllScopes=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.order_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orderBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.return_partial_success {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("returnPartialSuccess=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.service_project_number {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("serviceProjectNumber=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: ResourcePolicyAggregatedList =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE projects/{project}/regions/{region}/resourcePolicies/{resourcePolicy}
// -----------------------------------------------------------------------------

/// DELETE projects/{project}/regions/{region}/resourcePolicies/{resourcePolicy}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_resource_policies_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_resource_policies_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeResourcePoliciesDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/resourcePolicies/{}",
        args.project, args.region, args.resource_policy,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/regions/{region}/resourcePolicies/{resourcePolicy}
// -----------------------------------------------------------------------------

/// GET projects/{project}/regions/{region}/resourcePolicies/{resourcePolicy}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_resource_policies_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_resource_policies_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeResourcePoliciesGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ResourcePolicy>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/resourcePolicies/{}",
        args.project, args.region, args.resource_policy,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: ResourcePolicy =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/regions/{region}/resourcePolicies/{resource}/getIamPolicy
// -----------------------------------------------------------------------------

/// GET projects/{project}/regions/{region}/resourcePolicies/{resource}/getIamPolicy.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_resource_policies_get_iam_policy_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_resource_policies_get_iam_policy_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeResourcePoliciesGetIamPolicyArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/resourcePolicies/{}/getIamPolicy",
        args.project,
        args.region,
        args.resource,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.options_requested_policy_version {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("optionsRequestedPolicyVersion=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Policy =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/regions/{region}/resourcePolicies
// -----------------------------------------------------------------------------

/// POST projects/{project}/regions/{region}/resourcePolicies.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_resource_policies_insert_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_resource_policies_insert_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeResourcePoliciesInsertArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/resourcePolicies",
        args.project, args.region,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/regions/{region}/resourcePolicies
// -----------------------------------------------------------------------------

/// GET projects/{project}/regions/{region}/resourcePolicies.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_resource_policies_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_resource_policies_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeResourcePoliciesListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ResourcePolicyList>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/resourcePolicies",
        args.project, args.region,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.order_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orderBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.return_partial_success {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("returnPartialSuccess=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: ResourcePolicyList =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PATCH projects/{project}/regions/{region}/resourcePolicies/{resourcePolicy}
// -----------------------------------------------------------------------------

/// PATCH projects/{project}/regions/{region}/resourcePolicies/{resourcePolicy}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_resource_policies_patch_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_resource_policies_patch_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeResourcePoliciesPatchArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/resourcePolicies/{}",
        args.project, args.region, args.resource_policy,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.update_mask {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("updateMask=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .patch(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/regions/{region}/resourcePolicies/{resource}/setIamPolicy
// -----------------------------------------------------------------------------

/// POST projects/{project}/regions/{region}/resourcePolicies/{resource}/setIamPolicy.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_resource_policies_set_iam_policy_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_resource_policies_set_iam_policy_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeResourcePoliciesSetIamPolicyArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/resourcePolicies/{}/setIamPolicy",
        args.project,
        args.region,
        args.resource,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Policy =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/regions/{region}/resourcePolicies/{resource}/testIamPermissions
// -----------------------------------------------------------------------------

/// POST projects/{project}/regions/{region}/resourcePolicies/{resource}/testIamPermissions.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_resource_policies_test_iam_permissions_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_resource_policies_test_iam_permissions_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeResourcePoliciesTestIamPermissionsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TestPermissionsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/resourcePolicies/{}/testIamPermissions",
        args.project,
        args.region,
        args.resource,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: TestPermissionsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/aggregated/routers
// -----------------------------------------------------------------------------

/// GET projects/{project}/aggregated/routers.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_routers_aggregated_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_routers_aggregated_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRoutersAggregatedListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<RouterAggregatedList>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/aggregated/routers",
        args.project,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.include_all_scopes {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("includeAllScopes=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.order_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orderBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.return_partial_success {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("returnPartialSuccess=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.service_project_number {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("serviceProjectNumber=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: RouterAggregatedList =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE projects/{project}/regions/{region}/routers/{router}
// -----------------------------------------------------------------------------

/// DELETE projects/{project}/regions/{region}/routers/{router}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_routers_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_routers_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRoutersDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/routers/{}",
        args.project, args.region, args.router,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/regions/{region}/routers/{router}/deleteRoutePolicy
// -----------------------------------------------------------------------------

/// POST projects/{project}/regions/{region}/routers/{router}/deleteRoutePolicy.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_routers_delete_route_policy_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_routers_delete_route_policy_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRoutersDeleteRoutePolicyArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/routers/{}/deleteRoutePolicy",
        args.project,
        args.region,
        args.router,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.policy {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("policy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/regions/{region}/routers/{router}
// -----------------------------------------------------------------------------

/// GET projects/{project}/regions/{region}/routers/{router}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_routers_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_routers_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRoutersGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Router>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/routers/{}",
        args.project, args.region, args.router,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Router =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/regions/{region}/routers/{router}/getNatIpInfo
// -----------------------------------------------------------------------------

/// GET projects/{project}/regions/{region}/routers/{router}/getNatIpInfo.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_routers_get_nat_ip_info_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_routers_get_nat_ip_info_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRoutersGetNatIpInfoArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<NatIpInfoResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/routers/{}/getNatIpInfo",
        args.project, args.region, args.router,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.nat_name {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("natName=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: NatIpInfoResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/regions/{region}/routers/{router}/getNatMappingInfo
// -----------------------------------------------------------------------------

/// GET projects/{project}/regions/{region}/routers/{router}/getNatMappingInfo.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_routers_get_nat_mapping_info_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_routers_get_nat_mapping_info_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRoutersGetNatMappingInfoArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<VmEndpointNatMappingsList>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/routers/{}/getNatMappingInfo",
        args.project,
        args.region,
        args.router,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.nat_name {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("natName=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.order_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orderBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.return_partial_success {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("returnPartialSuccess=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: VmEndpointNatMappingsList =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/regions/{region}/routers/{router}/getRoutePolicy
// -----------------------------------------------------------------------------

/// GET projects/{project}/regions/{region}/routers/{router}/getRoutePolicy.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_routers_get_route_policy_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_routers_get_route_policy_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRoutersGetRoutePolicyArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<RoutersGetRoutePolicyResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/routers/{}/getRoutePolicy",
        args.project,
        args.region,
        args.router,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.policy {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("policy=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: RoutersGetRoutePolicyResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/regions/{region}/routers/{router}/getRouterStatus
// -----------------------------------------------------------------------------

/// GET projects/{project}/regions/{region}/routers/{router}/getRouterStatus.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_routers_get_router_status_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_routers_get_router_status_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRoutersGetRouterStatusArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<RouterStatusResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/routers/{}/getRouterStatus",
        args.project,
        args.region,
        args.router,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: RouterStatusResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/regions/{region}/routers
// -----------------------------------------------------------------------------

/// POST projects/{project}/regions/{region}/routers.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_routers_insert_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_routers_insert_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRoutersInsertArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/routers",
        args.project, args.region,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/regions/{region}/routers
// -----------------------------------------------------------------------------

/// GET projects/{project}/regions/{region}/routers.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_routers_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_routers_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRoutersListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<RouterList>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/routers",
        args.project, args.region,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.order_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orderBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.return_partial_success {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("returnPartialSuccess=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: RouterList =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/regions/{region}/routers/{router}/listBgpRoutes
// -----------------------------------------------------------------------------

/// GET projects/{project}/regions/{region}/routers/{router}/listBgpRoutes.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_routers_list_bgp_routes_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_routers_list_bgp_routes_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRoutersListBgpRoutesArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<RoutersListBgpRoutes>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/routers/{}/listBgpRoutes",
        args.project, args.region, args.router,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.address_family {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("addressFamily=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.destination_prefix {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("destinationPrefix=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.order_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orderBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.peer {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("peer=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.policy_applied {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("policyApplied=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.return_partial_success {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("returnPartialSuccess=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.route_type {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("routeType=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: RoutersListBgpRoutes =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/regions/{region}/routers/{router}/listRoutePolicies
// -----------------------------------------------------------------------------

/// GET projects/{project}/regions/{region}/routers/{router}/listRoutePolicies.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_routers_list_route_policies_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_routers_list_route_policies_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRoutersListRoutePoliciesArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<RoutersListRoutePolicies>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/routers/{}/listRoutePolicies",
        args.project,
        args.region,
        args.router,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.order_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orderBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.return_partial_success {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("returnPartialSuccess=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: RoutersListRoutePolicies =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PATCH projects/{project}/regions/{region}/routers/{router}
// -----------------------------------------------------------------------------

/// PATCH projects/{project}/regions/{region}/routers/{router}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_routers_patch_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_routers_patch_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRoutersPatchArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/routers/{}",
        args.project, args.region, args.router,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .patch(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/regions/{region}/routers/{router}/patchRoutePolicy
// -----------------------------------------------------------------------------

/// POST projects/{project}/regions/{region}/routers/{router}/patchRoutePolicy.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_routers_patch_route_policy_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_routers_patch_route_policy_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRoutersPatchRoutePolicyArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/routers/{}/patchRoutePolicy",
        args.project,
        args.region,
        args.router,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/regions/{region}/routers/{router}/preview
// -----------------------------------------------------------------------------

/// POST projects/{project}/regions/{region}/routers/{router}/preview.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_routers_preview_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_routers_preview_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRoutersPreviewArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<RoutersPreviewResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/routers/{}/preview",
        args.project, args.region, args.router,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: RoutersPreviewResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PUT projects/{project}/regions/{region}/routers/{router}
// -----------------------------------------------------------------------------

/// PUT projects/{project}/regions/{region}/routers/{router}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_routers_update_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_routers_update_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRoutersUpdateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/routers/{}",
        args.project, args.region, args.router,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .put(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/regions/{region}/routers/{router}/updateRoutePolicy
// -----------------------------------------------------------------------------

/// POST projects/{project}/regions/{region}/routers/{router}/updateRoutePolicy.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_routers_update_route_policy_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_routers_update_route_policy_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRoutersUpdateRoutePolicyArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/routers/{}/updateRoutePolicy",
        args.project,
        args.region,
        args.router,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE projects/{project}/global/routes/{route}
// -----------------------------------------------------------------------------

/// DELETE projects/{project}/global/routes/{route}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_routes_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_routes_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRoutesDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/routes/{}",
        args.project, args.route,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/global/routes/{route}
// -----------------------------------------------------------------------------

/// GET projects/{project}/global/routes/{route}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_routes_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_routes_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRoutesGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Route>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/routes/{}",
        args.project, args.route,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Route =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/global/routes
// -----------------------------------------------------------------------------

/// POST projects/{project}/global/routes.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_routes_insert_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_routes_insert_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRoutesInsertArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/routes",
        args.project,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/global/routes
// -----------------------------------------------------------------------------

/// GET projects/{project}/global/routes.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_routes_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_routes_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRoutesListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<RouteList>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/routes",
        args.project,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.order_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orderBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.return_partial_success {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("returnPartialSuccess=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: RouteList =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/global/routes/{resource}/testIamPermissions
// -----------------------------------------------------------------------------

/// POST projects/{project}/global/routes/{resource}/testIamPermissions.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_routes_test_iam_permissions_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_routes_test_iam_permissions_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRoutesTestIamPermissionsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TestPermissionsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/routes/{}/testIamPermissions",
        args.project, args.resource,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: TestPermissionsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/global/securityPolicies/{securityPolicy}/addRule
// -----------------------------------------------------------------------------

/// POST projects/{project}/global/securityPolicies/{securityPolicy}/addRule.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_security_policies_add_rule_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_security_policies_add_rule_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeSecurityPoliciesAddRuleArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/securityPolicies/{}/addRule",
        args.project, args.security_policy,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.validate_only {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("validateOnly=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/aggregated/securityPolicies
// -----------------------------------------------------------------------------

/// GET projects/{project}/aggregated/securityPolicies.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_security_policies_aggregated_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_security_policies_aggregated_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeSecurityPoliciesAggregatedListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SecurityPoliciesAggregatedList>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/aggregated/securityPolicies",
        args.project,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.include_all_scopes {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("includeAllScopes=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.order_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orderBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.return_partial_success {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("returnPartialSuccess=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.service_project_number {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("serviceProjectNumber=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: SecurityPoliciesAggregatedList =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE projects/{project}/global/securityPolicies/{securityPolicy}
// -----------------------------------------------------------------------------

/// DELETE projects/{project}/global/securityPolicies/{securityPolicy}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_security_policies_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_security_policies_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeSecurityPoliciesDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/securityPolicies/{}",
        args.project, args.security_policy,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/global/securityPolicies/{securityPolicy}
// -----------------------------------------------------------------------------

/// GET projects/{project}/global/securityPolicies/{securityPolicy}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_security_policies_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_security_policies_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeSecurityPoliciesGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SecurityPolicy>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/securityPolicies/{}",
        args.project, args.security_policy,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: SecurityPolicy =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/global/securityPolicies/{securityPolicy}/getRule
// -----------------------------------------------------------------------------

/// GET projects/{project}/global/securityPolicies/{securityPolicy}/getRule.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_security_policies_get_rule_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_security_policies_get_rule_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeSecurityPoliciesGetRuleArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SecurityPolicyRule>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/securityPolicies/{}/getRule",
        args.project, args.security_policy,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.priority {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("priority=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: SecurityPolicyRule =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/global/securityPolicies
// -----------------------------------------------------------------------------

/// POST projects/{project}/global/securityPolicies.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_security_policies_insert_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_security_policies_insert_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeSecurityPoliciesInsertArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/securityPolicies",
        args.project,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.validate_only {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("validateOnly=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/global/securityPolicies
// -----------------------------------------------------------------------------

/// GET projects/{project}/global/securityPolicies.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_security_policies_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_security_policies_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeSecurityPoliciesListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SecurityPolicyList>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/securityPolicies",
        args.project,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.order_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orderBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.return_partial_success {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("returnPartialSuccess=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: SecurityPolicyList =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/global/securityPolicies/listPreconfiguredExpressionSets
// -----------------------------------------------------------------------------

/// GET projects/{project}/global/securityPolicies/listPreconfiguredExpressionSets.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_security_policies_list_preconfigured_expression_sets_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_security_policies_list_preconfigured_expression_sets_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeSecurityPoliciesListPreconfiguredExpressionSetsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<SecurityPoliciesListPreconfiguredExpressionSetsResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/securityPolicies/listPreconfiguredExpressionSets",
        args.project,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.order_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orderBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.return_partial_success {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("returnPartialSuccess=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: SecurityPoliciesListPreconfiguredExpressionSetsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PATCH projects/{project}/global/securityPolicies/{securityPolicy}
// -----------------------------------------------------------------------------

/// PATCH projects/{project}/global/securityPolicies/{securityPolicy}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_security_policies_patch_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_security_policies_patch_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeSecurityPoliciesPatchArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/securityPolicies/{}",
        args.project, args.security_policy,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.update_mask {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("updateMask=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .patch(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/global/securityPolicies/{securityPolicy}/patchRule
// -----------------------------------------------------------------------------

/// POST projects/{project}/global/securityPolicies/{securityPolicy}/patchRule.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_security_policies_patch_rule_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_security_policies_patch_rule_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeSecurityPoliciesPatchRuleArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/securityPolicies/{}/patchRule",
        args.project,
        args.security_policy,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.priority {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("priority=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.update_mask {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("updateMask=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.validate_only {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("validateOnly=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/global/securityPolicies/{securityPolicy}/removeRule
// -----------------------------------------------------------------------------

/// POST projects/{project}/global/securityPolicies/{securityPolicy}/removeRule.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_security_policies_remove_rule_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_security_policies_remove_rule_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeSecurityPoliciesRemoveRuleArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/securityPolicies/{}/removeRule",
        args.project,
        args.security_policy,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.priority {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("priority=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/global/securityPolicies/{resource}/setLabels
// -----------------------------------------------------------------------------

/// POST projects/{project}/global/securityPolicies/{resource}/setLabels.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_security_policies_set_labels_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_security_policies_set_labels_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeSecurityPoliciesSetLabelsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/securityPolicies/{}/setLabels",
        args.project,
        args.resource,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/aggregated/serviceAttachments
// -----------------------------------------------------------------------------

/// GET projects/{project}/aggregated/serviceAttachments.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_service_attachments_aggregated_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_service_attachments_aggregated_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeServiceAttachmentsAggregatedListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ServiceAttachmentAggregatedList>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/aggregated/serviceAttachments",
        args.project,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.include_all_scopes {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("includeAllScopes=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.order_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orderBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.return_partial_success {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("returnPartialSuccess=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.service_project_number {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("serviceProjectNumber=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: ServiceAttachmentAggregatedList =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE projects/{project}/regions/{region}/serviceAttachments/{serviceAttachment}
// -----------------------------------------------------------------------------

/// DELETE projects/{project}/regions/{region}/serviceAttachments/{serviceAttachment}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_service_attachments_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_service_attachments_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeServiceAttachmentsDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/serviceAttachments/{}",
        args.project, args.region, args.service_attachment,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/regions/{region}/serviceAttachments/{serviceAttachment}
// -----------------------------------------------------------------------------

/// GET projects/{project}/regions/{region}/serviceAttachments/{serviceAttachment}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_service_attachments_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_service_attachments_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeServiceAttachmentsGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ServiceAttachment>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/serviceAttachments/{}",
        args.project, args.region, args.service_attachment,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.show_nat_ips {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("showNatIps=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: ServiceAttachment =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/regions/{region}/serviceAttachments/{resource}/getIamPolicy
// -----------------------------------------------------------------------------

/// GET projects/{project}/regions/{region}/serviceAttachments/{resource}/getIamPolicy.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_service_attachments_get_iam_policy_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_service_attachments_get_iam_policy_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeServiceAttachmentsGetIamPolicyArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/serviceAttachments/{}/getIamPolicy",
        args.project,
        args.region,
        args.resource,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.options_requested_policy_version {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("optionsRequestedPolicyVersion=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Policy =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/regions/{region}/serviceAttachments
// -----------------------------------------------------------------------------

/// POST projects/{project}/regions/{region}/serviceAttachments.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_service_attachments_insert_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_service_attachments_insert_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeServiceAttachmentsInsertArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/serviceAttachments",
        args.project, args.region,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/regions/{region}/serviceAttachments
// -----------------------------------------------------------------------------

/// GET projects/{project}/regions/{region}/serviceAttachments.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_service_attachments_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_service_attachments_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeServiceAttachmentsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ServiceAttachmentList>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/serviceAttachments",
        args.project, args.region,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.order_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orderBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.return_partial_success {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("returnPartialSuccess=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: ServiceAttachmentList =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PATCH projects/{project}/regions/{region}/serviceAttachments/{serviceAttachment}
// -----------------------------------------------------------------------------

/// PATCH projects/{project}/regions/{region}/serviceAttachments/{serviceAttachment}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_service_attachments_patch_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_service_attachments_patch_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeServiceAttachmentsPatchArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/serviceAttachments/{}",
        args.project, args.region, args.service_attachment,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .patch(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/regions/{region}/serviceAttachments/{resource}/setIamPolicy
// -----------------------------------------------------------------------------

/// POST projects/{project}/regions/{region}/serviceAttachments/{resource}/setIamPolicy.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_service_attachments_set_iam_policy_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_service_attachments_set_iam_policy_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeServiceAttachmentsSetIamPolicyArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/serviceAttachments/{}/setIamPolicy",
        args.project,
        args.region,
        args.resource,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Policy =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/regions/{region}/serviceAttachments/{resource}/testIamPermissions
// -----------------------------------------------------------------------------

/// POST projects/{project}/regions/{region}/serviceAttachments/{resource}/testIamPermissions.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_service_attachments_test_iam_permissions_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_service_attachments_test_iam_permissions_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeServiceAttachmentsTestIamPermissionsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TestPermissionsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/serviceAttachments/{}/testIamPermissions",
        args.project,
        args.region,
        args.resource,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: TestPermissionsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/global/snapshotSettings
// -----------------------------------------------------------------------------

/// GET projects/{project}/global/snapshotSettings.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_snapshot_settings_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_snapshot_settings_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeSnapshotSettingsGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SnapshotSettings>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/snapshotSettings",
        args.project,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: SnapshotSettings =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PATCH projects/{project}/global/snapshotSettings
// -----------------------------------------------------------------------------

/// PATCH projects/{project}/global/snapshotSettings.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_snapshot_settings_patch_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_snapshot_settings_patch_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeSnapshotSettingsPatchArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/snapshotSettings",
        args.project,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.update_mask {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("updateMask=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .patch(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE projects/{project}/global/snapshots/{snapshot}
// -----------------------------------------------------------------------------

/// DELETE projects/{project}/global/snapshots/{snapshot}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_snapshots_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_snapshots_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeSnapshotsDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/snapshots/{}",
        args.project, args.snapshot,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/global/snapshots/{snapshot}
// -----------------------------------------------------------------------------

/// GET projects/{project}/global/snapshots/{snapshot}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_snapshots_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_snapshots_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeSnapshotsGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Snapshot>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/snapshots/{}",
        args.project, args.snapshot,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Snapshot =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/global/snapshots/{resource}/getIamPolicy
// -----------------------------------------------------------------------------

/// GET projects/{project}/global/snapshots/{resource}/getIamPolicy.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_snapshots_get_iam_policy_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_snapshots_get_iam_policy_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeSnapshotsGetIamPolicyArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/snapshots/{}/getIamPolicy",
        args.project, args.resource,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.options_requested_policy_version {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("optionsRequestedPolicyVersion=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Policy =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/global/snapshots
// -----------------------------------------------------------------------------

/// POST projects/{project}/global/snapshots.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_snapshots_insert_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_snapshots_insert_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeSnapshotsInsertArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/snapshots",
        args.project,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/global/snapshots
// -----------------------------------------------------------------------------

/// GET projects/{project}/global/snapshots.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_snapshots_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_snapshots_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeSnapshotsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SnapshotList>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/snapshots",
        args.project,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.order_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orderBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.return_partial_success {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("returnPartialSuccess=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: SnapshotList =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/global/snapshots/{resource}/setIamPolicy
// -----------------------------------------------------------------------------

/// POST projects/{project}/global/snapshots/{resource}/setIamPolicy.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_snapshots_set_iam_policy_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_snapshots_set_iam_policy_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeSnapshotsSetIamPolicyArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/snapshots/{}/setIamPolicy",
        args.project, args.resource,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Policy =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/global/snapshots/{resource}/setLabels
// -----------------------------------------------------------------------------

/// POST projects/{project}/global/snapshots/{resource}/setLabels.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_snapshots_set_labels_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_snapshots_set_labels_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeSnapshotsSetLabelsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/snapshots/{}/setLabels",
        args.project, args.resource,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/global/snapshots/{resource}/testIamPermissions
// -----------------------------------------------------------------------------

/// POST projects/{project}/global/snapshots/{resource}/testIamPermissions.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_snapshots_test_iam_permissions_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_snapshots_test_iam_permissions_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeSnapshotsTestIamPermissionsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TestPermissionsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/snapshots/{}/testIamPermissions",
        args.project,
        args.resource,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: TestPermissionsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/global/snapshots/{snapshot}/updateKmsKey
// -----------------------------------------------------------------------------

/// POST projects/{project}/global/snapshots/{snapshot}/updateKmsKey.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_snapshots_update_kms_key_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_snapshots_update_kms_key_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeSnapshotsUpdateKmsKeyArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/snapshots/{}/updateKmsKey",
        args.project, args.snapshot,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/aggregated/sslCertificates
// -----------------------------------------------------------------------------

/// GET projects/{project}/aggregated/sslCertificates.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_ssl_certificates_aggregated_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_ssl_certificates_aggregated_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeSslCertificatesAggregatedListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SslCertificateAggregatedList>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/aggregated/sslCertificates",
        args.project,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.include_all_scopes {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("includeAllScopes=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.order_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orderBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.return_partial_success {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("returnPartialSuccess=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.service_project_number {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("serviceProjectNumber=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: SslCertificateAggregatedList =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE projects/{project}/global/sslCertificates/{sslCertificate}
// -----------------------------------------------------------------------------

/// DELETE projects/{project}/global/sslCertificates/{sslCertificate}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_ssl_certificates_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_ssl_certificates_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeSslCertificatesDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/sslCertificates/{}",
        args.project, args.ssl_certificate,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/global/sslCertificates/{sslCertificate}
// -----------------------------------------------------------------------------

/// GET projects/{project}/global/sslCertificates/{sslCertificate}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_ssl_certificates_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_ssl_certificates_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeSslCertificatesGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SslCertificate>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/sslCertificates/{}",
        args.project, args.ssl_certificate,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: SslCertificate =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/global/sslCertificates
// -----------------------------------------------------------------------------

/// POST projects/{project}/global/sslCertificates.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_ssl_certificates_insert_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_ssl_certificates_insert_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeSslCertificatesInsertArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/sslCertificates",
        args.project,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/global/sslCertificates
// -----------------------------------------------------------------------------

/// GET projects/{project}/global/sslCertificates.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_ssl_certificates_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_ssl_certificates_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeSslCertificatesListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SslCertificateList>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/sslCertificates",
        args.project,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.order_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orderBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.return_partial_success {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("returnPartialSuccess=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: SslCertificateList =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/aggregated/sslPolicies
// -----------------------------------------------------------------------------

/// GET projects/{project}/aggregated/sslPolicies.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_ssl_policies_aggregated_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_ssl_policies_aggregated_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeSslPoliciesAggregatedListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SslPoliciesAggregatedList>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/aggregated/sslPolicies",
        args.project,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.include_all_scopes {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("includeAllScopes=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.order_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orderBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.return_partial_success {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("returnPartialSuccess=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.service_project_number {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("serviceProjectNumber=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: SslPoliciesAggregatedList =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE projects/{project}/global/sslPolicies/{sslPolicy}
// -----------------------------------------------------------------------------

/// DELETE projects/{project}/global/sslPolicies/{sslPolicy}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_ssl_policies_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_ssl_policies_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeSslPoliciesDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/sslPolicies/{}",
        args.project, args.ssl_policy,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/global/sslPolicies/{sslPolicy}
// -----------------------------------------------------------------------------

/// GET projects/{project}/global/sslPolicies/{sslPolicy}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_ssl_policies_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_ssl_policies_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeSslPoliciesGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SslPolicy>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/sslPolicies/{}",
        args.project, args.ssl_policy,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: SslPolicy =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/global/sslPolicies
// -----------------------------------------------------------------------------

/// POST projects/{project}/global/sslPolicies.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_ssl_policies_insert_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_ssl_policies_insert_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeSslPoliciesInsertArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/sslPolicies",
        args.project,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/global/sslPolicies
// -----------------------------------------------------------------------------

/// GET projects/{project}/global/sslPolicies.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_ssl_policies_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_ssl_policies_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeSslPoliciesListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SslPoliciesList>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/sslPolicies",
        args.project,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.order_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orderBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.return_partial_success {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("returnPartialSuccess=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: SslPoliciesList =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/global/sslPolicies/listAvailableFeatures
// -----------------------------------------------------------------------------

/// GET projects/{project}/global/sslPolicies/listAvailableFeatures.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_ssl_policies_list_available_features_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_ssl_policies_list_available_features_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeSslPoliciesListAvailableFeaturesArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<SslPoliciesListAvailableFeaturesResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/sslPolicies/listAvailableFeatures",
        args.project,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.order_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orderBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.return_partial_success {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("returnPartialSuccess=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: SslPoliciesListAvailableFeaturesResponse = serde_json::from_str(&body)
                    .map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PATCH projects/{project}/global/sslPolicies/{sslPolicy}
// -----------------------------------------------------------------------------

/// PATCH projects/{project}/global/sslPolicies/{sslPolicy}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_ssl_policies_patch_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_ssl_policies_patch_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeSslPoliciesPatchArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/sslPolicies/{}",
        args.project, args.ssl_policy,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .patch(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/aggregated/storagePoolTypes
// -----------------------------------------------------------------------------

/// GET projects/{project}/aggregated/storagePoolTypes.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_storage_pool_types_aggregated_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_storage_pool_types_aggregated_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeStoragePoolTypesAggregatedListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<StoragePoolTypeAggregatedList>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/aggregated/storagePoolTypes",
        args.project,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.include_all_scopes {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("includeAllScopes=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.order_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orderBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.return_partial_success {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("returnPartialSuccess=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.service_project_number {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("serviceProjectNumber=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: StoragePoolTypeAggregatedList =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/zones/{zone}/storagePoolTypes/{storagePoolType}
// -----------------------------------------------------------------------------

/// GET projects/{project}/zones/{zone}/storagePoolTypes/{storagePoolType}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_storage_pool_types_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_storage_pool_types_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeStoragePoolTypesGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<StoragePoolType>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/storagePoolTypes/{}",
        args.project, args.zone, args.storage_pool_type,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: StoragePoolType =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/zones/{zone}/storagePoolTypes
// -----------------------------------------------------------------------------

/// GET projects/{project}/zones/{zone}/storagePoolTypes.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_storage_pool_types_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_storage_pool_types_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeStoragePoolTypesListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<StoragePoolTypeList>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/storagePoolTypes",
        args.project, args.zone,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.order_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orderBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.return_partial_success {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("returnPartialSuccess=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: StoragePoolTypeList =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/aggregated/storagePools
// -----------------------------------------------------------------------------

/// GET projects/{project}/aggregated/storagePools.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_storage_pools_aggregated_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_storage_pools_aggregated_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeStoragePoolsAggregatedListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<StoragePoolAggregatedList>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/aggregated/storagePools",
        args.project,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.include_all_scopes {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("includeAllScopes=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.order_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orderBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.return_partial_success {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("returnPartialSuccess=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.service_project_number {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("serviceProjectNumber=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: StoragePoolAggregatedList =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE projects/{project}/zones/{zone}/storagePools/{storagePool}
// -----------------------------------------------------------------------------

/// DELETE projects/{project}/zones/{zone}/storagePools/{storagePool}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_storage_pools_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_storage_pools_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeStoragePoolsDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/storagePools/{}",
        args.project, args.zone, args.storage_pool,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/zones/{zone}/storagePools/{storagePool}
// -----------------------------------------------------------------------------

/// GET projects/{project}/zones/{zone}/storagePools/{storagePool}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_storage_pools_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_storage_pools_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeStoragePoolsGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<StoragePool>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/storagePools/{}",
        args.project, args.zone, args.storage_pool,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: StoragePool =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/zones/{zone}/storagePools/{resource}/getIamPolicy
// -----------------------------------------------------------------------------

/// GET projects/{project}/zones/{zone}/storagePools/{resource}/getIamPolicy.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_storage_pools_get_iam_policy_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_storage_pools_get_iam_policy_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeStoragePoolsGetIamPolicyArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/storagePools/{}/getIamPolicy",
        args.project,
        args.zone,
        args.resource,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.options_requested_policy_version {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("optionsRequestedPolicyVersion=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Policy =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/zones/{zone}/storagePools
// -----------------------------------------------------------------------------

/// POST projects/{project}/zones/{zone}/storagePools.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_storage_pools_insert_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_storage_pools_insert_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeStoragePoolsInsertArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/storagePools",
        args.project, args.zone,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/zones/{zone}/storagePools
// -----------------------------------------------------------------------------

/// GET projects/{project}/zones/{zone}/storagePools.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_storage_pools_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_storage_pools_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeStoragePoolsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<StoragePoolList>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/storagePools",
        args.project, args.zone,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.order_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orderBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.return_partial_success {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("returnPartialSuccess=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: StoragePoolList =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/zones/{zone}/storagePools/{storagePool}/listDisks
// -----------------------------------------------------------------------------

/// GET projects/{project}/zones/{zone}/storagePools/{storagePool}/listDisks.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_storage_pools_list_disks_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_storage_pools_list_disks_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeStoragePoolsListDisksArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<StoragePoolListDisks>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/storagePools/{}/listDisks",
        args.project, args.zone, args.storage_pool,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.order_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orderBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.return_partial_success {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("returnPartialSuccess=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: StoragePoolListDisks =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/zones/{zone}/storagePools/{resource}/setIamPolicy
// -----------------------------------------------------------------------------

/// POST projects/{project}/zones/{zone}/storagePools/{resource}/setIamPolicy.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_storage_pools_set_iam_policy_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_storage_pools_set_iam_policy_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeStoragePoolsSetIamPolicyArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/storagePools/{}/setIamPolicy",
        args.project,
        args.zone,
        args.resource,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Policy =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/zones/{zone}/storagePools/{resource}/testIamPermissions
// -----------------------------------------------------------------------------

/// POST projects/{project}/zones/{zone}/storagePools/{resource}/testIamPermissions.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_storage_pools_test_iam_permissions_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_storage_pools_test_iam_permissions_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeStoragePoolsTestIamPermissionsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TestPermissionsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/storagePools/{}/testIamPermissions",
        args.project,
        args.zone,
        args.resource,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: TestPermissionsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PATCH projects/{project}/zones/{zone}/storagePools/{storagePool}
// -----------------------------------------------------------------------------

/// PATCH projects/{project}/zones/{zone}/storagePools/{storagePool}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_storage_pools_update_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_storage_pools_update_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeStoragePoolsUpdateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/storagePools/{}",
        args.project, args.zone, args.storage_pool,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.update_mask {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("updateMask=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .patch(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/aggregated/subnetworks
// -----------------------------------------------------------------------------

/// GET projects/{project}/aggregated/subnetworks.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_subnetworks_aggregated_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_subnetworks_aggregated_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeSubnetworksAggregatedListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SubnetworkAggregatedList>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/aggregated/subnetworks",
        args.project,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.include_all_scopes {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("includeAllScopes=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.order_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orderBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.return_partial_success {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("returnPartialSuccess=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.service_project_number {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("serviceProjectNumber=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.views {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("views=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: SubnetworkAggregatedList =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE projects/{project}/regions/{region}/subnetworks/{subnetwork}
// -----------------------------------------------------------------------------

/// DELETE projects/{project}/regions/{region}/subnetworks/{subnetwork}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_subnetworks_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_subnetworks_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeSubnetworksDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/subnetworks/{}",
        args.project, args.region, args.subnetwork,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/regions/{region}/subnetworks/{subnetwork}/expandIpCidrRange
// -----------------------------------------------------------------------------

/// POST projects/{project}/regions/{region}/subnetworks/{subnetwork}/expandIpCidrRange.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_subnetworks_expand_ip_cidr_range_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_subnetworks_expand_ip_cidr_range_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeSubnetworksExpandIpCidrRangeArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/subnetworks/{}/expandIpCidrRange",
        args.project,
        args.region,
        args.subnetwork,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/regions/{region}/subnetworks/{subnetwork}
// -----------------------------------------------------------------------------

/// GET projects/{project}/regions/{region}/subnetworks/{subnetwork}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_subnetworks_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_subnetworks_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeSubnetworksGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Subnetwork>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/subnetworks/{}",
        args.project, args.region, args.subnetwork,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.views {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("views=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Subnetwork =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/regions/{region}/subnetworks/{resource}/getIamPolicy
// -----------------------------------------------------------------------------

/// GET projects/{project}/regions/{region}/subnetworks/{resource}/getIamPolicy.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_subnetworks_get_iam_policy_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_subnetworks_get_iam_policy_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeSubnetworksGetIamPolicyArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/subnetworks/{}/getIamPolicy",
        args.project,
        args.region,
        args.resource,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.options_requested_policy_version {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("optionsRequestedPolicyVersion=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Policy =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/regions/{region}/subnetworks
// -----------------------------------------------------------------------------

/// POST projects/{project}/regions/{region}/subnetworks.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_subnetworks_insert_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_subnetworks_insert_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeSubnetworksInsertArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/subnetworks",
        args.project, args.region,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/regions/{region}/subnetworks
// -----------------------------------------------------------------------------

/// GET projects/{project}/regions/{region}/subnetworks.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_subnetworks_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_subnetworks_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeSubnetworksListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SubnetworkList>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/subnetworks",
        args.project, args.region,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.order_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orderBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.return_partial_success {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("returnPartialSuccess=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.views {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("views=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: SubnetworkList =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/aggregated/subnetworks/listUsable
// -----------------------------------------------------------------------------

/// GET projects/{project}/aggregated/subnetworks/listUsable.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_subnetworks_list_usable_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_subnetworks_list_usable_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeSubnetworksListUsableArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<UsableSubnetworksAggregatedList>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/aggregated/subnetworks/listUsable",
        args.project,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.order_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orderBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.return_partial_success {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("returnPartialSuccess=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.service_project {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("serviceProject=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: UsableSubnetworksAggregatedList =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PATCH projects/{project}/regions/{region}/subnetworks/{subnetwork}
// -----------------------------------------------------------------------------

/// PATCH projects/{project}/regions/{region}/subnetworks/{subnetwork}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_subnetworks_patch_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_subnetworks_patch_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeSubnetworksPatchArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/subnetworks/{}",
        args.project, args.region, args.subnetwork,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.drain_timeout_seconds {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("drainTimeoutSeconds=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .patch(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/regions/{region}/subnetworks/{resource}/setIamPolicy
// -----------------------------------------------------------------------------

/// POST projects/{project}/regions/{region}/subnetworks/{resource}/setIamPolicy.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_subnetworks_set_iam_policy_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_subnetworks_set_iam_policy_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeSubnetworksSetIamPolicyArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/subnetworks/{}/setIamPolicy",
        args.project,
        args.region,
        args.resource,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Policy =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/regions/{region}/subnetworks/{subnetwork}/setPrivateIpGoogleAccess
// -----------------------------------------------------------------------------

/// POST projects/{project}/regions/{region}/subnetworks/{subnetwork}/setPrivateIpGoogleAccess.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_subnetworks_set_private_ip_google_access_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_subnetworks_set_private_ip_google_access_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeSubnetworksSetPrivateIpGoogleAccessArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/subnetworks/{}/setPrivateIpGoogleAccess",
        args.project,
        args.region,
        args.subnetwork,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/regions/{region}/subnetworks/{resource}/testIamPermissions
// -----------------------------------------------------------------------------

/// POST projects/{project}/regions/{region}/subnetworks/{resource}/testIamPermissions.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_subnetworks_test_iam_permissions_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_subnetworks_test_iam_permissions_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeSubnetworksTestIamPermissionsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TestPermissionsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/subnetworks/{}/testIamPermissions",
        args.project,
        args.region,
        args.resource,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: TestPermissionsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE projects/{project}/global/targetGrpcProxies/{targetGrpcProxy}
// -----------------------------------------------------------------------------

/// DELETE projects/{project}/global/targetGrpcProxies/{targetGrpcProxy}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_target_grpc_proxies_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_target_grpc_proxies_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeTargetGrpcProxiesDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/targetGrpcProxies/{}",
        args.project, args.target_grpc_proxy,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/global/targetGrpcProxies/{targetGrpcProxy}
// -----------------------------------------------------------------------------

/// GET projects/{project}/global/targetGrpcProxies/{targetGrpcProxy}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_target_grpc_proxies_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_target_grpc_proxies_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeTargetGrpcProxiesGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TargetGrpcProxy>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/targetGrpcProxies/{}",
        args.project, args.target_grpc_proxy,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: TargetGrpcProxy =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/global/targetGrpcProxies
// -----------------------------------------------------------------------------

/// POST projects/{project}/global/targetGrpcProxies.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_target_grpc_proxies_insert_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_target_grpc_proxies_insert_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeTargetGrpcProxiesInsertArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/targetGrpcProxies",
        args.project,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/global/targetGrpcProxies
// -----------------------------------------------------------------------------

/// GET projects/{project}/global/targetGrpcProxies.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_target_grpc_proxies_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_target_grpc_proxies_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeTargetGrpcProxiesListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TargetGrpcProxyList>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/targetGrpcProxies",
        args.project,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.order_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orderBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.return_partial_success {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("returnPartialSuccess=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: TargetGrpcProxyList =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PATCH projects/{project}/global/targetGrpcProxies/{targetGrpcProxy}
// -----------------------------------------------------------------------------

/// PATCH projects/{project}/global/targetGrpcProxies/{targetGrpcProxy}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_target_grpc_proxies_patch_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_target_grpc_proxies_patch_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeTargetGrpcProxiesPatchArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/targetGrpcProxies/{}",
        args.project, args.target_grpc_proxy,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .patch(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/aggregated/targetHttpProxies
// -----------------------------------------------------------------------------

/// GET projects/{project}/aggregated/targetHttpProxies.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_target_http_proxies_aggregated_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_target_http_proxies_aggregated_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeTargetHttpProxiesAggregatedListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TargetHttpProxyAggregatedList>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/aggregated/targetHttpProxies",
        args.project,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.include_all_scopes {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("includeAllScopes=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.order_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orderBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.return_partial_success {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("returnPartialSuccess=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.service_project_number {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("serviceProjectNumber=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: TargetHttpProxyAggregatedList =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE projects/{project}/global/targetHttpProxies/{targetHttpProxy}
// -----------------------------------------------------------------------------

/// DELETE projects/{project}/global/targetHttpProxies/{targetHttpProxy}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_target_http_proxies_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_target_http_proxies_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeTargetHttpProxiesDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/targetHttpProxies/{}",
        args.project, args.target_http_proxy,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/global/targetHttpProxies/{targetHttpProxy}
// -----------------------------------------------------------------------------

/// GET projects/{project}/global/targetHttpProxies/{targetHttpProxy}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_target_http_proxies_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_target_http_proxies_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeTargetHttpProxiesGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TargetHttpProxy>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/targetHttpProxies/{}",
        args.project, args.target_http_proxy,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: TargetHttpProxy =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/global/targetHttpProxies
// -----------------------------------------------------------------------------

/// POST projects/{project}/global/targetHttpProxies.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_target_http_proxies_insert_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_target_http_proxies_insert_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeTargetHttpProxiesInsertArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/targetHttpProxies",
        args.project,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/global/targetHttpProxies
// -----------------------------------------------------------------------------

/// GET projects/{project}/global/targetHttpProxies.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_target_http_proxies_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_target_http_proxies_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeTargetHttpProxiesListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TargetHttpProxyList>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/targetHttpProxies",
        args.project,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.order_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orderBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.return_partial_success {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("returnPartialSuccess=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: TargetHttpProxyList =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PATCH projects/{project}/global/targetHttpProxies/{targetHttpProxy}
// -----------------------------------------------------------------------------

/// PATCH projects/{project}/global/targetHttpProxies/{targetHttpProxy}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_target_http_proxies_patch_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_target_http_proxies_patch_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeTargetHttpProxiesPatchArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/targetHttpProxies/{}",
        args.project, args.target_http_proxy,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .patch(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/targetHttpProxies/{targetHttpProxy}/setUrlMap
// -----------------------------------------------------------------------------

/// POST projects/{project}/targetHttpProxies/{targetHttpProxy}/setUrlMap.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_target_http_proxies_set_url_map_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_target_http_proxies_set_url_map_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeTargetHttpProxiesSetUrlMapArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/targetHttpProxies/{}/setUrlMap",
        args.project, args.target_http_proxy,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/aggregated/targetHttpsProxies
// -----------------------------------------------------------------------------

/// GET projects/{project}/aggregated/targetHttpsProxies.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_target_https_proxies_aggregated_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_target_https_proxies_aggregated_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeTargetHttpsProxiesAggregatedListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TargetHttpsProxyAggregatedList>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/aggregated/targetHttpsProxies",
        args.project,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.include_all_scopes {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("includeAllScopes=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.order_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orderBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.return_partial_success {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("returnPartialSuccess=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.service_project_number {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("serviceProjectNumber=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: TargetHttpsProxyAggregatedList =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE projects/{project}/global/targetHttpsProxies/{targetHttpsProxy}
// -----------------------------------------------------------------------------

/// DELETE projects/{project}/global/targetHttpsProxies/{targetHttpsProxy}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_target_https_proxies_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_target_https_proxies_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeTargetHttpsProxiesDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/targetHttpsProxies/{}",
        args.project, args.target_https_proxy,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/global/targetHttpsProxies/{targetHttpsProxy}
// -----------------------------------------------------------------------------

/// GET projects/{project}/global/targetHttpsProxies/{targetHttpsProxy}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_target_https_proxies_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_target_https_proxies_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeTargetHttpsProxiesGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TargetHttpsProxy>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/targetHttpsProxies/{}",
        args.project, args.target_https_proxy,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: TargetHttpsProxy =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/global/targetHttpsProxies
// -----------------------------------------------------------------------------

/// POST projects/{project}/global/targetHttpsProxies.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_target_https_proxies_insert_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_target_https_proxies_insert_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeTargetHttpsProxiesInsertArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/targetHttpsProxies",
        args.project,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/global/targetHttpsProxies
// -----------------------------------------------------------------------------

/// GET projects/{project}/global/targetHttpsProxies.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_target_https_proxies_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_target_https_proxies_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeTargetHttpsProxiesListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TargetHttpsProxyList>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/targetHttpsProxies",
        args.project,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.order_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orderBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.return_partial_success {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("returnPartialSuccess=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: TargetHttpsProxyList =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PATCH projects/{project}/global/targetHttpsProxies/{targetHttpsProxy}
// -----------------------------------------------------------------------------

/// PATCH projects/{project}/global/targetHttpsProxies/{targetHttpsProxy}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_target_https_proxies_patch_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_target_https_proxies_patch_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeTargetHttpsProxiesPatchArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/targetHttpsProxies/{}",
        args.project, args.target_https_proxy,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .patch(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/global/targetHttpsProxies/{targetHttpsProxy}/setCertificateMap
// -----------------------------------------------------------------------------

/// POST projects/{project}/global/targetHttpsProxies/{targetHttpsProxy}/setCertificateMap.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_target_https_proxies_set_certificate_map_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_target_https_proxies_set_certificate_map_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeTargetHttpsProxiesSetCertificateMapArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/targetHttpsProxies/{}/setCertificateMap",
        args.project,
        args.target_https_proxy,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/global/targetHttpsProxies/{targetHttpsProxy}/setQuicOverride
// -----------------------------------------------------------------------------

/// POST projects/{project}/global/targetHttpsProxies/{targetHttpsProxy}/setQuicOverride.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_target_https_proxies_set_quic_override_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_target_https_proxies_set_quic_override_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeTargetHttpsProxiesSetQuicOverrideArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/targetHttpsProxies/{}/setQuicOverride",
        args.project,
        args.target_https_proxy,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/targetHttpsProxies/{targetHttpsProxy}/setSslCertificates
// -----------------------------------------------------------------------------

/// POST projects/{project}/targetHttpsProxies/{targetHttpsProxy}/setSslCertificates.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_target_https_proxies_set_ssl_certificates_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_target_https_proxies_set_ssl_certificates_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeTargetHttpsProxiesSetSslCertificatesArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/targetHttpsProxies/{}/setSslCertificates",
        args.project,
        args.target_https_proxy,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/global/targetHttpsProxies/{targetHttpsProxy}/setSslPolicy
// -----------------------------------------------------------------------------

/// POST projects/{project}/global/targetHttpsProxies/{targetHttpsProxy}/setSslPolicy.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_target_https_proxies_set_ssl_policy_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_target_https_proxies_set_ssl_policy_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeTargetHttpsProxiesSetSslPolicyArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/targetHttpsProxies/{}/setSslPolicy",
        args.project,
        args.target_https_proxy,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/targetHttpsProxies/{targetHttpsProxy}/setUrlMap
// -----------------------------------------------------------------------------

/// POST projects/{project}/targetHttpsProxies/{targetHttpsProxy}/setUrlMap.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_target_https_proxies_set_url_map_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_target_https_proxies_set_url_map_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeTargetHttpsProxiesSetUrlMapArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/targetHttpsProxies/{}/setUrlMap",
        args.project, args.target_https_proxy,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/aggregated/targetInstances
// -----------------------------------------------------------------------------

/// GET projects/{project}/aggregated/targetInstances.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_target_instances_aggregated_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_target_instances_aggregated_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeTargetInstancesAggregatedListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TargetInstanceAggregatedList>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/aggregated/targetInstances",
        args.project,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.include_all_scopes {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("includeAllScopes=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.order_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orderBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.return_partial_success {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("returnPartialSuccess=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.service_project_number {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("serviceProjectNumber=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: TargetInstanceAggregatedList =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE projects/{project}/zones/{zone}/targetInstances/{targetInstance}
// -----------------------------------------------------------------------------

/// DELETE projects/{project}/zones/{zone}/targetInstances/{targetInstance}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_target_instances_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_target_instances_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeTargetInstancesDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/targetInstances/{}",
        args.project, args.zone, args.target_instance,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/zones/{zone}/targetInstances/{targetInstance}
// -----------------------------------------------------------------------------

/// GET projects/{project}/zones/{zone}/targetInstances/{targetInstance}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_target_instances_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_target_instances_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeTargetInstancesGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TargetInstance>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/targetInstances/{}",
        args.project, args.zone, args.target_instance,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: TargetInstance =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/zones/{zone}/targetInstances
// -----------------------------------------------------------------------------

/// POST projects/{project}/zones/{zone}/targetInstances.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_target_instances_insert_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_target_instances_insert_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeTargetInstancesInsertArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/targetInstances",
        args.project, args.zone,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/zones/{zone}/targetInstances
// -----------------------------------------------------------------------------

/// GET projects/{project}/zones/{zone}/targetInstances.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_target_instances_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_target_instances_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeTargetInstancesListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TargetInstanceList>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/targetInstances",
        args.project, args.zone,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.order_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orderBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.return_partial_success {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("returnPartialSuccess=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: TargetInstanceList =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/zones/{zone}/targetInstances/{targetInstance}/setSecurityPolicy
// -----------------------------------------------------------------------------

/// POST projects/{project}/zones/{zone}/targetInstances/{targetInstance}/setSecurityPolicy.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_target_instances_set_security_policy_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_target_instances_set_security_policy_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeTargetInstancesSetSecurityPolicyArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/targetInstances/{}/setSecurityPolicy",
        args.project,
        args.zone,
        args.target_instance,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/zones/{zone}/targetInstances/{resource}/testIamPermissions
// -----------------------------------------------------------------------------

/// POST projects/{project}/zones/{zone}/targetInstances/{resource}/testIamPermissions.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_target_instances_test_iam_permissions_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_target_instances_test_iam_permissions_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeTargetInstancesTestIamPermissionsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TestPermissionsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/targetInstances/{}/testIamPermissions",
        args.project,
        args.zone,
        args.resource,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: TestPermissionsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/regions/{region}/targetPools/{targetPool}/addHealthCheck
// -----------------------------------------------------------------------------

/// POST projects/{project}/regions/{region}/targetPools/{targetPool}/addHealthCheck.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_target_pools_add_health_check_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_target_pools_add_health_check_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeTargetPoolsAddHealthCheckArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/targetPools/{}/addHealthCheck",
        args.project,
        args.region,
        args.target_pool,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/regions/{region}/targetPools/{targetPool}/addInstance
// -----------------------------------------------------------------------------

/// POST projects/{project}/regions/{region}/targetPools/{targetPool}/addInstance.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_target_pools_add_instance_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_target_pools_add_instance_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeTargetPoolsAddInstanceArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/targetPools/{}/addInstance",
        args.project,
        args.region,
        args.target_pool,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/aggregated/targetPools
// -----------------------------------------------------------------------------

/// GET projects/{project}/aggregated/targetPools.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_target_pools_aggregated_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_target_pools_aggregated_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeTargetPoolsAggregatedListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TargetPoolAggregatedList>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/aggregated/targetPools",
        args.project,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.include_all_scopes {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("includeAllScopes=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.order_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orderBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.return_partial_success {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("returnPartialSuccess=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.service_project_number {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("serviceProjectNumber=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: TargetPoolAggregatedList =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE projects/{project}/regions/{region}/targetPools/{targetPool}
// -----------------------------------------------------------------------------

/// DELETE projects/{project}/regions/{region}/targetPools/{targetPool}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_target_pools_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_target_pools_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeTargetPoolsDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/targetPools/{}",
        args.project, args.region, args.target_pool,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/regions/{region}/targetPools/{targetPool}
// -----------------------------------------------------------------------------

/// GET projects/{project}/regions/{region}/targetPools/{targetPool}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_target_pools_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_target_pools_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeTargetPoolsGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TargetPool>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/targetPools/{}",
        args.project, args.region, args.target_pool,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: TargetPool =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/regions/{region}/targetPools/{targetPool}/getHealth
// -----------------------------------------------------------------------------

/// POST projects/{project}/regions/{region}/targetPools/{targetPool}/getHealth.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_target_pools_get_health_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_target_pools_get_health_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeTargetPoolsGetHealthArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TargetPoolInstanceHealth>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/targetPools/{}/getHealth",
        args.project, args.region, args.target_pool,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: TargetPoolInstanceHealth =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/regions/{region}/targetPools
// -----------------------------------------------------------------------------

/// POST projects/{project}/regions/{region}/targetPools.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_target_pools_insert_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_target_pools_insert_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeTargetPoolsInsertArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/targetPools",
        args.project, args.region,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/regions/{region}/targetPools
// -----------------------------------------------------------------------------

/// GET projects/{project}/regions/{region}/targetPools.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_target_pools_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_target_pools_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeTargetPoolsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TargetPoolList>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/targetPools",
        args.project, args.region,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.order_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orderBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.return_partial_success {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("returnPartialSuccess=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: TargetPoolList =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/regions/{region}/targetPools/{targetPool}/removeHealthCheck
// -----------------------------------------------------------------------------

/// POST projects/{project}/regions/{region}/targetPools/{targetPool}/removeHealthCheck.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_target_pools_remove_health_check_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_target_pools_remove_health_check_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeTargetPoolsRemoveHealthCheckArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/targetPools/{}/removeHealthCheck",
        args.project,
        args.region,
        args.target_pool,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/regions/{region}/targetPools/{targetPool}/removeInstance
// -----------------------------------------------------------------------------

/// POST projects/{project}/regions/{region}/targetPools/{targetPool}/removeInstance.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_target_pools_remove_instance_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_target_pools_remove_instance_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeTargetPoolsRemoveInstanceArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/targetPools/{}/removeInstance",
        args.project,
        args.region,
        args.target_pool,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/regions/{region}/targetPools/{targetPool}/setBackup
// -----------------------------------------------------------------------------

/// POST projects/{project}/regions/{region}/targetPools/{targetPool}/setBackup.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_target_pools_set_backup_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_target_pools_set_backup_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeTargetPoolsSetBackupArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/targetPools/{}/setBackup",
        args.project, args.region, args.target_pool,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.failover_ratio {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("failoverRatio=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/regions/{region}/targetPools/{targetPool}/setSecurityPolicy
// -----------------------------------------------------------------------------

/// POST projects/{project}/regions/{region}/targetPools/{targetPool}/setSecurityPolicy.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_target_pools_set_security_policy_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_target_pools_set_security_policy_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeTargetPoolsSetSecurityPolicyArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/targetPools/{}/setSecurityPolicy",
        args.project,
        args.region,
        args.target_pool,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/regions/{region}/targetPools/{resource}/testIamPermissions
// -----------------------------------------------------------------------------

/// POST projects/{project}/regions/{region}/targetPools/{resource}/testIamPermissions.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_target_pools_test_iam_permissions_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_target_pools_test_iam_permissions_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeTargetPoolsTestIamPermissionsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TestPermissionsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/targetPools/{}/testIamPermissions",
        args.project,
        args.region,
        args.resource,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: TestPermissionsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE projects/{project}/global/targetSslProxies/{targetSslProxy}
// -----------------------------------------------------------------------------

/// DELETE projects/{project}/global/targetSslProxies/{targetSslProxy}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_target_ssl_proxies_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_target_ssl_proxies_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeTargetSslProxiesDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/targetSslProxies/{}",
        args.project, args.target_ssl_proxy,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/global/targetSslProxies/{targetSslProxy}
// -----------------------------------------------------------------------------

/// GET projects/{project}/global/targetSslProxies/{targetSslProxy}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_target_ssl_proxies_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_target_ssl_proxies_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeTargetSslProxiesGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TargetSslProxy>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/targetSslProxies/{}",
        args.project, args.target_ssl_proxy,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: TargetSslProxy =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/global/targetSslProxies
// -----------------------------------------------------------------------------

/// POST projects/{project}/global/targetSslProxies.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_target_ssl_proxies_insert_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_target_ssl_proxies_insert_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeTargetSslProxiesInsertArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/targetSslProxies",
        args.project,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/global/targetSslProxies
// -----------------------------------------------------------------------------

/// GET projects/{project}/global/targetSslProxies.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_target_ssl_proxies_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_target_ssl_proxies_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeTargetSslProxiesListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TargetSslProxyList>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/targetSslProxies",
        args.project,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.order_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orderBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.return_partial_success {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("returnPartialSuccess=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: TargetSslProxyList =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/global/targetSslProxies/{targetSslProxy}/setBackendService
// -----------------------------------------------------------------------------

/// POST projects/{project}/global/targetSslProxies/{targetSslProxy}/setBackendService.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_target_ssl_proxies_set_backend_service_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_target_ssl_proxies_set_backend_service_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeTargetSslProxiesSetBackendServiceArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/targetSslProxies/{}/setBackendService",
        args.project,
        args.target_ssl_proxy,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/global/targetSslProxies/{targetSslProxy}/setCertificateMap
// -----------------------------------------------------------------------------

/// POST projects/{project}/global/targetSslProxies/{targetSslProxy}/setCertificateMap.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_target_ssl_proxies_set_certificate_map_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_target_ssl_proxies_set_certificate_map_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeTargetSslProxiesSetCertificateMapArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/targetSslProxies/{}/setCertificateMap",
        args.project,
        args.target_ssl_proxy,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/global/targetSslProxies/{targetSslProxy}/setProxyHeader
// -----------------------------------------------------------------------------

/// POST projects/{project}/global/targetSslProxies/{targetSslProxy}/setProxyHeader.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_target_ssl_proxies_set_proxy_header_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_target_ssl_proxies_set_proxy_header_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeTargetSslProxiesSetProxyHeaderArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/targetSslProxies/{}/setProxyHeader",
        args.project,
        args.target_ssl_proxy,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/global/targetSslProxies/{targetSslProxy}/setSslCertificates
// -----------------------------------------------------------------------------

/// POST projects/{project}/global/targetSslProxies/{targetSslProxy}/setSslCertificates.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_target_ssl_proxies_set_ssl_certificates_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_target_ssl_proxies_set_ssl_certificates_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeTargetSslProxiesSetSslCertificatesArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/targetSslProxies/{}/setSslCertificates",
        args.project,
        args.target_ssl_proxy,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/global/targetSslProxies/{targetSslProxy}/setSslPolicy
// -----------------------------------------------------------------------------

/// POST projects/{project}/global/targetSslProxies/{targetSslProxy}/setSslPolicy.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_target_ssl_proxies_set_ssl_policy_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_target_ssl_proxies_set_ssl_policy_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeTargetSslProxiesSetSslPolicyArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/targetSslProxies/{}/setSslPolicy",
        args.project,
        args.target_ssl_proxy,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/global/targetSslProxies/{resource}/testIamPermissions
// -----------------------------------------------------------------------------

/// POST projects/{project}/global/targetSslProxies/{resource}/testIamPermissions.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_target_ssl_proxies_test_iam_permissions_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_target_ssl_proxies_test_iam_permissions_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeTargetSslProxiesTestIamPermissionsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TestPermissionsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/targetSslProxies/{}/testIamPermissions",
        args.project,
        args.resource,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: TestPermissionsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/aggregated/targetTcpProxies
// -----------------------------------------------------------------------------

/// GET projects/{project}/aggregated/targetTcpProxies.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_target_tcp_proxies_aggregated_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_target_tcp_proxies_aggregated_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeTargetTcpProxiesAggregatedListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TargetTcpProxyAggregatedList>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/aggregated/targetTcpProxies",
        args.project,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.include_all_scopes {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("includeAllScopes=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.order_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orderBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.return_partial_success {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("returnPartialSuccess=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.service_project_number {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("serviceProjectNumber=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: TargetTcpProxyAggregatedList =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE projects/{project}/global/targetTcpProxies/{targetTcpProxy}
// -----------------------------------------------------------------------------

/// DELETE projects/{project}/global/targetTcpProxies/{targetTcpProxy}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_target_tcp_proxies_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_target_tcp_proxies_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeTargetTcpProxiesDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/targetTcpProxies/{}",
        args.project, args.target_tcp_proxy,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/global/targetTcpProxies/{targetTcpProxy}
// -----------------------------------------------------------------------------

/// GET projects/{project}/global/targetTcpProxies/{targetTcpProxy}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_target_tcp_proxies_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_target_tcp_proxies_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeTargetTcpProxiesGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TargetTcpProxy>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/targetTcpProxies/{}",
        args.project, args.target_tcp_proxy,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: TargetTcpProxy =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/global/targetTcpProxies
// -----------------------------------------------------------------------------

/// POST projects/{project}/global/targetTcpProxies.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_target_tcp_proxies_insert_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_target_tcp_proxies_insert_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeTargetTcpProxiesInsertArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/targetTcpProxies",
        args.project,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/global/targetTcpProxies
// -----------------------------------------------------------------------------

/// GET projects/{project}/global/targetTcpProxies.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_target_tcp_proxies_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_target_tcp_proxies_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeTargetTcpProxiesListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TargetTcpProxyList>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/targetTcpProxies",
        args.project,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.order_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orderBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.return_partial_success {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("returnPartialSuccess=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: TargetTcpProxyList =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/global/targetTcpProxies/{targetTcpProxy}/setBackendService
// -----------------------------------------------------------------------------

/// POST projects/{project}/global/targetTcpProxies/{targetTcpProxy}/setBackendService.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_target_tcp_proxies_set_backend_service_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_target_tcp_proxies_set_backend_service_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeTargetTcpProxiesSetBackendServiceArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/targetTcpProxies/{}/setBackendService",
        args.project,
        args.target_tcp_proxy,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/global/targetTcpProxies/{targetTcpProxy}/setProxyHeader
// -----------------------------------------------------------------------------

/// POST projects/{project}/global/targetTcpProxies/{targetTcpProxy}/setProxyHeader.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_target_tcp_proxies_set_proxy_header_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_target_tcp_proxies_set_proxy_header_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeTargetTcpProxiesSetProxyHeaderArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/targetTcpProxies/{}/setProxyHeader",
        args.project,
        args.target_tcp_proxy,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/global/targetTcpProxies/{resource}/testIamPermissions
// -----------------------------------------------------------------------------

/// POST projects/{project}/global/targetTcpProxies/{resource}/testIamPermissions.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_target_tcp_proxies_test_iam_permissions_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_target_tcp_proxies_test_iam_permissions_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeTargetTcpProxiesTestIamPermissionsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TestPermissionsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/targetTcpProxies/{}/testIamPermissions",
        args.project,
        args.resource,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: TestPermissionsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/aggregated/targetVpnGateways
// -----------------------------------------------------------------------------

/// GET projects/{project}/aggregated/targetVpnGateways.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_target_vpn_gateways_aggregated_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_target_vpn_gateways_aggregated_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeTargetVpnGatewaysAggregatedListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TargetVpnGatewayAggregatedList>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/aggregated/targetVpnGateways",
        args.project,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.include_all_scopes {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("includeAllScopes=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.order_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orderBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.return_partial_success {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("returnPartialSuccess=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.service_project_number {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("serviceProjectNumber=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: TargetVpnGatewayAggregatedList =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE projects/{project}/regions/{region}/targetVpnGateways/{targetVpnGateway}
// -----------------------------------------------------------------------------

/// DELETE projects/{project}/regions/{region}/targetVpnGateways/{targetVpnGateway}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_target_vpn_gateways_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_target_vpn_gateways_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeTargetVpnGatewaysDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/targetVpnGateways/{}",
        args.project, args.region, args.target_vpn_gateway,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/regions/{region}/targetVpnGateways/{targetVpnGateway}
// -----------------------------------------------------------------------------

/// GET projects/{project}/regions/{region}/targetVpnGateways/{targetVpnGateway}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_target_vpn_gateways_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_target_vpn_gateways_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeTargetVpnGatewaysGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TargetVpnGateway>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/targetVpnGateways/{}",
        args.project, args.region, args.target_vpn_gateway,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: TargetVpnGateway =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/regions/{region}/targetVpnGateways
// -----------------------------------------------------------------------------

/// POST projects/{project}/regions/{region}/targetVpnGateways.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_target_vpn_gateways_insert_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_target_vpn_gateways_insert_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeTargetVpnGatewaysInsertArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/targetVpnGateways",
        args.project, args.region,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/regions/{region}/targetVpnGateways
// -----------------------------------------------------------------------------

/// GET projects/{project}/regions/{region}/targetVpnGateways.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_target_vpn_gateways_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_target_vpn_gateways_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeTargetVpnGatewaysListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TargetVpnGatewayList>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/targetVpnGateways",
        args.project, args.region,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.order_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orderBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.return_partial_success {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("returnPartialSuccess=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: TargetVpnGatewayList =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/regions/{region}/targetVpnGateways/{resource}/setLabels
// -----------------------------------------------------------------------------

/// POST projects/{project}/regions/{region}/targetVpnGateways/{resource}/setLabels.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_target_vpn_gateways_set_labels_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_target_vpn_gateways_set_labels_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeTargetVpnGatewaysSetLabelsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/targetVpnGateways/{}/setLabels",
        args.project,
        args.region,
        args.resource,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/aggregated/urlMaps
// -----------------------------------------------------------------------------

/// GET projects/{project}/aggregated/urlMaps.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_url_maps_aggregated_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_url_maps_aggregated_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeUrlMapsAggregatedListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<UrlMapsAggregatedList>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/aggregated/urlMaps",
        args.project,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.include_all_scopes {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("includeAllScopes=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.order_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orderBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.return_partial_success {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("returnPartialSuccess=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.service_project_number {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("serviceProjectNumber=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: UrlMapsAggregatedList =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE projects/{project}/global/urlMaps/{urlMap}
// -----------------------------------------------------------------------------

/// DELETE projects/{project}/global/urlMaps/{urlMap}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_url_maps_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_url_maps_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeUrlMapsDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/urlMaps/{}",
        args.project, args.url_map,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/global/urlMaps/{urlMap}
// -----------------------------------------------------------------------------

/// GET projects/{project}/global/urlMaps/{urlMap}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_url_maps_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_url_maps_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeUrlMapsGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<UrlMap>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/urlMaps/{}",
        args.project, args.url_map,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: UrlMap =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/global/urlMaps
// -----------------------------------------------------------------------------

/// POST projects/{project}/global/urlMaps.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_url_maps_insert_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_url_maps_insert_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeUrlMapsInsertArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/urlMaps",
        args.project,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/global/urlMaps/{urlMap}/invalidateCache
// -----------------------------------------------------------------------------

/// POST projects/{project}/global/urlMaps/{urlMap}/invalidateCache.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_url_maps_invalidate_cache_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_url_maps_invalidate_cache_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeUrlMapsInvalidateCacheArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/urlMaps/{}/invalidateCache",
        args.project, args.url_map,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/global/urlMaps
// -----------------------------------------------------------------------------

/// GET projects/{project}/global/urlMaps.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_url_maps_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_url_maps_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeUrlMapsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<UrlMapList>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/urlMaps",
        args.project,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.order_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orderBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.return_partial_success {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("returnPartialSuccess=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: UrlMapList =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PATCH projects/{project}/global/urlMaps/{urlMap}
// -----------------------------------------------------------------------------

/// PATCH projects/{project}/global/urlMaps/{urlMap}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_url_maps_patch_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_url_maps_patch_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeUrlMapsPatchArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/urlMaps/{}",
        args.project, args.url_map,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .patch(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/global/urlMaps/{resource}/testIamPermissions
// -----------------------------------------------------------------------------

/// POST projects/{project}/global/urlMaps/{resource}/testIamPermissions.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_url_maps_test_iam_permissions_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_url_maps_test_iam_permissions_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeUrlMapsTestIamPermissionsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TestPermissionsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/urlMaps/{}/testIamPermissions",
        args.project,
        args.resource,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: TestPermissionsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PUT projects/{project}/global/urlMaps/{urlMap}
// -----------------------------------------------------------------------------

/// PUT projects/{project}/global/urlMaps/{urlMap}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_url_maps_update_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_url_maps_update_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeUrlMapsUpdateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/urlMaps/{}",
        args.project, args.url_map,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .put(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/global/urlMaps/{urlMap}/validate
// -----------------------------------------------------------------------------

/// POST projects/{project}/global/urlMaps/{urlMap}/validate.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_url_maps_validate_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_url_maps_validate_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeUrlMapsValidateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<UrlMapsValidateResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/urlMaps/{}/validate",
        args.project, args.url_map,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: UrlMapsValidateResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/aggregated/vpnGateways
// -----------------------------------------------------------------------------

/// GET projects/{project}/aggregated/vpnGateways.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_vpn_gateways_aggregated_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_vpn_gateways_aggregated_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeVpnGatewaysAggregatedListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<VpnGatewayAggregatedList>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/aggregated/vpnGateways",
        args.project,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.include_all_scopes {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("includeAllScopes=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.order_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orderBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.return_partial_success {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("returnPartialSuccess=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.service_project_number {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("serviceProjectNumber=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: VpnGatewayAggregatedList =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE projects/{project}/regions/{region}/vpnGateways/{vpnGateway}
// -----------------------------------------------------------------------------

/// DELETE projects/{project}/regions/{region}/vpnGateways/{vpnGateway}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_vpn_gateways_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_vpn_gateways_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeVpnGatewaysDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/vpnGateways/{}",
        args.project, args.region, args.vpn_gateway,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/regions/{region}/vpnGateways/{vpnGateway}
// -----------------------------------------------------------------------------

/// GET projects/{project}/regions/{region}/vpnGateways/{vpnGateway}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_vpn_gateways_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_vpn_gateways_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeVpnGatewaysGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<VpnGateway>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/vpnGateways/{}",
        args.project, args.region, args.vpn_gateway,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: VpnGateway =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/regions/{region}/vpnGateways/{vpnGateway}/getStatus
// -----------------------------------------------------------------------------

/// GET projects/{project}/regions/{region}/vpnGateways/{vpnGateway}/getStatus.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_vpn_gateways_get_status_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_vpn_gateways_get_status_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeVpnGatewaysGetStatusArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<VpnGatewaysGetStatusResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/vpnGateways/{}/getStatus",
        args.project, args.region, args.vpn_gateway,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: VpnGatewaysGetStatusResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/regions/{region}/vpnGateways
// -----------------------------------------------------------------------------

/// POST projects/{project}/regions/{region}/vpnGateways.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_vpn_gateways_insert_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_vpn_gateways_insert_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeVpnGatewaysInsertArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/vpnGateways",
        args.project, args.region,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/regions/{region}/vpnGateways
// -----------------------------------------------------------------------------

/// GET projects/{project}/regions/{region}/vpnGateways.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_vpn_gateways_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_vpn_gateways_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeVpnGatewaysListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<VpnGatewayList>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/vpnGateways",
        args.project, args.region,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.order_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orderBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.return_partial_success {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("returnPartialSuccess=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: VpnGatewayList =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/regions/{region}/vpnGateways/{resource}/setLabels
// -----------------------------------------------------------------------------

/// POST projects/{project}/regions/{region}/vpnGateways/{resource}/setLabels.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_vpn_gateways_set_labels_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_vpn_gateways_set_labels_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeVpnGatewaysSetLabelsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/vpnGateways/{}/setLabels",
        args.project, args.region, args.resource,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/regions/{region}/vpnGateways/{resource}/testIamPermissions
// -----------------------------------------------------------------------------

/// POST projects/{project}/regions/{region}/vpnGateways/{resource}/testIamPermissions.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_vpn_gateways_test_iam_permissions_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_vpn_gateways_test_iam_permissions_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeVpnGatewaysTestIamPermissionsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TestPermissionsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/vpnGateways/{}/testIamPermissions",
        args.project,
        args.region,
        args.resource,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: TestPermissionsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/aggregated/vpnTunnels
// -----------------------------------------------------------------------------

/// GET projects/{project}/aggregated/vpnTunnels.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_vpn_tunnels_aggregated_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_vpn_tunnels_aggregated_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeVpnTunnelsAggregatedListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<VpnTunnelAggregatedList>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/aggregated/vpnTunnels",
        args.project,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.include_all_scopes {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("includeAllScopes=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.order_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orderBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.return_partial_success {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("returnPartialSuccess=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.service_project_number {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("serviceProjectNumber=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: VpnTunnelAggregatedList =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE projects/{project}/regions/{region}/vpnTunnels/{vpnTunnel}
// -----------------------------------------------------------------------------

/// DELETE projects/{project}/regions/{region}/vpnTunnels/{vpnTunnel}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_vpn_tunnels_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_vpn_tunnels_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeVpnTunnelsDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/vpnTunnels/{}",
        args.project, args.region, args.vpn_tunnel,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/regions/{region}/vpnTunnels/{vpnTunnel}
// -----------------------------------------------------------------------------

/// GET projects/{project}/regions/{region}/vpnTunnels/{vpnTunnel}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_vpn_tunnels_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_vpn_tunnels_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeVpnTunnelsGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<VpnTunnel>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/vpnTunnels/{}",
        args.project, args.region, args.vpn_tunnel,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: VpnTunnel =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/regions/{region}/vpnTunnels
// -----------------------------------------------------------------------------

/// POST projects/{project}/regions/{region}/vpnTunnels.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_vpn_tunnels_insert_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_vpn_tunnels_insert_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeVpnTunnelsInsertArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/vpnTunnels",
        args.project, args.region,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/regions/{region}/vpnTunnels
// -----------------------------------------------------------------------------

/// GET projects/{project}/regions/{region}/vpnTunnels.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_vpn_tunnels_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_vpn_tunnels_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeVpnTunnelsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<VpnTunnelList>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/vpnTunnels",
        args.project, args.region,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.order_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orderBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.return_partial_success {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("returnPartialSuccess=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: VpnTunnelList =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/regions/{region}/vpnTunnels/{resource}/setLabels
// -----------------------------------------------------------------------------

/// POST projects/{project}/regions/{region}/vpnTunnels/{resource}/setLabels.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_vpn_tunnels_set_labels_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_vpn_tunnels_set_labels_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeVpnTunnelsSetLabelsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/vpnTunnels/{}/setLabels",
        args.project, args.region, args.resource,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE projects/{project}/global/crossSiteNetworks/{crossSiteNetwork}/wireGroups/{wireGroup}
// -----------------------------------------------------------------------------

/// DELETE projects/{project}/global/crossSiteNetworks/{crossSiteNetwork}/wireGroups/{wireGroup}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_wire_groups_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_wire_groups_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeWireGroupsDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/crossSiteNetworks/{}/wireGroups/{}",
        args.project,
        args.cross_site_network,
        args.wire_group,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/global/crossSiteNetworks/{crossSiteNetwork}/wireGroups/{wireGroup}
// -----------------------------------------------------------------------------

/// GET projects/{project}/global/crossSiteNetworks/{crossSiteNetwork}/wireGroups/{wireGroup}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_wire_groups_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_wire_groups_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeWireGroupsGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<WireGroup>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/crossSiteNetworks/{}/wireGroups/{}",
        args.project,
        args.cross_site_network,
        args.wire_group,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: WireGroup =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/global/crossSiteNetworks/{crossSiteNetwork}/wireGroups
// -----------------------------------------------------------------------------

/// POST projects/{project}/global/crossSiteNetworks/{crossSiteNetwork}/wireGroups.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_wire_groups_insert_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_wire_groups_insert_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeWireGroupsInsertArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/crossSiteNetworks/{}/wireGroups",
        args.project,
        args.cross_site_network,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.validate_only {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("validateOnly=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/global/crossSiteNetworks/{crossSiteNetwork}/wireGroups
// -----------------------------------------------------------------------------

/// GET projects/{project}/global/crossSiteNetworks/{crossSiteNetwork}/wireGroups.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_wire_groups_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_wire_groups_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeWireGroupsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<WireGroupList>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/crossSiteNetworks/{}/wireGroups",
        args.project,
        args.cross_site_network,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.order_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orderBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.return_partial_success {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("returnPartialSuccess=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: WireGroupList =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PATCH projects/{project}/global/crossSiteNetworks/{crossSiteNetwork}/wireGroups/{wireGroup}
// -----------------------------------------------------------------------------

/// PATCH projects/{project}/global/crossSiteNetworks/{crossSiteNetwork}/wireGroups/{wireGroup}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_wire_groups_patch_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_wire_groups_patch_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeWireGroupsPatchArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/global/crossSiteNetworks/{}/wireGroups/{}",
        args.project,
        args.cross_site_network,
        args.wire_group,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.update_mask {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("updateMask=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.validate_only {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("validateOnly=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .patch(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE projects/{project}/zones/{zone}/operations/{operation}
// -----------------------------------------------------------------------------

/// DELETE projects/{project}/zones/{zone}/operations/{operation}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_zone_operations_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_zone_operations_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeZoneOperationsDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/operations/{}",
        args.project, args.zone, args.operation,
    );

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream: _,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/zones/{zone}/operations/{operation}
// -----------------------------------------------------------------------------

/// GET projects/{project}/zones/{zone}/operations/{operation}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_zone_operations_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_zone_operations_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeZoneOperationsGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/operations/{}",
        args.project, args.zone, args.operation,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/zones/{zone}/operations
// -----------------------------------------------------------------------------

/// GET projects/{project}/zones/{zone}/operations.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_zone_operations_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_zone_operations_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeZoneOperationsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<OperationList>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/operations",
        args.project, args.zone,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.order_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orderBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.return_partial_success {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("returnPartialSuccess=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: OperationList =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/zones/{zone}/operations/{operation}/wait
// -----------------------------------------------------------------------------

/// POST projects/{project}/zones/{zone}/operations/{operation}/wait.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_zone_operations_wait_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_zone_operations_wait_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeZoneOperationsWaitArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/operations/{}/wait",
        args.project, args.zone, args.operation,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE projects/{project}/zones/{zone}/vmExtensionPolicies/{vmExtensionPolicy}
// -----------------------------------------------------------------------------

/// DELETE projects/{project}/zones/{zone}/vmExtensionPolicies/{vmExtensionPolicy}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_zone_vm_extension_policies_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_zone_vm_extension_policies_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeZoneVmExtensionPoliciesDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/vmExtensionPolicies/{}",
        args.project, args.zone, args.vm_extension_policy,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/zones/{zone}/vmExtensionPolicies/{vmExtensionPolicy}
// -----------------------------------------------------------------------------

/// GET projects/{project}/zones/{zone}/vmExtensionPolicies/{vmExtensionPolicy}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_zone_vm_extension_policies_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_zone_vm_extension_policies_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeZoneVmExtensionPoliciesGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<VmExtensionPolicy>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/vmExtensionPolicies/{}",
        args.project, args.zone, args.vm_extension_policy,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: VmExtensionPolicy =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/zones/{zone}/vmExtensionPolicies
// -----------------------------------------------------------------------------

/// POST projects/{project}/zones/{zone}/vmExtensionPolicies.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_zone_vm_extension_policies_insert_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_zone_vm_extension_policies_insert_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeZoneVmExtensionPoliciesInsertArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/vmExtensionPolicies",
        args.project, args.zone,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/zones/{zone}/vmExtensionPolicies
// -----------------------------------------------------------------------------

/// GET projects/{project}/zones/{zone}/vmExtensionPolicies.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_zone_vm_extension_policies_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_zone_vm_extension_policies_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeZoneVmExtensionPoliciesListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<VmExtensionPolicyList>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/vmExtensionPolicies",
        args.project, args.zone,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.order_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orderBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.return_partial_success {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("returnPartialSuccess=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: VmExtensionPolicyList =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PATCH projects/{project}/zones/{zone}/vmExtensionPolicies/{vmExtensionPolicy}
// -----------------------------------------------------------------------------

/// PATCH projects/{project}/zones/{zone}/vmExtensionPolicies/{vmExtensionPolicy}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_zone_vm_extension_policies_update_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_zone_vm_extension_policies_update_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeZoneVmExtensionPoliciesUpdateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/vmExtensionPolicies/{}",
        args.project, args.zone, args.vm_extension_policy,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .patch(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/zones/{zone}
// -----------------------------------------------------------------------------

/// GET projects/{project}/zones/{zone}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_zones_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_zones_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeZonesGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Zone>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}",
        args.project, args.zone,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: Zone =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/zones
// -----------------------------------------------------------------------------

/// GET projects/{project}/zones.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_zones_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_zones_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeZonesListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ZoneList>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones",
        args.project,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.order_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orderBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.return_partial_success {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("returnPartialSuccess=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_netio::simple_http::shared::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::body_reader::collect_string(stream);
                let parsed: ZoneList =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}
