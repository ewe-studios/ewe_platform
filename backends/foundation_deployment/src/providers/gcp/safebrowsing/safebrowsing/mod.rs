//! Auto-generated API module for gcp/safebrowsing safebrowsing.
//!
//! Generated by `cargo run --bin ewe_platform gen_api`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp_safebrowsing_safebrowsing `

#![cfg(feature = "gcp_safebrowsing_safebrowsing")]
#![allow(clippy::too_many_arguments, clippy::type_complexity)]
#![allow(
    clippy::missing_errors_doc,
    clippy::doc_markdown,
    clippy::useless_format
)]
#![allow(unused_imports)]

use foundation_core::valtron::{TaskIterator, TaskIteratorExt};
use foundation_core::wire::simple_http::client::{ClientRequestBuilder, SimpleHttpClient};
use foundation_macros::JsonHash;
use serde::{Deserialize, Serialize};

use super::shared::ApiResponse;

// =============================================================================
// TYPE DECLARATIONS
// =============================================================================

/// `GoogleSecuritySafebrowsingV5ListHashListsResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleSecuritySafebrowsingV5ListHashListsResponse {
    /// hashLists property.
    pub hash_lists: Option<Vec<GoogleSecuritySafebrowsingV5HashList>>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
}

/// `GoogleSecuritySafebrowsingV5BatchGetHashListsResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleSecuritySafebrowsingV5BatchGetHashListsResponse {
    /// hashLists property.
    pub hash_lists: Option<Vec<GoogleSecuritySafebrowsingV5HashList>>,
}

/// `GoogleSecuritySafebrowsingV5RiceDeltaEncoded32Bit` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleSecuritySafebrowsingV5RiceDeltaEncoded32Bit {
    /// encodedData property.
    pub encoded_data: Option<String>,
    /// entriesCount property.
    pub entries_count: Option<i64>,
    /// firstValue property.
    pub first_value: Option<i64>,
    /// riceParameter property.
    pub rice_parameter: Option<i64>,
}

/// `GoogleSecuritySafebrowsingV5RiceDeltaEncoded128Bit` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleSecuritySafebrowsingV5RiceDeltaEncoded128Bit {
    /// encodedData property.
    pub encoded_data: Option<String>,
    /// entriesCount property.
    pub entries_count: Option<i64>,
    /// firstValueHi property.
    pub first_value_hi: Option<String>,
    /// firstValueLo property.
    pub first_value_lo: Option<String>,
    /// riceParameter property.
    pub rice_parameter: Option<i64>,
}

/// `GoogleSecuritySafebrowsingV5SearchUrlsResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleSecuritySafebrowsingV5SearchUrlsResponse {
    /// cacheDuration property.
    pub cache_duration: Option<String>,
    /// threats property.
    pub threats: Option<Vec<GoogleSecuritySafebrowsingV5ThreatUrl>>,
}

/// `GoogleSecuritySafebrowsingV5HashList` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleSecuritySafebrowsingV5HashList {
    /// additionsEightBytes property.
    pub additions_eight_bytes: Option<GoogleSecuritySafebrowsingV5RiceDeltaEncoded64Bit>,
    /// additionsFourBytes property.
    pub additions_four_bytes: Option<GoogleSecuritySafebrowsingV5RiceDeltaEncoded32Bit>,
    /// additionsSixteenBytes property.
    pub additions_sixteen_bytes: Option<GoogleSecuritySafebrowsingV5RiceDeltaEncoded128Bit>,
    /// additionsThirtyTwoBytes property.
    pub additions_thirty_two_bytes: Option<GoogleSecuritySafebrowsingV5RiceDeltaEncoded256Bit>,
    /// compressedRemovals property.
    pub compressed_removals: Option<GoogleSecuritySafebrowsingV5RiceDeltaEncoded32Bit>,
    /// metadata property.
    pub metadata: Option<GoogleSecuritySafebrowsingV5HashListMetadata>,
    /// minimumWaitDuration property.
    pub minimum_wait_duration: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// partialUpdate property.
    pub partial_update: Option<bool>,
    /// sha256Checksum property.
    pub sha256_checksum: Option<String>,
    /// version property.
    pub version: Option<String>,
}

/// `GoogleSecuritySafebrowsingV5RiceDeltaEncoded256Bit` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleSecuritySafebrowsingV5RiceDeltaEncoded256Bit {
    /// encodedData property.
    pub encoded_data: Option<String>,
    /// entriesCount property.
    pub entries_count: Option<i64>,
    /// firstValueFirstPart property.
    pub first_value_first_part: Option<String>,
    /// firstValueFourthPart property.
    pub first_value_fourth_part: Option<String>,
    /// firstValueSecondPart property.
    pub first_value_second_part: Option<String>,
    /// firstValueThirdPart property.
    pub first_value_third_part: Option<String>,
    /// riceParameter property.
    pub rice_parameter: Option<i64>,
}

/// `GoogleSecuritySafebrowsingV5FullHash` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleSecuritySafebrowsingV5FullHash {
    /// fullHash property.
    pub full_hash: Option<String>,
    /// fullHashDetails property.
    pub full_hash_details: Option<Vec<GoogleSecuritySafebrowsingV5FullHashFullHashDetail>>,
}

/// `GoogleSecuritySafebrowsingV5FullHashFullHashDetail` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleSecuritySafebrowsingV5FullHashFullHashDetail {
    /// attributes property.
    pub attributes: Option<Vec<String>>,
    /// threatType property.
    pub threat_type: Option<String>,
}

/// `GoogleSecuritySafebrowsingV5SearchHashesResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleSecuritySafebrowsingV5SearchHashesResponse {
    /// cacheDuration property.
    pub cache_duration: Option<String>,
    /// fullHashes property.
    pub full_hashes: Option<Vec<GoogleSecuritySafebrowsingV5FullHash>>,
}

/// `GoogleSecuritySafebrowsingV5HashListMetadata` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleSecuritySafebrowsingV5HashListMetadata {
    /// description property.
    pub description: Option<String>,
    /// hashLength property.
    pub hash_length: Option<String>,
    /// likelySafeTypes property.
    pub likely_safe_types: Option<Vec<String>>,
    /// threatTypes property.
    pub threat_types: Option<Vec<String>>,
}

/// `GoogleSecuritySafebrowsingV5ThreatUrl` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleSecuritySafebrowsingV5ThreatUrl {
    /// threatTypes property.
    pub threat_types: Option<Vec<String>>,
    /// url property.
    pub url: Option<String>,
}

/// `GoogleSecuritySafebrowsingV5RiceDeltaEncoded64Bit` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleSecuritySafebrowsingV5RiceDeltaEncoded64Bit {
    /// encodedData property.
    pub encoded_data: Option<String>,
    /// entriesCount property.
    pub entries_count: Option<i64>,
    /// firstValue property.
    pub first_value: Option<String>,
    /// riceParameter property.
    pub rice_parameter: Option<i64>,
}

// =============================================================================
// ARGS TYPES (per-endpoint)
// =============================================================================

/// Arguments for [`safebrowsing.hashList.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct SafebrowsingHashListGetArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `sizeConstraints.maxDatabaseEntries`.
    pub size_constraints_max_database_entries: Option<String>,
    /// Query parameter: `sizeConstraints.maxUpdateEntries`.
    pub size_constraints_max_update_entries: Option<String>,
    /// Query parameter: `version`.
    pub version: Option<String>,
}

/// Arguments for [`safebrowsing.hashLists.batchGet_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct SafebrowsingHashListsBatchGetArgs {
    /// Query parameter: `names`.
    pub names: Option<String>,
    /// Query parameter: `sizeConstraints.maxDatabaseEntries`.
    pub size_constraints_max_database_entries: Option<String>,
    /// Query parameter: `sizeConstraints.maxUpdateEntries`.
    pub size_constraints_max_update_entries: Option<String>,
    /// Query parameter: `version`.
    pub version: Option<String>,
}

/// Arguments for [`safebrowsing.hashLists.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct SafebrowsingHashListsListArgs {
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

/// Arguments for [`safebrowsing.hashes.search_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct SafebrowsingHashesSearchArgs {
    /// Query parameter: `hashPrefixes`.
    pub hash_prefixes: Option<String>,
}

/// Arguments for [`safebrowsing.urls.search_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct SafebrowsingUrlsSearchArgs {
    /// Query parameter: `urls`.
    pub urls: Option<String>,
}

// =============================================================================
// CLIENT FUNCTIONS (per-endpoint)
// =============================================================================

// -----------------------------------------------------------------------------
// GET v5/hashList/{name}
// -----------------------------------------------------------------------------

/// GET v5/hashList/{name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = safebrowsing_hash_list_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn safebrowsing_hash_list_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &SafebrowsingHashListGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleSecuritySafebrowsingV5HashList>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://safebrowsing.googleapis.com/v5/hashList/{}",
        args.name,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.size_constraints_max_database_entries {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("sizeConstraints_maxDatabaseEntries=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.size_constraints_max_update_entries {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("sizeConstraints_maxUpdateEntries=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.version {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("version=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleSecuritySafebrowsingV5HashList = serde_json::from_str(&body)
                    .map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v5/hashLists:batchGet
// -----------------------------------------------------------------------------

/// GET v5/hashLists:batchGet.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = safebrowsing_hash_lists_batch_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn safebrowsing_hash_lists_batch_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &SafebrowsingHashListsBatchGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleSecuritySafebrowsingV5BatchGetHashListsResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://safebrowsing.googleapis.com/v5/hashLists:batchGet",);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.names {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("names=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.size_constraints_max_database_entries {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("sizeConstraints_maxDatabaseEntries=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.size_constraints_max_update_entries {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("sizeConstraints_maxUpdateEntries=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.version {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("version=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleSecuritySafebrowsingV5BatchGetHashListsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v5/hashLists
// -----------------------------------------------------------------------------

/// GET v5/hashLists.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = safebrowsing_hash_lists_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn safebrowsing_hash_lists_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &SafebrowsingHashListsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleSecuritySafebrowsingV5ListHashListsResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://safebrowsing.googleapis.com/v5/hashLists",);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleSecuritySafebrowsingV5ListHashListsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v5/hashes:search
// -----------------------------------------------------------------------------

/// GET v5/hashes:search.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = safebrowsing_hashes_search_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn safebrowsing_hashes_search_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &SafebrowsingHashesSearchArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleSecuritySafebrowsingV5SearchHashesResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://safebrowsing.googleapis.com/v5/hashes:search",);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.hash_prefixes {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("hashPrefixes=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleSecuritySafebrowsingV5SearchHashesResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v5/urls:search
// -----------------------------------------------------------------------------

/// GET v5/urls:search.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = safebrowsing_urls_search_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn safebrowsing_urls_search_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &SafebrowsingUrlsSearchArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleSecuritySafebrowsingV5SearchUrlsResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://safebrowsing.googleapis.com/v5/urls:search",);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.urls {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("urls=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleSecuritySafebrowsingV5SearchUrlsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}
