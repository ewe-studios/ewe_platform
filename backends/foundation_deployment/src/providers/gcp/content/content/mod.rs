//! Auto-generated API module for gcp/content content.
//!
//! Generated by `cargo run --bin ewe_platform gen_api`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp_content_content `

#![cfg(feature = "gcp_content_content")]
#![allow(clippy::too_many_arguments, clippy::type_complexity)]
#![allow(
    clippy::missing_errors_doc,
    clippy::doc_markdown,
    clippy::useless_format
)]
#![allow(unused_imports)]

use foundation_core::valtron::{TaskIterator, TaskIteratorExt};
use foundation_core::wire::simple_http::client::{ClientRequestBuilder, SimpleHttpClient};
use foundation_macros::JsonHash;
use serde::{Deserialize, Serialize};

use super::shared::ApiResponse;

// =============================================================================
// TYPE DECLARATIONS
// =============================================================================

/// `RegionalInventory` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct RegionalInventory {
    /// availability property.
    pub availability: Option<String>,
    /// customAttributes property.
    pub custom_attributes: Option<Vec<Box<CustomAttribute>>>,
    /// kind property.
    pub kind: Option<String>,
    /// price property.
    pub price: Option<Price>,
    /// regionId property.
    pub region_id: Option<String>,
    /// salePrice property.
    pub sale_price: Option<Price>,
    /// salePriceEffectiveDate property.
    pub sale_price_effective_date: Option<String>,
}

/// `DatafeedstatusesCustomBatchRequestEntry` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct DatafeedstatusesCustomBatchRequestEntry {
    /// batchId property.
    pub batch_id: Option<i64>,
    /// country property.
    pub country: Option<String>,
    /// datafeedId property.
    pub datafeed_id: Option<String>,
    /// feedLabel property.
    pub feed_label: Option<String>,
    /// language property.
    pub language: Option<String>,
    /// merchantId property.
    pub merchant_id: Option<String>,
    /// method property.
    pub method: Option<String>,
}

/// `ExternalAction` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ExternalAction {
    /// type property.
    pub r#type: Option<String>,
    /// uri property.
    pub uri: Option<String>,
}

/// `InputFieldChoiceInputChoiceInputOption` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct InputFieldChoiceInputChoiceInputOption {
    /// additionalInput property.
    pub additional_input: Option<Box<InputField>>,
    /// id property.
    pub id: Option<String>,
    /// label property.
    pub label: Option<TextWithTooltip>,
}

/// `CompetitiveVisibility` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct CompetitiveVisibility {
    /// adsOrganicRatio property.
    pub ads_organic_ratio: Option<f64>,
    /// categoryBenchmarkVisibilityTrend property.
    pub category_benchmark_visibility_trend: Option<f64>,
    /// categoryId property.
    pub category_id: Option<String>,
    /// countryCode property.
    pub country_code: Option<String>,
    /// date property.
    pub date: Option<Date>,
    /// domain property.
    pub domain: Option<String>,
    /// higherPositionRate property.
    pub higher_position_rate: Option<f64>,
    /// isYourDomain property.
    pub is_your_domain: Option<bool>,
    /// pageOverlapRate property.
    pub page_overlap_rate: Option<f64>,
    /// rank property.
    pub rank: Option<String>,
    /// relativeVisibility property.
    pub relative_visibility: Option<f64>,
    /// trafficSource property.
    pub traffic_source: Option<String>,
    /// yourDomainVisibilityTrend property.
    pub your_domain_visibility_trend: Option<f64>,
}

/// `PriceCompetitiveness` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct PriceCompetitiveness {
    /// benchmarkPriceCurrencyCode property.
    pub benchmark_price_currency_code: Option<String>,
    /// benchmarkPriceMicros property.
    pub benchmark_price_micros: Option<String>,
    /// countryCode property.
    pub country_code: Option<String>,
}

/// `ProductStatus` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ProductStatus {
    /// creationDate property.
    pub creation_date: Option<String>,
    /// destinationStatuses property.
    pub destination_statuses: Option<Vec<ProductStatusDestinationStatus>>,
    /// googleExpirationDate property.
    pub google_expiration_date: Option<String>,
    /// itemLevelIssues property.
    pub item_level_issues: Option<Vec<ProductStatusItemLevelIssue>>,
    /// kind property.
    pub kind: Option<String>,
    /// lastUpdateDate property.
    pub last_update_date: Option<String>,
    /// link property.
    pub link: Option<String>,
    /// productId property.
    pub product_id: Option<String>,
    /// title property.
    pub title: Option<String>,
}

/// `Table` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Table {
    /// columnHeaders property.
    pub column_headers: Option<Headers>,
    /// name property.
    pub name: Option<String>,
    /// rowHeaders property.
    pub row_headers: Option<Headers>,
    /// rows property.
    pub rows: Option<Vec<Row>>,
}

/// `PosCustomBatchResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct PosCustomBatchResponse {
    /// entries property.
    pub entries: Option<Vec<PosCustomBatchResponseEntry>>,
    /// kind property.
    pub kind: Option<String>,
}

/// `Headers` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Headers {
    /// locations property.
    pub locations: Option<Vec<LocationIdSet>>,
    /// numberOfItems property.
    pub number_of_items: Option<Vec<String>>,
    /// postalCodeGroupNames property.
    pub postal_code_group_names: Option<Vec<String>>,
    /// prices property.
    pub prices: Option<Vec<Price>>,
    /// weights property.
    pub weights: Option<Vec<Weight>>,
}

/// `PosCustomBatchResponseEntry` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct PosCustomBatchResponseEntry {
    /// batchId property.
    pub batch_id: Option<i64>,
    /// errors property.
    pub errors: Option<Errors>,
    /// inventory property.
    pub inventory: Option<PosInventory>,
    /// kind property.
    pub kind: Option<String>,
    /// sale property.
    pub sale: Option<PosSale>,
    /// store property.
    pub store: Option<PosStore>,
}

/// `ProductsCustomBatchResponseEntry` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ProductsCustomBatchResponseEntry {
    /// batchId property.
    pub batch_id: Option<i64>,
    /// errors property.
    pub errors: Option<Errors>,
    /// kind property.
    pub kind: Option<String>,
    /// product property.
    pub product: Option<Product>,
}

/// `PosStore` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct PosStore {
    /// gcidCategory property.
    pub gcid_category: Option<Vec<String>>,
    /// kind property.
    pub kind: Option<String>,
    /// matchingStatus property.
    pub matching_status: Option<String>,
    /// matchingStatusHint property.
    pub matching_status_hint: Option<String>,
    /// phoneNumber property.
    pub phone_number: Option<String>,
    /// placeId property.
    pub place_id: Option<String>,
    /// storeAddress property.
    pub store_address: Option<String>,
    /// storeCode property.
    pub store_code: Option<String>,
    /// storeName property.
    pub store_name: Option<String>,
    /// websiteUrl property.
    pub website_url: Option<String>,
}

/// `HolidayCutoff` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct HolidayCutoff {
    /// deadlineDate property.
    pub deadline_date: Option<String>,
    /// deadlineHour property.
    pub deadline_hour: Option<i64>,
    /// deadlineTimezone property.
    pub deadline_timezone: Option<String>,
    /// holidayId property.
    pub holiday_id: Option<String>,
    /// visibleFromDate property.
    pub visible_from_date: Option<String>,
}

/// `ProductShippingWeight` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ProductShippingWeight {
    /// unit property.
    pub unit: Option<String>,
    /// value property.
    pub value: Option<f64>,
}

/// `ProductsListResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ProductsListResponse {
    /// kind property.
    pub kind: Option<String>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// resources property.
    pub resources: Option<Vec<Product>>,
}

/// `ECommercePlatformLinkInfo` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ECommercePlatformLinkInfo {
    /// externalAccountId property.
    pub external_account_id: Option<String>,
}

/// `LiaOnDisplayToOrderSettings` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct LiaOnDisplayToOrderSettings {
    /// shippingCostPolicyUrl property.
    pub shipping_cost_policy_url: Option<String>,
    /// status property.
    pub status: Option<String>,
}

/// `PaymentServiceProviderLinkInfo` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct PaymentServiceProviderLinkInfo {
    /// externalAccountBusinessCountry property.
    pub external_account_business_country: Option<String>,
    /// externalAccountId property.
    pub external_account_id: Option<String>,
}

/// `AccounttaxListResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AccounttaxListResponse {
    /// kind property.
    pub kind: Option<String>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// resources property.
    pub resources: Option<Vec<AccountTax>>,
}

/// `PromotionPromotionStatusPromotionIssue` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct PromotionPromotionStatusPromotionIssue {
    /// code property.
    pub code: Option<String>,
    /// detail property.
    pub detail: Option<String>,
}

/// `ProductCertification` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ProductCertification {
    /// certificationAuthority property.
    pub certification_authority: Option<String>,
    /// certificationCode property.
    pub certification_code: Option<String>,
    /// certificationName property.
    pub certification_name: Option<String>,
    /// certificationValue property.
    pub certification_value: Option<String>,
}

/// `BuiltInSimpleActionAdditionalContent` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct BuiltInSimpleActionAdditionalContent {
    /// paragraphs property.
    pub paragraphs: Option<Vec<String>>,
    /// title property.
    pub title: Option<String>,
}

/// `LiasettingsListResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct LiasettingsListResponse {
    /// kind property.
    pub kind: Option<String>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// resources property.
    pub resources: Option<Vec<LiaSettings>>,
}

/// `BreakdownRegion` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct BreakdownRegion {
    /// code property.
    pub code: Option<String>,
    /// name property.
    pub name: Option<String>,
}

/// `RegionalinventoryCustomBatchResponseEntry` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct RegionalinventoryCustomBatchResponseEntry {
    /// batchId property.
    pub batch_id: Option<i64>,
    /// errors property.
    pub errors: Option<Errors>,
    /// kind property.
    pub kind: Option<String>,
    /// regionalInventory property.
    pub regional_inventory: Option<RegionalInventory>,
}

/// `ShoppingAdsProgramStatusReviewIneligibilityReasonDetails` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ShoppingAdsProgramStatusReviewIneligibilityReasonDetails {
    /// cooldownTime property.
    pub cooldown_time: Option<String>,
}

/// `AlternateDisputeResolution` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AlternateDisputeResolution {
    /// label property.
    pub label: Option<String>,
    /// uri property.
    pub uri: Option<String>,
}

/// `RenderProductIssuesRequestPayload` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct RenderProductIssuesRequestPayload {
    /// contentOption property.
    pub content_option: Option<String>,
    /// userInputActionOption property.
    pub user_input_action_option: Option<String>,
}

/// `LiasettingsCustomBatchResponseEntry` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct LiasettingsCustomBatchResponseEntry {
    /// batchId property.
    pub batch_id: Option<i64>,
    /// errors property.
    pub errors: Option<Errors>,
    /// gmbAccounts property.
    pub gmb_accounts: Option<GmbAccounts>,
    /// kind property.
    pub kind: Option<String>,
    /// liaSettings property.
    pub lia_settings: Option<LiaSettings>,
    /// omnichannelExperience property.
    pub omnichannel_experience: Option<LiaOmnichannelExperience>,
    /// posDataProviders property.
    pub pos_data_providers: Option<Vec<PosDataProviders>>,
}

/// `Price` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Price {
    /// currency property.
    pub currency: Option<String>,
    /// value property.
    pub value: Option<String>,
}

/// `MinimumOrderValueTable` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct MinimumOrderValueTable {
    /// storeCodeSetWithMovs property.
    pub store_code_set_with_movs: Option<Vec<MinimumOrderValueTableStoreCodeSetWithMov>>,
}

/// `AccountItemUpdates` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AccountItemUpdates {
    /// accountItemUpdatesSettings property.
    pub account_item_updates_settings: Option<AccountItemUpdatesSettings>,
    /// effectiveAllowAvailabilityUpdates property.
    pub effective_allow_availability_updates: Option<bool>,
    /// effectiveAllowConditionUpdates property.
    pub effective_allow_condition_updates: Option<bool>,
    /// effectiveAllowPriceUpdates property.
    pub effective_allow_price_updates: Option<bool>,
    /// effectiveAllowStrictAvailabilityUpdates property.
    pub effective_allow_strict_availability_updates: Option<bool>,
}

/// `LocalInventory` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct LocalInventory {
    /// availability property.
    pub availability: Option<String>,
    /// customAttributes property.
    pub custom_attributes: Option<Vec<Box<CustomAttribute>>>,
    /// instoreProductLocation property.
    pub instore_product_location: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// pickupMethod property.
    pub pickup_method: Option<String>,
    /// pickupSla property.
    pub pickup_sla: Option<String>,
    /// price property.
    pub price: Option<Price>,
    /// quantity property.
    pub quantity: Option<i64>,
    /// salePrice property.
    pub sale_price: Option<Price>,
    /// salePriceEffectiveDate property.
    pub sale_price_effective_date: Option<String>,
    /// storeCode property.
    pub store_code: Option<String>,
}

/// `ProductstatusesCustomBatchRequestEntry` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ProductstatusesCustomBatchRequestEntry {
    /// batchId property.
    pub batch_id: Option<i64>,
    /// destinations property.
    pub destinations: Option<Vec<String>>,
    /// includeAttributes property.
    pub include_attributes: Option<bool>,
    /// merchantId property.
    pub merchant_id: Option<String>,
    /// method property.
    pub method: Option<String>,
    /// productId property.
    pub product_id: Option<String>,
}

/// `InputValueChoiceInputValue` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct InputValueChoiceInputValue {
    /// choiceInputOptionId property.
    pub choice_input_option_id: Option<String>,
}

/// `DateTime` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct DateTime {
    /// day property.
    pub day: Option<i64>,
    /// hours property.
    pub hours: Option<i64>,
    /// minutes property.
    pub minutes: Option<i64>,
    /// month property.
    pub month: Option<i64>,
    /// nanos property.
    pub nanos: Option<i64>,
    /// seconds property.
    pub seconds: Option<i64>,
    /// timeZone property.
    pub time_zone: Option<TimeZone>,
    /// utcOffset property.
    pub utc_offset: Option<String>,
    /// year property.
    pub year: Option<i64>,
}

/// `Collection` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Collection {
    /// customLabel0 property.
    pub custom_label0: Option<String>,
    /// customLabel1 property.
    pub custom_label1: Option<String>,
    /// customLabel2 property.
    pub custom_label2: Option<String>,
    /// customLabel3 property.
    pub custom_label3: Option<String>,
    /// customLabel4 property.
    pub custom_label4: Option<String>,
    /// featuredProduct property.
    pub featured_product: Option<Vec<CollectionFeaturedProduct>>,
    /// headline property.
    pub headline: Option<Vec<String>>,
    /// id property.
    pub id: Option<String>,
    /// imageLink property.
    pub image_link: Option<Vec<String>>,
    /// language property.
    pub language: Option<String>,
    /// link property.
    pub link: Option<String>,
    /// mobileLink property.
    pub mobile_link: Option<String>,
    /// productCountry property.
    pub product_country: Option<String>,
}

/// `ProductsCustomBatchRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ProductsCustomBatchRequest {
    /// entries property.
    pub entries: Option<Vec<ProductsCustomBatchRequestEntry>>,
}

/// `InputValue` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct InputValue {
    /// checkboxInputValue property.
    pub checkbox_input_value: Option<InputValueCheckboxInputValue>,
    /// choiceInputValue property.
    pub choice_input_value: Option<InputValueChoiceInputValue>,
    /// inputFieldId property.
    pub input_field_id: Option<String>,
    /// textInputValue property.
    pub text_input_value: Option<InputValueTextInputValue>,
}

/// `PickupServicesPickupService` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct PickupServicesPickupService {
    /// carrierName property.
    pub carrier_name: Option<String>,
    /// country property.
    pub country: Option<String>,
    /// serviceName property.
    pub service_name: Option<String>,
}

/// `AccounttaxCustomBatchResponseEntry` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AccounttaxCustomBatchResponseEntry {
    /// accountTax property.
    pub account_tax: Option<AccountTax>,
    /// batchId property.
    pub batch_id: Option<i64>,
    /// errors property.
    pub errors: Option<Errors>,
    /// kind property.
    pub kind: Option<String>,
}

/// `AccountsCustomBatchResponseEntry` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AccountsCustomBatchResponseEntry {
    /// account property.
    pub account: Option<Account>,
    /// batchId property.
    pub batch_id: Option<i64>,
    /// errors property.
    pub errors: Option<Errors>,
    /// kind property.
    pub kind: Option<String>,
}

/// `AccountIssueImpact` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AccountIssueImpact {
    /// breakdowns property.
    pub breakdowns: Option<Vec<Breakdown>>,
    /// message property.
    pub message: Option<String>,
    /// severity property.
    pub severity: Option<String>,
}

/// `ShoppingAdsProgramStatusRegionStatus` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ShoppingAdsProgramStatusRegionStatus {
    /// disapprovalDate property.
    pub disapproval_date: Option<String>,
    /// eligibilityStatus property.
    pub eligibility_status: Option<String>,
    /// onboardingIssues property.
    pub onboarding_issues: Option<Vec<String>>,
    /// regionCodes property.
    pub region_codes: Option<Vec<String>>,
    /// reviewEligibilityStatus property.
    pub review_eligibility_status: Option<String>,
    /// reviewIneligibilityReason property.
    pub review_ineligibility_reason: Option<String>,
    /// reviewIneligibilityReasonDescription property.
    pub review_ineligibility_reason_description: Option<String>,
    /// reviewIneligibilityReasonDetails property.
    pub review_ineligibility_reason_details:
        Option<ShoppingAdsProgramStatusReviewIneligibilityReasonDetails>,
    /// reviewIssues property.
    pub review_issues: Option<Vec<String>>,
}

/// `Date` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Date {
    /// day property.
    pub day: Option<i64>,
    /// month property.
    pub month: Option<i64>,
    /// year property.
    pub year: Option<i64>,
}

/// `ProductDeliveryTimeAreaDeliveryTimeDeliveryTime` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ProductDeliveryTimeAreaDeliveryTimeDeliveryTime {
    /// maxHandlingTimeDays property.
    pub max_handling_time_days: Option<i64>,
    /// maxTransitTimeDays property.
    pub max_transit_time_days: Option<i64>,
    /// minHandlingTimeDays property.
    pub min_handling_time_days: Option<i64>,
    /// minTransitTimeDays property.
    pub min_transit_time_days: Option<i64>,
}

/// `AccountImageImprovementsSettings` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AccountImageImprovementsSettings {
    /// allowAutomaticImageImprovements property.
    pub allow_automatic_image_improvements: Option<bool>,
}

/// `DatafeedFetchSchedule` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct DatafeedFetchSchedule {
    /// dayOfMonth property.
    pub day_of_month: Option<i64>,
    /// fetchUrl property.
    pub fetch_url: Option<String>,
    /// hour property.
    pub hour: Option<i64>,
    /// minuteOfHour property.
    pub minute_of_hour: Option<i64>,
    /// password property.
    pub password: Option<String>,
    /// paused property.
    pub paused: Option<bool>,
    /// timeZone property.
    pub time_zone: Option<String>,
    /// username property.
    pub username: Option<String>,
    /// weekday property.
    pub weekday: Option<String>,
}

/// `DatafeedsCustomBatchResponseEntry` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct DatafeedsCustomBatchResponseEntry {
    /// batchId property.
    pub batch_id: Option<i64>,
    /// datafeed property.
    pub datafeed: Option<Datafeed>,
    /// errors property.
    pub errors: Option<Errors>,
}

/// `GenerateRecommendationsResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GenerateRecommendationsResponse {
    /// recommendations property.
    pub recommendations: Option<Vec<Recommendation>>,
    /// responseToken property.
    pub response_token: Option<String>,
}

/// `DatafeedFormat` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct DatafeedFormat {
    /// columnDelimiter property.
    pub column_delimiter: Option<String>,
    /// fileEncoding property.
    pub file_encoding: Option<String>,
    /// quotingMode property.
    pub quoting_mode: Option<String>,
}

/// `PosInventoryResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct PosInventoryResponse {
    /// contentLanguage property.
    pub content_language: Option<String>,
    /// gtin property.
    pub gtin: Option<String>,
    /// itemId property.
    pub item_id: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// pickupMethod property.
    pub pickup_method: Option<String>,
    /// pickupSla property.
    pub pickup_sla: Option<String>,
    /// price property.
    pub price: Option<Price>,
    /// quantity property.
    pub quantity: Option<String>,
    /// storeCode property.
    pub store_code: Option<String>,
    /// targetCountry property.
    pub target_country: Option<String>,
    /// timestamp property.
    pub timestamp: Option<String>,
}

/// `ProductWeight` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ProductWeight {
    /// unit property.
    pub unit: Option<String>,
    /// value property.
    pub value: Option<f64>,
}

/// `MinimumOrderValueTableStoreCodeSetWithMov` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct MinimumOrderValueTableStoreCodeSetWithMov {
    /// storeCodes property.
    pub store_codes: Option<Vec<String>>,
    /// value property.
    pub value: Option<Price>,
}

/// `BuiltInSimpleAction` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct BuiltInSimpleAction {
    /// additionalContent property.
    pub additional_content: Option<BuiltInSimpleActionAdditionalContent>,
    /// attributeCode property.
    pub attribute_code: Option<String>,
    /// type property.
    pub r#type: Option<String>,
}

/// `PosDataProviders` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct PosDataProviders {
    /// country property.
    pub country: Option<String>,
    /// posDataProviders property.
    pub pos_data_providers: Option<Vec<PosDataProvidersPosDataProvider>>,
}

/// `ProductIssueImpact` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ProductIssueImpact {
    /// breakdowns property.
    pub breakdowns: Option<Vec<Breakdown>>,
    /// message property.
    pub message: Option<String>,
    /// severity property.
    pub severity: Option<String>,
}

/// `AccounttaxCustomBatchRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AccounttaxCustomBatchRequest {
    /// entries property.
    pub entries: Option<Vec<AccounttaxCustomBatchRequestEntry>>,
}

/// `Errors` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Errors {
    /// code property.
    pub code: Option<i64>,
    /// errors property.
    pub errors: Option<Vec<Error>>,
    /// message property.
    pub message: Option<String>,
}

/// `ServiceStoreConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ServiceStoreConfig {
    /// cutoffConfig property.
    pub cutoff_config: Option<ServiceStoreConfigCutoffConfig>,
    /// serviceRadius property.
    pub service_radius: Option<Distance>,
    /// storeCodes property.
    pub store_codes: Option<Vec<String>>,
    /// storeServiceType property.
    pub store_service_type: Option<String>,
}

/// `Promotion` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Promotion {
    /// brand property.
    pub brand: Option<Vec<String>>,
    /// brandExclusion property.
    pub brand_exclusion: Option<Vec<String>>,
    /// contentLanguage property.
    pub content_language: Option<String>,
    /// couponValueType property.
    pub coupon_value_type: Option<String>,
    /// customRedemptionRestriction property.
    pub custom_redemption_restriction: Option<String>,
    /// freeGiftDescription property.
    pub free_gift_description: Option<String>,
    /// freeGiftItemId property.
    pub free_gift_item_id: Option<String>,
    /// freeGiftValue property.
    pub free_gift_value: Option<PriceAmount>,
    /// genericRedemptionCode property.
    pub generic_redemption_code: Option<String>,
    /// getThisQuantityDiscounted property.
    pub get_this_quantity_discounted: Option<i64>,
    /// id property.
    pub id: Option<String>,
    /// itemGroupId property.
    pub item_group_id: Option<Vec<String>>,
    /// itemGroupIdExclusion property.
    pub item_group_id_exclusion: Option<Vec<String>>,
    /// itemId property.
    pub item_id: Option<Vec<String>>,
    /// itemIdExclusion property.
    pub item_id_exclusion: Option<Vec<String>>,
    /// limitQuantity property.
    pub limit_quantity: Option<i64>,
    /// limitValue property.
    pub limit_value: Option<PriceAmount>,
    /// longTitle property.
    pub long_title: Option<String>,
    /// maxDiscountAmount property.
    pub max_discount_amount: Option<PriceAmount>,
    /// minimumPurchaseAmount property.
    pub minimum_purchase_amount: Option<PriceAmount>,
    /// minimumPurchaseQuantity property.
    pub minimum_purchase_quantity: Option<i64>,
    /// moneyBudget property.
    pub money_budget: Option<PriceAmount>,
    /// moneyOffAmount property.
    pub money_off_amount: Option<PriceAmount>,
    /// offerType property.
    pub offer_type: Option<String>,
    /// orderLimit property.
    pub order_limit: Option<i64>,
    /// percentOff property.
    pub percent_off: Option<i64>,
    /// productApplicability property.
    pub product_applicability: Option<String>,
    /// productType property.
    pub product_type: Option<Vec<String>>,
    /// productTypeExclusion property.
    pub product_type_exclusion: Option<Vec<String>>,
    /// promotionDestinationIds property.
    pub promotion_destination_ids: Option<Vec<String>>,
    /// promotionDisplayDates property.
    pub promotion_display_dates: Option<String>,
    /// promotionDisplayTimePeriod property.
    pub promotion_display_time_period: Option<TimePeriod>,
    /// promotionEffectiveDates property.
    pub promotion_effective_dates: Option<String>,
    /// promotionEffectiveTimePeriod property.
    pub promotion_effective_time_period: Option<TimePeriod>,
    /// promotionId property.
    pub promotion_id: Option<String>,
    /// promotionStatus property.
    pub promotion_status: Option<PromotionPromotionStatus>,
    /// promotionUrl property.
    pub promotion_url: Option<String>,
    /// redemptionChannel property.
    pub redemption_channel: Option<Vec<String>>,
    /// redemptionRestriction property.
    pub redemption_restriction: Option<String>,
    /// shippingServiceNames property.
    pub shipping_service_names: Option<Vec<String>>,
    /// storeApplicability property.
    pub store_applicability: Option<String>,
    /// storeCode property.
    pub store_code: Option<Vec<String>>,
    /// storeCodeExclusion property.
    pub store_code_exclusion: Option<Vec<String>>,
    /// targetCountry property.
    pub target_country: Option<String>,
}

/// `MerchantCenterDestination` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct MerchantCenterDestination {
    /// attributionSettings property.
    pub attribution_settings: Option<AttributionSettings>,
    /// currencyCode property.
    pub currency_code: Option<String>,
    /// destinationId property.
    pub destination_id: Option<String>,
    /// displayName property.
    pub display_name: Option<String>,
}

/// `LiasettingsListPosDataProvidersResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct LiasettingsListPosDataProvidersResponse {
    /// kind property.
    pub kind: Option<String>,
    /// posDataProviders property.
    pub pos_data_providers: Option<Vec<PosDataProviders>>,
}

/// `CollectionStatusDestinationStatus` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct CollectionStatusDestinationStatus {
    /// approvedCountries property.
    pub approved_countries: Option<Vec<String>>,
    /// destination property.
    pub destination: Option<String>,
    /// disapprovedCountries property.
    pub disapproved_countries: Option<Vec<String>>,
    /// pendingCountries property.
    pub pending_countries: Option<Vec<String>>,
    /// status property.
    pub status: Option<String>,
}

/// `PromotionPromotionStatusDestinationStatus` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct PromotionPromotionStatusDestinationStatus {
    /// destination property.
    pub destination: Option<String>,
    /// status property.
    pub status: Option<String>,
}

/// `ProductDeliveryTimeAreaDeliveryTime` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ProductDeliveryTimeAreaDeliveryTime {
    /// deliveryArea property.
    pub delivery_area: Option<DeliveryArea>,
    /// deliveryTime property.
    pub delivery_time: Option<ProductDeliveryTimeAreaDeliveryTimeDeliveryTime>,
}

/// `CollectionFeaturedProduct` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct CollectionFeaturedProduct {
    /// offerId property.
    pub offer_id: Option<String>,
    /// x property.
    pub x: Option<f64>,
    /// y property.
    pub y: Option<f64>,
}

/// `AccountsLinkResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AccountsLinkResponse {
    /// kind property.
    pub kind: Option<String>,
}

/// `LiasettingsRequestInventoryVerificationResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct LiasettingsRequestInventoryVerificationResponse {
    /// kind property.
    pub kind: Option<String>,
}

/// `TextWithTooltip` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct TextWithTooltip {
    /// simpleTooltipValue property.
    pub simple_tooltip_value: Option<String>,
    /// simpleValue property.
    pub simple_value: Option<String>,
    /// tooltipIconStyle property.
    pub tooltip_icon_style: Option<String>,
}

/// `PromotionPromotionStatus` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct PromotionPromotionStatus {
    /// creationDate property.
    pub creation_date: Option<String>,
    /// destinationStatuses property.
    pub destination_statuses: Option<Vec<PromotionPromotionStatusDestinationStatus>>,
    /// lastUpdateDate property.
    pub last_update_date: Option<String>,
    /// promotionIssue property.
    pub promotion_issue: Option<Vec<PromotionPromotionStatusPromotionIssue>>,
}

/// `BusinessDayConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct BusinessDayConfig {
    /// businessDays property.
    pub business_days: Option<Vec<String>>,
}

/// `RegionalinventoryCustomBatchRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct RegionalinventoryCustomBatchRequest {
    /// entries property.
    pub entries: Option<Vec<RegionalinventoryCustomBatchRequestEntry>>,
}

/// `AccountLabel` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AccountLabel {
    /// accountId property.
    pub account_id: Option<String>,
    /// description property.
    pub description: Option<String>,
    /// labelId property.
    pub label_id: Option<String>,
    /// labelType property.
    pub label_type: Option<String>,
    /// name property.
    pub name: Option<String>,
}

/// `AccountConversionSettings` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AccountConversionSettings {
    /// freeListingsAutoTaggingEnabled property.
    pub free_listings_auto_tagging_enabled: Option<bool>,
}

/// `ShippingsettingsCustomBatchRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ShippingsettingsCustomBatchRequest {
    /// entries property.
    pub entries: Option<Vec<ShippingsettingsCustomBatchRequestEntry>>,
}

/// `AccountAutomaticImprovements` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AccountAutomaticImprovements {
    /// imageImprovements property.
    pub image_improvements: Option<AccountImageImprovements>,
    /// itemUpdates property.
    pub item_updates: Option<AccountItemUpdates>,
    /// shippingImprovements property.
    pub shipping_improvements: Option<AccountShippingImprovements>,
}

/// `DatafeedTarget` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct DatafeedTarget {
    /// country property.
    pub country: Option<String>,
    /// excludedDestinations property.
    pub excluded_destinations: Option<Vec<String>>,
    /// feedLabel property.
    pub feed_label: Option<String>,
    /// includedDestinations property.
    pub included_destinations: Option<Vec<String>>,
    /// language property.
    pub language: Option<String>,
    /// targetCountries property.
    pub target_countries: Option<Vec<String>>,
}

/// `AccounttaxCustomBatchResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AccounttaxCustomBatchResponse {
    /// entries property.
    pub entries: Option<Vec<AccounttaxCustomBatchResponseEntry>>,
    /// kind property.
    pub kind: Option<String>,
}

/// `ReportRow` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ReportRow {
    /// bestSellers property.
    pub best_sellers: Option<BestSellers>,
    /// brand property.
    pub brand: Option<Brand>,
    /// competitiveVisibility property.
    pub competitive_visibility: Option<CompetitiveVisibility>,
    /// metrics property.
    pub metrics: Option<Metrics>,
    /// priceCompetitiveness property.
    pub price_competitiveness: Option<PriceCompetitiveness>,
    /// priceInsights property.
    pub price_insights: Option<PriceInsights>,
    /// productCluster property.
    pub product_cluster: Option<ProductCluster>,
    /// productView property.
    pub product_view: Option<ProductView>,
    /// segments property.
    pub segments: Option<Segments>,
    /// topicTrends property.
    pub topic_trends: Option<TopicTrends>,
}

/// `CutoffTime` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct CutoffTime {
    /// hour property.
    pub hour: Option<i64>,
    /// minute property.
    pub minute: Option<i64>,
    /// timezone property.
    pub timezone: Option<String>,
}

/// `Error` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Error {
    /// domain property.
    pub domain: Option<String>,
    /// message property.
    pub message: Option<String>,
    /// reason property.
    pub reason: Option<String>,
}

/// `AccountUser` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AccountUser {
    /// admin property.
    pub admin: Option<bool>,
    /// emailAddress property.
    pub email_address: Option<String>,
    /// orderManager property.
    pub order_manager: Option<bool>,
    /// paymentsAnalyst property.
    pub payments_analyst: Option<bool>,
    /// paymentsManager property.
    pub payments_manager: Option<bool>,
    /// readOnly property.
    pub read_only: Option<bool>,
    /// reportingManager property.
    pub reporting_manager: Option<bool>,
}

/// `AccountIssue` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AccountIssue {
    /// actions property.
    pub actions: Option<Vec<Box<Action>>>,
    /// impact property.
    pub impact: Option<AccountIssueImpact>,
    /// prerenderedContent property.
    pub prerendered_content: Option<String>,
    /// prerenderedOutOfCourtDisputeSettlement property.
    pub prerendered_out_of_court_dispute_settlement: Option<String>,
    /// title property.
    pub title: Option<String>,
}

/// `LiasettingsGetAccessibleGmbAccountsResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct LiasettingsGetAccessibleGmbAccountsResponse {
    /// accountId property.
    pub account_id: Option<String>,
    /// gmbAccounts property.
    pub gmb_accounts: Option<Vec<GmbAccountsGmbAccount>>,
    /// kind property.
    pub kind: Option<String>,
}

/// `TriggerActionPayload` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct TriggerActionPayload {
    /// actionContext property.
    pub action_context: Option<String>,
    /// actionInput property.
    pub action_input: Option<ActionInput>,
}

/// `AccountTaxTaxRule` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AccountTaxTaxRule {
    /// country property.
    pub country: Option<String>,
    /// locationId property.
    pub location_id: Option<String>,
    /// ratePercent property.
    pub rate_percent: Option<String>,
    /// shippingTaxed property.
    pub shipping_taxed: Option<bool>,
    /// useGlobalRate property.
    pub use_global_rate: Option<bool>,
}

/// `ShippingsettingsCustomBatchResponseEntry` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ShippingsettingsCustomBatchResponseEntry {
    /// batchId property.
    pub batch_id: Option<i64>,
    /// errors property.
    pub errors: Option<Errors>,
    /// kind property.
    pub kind: Option<String>,
    /// shippingSettings property.
    pub shipping_settings: Option<ShippingSettings>,
}

/// `ReturnPolicyOnlinePolicy` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ReturnPolicyOnlinePolicy {
    /// days property.
    pub days: Option<String>,
    /// type property.
    pub r#type: Option<String>,
}

/// `VerifyPhoneNumberResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct VerifyPhoneNumberResponse {
    /// verifiedPhoneNumber property.
    pub verified_phone_number: Option<String>,
}

/// `CarrierRate` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct CarrierRate {
    /// carrierName property.
    pub carrier_name: Option<String>,
    /// carrierService property.
    pub carrier_service: Option<String>,
    /// flatAdjustment property.
    pub flat_adjustment: Option<Price>,
    /// name property.
    pub name: Option<String>,
    /// originPostalCode property.
    pub origin_postal_code: Option<String>,
    /// percentageAdjustment property.
    pub percentage_adjustment: Option<String>,
}

/// `WarehouseBasedDeliveryTime` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct WarehouseBasedDeliveryTime {
    /// carrier property.
    pub carrier: Option<String>,
    /// carrierService property.
    pub carrier_service: Option<String>,
    /// originAdministrativeArea property.
    pub origin_administrative_area: Option<String>,
    /// originCity property.
    pub origin_city: Option<String>,
    /// originCountry property.
    pub origin_country: Option<String>,
    /// originPostalCode property.
    pub origin_postal_code: Option<String>,
    /// originStreetAddress property.
    pub origin_street_address: Option<String>,
    /// warehouseName property.
    pub warehouse_name: Option<String>,
}

/// `PosDataProvidersPosDataProvider` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct PosDataProvidersPosDataProvider {
    /// displayName property.
    pub display_name: Option<String>,
    /// fullName property.
    pub full_name: Option<String>,
    /// providerId property.
    pub provider_id: Option<String>,
}

/// `AccountIdentityType` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AccountIdentityType {
    /// selfIdentified property.
    pub self_identified: Option<bool>,
}

/// `RequestPhoneVerificationResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct RequestPhoneVerificationResponse {
    /// verificationId property.
    pub verification_id: Option<String>,
}

/// `LinkedAccount` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct LinkedAccount {
    /// linkedAccountId property.
    pub linked_account_id: Option<String>,
    /// services property.
    pub services: Option<Vec<LinkService>>,
}

/// `DatafeedstatusesCustomBatchResponseEntry` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct DatafeedstatusesCustomBatchResponseEntry {
    /// batchId property.
    pub batch_id: Option<i64>,
    /// datafeedStatus property.
    pub datafeed_status: Option<DatafeedStatus>,
    /// errors property.
    pub errors: Option<Errors>,
}

/// `AccountsClaimWebsiteResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AccountsClaimWebsiteResponse {
    /// kind property.
    pub kind: Option<String>,
}

/// `Datafeed` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Datafeed {
    /// attributeLanguage property.
    pub attribute_language: Option<String>,
    /// contentType property.
    pub content_type: Option<String>,
    /// fetchSchedule property.
    pub fetch_schedule: Option<DatafeedFetchSchedule>,
    /// fileName property.
    pub file_name: Option<String>,
    /// format property.
    pub format: Option<DatafeedFormat>,
    /// id property.
    pub id: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// targets property.
    pub targets: Option<Vec<DatafeedTarget>>,
}

/// `ShippingsettingsCustomBatchRequestEntry` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ShippingsettingsCustomBatchRequestEntry {
    /// accountId property.
    pub account_id: Option<String>,
    /// batchId property.
    pub batch_id: Option<i64>,
    /// merchantId property.
    pub merchant_id: Option<String>,
    /// method property.
    pub method: Option<String>,
    /// shippingSettings property.
    pub shipping_settings: Option<ShippingSettings>,
}

/// `AccountsListLinksResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AccountsListLinksResponse {
    /// kind property.
    pub kind: Option<String>,
    /// links property.
    pub links: Option<Vec<LinkedAccount>>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
}

/// `ProductstatusesCustomBatchRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ProductstatusesCustomBatchRequest {
    /// entries property.
    pub entries: Option<Vec<ProductstatusesCustomBatchRequestEntry>>,
}

/// `DatafeedsCustomBatchRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct DatafeedsCustomBatchRequest {
    /// entries property.
    pub entries: Option<Vec<DatafeedsCustomBatchRequestEntry>>,
}

/// `DatafeedStatus` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct DatafeedStatus {
    /// country property.
    pub country: Option<String>,
    /// datafeedId property.
    pub datafeed_id: Option<String>,
    /// errors property.
    pub errors: Option<Vec<DatafeedStatusError>>,
    /// feedLabel property.
    pub feed_label: Option<String>,
    /// itemsTotal property.
    pub items_total: Option<String>,
    /// itemsValid property.
    pub items_valid: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// language property.
    pub language: Option<String>,
    /// lastUploadDate property.
    pub last_upload_date: Option<String>,
    /// processingStatus property.
    pub processing_status: Option<String>,
    /// warnings property.
    pub warnings: Option<Vec<DatafeedStatusError>>,
}

/// `Row` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Row {
    /// cells property.
    pub cells: Option<Vec<Value>>,
}

/// `ProductUnitPricingBaseMeasure` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ProductUnitPricingBaseMeasure {
    /// unit property.
    pub unit: Option<String>,
    /// value property.
    pub value: Option<String>,
}

/// `RequestPhoneVerificationRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct RequestPhoneVerificationRequest {
    /// languageCode property.
    pub language_code: Option<String>,
    /// phoneNumber property.
    pub phone_number: Option<String>,
    /// phoneRegionCode property.
    pub phone_region_code: Option<String>,
    /// phoneVerificationMethod property.
    pub phone_verification_method: Option<String>,
}

/// `LiaOmnichannelExperience` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct LiaOmnichannelExperience {
    /// country property.
    pub country: Option<String>,
    /// lsfType property.
    pub lsf_type: Option<String>,
    /// pickupTypes property.
    pub pickup_types: Option<Vec<String>>,
}

/// `RenderAccountIssuesRequestPayload` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct RenderAccountIssuesRequestPayload {
    /// contentOption property.
    pub content_option: Option<String>,
    /// userInputActionOption property.
    pub user_input_action_option: Option<String>,
}

/// `RegionPostalCodeArea` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct RegionPostalCodeArea {
    /// postalCodes property.
    pub postal_codes: Option<Vec<RegionPostalCodeAreaPostalCodeRange>>,
    /// regionCode property.
    pub region_code: Option<String>,
}

/// `AccountsCustomBatchRequestEntry` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AccountsCustomBatchRequestEntry {
    /// account property.
    pub account: Option<Account>,
    /// accountId property.
    pub account_id: Option<String>,
    /// batchId property.
    pub batch_id: Option<i64>,
    /// force property.
    pub force: Option<bool>,
    /// labelIds property.
    pub label_ids: Option<Vec<String>>,
    /// linkRequest property.
    pub link_request: Option<AccountsCustomBatchRequestEntryLinkRequest>,
    /// merchantId property.
    pub merchant_id: Option<String>,
    /// method property.
    pub method: Option<String>,
    /// overwrite property.
    pub overwrite: Option<bool>,
    /// view property.
    pub view: Option<String>,
}

/// `RenderProductIssuesResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct RenderProductIssuesResponse {
    /// alternateDisputeResolution property.
    pub alternate_dispute_resolution: Option<AlternateDisputeResolution>,
    /// issues property.
    pub issues: Option<Vec<ProductIssue>>,
}

/// `FreeListingsProgramStatus` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct FreeListingsProgramStatus {
    /// globalState property.
    pub global_state: Option<String>,
    /// regionStatuses property.
    pub region_statuses: Option<Vec<FreeListingsProgramStatusRegionStatus>>,
}

/// `GoogleAnalyticsLink` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleAnalyticsLink {
    /// attributionSettings property.
    pub attribution_settings: Option<AttributionSettings>,
    /// propertyId property.
    pub property_id: Option<String>,
    /// propertyName property.
    pub property_name: Option<String>,
}

/// `MethodQuota` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct MethodQuota {
    /// method property.
    pub method: Option<String>,
    /// quotaLimit property.
    pub quota_limit: Option<String>,
    /// quotaMinuteLimit property.
    pub quota_minute_limit: Option<String>,
    /// quotaUsage property.
    pub quota_usage: Option<String>,
}

/// `Warehouse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Warehouse {
    /// businessDayConfig property.
    pub business_day_config: Option<BusinessDayConfig>,
    /// cutoffTime property.
    pub cutoff_time: Option<WarehouseCutoffTime>,
    /// handlingDays property.
    pub handling_days: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// shippingAddress property.
    pub shipping_address: Option<Address>,
}

/// `PubsubNotificationSettings` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct PubsubNotificationSettings {
    /// cloudTopicName property.
    pub cloud_topic_name: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// registeredEvents property.
    pub registered_events: Option<Vec<String>>,
}

/// `DeliveryTime` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct DeliveryTime {
    /// cutoffTime property.
    pub cutoff_time: Option<CutoffTime>,
    /// handlingBusinessDayConfig property.
    pub handling_business_day_config: Option<BusinessDayConfig>,
    /// holidayCutoffs property.
    pub holiday_cutoffs: Option<Vec<HolidayCutoff>>,
    /// maxHandlingTimeInDays property.
    pub max_handling_time_in_days: Option<i64>,
    /// maxTransitTimeInDays property.
    pub max_transit_time_in_days: Option<i64>,
    /// minHandlingTimeInDays property.
    pub min_handling_time_in_days: Option<i64>,
    /// minTransitTimeInDays property.
    pub min_transit_time_in_days: Option<i64>,
    /// transitBusinessDayConfig property.
    pub transit_business_day_config: Option<BusinessDayConfig>,
    /// transitTimeTable property.
    pub transit_time_table: Option<TransitTable>,
    /// warehouseBasedDeliveryTimes property.
    pub warehouse_based_delivery_times: Option<Vec<WarehouseBasedDeliveryTime>>,
}

/// `AccountCustomerService` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AccountCustomerService {
    /// email property.
    pub email: Option<String>,
    /// phoneNumber property.
    pub phone_number: Option<String>,
    /// url property.
    pub url: Option<String>,
}

/// `PriceAmount` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct PriceAmount {
    /// currency property.
    pub currency: Option<String>,
    /// value property.
    pub value: Option<String>,
}

/// `ServiceStoreConfigCutoffConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ServiceStoreConfigCutoffConfig {
    /// localCutoffTime property.
    pub local_cutoff_time: Option<ServiceStoreConfigCutoffConfigLocalCutoffTime>,
    /// noDeliveryPostCutoff property.
    pub no_delivery_post_cutoff: Option<bool>,
    /// storeCloseOffsetHours property.
    pub store_close_offset_hours: Option<String>,
}

/// `AccountIdentifier` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AccountIdentifier {
    /// aggregatorId property.
    pub aggregator_id: Option<String>,
    /// merchantId property.
    pub merchant_id: Option<String>,
}

/// `PostalCodeGroup` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct PostalCodeGroup {
    /// country property.
    pub country: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// postalCodeRanges property.
    pub postal_code_ranges: Option<Vec<PostalCodeRange>>,
}

/// `PosCustomBatchRequestEntry` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct PosCustomBatchRequestEntry {
    /// batchId property.
    pub batch_id: Option<i64>,
    /// inventory property.
    pub inventory: Option<PosInventory>,
    /// merchantId property.
    pub merchant_id: Option<String>,
    /// method property.
    pub method: Option<String>,
    /// sale property.
    pub sale: Option<PosSale>,
    /// store property.
    pub store: Option<PosStore>,
    /// storeCode property.
    pub store_code: Option<String>,
    /// targetMerchantId property.
    pub target_merchant_id: Option<String>,
}

/// `CloudExportAdditionalProperties` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct CloudExportAdditionalProperties {
    /// boolValue property.
    pub bool_value: Option<bool>,
    /// floatValue property.
    pub float_value: Option<Vec<f64>>,
    /// intValue property.
    pub int_value: Option<Vec<String>>,
    /// maxValue property.
    pub max_value: Option<f64>,
    /// minValue property.
    pub min_value: Option<f64>,
    /// propertyName property.
    pub property_name: Option<String>,
    /// textValue property.
    pub text_value: Option<Vec<String>>,
    /// unitCode property.
    pub unit_code: Option<String>,
}

/// `CheckoutSettings` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct CheckoutSettings {
    /// effectiveEnrollmentState property.
    pub effective_enrollment_state: Option<String>,
    /// effectiveReviewState property.
    pub effective_review_state: Option<String>,
    /// effectiveUriSettings property.
    pub effective_uri_settings: Option<UrlSettings>,
    /// enrollmentState property.
    pub enrollment_state: Option<String>,
    /// merchantId property.
    pub merchant_id: Option<String>,
    /// reviewState property.
    pub review_state: Option<String>,
    /// uriSettings property.
    pub uri_settings: Option<UrlSettings>,
}

/// `Region` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Region {
    /// displayName property.
    pub display_name: Option<String>,
    /// geotargetArea property.
    pub geotarget_area: Option<RegionGeoTargetArea>,
    /// merchantId property.
    pub merchant_id: Option<String>,
    /// postalCodeArea property.
    pub postal_code_area: Option<RegionPostalCodeArea>,
    /// regionId property.
    pub region_id: Option<String>,
    /// regionalInventoryEligible property.
    pub regional_inventory_eligible: Option<bool>,
    /// shippingEligible property.
    pub shipping_eligible: Option<bool>,
}

/// `ConversionSource` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ConversionSource {
    /// conversionSourceId property.
    pub conversion_source_id: Option<String>,
    /// expireTime property.
    pub expire_time: Option<String>,
    /// googleAnalyticsLink property.
    pub google_analytics_link: Option<GoogleAnalyticsLink>,
    /// merchantCenterDestination property.
    pub merchant_center_destination: Option<MerchantCenterDestination>,
    /// state property.
    pub state: Option<String>,
}

/// `Distance` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Distance {
    /// unit property.
    pub unit: Option<String>,
    /// value property.
    pub value: Option<String>,
}

/// `DatafeedStatusExample` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct DatafeedStatusExample {
    /// itemId property.
    pub item_id: Option<String>,
    /// lineNumber property.
    pub line_number: Option<String>,
    /// value property.
    pub value: Option<String>,
}

/// `LocalinventoryCustomBatchResponseEntry` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct LocalinventoryCustomBatchResponseEntry {
    /// batchId property.
    pub batch_id: Option<i64>,
    /// errors property.
    pub errors: Option<Errors>,
    /// kind property.
    pub kind: Option<String>,
}

/// `AccountReturnCarrier` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AccountReturnCarrier {
    /// carrierAccountId property.
    pub carrier_account_id: Option<String>,
    /// carrierAccountName property.
    pub carrier_account_name: Option<String>,
    /// carrierAccountNumber property.
    pub carrier_account_number: Option<String>,
    /// carrierCode property.
    pub carrier_code: Option<String>,
}

/// `BuiltInUserInputAction` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct BuiltInUserInputAction {
    /// actionContext property.
    pub action_context: Option<String>,
    /// flows property.
    pub flows: Option<Vec<ActionFlow>>,
}

/// `AccounttaxCustomBatchRequestEntry` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AccounttaxCustomBatchRequestEntry {
    /// accountId property.
    pub account_id: Option<String>,
    /// accountTax property.
    pub account_tax: Option<AccountTax>,
    /// batchId property.
    pub batch_id: Option<i64>,
    /// merchantId property.
    pub merchant_id: Option<String>,
    /// method property.
    pub method: Option<String>,
}

/// `AccountstatusesCustomBatchResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AccountstatusesCustomBatchResponse {
    /// entries property.
    pub entries: Option<Vec<AccountstatusesCustomBatchResponseEntry>>,
    /// kind property.
    pub kind: Option<String>,
}

/// `LiasettingsSetInventoryVerificationContactResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct LiasettingsSetInventoryVerificationContactResponse {
    /// kind property.
    pub kind: Option<String>,
}

/// `RateGroup` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct RateGroup {
    /// applicableShippingLabels property.
    pub applicable_shipping_labels: Option<Vec<String>>,
    /// carrierRates property.
    pub carrier_rates: Option<Vec<CarrierRate>>,
    /// mainTable property.
    pub main_table: Option<Table>,
    /// name property.
    pub name: Option<String>,
    /// singleValue property.
    pub single_value: Option<Value>,
    /// subtables property.
    pub subtables: Option<Vec<Table>>,
}

/// `ShippingSettings` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ShippingSettings {
    /// accountId property.
    pub account_id: Option<String>,
    /// postalCodeGroups property.
    pub postal_code_groups: Option<Vec<PostalCodeGroup>>,
    /// services property.
    pub services: Option<Vec<Service>>,
    /// warehouses property.
    pub warehouses: Option<Vec<Warehouse>>,
}

/// `RegionPostalCodeAreaPostalCodeRange` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct RegionPostalCodeAreaPostalCodeRange {
    /// begin property.
    pub begin: Option<String>,
    /// end property.
    pub end: Option<String>,
}

/// `AccountAdsLink` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AccountAdsLink {
    /// adsId property.
    pub ads_id: Option<String>,
    /// status property.
    pub status: Option<String>,
}

/// `FreeListingsProgramStatusRegionStatus` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct FreeListingsProgramStatusRegionStatus {
    /// disapprovalDate property.
    pub disapproval_date: Option<String>,
    /// eligibilityStatus property.
    pub eligibility_status: Option<String>,
    /// onboardingIssues property.
    pub onboarding_issues: Option<Vec<String>>,
    /// regionCodes property.
    pub region_codes: Option<Vec<String>>,
    /// reviewEligibilityStatus property.
    pub review_eligibility_status: Option<String>,
    /// reviewIneligibilityReason property.
    pub review_ineligibility_reason: Option<String>,
    /// reviewIneligibilityReasonDescription property.
    pub review_ineligibility_reason_description: Option<String>,
    /// reviewIneligibilityReasonDetails property.
    pub review_ineligibility_reason_details:
        Option<FreeListingsProgramStatusReviewIneligibilityReasonDetails>,
    /// reviewIssues property.
    pub review_issues: Option<Vec<String>>,
}

/// `InputFieldCheckboxInput` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct InputFieldCheckboxInput {}

/// `ListConversionSourcesResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ListConversionSourcesResponse {
    /// conversionSources property.
    pub conversion_sources: Option<Vec<ConversionSource>>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
}

/// `ProductDimension` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ProductDimension {
    /// unit property.
    pub unit: Option<String>,
    /// value property.
    pub value: Option<f64>,
}

/// `ProductShipping` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ProductShipping {
    /// country property.
    pub country: Option<String>,
    /// locationGroupName property.
    pub location_group_name: Option<String>,
    /// locationId property.
    pub location_id: Option<String>,
    /// maxHandlingTime property.
    pub max_handling_time: Option<String>,
    /// maxTransitTime property.
    pub max_transit_time: Option<String>,
    /// minHandlingTime property.
    pub min_handling_time: Option<String>,
    /// minTransitTime property.
    pub min_transit_time: Option<String>,
    /// postalCode property.
    pub postal_code: Option<String>,
    /// price property.
    pub price: Option<Price>,
    /// region property.
    pub region: Option<String>,
    /// service property.
    pub service: Option<String>,
}

/// `LiasettingsCustomBatchRequestEntry` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct LiasettingsCustomBatchRequestEntry {
    /// accountId property.
    pub account_id: Option<String>,
    /// batchId property.
    pub batch_id: Option<i64>,
    /// contactEmail property.
    pub contact_email: Option<String>,
    /// contactName property.
    pub contact_name: Option<String>,
    /// country property.
    pub country: Option<String>,
    /// gmbEmail property.
    pub gmb_email: Option<String>,
    /// liaSettings property.
    pub lia_settings: Option<LiaSettings>,
    /// merchantId property.
    pub merchant_id: Option<String>,
    /// method property.
    pub method: Option<String>,
    /// omnichannelExperience property.
    pub omnichannel_experience: Option<LiaOmnichannelExperience>,
    /// posDataProviderId property.
    pub pos_data_provider_id: Option<String>,
    /// posExternalAccountId property.
    pub pos_external_account_id: Option<String>,
}

/// `RecommendationCreative` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct RecommendationCreative {
    /// type property.
    pub r#type: Option<String>,
    /// uri property.
    pub uri: Option<String>,
}

/// `InputField` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct InputField {
    /// checkboxInput property.
    pub checkbox_input: Option<InputFieldCheckboxInput>,
    /// choiceInput property.
    pub choice_input: Option<Box<InputFieldChoiceInput>>,
    /// id property.
    pub id: Option<String>,
    /// label property.
    pub label: Option<TextWithTooltip>,
    /// required property.
    pub required: Option<bool>,
    /// textInput property.
    pub text_input: Option<InputFieldTextInput>,
}

/// `ProductShippingDimension` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ProductShippingDimension {
    /// unit property.
    pub unit: Option<String>,
    /// value property.
    pub value: Option<f64>,
}

/// `ListCollectionsResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ListCollectionsResponse {
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// resources property.
    pub resources: Option<Vec<Collection>>,
}

/// `AccountCredentials` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AccountCredentials {
    /// accessToken property.
    pub access_token: Option<String>,
    /// expiresIn property.
    pub expires_in: Option<String>,
    /// purpose property.
    pub purpose: Option<String>,
}

/// `InputValueTextInputValue` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct InputValueTextInputValue {
    /// value property.
    pub value: Option<String>,
}

/// `DatafeedsFetchNowResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct DatafeedsFetchNowResponse {
    /// kind property.
    pub kind: Option<String>,
}

/// `ListCollectionStatusesResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ListCollectionStatusesResponse {
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// resources property.
    pub resources: Option<Vec<CollectionStatus>>,
}

/// `ProductViewItemIssueItemIssueType` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ProductViewItemIssueItemIssueType {
    /// canonicalAttribute property.
    pub canonical_attribute: Option<String>,
    /// code property.
    pub code: Option<String>,
}

/// `ProductProductDetail` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ProductProductDetail {
    /// attributeName property.
    pub attribute_name: Option<String>,
    /// attributeValue property.
    pub attribute_value: Option<String>,
    /// sectionName property.
    pub section_name: Option<String>,
}

/// `PosListResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct PosListResponse {
    /// kind property.
    pub kind: Option<String>,
    /// resources property.
    pub resources: Option<Vec<PosStore>>,
}

/// `ProductViewItemIssue` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ProductViewItemIssue {
    /// issueType property.
    pub issue_type: Option<ProductViewItemIssueItemIssueType>,
    /// resolution property.
    pub resolution: Option<String>,
    /// severity property.
    pub severity: Option<ProductViewItemIssueItemIssueSeverity>,
}

/// `LiasettingsRequestGmbAccessResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct LiasettingsRequestGmbAccessResponse {
    /// kind property.
    pub kind: Option<String>,
}

/// `ListReturnPolicyOnlineResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ListReturnPolicyOnlineResponse {
    /// returnPolicies property.
    pub return_policies: Option<Vec<ReturnPolicyOnline>>,
}

/// `AccountsUpdateLabelsRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AccountsUpdateLabelsRequest {
    /// labelIds property.
    pub label_ids: Option<Vec<String>>,
}

/// `LiasettingsCustomBatchResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct LiasettingsCustomBatchResponse {
    /// entries property.
    pub entries: Option<Vec<LiasettingsCustomBatchResponseEntry>>,
    /// kind property.
    pub kind: Option<String>,
}

/// `RegionGeoTargetArea` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct RegionGeoTargetArea {
    /// geotargetCriteriaIds property.
    pub geotarget_criteria_ids: Option<Vec<String>>,
}

/// `CollectionStatus` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct CollectionStatus {
    /// collectionLevelIssuses property.
    pub collection_level_issuses: Option<Vec<CollectionStatusItemLevelIssue>>,
    /// creationDate property.
    pub creation_date: Option<String>,
    /// destinationStatuses property.
    pub destination_statuses: Option<Vec<CollectionStatusDestinationStatus>>,
    /// id property.
    pub id: Option<String>,
    /// lastUpdateDate property.
    pub last_update_date: Option<String>,
}

/// `ShippingsettingsGetSupportedCarriersResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ShippingsettingsGetSupportedCarriersResponse {
    /// carriers property.
    pub carriers: Option<Vec<CarriersCarrier>>,
    /// kind property.
    pub kind: Option<String>,
}

/// `CarriersCarrier` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct CarriersCarrier {
    /// country property.
    pub country: Option<String>,
    /// eddServices property.
    pub edd_services: Option<Vec<String>>,
    /// name property.
    pub name: Option<String>,
    /// services property.
    pub services: Option<Vec<String>>,
}

/// `ActionInput` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ActionInput {
    /// actionFlowId property.
    pub action_flow_id: Option<String>,
    /// inputValues property.
    pub input_values: Option<Vec<InputValue>>,
}

/// `ProductstatusesCustomBatchResponseEntry` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ProductstatusesCustomBatchResponseEntry {
    /// batchId property.
    pub batch_id: Option<i64>,
    /// errors property.
    pub errors: Option<Errors>,
    /// kind property.
    pub kind: Option<String>,
    /// productStatus property.
    pub product_status: Option<ProductStatus>,
}

/// `TriggerActionResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct TriggerActionResponse {
    /// message property.
    pub message: Option<String>,
}

/// `AccountStatusAccountLevelIssue` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AccountStatusAccountLevelIssue {
    /// country property.
    pub country: Option<String>,
    /// destination property.
    pub destination: Option<String>,
    /// detail property.
    pub detail: Option<String>,
    /// documentation property.
    pub documentation: Option<String>,
    /// id property.
    pub id: Option<String>,
    /// severity property.
    pub severity: Option<String>,
    /// title property.
    pub title: Option<String>,
}

/// `SearchResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SearchResponse {
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// results property.
    pub results: Option<Vec<ReportRow>>,
}

/// `RequestReviewFreeListingsRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct RequestReviewFreeListingsRequest {
    /// regionCode property.
    pub region_code: Option<String>,
}

/// `TopicTrends` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct TopicTrends {
    /// customerCountryCode property.
    pub customer_country_code: Option<String>,
    /// date property.
    pub date: Option<Date>,
    /// last120DaysSearchInterest property.
    pub last120_days_search_interest: Option<f64>,
    /// last30DaysSearchInterest property.
    pub last30_days_search_interest: Option<f64>,
    /// last7DaysSearchInterest property.
    pub last7_days_search_interest: Option<f64>,
    /// last90DaysSearchInterest property.
    pub last90_days_search_interest: Option<f64>,
    /// next7DaysSearchInterest property.
    pub next7_days_search_interest: Option<f64>,
    /// searchInterest property.
    pub search_interest: Option<f64>,
    /// topic property.
    pub topic: Option<String>,
}

/// `ShippingsettingsGetSupportedHolidaysResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ShippingsettingsGetSupportedHolidaysResponse {
    /// holidays property.
    pub holidays: Option<Vec<HolidaysHoliday>>,
    /// kind property.
    pub kind: Option<String>,
}

/// `AccountTax` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AccountTax {
    /// accountId property.
    pub account_id: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// rules property.
    pub rules: Option<Vec<AccountTaxTaxRule>>,
}

/// `ShippingsettingsListResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ShippingsettingsListResponse {
    /// kind property.
    pub kind: Option<String>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// resources property.
    pub resources: Option<Vec<ShippingSettings>>,
}

/// `RequestReviewShoppingAdsRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct RequestReviewShoppingAdsRequest {
    /// regionCode property.
    pub region_code: Option<String>,
}

/// `LiaAboutPageSettings` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct LiaAboutPageSettings {
    /// status property.
    pub status: Option<String>,
    /// url property.
    pub url: Option<String>,
}

/// `AccountstatusesListResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AccountstatusesListResponse {
    /// kind property.
    pub kind: Option<String>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// resources property.
    pub resources: Option<Vec<AccountStatus>>,
}

/// `ShoppingAdsProgramStatus` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ShoppingAdsProgramStatus {
    /// globalState property.
    pub global_state: Option<String>,
    /// regionStatuses property.
    pub region_statuses: Option<Vec<ShoppingAdsProgramStatusRegionStatus>>,
}

/// `RenderAccountIssuesResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct RenderAccountIssuesResponse {
    /// alternateDisputeResolution property.
    pub alternate_dispute_resolution: Option<AlternateDisputeResolution>,
    /// issues property.
    pub issues: Option<Vec<AccountIssue>>,
}

/// `PosSaleResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct PosSaleResponse {
    /// contentLanguage property.
    pub content_language: Option<String>,
    /// gtin property.
    pub gtin: Option<String>,
    /// itemId property.
    pub item_id: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// price property.
    pub price: Option<Price>,
    /// quantity property.
    pub quantity: Option<String>,
    /// saleId property.
    pub sale_id: Option<String>,
    /// storeCode property.
    pub store_code: Option<String>,
    /// targetCountry property.
    pub target_country: Option<String>,
    /// timestamp property.
    pub timestamp: Option<String>,
}

/// `ProductDeliveryTime` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ProductDeliveryTime {
    /// areaDeliveryTimes property.
    pub area_delivery_times: Option<Vec<ProductDeliveryTimeAreaDeliveryTime>>,
    /// productId property.
    pub product_id: Option<ProductId>,
}

/// `AccountStatus` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AccountStatus {
    /// accountId property.
    pub account_id: Option<String>,
    /// accountLevelIssues property.
    pub account_level_issues: Option<Vec<AccountStatusAccountLevelIssue>>,
    /// accountManagement property.
    pub account_management: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// products property.
    pub products: Option<Vec<AccountStatusProducts>>,
    /// websiteClaimed property.
    pub website_claimed: Option<bool>,
}

/// `ProductsCustomBatchResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ProductsCustomBatchResponse {
    /// entries property.
    pub entries: Option<Vec<ProductsCustomBatchResponseEntry>>,
    /// kind property.
    pub kind: Option<String>,
}

/// `AccountstatusesCustomBatchResponseEntry` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AccountstatusesCustomBatchResponseEntry {
    /// accountStatus property.
    pub account_status: Option<AccountStatus>,
    /// batchId property.
    pub batch_id: Option<i64>,
    /// errors property.
    pub errors: Option<Errors>,
}

/// `ProductStructuredTitle` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ProductStructuredTitle {
    /// content property.
    pub content: Option<String>,
    /// digitalSourceType property.
    pub digital_source_type: Option<String>,
}

/// `InputValueCheckboxInputValue` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct InputValueCheckboxInputValue {
    /// value property.
    pub value: Option<bool>,
}

/// `TimePeriod` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct TimePeriod {
    /// endTime property.
    pub end_time: Option<String>,
    /// startTime property.
    pub start_time: Option<String>,
}

/// `Recommendation` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Recommendation {
    /// additionalCallToAction property.
    pub additional_call_to_action: Option<Vec<RecommendationCallToAction>>,
    /// additionalDescriptions property.
    pub additional_descriptions: Option<Vec<RecommendationDescription>>,
    /// creative property.
    pub creative: Option<Vec<RecommendationCreative>>,
    /// defaultCallToAction property.
    pub default_call_to_action: Option<RecommendationCallToAction>,
    /// defaultDescription property.
    pub default_description: Option<String>,
    /// numericalImpact property.
    pub numerical_impact: Option<i64>,
    /// paid property.
    pub paid: Option<bool>,
    /// recommendationName property.
    pub recommendation_name: Option<String>,
    /// subType property.
    pub sub_type: Option<String>,
    /// title property.
    pub title: Option<String>,
    /// type property.
    pub r#type: Option<String>,
}

/// `AccountStatusProducts` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AccountStatusProducts {
    /// channel property.
    pub channel: Option<String>,
    /// country property.
    pub country: Option<String>,
    /// destination property.
    pub destination: Option<String>,
    /// itemLevelIssues property.
    pub item_level_issues: Option<Vec<AccountStatusItemLevelIssue>>,
    /// statistics property.
    pub statistics: Option<AccountStatusStatistics>,
}

/// `DatafeedstatusesCustomBatchRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct DatafeedstatusesCustomBatchRequest {
    /// entries property.
    pub entries: Option<Vec<DatafeedstatusesCustomBatchRequestEntry>>,
}

/// `ProductId` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ProductId {
    /// productId property.
    pub product_id: Option<String>,
}

/// `ProductCluster` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ProductCluster {
    /// brand property.
    pub brand: Option<String>,
    /// brandInventoryStatus property.
    pub brand_inventory_status: Option<String>,
    /// categoryL1 property.
    pub category_l1: Option<String>,
    /// categoryL2 property.
    pub category_l2: Option<String>,
    /// categoryL3 property.
    pub category_l3: Option<String>,
    /// categoryL4 property.
    pub category_l4: Option<String>,
    /// categoryL5 property.
    pub category_l5: Option<String>,
    /// inventoryStatus property.
    pub inventory_status: Option<String>,
    /// title property.
    pub title: Option<String>,
    /// variantGtins property.
    pub variant_gtins: Option<Vec<String>>,
}

/// `AccountsCustomBatchRequestEntryLinkRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AccountsCustomBatchRequestEntryLinkRequest {
    /// action property.
    pub action: Option<String>,
    /// linkType property.
    pub link_type: Option<String>,
    /// linkedAccountId property.
    pub linked_account_id: Option<String>,
    /// services property.
    pub services: Option<Vec<String>>,
}

/// `ProductView` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ProductView {
    /// aggregatedDestinationStatus property.
    pub aggregated_destination_status: Option<String>,
    /// availability property.
    pub availability: Option<String>,
    /// brand property.
    pub brand: Option<String>,
    /// categoryL1 property.
    pub category_l1: Option<String>,
    /// categoryL2 property.
    pub category_l2: Option<String>,
    /// categoryL3 property.
    pub category_l3: Option<String>,
    /// categoryL4 property.
    pub category_l4: Option<String>,
    /// categoryL5 property.
    pub category_l5: Option<String>,
    /// channel property.
    pub channel: Option<String>,
    /// clickPotential property.
    pub click_potential: Option<String>,
    /// clickPotentialRank property.
    pub click_potential_rank: Option<String>,
    /// condition property.
    pub condition: Option<String>,
    /// creationTime property.
    pub creation_time: Option<String>,
    /// currencyCode property.
    pub currency_code: Option<String>,
    /// expirationDate property.
    pub expiration_date: Option<Date>,
    /// gtin property.
    pub gtin: Option<Vec<String>>,
    /// id property.
    pub id: Option<String>,
    /// itemGroupId property.
    pub item_group_id: Option<String>,
    /// itemIssues property.
    pub item_issues: Option<Vec<ProductViewItemIssue>>,
    /// languageCode property.
    pub language_code: Option<String>,
    /// offerId property.
    pub offer_id: Option<String>,
    /// priceMicros property.
    pub price_micros: Option<String>,
    /// productTypeL1 property.
    pub product_type_l1: Option<String>,
    /// productTypeL2 property.
    pub product_type_l2: Option<String>,
    /// productTypeL3 property.
    pub product_type_l3: Option<String>,
    /// productTypeL4 property.
    pub product_type_l4: Option<String>,
    /// productTypeL5 property.
    pub product_type_l5: Option<String>,
    /// shippingLabel property.
    pub shipping_label: Option<String>,
    /// title property.
    pub title: Option<String>,
}

/// `ProductStatusDestinationStatus` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ProductStatusDestinationStatus {
    /// approvedCountries property.
    pub approved_countries: Option<Vec<String>>,
    /// channel property.
    pub channel: Option<String>,
    /// destination property.
    pub destination: Option<String>,
    /// disapprovedCountries property.
    pub disapproved_countries: Option<Vec<String>>,
    /// pendingCountries property.
    pub pending_countries: Option<Vec<String>>,
    /// status property.
    pub status: Option<String>,
}

/// `AccountsCustomBatchResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AccountsCustomBatchResponse {
    /// entries property.
    pub entries: Option<Vec<AccountsCustomBatchResponseEntry>>,
    /// kind property.
    pub kind: Option<String>,
}

/// `Brand` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Brand {
    /// name property.
    pub name: Option<String>,
}

/// `ListAccountLabelsResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ListAccountLabelsResponse {
    /// accountLabels property.
    pub account_labels: Option<Vec<AccountLabel>>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
}

/// `Weight` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Weight {
    /// unit property.
    pub unit: Option<String>,
    /// value property.
    pub value: Option<String>,
}

/// `RecommendationDescription` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct RecommendationDescription {
    /// text property.
    pub text: Option<String>,
    /// type property.
    pub r#type: Option<String>,
}

/// `ReturnPolicyOnline` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ReturnPolicyOnline {
    /// countries property.
    pub countries: Option<Vec<String>>,
    /// itemConditions property.
    pub item_conditions: Option<Vec<String>>,
    /// label property.
    pub label: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// policy property.
    pub policy: Option<ReturnPolicyOnlinePolicy>,
    /// restockingFee property.
    pub restocking_fee: Option<ReturnPolicyOnlineRestockingFee>,
    /// returnMethods property.
    pub return_methods: Option<Vec<String>>,
    /// returnPolicyId property.
    pub return_policy_id: Option<String>,
    /// returnPolicyUri property.
    pub return_policy_uri: Option<String>,
    /// returnReasonCategoryInfo property.
    pub return_reason_category_info: Option<Vec<ReturnPolicyOnlineReturnReasonCategoryInfo>>,
}

/// `AccountBusinessIdentity` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AccountBusinessIdentity {
    /// blackOwned property.
    pub black_owned: Option<AccountIdentityType>,
    /// includeForPromotions property.
    pub include_for_promotions: Option<bool>,
    /// latinoOwned property.
    pub latino_owned: Option<AccountIdentityType>,
    /// smallBusiness property.
    pub small_business: Option<AccountIdentityType>,
    /// veteranOwned property.
    pub veteran_owned: Option<AccountIdentityType>,
    /// womenOwned property.
    pub women_owned: Option<AccountIdentityType>,
}

/// `ListMethodQuotasResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ListMethodQuotasResponse {
    /// methodQuotas property.
    pub method_quotas: Option<Vec<MethodQuota>>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
}

/// `LiasettingsSetPosDataProviderResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct LiasettingsSetPosDataProviderResponse {
    /// kind property.
    pub kind: Option<String>,
}

/// `LiasettingsCustomBatchRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct LiasettingsCustomBatchRequest {
    /// entries property.
    pub entries: Option<Vec<LiasettingsCustomBatchRequestEntry>>,
}

/// `DatafeedsListResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct DatafeedsListResponse {
    /// kind property.
    pub kind: Option<String>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// resources property.
    pub resources: Option<Vec<Datafeed>>,
}

/// `ShippingsettingsCustomBatchResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ShippingsettingsCustomBatchResponse {
    /// entries property.
    pub entries: Option<Vec<ShippingsettingsCustomBatchResponseEntry>>,
    /// kind property.
    pub kind: Option<String>,
}

/// `LocalinventoryCustomBatchResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct LocalinventoryCustomBatchResponse {
    /// entries property.
    pub entries: Option<Vec<LocalinventoryCustomBatchResponseEntry>>,
    /// kind property.
    pub kind: Option<String>,
}

/// `AttributionSettingsConversionType` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AttributionSettingsConversionType {
    /// includeInReporting property.
    pub include_in_reporting: Option<bool>,
    /// name property.
    pub name: Option<String>,
}

/// `UrlSettings` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct UrlSettings {
    /// cartUriTemplate property.
    pub cart_uri_template: Option<String>,
    /// checkoutUriTemplate property.
    pub checkout_uri_template: Option<String>,
}

/// `LiaCountrySettings` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct LiaCountrySettings {
    /// about property.
    pub about: Option<LiaAboutPageSettings>,
    /// country property.
    pub country: Option<String>,
    /// hostedLocalStorefrontActive property.
    pub hosted_local_storefront_active: Option<bool>,
    /// inventory property.
    pub inventory: Option<LiaInventorySettings>,
    /// omnichannelExperience property.
    pub omnichannel_experience: Option<LiaOmnichannelExperience>,
    /// onDisplayToOrder property.
    pub on_display_to_order: Option<LiaOnDisplayToOrderSettings>,
    /// posDataProvider property.
    pub pos_data_provider: Option<LiaPosDataProvider>,
    /// storePickupActive property.
    pub store_pickup_active: Option<bool>,
}

/// `TransitTable` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct TransitTable {
    /// postalCodeGroupNames property.
    pub postal_code_group_names: Option<Vec<String>>,
    /// rows property.
    pub rows: Option<Vec<TransitTableTransitTimeRow>>,
    /// transitTimeLabels property.
    pub transit_time_labels: Option<Vec<String>>,
}

/// `RegionalinventoryCustomBatchResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct RegionalinventoryCustomBatchResponse {
    /// entries property.
    pub entries: Option<Vec<RegionalinventoryCustomBatchResponseEntry>>,
    /// kind property.
    pub kind: Option<String>,
}

/// `ProductUnitPricingMeasure` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ProductUnitPricingMeasure {
    /// unit property.
    pub unit: Option<String>,
    /// value property.
    pub value: Option<f64>,
}

/// `AccountBusinessInformation` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AccountBusinessInformation {
    /// address property.
    pub address: Option<AccountAddress>,
    /// customerService property.
    pub customer_service: Option<AccountCustomerService>,
    /// koreanBusinessRegistrationNumber property.
    pub korean_business_registration_number: Option<String>,
    /// phoneNumber property.
    pub phone_number: Option<String>,
    /// phoneVerificationStatus property.
    pub phone_verification_status: Option<String>,
}

/// `Css` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Css {
    /// cssDomainId property.
    pub css_domain_id: Option<String>,
    /// cssGroupId property.
    pub css_group_id: Option<String>,
    /// displayName property.
    pub display_name: Option<String>,
    /// fullName property.
    pub full_name: Option<String>,
    /// homepageUri property.
    pub homepage_uri: Option<String>,
    /// labelIds property.
    pub label_ids: Option<Vec<String>>,
}

/// `SearchRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SearchRequest {
    /// pageSize property.
    pub page_size: Option<i64>,
    /// pageToken property.
    pub page_token: Option<String>,
    /// query property.
    pub query: Option<String>,
}

/// `TimeZone` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct TimeZone {
    /// id property.
    pub id: Option<String>,
    /// version property.
    pub version: Option<String>,
}

/// `ReturnPolicyOnlineReturnReasonCategoryInfo` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ReturnPolicyOnlineReturnReasonCategoryInfo {
    /// returnLabelSource property.
    pub return_label_source: Option<String>,
    /// returnReasonCategory property.
    pub return_reason_category: Option<String>,
    /// returnShippingFee property.
    pub return_shipping_fee: Option<ReturnPolicyOnlineReturnShippingFee>,
}

/// `OrderTrackingSignal` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct OrderTrackingSignal {
    /// customerShippingFee property.
    pub customer_shipping_fee: Option<PriceAmount>,
    /// deliveryPostalCode property.
    pub delivery_postal_code: Option<String>,
    /// deliveryRegionCode property.
    pub delivery_region_code: Option<String>,
    /// lineItems property.
    pub line_items: Option<Vec<OrderTrackingSignalLineItemDetails>>,
    /// merchantId property.
    pub merchant_id: Option<String>,
    /// orderCreatedTime property.
    pub order_created_time: Option<DateTime>,
    /// orderId property.
    pub order_id: Option<String>,
    /// orderTrackingSignalId property.
    pub order_tracking_signal_id: Option<String>,
    /// shipmentLineItemMapping property.
    pub shipment_line_item_mapping: Option<Vec<OrderTrackingSignalShipmentLineItemMapping>>,
    /// shippingInfo property.
    pub shipping_info: Option<Vec<OrderTrackingSignalShippingInfo>>,
}

/// `LoyaltyProgram` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct LoyaltyProgram {
    /// cashbackForFutureUse property.
    pub cashback_for_future_use: Option<Price>,
    /// loyaltyPoints property.
    pub loyalty_points: Option<String>,
    /// memberPriceEffectiveDate property.
    pub member_price_effective_date: Option<String>,
    /// price property.
    pub price: Option<Price>,
    /// programLabel property.
    pub program_label: Option<String>,
    /// shippingLabel property.
    pub shipping_label: Option<String>,
    /// tierLabel property.
    pub tier_label: Option<String>,
}

/// `UndeleteConversionSourceRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct UndeleteConversionSourceRequest {}

/// `DatafeedstatusesListResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct DatafeedstatusesListResponse {
    /// kind property.
    pub kind: Option<String>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// resources property.
    pub resources: Option<Vec<DatafeedStatus>>,
}

/// `ProductStatusItemLevelIssue` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ProductStatusItemLevelIssue {
    /// applicableCountries property.
    pub applicable_countries: Option<Vec<String>>,
    /// attributeName property.
    pub attribute_name: Option<String>,
    /// code property.
    pub code: Option<String>,
    /// description property.
    pub description: Option<String>,
    /// destination property.
    pub destination: Option<String>,
    /// detail property.
    pub detail: Option<String>,
    /// documentation property.
    pub documentation: Option<String>,
    /// resolution property.
    pub resolution: Option<String>,
    /// servability property.
    pub servability: Option<String>,
}

/// `ReturnPolicyOnlineRestockingFee` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ReturnPolicyOnlineRestockingFee {
    /// fixedFee property.
    pub fixed_fee: Option<PriceAmount>,
    /// microPercent property.
    pub micro_percent: Option<i64>,
}

/// `AccountsLinkRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AccountsLinkRequest {
    /// action property.
    pub action: Option<String>,
    /// eCommercePlatformLinkInfo property.
    pub e_commerce_platform_link_info: Option<ECommercePlatformLinkInfo>,
    /// linkType property.
    pub link_type: Option<String>,
    /// linkedAccountId property.
    pub linked_account_id: Option<String>,
    /// paymentServiceProviderLinkInfo property.
    pub payment_service_provider_link_info: Option<PaymentServiceProviderLinkInfo>,
    /// services property.
    pub services: Option<Vec<String>>,
}

/// `AccountsAuthInfoResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AccountsAuthInfoResponse {
    /// accountIdentifiers property.
    pub account_identifiers: Option<Vec<AccountIdentifier>>,
    /// kind property.
    pub kind: Option<String>,
}

/// `LocationIdSet` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct LocationIdSet {
    /// locationIds property.
    pub location_ids: Option<Vec<String>>,
}

/// `AccountImageImprovements` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AccountImageImprovements {
    /// accountImageImprovementsSettings property.
    pub account_image_improvements_settings: Option<AccountImageImprovementsSettings>,
    /// effectiveAllowAutomaticImageImprovements property.
    pub effective_allow_automatic_image_improvements: Option<bool>,
}

/// `AccountItemUpdatesSettings` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AccountItemUpdatesSettings {
    /// allowAvailabilityUpdates property.
    pub allow_availability_updates: Option<bool>,
    /// allowConditionUpdates property.
    pub allow_condition_updates: Option<bool>,
    /// allowPriceUpdates property.
    pub allow_price_updates: Option<bool>,
    /// allowStrictAvailabilityUpdates property.
    pub allow_strict_availability_updates: Option<bool>,
}

/// `AccountStatusStatistics` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AccountStatusStatistics {
    /// active property.
    pub active: Option<String>,
    /// disapproved property.
    pub disapproved: Option<String>,
    /// expiring property.
    pub expiring: Option<String>,
    /// pending property.
    pub pending: Option<String>,
}

/// `PickupCarrierService` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct PickupCarrierService {
    /// carrierName property.
    pub carrier_name: Option<String>,
    /// serviceName property.
    pub service_name: Option<String>,
}

/// `AccountsCustomBatchRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AccountsCustomBatchRequest {
    /// entries property.
    pub entries: Option<Vec<AccountsCustomBatchRequestEntry>>,
}

/// `AccountstatusesCustomBatchRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AccountstatusesCustomBatchRequest {
    /// entries property.
    pub entries: Option<Vec<AccountstatusesCustomBatchRequestEntry>>,
}

/// `AccountStatusItemLevelIssue` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AccountStatusItemLevelIssue {
    /// attributeName property.
    pub attribute_name: Option<String>,
    /// code property.
    pub code: Option<String>,
    /// description property.
    pub description: Option<String>,
    /// detail property.
    pub detail: Option<String>,
    /// documentation property.
    pub documentation: Option<String>,
    /// numItems property.
    pub num_items: Option<String>,
    /// resolution property.
    pub resolution: Option<String>,
    /// servability property.
    pub servability: Option<String>,
}

/// `Value` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Value {
    /// carrierRateName property.
    pub carrier_rate_name: Option<String>,
    /// flatRate property.
    pub flat_rate: Option<Price>,
    /// noShipping property.
    pub no_shipping: Option<bool>,
    /// pricePercentage property.
    pub price_percentage: Option<String>,
    /// subtableName property.
    pub subtable_name: Option<String>,
}

/// `AccountAddress` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AccountAddress {
    /// country property.
    pub country: Option<String>,
    /// locality property.
    pub locality: Option<String>,
    /// postalCode property.
    pub postal_code: Option<String>,
    /// region property.
    pub region: Option<String>,
    /// streetAddress property.
    pub street_address: Option<String>,
}

/// `PosSale` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct PosSale {
    /// contentLanguage property.
    pub content_language: Option<String>,
    /// gtin property.
    pub gtin: Option<String>,
    /// itemId property.
    pub item_id: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// price property.
    pub price: Option<Price>,
    /// quantity property.
    pub quantity: Option<String>,
    /// saleId property.
    pub sale_id: Option<String>,
    /// storeCode property.
    pub store_code: Option<String>,
    /// targetCountry property.
    pub target_country: Option<String>,
    /// timestamp property.
    pub timestamp: Option<String>,
}

/// `ProductViewItemIssueItemIssueSeverity` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ProductViewItemIssueItemIssueSeverity {
    /// aggregatedSeverity property.
    pub aggregated_severity: Option<String>,
    /// severityPerDestination property.
    pub severity_per_destination: Option<Vec<ProductViewItemIssueIssueSeverityPerDestination>>,
}

/// `LocalinventoryCustomBatchRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct LocalinventoryCustomBatchRequest {
    /// entries property.
    pub entries: Option<Vec<LocalinventoryCustomBatchRequestEntry>>,
}

/// `AccountsUpdateLabelsResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AccountsUpdateLabelsResponse {
    /// kind property.
    pub kind: Option<String>,
}

/// `Callout` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Callout {
    /// fullMessage property.
    pub full_message: Option<TextWithTooltip>,
    /// styleHint property.
    pub style_hint: Option<String>,
}

/// `ListAccountReturnCarrierResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ListAccountReturnCarrierResponse {
    /// accountReturnCarriers property.
    pub account_return_carriers: Option<Vec<AccountReturnCarrier>>,
}

/// `TransitTableTransitTimeRowTransitTimeValue` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct TransitTableTransitTimeRowTransitTimeValue {
    /// maxTransitTimeInDays property.
    pub max_transit_time_in_days: Option<i64>,
    /// minTransitTimeInDays property.
    pub min_transit_time_in_days: Option<i64>,
}

/// `AccountYouTubeChannelLink` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AccountYouTubeChannelLink {
    /// channelId property.
    pub channel_id: Option<String>,
    /// status property.
    pub status: Option<String>,
}

/// `AccountShippingImprovements` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AccountShippingImprovements {
    /// allowShippingImprovements property.
    pub allow_shipping_improvements: Option<bool>,
}

/// `ListPromotionResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ListPromotionResponse {
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// promotions property.
    pub promotions: Option<Vec<Promotion>>,
}

/// `ProductTax` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ProductTax {
    /// country property.
    pub country: Option<String>,
    /// locationId property.
    pub location_id: Option<String>,
    /// postalCode property.
    pub postal_code: Option<String>,
    /// rate property.
    pub rate: Option<f64>,
    /// region property.
    pub region: Option<String>,
    /// taxShip property.
    pub tax_ship: Option<bool>,
}

/// `ProductSustainabilityIncentive` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ProductSustainabilityIncentive {
    /// amount property.
    pub amount: Option<Price>,
    /// percentage property.
    pub percentage: Option<f64>,
    /// type property.
    pub r#type: Option<String>,
}

/// `AccountstatusesCustomBatchRequestEntry` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AccountstatusesCustomBatchRequestEntry {
    /// accountId property.
    pub account_id: Option<String>,
    /// batchId property.
    pub batch_id: Option<i64>,
    /// destinations property.
    pub destinations: Option<Vec<String>>,
    /// merchantId property.
    pub merchant_id: Option<String>,
    /// method property.
    pub method: Option<String>,
}

/// `PosInventory` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct PosInventory {
    /// contentLanguage property.
    pub content_language: Option<String>,
    /// gtin property.
    pub gtin: Option<String>,
    /// itemId property.
    pub item_id: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// pickupMethod property.
    pub pickup_method: Option<String>,
    /// pickupSla property.
    pub pickup_sla: Option<String>,
    /// price property.
    pub price: Option<Price>,
    /// quantity property.
    pub quantity: Option<String>,
    /// storeCode property.
    pub store_code: Option<String>,
    /// targetCountry property.
    pub target_country: Option<String>,
    /// timestamp property.
    pub timestamp: Option<String>,
}

/// `ReturnPolicyOnlineReturnShippingFee` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ReturnPolicyOnlineReturnShippingFee {
    /// fixedFee property.
    pub fixed_fee: Option<PriceAmount>,
    /// type property.
    pub r#type: Option<String>,
}

/// `Segments` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Segments {
    /// brand property.
    pub brand: Option<String>,
    /// categoryL1 property.
    pub category_l1: Option<String>,
    /// categoryL2 property.
    pub category_l2: Option<String>,
    /// categoryL3 property.
    pub category_l3: Option<String>,
    /// categoryL4 property.
    pub category_l4: Option<String>,
    /// categoryL5 property.
    pub category_l5: Option<String>,
    /// currencyCode property.
    pub currency_code: Option<String>,
    /// customLabel0 property.
    pub custom_label0: Option<String>,
    /// customLabel1 property.
    pub custom_label1: Option<String>,
    /// customLabel2 property.
    pub custom_label2: Option<String>,
    /// customLabel3 property.
    pub custom_label3: Option<String>,
    /// customLabel4 property.
    pub custom_label4: Option<String>,
    /// customerCountryCode property.
    pub customer_country_code: Option<String>,
    /// date property.
    pub date: Option<Date>,
    /// offerId property.
    pub offer_id: Option<String>,
    /// productTypeL1 property.
    pub product_type_l1: Option<String>,
    /// productTypeL2 property.
    pub product_type_l2: Option<String>,
    /// productTypeL3 property.
    pub product_type_l3: Option<String>,
    /// productTypeL4 property.
    pub product_type_l4: Option<String>,
    /// productTypeL5 property.
    pub product_type_l5: Option<String>,
    /// program property.
    pub program: Option<String>,
    /// title property.
    pub title: Option<String>,
    /// week property.
    pub week: Option<Date>,
}

/// `ProductstatusesListResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ProductstatusesListResponse {
    /// kind property.
    pub kind: Option<String>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// resources property.
    pub resources: Option<Vec<ProductStatus>>,
}

/// `AccountGoogleMyBusinessLink` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AccountGoogleMyBusinessLink {
    /// gmbAccountId property.
    pub gmb_account_id: Option<String>,
    /// gmbEmail property.
    pub gmb_email: Option<String>,
    /// status property.
    pub status: Option<String>,
}

/// `AccountsListResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AccountsListResponse {
    /// kind property.
    pub kind: Option<String>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// resources property.
    pub resources: Option<Vec<Account>>,
}

/// `PostalCodeRange` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct PostalCodeRange {
    /// postalCodeRangeBegin property.
    pub postal_code_range_begin: Option<String>,
    /// postalCodeRangeEnd property.
    pub postal_code_range_end: Option<String>,
}

/// `ProductstatusesCustomBatchResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ProductstatusesCustomBatchResponse {
    /// entries property.
    pub entries: Option<Vec<ProductstatusesCustomBatchResponseEntry>>,
    /// kind property.
    pub kind: Option<String>,
}

/// `DatafeedstatusesCustomBatchResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct DatafeedstatusesCustomBatchResponse {
    /// entries property.
    pub entries: Option<Vec<DatafeedstatusesCustomBatchResponseEntry>>,
    /// kind property.
    pub kind: Option<String>,
}

/// `PriceInsights` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct PriceInsights {
    /// effectiveness property.
    pub effectiveness: Option<String>,
    /// predictedClicksChangeFraction property.
    pub predicted_clicks_change_fraction: Option<f64>,
    /// predictedConversionsChangeFraction property.
    pub predicted_conversions_change_fraction: Option<f64>,
    /// predictedGrossProfitChangeFraction property.
    pub predicted_gross_profit_change_fraction: Option<f64>,
    /// predictedImpressionsChangeFraction property.
    pub predicted_impressions_change_fraction: Option<f64>,
    /// predictedMonthlyGrossProfitChangeCurrencyCode property.
    pub predicted_monthly_gross_profit_change_currency_code: Option<String>,
    /// predictedMonthlyGrossProfitChangeMicros property.
    pub predicted_monthly_gross_profit_change_micros: Option<String>,
    /// suggestedPriceCurrencyCode property.
    pub suggested_price_currency_code: Option<String>,
    /// suggestedPriceMicros property.
    pub suggested_price_micros: Option<String>,
}

/// `GmbAccounts` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GmbAccounts {
    /// accountId property.
    pub account_id: Option<String>,
    /// gmbAccounts property.
    pub gmb_accounts: Option<Vec<GmbAccountsGmbAccount>>,
}

/// `InputFieldChoiceInput` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct InputFieldChoiceInput {
    /// options property.
    pub options: Option<Vec<Box<InputFieldChoiceInputChoiceInputOption>>>,
}

/// `ProductStructuredDescription` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ProductStructuredDescription {
    /// content property.
    pub content: Option<String>,
    /// digitalSourceType property.
    pub digital_source_type: Option<String>,
}

/// `OrderTrackingSignalLineItemDetails` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct OrderTrackingSignalLineItemDetails {
    /// brand property.
    pub brand: Option<String>,
    /// gtin property.
    pub gtin: Option<String>,
    /// lineItemId property.
    pub line_item_id: Option<String>,
    /// mpn property.
    pub mpn: Option<String>,
    /// productDescription property.
    pub product_description: Option<String>,
    /// productId property.
    pub product_id: Option<String>,
    /// productTitle property.
    pub product_title: Option<String>,
    /// quantity property.
    pub quantity: Option<String>,
    /// sku property.
    pub sku: Option<String>,
    /// upc property.
    pub upc: Option<String>,
}

/// `BestSellers` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct BestSellers {
    /// categoryId property.
    pub category_id: Option<String>,
    /// countryCode property.
    pub country_code: Option<String>,
    /// previousRank property.
    pub previous_rank: Option<String>,
    /// previousRelativeDemand property.
    pub previous_relative_demand: Option<String>,
    /// rank property.
    pub rank: Option<String>,
    /// relativeDemand property.
    pub relative_demand: Option<String>,
    /// relativeDemandChange property.
    pub relative_demand_change: Option<String>,
    /// reportDate property.
    pub report_date: Option<Date>,
    /// reportGranularity property.
    pub report_granularity: Option<String>,
}

/// `RecommendationCallToAction` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct RecommendationCallToAction {
    /// intent property.
    pub intent: Option<String>,
    /// localizedText property.
    pub localized_text: Option<String>,
    /// uri property.
    pub uri: Option<String>,
}

/// `DatafeedStatusError` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct DatafeedStatusError {
    /// code property.
    pub code: Option<String>,
    /// count property.
    pub count: Option<String>,
    /// examples property.
    pub examples: Option<Vec<DatafeedStatusExample>>,
    /// message property.
    pub message: Option<String>,
}

/// `ProductIssue` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ProductIssue {
    /// actions property.
    pub actions: Option<Vec<Box<Action>>>,
    /// impact property.
    pub impact: Option<ProductIssueImpact>,
    /// prerenderedContent property.
    pub prerendered_content: Option<String>,
    /// prerenderedOutOfCourtDisputeSettlement property.
    pub prerendered_out_of_court_dispute_settlement: Option<String>,
    /// title property.
    pub title: Option<String>,
}

/// `GmbAccountsGmbAccount` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GmbAccountsGmbAccount {
    /// email property.
    pub email: Option<String>,
    /// listingCount property.
    pub listing_count: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// type property.
    pub r#type: Option<String>,
}

/// `ActionReason` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ActionReason {
    /// action property.
    pub action: Option<Box<Action>>,
    /// detail property.
    pub detail: Option<String>,
    /// message property.
    pub message: Option<String>,
}

/// `Breakdown` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Breakdown {
    /// details property.
    pub details: Option<Vec<String>>,
    /// regions property.
    pub regions: Option<Vec<BreakdownRegion>>,
}

/// `Installment` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Installment {
    /// amount property.
    pub amount: Option<Price>,
    /// creditType property.
    pub credit_type: Option<String>,
    /// downpayment property.
    pub downpayment: Option<Price>,
    /// months property.
    pub months: Option<String>,
}

/// `Product` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Product {
    /// additionalImageLinks property.
    pub additional_image_links: Option<Vec<String>>,
    /// additionalSizeType property.
    pub additional_size_type: Option<String>,
    /// adsGrouping property.
    pub ads_grouping: Option<String>,
    /// adsLabels property.
    pub ads_labels: Option<Vec<String>>,
    /// adsRedirect property.
    pub ads_redirect: Option<String>,
    /// adult property.
    pub adult: Option<bool>,
    /// ageGroup property.
    pub age_group: Option<String>,
    /// autoPricingMinPrice property.
    pub auto_pricing_min_price: Option<Price>,
    /// availability property.
    pub availability: Option<String>,
    /// availabilityDate property.
    pub availability_date: Option<String>,
    /// brand property.
    pub brand: Option<String>,
    /// canonicalLink property.
    pub canonical_link: Option<String>,
    /// certifications property.
    pub certifications: Option<Vec<ProductCertification>>,
    /// channel property.
    pub channel: Option<String>,
    /// cloudExportAdditionalProperties property.
    pub cloud_export_additional_properties: Option<Vec<CloudExportAdditionalProperties>>,
    /// color property.
    pub color: Option<String>,
    /// condition property.
    pub condition: Option<String>,
    /// contentLanguage property.
    pub content_language: Option<String>,
    /// costOfGoodsSold property.
    pub cost_of_goods_sold: Option<Price>,
    /// customAttributes property.
    pub custom_attributes: Option<Vec<Box<CustomAttribute>>>,
    /// customLabel0 property.
    pub custom_label0: Option<String>,
    /// customLabel1 property.
    pub custom_label1: Option<String>,
    /// customLabel2 property.
    pub custom_label2: Option<String>,
    /// customLabel3 property.
    pub custom_label3: Option<String>,
    /// customLabel4 property.
    pub custom_label4: Option<String>,
    /// description property.
    pub description: Option<String>,
    /// disclosureDate property.
    pub disclosure_date: Option<String>,
    /// displayAdsId property.
    pub display_ads_id: Option<String>,
    /// displayAdsLink property.
    pub display_ads_link: Option<String>,
    /// displayAdsSimilarIds property.
    pub display_ads_similar_ids: Option<Vec<String>>,
    /// displayAdsTitle property.
    pub display_ads_title: Option<String>,
    /// displayAdsValue property.
    pub display_ads_value: Option<f64>,
    /// energyEfficiencyClass property.
    pub energy_efficiency_class: Option<String>,
    /// excludedDestinations property.
    pub excluded_destinations: Option<Vec<String>>,
    /// expirationDate property.
    pub expiration_date: Option<String>,
    /// externalSellerId property.
    pub external_seller_id: Option<String>,
    /// feedLabel property.
    pub feed_label: Option<String>,
    /// freeShippingThreshold property.
    pub free_shipping_threshold: Option<Vec<FreeShippingThreshold>>,
    /// gender property.
    pub gender: Option<String>,
    /// googleProductCategory property.
    pub google_product_category: Option<String>,
    /// gtin property.
    pub gtin: Option<String>,
    /// id property.
    pub id: Option<String>,
    /// identifierExists property.
    pub identifier_exists: Option<bool>,
    /// imageLink property.
    pub image_link: Option<String>,
    /// includedDestinations property.
    pub included_destinations: Option<Vec<String>>,
    /// installment property.
    pub installment: Option<Installment>,
    /// isBundle property.
    pub is_bundle: Option<bool>,
    /// itemGroupId property.
    pub item_group_id: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// lifestyleImageLinks property.
    pub lifestyle_image_links: Option<Vec<String>>,
    /// link property.
    pub link: Option<String>,
    /// linkTemplate property.
    pub link_template: Option<String>,
    /// loyaltyProgram property.
    pub loyalty_program: Option<LoyaltyProgram>,
    /// loyaltyPrograms property.
    pub loyalty_programs: Option<Vec<LoyaltyProgram>>,
    /// material property.
    pub material: Option<String>,
    /// maxEnergyEfficiencyClass property.
    pub max_energy_efficiency_class: Option<String>,
    /// maxHandlingTime property.
    pub max_handling_time: Option<String>,
    /// maximumRetailPrice property.
    pub maximum_retail_price: Option<Price>,
    /// minEnergyEfficiencyClass property.
    pub min_energy_efficiency_class: Option<String>,
    /// minHandlingTime property.
    pub min_handling_time: Option<String>,
    /// mobileLink property.
    pub mobile_link: Option<String>,
    /// mobileLinkTemplate property.
    pub mobile_link_template: Option<String>,
    /// mpn property.
    pub mpn: Option<String>,
    /// multipack property.
    pub multipack: Option<String>,
    /// offerId property.
    pub offer_id: Option<String>,
    /// pattern property.
    pub pattern: Option<String>,
    /// pause property.
    pub pause: Option<String>,
    /// pickupMethod property.
    pub pickup_method: Option<String>,
    /// pickupSla property.
    pub pickup_sla: Option<String>,
    /// price property.
    pub price: Option<Price>,
    /// productDetails property.
    pub product_details: Option<Vec<ProductProductDetail>>,
    /// productHeight property.
    pub product_height: Option<ProductDimension>,
    /// productHighlights property.
    pub product_highlights: Option<Vec<String>>,
    /// productLength property.
    pub product_length: Option<ProductDimension>,
    /// productTypes property.
    pub product_types: Option<Vec<String>>,
    /// productWeight property.
    pub product_weight: Option<ProductWeight>,
    /// productWidth property.
    pub product_width: Option<ProductDimension>,
    /// promotionIds property.
    pub promotion_ids: Option<Vec<String>>,
    /// salePrice property.
    pub sale_price: Option<Price>,
    /// salePriceEffectiveDate property.
    pub sale_price_effective_date: Option<String>,
    /// sellOnGoogleQuantity property.
    pub sell_on_google_quantity: Option<String>,
    /// shipping property.
    pub shipping: Option<Vec<ProductShipping>>,
    /// shippingHeight property.
    pub shipping_height: Option<ProductShippingDimension>,
    /// shippingLabel property.
    pub shipping_label: Option<String>,
    /// shippingLength property.
    pub shipping_length: Option<ProductShippingDimension>,
    /// shippingWeight property.
    pub shipping_weight: Option<ProductShippingWeight>,
    /// shippingWidth property.
    pub shipping_width: Option<ProductShippingDimension>,
    /// shoppingAdsExcludedCountries property.
    pub shopping_ads_excluded_countries: Option<Vec<String>>,
    /// sizeSystem property.
    pub size_system: Option<String>,
    /// sizeType property.
    pub size_type: Option<String>,
    /// sizes property.
    pub sizes: Option<Vec<String>>,
    /// source property.
    pub source: Option<String>,
    /// structuredDescription property.
    pub structured_description: Option<ProductStructuredDescription>,
    /// structuredTitle property.
    pub structured_title: Option<ProductStructuredTitle>,
    /// subscriptionCost property.
    pub subscription_cost: Option<ProductSubscriptionCost>,
    /// sustainabilityIncentives property.
    pub sustainability_incentives: Option<Vec<ProductSustainabilityIncentive>>,
    /// targetCountry property.
    pub target_country: Option<String>,
    /// taxCategory property.
    pub tax_category: Option<String>,
    /// taxes property.
    pub taxes: Option<Vec<ProductTax>>,
    /// title property.
    pub title: Option<String>,
    /// transitTimeLabel property.
    pub transit_time_label: Option<String>,
    /// unitPricingBaseMeasure property.
    pub unit_pricing_base_measure: Option<ProductUnitPricingBaseMeasure>,
    /// unitPricingMeasure property.
    pub unit_pricing_measure: Option<ProductUnitPricingMeasure>,
    /// virtualModelLink property.
    pub virtual_model_link: Option<String>,
}

/// `FreeShippingThreshold` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct FreeShippingThreshold {
    /// country property.
    pub country: Option<String>,
    /// priceThreshold property.
    pub price_threshold: Option<Price>,
}

/// `LiaInventorySettings` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct LiaInventorySettings {
    /// inventoryVerificationContactEmail property.
    pub inventory_verification_contact_email: Option<String>,
    /// inventoryVerificationContactName property.
    pub inventory_verification_contact_name: Option<String>,
    /// inventoryVerificationContactStatus property.
    pub inventory_verification_contact_status: Option<String>,
    /// status property.
    pub status: Option<String>,
}

/// `InsertCheckoutSettingsRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct InsertCheckoutSettingsRequest {
    /// uriSettings property.
    pub uri_settings: Option<UrlSettings>,
}

/// `PosCustomBatchRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct PosCustomBatchRequest {
    /// entries property.
    pub entries: Option<Vec<PosCustomBatchRequestEntry>>,
}

/// `LabelIds` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct LabelIds {
    /// labelIds property.
    pub label_ids: Option<Vec<String>>,
}

/// `ReportInteractionRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ReportInteractionRequest {
    /// interactionType property.
    pub interaction_type: Option<String>,
    /// responseToken property.
    pub response_token: Option<String>,
    /// subtype property.
    pub subtype: Option<String>,
    /// type property.
    pub r#type: Option<String>,
}

/// `VerifyPhoneNumberRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct VerifyPhoneNumberRequest {
    /// phoneVerificationMethod property.
    pub phone_verification_method: Option<String>,
    /// verificationCode property.
    pub verification_code: Option<String>,
    /// verificationId property.
    pub verification_id: Option<String>,
}

/// `DeliveryAreaPostalCodeRange` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct DeliveryAreaPostalCodeRange {
    /// firstPostalCode property.
    pub first_postal_code: Option<String>,
    /// lastPostalCode property.
    pub last_postal_code: Option<String>,
}

/// `WarehouseCutoffTime` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct WarehouseCutoffTime {
    /// hour property.
    pub hour: Option<i64>,
    /// minute property.
    pub minute: Option<i64>,
}

/// `LiaSettings` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct LiaSettings {
    /// accountId property.
    pub account_id: Option<String>,
    /// countrySettings property.
    pub country_settings: Option<Vec<LiaCountrySettings>>,
    /// kind property.
    pub kind: Option<String>,
}

/// `ProductsCustomBatchRequestEntry` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ProductsCustomBatchRequestEntry {
    /// batchId property.
    pub batch_id: Option<i64>,
    /// feedId property.
    pub feed_id: Option<String>,
    /// merchantId property.
    pub merchant_id: Option<String>,
    /// method property.
    pub method: Option<String>,
    /// product property.
    pub product: Option<Product>,
    /// productId property.
    pub product_id: Option<String>,
    /// updateMask property.
    pub update_mask: Option<String>,
}

/// `InputFieldTextInput` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct InputFieldTextInput {
    /// additionalInfo property.
    pub additional_info: Option<TextWithTooltip>,
    /// ariaLabel property.
    pub aria_label: Option<String>,
    /// formatInfo property.
    pub format_info: Option<String>,
    /// type property.
    pub r#type: Option<String>,
}

/// `HolidaysHoliday` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct HolidaysHoliday {
    /// countryCode property.
    pub country_code: Option<String>,
    /// date property.
    pub date: Option<String>,
    /// deliveryGuaranteeDate property.
    pub delivery_guarantee_date: Option<String>,
    /// deliveryGuaranteeHour property.
    pub delivery_guarantee_hour: Option<String>,
    /// id property.
    pub id: Option<String>,
    /// type property.
    pub r#type: Option<String>,
}

/// `PosSaleRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct PosSaleRequest {
    /// contentLanguage property.
    pub content_language: Option<String>,
    /// gtin property.
    pub gtin: Option<String>,
    /// itemId property.
    pub item_id: Option<String>,
    /// price property.
    pub price: Option<Price>,
    /// quantity property.
    pub quantity: Option<String>,
    /// saleId property.
    pub sale_id: Option<String>,
    /// storeCode property.
    pub store_code: Option<String>,
    /// targetCountry property.
    pub target_country: Option<String>,
    /// timestamp property.
    pub timestamp: Option<String>,
}

/// `ListRegionsResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ListRegionsResponse {
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// regions property.
    pub regions: Option<Vec<Region>>,
}

/// `Action` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Action {
    /// builtinSimpleAction property.
    pub builtin_simple_action: Option<BuiltInSimpleAction>,
    /// builtinUserInputAction property.
    pub builtin_user_input_action: Option<BuiltInUserInputAction>,
    /// buttonLabel property.
    pub button_label: Option<String>,
    /// externalAction property.
    pub external_action: Option<ExternalAction>,
    /// isAvailable property.
    pub is_available: Option<bool>,
    /// reasons property.
    pub reasons: Option<Vec<Box<ActionReason>>>,
}

/// `Account` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Account {
    /// accountManagement property.
    pub account_management: Option<String>,
    /// adsLinks property.
    pub ads_links: Option<Vec<AccountAdsLink>>,
    /// adultContent property.
    pub adult_content: Option<bool>,
    /// automaticImprovements property.
    pub automatic_improvements: Option<AccountAutomaticImprovements>,
    /// automaticLabelIds property.
    pub automatic_label_ids: Option<Vec<String>>,
    /// businessIdentity property.
    pub business_identity: Option<AccountBusinessIdentity>,
    /// businessInformation property.
    pub business_information: Option<AccountBusinessInformation>,
    /// conversionSettings property.
    pub conversion_settings: Option<AccountConversionSettings>,
    /// cssId property.
    pub css_id: Option<String>,
    /// googleMyBusinessLink property.
    pub google_my_business_link: Option<AccountGoogleMyBusinessLink>,
    /// id property.
    pub id: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// labelIds property.
    pub label_ids: Option<Vec<String>>,
    /// name property.
    pub name: Option<String>,
    /// sellerId property.
    pub seller_id: Option<String>,
    /// users property.
    pub users: Option<Vec<AccountUser>>,
    /// websiteUrl property.
    pub website_url: Option<String>,
    /// youtubeChannelLinks property.
    pub youtube_channel_links: Option<Vec<AccountYouTubeChannelLink>>,
}

/// `LiaPosDataProvider` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct LiaPosDataProvider {
    /// posDataProviderId property.
    pub pos_data_provider_id: Option<String>,
    /// posExternalAccountId property.
    pub pos_external_account_id: Option<String>,
}

/// `ActionFlow` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ActionFlow {
    /// dialogButtonLabel property.
    pub dialog_button_label: Option<String>,
    /// dialogCallout property.
    pub dialog_callout: Option<Callout>,
    /// dialogMessage property.
    pub dialog_message: Option<TextWithTooltip>,
    /// dialogTitle property.
    pub dialog_title: Option<String>,
    /// id property.
    pub id: Option<String>,
    /// inputs property.
    pub inputs: Option<Vec<Box<InputField>>>,
    /// label property.
    pub label: Option<String>,
}

/// `LocalinventoryCustomBatchRequestEntry` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct LocalinventoryCustomBatchRequestEntry {
    /// batchId property.
    pub batch_id: Option<i64>,
    /// localInventory property.
    pub local_inventory: Option<LocalInventory>,
    /// merchantId property.
    pub merchant_id: Option<String>,
    /// method property.
    pub method: Option<String>,
    /// productId property.
    pub product_id: Option<String>,
}

/// `ListCssesResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ListCssesResponse {
    /// csses property.
    pub csses: Option<Vec<Css>>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
}

/// `CollectionStatusItemLevelIssue` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct CollectionStatusItemLevelIssue {
    /// applicableCountries property.
    pub applicable_countries: Option<Vec<String>>,
    /// attributeName property.
    pub attribute_name: Option<String>,
    /// code property.
    pub code: Option<String>,
    /// description property.
    pub description: Option<String>,
    /// destination property.
    pub destination: Option<String>,
    /// detail property.
    pub detail: Option<String>,
    /// documentation property.
    pub documentation: Option<String>,
    /// resolution property.
    pub resolution: Option<String>,
    /// servability property.
    pub servability: Option<String>,
}

/// `RegionalinventoryCustomBatchRequestEntry` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct RegionalinventoryCustomBatchRequestEntry {
    /// batchId property.
    pub batch_id: Option<i64>,
    /// merchantId property.
    pub merchant_id: Option<String>,
    /// method property.
    pub method: Option<String>,
    /// productId property.
    pub product_id: Option<String>,
    /// regionalInventory property.
    pub regional_inventory: Option<RegionalInventory>,
}

/// `FreeListingsProgramStatusReviewIneligibilityReasonDetails` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct FreeListingsProgramStatusReviewIneligibilityReasonDetails {
    /// cooldownTime property.
    pub cooldown_time: Option<String>,
}

/// `Service` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Service {
    /// active property.
    pub active: Option<bool>,
    /// currency property.
    pub currency: Option<String>,
    /// deliveryCountry property.
    pub delivery_country: Option<String>,
    /// deliveryTime property.
    pub delivery_time: Option<DeliveryTime>,
    /// eligibility property.
    pub eligibility: Option<String>,
    /// minimumOrderValue property.
    pub minimum_order_value: Option<Price>,
    /// minimumOrderValueTable property.
    pub minimum_order_value_table: Option<MinimumOrderValueTable>,
    /// name property.
    pub name: Option<String>,
    /// pickupService property.
    pub pickup_service: Option<PickupCarrierService>,
    /// rateGroups property.
    pub rate_groups: Option<Vec<RateGroup>>,
    /// shipmentType property.
    pub shipment_type: Option<String>,
    /// storeConfig property.
    pub store_config: Option<ServiceStoreConfig>,
}

/// `AttributionSettings` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AttributionSettings {
    /// attributionLookbackWindowInDays property.
    pub attribution_lookback_window_in_days: Option<i64>,
    /// attributionModel property.
    pub attribution_model: Option<String>,
    /// conversionType property.
    pub conversion_type: Option<Vec<AttributionSettingsConversionType>>,
}

/// `OrderTrackingSignalShippingInfo` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct OrderTrackingSignalShippingInfo {
    /// actualDeliveryTime property.
    pub actual_delivery_time: Option<DateTime>,
    /// carrierName property.
    pub carrier_name: Option<String>,
    /// carrierServiceName property.
    pub carrier_service_name: Option<String>,
    /// earliestDeliveryPromiseTime property.
    pub earliest_delivery_promise_time: Option<DateTime>,
    /// latestDeliveryPromiseTime property.
    pub latest_delivery_promise_time: Option<DateTime>,
    /// originPostalCode property.
    pub origin_postal_code: Option<String>,
    /// originRegionCode property.
    pub origin_region_code: Option<String>,
    /// shipmentId property.
    pub shipment_id: Option<String>,
    /// shippedTime property.
    pub shipped_time: Option<DateTime>,
    /// shippingStatus property.
    pub shipping_status: Option<String>,
    /// trackingId property.
    pub tracking_id: Option<String>,
}

/// `Address` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Address {
    /// administrativeArea property.
    pub administrative_area: Option<String>,
    /// city property.
    pub city: Option<String>,
    /// country property.
    pub country: Option<String>,
    /// postalCode property.
    pub postal_code: Option<String>,
    /// streetAddress property.
    pub street_address: Option<String>,
}

/// `ServiceStoreConfigCutoffConfigLocalCutoffTime` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ServiceStoreConfigCutoffConfigLocalCutoffTime {
    /// hour property.
    pub hour: Option<String>,
    /// minute property.
    pub minute: Option<String>,
}

/// `DatafeedsCustomBatchResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct DatafeedsCustomBatchResponse {
    /// entries property.
    pub entries: Option<Vec<DatafeedsCustomBatchResponseEntry>>,
    /// kind property.
    pub kind: Option<String>,
}

/// `PosInventoryRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct PosInventoryRequest {
    /// contentLanguage property.
    pub content_language: Option<String>,
    /// gtin property.
    pub gtin: Option<String>,
    /// itemId property.
    pub item_id: Option<String>,
    /// pickupMethod property.
    pub pickup_method: Option<String>,
    /// pickupSla property.
    pub pickup_sla: Option<String>,
    /// price property.
    pub price: Option<Price>,
    /// quantity property.
    pub quantity: Option<String>,
    /// storeCode property.
    pub store_code: Option<String>,
    /// targetCountry property.
    pub target_country: Option<String>,
    /// timestamp property.
    pub timestamp: Option<String>,
}

/// `LinkService` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct LinkService {
    /// service property.
    pub service: Option<String>,
    /// status property.
    pub status: Option<String>,
}

/// `DatafeedsCustomBatchRequestEntry` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct DatafeedsCustomBatchRequestEntry {
    /// batchId property.
    pub batch_id: Option<i64>,
    /// datafeed property.
    pub datafeed: Option<Datafeed>,
    /// datafeedId property.
    pub datafeed_id: Option<String>,
    /// merchantId property.
    pub merchant_id: Option<String>,
    /// method property.
    pub method: Option<String>,
}

/// `OrderTrackingSignalShipmentLineItemMapping` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct OrderTrackingSignalShipmentLineItemMapping {
    /// lineItemId property.
    pub line_item_id: Option<String>,
    /// quantity property.
    pub quantity: Option<String>,
    /// shipmentId property.
    pub shipment_id: Option<String>,
}

/// `ProductSubscriptionCost` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ProductSubscriptionCost {
    /// amount property.
    pub amount: Option<Price>,
    /// period property.
    pub period: Option<String>,
    /// periodLength property.
    pub period_length: Option<String>,
}

/// `DeliveryArea` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct DeliveryArea {
    /// countryCode property.
    pub country_code: Option<String>,
    /// postalCodeRange property.
    pub postal_code_range: Option<DeliveryAreaPostalCodeRange>,
    /// regionCode property.
    pub region_code: Option<String>,
}

/// `ProductViewItemIssueIssueSeverityPerDestination` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ProductViewItemIssueIssueSeverityPerDestination {
    /// demotedCountries property.
    pub demoted_countries: Option<Vec<String>>,
    /// destination property.
    pub destination: Option<String>,
    /// disapprovedCountries property.
    pub disapproved_countries: Option<Vec<String>>,
}

/// `Metrics` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Metrics {
    /// aos property.
    pub aos: Option<f64>,
    /// aovMicros property.
    pub aov_micros: Option<f64>,
    /// clicks property.
    pub clicks: Option<String>,
    /// conversionRate property.
    pub conversion_rate: Option<f64>,
    /// conversionValueMicros property.
    pub conversion_value_micros: Option<String>,
    /// conversions property.
    pub conversions: Option<f64>,
    /// ctr property.
    pub ctr: Option<f64>,
    /// daysToShip property.
    pub days_to_ship: Option<f64>,
    /// impressions property.
    pub impressions: Option<String>,
    /// itemDaysToShip property.
    pub item_days_to_ship: Option<f64>,
    /// itemFillRate property.
    pub item_fill_rate: Option<f64>,
    /// orderedItemSalesMicros property.
    pub ordered_item_sales_micros: Option<String>,
    /// orderedItems property.
    pub ordered_items: Option<String>,
    /// orders property.
    pub orders: Option<String>,
    /// rejectedItems property.
    pub rejected_items: Option<String>,
    /// returnRate property.
    pub return_rate: Option<f64>,
    /// returnedItems property.
    pub returned_items: Option<String>,
    /// returnsMicros property.
    pub returns_micros: Option<String>,
    /// shippedItemSalesMicros property.
    pub shipped_item_sales_micros: Option<String>,
    /// shippedItems property.
    pub shipped_items: Option<String>,
    /// shippedOrders property.
    pub shipped_orders: Option<String>,
    /// unshippedItems property.
    pub unshipped_items: Option<f64>,
    /// unshippedOrders property.
    pub unshipped_orders: Option<f64>,
}

/// `TransitTableTransitTimeRow` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct TransitTableTransitTimeRow {
    /// values property.
    pub values: Option<Vec<TransitTableTransitTimeRowTransitTimeValue>>,
}

/// `ShippingsettingsGetSupportedPickupServicesResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ShippingsettingsGetSupportedPickupServicesResponse {
    /// kind property.
    pub kind: Option<String>,
    /// pickupServices property.
    pub pickup_services: Option<Vec<PickupServicesPickupService>>,
}

/// `CustomAttribute` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct CustomAttribute {
    /// groupValues property.
    pub group_values: Option<Vec<Box<CustomAttribute>>>,
    /// name property.
    pub name: Option<String>,
    /// value property.
    pub value: Option<String>,
}

// =============================================================================
// ARGS TYPES (per-endpoint)
// =============================================================================

/// Arguments for [`content.accounts.claimwebsite_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContentAccountsClaimwebsiteArgs {
    /// Path parameter: `merchantId`.
    pub merchant_id: String,
    /// Path parameter: `accountId`.
    pub account_id: String,
    /// Query parameter: `overwrite`.
    pub overwrite: Option<String>,
}

/// Arguments for [`content.accounts.custombatch_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContentAccountsCustombatchArgs {
    /// Request body.
    pub body: AccountsCustomBatchRequest,
}

/// Arguments for [`content.accounts.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContentAccountsDeleteArgs {
    /// Path parameter: `merchantId`.
    pub merchant_id: String,
    /// Path parameter: `accountId`.
    pub account_id: String,
    /// Query parameter: `force`.
    pub force: Option<String>,
}

/// Arguments for [`content.accounts.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContentAccountsGetArgs {
    /// Path parameter: `merchantId`.
    pub merchant_id: String,
    /// Path parameter: `accountId`.
    pub account_id: String,
    /// Query parameter: `view`.
    pub view: Option<String>,
}

/// Arguments for [`content.accounts.insert_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContentAccountsInsertArgs {
    /// Path parameter: `merchantId`.
    pub merchant_id: String,
    /// Request body.
    pub body: Account,
}

/// Arguments for [`content.accounts.link_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContentAccountsLinkArgs {
    /// Path parameter: `merchantId`.
    pub merchant_id: String,
    /// Path parameter: `accountId`.
    pub account_id: String,
    /// Request body.
    pub body: AccountsLinkRequest,
}

/// Arguments for [`content.accounts.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContentAccountsListArgs {
    /// Path parameter: `merchantId`.
    pub merchant_id: String,
    /// Query parameter: `label`.
    pub label: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `name`.
    pub name: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `view`.
    pub view: Option<String>,
}

/// Arguments for [`content.accounts.listlinks_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContentAccountsListlinksArgs {
    /// Path parameter: `merchantId`.
    pub merchant_id: String,
    /// Path parameter: `accountId`.
    pub account_id: String,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

/// Arguments for [`content.accounts.requestphoneverification_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContentAccountsRequestphoneverificationArgs {
    /// Path parameter: `merchantId`.
    pub merchant_id: String,
    /// Path parameter: `accountId`.
    pub account_id: String,
    /// Request body.
    pub body: RequestPhoneVerificationRequest,
}

/// Arguments for [`content.accounts.update_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContentAccountsUpdateArgs {
    /// Path parameter: `merchantId`.
    pub merchant_id: String,
    /// Path parameter: `accountId`.
    pub account_id: String,
    /// Request body.
    pub body: Account,
}

/// Arguments for [`content.accounts.updatelabels_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContentAccountsUpdatelabelsArgs {
    /// Path parameter: `merchantId`.
    pub merchant_id: String,
    /// Path parameter: `accountId`.
    pub account_id: String,
    /// Request body.
    pub body: AccountsUpdateLabelsRequest,
}

/// Arguments for [`content.accounts.verifyphonenumber_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContentAccountsVerifyphonenumberArgs {
    /// Path parameter: `merchantId`.
    pub merchant_id: String,
    /// Path parameter: `accountId`.
    pub account_id: String,
    /// Request body.
    pub body: VerifyPhoneNumberRequest,
}

/// Arguments for [`content.accounts.credentials.create_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContentAccountsCredentialsCreateArgs {
    /// Path parameter: `accountId`.
    pub account_id: String,
    /// Request body.
    pub body: AccountCredentials,
}

/// Arguments for [`content.accounts.labels.create_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContentAccountsLabelsCreateArgs {
    /// Path parameter: `accountId`.
    pub account_id: String,
    /// Request body.
    pub body: AccountLabel,
}

/// Arguments for [`content.accounts.labels.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContentAccountsLabelsDeleteArgs {
    /// Path parameter: `accountId`.
    pub account_id: String,
    /// Path parameter: `labelId`.
    pub label_id: String,
}

/// Arguments for [`content.accounts.labels.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContentAccountsLabelsListArgs {
    /// Path parameter: `accountId`.
    pub account_id: String,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

/// Arguments for [`content.accounts.labels.patch_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContentAccountsLabelsPatchArgs {
    /// Path parameter: `accountId`.
    pub account_id: String,
    /// Path parameter: `labelId`.
    pub label_id: String,
    /// Request body.
    pub body: AccountLabel,
}

/// Arguments for [`content.accounts.returncarrier.create_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContentAccountsReturncarrierCreateArgs {
    /// Path parameter: `accountId`.
    pub account_id: String,
    /// Request body.
    pub body: AccountReturnCarrier,
}

/// Arguments for [`content.accounts.returncarrier.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContentAccountsReturncarrierDeleteArgs {
    /// Path parameter: `accountId`.
    pub account_id: String,
    /// Path parameter: `carrierAccountId`.
    pub carrier_account_id: String,
}

/// Arguments for [`content.accounts.returncarrier.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContentAccountsReturncarrierListArgs {
    /// Path parameter: `accountId`.
    pub account_id: String,
}

/// Arguments for [`content.accounts.returncarrier.patch_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContentAccountsReturncarrierPatchArgs {
    /// Path parameter: `accountId`.
    pub account_id: String,
    /// Path parameter: `carrierAccountId`.
    pub carrier_account_id: String,
    /// Request body.
    pub body: AccountReturnCarrier,
}

/// Arguments for [`content.accountstatuses.custombatch_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContentAccountstatusesCustombatchArgs {
    /// Request body.
    pub body: AccountstatusesCustomBatchRequest,
}

/// Arguments for [`content.accountstatuses.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContentAccountstatusesGetArgs {
    /// Path parameter: `merchantId`.
    pub merchant_id: String,
    /// Path parameter: `accountId`.
    pub account_id: String,
    /// Query parameter: `destinations`.
    pub destinations: Option<String>,
}

/// Arguments for [`content.accountstatuses.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContentAccountstatusesListArgs {
    /// Path parameter: `merchantId`.
    pub merchant_id: String,
    /// Query parameter: `destinations`.
    pub destinations: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `name`.
    pub name: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

/// Arguments for [`content.accounttax.custombatch_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContentAccounttaxCustombatchArgs {
    /// Request body.
    pub body: AccounttaxCustomBatchRequest,
}

/// Arguments for [`content.accounttax.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContentAccounttaxGetArgs {
    /// Path parameter: `merchantId`.
    pub merchant_id: String,
    /// Path parameter: `accountId`.
    pub account_id: String,
}

/// Arguments for [`content.accounttax.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContentAccounttaxListArgs {
    /// Path parameter: `merchantId`.
    pub merchant_id: String,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

/// Arguments for [`content.accounttax.update_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContentAccounttaxUpdateArgs {
    /// Path parameter: `merchantId`.
    pub merchant_id: String,
    /// Path parameter: `accountId`.
    pub account_id: String,
    /// Request body.
    pub body: AccountTax,
}

/// Arguments for [`content.collections.create_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContentCollectionsCreateArgs {
    /// Path parameter: `merchantId`.
    pub merchant_id: String,
    /// Request body.
    pub body: Collection,
}

/// Arguments for [`content.collections.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContentCollectionsDeleteArgs {
    /// Path parameter: `merchantId`.
    pub merchant_id: String,
    /// Path parameter: `collectionId`.
    pub collection_id: String,
}

/// Arguments for [`content.collections.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContentCollectionsGetArgs {
    /// Path parameter: `merchantId`.
    pub merchant_id: String,
    /// Path parameter: `collectionId`.
    pub collection_id: String,
}

/// Arguments for [`content.collections.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContentCollectionsListArgs {
    /// Path parameter: `merchantId`.
    pub merchant_id: String,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

/// Arguments for [`content.collectionstatuses.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContentCollectionstatusesGetArgs {
    /// Path parameter: `merchantId`.
    pub merchant_id: String,
    /// Path parameter: `collectionId`.
    pub collection_id: String,
}

/// Arguments for [`content.collectionstatuses.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContentCollectionstatusesListArgs {
    /// Path parameter: `merchantId`.
    pub merchant_id: String,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

/// Arguments for [`content.conversionsources.create_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContentConversionsourcesCreateArgs {
    /// Path parameter: `merchantId`.
    pub merchant_id: String,
    /// Request body.
    pub body: ConversionSource,
}

/// Arguments for [`content.conversionsources.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContentConversionsourcesDeleteArgs {
    /// Path parameter: `merchantId`.
    pub merchant_id: String,
    /// Path parameter: `conversionSourceId`.
    pub conversion_source_id: String,
}

/// Arguments for [`content.conversionsources.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContentConversionsourcesGetArgs {
    /// Path parameter: `merchantId`.
    pub merchant_id: String,
    /// Path parameter: `conversionSourceId`.
    pub conversion_source_id: String,
}

/// Arguments for [`content.conversionsources.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContentConversionsourcesListArgs {
    /// Path parameter: `merchantId`.
    pub merchant_id: String,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `showDeleted`.
    pub show_deleted: Option<String>,
}

/// Arguments for [`content.conversionsources.patch_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContentConversionsourcesPatchArgs {
    /// Path parameter: `merchantId`.
    pub merchant_id: String,
    /// Path parameter: `conversionSourceId`.
    pub conversion_source_id: String,
    /// Query parameter: `updateMask`.
    pub update_mask: Option<String>,
    /// Request body.
    pub body: ConversionSource,
}

/// Arguments for [`content.conversionsources.undelete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContentConversionsourcesUndeleteArgs {
    /// Path parameter: `merchantId`.
    pub merchant_id: String,
    /// Path parameter: `conversionSourceId`.
    pub conversion_source_id: String,
    /// Request body.
    pub body: UndeleteConversionSourceRequest,
}

/// Arguments for [`content.csses.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContentCssesGetArgs {
    /// Path parameter: `cssGroupId`.
    pub css_group_id: String,
    /// Path parameter: `cssDomainId`.
    pub css_domain_id: String,
}

/// Arguments for [`content.csses.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContentCssesListArgs {
    /// Path parameter: `cssGroupId`.
    pub css_group_id: String,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

/// Arguments for [`content.csses.updatelabels_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContentCssesUpdatelabelsArgs {
    /// Path parameter: `cssGroupId`.
    pub css_group_id: String,
    /// Path parameter: `cssDomainId`.
    pub css_domain_id: String,
    /// Request body.
    pub body: LabelIds,
}

/// Arguments for [`content.datafeeds.custombatch_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContentDatafeedsCustombatchArgs {
    /// Request body.
    pub body: DatafeedsCustomBatchRequest,
}

/// Arguments for [`content.datafeeds.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContentDatafeedsDeleteArgs {
    /// Path parameter: `merchantId`.
    pub merchant_id: String,
    /// Path parameter: `datafeedId`.
    pub datafeed_id: String,
}

/// Arguments for [`content.datafeeds.fetchnow_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContentDatafeedsFetchnowArgs {
    /// Path parameter: `merchantId`.
    pub merchant_id: String,
    /// Path parameter: `datafeedId`.
    pub datafeed_id: String,
}

/// Arguments for [`content.datafeeds.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContentDatafeedsGetArgs {
    /// Path parameter: `merchantId`.
    pub merchant_id: String,
    /// Path parameter: `datafeedId`.
    pub datafeed_id: String,
}

/// Arguments for [`content.datafeeds.insert_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContentDatafeedsInsertArgs {
    /// Path parameter: `merchantId`.
    pub merchant_id: String,
    /// Request body.
    pub body: Datafeed,
}

/// Arguments for [`content.datafeeds.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContentDatafeedsListArgs {
    /// Path parameter: `merchantId`.
    pub merchant_id: String,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

/// Arguments for [`content.datafeeds.update_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContentDatafeedsUpdateArgs {
    /// Path parameter: `merchantId`.
    pub merchant_id: String,
    /// Path parameter: `datafeedId`.
    pub datafeed_id: String,
    /// Request body.
    pub body: Datafeed,
}

/// Arguments for [`content.datafeedstatuses.custombatch_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContentDatafeedstatusesCustombatchArgs {
    /// Request body.
    pub body: DatafeedstatusesCustomBatchRequest,
}

/// Arguments for [`content.datafeedstatuses.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContentDatafeedstatusesGetArgs {
    /// Path parameter: `merchantId`.
    pub merchant_id: String,
    /// Path parameter: `datafeedId`.
    pub datafeed_id: String,
    /// Query parameter: `country`.
    pub country: Option<String>,
    /// Query parameter: `feedLabel`.
    pub feed_label: Option<String>,
    /// Query parameter: `language`.
    pub language: Option<String>,
}

/// Arguments for [`content.datafeedstatuses.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContentDatafeedstatusesListArgs {
    /// Path parameter: `merchantId`.
    pub merchant_id: String,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

/// Arguments for [`content.freelistingsprogram.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContentFreelistingsprogramGetArgs {
    /// Path parameter: `merchantId`.
    pub merchant_id: String,
}

/// Arguments for [`content.freelistingsprogram.requestreview_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContentFreelistingsprogramRequestreviewArgs {
    /// Path parameter: `merchantId`.
    pub merchant_id: String,
    /// Request body.
    pub body: RequestReviewFreeListingsRequest,
}

/// Arguments for [`content.freelistingsprogram.checkoutsettings.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContentFreelistingsprogramCheckoutsettingsDeleteArgs {
    /// Path parameter: `merchantId`.
    pub merchant_id: String,
}

/// Arguments for [`content.freelistingsprogram.checkoutsettings.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContentFreelistingsprogramCheckoutsettingsGetArgs {
    /// Path parameter: `merchantId`.
    pub merchant_id: String,
}

/// Arguments for [`content.freelistingsprogram.checkoutsettings.insert_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContentFreelistingsprogramCheckoutsettingsInsertArgs {
    /// Path parameter: `merchantId`.
    pub merchant_id: String,
    /// Request body.
    pub body: InsertCheckoutSettingsRequest,
}

/// Arguments for [`content.liasettings.custombatch_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContentLiasettingsCustombatchArgs {
    /// Request body.
    pub body: LiasettingsCustomBatchRequest,
}

/// Arguments for [`content.liasettings.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContentLiasettingsGetArgs {
    /// Path parameter: `merchantId`.
    pub merchant_id: String,
    /// Path parameter: `accountId`.
    pub account_id: String,
}

/// Arguments for [`content.liasettings.getaccessiblegmbaccounts_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContentLiasettingsGetaccessiblegmbaccountsArgs {
    /// Path parameter: `merchantId`.
    pub merchant_id: String,
    /// Path parameter: `accountId`.
    pub account_id: String,
}

/// Arguments for [`content.liasettings.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContentLiasettingsListArgs {
    /// Path parameter: `merchantId`.
    pub merchant_id: String,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

/// Arguments for [`content.liasettings.requestgmbaccess_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContentLiasettingsRequestgmbaccessArgs {
    /// Path parameter: `merchantId`.
    pub merchant_id: String,
    /// Path parameter: `accountId`.
    pub account_id: String,
    /// Query parameter: `gmbEmail`.
    pub gmb_email: Option<String>,
}

/// Arguments for [`content.liasettings.requestinventoryverification_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContentLiasettingsRequestinventoryverificationArgs {
    /// Path parameter: `merchantId`.
    pub merchant_id: String,
    /// Path parameter: `accountId`.
    pub account_id: String,
    /// Path parameter: `country`.
    pub country: String,
}

/// Arguments for [`content.liasettings.setinventoryverificationcontact_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContentLiasettingsSetinventoryverificationcontactArgs {
    /// Path parameter: `merchantId`.
    pub merchant_id: String,
    /// Path parameter: `accountId`.
    pub account_id: String,
    /// Query parameter: `contactEmail`.
    pub contact_email: Option<String>,
    /// Query parameter: `contactName`.
    pub contact_name: Option<String>,
    /// Query parameter: `country`.
    pub country: Option<String>,
    /// Query parameter: `language`.
    pub language: Option<String>,
}

/// Arguments for [`content.liasettings.setomnichannelexperience_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContentLiasettingsSetomnichannelexperienceArgs {
    /// Path parameter: `merchantId`.
    pub merchant_id: String,
    /// Path parameter: `accountId`.
    pub account_id: String,
    /// Query parameter: `country`.
    pub country: Option<String>,
    /// Query parameter: `lsfType`.
    pub lsf_type: Option<String>,
    /// Query parameter: `pickupTypes`.
    pub pickup_types: Option<String>,
}

/// Arguments for [`content.liasettings.setposdataprovider_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContentLiasettingsSetposdataproviderArgs {
    /// Path parameter: `merchantId`.
    pub merchant_id: String,
    /// Path parameter: `accountId`.
    pub account_id: String,
    /// Query parameter: `country`.
    pub country: Option<String>,
    /// Query parameter: `posDataProviderId`.
    pub pos_data_provider_id: Option<String>,
    /// Query parameter: `posExternalAccountId`.
    pub pos_external_account_id: Option<String>,
}

/// Arguments for [`content.liasettings.update_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContentLiasettingsUpdateArgs {
    /// Path parameter: `merchantId`.
    pub merchant_id: String,
    /// Path parameter: `accountId`.
    pub account_id: String,
    /// Request body.
    pub body: LiaSettings,
}

/// Arguments for [`content.localinventory.custombatch_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContentLocalinventoryCustombatchArgs {
    /// Request body.
    pub body: LocalinventoryCustomBatchRequest,
}

/// Arguments for [`content.localinventory.insert_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContentLocalinventoryInsertArgs {
    /// Path parameter: `merchantId`.
    pub merchant_id: String,
    /// Path parameter: `productId`.
    pub product_id: String,
    /// Request body.
    pub body: LocalInventory,
}

/// Arguments for [`content.merchantsupport.renderaccountissues_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContentMerchantsupportRenderaccountissuesArgs {
    /// Path parameter: `merchantId`.
    pub merchant_id: String,
    /// Query parameter: `languageCode`.
    pub language_code: Option<String>,
    /// Query parameter: `timeZone`.
    pub time_zone: Option<String>,
    /// Request body.
    pub body: RenderAccountIssuesRequestPayload,
}

/// Arguments for [`content.merchantsupport.renderproductissues_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContentMerchantsupportRenderproductissuesArgs {
    /// Path parameter: `merchantId`.
    pub merchant_id: String,
    /// Path parameter: `productId`.
    pub product_id: String,
    /// Query parameter: `languageCode`.
    pub language_code: Option<String>,
    /// Query parameter: `timeZone`.
    pub time_zone: Option<String>,
    /// Request body.
    pub body: RenderProductIssuesRequestPayload,
}

/// Arguments for [`content.merchantsupport.triggeraction_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContentMerchantsupportTriggeractionArgs {
    /// Path parameter: `merchantId`.
    pub merchant_id: String,
    /// Query parameter: `languageCode`.
    pub language_code: Option<String>,
    /// Request body.
    pub body: TriggerActionPayload,
}

/// Arguments for [`content.ordertrackingsignals.create_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContentOrdertrackingsignalsCreateArgs {
    /// Path parameter: `merchantId`.
    pub merchant_id: String,
    /// Request body.
    pub body: OrderTrackingSignal,
}

/// Arguments for [`content.pos.custombatch_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContentPosCustombatchArgs {
    /// Request body.
    pub body: PosCustomBatchRequest,
}

/// Arguments for [`content.pos.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContentPosDeleteArgs {
    /// Path parameter: `merchantId`.
    pub merchant_id: String,
    /// Path parameter: `targetMerchantId`.
    pub target_merchant_id: String,
    /// Path parameter: `storeCode`.
    pub store_code: String,
}

/// Arguments for [`content.pos.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContentPosGetArgs {
    /// Path parameter: `merchantId`.
    pub merchant_id: String,
    /// Path parameter: `targetMerchantId`.
    pub target_merchant_id: String,
    /// Path parameter: `storeCode`.
    pub store_code: String,
}

/// Arguments for [`content.pos.insert_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContentPosInsertArgs {
    /// Path parameter: `merchantId`.
    pub merchant_id: String,
    /// Path parameter: `targetMerchantId`.
    pub target_merchant_id: String,
    /// Request body.
    pub body: PosStore,
}

/// Arguments for [`content.pos.inventory_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContentPosInventoryArgs {
    /// Path parameter: `merchantId`.
    pub merchant_id: String,
    /// Path parameter: `targetMerchantId`.
    pub target_merchant_id: String,
    /// Request body.
    pub body: PosInventoryRequest,
}

/// Arguments for [`content.pos.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContentPosListArgs {
    /// Path parameter: `merchantId`.
    pub merchant_id: String,
    /// Path parameter: `targetMerchantId`.
    pub target_merchant_id: String,
}

/// Arguments for [`content.pos.sale_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContentPosSaleArgs {
    /// Path parameter: `merchantId`.
    pub merchant_id: String,
    /// Path parameter: `targetMerchantId`.
    pub target_merchant_id: String,
    /// Request body.
    pub body: PosSaleRequest,
}

/// Arguments for [`content.productdeliverytime.create_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContentProductdeliverytimeCreateArgs {
    /// Path parameter: `merchantId`.
    pub merchant_id: String,
    /// Request body.
    pub body: ProductDeliveryTime,
}

/// Arguments for [`content.productdeliverytime.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContentProductdeliverytimeDeleteArgs {
    /// Path parameter: `merchantId`.
    pub merchant_id: String,
    /// Path parameter: `productId`.
    pub product_id: String,
}

/// Arguments for [`content.productdeliverytime.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContentProductdeliverytimeGetArgs {
    /// Path parameter: `merchantId`.
    pub merchant_id: String,
    /// Path parameter: `productId`.
    pub product_id: String,
}

/// Arguments for [`content.products.custombatch_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContentProductsCustombatchArgs {
    /// Request body.
    pub body: ProductsCustomBatchRequest,
}

/// Arguments for [`content.products.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContentProductsDeleteArgs {
    /// Path parameter: `merchantId`.
    pub merchant_id: String,
    /// Path parameter: `productId`.
    pub product_id: String,
    /// Query parameter: `feedId`.
    pub feed_id: Option<String>,
}

/// Arguments for [`content.products.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContentProductsGetArgs {
    /// Path parameter: `merchantId`.
    pub merchant_id: String,
    /// Path parameter: `productId`.
    pub product_id: String,
}

/// Arguments for [`content.products.insert_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContentProductsInsertArgs {
    /// Path parameter: `merchantId`.
    pub merchant_id: String,
    /// Query parameter: `feedId`.
    pub feed_id: Option<String>,
    /// Request body.
    pub body: Product,
}

/// Arguments for [`content.products.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContentProductsListArgs {
    /// Path parameter: `merchantId`.
    pub merchant_id: String,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

/// Arguments for [`content.products.update_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContentProductsUpdateArgs {
    /// Path parameter: `merchantId`.
    pub merchant_id: String,
    /// Path parameter: `productId`.
    pub product_id: String,
    /// Query parameter: `updateMask`.
    pub update_mask: Option<String>,
    /// Request body.
    pub body: Product,
}

/// Arguments for [`content.productstatuses.custombatch_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContentProductstatusesCustombatchArgs {
    /// Request body.
    pub body: ProductstatusesCustomBatchRequest,
}

/// Arguments for [`content.productstatuses.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContentProductstatusesGetArgs {
    /// Path parameter: `merchantId`.
    pub merchant_id: String,
    /// Path parameter: `productId`.
    pub product_id: String,
    /// Query parameter: `destinations`.
    pub destinations: Option<String>,
}

/// Arguments for [`content.productstatuses.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContentProductstatusesListArgs {
    /// Path parameter: `merchantId`.
    pub merchant_id: String,
    /// Query parameter: `destinations`.
    pub destinations: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

/// Arguments for [`content.promotions.create_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContentPromotionsCreateArgs {
    /// Path parameter: `merchantId`.
    pub merchant_id: String,
    /// Request body.
    pub body: Promotion,
}

/// Arguments for [`content.promotions.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContentPromotionsGetArgs {
    /// Path parameter: `merchantId`.
    pub merchant_id: String,
    /// Path parameter: `id`.
    pub id: String,
}

/// Arguments for [`content.promotions.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContentPromotionsListArgs {
    /// Path parameter: `merchantId`.
    pub merchant_id: String,
    /// Query parameter: `countryCode`.
    pub country_code: Option<String>,
    /// Query parameter: `languageCode`.
    pub language_code: Option<String>,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

/// Arguments for [`content.pubsubnotificationsettings.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContentPubsubnotificationsettingsGetArgs {
    /// Path parameter: `merchantId`.
    pub merchant_id: String,
}

/// Arguments for [`content.pubsubnotificationsettings.update_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContentPubsubnotificationsettingsUpdateArgs {
    /// Path parameter: `merchantId`.
    pub merchant_id: String,
    /// Request body.
    pub body: PubsubNotificationSettings,
}

/// Arguments for [`content.quotas.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContentQuotasListArgs {
    /// Path parameter: `merchantId`.
    pub merchant_id: String,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

/// Arguments for [`content.recommendations.generate_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContentRecommendationsGenerateArgs {
    /// Path parameter: `merchantId`.
    pub merchant_id: String,
    /// Query parameter: `allowedTag`.
    pub allowed_tag: Option<String>,
    /// Query parameter: `languageCode`.
    pub language_code: Option<String>,
}

/// Arguments for [`content.recommendations.reportInteraction_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContentRecommendationsReportInteractionArgs {
    /// Path parameter: `merchantId`.
    pub merchant_id: String,
    /// Request body.
    pub body: ReportInteractionRequest,
}

/// Arguments for [`content.regionalinventory.custombatch_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContentRegionalinventoryCustombatchArgs {
    /// Request body.
    pub body: RegionalinventoryCustomBatchRequest,
}

/// Arguments for [`content.regionalinventory.insert_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContentRegionalinventoryInsertArgs {
    /// Path parameter: `merchantId`.
    pub merchant_id: String,
    /// Path parameter: `productId`.
    pub product_id: String,
    /// Request body.
    pub body: RegionalInventory,
}

/// Arguments for [`content.regions.create_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContentRegionsCreateArgs {
    /// Path parameter: `merchantId`.
    pub merchant_id: String,
    /// Query parameter: `regionId`.
    pub region_id: Option<String>,
    /// Request body.
    pub body: Region,
}

/// Arguments for [`content.regions.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContentRegionsDeleteArgs {
    /// Path parameter: `merchantId`.
    pub merchant_id: String,
    /// Path parameter: `regionId`.
    pub region_id: String,
}

/// Arguments for [`content.regions.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContentRegionsGetArgs {
    /// Path parameter: `merchantId`.
    pub merchant_id: String,
    /// Path parameter: `regionId`.
    pub region_id: String,
}

/// Arguments for [`content.regions.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContentRegionsListArgs {
    /// Path parameter: `merchantId`.
    pub merchant_id: String,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

/// Arguments for [`content.regions.patch_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContentRegionsPatchArgs {
    /// Path parameter: `merchantId`.
    pub merchant_id: String,
    /// Path parameter: `regionId`.
    pub region_id: String,
    /// Query parameter: `updateMask`.
    pub update_mask: Option<String>,
    /// Request body.
    pub body: Region,
}

/// Arguments for [`content.reports.search_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContentReportsSearchArgs {
    /// Path parameter: `merchantId`.
    pub merchant_id: String,
    /// Request body.
    pub body: SearchRequest,
}

/// Arguments for [`content.returnpolicyonline.create_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContentReturnpolicyonlineCreateArgs {
    /// Path parameter: `merchantId`.
    pub merchant_id: String,
    /// Request body.
    pub body: ReturnPolicyOnline,
}

/// Arguments for [`content.returnpolicyonline.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContentReturnpolicyonlineDeleteArgs {
    /// Path parameter: `merchantId`.
    pub merchant_id: String,
    /// Path parameter: `returnPolicyId`.
    pub return_policy_id: String,
}

/// Arguments for [`content.returnpolicyonline.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContentReturnpolicyonlineGetArgs {
    /// Path parameter: `merchantId`.
    pub merchant_id: String,
    /// Path parameter: `returnPolicyId`.
    pub return_policy_id: String,
}

/// Arguments for [`content.returnpolicyonline.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContentReturnpolicyonlineListArgs {
    /// Path parameter: `merchantId`.
    pub merchant_id: String,
}

/// Arguments for [`content.returnpolicyonline.patch_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContentReturnpolicyonlinePatchArgs {
    /// Path parameter: `merchantId`.
    pub merchant_id: String,
    /// Path parameter: `returnPolicyId`.
    pub return_policy_id: String,
    /// Request body.
    pub body: ReturnPolicyOnline,
}

/// Arguments for [`content.shippingsettings.custombatch_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContentShippingsettingsCustombatchArgs {
    /// Request body.
    pub body: ShippingsettingsCustomBatchRequest,
}

/// Arguments for [`content.shippingsettings.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContentShippingsettingsGetArgs {
    /// Path parameter: `merchantId`.
    pub merchant_id: String,
    /// Path parameter: `accountId`.
    pub account_id: String,
}

/// Arguments for [`content.shippingsettings.getsupportedcarriers_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContentShippingsettingsGetsupportedcarriersArgs {
    /// Path parameter: `merchantId`.
    pub merchant_id: String,
}

/// Arguments for [`content.shippingsettings.getsupportedholidays_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContentShippingsettingsGetsupportedholidaysArgs {
    /// Path parameter: `merchantId`.
    pub merchant_id: String,
}

/// Arguments for [`content.shippingsettings.getsupportedpickupservices_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContentShippingsettingsGetsupportedpickupservicesArgs {
    /// Path parameter: `merchantId`.
    pub merchant_id: String,
}

/// Arguments for [`content.shippingsettings.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContentShippingsettingsListArgs {
    /// Path parameter: `merchantId`.
    pub merchant_id: String,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

/// Arguments for [`content.shippingsettings.update_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContentShippingsettingsUpdateArgs {
    /// Path parameter: `merchantId`.
    pub merchant_id: String,
    /// Path parameter: `accountId`.
    pub account_id: String,
    /// Request body.
    pub body: ShippingSettings,
}

/// Arguments for [`content.shoppingadsprogram.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContentShoppingadsprogramGetArgs {
    /// Path parameter: `merchantId`.
    pub merchant_id: String,
}

/// Arguments for [`content.shoppingadsprogram.requestreview_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContentShoppingadsprogramRequestreviewArgs {
    /// Path parameter: `merchantId`.
    pub merchant_id: String,
    /// Request body.
    pub body: RequestReviewShoppingAdsRequest,
}

// =============================================================================
// CLIENT FUNCTIONS (per-endpoint)
// =============================================================================

// -----------------------------------------------------------------------------
// GET accounts/authinfo
// -----------------------------------------------------------------------------

/// GET accounts/authinfo.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = content_accounts_authinfo_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn content_accounts_authinfo_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContentAccountsAuthinfoArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<AccountsAuthInfoResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url =
        format!("https://shoppingcontent.googleapis.com/content/v2.1/accounts/authinfo",);

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: AccountsAuthInfoResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST {merchantId}/accounts/{accountId}/claimwebsite
// -----------------------------------------------------------------------------

/// POST {merchantId}/accounts/{accountId}/claimwebsite.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = content_accounts_claimwebsite_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn content_accounts_claimwebsite_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContentAccountsClaimwebsiteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<AccountsClaimWebsiteResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://shoppingcontent.googleapis.com/content/v2.1/{}/accounts/{}/claimwebsite",
        args.merchant_id, args.account_id,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.overwrite {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("overwrite=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: AccountsClaimWebsiteResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST accounts/batch
// -----------------------------------------------------------------------------

/// POST accounts/batch.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = content_accounts_custombatch_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn content_accounts_custombatch_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContentAccountsCustombatchArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<AccountsCustomBatchResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url =
        format!("https://shoppingcontent.googleapis.com/content/v2.1/accounts/batch",);

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: AccountsCustomBatchResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE {merchantId}/accounts/{accountId}
// -----------------------------------------------------------------------------

/// DELETE {merchantId}/accounts/{accountId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = content_accounts_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn content_accounts_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContentAccountsDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://shoppingcontent.googleapis.com/content/v2.1/{}/accounts/{}",
        args.merchant_id, args.account_id,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.force {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("force=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream: _,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET {merchantId}/accounts/{accountId}
// -----------------------------------------------------------------------------

/// GET {merchantId}/accounts/{accountId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = content_accounts_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn content_accounts_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContentAccountsGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Account>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://shoppingcontent.googleapis.com/content/v2.1/{}/accounts/{}",
        args.merchant_id, args.account_id,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.view {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("view=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Account =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST {merchantId}/accounts
// -----------------------------------------------------------------------------

/// POST {merchantId}/accounts.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = content_accounts_insert_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn content_accounts_insert_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContentAccountsInsertArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Account>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://shoppingcontent.googleapis.com/content/v2.1/{}/accounts",
        args.merchant_id,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Account =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST {merchantId}/accounts/{accountId}/link
// -----------------------------------------------------------------------------

/// POST {merchantId}/accounts/{accountId}/link.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = content_accounts_link_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn content_accounts_link_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContentAccountsLinkArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<AccountsLinkResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://shoppingcontent.googleapis.com/content/v2.1/{}/accounts/{}/link",
        args.merchant_id, args.account_id,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: AccountsLinkResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET {merchantId}/accounts
// -----------------------------------------------------------------------------

/// GET {merchantId}/accounts.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = content_accounts_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn content_accounts_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContentAccountsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<AccountsListResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://shoppingcontent.googleapis.com/content/v2.1/{}/accounts",
        args.merchant_id,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.label {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("label=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.name {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("name=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.view {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("view=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: AccountsListResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET {merchantId}/accounts/{accountId}/listlinks
// -----------------------------------------------------------------------------

/// GET {merchantId}/accounts/{accountId}/listlinks.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = content_accounts_listlinks_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn content_accounts_listlinks_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContentAccountsListlinksArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<AccountsListLinksResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://shoppingcontent.googleapis.com/content/v2.1/{}/accounts/{}/listlinks",
        args.merchant_id, args.account_id,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: AccountsListLinksResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST {merchantId}/accounts/{accountId}/requestphoneverification
// -----------------------------------------------------------------------------

/// POST {merchantId}/accounts/{accountId}/requestphoneverification.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = content_accounts_requestphoneverification_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn content_accounts_requestphoneverification_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContentAccountsRequestphoneverificationArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<RequestPhoneVerificationResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://shoppingcontent.googleapis.com/content/v2.1/{}/accounts/{}/requestphoneverification",
        args.merchant_id,
        args.account_id,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: RequestPhoneVerificationResponse = serde_json::from_str(&body)
                    .map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PUT {merchantId}/accounts/{accountId}
// -----------------------------------------------------------------------------

/// PUT {merchantId}/accounts/{accountId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = content_accounts_update_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn content_accounts_update_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContentAccountsUpdateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Account>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://shoppingcontent.googleapis.com/content/v2.1/{}/accounts/{}",
        args.merchant_id, args.account_id,
    );

    let mut builder = client
        .put(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Account =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST {merchantId}/accounts/{accountId}/updatelabels
// -----------------------------------------------------------------------------

/// POST {merchantId}/accounts/{accountId}/updatelabels.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = content_accounts_updatelabels_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn content_accounts_updatelabels_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContentAccountsUpdatelabelsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<AccountsUpdateLabelsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://shoppingcontent.googleapis.com/content/v2.1/{}/accounts/{}/updatelabels",
        args.merchant_id, args.account_id,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: AccountsUpdateLabelsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST {merchantId}/accounts/{accountId}/verifyphonenumber
// -----------------------------------------------------------------------------

/// POST {merchantId}/accounts/{accountId}/verifyphonenumber.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = content_accounts_verifyphonenumber_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn content_accounts_verifyphonenumber_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContentAccountsVerifyphonenumberArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<VerifyPhoneNumberResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://shoppingcontent.googleapis.com/content/v2.1/{}/accounts/{}/verifyphonenumber",
        args.merchant_id, args.account_id,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: VerifyPhoneNumberResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST accounts/{accountId}/credentials
// -----------------------------------------------------------------------------

/// POST accounts/{accountId}/credentials.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = content_accounts_credentials_create_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn content_accounts_credentials_create_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContentAccountsCredentialsCreateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<AccountCredentials>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://shoppingcontent.googleapis.com/content/v2.1/accounts/{}/credentials",
        args.account_id,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: AccountCredentials =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST accounts/{accountId}/labels
// -----------------------------------------------------------------------------

/// POST accounts/{accountId}/labels.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = content_accounts_labels_create_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn content_accounts_labels_create_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContentAccountsLabelsCreateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<AccountLabel>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://shoppingcontent.googleapis.com/content/v2.1/accounts/{}/labels",
        args.account_id,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: AccountLabel =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE accounts/{accountId}/labels/{labelId}
// -----------------------------------------------------------------------------

/// DELETE accounts/{accountId}/labels/{labelId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = content_accounts_labels_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn content_accounts_labels_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContentAccountsLabelsDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://shoppingcontent.googleapis.com/content/v2.1/accounts/{}/labels/{}",
        args.account_id, args.label_id,
    );

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream: _,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET accounts/{accountId}/labels
// -----------------------------------------------------------------------------

/// GET accounts/{accountId}/labels.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = content_accounts_labels_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn content_accounts_labels_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContentAccountsLabelsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListAccountLabelsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://shoppingcontent.googleapis.com/content/v2.1/accounts/{}/labels",
        args.account_id,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ListAccountLabelsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PATCH accounts/{accountId}/labels/{labelId}
// -----------------------------------------------------------------------------

/// PATCH accounts/{accountId}/labels/{labelId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = content_accounts_labels_patch_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn content_accounts_labels_patch_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContentAccountsLabelsPatchArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<AccountLabel>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://shoppingcontent.googleapis.com/content/v2.1/accounts/{}/labels/{}",
        args.account_id, args.label_id,
    );

    let mut builder = client
        .patch(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: AccountLabel =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST accounts/{accountId}/returncarrier
// -----------------------------------------------------------------------------

/// POST accounts/{accountId}/returncarrier.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = content_accounts_returncarrier_create_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn content_accounts_returncarrier_create_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContentAccountsReturncarrierCreateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<AccountReturnCarrier>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://shoppingcontent.googleapis.com/content/v2.1/accounts/{}/returncarrier",
        args.account_id,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: AccountReturnCarrier =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE accounts/{accountId}/returncarrier/{carrierAccountId}
// -----------------------------------------------------------------------------

/// DELETE accounts/{accountId}/returncarrier/{carrierAccountId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = content_accounts_returncarrier_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn content_accounts_returncarrier_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContentAccountsReturncarrierDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://shoppingcontent.googleapis.com/content/v2.1/accounts/{}/returncarrier/{}",
        args.account_id, args.carrier_account_id,
    );

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream: _,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET accounts/{accountId}/returncarrier
// -----------------------------------------------------------------------------

/// GET accounts/{accountId}/returncarrier.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = content_accounts_returncarrier_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn content_accounts_returncarrier_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContentAccountsReturncarrierListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListAccountReturnCarrierResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://shoppingcontent.googleapis.com/content/v2.1/accounts/{}/returncarrier",
        args.account_id,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ListAccountReturnCarrierResponse = serde_json::from_str(&body)
                    .map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PATCH accounts/{accountId}/returncarrier/{carrierAccountId}
// -----------------------------------------------------------------------------

/// PATCH accounts/{accountId}/returncarrier/{carrierAccountId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = content_accounts_returncarrier_patch_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn content_accounts_returncarrier_patch_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContentAccountsReturncarrierPatchArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<AccountReturnCarrier>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://shoppingcontent.googleapis.com/content/v2.1/accounts/{}/returncarrier/{}",
        args.account_id, args.carrier_account_id,
    );

    let mut builder = client
        .patch(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: AccountReturnCarrier =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST accountstatuses/batch
// -----------------------------------------------------------------------------

/// POST accountstatuses/batch.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = content_accountstatuses_custombatch_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn content_accountstatuses_custombatch_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContentAccountstatusesCustombatchArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<AccountstatusesCustomBatchResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url =
        format!("https://shoppingcontent.googleapis.com/content/v2.1/accountstatuses/batch",);

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: AccountstatusesCustomBatchResponse = serde_json::from_str(&body)
                    .map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET {merchantId}/accountstatuses/{accountId}
// -----------------------------------------------------------------------------

/// GET {merchantId}/accountstatuses/{accountId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = content_accountstatuses_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn content_accountstatuses_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContentAccountstatusesGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<AccountStatus>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://shoppingcontent.googleapis.com/content/v2.1/{}/accountstatuses/{}",
        args.merchant_id, args.account_id,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.destinations {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("destinations=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: AccountStatus =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET {merchantId}/accountstatuses
// -----------------------------------------------------------------------------

/// GET {merchantId}/accountstatuses.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = content_accountstatuses_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn content_accountstatuses_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContentAccountstatusesListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<AccountstatusesListResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://shoppingcontent.googleapis.com/content/v2.1/{}/accountstatuses",
        args.merchant_id,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.destinations {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("destinations=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.name {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("name=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: AccountstatusesListResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST accounttax/batch
// -----------------------------------------------------------------------------

/// POST accounttax/batch.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = content_accounttax_custombatch_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn content_accounttax_custombatch_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContentAccounttaxCustombatchArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<AccounttaxCustomBatchResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url =
        format!("https://shoppingcontent.googleapis.com/content/v2.1/accounttax/batch",);

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: AccounttaxCustomBatchResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET {merchantId}/accounttax/{accountId}
// -----------------------------------------------------------------------------

/// GET {merchantId}/accounttax/{accountId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = content_accounttax_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn content_accounttax_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContentAccounttaxGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<AccountTax>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://shoppingcontent.googleapis.com/content/v2.1/{}/accounttax/{}",
        args.merchant_id, args.account_id,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: AccountTax =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET {merchantId}/accounttax
// -----------------------------------------------------------------------------

/// GET {merchantId}/accounttax.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = content_accounttax_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn content_accounttax_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContentAccounttaxListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<AccounttaxListResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://shoppingcontent.googleapis.com/content/v2.1/{}/accounttax",
        args.merchant_id,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: AccounttaxListResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PUT {merchantId}/accounttax/{accountId}
// -----------------------------------------------------------------------------

/// PUT {merchantId}/accounttax/{accountId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = content_accounttax_update_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn content_accounttax_update_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContentAccounttaxUpdateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<AccountTax>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://shoppingcontent.googleapis.com/content/v2.1/{}/accounttax/{}",
        args.merchant_id, args.account_id,
    );

    let mut builder = client
        .put(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: AccountTax =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST {merchantId}/collections
// -----------------------------------------------------------------------------

/// POST {merchantId}/collections.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = content_collections_create_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn content_collections_create_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContentCollectionsCreateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Collection>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://shoppingcontent.googleapis.com/content/v2.1/{}/collections",
        args.merchant_id,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Collection =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE {merchantId}/collections/{collectionId}
// -----------------------------------------------------------------------------

/// DELETE {merchantId}/collections/{collectionId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = content_collections_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn content_collections_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContentCollectionsDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://shoppingcontent.googleapis.com/content/v2.1/{}/collections/{}",
        args.merchant_id, args.collection_id,
    );

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream: _,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET {merchantId}/collections/{collectionId}
// -----------------------------------------------------------------------------

/// GET {merchantId}/collections/{collectionId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = content_collections_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn content_collections_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContentCollectionsGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Collection>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://shoppingcontent.googleapis.com/content/v2.1/{}/collections/{}",
        args.merchant_id, args.collection_id,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Collection =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET {merchantId}/collections
// -----------------------------------------------------------------------------

/// GET {merchantId}/collections.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = content_collections_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn content_collections_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContentCollectionsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListCollectionsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://shoppingcontent.googleapis.com/content/v2.1/{}/collections",
        args.merchant_id,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ListCollectionsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET {merchantId}/collectionstatuses/{collectionId}
// -----------------------------------------------------------------------------

/// GET {merchantId}/collectionstatuses/{collectionId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = content_collectionstatuses_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn content_collectionstatuses_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContentCollectionstatusesGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<CollectionStatus>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://shoppingcontent.googleapis.com/content/v2.1/{}/collectionstatuses/{}",
        args.merchant_id, args.collection_id,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: CollectionStatus =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET {merchantId}/collectionstatuses
// -----------------------------------------------------------------------------

/// GET {merchantId}/collectionstatuses.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = content_collectionstatuses_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn content_collectionstatuses_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContentCollectionstatusesListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListCollectionStatusesResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://shoppingcontent.googleapis.com/content/v2.1/{}/collectionstatuses",
        args.merchant_id,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ListCollectionStatusesResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST {merchantId}/conversionsources
// -----------------------------------------------------------------------------

/// POST {merchantId}/conversionsources.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = content_conversionsources_create_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn content_conversionsources_create_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContentConversionsourcesCreateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ConversionSource>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://shoppingcontent.googleapis.com/content/v2.1/{}/conversionsources",
        args.merchant_id,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ConversionSource =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE {merchantId}/conversionsources/{conversionSourceId}
// -----------------------------------------------------------------------------

/// DELETE {merchantId}/conversionsources/{conversionSourceId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = content_conversionsources_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn content_conversionsources_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContentConversionsourcesDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://shoppingcontent.googleapis.com/content/v2.1/{}/conversionsources/{}",
        args.merchant_id, args.conversion_source_id,
    );

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream: _,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET {merchantId}/conversionsources/{conversionSourceId}
// -----------------------------------------------------------------------------

/// GET {merchantId}/conversionsources/{conversionSourceId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = content_conversionsources_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn content_conversionsources_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContentConversionsourcesGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ConversionSource>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://shoppingcontent.googleapis.com/content/v2.1/{}/conversionsources/{}",
        args.merchant_id, args.conversion_source_id,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ConversionSource =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET {merchantId}/conversionsources
// -----------------------------------------------------------------------------

/// GET {merchantId}/conversionsources.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = content_conversionsources_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn content_conversionsources_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContentConversionsourcesListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListConversionSourcesResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://shoppingcontent.googleapis.com/content/v2.1/{}/conversionsources",
        args.merchant_id,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.show_deleted {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("showDeleted=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ListConversionSourcesResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PATCH {merchantId}/conversionsources/{conversionSourceId}
// -----------------------------------------------------------------------------

/// PATCH {merchantId}/conversionsources/{conversionSourceId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = content_conversionsources_patch_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn content_conversionsources_patch_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContentConversionsourcesPatchArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ConversionSource>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://shoppingcontent.googleapis.com/content/v2.1/{}/conversionsources/{}",
        args.merchant_id, args.conversion_source_id,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.update_mask {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("updateMask=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .patch(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ConversionSource =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST {merchantId}/conversionsources/{conversionSourceId}:undelete
// -----------------------------------------------------------------------------

/// POST {merchantId}/conversionsources/{conversionSourceId}:undelete.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = content_conversionsources_undelete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn content_conversionsources_undelete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContentConversionsourcesUndeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://shoppingcontent.googleapis.com/content/v2.1/{}/conversionsources/{}:undelete",
        args.merchant_id, args.conversion_source_id,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream: _,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET {cssGroupId}/csses/{cssDomainId}
// -----------------------------------------------------------------------------

/// GET {cssGroupId}/csses/{cssDomainId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = content_csses_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn content_csses_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContentCssesGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Css>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://shoppingcontent.googleapis.com/content/v2.1/{}/csses/{}",
        args.css_group_id, args.css_domain_id,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Css = serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                    super::shared::ApiError::ParseFailed(e.to_string())
                })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET {cssGroupId}/csses
// -----------------------------------------------------------------------------

/// GET {cssGroupId}/csses.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = content_csses_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn content_csses_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContentCssesListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListCssesResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://shoppingcontent.googleapis.com/content/v2.1/{}/csses",
        args.css_group_id,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ListCssesResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST {cssGroupId}/csses/{cssDomainId}/updatelabels
// -----------------------------------------------------------------------------

/// POST {cssGroupId}/csses/{cssDomainId}/updatelabels.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = content_csses_updatelabels_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn content_csses_updatelabels_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContentCssesUpdatelabelsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Css>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://shoppingcontent.googleapis.com/content/v2.1/{}/csses/{}/updatelabels",
        args.css_group_id, args.css_domain_id,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Css = serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                    super::shared::ApiError::ParseFailed(e.to_string())
                })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST datafeeds/batch
// -----------------------------------------------------------------------------

/// POST datafeeds/batch.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = content_datafeeds_custombatch_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn content_datafeeds_custombatch_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContentDatafeedsCustombatchArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<DatafeedsCustomBatchResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url =
        format!("https://shoppingcontent.googleapis.com/content/v2.1/datafeeds/batch",);

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: DatafeedsCustomBatchResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE {merchantId}/datafeeds/{datafeedId}
// -----------------------------------------------------------------------------

/// DELETE {merchantId}/datafeeds/{datafeedId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = content_datafeeds_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn content_datafeeds_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContentDatafeedsDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://shoppingcontent.googleapis.com/content/v2.1/{}/datafeeds/{}",
        args.merchant_id, args.datafeed_id,
    );

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream: _,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST {merchantId}/datafeeds/{datafeedId}/fetchNow
// -----------------------------------------------------------------------------

/// POST {merchantId}/datafeeds/{datafeedId}/fetchNow.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = content_datafeeds_fetchnow_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn content_datafeeds_fetchnow_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContentDatafeedsFetchnowArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<DatafeedsFetchNowResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://shoppingcontent.googleapis.com/content/v2.1/{}/datafeeds/{}/fetchNow",
        args.merchant_id, args.datafeed_id,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: DatafeedsFetchNowResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET {merchantId}/datafeeds/{datafeedId}
// -----------------------------------------------------------------------------

/// GET {merchantId}/datafeeds/{datafeedId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = content_datafeeds_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn content_datafeeds_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContentDatafeedsGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Datafeed>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://shoppingcontent.googleapis.com/content/v2.1/{}/datafeeds/{}",
        args.merchant_id, args.datafeed_id,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Datafeed =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST {merchantId}/datafeeds
// -----------------------------------------------------------------------------

/// POST {merchantId}/datafeeds.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = content_datafeeds_insert_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn content_datafeeds_insert_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContentDatafeedsInsertArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Datafeed>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://shoppingcontent.googleapis.com/content/v2.1/{}/datafeeds",
        args.merchant_id,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Datafeed =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET {merchantId}/datafeeds
// -----------------------------------------------------------------------------

/// GET {merchantId}/datafeeds.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = content_datafeeds_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn content_datafeeds_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContentDatafeedsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<DatafeedsListResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://shoppingcontent.googleapis.com/content/v2.1/{}/datafeeds",
        args.merchant_id,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: DatafeedsListResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PUT {merchantId}/datafeeds/{datafeedId}
// -----------------------------------------------------------------------------

/// PUT {merchantId}/datafeeds/{datafeedId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = content_datafeeds_update_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn content_datafeeds_update_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContentDatafeedsUpdateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Datafeed>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://shoppingcontent.googleapis.com/content/v2.1/{}/datafeeds/{}",
        args.merchant_id, args.datafeed_id,
    );

    let mut builder = client
        .put(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Datafeed =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST datafeedstatuses/batch
// -----------------------------------------------------------------------------

/// POST datafeedstatuses/batch.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = content_datafeedstatuses_custombatch_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn content_datafeedstatuses_custombatch_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContentDatafeedstatusesCustombatchArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<DatafeedstatusesCustomBatchResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url =
        format!("https://shoppingcontent.googleapis.com/content/v2.1/datafeedstatuses/batch",);

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: DatafeedstatusesCustomBatchResponse = serde_json::from_str(&body)
                    .map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET {merchantId}/datafeedstatuses/{datafeedId}
// -----------------------------------------------------------------------------

/// GET {merchantId}/datafeedstatuses/{datafeedId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = content_datafeedstatuses_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn content_datafeedstatuses_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContentDatafeedstatusesGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<DatafeedStatus>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://shoppingcontent.googleapis.com/content/v2.1/{}/datafeedstatuses/{}",
        args.merchant_id, args.datafeed_id,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.country {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("country=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.feed_label {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("feedLabel=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.language {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("language=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: DatafeedStatus =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET {merchantId}/datafeedstatuses
// -----------------------------------------------------------------------------

/// GET {merchantId}/datafeedstatuses.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = content_datafeedstatuses_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn content_datafeedstatuses_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContentDatafeedstatusesListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<DatafeedstatusesListResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://shoppingcontent.googleapis.com/content/v2.1/{}/datafeedstatuses",
        args.merchant_id,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: DatafeedstatusesListResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET {merchantId}/freelistingsprogram
// -----------------------------------------------------------------------------

/// GET {merchantId}/freelistingsprogram.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = content_freelistingsprogram_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn content_freelistingsprogram_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContentFreelistingsprogramGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<FreeListingsProgramStatus>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://shoppingcontent.googleapis.com/content/v2.1/{}/freelistingsprogram",
        args.merchant_id,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: FreeListingsProgramStatus =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST {merchantId}/freelistingsprogram/requestreview
// -----------------------------------------------------------------------------

/// POST {merchantId}/freelistingsprogram/requestreview.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = content_freelistingsprogram_requestreview_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn content_freelistingsprogram_requestreview_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContentFreelistingsprogramRequestreviewArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://shoppingcontent.googleapis.com/content/v2.1/{}/freelistingsprogram/requestreview",
        args.merchant_id,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream: _,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE {merchantId}/freelistingsprogram/checkoutsettings
// -----------------------------------------------------------------------------

/// DELETE {merchantId}/freelistingsprogram/checkoutsettings.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = content_freelistingsprogram_checkoutsettings_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn content_freelistingsprogram_checkoutsettings_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContentFreelistingsprogramCheckoutsettingsDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://shoppingcontent.googleapis.com/content/v2.1/{}/freelistingsprogram/checkoutsettings",
        args.merchant_id,
    );

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream: _,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET {merchantId}/freelistingsprogram/checkoutsettings
// -----------------------------------------------------------------------------

/// GET {merchantId}/freelistingsprogram/checkoutsettings.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = content_freelistingsprogram_checkoutsettings_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn content_freelistingsprogram_checkoutsettings_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContentFreelistingsprogramCheckoutsettingsGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<CheckoutSettings>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://shoppingcontent.googleapis.com/content/v2.1/{}/freelistingsprogram/checkoutsettings",
        args.merchant_id,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: CheckoutSettings =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST {merchantId}/freelistingsprogram/checkoutsettings
// -----------------------------------------------------------------------------

/// POST {merchantId}/freelistingsprogram/checkoutsettings.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = content_freelistingsprogram_checkoutsettings_insert_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn content_freelistingsprogram_checkoutsettings_insert_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContentFreelistingsprogramCheckoutsettingsInsertArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<CheckoutSettings>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://shoppingcontent.googleapis.com/content/v2.1/{}/freelistingsprogram/checkoutsettings",
        args.merchant_id,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: CheckoutSettings =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST liasettings/batch
// -----------------------------------------------------------------------------

/// POST liasettings/batch.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = content_liasettings_custombatch_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn content_liasettings_custombatch_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContentLiasettingsCustombatchArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<LiasettingsCustomBatchResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url =
        format!("https://shoppingcontent.googleapis.com/content/v2.1/liasettings/batch",);

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: LiasettingsCustomBatchResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET {merchantId}/liasettings/{accountId}
// -----------------------------------------------------------------------------

/// GET {merchantId}/liasettings/{accountId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = content_liasettings_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn content_liasettings_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContentLiasettingsGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<LiaSettings>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://shoppingcontent.googleapis.com/content/v2.1/{}/liasettings/{}",
        args.merchant_id, args.account_id,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: LiaSettings =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET {merchantId}/liasettings/{accountId}/accessiblegmbaccounts
// -----------------------------------------------------------------------------

/// GET {merchantId}/liasettings/{accountId}/accessiblegmbaccounts.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = content_liasettings_getaccessiblegmbaccounts_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn content_liasettings_getaccessiblegmbaccounts_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContentLiasettingsGetaccessiblegmbaccountsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<LiasettingsGetAccessibleGmbAccountsResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://shoppingcontent.googleapis.com/content/v2.1/{}/liasettings/{}/accessiblegmbaccounts",
        args.merchant_id,
        args.account_id,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: LiasettingsGetAccessibleGmbAccountsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET {merchantId}/liasettings
// -----------------------------------------------------------------------------

/// GET {merchantId}/liasettings.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = content_liasettings_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn content_liasettings_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContentLiasettingsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<LiasettingsListResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://shoppingcontent.googleapis.com/content/v2.1/{}/liasettings",
        args.merchant_id,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: LiasettingsListResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET liasettings/posdataproviders
// -----------------------------------------------------------------------------

/// GET liasettings/posdataproviders.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = content_liasettings_listposdataproviders_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn content_liasettings_listposdataproviders_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContentLiasettingsListposdataprovidersArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<LiasettingsListPosDataProvidersResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://shoppingcontent.googleapis.com/content/v2.1/liasettings/posdataproviders",
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: LiasettingsListPosDataProvidersResponse = serde_json::from_str(&body)
                    .map_err(
                    |e: serde_json::Error| super::shared::ApiError::ParseFailed(e.to_string()),
                )?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST {merchantId}/liasettings/{accountId}/requestgmbaccess
// -----------------------------------------------------------------------------

/// POST {merchantId}/liasettings/{accountId}/requestgmbaccess.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = content_liasettings_requestgmbaccess_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn content_liasettings_requestgmbaccess_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContentLiasettingsRequestgmbaccessArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<LiasettingsRequestGmbAccessResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://shoppingcontent.googleapis.com/content/v2.1/{}/liasettings/{}/requestgmbaccess",
        args.merchant_id, args.account_id,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.gmb_email {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("gmbEmail=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: LiasettingsRequestGmbAccessResponse = serde_json::from_str(&body)
                    .map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST {merchantId}/liasettings/{accountId}/requestinventoryverification/{country}
// -----------------------------------------------------------------------------

/// POST {merchantId}/liasettings/{accountId}/requestinventoryverification/{country}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = content_liasettings_requestinventoryverification_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn content_liasettings_requestinventoryverification_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContentLiasettingsRequestinventoryverificationArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<LiasettingsRequestInventoryVerificationResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://shoppingcontent.googleapis.com/content/v2.1/{}/liasettings/{}/requestinventoryverification/{}",
        args.merchant_id,
        args.account_id,
        args.country,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: LiasettingsRequestInventoryVerificationResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST {merchantId}/liasettings/{accountId}/setinventoryverificationcontact
// -----------------------------------------------------------------------------

/// POST {merchantId}/liasettings/{accountId}/setinventoryverificationcontact.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = content_liasettings_setinventoryverificationcontact_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn content_liasettings_setinventoryverificationcontact_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContentLiasettingsSetinventoryverificationcontactArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<LiasettingsSetInventoryVerificationContactResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://shoppingcontent.googleapis.com/content/v2.1/{}/liasettings/{}/setinventoryverificationcontact",
        args.merchant_id,
        args.account_id,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.contact_email {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("contactEmail=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.contact_name {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("contactName=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.country {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("country=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.language {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("language=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: LiasettingsSetInventoryVerificationContactResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST {merchantId}/liasettings/{accountId}/setomnichannelexperience
// -----------------------------------------------------------------------------

/// POST {merchantId}/liasettings/{accountId}/setomnichannelexperience.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = content_liasettings_setomnichannelexperience_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn content_liasettings_setomnichannelexperience_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContentLiasettingsSetomnichannelexperienceArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<LiaOmnichannelExperience>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://shoppingcontent.googleapis.com/content/v2.1/{}/liasettings/{}/setomnichannelexperience",
        args.merchant_id,
        args.account_id,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.country {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("country=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.lsf_type {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("lsfType=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.pickup_types {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pickupTypes=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: LiaOmnichannelExperience =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST {merchantId}/liasettings/{accountId}/setposdataprovider
// -----------------------------------------------------------------------------

/// POST {merchantId}/liasettings/{accountId}/setposdataprovider.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = content_liasettings_setposdataprovider_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn content_liasettings_setposdataprovider_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContentLiasettingsSetposdataproviderArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<LiasettingsSetPosDataProviderResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://shoppingcontent.googleapis.com/content/v2.1/{}/liasettings/{}/setposdataprovider",
        args.merchant_id, args.account_id,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.country {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("country=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.pos_data_provider_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("posDataProviderId=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.pos_external_account_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("posExternalAccountId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: LiasettingsSetPosDataProviderResponse = serde_json::from_str(&body)
                    .map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PUT {merchantId}/liasettings/{accountId}
// -----------------------------------------------------------------------------

/// PUT {merchantId}/liasettings/{accountId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = content_liasettings_update_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn content_liasettings_update_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContentLiasettingsUpdateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<LiaSettings>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://shoppingcontent.googleapis.com/content/v2.1/{}/liasettings/{}",
        args.merchant_id, args.account_id,
    );

    let mut builder = client
        .put(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: LiaSettings =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST localinventory/batch
// -----------------------------------------------------------------------------

/// POST localinventory/batch.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = content_localinventory_custombatch_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn content_localinventory_custombatch_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContentLocalinventoryCustombatchArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<LocalinventoryCustomBatchResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url =
        format!("https://shoppingcontent.googleapis.com/content/v2.1/localinventory/batch",);

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: LocalinventoryCustomBatchResponse = serde_json::from_str(&body)
                    .map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST {merchantId}/products/{productId}/localinventory
// -----------------------------------------------------------------------------

/// POST {merchantId}/products/{productId}/localinventory.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = content_localinventory_insert_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn content_localinventory_insert_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContentLocalinventoryInsertArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<LocalInventory>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://shoppingcontent.googleapis.com/content/v2.1/{}/products/{}/localinventory",
        args.merchant_id, args.product_id,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: LocalInventory =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST {merchantId}/merchantsupport/renderaccountissues
// -----------------------------------------------------------------------------

/// POST {merchantId}/merchantsupport/renderaccountissues.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = content_merchantsupport_renderaccountissues_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn content_merchantsupport_renderaccountissues_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContentMerchantsupportRenderaccountissuesArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<RenderAccountIssuesResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://shoppingcontent.googleapis.com/content/v2.1/{}/merchantsupport/renderaccountissues",
        args.merchant_id,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.language_code {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("languageCode=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.time_zone {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("timeZone=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: RenderAccountIssuesResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST {merchantId}/merchantsupport/renderproductissues/{productId}
// -----------------------------------------------------------------------------

/// POST {merchantId}/merchantsupport/renderproductissues/{productId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = content_merchantsupport_renderproductissues_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn content_merchantsupport_renderproductissues_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContentMerchantsupportRenderproductissuesArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<RenderProductIssuesResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://shoppingcontent.googleapis.com/content/v2.1/{}/merchantsupport/renderproductissues/{}",
        args.merchant_id,
        args.product_id,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.language_code {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("languageCode=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.time_zone {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("timeZone=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: RenderProductIssuesResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST {merchantId}/merchantsupport/triggeraction
// -----------------------------------------------------------------------------

/// POST {merchantId}/merchantsupport/triggeraction.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = content_merchantsupport_triggeraction_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn content_merchantsupport_triggeraction_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContentMerchantsupportTriggeractionArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TriggerActionResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://shoppingcontent.googleapis.com/content/v2.1/{}/merchantsupport/triggeraction",
        args.merchant_id,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.language_code {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("languageCode=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: TriggerActionResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST {merchantId}/ordertrackingsignals
// -----------------------------------------------------------------------------

/// POST {merchantId}/ordertrackingsignals.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = content_ordertrackingsignals_create_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn content_ordertrackingsignals_create_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContentOrdertrackingsignalsCreateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<OrderTrackingSignal>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://shoppingcontent.googleapis.com/content/v2.1/{}/ordertrackingsignals",
        args.merchant_id,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: OrderTrackingSignal =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST pos/batch
// -----------------------------------------------------------------------------

/// POST pos/batch.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = content_pos_custombatch_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn content_pos_custombatch_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContentPosCustombatchArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<PosCustomBatchResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://shoppingcontent.googleapis.com/content/v2.1/pos/batch",);

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: PosCustomBatchResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE {merchantId}/pos/{targetMerchantId}/store/{storeCode}
// -----------------------------------------------------------------------------

/// DELETE {merchantId}/pos/{targetMerchantId}/store/{storeCode}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = content_pos_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn content_pos_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContentPosDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://shoppingcontent.googleapis.com/content/v2.1/{}/pos/{}/store/{}",
        args.merchant_id, args.target_merchant_id, args.store_code,
    );

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream: _,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET {merchantId}/pos/{targetMerchantId}/store/{storeCode}
// -----------------------------------------------------------------------------

/// GET {merchantId}/pos/{targetMerchantId}/store/{storeCode}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = content_pos_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn content_pos_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContentPosGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<PosStore>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://shoppingcontent.googleapis.com/content/v2.1/{}/pos/{}/store/{}",
        args.merchant_id, args.target_merchant_id, args.store_code,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: PosStore =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST {merchantId}/pos/{targetMerchantId}/store
// -----------------------------------------------------------------------------

/// POST {merchantId}/pos/{targetMerchantId}/store.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = content_pos_insert_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn content_pos_insert_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContentPosInsertArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<PosStore>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://shoppingcontent.googleapis.com/content/v2.1/{}/pos/{}/store",
        args.merchant_id, args.target_merchant_id,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: PosStore =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST {merchantId}/pos/{targetMerchantId}/inventory
// -----------------------------------------------------------------------------

/// POST {merchantId}/pos/{targetMerchantId}/inventory.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = content_pos_inventory_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn content_pos_inventory_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContentPosInventoryArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<PosInventoryResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://shoppingcontent.googleapis.com/content/v2.1/{}/pos/{}/inventory",
        args.merchant_id, args.target_merchant_id,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: PosInventoryResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET {merchantId}/pos/{targetMerchantId}/store
// -----------------------------------------------------------------------------

/// GET {merchantId}/pos/{targetMerchantId}/store.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = content_pos_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn content_pos_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContentPosListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<PosListResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://shoppingcontent.googleapis.com/content/v2.1/{}/pos/{}/store",
        args.merchant_id, args.target_merchant_id,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: PosListResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST {merchantId}/pos/{targetMerchantId}/sale
// -----------------------------------------------------------------------------

/// POST {merchantId}/pos/{targetMerchantId}/sale.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = content_pos_sale_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn content_pos_sale_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContentPosSaleArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<PosSaleResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://shoppingcontent.googleapis.com/content/v2.1/{}/pos/{}/sale",
        args.merchant_id, args.target_merchant_id,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: PosSaleResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST {merchantId}/productdeliverytime
// -----------------------------------------------------------------------------

/// POST {merchantId}/productdeliverytime.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = content_productdeliverytime_create_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn content_productdeliverytime_create_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContentProductdeliverytimeCreateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ProductDeliveryTime>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://shoppingcontent.googleapis.com/content/v2.1/{}/productdeliverytime",
        args.merchant_id,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ProductDeliveryTime =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE {merchantId}/productdeliverytime/{productId}
// -----------------------------------------------------------------------------

/// DELETE {merchantId}/productdeliverytime/{productId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = content_productdeliverytime_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn content_productdeliverytime_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContentProductdeliverytimeDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://shoppingcontent.googleapis.com/content/v2.1/{}/productdeliverytime/{}",
        args.merchant_id, args.product_id,
    );

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream: _,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET {merchantId}/productdeliverytime/{productId}
// -----------------------------------------------------------------------------

/// GET {merchantId}/productdeliverytime/{productId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = content_productdeliverytime_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn content_productdeliverytime_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContentProductdeliverytimeGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ProductDeliveryTime>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://shoppingcontent.googleapis.com/content/v2.1/{}/productdeliverytime/{}",
        args.merchant_id, args.product_id,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ProductDeliveryTime =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST products/batch
// -----------------------------------------------------------------------------

/// POST products/batch.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = content_products_custombatch_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn content_products_custombatch_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContentProductsCustombatchArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ProductsCustomBatchResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url =
        format!("https://shoppingcontent.googleapis.com/content/v2.1/products/batch",);

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ProductsCustomBatchResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE {merchantId}/products/{productId}
// -----------------------------------------------------------------------------

/// DELETE {merchantId}/products/{productId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = content_products_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn content_products_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContentProductsDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://shoppingcontent.googleapis.com/content/v2.1/{}/products/{}",
        args.merchant_id, args.product_id,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.feed_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("feedId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream: _,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET {merchantId}/products/{productId}
// -----------------------------------------------------------------------------

/// GET {merchantId}/products/{productId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = content_products_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn content_products_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContentProductsGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Product>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://shoppingcontent.googleapis.com/content/v2.1/{}/products/{}",
        args.merchant_id, args.product_id,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Product =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST {merchantId}/products
// -----------------------------------------------------------------------------

/// POST {merchantId}/products.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = content_products_insert_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn content_products_insert_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContentProductsInsertArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Product>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://shoppingcontent.googleapis.com/content/v2.1/{}/products",
        args.merchant_id,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.feed_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("feedId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Product =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET {merchantId}/products
// -----------------------------------------------------------------------------

/// GET {merchantId}/products.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = content_products_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn content_products_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContentProductsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ProductsListResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://shoppingcontent.googleapis.com/content/v2.1/{}/products",
        args.merchant_id,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ProductsListResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PATCH {merchantId}/products/{productId}
// -----------------------------------------------------------------------------

/// PATCH {merchantId}/products/{productId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = content_products_update_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn content_products_update_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContentProductsUpdateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Product>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://shoppingcontent.googleapis.com/content/v2.1/{}/products/{}",
        args.merchant_id, args.product_id,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.update_mask {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("updateMask=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .patch(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Product =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST productstatuses/batch
// -----------------------------------------------------------------------------

/// POST productstatuses/batch.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = content_productstatuses_custombatch_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn content_productstatuses_custombatch_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContentProductstatusesCustombatchArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<ProductstatusesCustomBatchResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url =
        format!("https://shoppingcontent.googleapis.com/content/v2.1/productstatuses/batch",);

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ProductstatusesCustomBatchResponse = serde_json::from_str(&body)
                    .map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET {merchantId}/productstatuses/{productId}
// -----------------------------------------------------------------------------

/// GET {merchantId}/productstatuses/{productId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = content_productstatuses_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn content_productstatuses_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContentProductstatusesGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ProductStatus>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://shoppingcontent.googleapis.com/content/v2.1/{}/productstatuses/{}",
        args.merchant_id, args.product_id,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.destinations {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("destinations=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ProductStatus =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET {merchantId}/productstatuses
// -----------------------------------------------------------------------------

/// GET {merchantId}/productstatuses.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = content_productstatuses_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn content_productstatuses_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContentProductstatusesListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ProductstatusesListResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://shoppingcontent.googleapis.com/content/v2.1/{}/productstatuses",
        args.merchant_id,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.destinations {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("destinations=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ProductstatusesListResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST {merchantId}/promotions
// -----------------------------------------------------------------------------

/// POST {merchantId}/promotions.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = content_promotions_create_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn content_promotions_create_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContentPromotionsCreateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Promotion>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://shoppingcontent.googleapis.com/content/v2.1/{}/promotions",
        args.merchant_id,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Promotion =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET {merchantId}/promotions/{id}
// -----------------------------------------------------------------------------

/// GET {merchantId}/promotions/{id}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = content_promotions_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn content_promotions_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContentPromotionsGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Promotion>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://shoppingcontent.googleapis.com/content/v2.1/{}/promotions/{}",
        args.merchant_id, args.id,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Promotion =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET {merchantId}/promotions
// -----------------------------------------------------------------------------

/// GET {merchantId}/promotions.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = content_promotions_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn content_promotions_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContentPromotionsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListPromotionResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://shoppingcontent.googleapis.com/content/v2.1/{}/promotions",
        args.merchant_id,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.country_code {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("countryCode=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.language_code {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("languageCode=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ListPromotionResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET {merchantId}/pubsubnotificationsettings
// -----------------------------------------------------------------------------

/// GET {merchantId}/pubsubnotificationsettings.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = content_pubsubnotificationsettings_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn content_pubsubnotificationsettings_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContentPubsubnotificationsettingsGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<PubsubNotificationSettings>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://shoppingcontent.googleapis.com/content/v2.1/{}/pubsubnotificationsettings",
        args.merchant_id,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: PubsubNotificationSettings =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PUT {merchantId}/pubsubnotificationsettings
// -----------------------------------------------------------------------------

/// PUT {merchantId}/pubsubnotificationsettings.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = content_pubsubnotificationsettings_update_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn content_pubsubnotificationsettings_update_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContentPubsubnotificationsettingsUpdateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<PubsubNotificationSettings>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://shoppingcontent.googleapis.com/content/v2.1/{}/pubsubnotificationsettings",
        args.merchant_id,
    );

    let mut builder = client
        .put(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: PubsubNotificationSettings =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET {merchantId}/quotas
// -----------------------------------------------------------------------------

/// GET {merchantId}/quotas.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = content_quotas_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn content_quotas_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContentQuotasListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListMethodQuotasResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://shoppingcontent.googleapis.com/content/v2.1/{}/quotas",
        args.merchant_id,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ListMethodQuotasResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET {merchantId}/recommendations/generate
// -----------------------------------------------------------------------------

/// GET {merchantId}/recommendations/generate.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = content_recommendations_generate_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn content_recommendations_generate_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContentRecommendationsGenerateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GenerateRecommendationsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://shoppingcontent.googleapis.com/content/v2.1/{}/recommendations/generate",
        args.merchant_id,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.allowed_tag {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("allowedTag=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.language_code {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("languageCode=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GenerateRecommendationsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST {merchantId}/recommendations/reportInteraction
// -----------------------------------------------------------------------------

/// POST {merchantId}/recommendations/reportInteraction.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = content_recommendations_report_interaction_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn content_recommendations_report_interaction_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContentRecommendationsReportInteractionArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://shoppingcontent.googleapis.com/content/v2.1/{}/recommendations/reportInteraction",
        args.merchant_id,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream: _,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST regionalinventory/batch
// -----------------------------------------------------------------------------

/// POST regionalinventory/batch.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = content_regionalinventory_custombatch_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn content_regionalinventory_custombatch_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContentRegionalinventoryCustombatchArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<RegionalinventoryCustomBatchResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url =
        format!("https://shoppingcontent.googleapis.com/content/v2.1/regionalinventory/batch",);

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: RegionalinventoryCustomBatchResponse = serde_json::from_str(&body)
                    .map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST {merchantId}/products/{productId}/regionalinventory
// -----------------------------------------------------------------------------

/// POST {merchantId}/products/{productId}/regionalinventory.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = content_regionalinventory_insert_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn content_regionalinventory_insert_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContentRegionalinventoryInsertArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<RegionalInventory>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://shoppingcontent.googleapis.com/content/v2.1/{}/products/{}/regionalinventory",
        args.merchant_id, args.product_id,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: RegionalInventory =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST {merchantId}/regions
// -----------------------------------------------------------------------------

/// POST {merchantId}/regions.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = content_regions_create_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn content_regions_create_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContentRegionsCreateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Region>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://shoppingcontent.googleapis.com/content/v2.1/{}/regions",
        args.merchant_id,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.region_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("regionId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Region =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE {merchantId}/regions/{regionId}
// -----------------------------------------------------------------------------

/// DELETE {merchantId}/regions/{regionId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = content_regions_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn content_regions_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContentRegionsDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://shoppingcontent.googleapis.com/content/v2.1/{}/regions/{}",
        args.merchant_id, args.region_id,
    );

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream: _,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET {merchantId}/regions/{regionId}
// -----------------------------------------------------------------------------

/// GET {merchantId}/regions/{regionId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = content_regions_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn content_regions_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContentRegionsGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Region>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://shoppingcontent.googleapis.com/content/v2.1/{}/regions/{}",
        args.merchant_id, args.region_id,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Region =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET {merchantId}/regions
// -----------------------------------------------------------------------------

/// GET {merchantId}/regions.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = content_regions_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn content_regions_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContentRegionsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListRegionsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://shoppingcontent.googleapis.com/content/v2.1/{}/regions",
        args.merchant_id,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ListRegionsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PATCH {merchantId}/regions/{regionId}
// -----------------------------------------------------------------------------

/// PATCH {merchantId}/regions/{regionId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = content_regions_patch_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn content_regions_patch_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContentRegionsPatchArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Region>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://shoppingcontent.googleapis.com/content/v2.1/{}/regions/{}",
        args.merchant_id, args.region_id,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.update_mask {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("updateMask=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .patch(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Region =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST {merchantId}/reports/search
// -----------------------------------------------------------------------------

/// POST {merchantId}/reports/search.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = content_reports_search_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn content_reports_search_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContentReportsSearchArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SearchResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://shoppingcontent.googleapis.com/content/v2.1/{}/reports/search",
        args.merchant_id,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: SearchResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST {merchantId}/returnpolicyonline
// -----------------------------------------------------------------------------

/// POST {merchantId}/returnpolicyonline.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = content_returnpolicyonline_create_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn content_returnpolicyonline_create_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContentReturnpolicyonlineCreateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ReturnPolicyOnline>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://shoppingcontent.googleapis.com/content/v2.1/{}/returnpolicyonline",
        args.merchant_id,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ReturnPolicyOnline =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE {merchantId}/returnpolicyonline/{returnPolicyId}
// -----------------------------------------------------------------------------

/// DELETE {merchantId}/returnpolicyonline/{returnPolicyId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = content_returnpolicyonline_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn content_returnpolicyonline_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContentReturnpolicyonlineDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://shoppingcontent.googleapis.com/content/v2.1/{}/returnpolicyonline/{}",
        args.merchant_id, args.return_policy_id,
    );

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream: _,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET {merchantId}/returnpolicyonline/{returnPolicyId}
// -----------------------------------------------------------------------------

/// GET {merchantId}/returnpolicyonline/{returnPolicyId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = content_returnpolicyonline_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn content_returnpolicyonline_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContentReturnpolicyonlineGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ReturnPolicyOnline>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://shoppingcontent.googleapis.com/content/v2.1/{}/returnpolicyonline/{}",
        args.merchant_id, args.return_policy_id,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ReturnPolicyOnline =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET {merchantId}/returnpolicyonline
// -----------------------------------------------------------------------------

/// GET {merchantId}/returnpolicyonline.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = content_returnpolicyonline_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn content_returnpolicyonline_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContentReturnpolicyonlineListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListReturnPolicyOnlineResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://shoppingcontent.googleapis.com/content/v2.1/{}/returnpolicyonline",
        args.merchant_id,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ListReturnPolicyOnlineResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PATCH {merchantId}/returnpolicyonline/{returnPolicyId}
// -----------------------------------------------------------------------------

/// PATCH {merchantId}/returnpolicyonline/{returnPolicyId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = content_returnpolicyonline_patch_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn content_returnpolicyonline_patch_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContentReturnpolicyonlinePatchArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ReturnPolicyOnline>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://shoppingcontent.googleapis.com/content/v2.1/{}/returnpolicyonline/{}",
        args.merchant_id, args.return_policy_id,
    );

    let mut builder = client
        .patch(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ReturnPolicyOnline =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST shippingsettings/batch
// -----------------------------------------------------------------------------

/// POST shippingsettings/batch.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = content_shippingsettings_custombatch_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn content_shippingsettings_custombatch_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContentShippingsettingsCustombatchArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<ShippingsettingsCustomBatchResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url =
        format!("https://shoppingcontent.googleapis.com/content/v2.1/shippingsettings/batch",);

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ShippingsettingsCustomBatchResponse = serde_json::from_str(&body)
                    .map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET {merchantId}/shippingsettings/{accountId}
// -----------------------------------------------------------------------------

/// GET {merchantId}/shippingsettings/{accountId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = content_shippingsettings_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn content_shippingsettings_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContentShippingsettingsGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ShippingSettings>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://shoppingcontent.googleapis.com/content/v2.1/{}/shippingsettings/{}",
        args.merchant_id, args.account_id,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ShippingSettings =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET {merchantId}/supportedCarriers
// -----------------------------------------------------------------------------

/// GET {merchantId}/supportedCarriers.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = content_shippingsettings_getsupportedcarriers_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn content_shippingsettings_getsupportedcarriers_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContentShippingsettingsGetsupportedcarriersArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<ShippingsettingsGetSupportedCarriersResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://shoppingcontent.googleapis.com/content/v2.1/{}/supportedCarriers",
        args.merchant_id,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ShippingsettingsGetSupportedCarriersResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET {merchantId}/supportedHolidays
// -----------------------------------------------------------------------------

/// GET {merchantId}/supportedHolidays.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = content_shippingsettings_getsupportedholidays_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn content_shippingsettings_getsupportedholidays_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContentShippingsettingsGetsupportedholidaysArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<ShippingsettingsGetSupportedHolidaysResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://shoppingcontent.googleapis.com/content/v2.1/{}/supportedHolidays",
        args.merchant_id,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ShippingsettingsGetSupportedHolidaysResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET {merchantId}/supportedPickupServices
// -----------------------------------------------------------------------------

/// GET {merchantId}/supportedPickupServices.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = content_shippingsettings_getsupportedpickupservices_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn content_shippingsettings_getsupportedpickupservices_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContentShippingsettingsGetsupportedpickupservicesArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<ShippingsettingsGetSupportedPickupServicesResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://shoppingcontent.googleapis.com/content/v2.1/{}/supportedPickupServices",
        args.merchant_id,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ShippingsettingsGetSupportedPickupServicesResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET {merchantId}/shippingsettings
// -----------------------------------------------------------------------------

/// GET {merchantId}/shippingsettings.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = content_shippingsettings_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn content_shippingsettings_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContentShippingsettingsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ShippingsettingsListResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://shoppingcontent.googleapis.com/content/v2.1/{}/shippingsettings",
        args.merchant_id,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ShippingsettingsListResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PUT {merchantId}/shippingsettings/{accountId}
// -----------------------------------------------------------------------------

/// PUT {merchantId}/shippingsettings/{accountId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = content_shippingsettings_update_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn content_shippingsettings_update_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContentShippingsettingsUpdateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ShippingSettings>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://shoppingcontent.googleapis.com/content/v2.1/{}/shippingsettings/{}",
        args.merchant_id, args.account_id,
    );

    let mut builder = client
        .put(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ShippingSettings =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET {merchantId}/shoppingadsprogram
// -----------------------------------------------------------------------------

/// GET {merchantId}/shoppingadsprogram.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = content_shoppingadsprogram_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn content_shoppingadsprogram_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContentShoppingadsprogramGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ShoppingAdsProgramStatus>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://shoppingcontent.googleapis.com/content/v2.1/{}/shoppingadsprogram",
        args.merchant_id,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ShoppingAdsProgramStatus =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST {merchantId}/shoppingadsprogram/requestreview
// -----------------------------------------------------------------------------

/// POST {merchantId}/shoppingadsprogram/requestreview.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = content_shoppingadsprogram_requestreview_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn content_shoppingadsprogram_requestreview_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContentShoppingadsprogramRequestreviewArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://shoppingcontent.googleapis.com/content/v2.1/{}/shoppingadsprogram/requestreview",
        args.merchant_id,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream: _,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}
