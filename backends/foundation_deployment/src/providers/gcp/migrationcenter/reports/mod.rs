//! Auto-generated API module for gcp/migrationcenter reports.
//!
//! Generated by `cargo run --bin ewe_platform gen_api`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp_migrationcenter_reports `

#![cfg(feature = "gcp_migrationcenter_reports")]
#![allow(clippy::too_many_arguments, clippy::type_complexity)]
#![allow(
    clippy::missing_errors_doc,
    clippy::doc_markdown,
    clippy::useless_format
)]
#![allow(unused_imports)]

use foundation_core::valtron::{TaskIterator, TaskIteratorExt};
use foundation_core::wire::simple_http::client::{ClientRequestBuilder, SimpleHttpClient};
use foundation_macros::JsonHash;
use serde::{Deserialize, Serialize};

// Import shared types used by this module
use super::shared::Operation;

use super::shared::ApiResponse;

// =============================================================================
// TYPE DECLARATIONS
// =============================================================================

/// `Money` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Money {
    /// currencyCode property.
    pub currency_code: Option<String>,
    /// nanos property.
    pub nanos: Option<i64>,
    /// units property.
    pub units: Option<String>,
}

/// `ListReportsResponse` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ListReportsResponse {
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// reports property.
    pub reports: Option<Vec<Report>>,
    /// unreachable property.
    pub unreachable: Option<Vec<String>>,
}

/// `ReportSummaryChartData` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ReportSummaryChartData {
    /// dataPoints property.
    pub data_points: Option<Vec<ReportSummaryChartDataDataPoint>>,
}

/// `ReportSummaryGroupFinding` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ReportSummaryGroupFinding {
    /// assetAggregateStats property.
    pub asset_aggregate_stats: Option<ReportSummaryAssetAggregateStats>,
    /// description property.
    pub description: Option<String>,
    /// displayName property.
    pub display_name: Option<String>,
    /// overlappingAssetCount property.
    pub overlapping_asset_count: Option<String>,
    /// preferenceSetFindings property.
    pub preference_set_findings: Option<Vec<ReportSummaryGroupPreferenceSetFinding>>,
}

/// `ReportSummaryHistogramChartDataBucket` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ReportSummaryHistogramChartDataBucket {
    /// count property.
    pub count: Option<String>,
    /// lowerBound property.
    pub lower_bound: Option<String>,
    /// upperBound property.
    pub upper_bound: Option<String>,
}

/// `Report` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Report {
    /// createTime property.
    pub create_time: Option<String>,
    /// description property.
    pub description: Option<String>,
    /// displayName property.
    pub display_name: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// state property.
    pub state: Option<String>,
    /// summary property.
    pub summary: Option<ReportSummary>,
    /// type property.
    pub r#type: Option<String>,
    /// updateTime property.
    pub update_time: Option<String>,
}

/// `ReportSummarySoleTenantFinding` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ReportSummarySoleTenantFinding {
    /// allocatedAssetCount property.
    pub allocated_asset_count: Option<String>,
    /// allocatedRegions property.
    pub allocated_regions: Option<Vec<String>>,
    /// nodeAllocations property.
    pub node_allocations: Option<Vec<ReportSummarySoleTenantNodeAllocation>>,
}

/// `ReportSummaryChartDataDataPoint` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ReportSummaryChartDataDataPoint {
    /// label property.
    pub label: Option<String>,
    /// value property.
    pub value: Option<f64>,
}

/// `ReportSummaryVmwareEngineFinding` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ReportSummaryVmwareEngineFinding {
    /// allocatedAssetCount property.
    pub allocated_asset_count: Option<String>,
    /// allocatedRegions property.
    pub allocated_regions: Option<Vec<String>>,
    /// nodeAllocations property.
    pub node_allocations: Option<Vec<ReportSummaryVmwareNodeAllocation>>,
}

/// `ReportSummaryUtilizationChartData` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ReportSummaryUtilizationChartData {
    /// free property.
    pub free: Option<String>,
    /// used property.
    pub used: Option<String>,
}

/// `ReportSummarySoleTenantNodeAllocation` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ReportSummarySoleTenantNodeAllocation {
    /// allocatedAssetCount property.
    pub allocated_asset_count: Option<String>,
    /// node property.
    pub node: Option<SoleTenantNodeType>,
    /// nodeCount property.
    pub node_count: Option<String>,
}

/// `ReportSummaryVmwareNodeAllocation` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ReportSummaryVmwareNodeAllocation {
    /// allocatedAssetCount property.
    pub allocated_asset_count: Option<String>,
    /// nodeCount property.
    pub node_count: Option<String>,
    /// vmwareNode property.
    pub vmware_node: Option<ReportSummaryVmwareNode>,
}

/// `SoleTenantNodeType` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct SoleTenantNodeType {
    /// nodeName property.
    pub node_name: Option<String>,
}

/// `ReportSummaryComputeEngineFinding` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ReportSummaryComputeEngineFinding {
    /// allocatedAssetCount property.
    pub allocated_asset_count: Option<String>,
    /// allocatedDiskTypes property.
    pub allocated_disk_types: Option<Vec<String>>,
    /// allocatedRegions property.
    pub allocated_regions: Option<Vec<String>>,
    /// machineSeriesAllocations property.
    pub machine_series_allocations: Option<Vec<ReportSummaryMachineSeriesAllocation>>,
}

/// `VirtualMachinePreferences` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct VirtualMachinePreferences {
    /// commitmentPlan property.
    pub commitment_plan: Option<String>,
    /// computeEnginePreferences property.
    pub compute_engine_preferences: Option<ComputeEnginePreferences>,
    /// regionPreferences property.
    pub region_preferences: Option<RegionPreferences>,
    /// sizingOptimizationStrategy property.
    pub sizing_optimization_strategy: Option<String>,
    /// soleTenancyPreferences property.
    pub sole_tenancy_preferences: Option<SoleTenancyPreferences>,
    /// targetProduct property.
    pub target_product: Option<String>,
    /// vmwareEnginePreferences property.
    pub vmware_engine_preferences: Option<VmwareEnginePreferences>,
}

/// `ComputeEnginePreferences` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ComputeEnginePreferences {
    /// licenseType property.
    pub license_type: Option<String>,
    /// machinePreferences property.
    pub machine_preferences: Option<MachinePreferences>,
    /// persistentDiskType property.
    pub persistent_disk_type: Option<String>,
}

/// `MachineSeries` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct MachineSeries {
    /// code property.
    pub code: Option<String>,
}

/// `ReportSummary` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ReportSummary {
    /// allAssetsStats property.
    pub all_assets_stats: Option<ReportSummaryAssetAggregateStats>,
    /// groupFindings property.
    pub group_findings: Option<Vec<ReportSummaryGroupFinding>>,
}

/// `MachinePreferences` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct MachinePreferences {
    /// allowedMachineSeries property.
    pub allowed_machine_series: Option<Vec<MachineSeries>>,
}

/// `ReportSummaryMachineSeriesAllocation` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ReportSummaryMachineSeriesAllocation {
    /// allocatedAssetCount property.
    pub allocated_asset_count: Option<String>,
    /// machineSeries property.
    pub machine_series: Option<MachineSeries>,
}

/// `ReportSummaryHistogramChartData` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ReportSummaryHistogramChartData {
    /// buckets property.
    pub buckets: Option<Vec<ReportSummaryHistogramChartDataBucket>>,
}

/// `ReportSummaryGroupPreferenceSetFinding` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ReportSummaryGroupPreferenceSetFinding {
    /// computeEngineFinding property.
    pub compute_engine_finding: Option<ReportSummaryComputeEngineFinding>,
    /// description property.
    pub description: Option<String>,
    /// displayName property.
    pub display_name: Option<String>,
    /// machinePreferences property.
    pub machine_preferences: Option<VirtualMachinePreferences>,
    /// monthlyCostCompute property.
    pub monthly_cost_compute: Option<Money>,
    /// monthlyCostNetworkEgress property.
    pub monthly_cost_network_egress: Option<Money>,
    /// monthlyCostOsLicense property.
    pub monthly_cost_os_license: Option<Money>,
    /// monthlyCostOther property.
    pub monthly_cost_other: Option<Money>,
    /// monthlyCostStorage property.
    pub monthly_cost_storage: Option<Money>,
    /// monthlyCostTotal property.
    pub monthly_cost_total: Option<Money>,
    /// soleTenantFinding property.
    pub sole_tenant_finding: Option<ReportSummarySoleTenantFinding>,
    /// vmwareEngineFinding property.
    pub vmware_engine_finding: Option<ReportSummaryVmwareEngineFinding>,
}

/// `ReportSummaryAssetAggregateStats` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ReportSummaryAssetAggregateStats {
    /// coreCountHistogram property.
    pub core_count_histogram: Option<ReportSummaryHistogramChartData>,
    /// memoryBytesHistogram property.
    pub memory_bytes_histogram: Option<ReportSummaryHistogramChartData>,
    /// memoryUtilizationChart property.
    pub memory_utilization_chart: Option<ReportSummaryUtilizationChartData>,
    /// operatingSystem property.
    pub operating_system: Option<ReportSummaryChartData>,
    /// softwareInstances property.
    pub software_instances: Option<ReportSummaryChartData>,
    /// storageBytesHistogram property.
    pub storage_bytes_histogram: Option<ReportSummaryHistogramChartData>,
    /// storageUtilizationChart property.
    pub storage_utilization_chart: Option<ReportSummaryUtilizationChartData>,
    /// totalAssets property.
    pub total_assets: Option<String>,
    /// totalCores property.
    pub total_cores: Option<String>,
    /// totalMemoryBytes property.
    pub total_memory_bytes: Option<String>,
    /// totalStorageBytes property.
    pub total_storage_bytes: Option<String>,
}

/// `SoleTenancyPreferences` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct SoleTenancyPreferences {
    /// commitmentPlan property.
    pub commitment_plan: Option<String>,
    /// cpuOvercommitRatio property.
    pub cpu_overcommit_ratio: Option<f64>,
    /// hostMaintenancePolicy property.
    pub host_maintenance_policy: Option<String>,
    /// nodeTypes property.
    pub node_types: Option<Vec<SoleTenantNodeType>>,
}

/// `RegionPreferences` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct RegionPreferences {
    /// preferredRegions property.
    pub preferred_regions: Option<Vec<String>>,
}

/// `Status` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Status {
    /// code property.
    pub code: Option<i64>,
    /// details property.
    pub details: Option<Vec<serde_json::Value>>,
    /// message property.
    pub message: Option<String>,
}

/// `ReportSummaryVmwareNode` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ReportSummaryVmwareNode {
    /// code property.
    pub code: Option<String>,
}

/// `VmwareEnginePreferences` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct VmwareEnginePreferences {
    /// commitmentPlan property.
    pub commitment_plan: Option<String>,
    /// cpuOvercommitRatio property.
    pub cpu_overcommit_ratio: Option<f64>,
    /// memoryOvercommitRatio property.
    pub memory_overcommit_ratio: Option<f64>,
    /// storageDeduplicationCompressionRatio property.
    pub storage_deduplication_compression_ratio: Option<f64>,
}

// =============================================================================
// ARGS TYPES (per-endpoint)
// =============================================================================

/// Arguments for [`migrationcenter.projects.locations.reportConfigs.reports.create_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MigrationcenterProjectsLocationsReportConfigsReportsCreateArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `reportId`.
    pub report_id: Option<String>,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
}

/// Arguments for [`migrationcenter.projects.locations.reportConfigs.reports.delete_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MigrationcenterProjectsLocationsReportConfigsReportsDeleteArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
}

/// Arguments for [`migrationcenter.projects.locations.reportConfigs.reports.get_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MigrationcenterProjectsLocationsReportConfigsReportsGetArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `view`.
    pub view: Option<String>,
}

/// Arguments for [`migrationcenter.projects.locations.reportConfigs.reports.list_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MigrationcenterProjectsLocationsReportConfigsReportsListArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `view`.
    pub view: Option<String>,
}

// =============================================================================
// CLIENT FUNCTIONS (per-endpoint)
// =============================================================================

// -----------------------------------------------------------------------------
// POST v1/projects/{projectsId}/locations/{locationsId}/reportConfigs/{reportConfigsId}/reports
// -----------------------------------------------------------------------------

/// POST v1/projects/{projectsId}/locations/{locationsId}/reportConfigs/{reportConfigsId}/reports.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = migrationcenter_projects_locations_report_configs_reports_create_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn migrationcenter_projects_locations_report_configs_reports_create_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://migrationcenter.googleapis.com/v1/projects/{{projectsId}}/locations/{{locationsId}}/reportConfigs/{{reportConfigsId}}/reports",
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE v1/projects/{projectsId}/locations/{locationsId}/reportConfigs/{reportConfigsId}/reports/{reportsId}
// -----------------------------------------------------------------------------

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/reportConfigs/{reportConfigsId}/reports/{reportsId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = migrationcenter_projects_locations_report_configs_reports_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn migrationcenter_projects_locations_report_configs_reports_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://migrationcenter.googleapis.com/v1/projects/{{projectsId}}/locations/{{locationsId}}/reportConfigs/{{reportConfigsId}}/reports/{{reportsId}}",
    );

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/projects/{projectsId}/locations/{locationsId}/reportConfigs/{reportConfigsId}/reports/{reportsId}
// -----------------------------------------------------------------------------

/// GET v1/projects/{projectsId}/locations/{locationsId}/reportConfigs/{reportConfigsId}/reports/{reportsId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = migrationcenter_projects_locations_report_configs_reports_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn migrationcenter_projects_locations_report_configs_reports_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Report>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://migrationcenter.googleapis.com/v1/projects/{{projectsId}}/locations/{{locationsId}}/reportConfigs/{{reportConfigsId}}/reports/{{reportsId}}",
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Report =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/projects/{projectsId}/locations/{locationsId}/reportConfigs/{reportConfigsId}/reports
// -----------------------------------------------------------------------------

/// GET v1/projects/{projectsId}/locations/{locationsId}/reportConfigs/{reportConfigsId}/reports.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = migrationcenter_projects_locations_report_configs_reports_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn migrationcenter_projects_locations_report_configs_reports_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListReportsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://migrationcenter.googleapis.com/v1/projects/{{projectsId}}/locations/{{locationsId}}/reportConfigs/{{reportConfigsId}}/reports",
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ListReportsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}
