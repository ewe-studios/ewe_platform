//! Auto-generated API module for gcp/redis clusters.
//!
//! Generated by `cargo run --bin ewe_platform gen_api`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp_redis_clusters `

#![cfg(feature = "gcp_redis_clusters")]
#![allow(clippy::too_many_arguments, clippy::type_complexity)]
#![allow(
    clippy::missing_errors_doc,
    clippy::doc_markdown,
    clippy::useless_format
)]

use foundation_core::valtron::{execute, StreamIterator, TaskIterator, TaskIteratorExt};
use foundation_core::wire::simple_http::client::{ClientRequestBuilder, SimpleHttpClient};
use foundation_macros::JsonHash;
use serde::{Deserialize, Serialize};

// Import shared types used by this module
use super::shared::Operation;

use super::shared::{ApiError, ApiPending, ApiResponse};

// =============================================================================
// TYPE DECLARATIONS
// =============================================================================

/// `EncryptionInfo` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct EncryptionInfo {
    /// encryptionType property.
    pub encryption_type: Option<String>,
    /// kmsKeyPrimaryState property.
    pub kms_key_primary_state: Option<String>,
    /// kmsKeyVersions property.
    pub kms_key_versions: Option<Vec<String>>,
    /// lastUpdateTime property.
    pub last_update_time: Option<String>,
}

/// `StateInfo` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct StateInfo {
    /// updateInfo property.
    pub update_info: Option<UpdateInfo>,
}

/// `ClusterPersistenceConfig` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ClusterPersistenceConfig {
    /// aofConfig property.
    pub aof_config: Option<AOFConfig>,
    /// mode property.
    pub mode: Option<String>,
    /// rdbConfig property.
    pub rdb_config: Option<RDBConfig>,
}

/// `ConnectionDetail` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ConnectionDetail {
    /// pscAutoConnection property.
    pub psc_auto_connection: Option<PscAutoConnection>,
    /// pscConnection property.
    pub psc_connection: Option<PscConnection>,
}

/// `DiscoveryEndpoint` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct DiscoveryEndpoint {
    /// address property.
    pub address: Option<String>,
    /// port property.
    pub port: Option<i64>,
    /// pscConfig property.
    pub psc_config: Option<PscConfig>,
}

/// `AOFConfig` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct AOFConfig {
    /// appendFsync property.
    pub append_fsync: Option<String>,
}

/// `Membership` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Membership {
    /// primaryCluster property.
    pub primary_cluster: Option<RemoteCluster>,
    /// secondaryClusters property.
    pub secondary_clusters: Option<Vec<RemoteCluster>>,
}

/// `PscConfig` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct PscConfig {
    /// network property.
    pub network: Option<String>,
}

/// `FixedFrequencySchedule` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct FixedFrequencySchedule {
    /// startTime property.
    pub start_time: Option<TimeOfDay>,
}

/// `PscAutoConnection` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct PscAutoConnection {
    /// address property.
    pub address: Option<String>,
    /// connectionType property.
    pub connection_type: Option<String>,
    /// forwardingRule property.
    pub forwarding_rule: Option<String>,
    /// network property.
    pub network: Option<String>,
    /// projectId property.
    pub project_id: Option<String>,
    /// pscConnectionId property.
    pub psc_connection_id: Option<String>,
    /// pscConnectionStatus property.
    pub psc_connection_status: Option<String>,
    /// serviceAttachment property.
    pub service_attachment: Option<String>,
}

/// `RDBConfig` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct RDBConfig {
    /// rdbSnapshotPeriod property.
    pub rdb_snapshot_period: Option<String>,
    /// rdbSnapshotStartTime property.
    pub rdb_snapshot_start_time: Option<String>,
}

/// `GcsBackupSource` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct GcsBackupSource {
    /// uris property.
    pub uris: Option<Vec<String>>,
}

/// `ClusterMaintenancePolicy` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ClusterMaintenancePolicy {
    /// createTime property.
    pub create_time: Option<String>,
    /// updateTime property.
    pub update_time: Option<String>,
    /// weeklyMaintenanceWindow property.
    pub weekly_maintenance_window: Option<Vec<ClusterWeeklyMaintenanceWindow>>,
}

/// `CrossClusterReplicationConfig` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct CrossClusterReplicationConfig {
    /// clusterRole property.
    pub cluster_role: Option<String>,
    /// membership property.
    pub membership: Option<Membership>,
    /// primaryCluster property.
    pub primary_cluster: Option<RemoteCluster>,
    /// secondaryClusters property.
    pub secondary_clusters: Option<Vec<RemoteCluster>>,
    /// updateTime property.
    pub update_time: Option<String>,
}

/// `Cluster` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Cluster {
    /// aclPolicy property.
    pub acl_policy: Option<String>,
    /// aclPolicyInSync property.
    pub acl_policy_in_sync: Option<bool>,
    /// allowFewerZonesDeployment property.
    pub allow_fewer_zones_deployment: Option<bool>,
    /// asyncClusterEndpointsDeletionEnabled property.
    pub async_cluster_endpoints_deletion_enabled: Option<bool>,
    /// authorizationMode property.
    pub authorization_mode: Option<String>,
    /// automatedBackupConfig property.
    pub automated_backup_config: Option<AutomatedBackupConfig>,
    /// availableMaintenanceVersions property.
    pub available_maintenance_versions: Option<Vec<String>>,
    /// backupCollection property.
    pub backup_collection: Option<String>,
    /// clusterEndpoints property.
    pub cluster_endpoints: Option<Vec<ClusterEndpoint>>,
    /// createTime property.
    pub create_time: Option<String>,
    /// crossClusterReplicationConfig property.
    pub cross_cluster_replication_config: Option<CrossClusterReplicationConfig>,
    /// deletionProtectionEnabled property.
    pub deletion_protection_enabled: Option<bool>,
    /// discoveryEndpoints property.
    pub discovery_endpoints: Option<Vec<DiscoveryEndpoint>>,
    /// effectiveMaintenanceVersion property.
    pub effective_maintenance_version: Option<String>,
    /// encryptionInfo property.
    pub encryption_info: Option<EncryptionInfo>,
    /// gcsSource property.
    pub gcs_source: Option<GcsBackupSource>,
    /// kmsKey property.
    pub kms_key: Option<String>,
    /// labels property.
    pub labels: Option<serde_json::Value>,
    /// maintenancePolicy property.
    pub maintenance_policy: Option<ClusterMaintenancePolicy>,
    /// maintenanceSchedule property.
    pub maintenance_schedule: Option<ClusterMaintenanceSchedule>,
    /// maintenanceVersion property.
    pub maintenance_version: Option<String>,
    /// managedBackupSource property.
    pub managed_backup_source: Option<ManagedBackupSource>,
    /// name property.
    pub name: Option<String>,
    /// nodeType property.
    pub node_type: Option<String>,
    /// ondemandMaintenance property.
    pub ondemand_maintenance: Option<bool>,
    /// persistenceConfig property.
    pub persistence_config: Option<ClusterPersistenceConfig>,
    /// preciseSizeGb property.
    pub precise_size_gb: Option<f64>,
    /// pscConfigs property.
    pub psc_configs: Option<Vec<PscConfig>>,
    /// pscConnections property.
    pub psc_connections: Option<Vec<PscConnection>>,
    /// pscServiceAttachments property.
    pub psc_service_attachments: Option<Vec<PscServiceAttachment>>,
    /// redisConfigs property.
    pub redis_configs: Option<serde_json::Value>,
    /// replicaCount property.
    pub replica_count: Option<i64>,
    /// rotateServerCertificate property.
    pub rotate_server_certificate: Option<bool>,
    /// satisfiesPzi property.
    pub satisfies_pzi: Option<bool>,
    /// satisfiesPzs property.
    pub satisfies_pzs: Option<bool>,
    /// serverCaMode property.
    pub server_ca_mode: Option<String>,
    /// serverCaPool property.
    pub server_ca_pool: Option<String>,
    /// shardCount property.
    pub shard_count: Option<i64>,
    /// simulateMaintenanceEvent property.
    pub simulate_maintenance_event: Option<bool>,
    /// sizeGb property.
    pub size_gb: Option<i64>,
    /// state property.
    pub state: Option<String>,
    /// stateInfo property.
    pub state_info: Option<StateInfo>,
    /// transitEncryptionMode property.
    pub transit_encryption_mode: Option<String>,
    /// uid property.
    pub uid: Option<String>,
    /// zoneDistributionConfig property.
    pub zone_distribution_config: Option<ZoneDistributionConfig>,
}

/// `ListClustersResponse` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ListClustersResponse {
    /// clusters property.
    pub clusters: Option<Vec<Cluster>>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// unreachable property.
    pub unreachable: Option<Vec<String>>,
}

/// `ZoneDistributionConfig` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ZoneDistributionConfig {
    /// mode property.
    pub mode: Option<String>,
    /// zone property.
    pub zone: Option<String>,
}

/// `UpdateInfo` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct UpdateInfo {
    /// targetNodeType property.
    pub target_node_type: Option<String>,
    /// targetReplicaCount property.
    pub target_replica_count: Option<i64>,
    /// targetShardCount property.
    pub target_shard_count: Option<i64>,
}

/// `ClusterWeeklyMaintenanceWindow` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ClusterWeeklyMaintenanceWindow {
    /// day property.
    pub day: Option<String>,
    /// startTime property.
    pub start_time: Option<TimeOfDay>,
}

/// `AutomatedBackupConfig` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct AutomatedBackupConfig {
    /// automatedBackupMode property.
    pub automated_backup_mode: Option<String>,
    /// fixedFrequencySchedule property.
    pub fixed_frequency_schedule: Option<FixedFrequencySchedule>,
    /// retention property.
    pub retention: Option<String>,
}

/// `RemoteCluster` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct RemoteCluster {
    /// cluster property.
    pub cluster: Option<String>,
    /// uid property.
    pub uid: Option<String>,
}

/// `Status` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Status {
    /// code property.
    pub code: Option<i64>,
    /// details property.
    pub details: Option<Vec<serde_json::Value>>,
    /// message property.
    pub message: Option<String>,
}

/// `ClusterEndpoint` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ClusterEndpoint {
    /// connections property.
    pub connections: Option<Vec<ConnectionDetail>>,
}

/// `ClusterMaintenanceSchedule` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ClusterMaintenanceSchedule {
    /// endTime property.
    pub end_time: Option<String>,
    /// startTime property.
    pub start_time: Option<String>,
}

/// `ManagedBackupSource` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ManagedBackupSource {
    /// backup property.
    pub backup: Option<String>,
}

/// `TimeOfDay` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct TimeOfDay {
    /// hours property.
    pub hours: Option<i64>,
    /// minutes property.
    pub minutes: Option<i64>,
    /// nanos property.
    pub nanos: Option<i64>,
    /// seconds property.
    pub seconds: Option<i64>,
}

/// `PscServiceAttachment` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct PscServiceAttachment {
    /// connectionType property.
    pub connection_type: Option<String>,
    /// serviceAttachment property.
    pub service_attachment: Option<String>,
}

/// `PscConnection` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct PscConnection {
    /// address property.
    pub address: Option<String>,
    /// connectionType property.
    pub connection_type: Option<String>,
    /// forwardingRule property.
    pub forwarding_rule: Option<String>,
    /// network property.
    pub network: Option<String>,
    /// port property.
    pub port: Option<i64>,
    /// projectId property.
    pub project_id: Option<String>,
    /// pscConnectionId property.
    pub psc_connection_id: Option<String>,
    /// pscConnectionStatus property.
    pub psc_connection_status: Option<String>,
    /// serviceAttachment property.
    pub service_attachment: Option<String>,
}

// =============================================================================
// ARGS TYPES (per-endpoint)
// =============================================================================

/// Arguments for [`redis.projects.locations.clusters.addTokenAuthUser_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RedisProjectsLocationsClustersAddTokenAuthUserArgs {
    /// Path parameter: `cluster`.
    pub cluster: String,
}

/// Arguments for [`redis.projects.locations.clusters.backup_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RedisProjectsLocationsClustersBackupArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`redis.projects.locations.clusters.create_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RedisProjectsLocationsClustersCreateArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `clusterId`.
    pub cluster_id: Option<String>,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
}

/// Arguments for [`redis.projects.locations.clusters.delete_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RedisProjectsLocationsClustersDeleteArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
}

/// Arguments for [`redis.projects.locations.clusters.get_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RedisProjectsLocationsClustersGetArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`redis.projects.locations.clusters.list_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RedisProjectsLocationsClustersListArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

/// Arguments for [`redis.projects.locations.clusters.patch_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RedisProjectsLocationsClustersPatchArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Query parameter: `updateMask`.
    pub update_mask: Option<String>,
}

/// Arguments for [`redis.projects.locations.clusters.rescheduleClusterMaintenance_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RedisProjectsLocationsClustersRescheduleClusterMaintenanceArgs {
    /// Path parameter: `name`.
    pub name: String,
}

// =============================================================================
// CLIENT FUNCTIONS (per-endpoint)
// =============================================================================

// -----------------------------------------------------------------------------
// POST v1/projects/{projectsId}/locations/{locationsId}/clusters/{clustersId}:addTokenAuthUser
// -----------------------------------------------------------------------------

/// POST v1/projects/{projectsId}/locations/{locationsId}/clusters/{clustersId}:addTokenAuthUser.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = redis_projects_locations_clusters_add_token_auth_user_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn redis_projects_locations_clusters_add_token_auth_user_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://redis.googleapis.com/v1/projects/{{projectsId}}/locations/{{locationsId}}/clusters/{{clustersId}}:addTokenAuthUser",
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/projects/{projectsId}/locations/{locationsId}/clusters/{clustersId}:backup
// -----------------------------------------------------------------------------

/// POST v1/projects/{projectsId}/locations/{locationsId}/clusters/{clustersId}:backup.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = redis_projects_locations_clusters_backup_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn redis_projects_locations_clusters_backup_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://redis.googleapis.com/v1/projects/{{projectsId}}/locations/{{locationsId}}/clusters/{{clustersId}}:backup",
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/projects/{projectsId}/locations/{locationsId}/clusters
// -----------------------------------------------------------------------------

/// POST v1/projects/{projectsId}/locations/{locationsId}/clusters.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = redis_projects_locations_clusters_create_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn redis_projects_locations_clusters_create_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://redis.googleapis.com/v1/projects/{{projectsId}}/locations/{{locationsId}}/clusters",
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE v1/projects/{projectsId}/locations/{locationsId}/clusters/{clustersId}
// -----------------------------------------------------------------------------

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/clusters/{clustersId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = redis_projects_locations_clusters_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn redis_projects_locations_clusters_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://redis.googleapis.com/v1/projects/{{projectsId}}/locations/{{locationsId}}/clusters/{{clustersId}}",
    );

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/projects/{projectsId}/locations/{locationsId}/clusters/{clustersId}
// -----------------------------------------------------------------------------

/// GET v1/projects/{projectsId}/locations/{locationsId}/clusters/{clustersId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = redis_projects_locations_clusters_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn redis_projects_locations_clusters_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Cluster>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://redis.googleapis.com/v1/projects/{{projectsId}}/locations/{{locationsId}}/clusters/{{clustersId}}",
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Cluster =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/projects/{projectsId}/locations/{locationsId}/clusters
// -----------------------------------------------------------------------------

/// GET v1/projects/{projectsId}/locations/{locationsId}/clusters.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = redis_projects_locations_clusters_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn redis_projects_locations_clusters_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListClustersResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://redis.googleapis.com/v1/projects/{{projectsId}}/locations/{{locationsId}}/clusters",
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ListClustersResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PATCH v1/projects/{projectsId}/locations/{locationsId}/clusters/{clustersId}
// -----------------------------------------------------------------------------

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/clusters/{clustersId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = redis_projects_locations_clusters_patch_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn redis_projects_locations_clusters_patch_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://redis.googleapis.com/v1/projects/{{projectsId}}/locations/{{locationsId}}/clusters/{{clustersId}}",
    );

    let mut builder = client
        .patch(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/projects/{projectsId}/locations/{locationsId}/clusters/{clustersId}:rescheduleClusterMaintenance
// -----------------------------------------------------------------------------

/// POST v1/projects/{projectsId}/locations/{locationsId}/clusters/{clustersId}:rescheduleClusterMaintenance.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = redis_projects_locations_clusters_reschedule_cluster_maintenance_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn redis_projects_locations_clusters_reschedule_cluster_maintenance_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://redis.googleapis.com/v1/projects/{{projectsId}}/locations/{{locationsId}}/clusters/{{clustersId}}:rescheduleClusterMaintenance",
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}
