//! Auto-generated API module for gcp/testing testing.
//!
//! Generated by `cargo run --bin ewe_platform gen_api`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp_testing_testing `

#![cfg(feature = "gcp_testing_testing")]
#![allow(clippy::too_many_arguments, clippy::type_complexity)]
#![allow(
    clippy::missing_errors_doc,
    clippy::doc_markdown,
    clippy::useless_format
)]
#![allow(unused_imports)]

use foundation_core::valtron::{TaskIterator, TaskIteratorExt};
use foundation_core::wire::simple_http::client::{ClientRequestBuilder, SimpleHttpClient};
use foundation_macros::JsonHash;
use serde::{Deserialize, Serialize};

// Import shared types used by this module
use super::shared::Empty;

use super::shared::ApiResponse;

// =============================================================================
// TYPE DECLARATIONS
// =============================================================================

/// `ClientInfo` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ClientInfo {
    /// clientInfoDetails property.
    pub client_info_details: Option<Vec<ClientInfoDetail>>,
    /// name property.
    pub name: Option<String>,
}

/// `ListDeviceSessionsResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ListDeviceSessionsResponse {
    /// deviceSessions property.
    pub device_sessions: Option<Vec<DeviceSession>>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
}

/// `DeviceIpBlockCatalog` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct DeviceIpBlockCatalog {
    /// ipBlocks property.
    pub ip_blocks: Option<Vec<DeviceIpBlock>>,
}

/// `IosVersion` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct IosVersion {
    /// id property.
    pub id: Option<String>,
    /// majorVersion property.
    pub major_version: Option<i64>,
    /// minorVersion property.
    pub minor_version: Option<i64>,
    /// supportedXcodeVersionIds property.
    pub supported_xcode_version_ids: Option<Vec<String>>,
    /// tags property.
    pub tags: Option<Vec<String>>,
}

/// `AndroidRuntimeConfiguration` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AndroidRuntimeConfiguration {
    /// locales property.
    pub locales: Option<Vec<Locale>>,
    /// orientations property.
    pub orientations: Option<Vec<Orientation>>,
}

/// `AndroidTestLoop` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AndroidTestLoop {
    /// appApk property.
    pub app_apk: Option<FileReference>,
    /// appBundle property.
    pub app_bundle: Option<AppBundle>,
    /// appPackageId property.
    pub app_package_id: Option<String>,
    /// scenarioLabels property.
    pub scenario_labels: Option<Vec<String>>,
    /// scenarios property.
    pub scenarios: Option<Vec<i64>>,
}

/// `PerAndroidVersionInfo` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct PerAndroidVersionInfo {
    /// deviceCapacity property.
    pub device_capacity: Option<String>,
    /// directAccessVersionInfo property.
    pub direct_access_version_info: Option<DirectAccessVersionInfo>,
    /// interactiveDeviceAvailabilityEstimate property.
    pub interactive_device_availability_estimate: Option<String>,
    /// versionId property.
    pub version_id: Option<String>,
}

/// `Apk` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Apk {
    /// location property.
    pub location: Option<FileReference>,
    /// packageName property.
    pub package_name: Option<String>,
}

/// `RegularFile` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct RegularFile {
    /// content property.
    pub content: Option<FileReference>,
    /// devicePath property.
    pub device_path: Option<String>,
}

/// `TestEnvironmentCatalog` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct TestEnvironmentCatalog {
    /// androidDeviceCatalog property.
    pub android_device_catalog: Option<AndroidDeviceCatalog>,
    /// deviceIpBlockCatalog property.
    pub device_ip_block_catalog: Option<DeviceIpBlockCatalog>,
    /// iosDeviceCatalog property.
    pub ios_device_catalog: Option<IosDeviceCatalog>,
    /// networkConfigurationCatalog property.
    pub network_configuration_catalog: Option<NetworkConfigurationCatalog>,
    /// softwareCatalog property.
    pub software_catalog: Option<ProvidedSoftwareCatalog>,
}

/// `NetworkConfigurationCatalog` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct NetworkConfigurationCatalog {
    /// configurations property.
    pub configurations: Option<Vec<NetworkConfiguration>>,
}

/// `IosModel` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct IosModel {
    /// deviceCapabilities property.
    pub device_capabilities: Option<Vec<String>>,
    /// formFactor property.
    pub form_factor: Option<String>,
    /// id property.
    pub id: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// perVersionInfo property.
    pub per_version_info: Option<Vec<PerIosVersionInfo>>,
    /// screenDensity property.
    pub screen_density: Option<i64>,
    /// screenX property.
    pub screen_x: Option<i64>,
    /// screenY property.
    pub screen_y: Option<i64>,
    /// supportedVersionIds property.
    pub supported_version_ids: Option<Vec<String>>,
    /// tags property.
    pub tags: Option<Vec<String>>,
}

/// `RoboStartingIntent` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct RoboStartingIntent {
    /// launcherActivity property.
    pub launcher_activity: Option<LauncherActivityIntent>,
    /// noActivity property.
    pub no_activity: Option<NoActivityIntent>,
    /// startActivity property.
    pub start_activity: Option<StartActivityIntent>,
    /// timeout property.
    pub timeout: Option<String>,
}

/// `ToolResultsStep` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ToolResultsStep {
    /// executionId property.
    pub execution_id: Option<String>,
    /// historyId property.
    pub history_id: Option<String>,
    /// projectId property.
    pub project_id: Option<String>,
    /// stepId property.
    pub step_id: Option<String>,
}

/// `ToolResultsHistory` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ToolResultsHistory {
    /// historyId property.
    pub history_id: Option<String>,
    /// projectId property.
    pub project_id: Option<String>,
}

/// `XcodeVersion` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct XcodeVersion {
    /// tags property.
    pub tags: Option<Vec<String>>,
    /// version property.
    pub version: Option<String>,
}

/// `DeviceSession` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct DeviceSession {
    /// activeStartTime property.
    pub active_start_time: Option<String>,
    /// androidDevice property.
    pub android_device: Option<AndroidDevice>,
    /// createTime property.
    pub create_time: Option<String>,
    /// displayName property.
    pub display_name: Option<String>,
    /// expireTime property.
    pub expire_time: Option<String>,
    /// inactivityTimeout property.
    pub inactivity_timeout: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// state property.
    pub state: Option<String>,
    /// stateHistories property.
    pub state_histories: Option<Vec<SessionStateEvent>>,
    /// ttl property.
    pub ttl: Option<String>,
}

/// `CancelTestMatrixResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct CancelTestMatrixResponse {
    /// testState property.
    pub test_state: Option<String>,
}

/// `Date` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Date {
    /// day property.
    pub day: Option<i64>,
    /// month property.
    pub month: Option<i64>,
    /// year property.
    pub year: Option<i64>,
}

/// `EnvironmentMatrix` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct EnvironmentMatrix {
    /// androidDeviceList property.
    pub android_device_list: Option<AndroidDeviceList>,
    /// androidMatrix property.
    pub android_matrix: Option<AndroidMatrix>,
    /// iosDeviceList property.
    pub ios_device_list: Option<IosDeviceList>,
}

/// `IosTestLoop` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct IosTestLoop {
    /// appBundleId property.
    pub app_bundle_id: Option<String>,
    /// appIpa property.
    pub app_ipa: Option<FileReference>,
    /// scenarios property.
    pub scenarios: Option<Vec<i64>>,
}

/// `TrafficRule` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct TrafficRule {
    /// bandwidth property.
    pub bandwidth: Option<f64>,
    /// burst property.
    pub burst: Option<f64>,
    /// delay property.
    pub delay: Option<String>,
    /// packetDuplicationRatio property.
    pub packet_duplication_ratio: Option<f64>,
    /// packetLossRatio property.
    pub packet_loss_ratio: Option<f64>,
}

/// `AppBundle` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AppBundle {
    /// apks property.
    pub apks: Option<ApkSplits>,
    /// bundleLocation property.
    pub bundle_location: Option<FileReference>,
}

/// `UsesPermissionTag` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct UsesPermissionTag {
    /// maxSdkVersion property.
    pub max_sdk_version: Option<i64>,
    /// name property.
    pub name: Option<String>,
}

/// `IosDeviceList` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct IosDeviceList {
    /// iosDevices property.
    pub ios_devices: Option<Vec<IosDevice>>,
}

/// `TestSpecification` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct TestSpecification {
    /// androidInstrumentationTest property.
    pub android_instrumentation_test: Option<AndroidInstrumentationTest>,
    /// androidRoboTest property.
    pub android_robo_test: Option<AndroidRoboTest>,
    /// androidTestLoop property.
    pub android_test_loop: Option<AndroidTestLoop>,
    /// disablePerformanceMetrics property.
    pub disable_performance_metrics: Option<bool>,
    /// disableVideoRecording property.
    pub disable_video_recording: Option<bool>,
    /// iosRoboTest property.
    pub ios_robo_test: Option<IosRoboTest>,
    /// iosTestLoop property.
    pub ios_test_loop: Option<IosTestLoop>,
    /// iosTestSetup property.
    pub ios_test_setup: Option<IosTestSetup>,
    /// iosXcTest property.
    pub ios_xc_test: Option<IosXcTest>,
    /// testSetup property.
    pub test_setup: Option<TestSetup>,
    /// testTimeout property.
    pub test_timeout: Option<String>,
}

/// `Environment` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Environment {
    /// androidDevice property.
    pub android_device: Option<AndroidDevice>,
    /// iosDevice property.
    pub ios_device: Option<IosDevice>,
}

/// `TestDetails` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct TestDetails {
    /// errorMessage property.
    pub error_message: Option<String>,
    /// progressMessages property.
    pub progress_messages: Option<Vec<String>>,
}

/// `SessionStateEvent` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SessionStateEvent {
    /// eventTime property.
    pub event_time: Option<String>,
    /// sessionState property.
    pub session_state: Option<String>,
    /// stateMessage property.
    pub state_message: Option<String>,
}

/// `CancelDeviceSessionRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct CancelDeviceSessionRequest {}

/// `ManualSharding` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ManualSharding {
    /// testTargetsForShard property.
    pub test_targets_for_shard: Option<Vec<TestTargetsForShard>>,
}

/// `Distribution` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Distribution {
    /// marketShare property.
    pub market_share: Option<f64>,
    /// measurementTime property.
    pub measurement_time: Option<String>,
}

/// `ApkManifest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ApkManifest {
    /// applicationLabel property.
    pub application_label: Option<String>,
    /// intentFilters property.
    pub intent_filters: Option<Vec<IntentFilter>>,
    /// maxSdkVersion property.
    pub max_sdk_version: Option<i64>,
    /// metadata property.
    pub metadata: Option<Vec<Metadata>>,
    /// minSdkVersion property.
    pub min_sdk_version: Option<i64>,
    /// packageName property.
    pub package_name: Option<String>,
    /// services property.
    pub services: Option<Vec<Service>>,
    /// targetSdkVersion property.
    pub target_sdk_version: Option<i64>,
    /// usesFeature property.
    pub uses_feature: Option<Vec<UsesFeature>>,
    /// usesPermission property.
    pub uses_permission: Option<Vec<String>>,
    /// usesPermissionTags property.
    pub uses_permission_tags: Option<Vec<UsesPermissionTag>>,
    /// versionCode property.
    pub version_code: Option<String>,
    /// versionName property.
    pub version_name: Option<String>,
}

/// `IosXcTest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct IosXcTest {
    /// appBundleId property.
    pub app_bundle_id: Option<String>,
    /// testSpecialEntitlements property.
    pub test_special_entitlements: Option<bool>,
    /// testsZip property.
    pub tests_zip: Option<FileReference>,
    /// xcodeVersion property.
    pub xcode_version: Option<String>,
    /// xctestrun property.
    pub xctestrun: Option<FileReference>,
}

/// `GoogleAuto` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleAuto {}

/// `AndroidMatrix` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AndroidMatrix {
    /// androidModelIds property.
    pub android_model_ids: Option<Vec<String>>,
    /// androidVersionIds property.
    pub android_version_ids: Option<Vec<String>>,
    /// locales property.
    pub locales: Option<Vec<String>>,
    /// orientations property.
    pub orientations: Option<Vec<String>>,
}

/// `Service` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Service {
    /// intentFilter property.
    pub intent_filter: Option<Vec<IntentFilter>>,
    /// name property.
    pub name: Option<String>,
}

/// `Locale` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Locale {
    /// id property.
    pub id: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// region property.
    pub region: Option<String>,
    /// tags property.
    pub tags: Option<Vec<String>>,
}

/// `Shard` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Shard {
    /// estimatedShardDuration property.
    pub estimated_shard_duration: Option<String>,
    /// numShards property.
    pub num_shards: Option<i64>,
    /// shardIndex property.
    pub shard_index: Option<i64>,
    /// testTargetsForShard property.
    pub test_targets_for_shard: Option<TestTargetsForShard>,
}

/// `DeviceFile` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct DeviceFile {
    /// obbFile property.
    pub obb_file: Option<ObbFile>,
    /// regularFile property.
    pub regular_file: Option<RegularFile>,
}

/// `ApkSplits` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ApkSplits {
    /// bundleSplits property.
    pub bundle_splits: Option<Vec<FileReference>>,
}

/// `AndroidDeviceList` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AndroidDeviceList {
    /// androidDevices property.
    pub android_devices: Option<Vec<AndroidDevice>>,
}

/// `Orientation` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Orientation {
    /// id property.
    pub id: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// tags property.
    pub tags: Option<Vec<String>>,
}

/// `IosRuntimeConfiguration` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct IosRuntimeConfiguration {
    /// locales property.
    pub locales: Option<Vec<Locale>>,
    /// orientations property.
    pub orientations: Option<Vec<Orientation>>,
}

/// `ShardingOption` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ShardingOption {
    /// manualSharding property.
    pub manual_sharding: Option<ManualSharding>,
    /// smartSharding property.
    pub smart_sharding: Option<SmartSharding>,
    /// uniformSharding property.
    pub uniform_sharding: Option<UniformSharding>,
}

/// `Metadata` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Metadata {
    /// name property.
    pub name: Option<String>,
    /// value property.
    pub value: Option<String>,
}

/// `MatrixErrorDetail` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct MatrixErrorDetail {
    /// message property.
    pub message: Option<String>,
    /// reason property.
    pub reason: Option<String>,
}

/// `Account` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Account {
    /// googleAuto property.
    pub google_auto: Option<GoogleAuto>,
}

/// `AndroidVersion` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AndroidVersion {
    /// apiLevel property.
    pub api_level: Option<i64>,
    /// codeName property.
    pub code_name: Option<String>,
    /// distribution property.
    pub distribution: Option<Distribution>,
    /// id property.
    pub id: Option<String>,
    /// releaseDate property.
    pub release_date: Option<Date>,
    /// tags property.
    pub tags: Option<Vec<String>>,
    /// versionString property.
    pub version_string: Option<String>,
}

/// `RoboDirective` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct RoboDirective {
    /// actionType property.
    pub action_type: Option<String>,
    /// inputText property.
    pub input_text: Option<String>,
    /// resourceName property.
    pub resource_name: Option<String>,
}

/// `DirectAccessVersionInfo` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct DirectAccessVersionInfo {
    /// directAccessSupported property.
    pub direct_access_supported: Option<bool>,
    /// minimumAndroidStudioVersion property.
    pub minimum_android_studio_version: Option<String>,
}

/// `IosDevice` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct IosDevice {
    /// iosModelId property.
    pub ios_model_id: Option<String>,
    /// iosVersionId property.
    pub ios_version_id: Option<String>,
    /// locale property.
    pub locale: Option<String>,
    /// orientation property.
    pub orientation: Option<String>,
}

/// `NetworkConfiguration` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct NetworkConfiguration {
    /// downRule property.
    pub down_rule: Option<TrafficRule>,
    /// id property.
    pub id: Option<String>,
    /// upRule property.
    pub up_rule: Option<TrafficRule>,
}

/// `StartActivityIntent` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct StartActivityIntent {
    /// action property.
    pub action: Option<String>,
    /// categories property.
    pub categories: Option<Vec<String>>,
    /// uri property.
    pub uri: Option<String>,
}

/// `GoogleCloudStorage` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudStorage {
    /// gcsPath property.
    pub gcs_path: Option<String>,
}

/// `NoActivityIntent` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct NoActivityIntent {}

/// `UsesFeature` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct UsesFeature {
    /// isRequired property.
    pub is_required: Option<bool>,
    /// name property.
    pub name: Option<String>,
}

/// `AndroidInstrumentationTest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AndroidInstrumentationTest {
    /// appApk property.
    pub app_apk: Option<FileReference>,
    /// appBundle property.
    pub app_bundle: Option<AppBundle>,
    /// appPackageId property.
    pub app_package_id: Option<String>,
    /// orchestratorOption property.
    pub orchestrator_option: Option<String>,
    /// shardingOption property.
    pub sharding_option: Option<ShardingOption>,
    /// testApk property.
    pub test_apk: Option<FileReference>,
    /// testPackageId property.
    pub test_package_id: Option<String>,
    /// testRunnerClass property.
    pub test_runner_class: Option<String>,
    /// testTargets property.
    pub test_targets: Option<Vec<String>>,
}

/// `AndroidModel` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AndroidModel {
    /// accessDeniedReasons property.
    pub access_denied_reasons: Option<Vec<String>>,
    /// brand property.
    pub brand: Option<String>,
    /// codename property.
    pub codename: Option<String>,
    /// form property.
    pub form: Option<String>,
    /// formFactor property.
    pub form_factor: Option<String>,
    /// id property.
    pub id: Option<String>,
    /// labInfo property.
    pub lab_info: Option<LabInfo>,
    /// lowFpsVideoRecording property.
    pub low_fps_video_recording: Option<bool>,
    /// manufacturer property.
    pub manufacturer: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// perVersionInfo property.
    pub per_version_info: Option<Vec<PerAndroidVersionInfo>>,
    /// screenDensity property.
    pub screen_density: Option<i64>,
    /// screenX property.
    pub screen_x: Option<i64>,
    /// screenY property.
    pub screen_y: Option<i64>,
    /// supportedAbis property.
    pub supported_abis: Option<Vec<String>>,
    /// supportedVersionIds property.
    pub supported_version_ids: Option<Vec<String>>,
    /// tags property.
    pub tags: Option<Vec<String>>,
    /// thumbnailUrl property.
    pub thumbnail_url: Option<String>,
}

/// `IosDeviceCatalog` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct IosDeviceCatalog {
    /// models property.
    pub models: Option<Vec<IosModel>>,
    /// runtimeConfiguration property.
    pub runtime_configuration: Option<IosRuntimeConfiguration>,
    /// versions property.
    pub versions: Option<Vec<IosVersion>>,
    /// xcodeVersions property.
    pub xcode_versions: Option<Vec<XcodeVersion>>,
}

/// `AndroidDevice` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AndroidDevice {
    /// androidModelId property.
    pub android_model_id: Option<String>,
    /// androidVersionId property.
    pub android_version_id: Option<String>,
    /// locale property.
    pub locale: Option<String>,
    /// orientation property.
    pub orientation: Option<String>,
}

/// `IosTestSetup` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct IosTestSetup {
    /// additionalIpas property.
    pub additional_ipas: Option<Vec<FileReference>>,
    /// networkProfile property.
    pub network_profile: Option<String>,
    /// pullDirectories property.
    pub pull_directories: Option<Vec<IosDeviceFile>>,
    /// pushFiles property.
    pub push_files: Option<Vec<IosDeviceFile>>,
}

/// `ResultStorage` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ResultStorage {
    /// googleCloudStorage property.
    pub google_cloud_storage: Option<GoogleCloudStorage>,
    /// resultsUrl property.
    pub results_url: Option<String>,
    /// toolResultsExecution property.
    pub tool_results_execution: Option<ToolResultsExecution>,
    /// toolResultsHistory property.
    pub tool_results_history: Option<ToolResultsHistory>,
}

/// `ProvidedSoftwareCatalog` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ProvidedSoftwareCatalog {
    /// androidxOrchestratorVersion property.
    pub androidx_orchestrator_version: Option<String>,
    /// orchestratorVersion property.
    pub orchestrator_version: Option<String>,
}

/// `EnvironmentVariable` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct EnvironmentVariable {
    /// key property.
    pub key: Option<String>,
    /// value property.
    pub value: Option<String>,
}

/// `LauncherActivityIntent` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct LauncherActivityIntent {}

/// `ToolResultsExecution` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ToolResultsExecution {
    /// executionId property.
    pub execution_id: Option<String>,
    /// historyId property.
    pub history_id: Option<String>,
    /// projectId property.
    pub project_id: Option<String>,
}

/// `AndroidRoboTest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AndroidRoboTest {
    /// appApk property.
    pub app_apk: Option<FileReference>,
    /// appBundle property.
    pub app_bundle: Option<AppBundle>,
    /// appInitialActivity property.
    pub app_initial_activity: Option<String>,
    /// appPackageId property.
    pub app_package_id: Option<String>,
    /// maxDepth property.
    pub max_depth: Option<i64>,
    /// maxSteps property.
    pub max_steps: Option<i64>,
    /// roboDirectives property.
    pub robo_directives: Option<Vec<RoboDirective>>,
    /// roboMode property.
    pub robo_mode: Option<String>,
    /// roboScript property.
    pub robo_script: Option<FileReference>,
    /// startingIntents property.
    pub starting_intents: Option<Vec<RoboStartingIntent>>,
}

/// `SystraceSetup` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SystraceSetup {
    /// durationSeconds property.
    pub duration_seconds: Option<i64>,
}

/// `TestSetup` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct TestSetup {
    /// account property.
    pub account: Option<Account>,
    /// additionalApks property.
    pub additional_apks: Option<Vec<Apk>>,
    /// directoriesToPull property.
    pub directories_to_pull: Option<Vec<String>>,
    /// dontAutograntPermissions property.
    pub dont_autogrant_permissions: Option<bool>,
    /// environmentVariables property.
    pub environment_variables: Option<Vec<EnvironmentVariable>>,
    /// filesToPush property.
    pub files_to_push: Option<Vec<DeviceFile>>,
    /// initialSetupApks property.
    pub initial_setup_apks: Option<Vec<Apk>>,
    /// networkProfile property.
    pub network_profile: Option<String>,
    /// systrace property.
    pub systrace: Option<SystraceSetup>,
}

/// `GetApkDetailsResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GetApkDetailsResponse {
    /// apkDetail property.
    pub apk_detail: Option<ApkDetail>,
}

/// `IosRoboTest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct IosRoboTest {
    /// appBundleId property.
    pub app_bundle_id: Option<String>,
    /// appIpa property.
    pub app_ipa: Option<FileReference>,
    /// roboScript property.
    pub robo_script: Option<FileReference>,
}

/// `SmartSharding` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SmartSharding {
    /// targetedShardDuration property.
    pub targeted_shard_duration: Option<String>,
}

/// `TestExecution` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct TestExecution {
    /// environment property.
    pub environment: Option<Environment>,
    /// id property.
    pub id: Option<String>,
    /// matrixId property.
    pub matrix_id: Option<String>,
    /// projectId property.
    pub project_id: Option<String>,
    /// shard property.
    pub shard: Option<Shard>,
    /// state property.
    pub state: Option<String>,
    /// testDetails property.
    pub test_details: Option<TestDetails>,
    /// testSpecification property.
    pub test_specification: Option<TestSpecification>,
    /// timestamp property.
    pub timestamp: Option<String>,
    /// toolResultsStep property.
    pub tool_results_step: Option<ToolResultsStep>,
}

/// `PerIosVersionInfo` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct PerIosVersionInfo {
    /// deviceCapacity property.
    pub device_capacity: Option<String>,
    /// versionId property.
    pub version_id: Option<String>,
}

/// `TestTargetsForShard` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct TestTargetsForShard {
    /// testTargets property.
    pub test_targets: Option<Vec<String>>,
}

/// `AndroidDeviceCatalog` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AndroidDeviceCatalog {
    /// models property.
    pub models: Option<Vec<AndroidModel>>,
    /// runtimeConfiguration property.
    pub runtime_configuration: Option<AndroidRuntimeConfiguration>,
    /// versions property.
    pub versions: Option<Vec<AndroidVersion>>,
}

/// `FileReference` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct FileReference {
    /// gcsPath property.
    pub gcs_path: Option<String>,
}

/// `IosDeviceFile` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct IosDeviceFile {
    /// bundleId property.
    pub bundle_id: Option<String>,
    /// content property.
    pub content: Option<FileReference>,
    /// devicePath property.
    pub device_path: Option<String>,
}

/// `DeviceIpBlock` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct DeviceIpBlock {
    /// addedDate property.
    pub added_date: Option<Date>,
    /// block property.
    pub block: Option<String>,
    /// form property.
    pub form: Option<String>,
}

/// `TestMatrix` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct TestMatrix {
    /// clientInfo property.
    pub client_info: Option<ClientInfo>,
    /// environmentMatrix property.
    pub environment_matrix: Option<EnvironmentMatrix>,
    /// extendedInvalidMatrixDetails property.
    pub extended_invalid_matrix_details: Option<Vec<MatrixErrorDetail>>,
    /// failFast property.
    pub fail_fast: Option<bool>,
    /// flakyTestAttempts property.
    pub flaky_test_attempts: Option<i64>,
    /// invalidMatrixDetails property.
    pub invalid_matrix_details: Option<String>,
    /// outcomeSummary property.
    pub outcome_summary: Option<String>,
    /// projectId property.
    pub project_id: Option<String>,
    /// resultStorage property.
    pub result_storage: Option<ResultStorage>,
    /// state property.
    pub state: Option<String>,
    /// testExecutions property.
    pub test_executions: Option<Vec<TestExecution>>,
    /// testMatrixId property.
    pub test_matrix_id: Option<String>,
    /// testSpecification property.
    pub test_specification: Option<TestSpecification>,
    /// timestamp property.
    pub timestamp: Option<String>,
}

/// `ApkDetail` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ApkDetail {
    /// apkManifest property.
    pub apk_manifest: Option<ApkManifest>,
}

/// `ObbFile` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ObbFile {
    /// obb property.
    pub obb: Option<FileReference>,
    /// obbFileName property.
    pub obb_file_name: Option<String>,
}

/// `UniformSharding` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct UniformSharding {
    /// numShards property.
    pub num_shards: Option<i64>,
}

/// `LabInfo` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct LabInfo {
    /// name property.
    pub name: Option<String>,
    /// regionCode property.
    pub region_code: Option<String>,
}

/// `IntentFilter` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct IntentFilter {
    /// actionNames property.
    pub action_names: Option<Vec<String>>,
    /// categoryNames property.
    pub category_names: Option<Vec<String>>,
    /// mimeType property.
    pub mime_type: Option<String>,
}

/// `ClientInfoDetail` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ClientInfoDetail {
    /// key property.
    pub key: Option<String>,
    /// value property.
    pub value: Option<String>,
}

// =============================================================================
// ARGS TYPES (per-endpoint)
// =============================================================================

/// Arguments for [`testing.applicationDetailService.getApkDetails_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct TestingApplicationDetailServiceGetApkDetailsArgs {
    /// Query parameter: `bundleLocation.gcsPath`.
    pub bundle_location_gcs_path: Option<String>,
    /// Request body.
    pub body: FileReference,
}

/// Arguments for [`testing.projects.deviceSessions.cancel_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct TestingProjectsDeviceSessionsCancelArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Request body.
    pub body: CancelDeviceSessionRequest,
}

/// Arguments for [`testing.projects.deviceSessions.create_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct TestingProjectsDeviceSessionsCreateArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Request body.
    pub body: DeviceSession,
}

/// Arguments for [`testing.projects.deviceSessions.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct TestingProjectsDeviceSessionsGetArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`testing.projects.deviceSessions.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct TestingProjectsDeviceSessionsListArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

/// Arguments for [`testing.projects.deviceSessions.patch_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct TestingProjectsDeviceSessionsPatchArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `updateMask`.
    pub update_mask: Option<String>,
    /// Request body.
    pub body: DeviceSession,
}

/// Arguments for [`testing.projects.testMatrices.cancel_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct TestingProjectsTestMatricesCancelArgs {
    /// Path parameter: `projectId`.
    pub project_id: String,
    /// Path parameter: `testMatrixId`.
    pub test_matrix_id: String,
}

/// Arguments for [`testing.projects.testMatrices.create_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct TestingProjectsTestMatricesCreateArgs {
    /// Path parameter: `projectId`.
    pub project_id: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: TestMatrix,
}

/// Arguments for [`testing.projects.testMatrices.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct TestingProjectsTestMatricesGetArgs {
    /// Path parameter: `projectId`.
    pub project_id: String,
    /// Path parameter: `testMatrixId`.
    pub test_matrix_id: String,
}

/// Arguments for [`testing.testEnvironmentCatalog.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct TestingTestEnvironmentCatalogGetArgs {
    /// Path parameter: `environmentType`.
    pub environment_type: String,
    /// Query parameter: `includeViewableModels`.
    pub include_viewable_models: Option<String>,
    /// Query parameter: `projectId`.
    pub project_id: Option<String>,
}

// =============================================================================
// CLIENT FUNCTIONS (per-endpoint)
// =============================================================================

// -----------------------------------------------------------------------------
// POST v1/applicationDetailService/getApkDetails
// -----------------------------------------------------------------------------

/// POST v1/applicationDetailService/getApkDetails.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = testing_application_detail_service_get_apk_details_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn testing_application_detail_service_get_apk_details_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &TestingApplicationDetailServiceGetApkDetailsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GetApkDetailsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url =
        format!("https://testing.googleapis.com/v1/applicationDetailService/getApkDetails",);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.bundle_location_gcs_path {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("bundleLocation_gcsPath=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GetApkDetailsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/{+name}:cancel
// -----------------------------------------------------------------------------

/// POST v1/{+name}:cancel.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = testing_projects_device_sessions_cancel_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn testing_projects_device_sessions_cancel_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &TestingProjectsDeviceSessionsCancelArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://testing.googleapis.com/v1/{}:cancel", args.name,);

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Empty =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/{+parent}/deviceSessions
// -----------------------------------------------------------------------------

/// POST v1/{+parent}/deviceSessions.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = testing_projects_device_sessions_create_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn testing_projects_device_sessions_create_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &TestingProjectsDeviceSessionsCreateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<DeviceSession>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://testing.googleapis.com/v1/{}/deviceSessions",
        args.parent,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: DeviceSession =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/{+name}
// -----------------------------------------------------------------------------

/// GET v1/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = testing_projects_device_sessions_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn testing_projects_device_sessions_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &TestingProjectsDeviceSessionsGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<DeviceSession>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://testing.googleapis.com/v1/{}", args.name,);

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: DeviceSession =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/{+parent}/deviceSessions
// -----------------------------------------------------------------------------

/// GET v1/{+parent}/deviceSessions.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = testing_projects_device_sessions_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn testing_projects_device_sessions_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &TestingProjectsDeviceSessionsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListDeviceSessionsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://testing.googleapis.com/v1/{}/deviceSessions",
        args.parent,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ListDeviceSessionsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PATCH v1/{+name}
// -----------------------------------------------------------------------------

/// PATCH v1/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = testing_projects_device_sessions_patch_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn testing_projects_device_sessions_patch_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &TestingProjectsDeviceSessionsPatchArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<DeviceSession>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://testing.googleapis.com/v1/{}", args.name,);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.update_mask {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("updateMask=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .patch(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: DeviceSession =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/projects/{projectId}/testMatrices/{testMatrixId}:cancel
// -----------------------------------------------------------------------------

/// POST v1/projects/{projectId}/testMatrices/{testMatrixId}:cancel.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = testing_projects_test_matrices_cancel_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn testing_projects_test_matrices_cancel_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &TestingProjectsTestMatricesCancelArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<CancelTestMatrixResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://testing.googleapis.com/v1/projects/{}/testMatrices/{}:cancel",
        args.project_id, args.test_matrix_id,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: CancelTestMatrixResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/projects/{projectId}/testMatrices
// -----------------------------------------------------------------------------

/// POST v1/projects/{projectId}/testMatrices.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = testing_projects_test_matrices_create_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn testing_projects_test_matrices_create_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &TestingProjectsTestMatricesCreateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TestMatrix>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://testing.googleapis.com/v1/projects/{}/testMatrices",
        args.project_id,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: TestMatrix =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/projects/{projectId}/testMatrices/{testMatrixId}
// -----------------------------------------------------------------------------

/// GET v1/projects/{projectId}/testMatrices/{testMatrixId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = testing_projects_test_matrices_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn testing_projects_test_matrices_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &TestingProjectsTestMatricesGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TestMatrix>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://testing.googleapis.com/v1/projects/{}/testMatrices/{}",
        args.project_id, args.test_matrix_id,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: TestMatrix =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/testEnvironmentCatalog/{environmentType}
// -----------------------------------------------------------------------------

/// GET v1/testEnvironmentCatalog/{environmentType}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = testing_test_environment_catalog_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn testing_test_environment_catalog_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &TestingTestEnvironmentCatalogGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TestEnvironmentCatalog>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://testing.googleapis.com/v1/testEnvironmentCatalog/{}",
        args.environment_type,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.include_viewable_models {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("includeViewableModels=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.project_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("projectId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: TestEnvironmentCatalog =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}
