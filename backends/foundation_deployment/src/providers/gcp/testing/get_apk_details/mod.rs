//! Auto-generated API module for gcp/testing getApkDetails.
//!
//! Generated by `cargo run --bin ewe_platform gen_api`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp_testing_get_apk_details `

#![cfg(feature = "gcp_testing_get_apk_details")]
#![allow(clippy::too_many_arguments, clippy::type_complexity)]
#![allow(
    clippy::missing_errors_doc,
    clippy::doc_markdown,
    clippy::useless_format
)]

use foundation_core::valtron::{execute, StreamIterator, TaskIterator, TaskIteratorExt};
use foundation_core::wire::simple_http::client::{ClientRequestBuilder, SimpleHttpClient};
use foundation_macros::JsonHash;
use serde::{Deserialize, Serialize};

use super::shared::{ApiError, ApiPending, ApiResponse};

// =============================================================================
// TYPE DECLARATIONS
// =============================================================================

/// `UsesPermissionTag` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct UsesPermissionTag {
    /// maxSdkVersion property.
    pub max_sdk_version: Option<i64>,
    /// name property.
    pub name: Option<String>,
}

/// `UsesFeature` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct UsesFeature {
    /// isRequired property.
    pub is_required: Option<bool>,
    /// name property.
    pub name: Option<String>,
}

/// `IntentFilter` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct IntentFilter {
    /// actionNames property.
    pub action_names: Option<Vec<String>>,
    /// categoryNames property.
    pub category_names: Option<Vec<String>>,
    /// mimeType property.
    pub mime_type: Option<String>,
}

/// `Metadata` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Metadata {
    /// name property.
    pub name: Option<String>,
    /// value property.
    pub value: Option<String>,
}

/// `GetApkDetailsResponse` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct GetApkDetailsResponse {
    /// apkDetail property.
    pub apk_detail: Option<ApkDetail>,
}

/// `ApkManifest` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ApkManifest {
    /// applicationLabel property.
    pub application_label: Option<String>,
    /// intentFilters property.
    pub intent_filters: Option<Vec<IntentFilter>>,
    /// maxSdkVersion property.
    pub max_sdk_version: Option<i64>,
    /// metadata property.
    pub metadata: Option<Vec<Metadata>>,
    /// minSdkVersion property.
    pub min_sdk_version: Option<i64>,
    /// packageName property.
    pub package_name: Option<String>,
    /// services property.
    pub services: Option<Vec<Service>>,
    /// targetSdkVersion property.
    pub target_sdk_version: Option<i64>,
    /// usesFeature property.
    pub uses_feature: Option<Vec<UsesFeature>>,
    /// usesPermission property.
    pub uses_permission: Option<Vec<String>>,
    /// usesPermissionTags property.
    pub uses_permission_tags: Option<Vec<UsesPermissionTag>>,
    /// versionCode property.
    pub version_code: Option<String>,
    /// versionName property.
    pub version_name: Option<String>,
}

/// `ApkDetail` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ApkDetail {
    /// apkManifest property.
    pub apk_manifest: Option<ApkManifest>,
}

/// `Service` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Service {
    /// intentFilter property.
    pub intent_filter: Option<Vec<IntentFilter>>,
    /// name property.
    pub name: Option<String>,
}

// =============================================================================
// ARGS TYPES (per-endpoint)
// =============================================================================

/// Arguments for [`testing.applicationDetailService.getApkDetails_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct TestingApplicationDetailServiceGetApkDetailsArgs {
    /// Query parameter: `bundleLocation.gcsPath`.
    pub bundle_location_gcs_path: Option<String>,
}

// =============================================================================
// CLIENT FUNCTIONS (per-endpoint)
// =============================================================================

// -----------------------------------------------------------------------------
// POST v1/applicationDetailService/getApkDetails
// -----------------------------------------------------------------------------

/// POST v1/applicationDetailService/getApkDetails.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = testing_application_detail_service_get_apk_details_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn testing_application_detail_service_get_apk_details_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GetApkDetailsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url =
        format!("https://testing.googleapis.com/v1/applicationDetailService/getApkDetails",);

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GetApkDetailsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}
