//! Auto-generated API module for gcp/looker instances.
//!
//! Generated by `cargo run --bin ewe_platform gen_api`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp_looker_instances `

#![cfg(feature = "gcp_looker_instances")]
#![allow(clippy::too_many_arguments, clippy::type_complexity)]
#![allow(
    clippy::missing_errors_doc,
    clippy::doc_markdown,
    clippy::useless_format
)]

use foundation_core::valtron::{execute, StreamIterator, TaskIterator, TaskIteratorExt};
use foundation_core::wire::simple_http::client::{ClientRequestBuilder, SimpleHttpClient};
use foundation_macros::JsonHash;
use serde::{Deserialize, Serialize};

// Import shared types used by this module
use super::shared::Operation;

use super::shared::{ApiError, ApiPending, ApiResponse};

// =============================================================================
// TYPE DECLARATIONS
// =============================================================================

/// `PscConfig` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct PscConfig {
    /// allowedVpcs property.
    pub allowed_vpcs: Option<Vec<String>>,
    /// lookerServiceAttachmentUri property.
    pub looker_service_attachment_uri: Option<String>,
    /// serviceAttachments property.
    pub service_attachments: Option<Vec<ServiceAttachment>>,
}

/// `MaintenanceWindow` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct MaintenanceWindow {
    /// dayOfWeek property.
    pub day_of_week: Option<String>,
    /// startTime property.
    pub start_time: Option<TimeOfDay>,
}

/// `UserMetadata` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct UserMetadata {
    /// additionalDeveloperUserCount property.
    pub additional_developer_user_count: Option<i64>,
    /// additionalStandardUserCount property.
    pub additional_standard_user_count: Option<i64>,
    /// additionalViewerUserCount property.
    pub additional_viewer_user_count: Option<i64>,
}

/// `IngressIpAllowlistRule` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct IngressIpAllowlistRule {
    /// description property.
    pub description: Option<String>,
    /// ipRange property.
    pub ip_range: Option<String>,
}

/// `DenyMaintenancePeriod` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct DenyMaintenancePeriod {
    /// endDate property.
    pub end_date: Option<Date>,
    /// startDate property.
    pub start_date: Option<Date>,
    /// time property.
    pub time: Option<TimeOfDay>,
}

/// `MaintenanceSchedule` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct MaintenanceSchedule {
    /// endTime property.
    pub end_time: Option<String>,
    /// startTime property.
    pub start_time: Option<String>,
}

/// `ControlledEgressConfig` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ControlledEgressConfig {
    /// egressFqdns property.
    pub egress_fqdns: Option<Vec<String>>,
    /// marketplaceEnabled property.
    pub marketplace_enabled: Option<bool>,
    /// webProxyIps property.
    pub web_proxy_ips: Option<Vec<String>>,
}

/// `CustomDomain` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct CustomDomain {
    /// domain property.
    pub domain: Option<String>,
    /// state property.
    pub state: Option<String>,
}

/// `OAuthConfig` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct OAuthConfig {
    /// clientId property.
    pub client_id: Option<String>,
    /// clientSecret property.
    pub client_secret: Option<String>,
    /// sharedOauthClientEnabled property.
    pub shared_oauth_client_enabled: Option<bool>,
}

/// `Status` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Status {
    /// code property.
    pub code: Option<i64>,
    /// details property.
    pub details: Option<Vec<serde_json::Value>>,
    /// message property.
    pub message: Option<String>,
}

/// `EncryptionConfig` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct EncryptionConfig {
    /// kmsKeyName property.
    pub kms_key_name: Option<String>,
    /// kmsKeyNameVersion property.
    pub kms_key_name_version: Option<String>,
    /// kmsKeyState property.
    pub kms_key_state: Option<String>,
}

/// `IngressIpAllowlistConfig` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct IngressIpAllowlistConfig {
    /// allowlistRules property.
    pub allowlist_rules: Option<Vec<IngressIpAllowlistRule>>,
    /// enabled property.
    pub enabled: Option<bool>,
    /// googleServicesEnabled property.
    pub google_services_enabled: Option<bool>,
}

/// `Date` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Date {
    /// day property.
    pub day: Option<i64>,
    /// month property.
    pub month: Option<i64>,
    /// year property.
    pub year: Option<i64>,
}

/// `Instance` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Instance {
    /// adminSettings property.
    pub admin_settings: Option<AdminSettings>,
    /// catalogIntegrationOptOut property.
    pub catalog_integration_opt_out: Option<bool>,
    /// classType property.
    pub class_type: Option<String>,
    /// consumerNetwork property.
    pub consumer_network: Option<String>,
    /// controlledEgressConfig property.
    pub controlled_egress_config: Option<ControlledEgressConfig>,
    /// controlledEgressEnabled property.
    pub controlled_egress_enabled: Option<bool>,
    /// createTime property.
    pub create_time: Option<String>,
    /// customDomain property.
    pub custom_domain: Option<CustomDomain>,
    /// denyMaintenancePeriod property.
    pub deny_maintenance_period: Option<DenyMaintenancePeriod>,
    /// egressPublicIp property.
    pub egress_public_ip: Option<String>,
    /// encryptionConfig property.
    pub encryption_config: Option<EncryptionConfig>,
    /// fipsEnabled property.
    pub fips_enabled: Option<bool>,
    /// geminiEnabled property.
    pub gemini_enabled: Option<bool>,
    /// ingressIpAllowlistConfig property.
    pub ingress_ip_allowlist_config: Option<IngressIpAllowlistConfig>,
    /// ingressPrivateIp property.
    pub ingress_private_ip: Option<String>,
    /// ingressPublicIp property.
    pub ingress_public_ip: Option<String>,
    /// lastDenyMaintenancePeriod property.
    pub last_deny_maintenance_period: Option<DenyMaintenancePeriod>,
    /// linkedLspProjectNumber property.
    pub linked_lsp_project_number: Option<String>,
    /// lookerUri property.
    pub looker_uri: Option<String>,
    /// lookerVersion property.
    pub looker_version: Option<String>,
    /// maintenanceSchedule property.
    pub maintenance_schedule: Option<MaintenanceSchedule>,
    /// maintenanceWindow property.
    pub maintenance_window: Option<MaintenanceWindow>,
    /// name property.
    pub name: Option<String>,
    /// oauthConfig property.
    pub oauth_config: Option<OAuthConfig>,
    /// periodicExportConfig property.
    pub periodic_export_config: Option<PeriodicExportConfig>,
    /// platformEdition property.
    pub platform_edition: Option<String>,
    /// privateIpEnabled property.
    pub private_ip_enabled: Option<bool>,
    /// pscConfig property.
    pub psc_config: Option<PscConfig>,
    /// pscEnabled property.
    pub psc_enabled: Option<bool>,
    /// publicIpEnabled property.
    pub public_ip_enabled: Option<bool>,
    /// reservedRange property.
    pub reserved_range: Option<String>,
    /// satisfiesPzi property.
    pub satisfies_pzi: Option<bool>,
    /// satisfiesPzs property.
    pub satisfies_pzs: Option<bool>,
    /// state property.
    pub state: Option<String>,
    /// updateTime property.
    pub update_time: Option<String>,
    /// userMetadata property.
    pub user_metadata: Option<UserMetadata>,
}

/// `ServiceAttachment` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ServiceAttachment {
    /// connectionStatus property.
    pub connection_status: Option<String>,
    /// failureReason property.
    pub failure_reason: Option<String>,
    /// localFqdn property.
    pub local_fqdn: Option<String>,
    /// localFqdns property.
    pub local_fqdns: Option<Vec<String>>,
    /// targetServiceAttachmentUri property.
    pub target_service_attachment_uri: Option<String>,
}

/// `TimeOfDay` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct TimeOfDay {
    /// hours property.
    pub hours: Option<i64>,
    /// minutes property.
    pub minutes: Option<i64>,
    /// nanos property.
    pub nanos: Option<i64>,
    /// seconds property.
    pub seconds: Option<i64>,
}

/// `PeriodicExportConfig` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct PeriodicExportConfig {
    /// gcsUri property.
    pub gcs_uri: Option<String>,
    /// kmsKey property.
    pub kms_key: Option<String>,
    /// startTime property.
    pub start_time: Option<TimeOfDay>,
}

/// `ListInstancesResponse` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ListInstancesResponse {
    /// instances property.
    pub instances: Option<Vec<Instance>>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// unreachable property.
    pub unreachable: Option<Vec<String>>,
}

/// `AdminSettings` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct AdminSettings {
    /// allowedEmailDomains property.
    pub allowed_email_domains: Option<Vec<String>>,
}

// =============================================================================
// ARGS TYPES (per-endpoint)
// =============================================================================

/// Arguments for [`looker.projects.locations.instances.create_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LookerProjectsLocationsInstancesCreateArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `instanceId`.
    pub instance_id: Option<String>,
}

/// Arguments for [`looker.projects.locations.instances.delete_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LookerProjectsLocationsInstancesDeleteArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `force`.
    pub force: Option<String>,
}

/// Arguments for [`looker.projects.locations.instances.export_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LookerProjectsLocationsInstancesExportArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`looker.projects.locations.instances.get_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LookerProjectsLocationsInstancesGetArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`looker.projects.locations.instances.import_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LookerProjectsLocationsInstancesImportArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`looker.projects.locations.instances.list_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LookerProjectsLocationsInstancesListArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

/// Arguments for [`looker.projects.locations.instances.patch_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LookerProjectsLocationsInstancesPatchArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `updateMask`.
    pub update_mask: Option<String>,
}

/// Arguments for [`looker.projects.locations.instances.restart_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LookerProjectsLocationsInstancesRestartArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`looker.projects.locations.instances.restore_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LookerProjectsLocationsInstancesRestoreArgs {
    /// Path parameter: `name`.
    pub name: String,
}

// =============================================================================
// CLIENT FUNCTIONS (per-endpoint)
// =============================================================================

// -----------------------------------------------------------------------------
// POST v1/projects/{projectsId}/locations/{locationsId}/instances
// -----------------------------------------------------------------------------

/// POST v1/projects/{projectsId}/locations/{locationsId}/instances.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = looker_projects_locations_instances_create_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn looker_projects_locations_instances_create_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://looker.googleapis.com/v1/projects/{{projectsId}}/locations/{{locationsId}}/instances",
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE v1/projects/{projectsId}/locations/{locationsId}/instances/{instancesId}
// -----------------------------------------------------------------------------

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/instances/{instancesId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = looker_projects_locations_instances_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn looker_projects_locations_instances_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://looker.googleapis.com/v1/projects/{{projectsId}}/locations/{{locationsId}}/instances/{{instancesId}}",
    );

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/projects/{projectsId}/locations/{locationsId}/instances/{instancesId}:export
// -----------------------------------------------------------------------------

/// POST v1/projects/{projectsId}/locations/{locationsId}/instances/{instancesId}:export.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = looker_projects_locations_instances_export_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn looker_projects_locations_instances_export_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://looker.googleapis.com/v1/projects/{{projectsId}}/locations/{{locationsId}}/instances/{{instancesId}}:export",
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/projects/{projectsId}/locations/{locationsId}/instances/{instancesId}
// -----------------------------------------------------------------------------

/// GET v1/projects/{projectsId}/locations/{locationsId}/instances/{instancesId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = looker_projects_locations_instances_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn looker_projects_locations_instances_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Instance>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://looker.googleapis.com/v1/projects/{{projectsId}}/locations/{{locationsId}}/instances/{{instancesId}}",
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Instance =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/projects/{projectsId}/locations/{locationsId}/instances/{instancesId}:import
// -----------------------------------------------------------------------------

/// POST v1/projects/{projectsId}/locations/{locationsId}/instances/{instancesId}:import.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = looker_projects_locations_instances_import_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn looker_projects_locations_instances_import_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://looker.googleapis.com/v1/projects/{{projectsId}}/locations/{{locationsId}}/instances/{{instancesId}}:import",
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/projects/{projectsId}/locations/{locationsId}/instances
// -----------------------------------------------------------------------------

/// GET v1/projects/{projectsId}/locations/{locationsId}/instances.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = looker_projects_locations_instances_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn looker_projects_locations_instances_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListInstancesResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://looker.googleapis.com/v1/projects/{{projectsId}}/locations/{{locationsId}}/instances",
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ListInstancesResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PATCH v1/projects/{projectsId}/locations/{locationsId}/instances/{instancesId}
// -----------------------------------------------------------------------------

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/instances/{instancesId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = looker_projects_locations_instances_patch_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn looker_projects_locations_instances_patch_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://looker.googleapis.com/v1/projects/{{projectsId}}/locations/{{locationsId}}/instances/{{instancesId}}",
    );

    let mut builder = client
        .patch(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/projects/{projectsId}/locations/{locationsId}/instances/{instancesId}:restart
// -----------------------------------------------------------------------------

/// POST v1/projects/{projectsId}/locations/{locationsId}/instances/{instancesId}:restart.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = looker_projects_locations_instances_restart_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn looker_projects_locations_instances_restart_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://looker.googleapis.com/v1/projects/{{projectsId}}/locations/{{locationsId}}/instances/{{instancesId}}:restart",
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/projects/{projectsId}/locations/{locationsId}/instances/{instancesId}:restore
// -----------------------------------------------------------------------------

/// POST v1/projects/{projectsId}/locations/{locationsId}/instances/{instancesId}:restore.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = looker_projects_locations_instances_restore_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn looker_projects_locations_instances_restore_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://looker.googleapis.com/v1/projects/{{projectsId}}/locations/{{locationsId}}/instances/{{instancesId}}:restore",
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}
