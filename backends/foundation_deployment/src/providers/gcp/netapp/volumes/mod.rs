//! Auto-generated API module for gcp/netapp volumes.
//!
//! Generated by `cargo run --bin ewe_platform gen_api`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp_netapp_volumes `

#![cfg(feature = "gcp_netapp_volumes")]
#![allow(clippy::too_many_arguments, clippy::type_complexity)]
#![allow(
    clippy::missing_errors_doc,
    clippy::doc_markdown,
    clippy::useless_format
)]

use foundation_core::valtron::{execute, StreamIterator, TaskIterator, TaskIteratorExt};
use foundation_core::wire::simple_http::client::{ClientRequestBuilder, SimpleHttpClient};
use foundation_macros::JsonHash;
use serde::{Deserialize, Serialize};

// Import shared types used by this module
use super::shared::Operation;

use super::shared::{ApiError, ApiPending, ApiResponse};

// =============================================================================
// TYPE DECLARATIONS
// =============================================================================

/// `WeeklySchedule` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct WeeklySchedule {
    /// day property.
    pub day: Option<String>,
    /// hour property.
    pub hour: Option<f64>,
    /// minute property.
    pub minute: Option<f64>,
    /// snapshotsToKeep property.
    pub snapshots_to_keep: Option<f64>,
}

/// `CacheConfig` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct CacheConfig {
    /// cachePrePopulate property.
    pub cache_pre_populate: Option<CachePrePopulate>,
    /// cachePrePopulateState property.
    pub cache_pre_populate_state: Option<String>,
    /// cifsChangeNotifyEnabled property.
    pub cifs_change_notify_enabled: Option<bool>,
    /// writebackEnabled property.
    pub writeback_enabled: Option<bool>,
}

/// `DailySchedule` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct DailySchedule {
    /// hour property.
    pub hour: Option<f64>,
    /// minute property.
    pub minute: Option<f64>,
    /// snapshotsToKeep property.
    pub snapshots_to_keep: Option<f64>,
}

/// `Volume` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Volume {
    /// activeDirectory property.
    pub active_directory: Option<String>,
    /// backupConfig property.
    pub backup_config: Option<BackupConfig>,
    /// blockDevices property.
    pub block_devices: Option<Vec<BlockDevice>>,
    /// cacheParameters property.
    pub cache_parameters: Option<CacheParameters>,
    /// capacityGib property.
    pub capacity_gib: Option<String>,
    /// cloneDetails property.
    pub clone_details: Option<CloneDetails>,
    /// coldTierSizeGib property.
    pub cold_tier_size_gib: Option<String>,
    /// createTime property.
    pub create_time: Option<String>,
    /// description property.
    pub description: Option<String>,
    /// encryptionType property.
    pub encryption_type: Option<String>,
    /// exportPolicy property.
    pub export_policy: Option<ExportPolicy>,
    /// hasReplication property.
    pub has_replication: Option<bool>,
    /// hotTierSizeUsedGib property.
    pub hot_tier_size_used_gib: Option<String>,
    /// hybridReplicationParameters property.
    pub hybrid_replication_parameters: Option<HybridReplicationParameters>,
    /// kerberosEnabled property.
    pub kerberos_enabled: Option<bool>,
    /// kmsConfig property.
    pub kms_config: Option<String>,
    /// labels property.
    pub labels: Option<serde_json::Value>,
    /// largeCapacity property.
    pub large_capacity: Option<bool>,
    /// largeCapacityConfig property.
    pub large_capacity_config: Option<LargeCapacityConfig>,
    /// ldapEnabled property.
    pub ldap_enabled: Option<bool>,
    /// mountOptions property.
    pub mount_options: Option<Vec<MountOption>>,
    /// multipleEndpoints property.
    pub multiple_endpoints: Option<bool>,
    /// name property.
    pub name: Option<String>,
    /// network property.
    pub network: Option<String>,
    /// protocols property.
    pub protocols: Option<Vec<String>>,
    /// psaRange property.
    pub psa_range: Option<String>,
    /// replicaZone property.
    pub replica_zone: Option<String>,
    /// restoreParameters property.
    pub restore_parameters: Option<RestoreParameters>,
    /// restrictedActions property.
    pub restricted_actions: Option<Vec<String>>,
    /// securityStyle property.
    pub security_style: Option<String>,
    /// serviceLevel property.
    pub service_level: Option<String>,
    /// shareName property.
    pub share_name: Option<String>,
    /// smbSettings property.
    pub smb_settings: Option<Vec<String>>,
    /// snapReserve property.
    pub snap_reserve: Option<f64>,
    /// snapshotDirectory property.
    pub snapshot_directory: Option<bool>,
    /// snapshotPolicy property.
    pub snapshot_policy: Option<SnapshotPolicy>,
    /// state property.
    pub state: Option<String>,
    /// stateDetails property.
    pub state_details: Option<String>,
    /// storagePool property.
    pub storage_pool: Option<String>,
    /// throughputMibps property.
    pub throughput_mibps: Option<f64>,
    /// tieringPolicy property.
    pub tiering_policy: Option<TieringPolicy>,
    /// unixPermissions property.
    pub unix_permissions: Option<String>,
    /// usedGib property.
    pub used_gib: Option<String>,
    /// zone property.
    pub zone: Option<String>,
}

/// `CloneDetails` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct CloneDetails {
    /// sharedSpaceGib property.
    pub shared_space_gib: Option<String>,
    /// sourceSnapshot property.
    pub source_snapshot: Option<String>,
    /// sourceVolume property.
    pub source_volume: Option<String>,
}

/// `BlockDevice` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct BlockDevice {
    /// hostGroups property.
    pub host_groups: Option<Vec<String>>,
    /// identifier property.
    pub identifier: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// osType property.
    pub os_type: Option<String>,
    /// sizeGib property.
    pub size_gib: Option<String>,
}

/// `MountOption` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct MountOption {
    /// export property.
    pub export: Option<String>,
    /// exportFull property.
    pub export_full: Option<String>,
    /// instructions property.
    pub instructions: Option<String>,
    /// ipAddress property.
    pub ip_address: Option<String>,
    /// protocol property.
    pub protocol: Option<String>,
}

/// `CachePrePopulate` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct CachePrePopulate {
    /// excludePathList property.
    pub exclude_path_list: Option<Vec<String>>,
    /// pathList property.
    pub path_list: Option<Vec<String>>,
    /// recursion property.
    pub recursion: Option<bool>,
}

/// `HybridReplicationParameters` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct HybridReplicationParameters {
    /// clusterLocation property.
    pub cluster_location: Option<String>,
    /// description property.
    pub description: Option<String>,
    /// hybridReplicationType property.
    pub hybrid_replication_type: Option<String>,
    /// labels property.
    pub labels: Option<serde_json::Value>,
    /// largeVolumeConstituentCount property.
    pub large_volume_constituent_count: Option<i64>,
    /// peerClusterName property.
    pub peer_cluster_name: Option<String>,
    /// peerIpAddresses property.
    pub peer_ip_addresses: Option<Vec<String>>,
    /// peerSvmName property.
    pub peer_svm_name: Option<String>,
    /// peerVolumeName property.
    pub peer_volume_name: Option<String>,
    /// replication property.
    pub replication: Option<String>,
    /// replicationSchedule property.
    pub replication_schedule: Option<String>,
}

/// `BackupConfig` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct BackupConfig {
    /// backupChainBytes property.
    pub backup_chain_bytes: Option<String>,
    /// backupPolicies property.
    pub backup_policies: Option<Vec<String>>,
    /// backupVault property.
    pub backup_vault: Option<String>,
    /// scheduledBackupEnabled property.
    pub scheduled_backup_enabled: Option<bool>,
}

/// `LargeCapacityConfig` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct LargeCapacityConfig {
    /// constituentCount property.
    pub constituent_count: Option<i64>,
}

/// `SimpleExportPolicyRule` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct SimpleExportPolicyRule {
    /// accessType property.
    pub access_type: Option<String>,
    /// allowedClients property.
    pub allowed_clients: Option<String>,
    /// anonUid property.
    pub anon_uid: Option<String>,
    /// hasRootAccess property.
    pub has_root_access: Option<String>,
    /// kerberos5ReadOnly property.
    pub kerberos5_read_only: Option<bool>,
    /// kerberos5ReadWrite property.
    pub kerberos5_read_write: Option<bool>,
    /// kerberos5iReadOnly property.
    pub kerberos5i_read_only: Option<bool>,
    /// kerberos5iReadWrite property.
    pub kerberos5i_read_write: Option<bool>,
    /// kerberos5pReadOnly property.
    pub kerberos5p_read_only: Option<bool>,
    /// kerberos5pReadWrite property.
    pub kerberos5p_read_write: Option<bool>,
    /// nfsv3 property.
    pub nfsv3: Option<bool>,
    /// nfsv4 property.
    pub nfsv4: Option<bool>,
    /// squashMode property.
    pub squash_mode: Option<String>,
}

/// `ListVolumesResponse` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ListVolumesResponse {
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// unreachable property.
    pub unreachable: Option<Vec<String>>,
    /// volumes property.
    pub volumes: Option<Vec<Volume>>,
}

/// `ExportPolicy` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ExportPolicy {
    /// rules property.
    pub rules: Option<Vec<SimpleExportPolicyRule>>,
}

/// `RestoreParameters` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct RestoreParameters {
    /// sourceBackup property.
    pub source_backup: Option<String>,
    /// sourceSnapshot property.
    pub source_snapshot: Option<String>,
}

/// `CacheParameters` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct CacheParameters {
    /// cacheConfig property.
    pub cache_config: Option<CacheConfig>,
    /// cacheState property.
    pub cache_state: Option<String>,
    /// command property.
    pub command: Option<String>,
    /// enableGlobalFileLock property.
    pub enable_global_file_lock: Option<bool>,
    /// passphrase property.
    pub passphrase: Option<String>,
    /// peerClusterName property.
    pub peer_cluster_name: Option<String>,
    /// peerIpAddresses property.
    pub peer_ip_addresses: Option<Vec<String>>,
    /// peerSvmName property.
    pub peer_svm_name: Option<String>,
    /// peerVolumeName property.
    pub peer_volume_name: Option<String>,
    /// peeringCommandExpiryTime property.
    pub peering_command_expiry_time: Option<String>,
    /// stateDetails property.
    pub state_details: Option<String>,
}

/// `SnapshotPolicy` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct SnapshotPolicy {
    /// dailySchedule property.
    pub daily_schedule: Option<DailySchedule>,
    /// enabled property.
    pub enabled: Option<bool>,
    /// hourlySchedule property.
    pub hourly_schedule: Option<HourlySchedule>,
    /// monthlySchedule property.
    pub monthly_schedule: Option<MonthlySchedule>,
    /// weeklySchedule property.
    pub weekly_schedule: Option<WeeklySchedule>,
}

/// `TieringPolicy` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct TieringPolicy {
    /// coolingThresholdDays property.
    pub cooling_threshold_days: Option<i64>,
    /// hotTierBypassModeEnabled property.
    pub hot_tier_bypass_mode_enabled: Option<bool>,
    /// tierAction property.
    pub tier_action: Option<String>,
}

/// `HourlySchedule` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct HourlySchedule {
    /// minute property.
    pub minute: Option<f64>,
    /// snapshotsToKeep property.
    pub snapshots_to_keep: Option<f64>,
}

/// `Status` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Status {
    /// code property.
    pub code: Option<i64>,
    /// details property.
    pub details: Option<Vec<serde_json::Value>>,
    /// message property.
    pub message: Option<String>,
}

/// `MonthlySchedule` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct MonthlySchedule {
    /// daysOfMonth property.
    pub days_of_month: Option<String>,
    /// hour property.
    pub hour: Option<f64>,
    /// minute property.
    pub minute: Option<f64>,
    /// snapshotsToKeep property.
    pub snapshots_to_keep: Option<f64>,
}

// =============================================================================
// ARGS TYPES (per-endpoint)
// =============================================================================

/// Arguments for [`netapp.projects.locations.volumes.create_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetappProjectsLocationsVolumesCreateArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `volumeId`.
    pub volume_id: Option<String>,
}

/// Arguments for [`netapp.projects.locations.volumes.delete_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetappProjectsLocationsVolumesDeleteArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `force`.
    pub force: Option<String>,
}

/// Arguments for [`netapp.projects.locations.volumes.establishPeering_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetappProjectsLocationsVolumesEstablishPeeringArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`netapp.projects.locations.volumes.get_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetappProjectsLocationsVolumesGetArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`netapp.projects.locations.volumes.list_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetappProjectsLocationsVolumesListArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

/// Arguments for [`netapp.projects.locations.volumes.patch_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetappProjectsLocationsVolumesPatchArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `updateMask`.
    pub update_mask: Option<String>,
}

/// Arguments for [`netapp.projects.locations.volumes.restore_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetappProjectsLocationsVolumesRestoreArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`netapp.projects.locations.volumes.revert_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetappProjectsLocationsVolumesRevertArgs {
    /// Path parameter: `name`.
    pub name: String,
}

// =============================================================================
// CLIENT FUNCTIONS (per-endpoint)
// =============================================================================

// -----------------------------------------------------------------------------
// POST v1/projects/{projectsId}/locations/{locationsId}/volumes
// -----------------------------------------------------------------------------

/// POST v1/projects/{projectsId}/locations/{locationsId}/volumes.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = netapp_projects_locations_volumes_create_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn netapp_projects_locations_volumes_create_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://netapp.googleapis.com/v1/projects/{{projectsId}}/locations/{{locationsId}}/volumes",
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE v1/projects/{projectsId}/locations/{locationsId}/volumes/{volumesId}
// -----------------------------------------------------------------------------

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/volumes/{volumesId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = netapp_projects_locations_volumes_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn netapp_projects_locations_volumes_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://netapp.googleapis.com/v1/projects/{{projectsId}}/locations/{{locationsId}}/volumes/{{volumesId}}",
    );

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/projects/{projectsId}/locations/{locationsId}/volumes/{volumesId}:establishPeering
// -----------------------------------------------------------------------------

/// POST v1/projects/{projectsId}/locations/{locationsId}/volumes/{volumesId}:establishPeering.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = netapp_projects_locations_volumes_establish_peering_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn netapp_projects_locations_volumes_establish_peering_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://netapp.googleapis.com/v1/projects/{{projectsId}}/locations/{{locationsId}}/volumes/{{volumesId}}:establishPeering",
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/projects/{projectsId}/locations/{locationsId}/volumes/{volumesId}
// -----------------------------------------------------------------------------

/// GET v1/projects/{projectsId}/locations/{locationsId}/volumes/{volumesId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = netapp_projects_locations_volumes_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn netapp_projects_locations_volumes_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Volume>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://netapp.googleapis.com/v1/projects/{{projectsId}}/locations/{{locationsId}}/volumes/{{volumesId}}",
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Volume =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/projects/{projectsId}/locations/{locationsId}/volumes
// -----------------------------------------------------------------------------

/// GET v1/projects/{projectsId}/locations/{locationsId}/volumes.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = netapp_projects_locations_volumes_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn netapp_projects_locations_volumes_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListVolumesResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://netapp.googleapis.com/v1/projects/{{projectsId}}/locations/{{locationsId}}/volumes",
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ListVolumesResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PATCH v1/projects/{projectsId}/locations/{locationsId}/volumes/{volumesId}
// -----------------------------------------------------------------------------

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/volumes/{volumesId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = netapp_projects_locations_volumes_patch_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn netapp_projects_locations_volumes_patch_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://netapp.googleapis.com/v1/projects/{{projectsId}}/locations/{{locationsId}}/volumes/{{volumesId}}",
    );

    let mut builder = client
        .patch(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/projects/{projectsId}/locations/{locationsId}/volumes/{volumesId}:restore
// -----------------------------------------------------------------------------

/// POST v1/projects/{projectsId}/locations/{locationsId}/volumes/{volumesId}:restore.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = netapp_projects_locations_volumes_restore_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn netapp_projects_locations_volumes_restore_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://netapp.googleapis.com/v1/projects/{{projectsId}}/locations/{{locationsId}}/volumes/{{volumesId}}:restore",
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/projects/{projectsId}/locations/{locationsId}/volumes/{volumesId}:revert
// -----------------------------------------------------------------------------

/// POST v1/projects/{projectsId}/locations/{locationsId}/volumes/{volumesId}:revert.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = netapp_projects_locations_volumes_revert_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn netapp_projects_locations_volumes_revert_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://netapp.googleapis.com/v1/projects/{{projectsId}}/locations/{{locationsId}}/volumes/{{volumesId}}:revert",
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}
