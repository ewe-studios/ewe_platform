//! Auto-generated API module for gcp/language language.
//!
//! Generated by `cargo run --bin ewe_platform gen_api`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp_language_language `

#![cfg(feature = "gcp_language_language")]
#![allow(clippy::too_many_arguments, clippy::type_complexity)]
#![allow(
    clippy::missing_errors_doc,
    clippy::doc_markdown,
    clippy::useless_format
)]
#![allow(unused_imports)]

use foundation_core::valtron::{TaskIterator, TaskIteratorExt};
use foundation_core::wire::simple_http::client::{ClientRequestBuilder, SimpleHttpClient};
use foundation_macros::JsonHash;
use serde::{Deserialize, Serialize};

use super::shared::ApiResponse;

// =============================================================================
// TYPE DECLARATIONS
// =============================================================================

/// `Document` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Document {
    /// content property.
    pub content: Option<String>,
    /// gcsContentUri property.
    pub gcs_content_uri: Option<String>,
    /// languageCode property.
    pub language_code: Option<String>,
    /// type property.
    pub r#type: Option<String>,
}

/// `AnalyzeEntitiesRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AnalyzeEntitiesRequest {
    /// document property.
    pub document: Option<Document>,
    /// encodingType property.
    pub encoding_type: Option<String>,
}

/// `AnalyzeEntitiesResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AnalyzeEntitiesResponse {
    /// entities property.
    pub entities: Option<Vec<Entity>>,
    /// languageCode property.
    pub language_code: Option<String>,
    /// languageSupported property.
    pub language_supported: Option<bool>,
}

/// `AnnotateTextRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AnnotateTextRequest {
    /// document property.
    pub document: Option<Document>,
    /// encodingType property.
    pub encoding_type: Option<String>,
    /// features property.
    pub features: Option<AnnotateTextRequestFeatures>,
}

/// `Sentence` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Sentence {
    /// sentiment property.
    pub sentiment: Option<Sentiment>,
    /// text property.
    pub text: Option<TextSpan>,
}

/// `AnalyzeSentimentResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AnalyzeSentimentResponse {
    /// documentSentiment property.
    pub document_sentiment: Option<Sentiment>,
    /// languageCode property.
    pub language_code: Option<String>,
    /// languageSupported property.
    pub language_supported: Option<bool>,
    /// sentences property.
    pub sentences: Option<Vec<Sentence>>,
}

/// `ModerateTextResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ModerateTextResponse {
    /// languageCode property.
    pub language_code: Option<String>,
    /// languageSupported property.
    pub language_supported: Option<bool>,
    /// moderationCategories property.
    pub moderation_categories: Option<Vec<ClassificationCategory>>,
}

/// `Entity` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Entity {
    /// mentions property.
    pub mentions: Option<Vec<EntityMention>>,
    /// metadata property.
    pub metadata: Option<serde_json::Value>,
    /// name property.
    pub name: Option<String>,
    /// sentiment property.
    pub sentiment: Option<Sentiment>,
    /// type property.
    pub r#type: Option<String>,
}

/// `Sentiment` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Sentiment {
    /// magnitude property.
    pub magnitude: Option<f64>,
    /// score property.
    pub score: Option<f64>,
}

/// `ClassifyTextResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ClassifyTextResponse {
    /// categories property.
    pub categories: Option<Vec<ClassificationCategory>>,
    /// languageCode property.
    pub language_code: Option<String>,
    /// languageSupported property.
    pub language_supported: Option<bool>,
}

/// `TextSpan` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct TextSpan {
    /// beginOffset property.
    pub begin_offset: Option<i64>,
    /// content property.
    pub content: Option<String>,
}

/// `EntityMention` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct EntityMention {
    /// probability property.
    pub probability: Option<f64>,
    /// sentiment property.
    pub sentiment: Option<Sentiment>,
    /// text property.
    pub text: Option<TextSpan>,
    /// type property.
    pub r#type: Option<String>,
}

/// `ModerateTextRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ModerateTextRequest {
    /// document property.
    pub document: Option<Document>,
    /// modelVersion property.
    pub model_version: Option<String>,
}

/// `ClassificationCategory` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ClassificationCategory {
    /// confidence property.
    pub confidence: Option<f64>,
    /// name property.
    pub name: Option<String>,
    /// severity property.
    pub severity: Option<f64>,
}

/// `AnnotateTextRequestFeatures` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AnnotateTextRequestFeatures {
    /// classifyText property.
    pub classify_text: Option<bool>,
    /// extractDocumentSentiment property.
    pub extract_document_sentiment: Option<bool>,
    /// extractEntities property.
    pub extract_entities: Option<bool>,
    /// moderateText property.
    pub moderate_text: Option<bool>,
}

/// `AnalyzeSentimentRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AnalyzeSentimentRequest {
    /// document property.
    pub document: Option<Document>,
    /// encodingType property.
    pub encoding_type: Option<String>,
}

/// `AnnotateTextResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AnnotateTextResponse {
    /// categories property.
    pub categories: Option<Vec<ClassificationCategory>>,
    /// documentSentiment property.
    pub document_sentiment: Option<Sentiment>,
    /// entities property.
    pub entities: Option<Vec<Entity>>,
    /// languageCode property.
    pub language_code: Option<String>,
    /// languageSupported property.
    pub language_supported: Option<bool>,
    /// moderationCategories property.
    pub moderation_categories: Option<Vec<ClassificationCategory>>,
    /// sentences property.
    pub sentences: Option<Vec<Sentence>>,
}

/// `ClassifyTextRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ClassifyTextRequest {
    /// document property.
    pub document: Option<Document>,
}

// =============================================================================
// ARGS TYPES (per-endpoint)
// =============================================================================

/// Arguments for [`language.documents.analyzeEntities_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct LanguageDocumentsAnalyzeEntitiesArgs {
    /// Request body.
    pub body: AnalyzeEntitiesRequest,
}

/// Arguments for [`language.documents.analyzeSentiment_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct LanguageDocumentsAnalyzeSentimentArgs {
    /// Request body.
    pub body: AnalyzeSentimentRequest,
}

/// Arguments for [`language.documents.annotateText_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct LanguageDocumentsAnnotateTextArgs {
    /// Request body.
    pub body: AnnotateTextRequest,
}

/// Arguments for [`language.documents.classifyText_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct LanguageDocumentsClassifyTextArgs {
    /// Request body.
    pub body: ClassifyTextRequest,
}

/// Arguments for [`language.documents.moderateText_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct LanguageDocumentsModerateTextArgs {
    /// Request body.
    pub body: ModerateTextRequest,
}

// =============================================================================
// CLIENT FUNCTIONS (per-endpoint)
// =============================================================================

// -----------------------------------------------------------------------------
// POST v2/documents:analyzeEntities
// -----------------------------------------------------------------------------

/// POST v2/documents:analyzeEntities.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = language_documents_analyze_entities_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn language_documents_analyze_entities_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &LanguageDocumentsAnalyzeEntitiesArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<AnalyzeEntitiesResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://language.googleapis.com/v2/documents:analyzeEntities",);

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: AnalyzeEntitiesResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v2/documents:analyzeSentiment
// -----------------------------------------------------------------------------

/// POST v2/documents:analyzeSentiment.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = language_documents_analyze_sentiment_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn language_documents_analyze_sentiment_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &LanguageDocumentsAnalyzeSentimentArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<AnalyzeSentimentResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://language.googleapis.com/v2/documents:analyzeSentiment",);

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: AnalyzeSentimentResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v2/documents:annotateText
// -----------------------------------------------------------------------------

/// POST v2/documents:annotateText.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = language_documents_annotate_text_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn language_documents_annotate_text_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &LanguageDocumentsAnnotateTextArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<AnnotateTextResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://language.googleapis.com/v2/documents:annotateText",);

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: AnnotateTextResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v2/documents:classifyText
// -----------------------------------------------------------------------------

/// POST v2/documents:classifyText.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = language_documents_classify_text_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn language_documents_classify_text_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &LanguageDocumentsClassifyTextArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ClassifyTextResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://language.googleapis.com/v2/documents:classifyText",);

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ClassifyTextResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v2/documents:moderateText
// -----------------------------------------------------------------------------

/// POST v2/documents:moderateText.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = language_documents_moderate_text_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn language_documents_moderate_text_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &LanguageDocumentsModerateTextArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ModerateTextResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://language.googleapis.com/v2/documents:moderateText",);

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ModerateTextResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}
