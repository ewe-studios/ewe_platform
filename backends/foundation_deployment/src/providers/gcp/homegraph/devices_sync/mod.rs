//! Auto-generated API module for gcp/homegraph devices:sync.
//!
//! Generated by `cargo run --bin ewe_platform gen_api`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp_homegraph_devices_sync `

#![cfg(feature = "gcp_homegraph_devices_sync")]
#![allow(clippy::too_many_arguments, clippy::type_complexity)]
#![allow(
    clippy::missing_errors_doc,
    clippy::doc_markdown,
    clippy::useless_format
)]
#![allow(unused_imports)]

use foundation_core::valtron::{TaskIterator, TaskIteratorExt};
use foundation_core::wire::simple_http::client::{ClientRequestBuilder, SimpleHttpClient};
use foundation_macros::JsonHash;
use serde::{Deserialize, Serialize};

use super::shared::ApiResponse;

// =============================================================================
// TYPE DECLARATIONS
// =============================================================================

/// `Device` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Device {
    /// attributes property.
    pub attributes: Option<serde_json::Value>,
    /// customData property.
    pub custom_data: Option<serde_json::Value>,
    /// deviceInfo property.
    pub device_info: Option<DeviceInfo>,
    /// id property.
    pub id: Option<String>,
    /// name property.
    pub name: Option<DeviceNames>,
    /// notificationSupportedByAgent property.
    pub notification_supported_by_agent: Option<bool>,
    /// otherDeviceIds property.
    pub other_device_ids: Option<Vec<AgentOtherDeviceId>>,
    /// roomHint property.
    pub room_hint: Option<String>,
    /// structureHint property.
    pub structure_hint: Option<String>,
    /// traits property.
    pub traits: Option<Vec<String>>,
    /// type property.
    pub r#type: Option<String>,
    /// willReportState property.
    pub will_report_state: Option<bool>,
}

/// `AgentOtherDeviceId` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct AgentOtherDeviceId {
    /// agentId property.
    pub agent_id: Option<String>,
    /// deviceId property.
    pub device_id: Option<String>,
}

/// `SyncResponse` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct SyncResponse {
    /// payload property.
    pub payload: Option<SyncResponsePayload>,
    /// requestId property.
    pub request_id: Option<String>,
}

/// `DeviceInfo` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct DeviceInfo {
    /// hwVersion property.
    pub hw_version: Option<String>,
    /// manufacturer property.
    pub manufacturer: Option<String>,
    /// model property.
    pub model: Option<String>,
    /// swVersion property.
    pub sw_version: Option<String>,
}

/// `DeviceNames` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct DeviceNames {
    /// defaultNames property.
    pub default_names: Option<Vec<String>>,
    /// name property.
    pub name: Option<String>,
    /// nicknames property.
    pub nicknames: Option<Vec<String>>,
}

/// `SyncResponsePayload` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct SyncResponsePayload {
    /// agentUserId property.
    pub agent_user_id: Option<String>,
    /// devices property.
    pub devices: Option<Vec<Device>>,
}

// =============================================================================
// ARGS TYPES (per-endpoint)
// =============================================================================

// =============================================================================
// CLIENT FUNCTIONS (per-endpoint)
// =============================================================================

// -----------------------------------------------------------------------------
// POST v1/devices:sync
// -----------------------------------------------------------------------------

/// POST v1/devices:sync.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = homegraph_devices_sync_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn homegraph_devices_sync_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SyncResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://homegraph.googleapis.com/v1/devices:sync",);

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: SyncResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}
