//! Auto-generated API module for gcp/securityposture postures.
//!
//! Generated by `cargo run --bin ewe_platform gen_api`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp_securityposture_postures `

#![cfg(feature = "gcp_securityposture_postures")]
#![allow(clippy::too_many_arguments, clippy::type_complexity)]
#![allow(
    clippy::missing_errors_doc,
    clippy::doc_markdown,
    clippy::useless_format
)]
#![allow(unused_imports)]

use foundation_core::valtron::{TaskIterator, TaskIteratorExt};
use foundation_core::wire::simple_http::client::{ClientRequestBuilder, SimpleHttpClient};
use foundation_macros::JsonHash;
use serde::{Deserialize, Serialize};

// Import shared types used by this module
use super::shared::Operation;

use super::shared::ApiResponse;

// =============================================================================
// TYPE DECLARATIONS
// =============================================================================

/// `SecurityHealthAnalyticsCustomModule` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct SecurityHealthAnalyticsCustomModule {
    /// config property.
    pub config: Option<CustomConfig>,
    /// displayName property.
    pub display_name: Option<String>,
    /// id property.
    pub id: Option<String>,
    /// moduleEnablementState property.
    pub module_enablement_state: Option<String>,
}

/// `SecurityHealthAnalyticsModule` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct SecurityHealthAnalyticsModule {
    /// moduleEnablementState property.
    pub module_enablement_state: Option<String>,
    /// moduleName property.
    pub module_name: Option<String>,
}

/// `OrgPolicyConstraintCustom` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct OrgPolicyConstraintCustom {
    /// customConstraint property.
    pub custom_constraint: Option<GoogleCloudSecuritypostureV1CustomConstraint>,
    /// policyRules property.
    pub policy_rules: Option<Vec<GoogleCloudSecuritypostureV1PolicyRule>>,
}

/// `GoogleCloudSecuritypostureV1CustomConstraint` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudSecuritypostureV1CustomConstraint {
    /// actionType property.
    pub action_type: Option<String>,
    /// condition property.
    pub condition: Option<String>,
    /// description property.
    pub description: Option<String>,
    /// displayName property.
    pub display_name: Option<String>,
    /// methodTypes property.
    pub method_types: Option<Vec<String>>,
    /// name property.
    pub name: Option<String>,
    /// resourceTypes property.
    pub resource_types: Option<Vec<String>>,
    /// updateTime property.
    pub update_time: Option<String>,
}

/// `ResourceSelector` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ResourceSelector {
    /// resourceTypes property.
    pub resource_types: Option<Vec<String>>,
}

/// `Property` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Property {
    /// name property.
    pub name: Option<String>,
    /// valueExpression property.
    pub value_expression: Option<Expr>,
}

/// `ResourceTypes` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ResourceTypes {
    /// included property.
    pub included: Option<Vec<String>>,
}

/// `GoogleCloudSecuritypostureV1PolicyRuleStringValues` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudSecuritypostureV1PolicyRuleStringValues {
    /// allowedValues property.
    pub allowed_values: Option<Vec<String>>,
    /// deniedValues property.
    pub denied_values: Option<Vec<String>>,
}

/// `OrgPolicyConstraint` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct OrgPolicyConstraint {
    /// cannedConstraintId property.
    pub canned_constraint_id: Option<String>,
    /// policyRules property.
    pub policy_rules: Option<Vec<GoogleCloudSecuritypostureV1PolicyRule>>,
}

/// `GoogleCloudSecuritypostureV1PolicyRule` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudSecuritypostureV1PolicyRule {
    /// allowAll property.
    pub allow_all: Option<bool>,
    /// condition property.
    pub condition: Option<Expr>,
    /// denyAll property.
    pub deny_all: Option<bool>,
    /// enforce property.
    pub enforce: Option<bool>,
    /// parameters property.
    pub parameters: Option<serde_json::Value>,
    /// resourceTypes property.
    pub resource_types: Option<ResourceTypes>,
    /// values property.
    pub values: Option<GoogleCloudSecuritypostureV1PolicyRuleStringValues>,
}

/// `Posture` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Posture {
    /// annotations property.
    pub annotations: Option<serde_json::Value>,
    /// categories property.
    pub categories: Option<Vec<String>>,
    /// createTime property.
    pub create_time: Option<String>,
    /// description property.
    pub description: Option<String>,
    /// etag property.
    pub etag: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// policySets property.
    pub policy_sets: Option<Vec<PolicySet>>,
    /// reconciling property.
    pub reconciling: Option<bool>,
    /// revisionId property.
    pub revision_id: Option<String>,
    /// state property.
    pub state: Option<String>,
    /// updateTime property.
    pub update_time: Option<String>,
}

/// `PolicySet` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct PolicySet {
    /// description property.
    pub description: Option<String>,
    /// policies property.
    pub policies: Option<Vec<Policy>>,
    /// policySetId property.
    pub policy_set_id: Option<String>,
}

/// `ListPosturesResponse` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ListPosturesResponse {
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// postures property.
    pub postures: Option<Vec<Posture>>,
    /// unreachable property.
    pub unreachable: Option<Vec<String>>,
}

/// `Policy` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Policy {
    /// complianceStandards property.
    pub compliance_standards: Option<Vec<ComplianceStandard>>,
    /// constraint property.
    pub constraint: Option<Constraint>,
    /// description property.
    pub description: Option<String>,
    /// policyId property.
    pub policy_id: Option<String>,
}

/// `ListPostureRevisionsResponse` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ListPostureRevisionsResponse {
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// revisions property.
    pub revisions: Option<Vec<Posture>>,
}

/// `ComplianceStandard` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ComplianceStandard {
    /// control property.
    pub control: Option<String>,
    /// standard property.
    pub standard: Option<String>,
}

/// `CustomConfig` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct CustomConfig {
    /// customOutput property.
    pub custom_output: Option<CustomOutputSpec>,
    /// description property.
    pub description: Option<String>,
    /// predicate property.
    pub predicate: Option<Expr>,
    /// recommendation property.
    pub recommendation: Option<String>,
    /// resourceSelector property.
    pub resource_selector: Option<ResourceSelector>,
    /// severity property.
    pub severity: Option<String>,
}

/// `CustomOutputSpec` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct CustomOutputSpec {
    /// properties property.
    pub properties: Option<Vec<Property>>,
}

/// `Status` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Status {
    /// code property.
    pub code: Option<i64>,
    /// details property.
    pub details: Option<Vec<serde_json::Value>>,
    /// message property.
    pub message: Option<String>,
}

/// `Expr` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Expr {
    /// description property.
    pub description: Option<String>,
    /// expression property.
    pub expression: Option<String>,
    /// location property.
    pub location: Option<String>,
    /// title property.
    pub title: Option<String>,
}

/// `Constraint` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Constraint {
    /// orgPolicyConstraint property.
    pub org_policy_constraint: Option<OrgPolicyConstraint>,
    /// orgPolicyConstraintCustom property.
    pub org_policy_constraint_custom: Option<OrgPolicyConstraintCustom>,
    /// securityHealthAnalyticsCustomModule property.
    pub security_health_analytics_custom_module: Option<SecurityHealthAnalyticsCustomModule>,
    /// securityHealthAnalyticsModule property.
    pub security_health_analytics_module: Option<SecurityHealthAnalyticsModule>,
}

// =============================================================================
// ARGS TYPES (per-endpoint)
// =============================================================================

/// Arguments for [`securityposture.organizations.locations.postures.create_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritypostureOrganizationsLocationsPosturesCreateArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `postureId`.
    pub posture_id: Option<String>,
}

/// Arguments for [`securityposture.organizations.locations.postures.delete_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritypostureOrganizationsLocationsPosturesDeleteArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `etag`.
    pub etag: Option<String>,
}

/// Arguments for [`securityposture.organizations.locations.postures.get_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritypostureOrganizationsLocationsPosturesGetArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `revisionId`.
    pub revision_id: Option<String>,
}

/// Arguments for [`securityposture.organizations.locations.postures.list_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritypostureOrganizationsLocationsPosturesListArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

/// Arguments for [`securityposture.organizations.locations.postures.listRevisions_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritypostureOrganizationsLocationsPosturesListRevisionsArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

/// Arguments for [`securityposture.organizations.locations.postures.patch_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritypostureOrganizationsLocationsPosturesPatchArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `revisionId`.
    pub revision_id: Option<String>,
    /// Query parameter: `updateMask`.
    pub update_mask: Option<String>,
}

// =============================================================================
// CLIENT FUNCTIONS (per-endpoint)
// =============================================================================

// -----------------------------------------------------------------------------
// POST v1/organizations/{organizationsId}/locations/{locationsId}/postures
// -----------------------------------------------------------------------------

/// POST v1/organizations/{organizationsId}/locations/{locationsId}/postures.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = securityposture_organizations_locations_postures_create_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn securityposture_organizations_locations_postures_create_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://securityposture.googleapis.com/v1/organizations/{{organizationsId}}/locations/{{locationsId}}/postures",
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE v1/organizations/{organizationsId}/locations/{locationsId}/postures/{posturesId}
// -----------------------------------------------------------------------------

/// DELETE v1/organizations/{organizationsId}/locations/{locationsId}/postures/{posturesId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = securityposture_organizations_locations_postures_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn securityposture_organizations_locations_postures_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://securityposture.googleapis.com/v1/organizations/{{organizationsId}}/locations/{{locationsId}}/postures/{{posturesId}}",
    );

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/organizations/{organizationsId}/locations/{locationsId}/postures/{posturesId}
// -----------------------------------------------------------------------------

/// GET v1/organizations/{organizationsId}/locations/{locationsId}/postures/{posturesId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = securityposture_organizations_locations_postures_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn securityposture_organizations_locations_postures_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Posture>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://securityposture.googleapis.com/v1/organizations/{{organizationsId}}/locations/{{locationsId}}/postures/{{posturesId}}",
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Posture =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/organizations/{organizationsId}/locations/{locationsId}/postures
// -----------------------------------------------------------------------------

/// GET v1/organizations/{organizationsId}/locations/{locationsId}/postures.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = securityposture_organizations_locations_postures_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn securityposture_organizations_locations_postures_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListPosturesResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://securityposture.googleapis.com/v1/organizations/{{organizationsId}}/locations/{{locationsId}}/postures",
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ListPosturesResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/organizations/{organizationsId}/locations/{locationsId}/postures/{posturesId}:listRevisions
// -----------------------------------------------------------------------------

/// GET v1/organizations/{organizationsId}/locations/{locationsId}/postures/{posturesId}:listRevisions.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = securityposture_organizations_locations_postures_list_revisions_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn securityposture_organizations_locations_postures_list_revisions_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListPostureRevisionsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://securityposture.googleapis.com/v1/organizations/{{organizationsId}}/locations/{{locationsId}}/postures/{{posturesId}}:listRevisions",
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ListPostureRevisionsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PATCH v1/organizations/{organizationsId}/locations/{locationsId}/postures/{posturesId}
// -----------------------------------------------------------------------------

/// PATCH v1/organizations/{organizationsId}/locations/{locationsId}/postures/{posturesId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = securityposture_organizations_locations_postures_patch_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn securityposture_organizations_locations_postures_patch_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://securityposture.googleapis.com/v1/organizations/{{organizationsId}}/locations/{{locationsId}}/postures/{{posturesId}}",
    );

    let mut builder = client
        .patch(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}
