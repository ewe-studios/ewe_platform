//! Auto-generated API module for gcp/securityposture reports.
//!
//! Generated by `cargo run --bin ewe_platform gen_api`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp_securityposture_reports `

#![cfg(feature = "gcp_securityposture_reports")]
#![allow(clippy::too_many_arguments, clippy::type_complexity)]
#![allow(
    clippy::missing_errors_doc,
    clippy::doc_markdown,
    clippy::useless_format
)]
#![allow(unused_imports)]

use foundation_core::valtron::{TaskIterator, TaskIteratorExt};
use foundation_core::wire::simple_http::client::{ClientRequestBuilder, SimpleHttpClient};
use foundation_macros::JsonHash;
use serde::{Deserialize, Serialize};

use super::shared::ApiResponse;

// =============================================================================
// TYPE DECLARATIONS
// =============================================================================

/// `AssetDetails` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct AssetDetails {
    /// asset property.
    pub asset: Option<String>,
    /// assetType property.
    pub asset_type: Option<String>,
}

/// `PostureDetails` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct PostureDetails {
    /// policySet property.
    pub policy_set: Option<String>,
    /// posture property.
    pub posture: Option<String>,
    /// postureDeployment property.
    pub posture_deployment: Option<String>,
    /// postureDeploymentTargetResource property.
    pub posture_deployment_target_resource: Option<String>,
    /// postureRevisionId property.
    pub posture_revision_id: Option<String>,
}

/// `IaCValidationReport` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct IaCValidationReport {
    /// note property.
    pub note: Option<String>,
    /// violations property.
    pub violations: Option<Vec<Violation>>,
}

/// `Report` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Report {
    /// createTime property.
    pub create_time: Option<String>,
    /// iacValidationReport property.
    pub iac_validation_report: Option<IaCValidationReport>,
    /// name property.
    pub name: Option<String>,
    /// updateTime property.
    pub update_time: Option<String>,
}

/// `ListReportsResponse` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ListReportsResponse {
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// reports property.
    pub reports: Option<Vec<Report>>,
    /// unreachable property.
    pub unreachable: Option<Vec<String>>,
}

/// `Violation` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Violation {
    /// assetId property.
    pub asset_id: Option<String>,
    /// nextSteps property.
    pub next_steps: Option<String>,
    /// policyId property.
    pub policy_id: Option<String>,
    /// severity property.
    pub severity: Option<String>,
    /// violatedAsset property.
    pub violated_asset: Option<AssetDetails>,
    /// violatedPolicy property.
    pub violated_policy: Option<PolicyDetails>,
    /// violatedPosture property.
    pub violated_posture: Option<PostureDetails>,
}

/// `PolicyDetails` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct PolicyDetails {
    /// complianceStandards property.
    pub compliance_standards: Option<Vec<String>>,
    /// constraint property.
    pub constraint: Option<String>,
    /// constraintType property.
    pub constraint_type: Option<String>,
    /// description property.
    pub description: Option<String>,
}

// =============================================================================
// ARGS TYPES (per-endpoint)
// =============================================================================

/// Arguments for [`securityposture.organizations.locations.reports.get_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritypostureOrganizationsLocationsReportsGetArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`securityposture.organizations.locations.reports.list_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SecuritypostureOrganizationsLocationsReportsListArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

// =============================================================================
// CLIENT FUNCTIONS (per-endpoint)
// =============================================================================

// -----------------------------------------------------------------------------
// GET v1/organizations/{organizationsId}/locations/{locationsId}/reports/{reportsId}
// -----------------------------------------------------------------------------

/// GET v1/organizations/{organizationsId}/locations/{locationsId}/reports/{reportsId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = securityposture_organizations_locations_reports_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn securityposture_organizations_locations_reports_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Report>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://securityposture.googleapis.com/v1/organizations/{{organizationsId}}/locations/{{locationsId}}/reports/{{reportsId}}",
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Report =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/organizations/{organizationsId}/locations/{locationsId}/reports
// -----------------------------------------------------------------------------

/// GET v1/organizations/{organizationsId}/locations/{locationsId}/reports.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = securityposture_organizations_locations_reports_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn securityposture_organizations_locations_reports_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListReportsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://securityposture.googleapis.com/v1/organizations/{{organizationsId}}/locations/{{locationsId}}/reports",
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ListReportsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}
