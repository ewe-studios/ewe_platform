//! Auto-generated API module for gcp/identitytoolkit identitytoolkit.
//!
//! Generated by `cargo run --bin ewe_platform gen_api`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp_identitytoolkit_identitytoolkit `

#![cfg(feature = "gcp_identitytoolkit_identitytoolkit")]
#![allow(clippy::too_many_arguments, clippy::type_complexity)]
#![allow(
    clippy::missing_errors_doc,
    clippy::doc_markdown,
    clippy::useless_format
)]
#![allow(unused_imports)]

use foundation_core::valtron::{TaskIterator, TaskIteratorExt};
use foundation_core::wire::simple_http::client::{ClientRequestBuilder, SimpleHttpClient};
use foundation_macros::JsonHash;
use serde::{Deserialize, Serialize};

use super::shared::ApiResponse;

// =============================================================================
// TYPE DECLARATIONS
// =============================================================================

/// `GetRecaptchaParamResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GetRecaptchaParamResponse {
    /// kind property.
    pub kind: Option<String>,
    /// recaptchaSiteKey property.
    pub recaptcha_site_key: Option<String>,
    /// recaptchaStoken property.
    pub recaptcha_stoken: Option<String>,
}

/// `CreateAuthUriResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct CreateAuthUriResponse {
    /// allProviders property.
    pub all_providers: Option<Vec<String>>,
    /// authUri property.
    pub auth_uri: Option<String>,
    /// captchaRequired property.
    pub captcha_required: Option<bool>,
    /// forExistingProvider property.
    pub for_existing_provider: Option<bool>,
    /// kind property.
    pub kind: Option<String>,
    /// providerId property.
    pub provider_id: Option<String>,
    /// registered property.
    pub registered: Option<bool>,
    /// sessionId property.
    pub session_id: Option<String>,
    /// signinMethods property.
    pub signin_methods: Option<Vec<String>>,
}

/// `UploadAccountResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct UploadAccountResponse {
    /// error property.
    pub error: Option<Vec<std::collections::HashMap<String, serde_json::Value>>>,
    /// kind property.
    pub kind: Option<String>,
}

/// `IdentitytoolkitRelyingpartyDeleteAccountRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct IdentitytoolkitRelyingpartyDeleteAccountRequest {
    /// delegatedProjectNumber property.
    pub delegated_project_number: Option<String>,
    /// idToken property.
    pub id_token: Option<String>,
    /// localId property.
    pub local_id: Option<String>,
}

/// `VerifyCustomTokenResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct VerifyCustomTokenResponse {
    /// expiresIn property.
    pub expires_in: Option<String>,
    /// idToken property.
    pub id_token: Option<String>,
    /// isNewUser property.
    pub is_new_user: Option<bool>,
    /// kind property.
    pub kind: Option<String>,
    /// refreshToken property.
    pub refresh_token: Option<String>,
}

/// `IdentitytoolkitRelyingpartyGetPublicKeysResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct IdentitytoolkitRelyingpartyGetPublicKeysResponse {
    #[serde(flatten)]
    pub data: std::collections::HashMap<String, serde_json::Value>,
}

/// `IdentitytoolkitRelyingpartySignOutUserRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct IdentitytoolkitRelyingpartySignOutUserRequest {
    /// instanceId property.
    pub instance_id: Option<String>,
    /// localId property.
    pub local_id: Option<String>,
}

/// `IdpConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct IdpConfig {
    /// clientId property.
    pub client_id: Option<String>,
    /// enabled property.
    pub enabled: Option<bool>,
    /// experimentPercent property.
    pub experiment_percent: Option<i64>,
    /// provider property.
    pub provider: Option<String>,
    /// secret property.
    pub secret: Option<String>,
    /// whitelistedAudiences property.
    pub whitelisted_audiences: Option<Vec<String>>,
}

/// `EmailTemplate` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct EmailTemplate {
    /// body property.
    pub body: Option<String>,
    /// format property.
    pub format: Option<String>,
    /// from property.
    pub from: Option<String>,
    /// fromDisplayName property.
    pub from_display_name: Option<String>,
    /// replyTo property.
    pub reply_to: Option<String>,
    /// subject property.
    pub subject: Option<String>,
}

/// `IdentitytoolkitRelyingpartySendVerificationCodeRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct IdentitytoolkitRelyingpartySendVerificationCodeRequest {
    /// iosReceipt property.
    pub ios_receipt: Option<String>,
    /// iosSecret property.
    pub ios_secret: Option<String>,
    /// phoneNumber property.
    pub phone_number: Option<String>,
    /// recaptchaToken property.
    pub recaptcha_token: Option<String>,
}

/// `IdentitytoolkitRelyingpartySetAccountInfoRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct IdentitytoolkitRelyingpartySetAccountInfoRequest {
    /// captchaChallenge property.
    pub captcha_challenge: Option<String>,
    /// captchaResponse property.
    pub captcha_response: Option<String>,
    /// createdAt property.
    pub created_at: Option<String>,
    /// customAttributes property.
    pub custom_attributes: Option<String>,
    /// delegatedProjectNumber property.
    pub delegated_project_number: Option<String>,
    /// deleteAttribute property.
    pub delete_attribute: Option<Vec<String>>,
    /// deleteProvider property.
    pub delete_provider: Option<Vec<String>>,
    /// disableUser property.
    pub disable_user: Option<bool>,
    /// displayName property.
    pub display_name: Option<String>,
    /// email property.
    pub email: Option<String>,
    /// emailVerified property.
    pub email_verified: Option<bool>,
    /// idToken property.
    pub id_token: Option<String>,
    /// instanceId property.
    pub instance_id: Option<String>,
    /// lastLoginAt property.
    pub last_login_at: Option<String>,
    /// localId property.
    pub local_id: Option<String>,
    /// oobCode property.
    pub oob_code: Option<String>,
    /// password property.
    pub password: Option<String>,
    /// phoneNumber property.
    pub phone_number: Option<String>,
    /// photoUrl property.
    pub photo_url: Option<String>,
    /// provider property.
    pub provider: Option<Vec<String>>,
    /// returnSecureToken property.
    pub return_secure_token: Option<bool>,
    /// upgradeToFederatedLogin property.
    pub upgrade_to_federated_login: Option<bool>,
    /// validSince property.
    pub valid_since: Option<String>,
}

/// `DeleteAccountResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct DeleteAccountResponse {
    /// kind property.
    pub kind: Option<String>,
}

/// `VerifyPasswordResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct VerifyPasswordResponse {
    /// displayName property.
    pub display_name: Option<String>,
    /// email property.
    pub email: Option<String>,
    /// expiresIn property.
    pub expires_in: Option<String>,
    /// idToken property.
    pub id_token: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// localId property.
    pub local_id: Option<String>,
    /// oauthAccessToken property.
    pub oauth_access_token: Option<String>,
    /// oauthAuthorizationCode property.
    pub oauth_authorization_code: Option<String>,
    /// oauthExpireIn property.
    pub oauth_expire_in: Option<i64>,
    /// photoUrl property.
    pub photo_url: Option<String>,
    /// refreshToken property.
    pub refresh_token: Option<String>,
    /// registered property.
    pub registered: Option<bool>,
}

/// `IdentitytoolkitRelyingpartyResetPasswordRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct IdentitytoolkitRelyingpartyResetPasswordRequest {
    /// email property.
    pub email: Option<String>,
    /// newPassword property.
    pub new_password: Option<String>,
    /// oldPassword property.
    pub old_password: Option<String>,
    /// oobCode property.
    pub oob_code: Option<String>,
}

/// `GetOobConfirmationCodeResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GetOobConfirmationCodeResponse {
    /// email property.
    pub email: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// oobCode property.
    pub oob_code: Option<String>,
}

/// `IdentitytoolkitRelyingpartyUploadAccountRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct IdentitytoolkitRelyingpartyUploadAccountRequest {
    /// allowOverwrite property.
    pub allow_overwrite: Option<bool>,
    /// blockSize property.
    pub block_size: Option<i64>,
    /// cpuMemCost property.
    pub cpu_mem_cost: Option<i64>,
    /// delegatedProjectNumber property.
    pub delegated_project_number: Option<String>,
    /// dkLen property.
    pub dk_len: Option<i64>,
    /// hashAlgorithm property.
    pub hash_algorithm: Option<String>,
    /// memoryCost property.
    pub memory_cost: Option<i64>,
    /// parallelization property.
    pub parallelization: Option<i64>,
    /// rounds property.
    pub rounds: Option<i64>,
    /// saltSeparator property.
    pub salt_separator: Option<String>,
    /// sanityCheck property.
    pub sanity_check: Option<bool>,
    /// signerKey property.
    pub signer_key: Option<String>,
    /// targetProjectId property.
    pub target_project_id: Option<String>,
    /// users property.
    pub users: Option<Vec<UserInfo>>,
}

/// `IdentitytoolkitRelyingpartyVerifyAssertionRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct IdentitytoolkitRelyingpartyVerifyAssertionRequest {
    /// autoCreate property.
    pub auto_create: Option<bool>,
    /// delegatedProjectNumber property.
    pub delegated_project_number: Option<String>,
    /// idToken property.
    pub id_token: Option<String>,
    /// instanceId property.
    pub instance_id: Option<String>,
    /// pendingIdToken property.
    pub pending_id_token: Option<String>,
    /// postBody property.
    pub post_body: Option<String>,
    /// requestUri property.
    pub request_uri: Option<String>,
    /// returnIdpCredential property.
    pub return_idp_credential: Option<bool>,
    /// returnRefreshToken property.
    pub return_refresh_token: Option<bool>,
    /// returnSecureToken property.
    pub return_secure_token: Option<bool>,
    /// sessionId property.
    pub session_id: Option<String>,
    /// tenantId property.
    pub tenant_id: Option<String>,
    /// tenantProjectNumber property.
    pub tenant_project_number: Option<String>,
}

/// `IdentitytoolkitRelyingpartyVerifyPhoneNumberResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct IdentitytoolkitRelyingpartyVerifyPhoneNumberResponse {
    /// expiresIn property.
    pub expires_in: Option<String>,
    /// idToken property.
    pub id_token: Option<String>,
    /// isNewUser property.
    pub is_new_user: Option<bool>,
    /// localId property.
    pub local_id: Option<String>,
    /// phoneNumber property.
    pub phone_number: Option<String>,
    /// refreshToken property.
    pub refresh_token: Option<String>,
    /// temporaryProof property.
    pub temporary_proof: Option<String>,
    /// temporaryProofExpiresIn property.
    pub temporary_proof_expires_in: Option<String>,
    /// verificationProof property.
    pub verification_proof: Option<String>,
    /// verificationProofExpiresIn property.
    pub verification_proof_expires_in: Option<String>,
}

/// `IdentitytoolkitRelyingpartyCreateAuthUriRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct IdentitytoolkitRelyingpartyCreateAuthUriRequest {
    /// appId property.
    pub app_id: Option<String>,
    /// authFlowType property.
    pub auth_flow_type: Option<String>,
    /// clientId property.
    pub client_id: Option<String>,
    /// context property.
    pub context: Option<String>,
    /// continueUri property.
    pub continue_uri: Option<String>,
    /// customParameter property.
    pub custom_parameter: Option<serde_json::Value>,
    /// hostedDomain property.
    pub hosted_domain: Option<String>,
    /// identifier property.
    pub identifier: Option<String>,
    /// oauthConsumerKey property.
    pub oauth_consumer_key: Option<String>,
    /// oauthScope property.
    pub oauth_scope: Option<String>,
    /// openidRealm property.
    pub openid_realm: Option<String>,
    /// otaApp property.
    pub ota_app: Option<String>,
    /// providerId property.
    pub provider_id: Option<String>,
    /// sessionId property.
    pub session_id: Option<String>,
    /// tenantId property.
    pub tenant_id: Option<String>,
    /// tenantProjectNumber property.
    pub tenant_project_number: Option<String>,
}

/// `GetAccountInfoResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GetAccountInfoResponse {
    /// kind property.
    pub kind: Option<String>,
    /// users property.
    pub users: Option<Vec<UserInfo>>,
}

/// `IdentitytoolkitRelyingpartySendVerificationCodeResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct IdentitytoolkitRelyingpartySendVerificationCodeResponse {
    /// sessionInfo property.
    pub session_info: Option<String>,
}

/// `IdentitytoolkitRelyingpartyVerifyCustomTokenRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct IdentitytoolkitRelyingpartyVerifyCustomTokenRequest {
    /// delegatedProjectNumber property.
    pub delegated_project_number: Option<String>,
    /// instanceId property.
    pub instance_id: Option<String>,
    /// returnSecureToken property.
    pub return_secure_token: Option<bool>,
    /// token property.
    pub token: Option<String>,
}

/// `IdentitytoolkitRelyingpartyVerifyPasswordRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct IdentitytoolkitRelyingpartyVerifyPasswordRequest {
    /// captchaChallenge property.
    pub captcha_challenge: Option<String>,
    /// captchaResponse property.
    pub captcha_response: Option<String>,
    /// delegatedProjectNumber property.
    pub delegated_project_number: Option<String>,
    /// email property.
    pub email: Option<String>,
    /// idToken property.
    pub id_token: Option<String>,
    /// instanceId property.
    pub instance_id: Option<String>,
    /// password property.
    pub password: Option<String>,
    /// pendingIdToken property.
    pub pending_id_token: Option<String>,
    /// returnSecureToken property.
    pub return_secure_token: Option<bool>,
    /// tenantId property.
    pub tenant_id: Option<String>,
    /// tenantProjectNumber property.
    pub tenant_project_number: Option<String>,
}

/// `IdentitytoolkitRelyingpartySignOutUserResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct IdentitytoolkitRelyingpartySignOutUserResponse {
    /// localId property.
    pub local_id: Option<String>,
}

/// `IdentitytoolkitRelyingpartyDownloadAccountRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct IdentitytoolkitRelyingpartyDownloadAccountRequest {
    /// delegatedProjectNumber property.
    pub delegated_project_number: Option<String>,
    /// maxResults property.
    pub max_results: Option<i64>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// targetProjectId property.
    pub target_project_id: Option<String>,
}

/// `Relyingparty` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Relyingparty {
    /// androidInstallApp property.
    pub android_install_app: Option<bool>,
    /// androidMinimumVersion property.
    pub android_minimum_version: Option<String>,
    /// androidPackageName property.
    pub android_package_name: Option<String>,
    /// canHandleCodeInApp property.
    pub can_handle_code_in_app: Option<bool>,
    /// captchaResp property.
    pub captcha_resp: Option<String>,
    /// challenge property.
    pub challenge: Option<String>,
    /// continueUrl property.
    pub continue_url: Option<String>,
    /// email property.
    pub email: Option<String>,
    /// iOSAppStoreId property.
    pub i_os_app_store_id: Option<String>,
    /// iOSBundleId property.
    pub i_os_bundle_id: Option<String>,
    /// idToken property.
    pub id_token: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// newEmail property.
    pub new_email: Option<String>,
    /// requestType property.
    pub request_type: Option<String>,
    /// userIp property.
    pub user_ip: Option<String>,
}

/// `VerifyAssertionResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct VerifyAssertionResponse {
    /// action property.
    pub action: Option<String>,
    /// appInstallationUrl property.
    pub app_installation_url: Option<String>,
    /// appScheme property.
    pub app_scheme: Option<String>,
    /// context property.
    pub context: Option<String>,
    /// dateOfBirth property.
    pub date_of_birth: Option<String>,
    /// displayName property.
    pub display_name: Option<String>,
    /// email property.
    pub email: Option<String>,
    /// emailRecycled property.
    pub email_recycled: Option<bool>,
    /// emailVerified property.
    pub email_verified: Option<bool>,
    /// errorMessage property.
    pub error_message: Option<String>,
    /// expiresIn property.
    pub expires_in: Option<String>,
    /// federatedId property.
    pub federated_id: Option<String>,
    /// firstName property.
    pub first_name: Option<String>,
    /// fullName property.
    pub full_name: Option<String>,
    /// idToken property.
    pub id_token: Option<String>,
    /// inputEmail property.
    pub input_email: Option<String>,
    /// isNewUser property.
    pub is_new_user: Option<bool>,
    /// kind property.
    pub kind: Option<String>,
    /// language property.
    pub language: Option<String>,
    /// lastName property.
    pub last_name: Option<String>,
    /// localId property.
    pub local_id: Option<String>,
    /// needConfirmation property.
    pub need_confirmation: Option<bool>,
    /// needEmail property.
    pub need_email: Option<bool>,
    /// nickName property.
    pub nick_name: Option<String>,
    /// oauthAccessToken property.
    pub oauth_access_token: Option<String>,
    /// oauthAuthorizationCode property.
    pub oauth_authorization_code: Option<String>,
    /// oauthExpireIn property.
    pub oauth_expire_in: Option<i64>,
    /// oauthIdToken property.
    pub oauth_id_token: Option<String>,
    /// oauthRequestToken property.
    pub oauth_request_token: Option<String>,
    /// oauthScope property.
    pub oauth_scope: Option<String>,
    /// oauthTokenSecret property.
    pub oauth_token_secret: Option<String>,
    /// originalEmail property.
    pub original_email: Option<String>,
    /// photoUrl property.
    pub photo_url: Option<String>,
    /// providerId property.
    pub provider_id: Option<String>,
    /// rawUserInfo property.
    pub raw_user_info: Option<String>,
    /// refreshToken property.
    pub refresh_token: Option<String>,
    /// screenName property.
    pub screen_name: Option<String>,
    /// timeZone property.
    pub time_zone: Option<String>,
    /// verifiedProvider property.
    pub verified_provider: Option<Vec<String>>,
}

/// `IdentitytoolkitRelyingpartySetProjectConfigRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct IdentitytoolkitRelyingpartySetProjectConfigRequest {
    /// allowPasswordUser property.
    pub allow_password_user: Option<bool>,
    /// apiKey property.
    pub api_key: Option<String>,
    /// authorizedDomains property.
    pub authorized_domains: Option<Vec<String>>,
    /// changeEmailTemplate property.
    pub change_email_template: Option<EmailTemplate>,
    /// delegatedProjectNumber property.
    pub delegated_project_number: Option<String>,
    /// enableAnonymousUser property.
    pub enable_anonymous_user: Option<bool>,
    /// idpConfig property.
    pub idp_config: Option<Vec<IdpConfig>>,
    /// legacyResetPasswordTemplate property.
    pub legacy_reset_password_template: Option<EmailTemplate>,
    /// resetPasswordTemplate property.
    pub reset_password_template: Option<EmailTemplate>,
    /// useEmailSending property.
    pub use_email_sending: Option<bool>,
    /// verifyEmailTemplate property.
    pub verify_email_template: Option<EmailTemplate>,
}

/// `UserInfo` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct UserInfo {
    /// createdAt property.
    pub created_at: Option<String>,
    /// customAttributes property.
    pub custom_attributes: Option<String>,
    /// customAuth property.
    pub custom_auth: Option<bool>,
    /// disabled property.
    pub disabled: Option<bool>,
    /// displayName property.
    pub display_name: Option<String>,
    /// email property.
    pub email: Option<String>,
    /// emailVerified property.
    pub email_verified: Option<bool>,
    /// lastLoginAt property.
    pub last_login_at: Option<String>,
    /// localId property.
    pub local_id: Option<String>,
    /// passwordHash property.
    pub password_hash: Option<String>,
    /// passwordUpdatedAt property.
    pub password_updated_at: Option<f64>,
    /// phoneNumber property.
    pub phone_number: Option<String>,
    /// photoUrl property.
    pub photo_url: Option<String>,
    /// providerUserInfo property.
    pub provider_user_info: Option<Vec<std::collections::HashMap<String, serde_json::Value>>>,
    /// rawPassword property.
    pub raw_password: Option<String>,
    /// salt property.
    pub salt: Option<String>,
    /// screenName property.
    pub screen_name: Option<String>,
    /// validSince property.
    pub valid_since: Option<String>,
    /// version property.
    pub version: Option<i64>,
}

/// `IdentitytoolkitRelyingpartySignupNewUserRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct IdentitytoolkitRelyingpartySignupNewUserRequest {
    /// captchaChallenge property.
    pub captcha_challenge: Option<String>,
    /// captchaResponse property.
    pub captcha_response: Option<String>,
    /// disabled property.
    pub disabled: Option<bool>,
    /// displayName property.
    pub display_name: Option<String>,
    /// email property.
    pub email: Option<String>,
    /// emailVerified property.
    pub email_verified: Option<bool>,
    /// idToken property.
    pub id_token: Option<String>,
    /// instanceId property.
    pub instance_id: Option<String>,
    /// localId property.
    pub local_id: Option<String>,
    /// password property.
    pub password: Option<String>,
    /// phoneNumber property.
    pub phone_number: Option<String>,
    /// photoUrl property.
    pub photo_url: Option<String>,
    /// tenantId property.
    pub tenant_id: Option<String>,
    /// tenantProjectNumber property.
    pub tenant_project_number: Option<String>,
}

/// `EmailLinkSigninResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct EmailLinkSigninResponse {
    /// email property.
    pub email: Option<String>,
    /// expiresIn property.
    pub expires_in: Option<String>,
    /// idToken property.
    pub id_token: Option<String>,
    /// isNewUser property.
    pub is_new_user: Option<bool>,
    /// kind property.
    pub kind: Option<String>,
    /// localId property.
    pub local_id: Option<String>,
    /// refreshToken property.
    pub refresh_token: Option<String>,
}

/// `SetAccountInfoResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SetAccountInfoResponse {
    /// displayName property.
    pub display_name: Option<String>,
    /// email property.
    pub email: Option<String>,
    /// emailVerified property.
    pub email_verified: Option<bool>,
    /// expiresIn property.
    pub expires_in: Option<String>,
    /// idToken property.
    pub id_token: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// localId property.
    pub local_id: Option<String>,
    /// newEmail property.
    pub new_email: Option<String>,
    /// passwordHash property.
    pub password_hash: Option<String>,
    /// photoUrl property.
    pub photo_url: Option<String>,
    /// providerUserInfo property.
    pub provider_user_info: Option<Vec<std::collections::HashMap<String, serde_json::Value>>>,
    /// refreshToken property.
    pub refresh_token: Option<String>,
}

/// `DownloadAccountResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct DownloadAccountResponse {
    /// kind property.
    pub kind: Option<String>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// users property.
    pub users: Option<Vec<UserInfo>>,
}

/// `ResetPasswordResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ResetPasswordResponse {
    /// email property.
    pub email: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// newEmail property.
    pub new_email: Option<String>,
    /// requestType property.
    pub request_type: Option<String>,
}

/// `IdentitytoolkitRelyingpartyGetProjectConfigResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct IdentitytoolkitRelyingpartyGetProjectConfigResponse {
    /// allowPasswordUser property.
    pub allow_password_user: Option<bool>,
    /// apiKey property.
    pub api_key: Option<String>,
    /// authorizedDomains property.
    pub authorized_domains: Option<Vec<String>>,
    /// changeEmailTemplate property.
    pub change_email_template: Option<EmailTemplate>,
    /// dynamicLinksDomain property.
    pub dynamic_links_domain: Option<String>,
    /// enableAnonymousUser property.
    pub enable_anonymous_user: Option<bool>,
    /// idpConfig property.
    pub idp_config: Option<Vec<IdpConfig>>,
    /// legacyResetPasswordTemplate property.
    pub legacy_reset_password_template: Option<EmailTemplate>,
    /// projectId property.
    pub project_id: Option<String>,
    /// resetPasswordTemplate property.
    pub reset_password_template: Option<EmailTemplate>,
    /// useEmailSending property.
    pub use_email_sending: Option<bool>,
    /// verifyEmailTemplate property.
    pub verify_email_template: Option<EmailTemplate>,
}

/// `IdentitytoolkitRelyingpartyEmailLinkSigninRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct IdentitytoolkitRelyingpartyEmailLinkSigninRequest {
    /// email property.
    pub email: Option<String>,
    /// idToken property.
    pub id_token: Option<String>,
    /// oobCode property.
    pub oob_code: Option<String>,
}

/// `IdentitytoolkitRelyingpartyGetAccountInfoRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct IdentitytoolkitRelyingpartyGetAccountInfoRequest {
    /// delegatedProjectNumber property.
    pub delegated_project_number: Option<String>,
    /// email property.
    pub email: Option<Vec<String>>,
    /// idToken property.
    pub id_token: Option<String>,
    /// localId property.
    pub local_id: Option<Vec<String>>,
    /// phoneNumber property.
    pub phone_number: Option<Vec<String>>,
}

/// `IdentitytoolkitRelyingpartySetProjectConfigResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct IdentitytoolkitRelyingpartySetProjectConfigResponse {
    /// projectId property.
    pub project_id: Option<String>,
}

/// `SignupNewUserResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SignupNewUserResponse {
    /// displayName property.
    pub display_name: Option<String>,
    /// email property.
    pub email: Option<String>,
    /// expiresIn property.
    pub expires_in: Option<String>,
    /// idToken property.
    pub id_token: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// localId property.
    pub local_id: Option<String>,
    /// refreshToken property.
    pub refresh_token: Option<String>,
}

/// `IdentitytoolkitRelyingpartyVerifyPhoneNumberRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct IdentitytoolkitRelyingpartyVerifyPhoneNumberRequest {
    /// code property.
    pub code: Option<String>,
    /// idToken property.
    pub id_token: Option<String>,
    /// operation property.
    pub operation: Option<String>,
    /// phoneNumber property.
    pub phone_number: Option<String>,
    /// sessionInfo property.
    pub session_info: Option<String>,
    /// temporaryProof property.
    pub temporary_proof: Option<String>,
    /// verificationProof property.
    pub verification_proof: Option<String>,
}

// =============================================================================
// ARGS TYPES (per-endpoint)
// =============================================================================

/// Arguments for [`identitytoolkit.relyingparty.createAuthUri_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct IdentitytoolkitRelyingpartyCreateAuthUriArgs {
    /// Request body.
    pub body: IdentitytoolkitRelyingpartyCreateAuthUriRequest,
}

/// Arguments for [`identitytoolkit.relyingparty.deleteAccount_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct IdentitytoolkitRelyingpartyDeleteAccountArgs {
    /// Request body.
    pub body: IdentitytoolkitRelyingpartyDeleteAccountRequest,
}

/// Arguments for [`identitytoolkit.relyingparty.downloadAccount_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct IdentitytoolkitRelyingpartyDownloadAccountArgs {
    /// Request body.
    pub body: IdentitytoolkitRelyingpartyDownloadAccountRequest,
}

/// Arguments for [`identitytoolkit.relyingparty.emailLinkSignin_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct IdentitytoolkitRelyingpartyEmailLinkSigninArgs {
    /// Request body.
    pub body: IdentitytoolkitRelyingpartyEmailLinkSigninRequest,
}

/// Arguments for [`identitytoolkit.relyingparty.getAccountInfo_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct IdentitytoolkitRelyingpartyGetAccountInfoArgs {
    /// Request body.
    pub body: IdentitytoolkitRelyingpartyGetAccountInfoRequest,
}

/// Arguments for [`identitytoolkit.relyingparty.getOobConfirmationCode_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct IdentitytoolkitRelyingpartyGetOobConfirmationCodeArgs {
    /// Request body.
    pub body: Relyingparty,
}

/// Arguments for [`identitytoolkit.relyingparty.getProjectConfig_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct IdentitytoolkitRelyingpartyGetProjectConfigArgs {
    /// Query parameter: `delegatedProjectNumber`.
    pub delegated_project_number: Option<String>,
    /// Query parameter: `projectNumber`.
    pub project_number: Option<String>,
}

/// Arguments for [`identitytoolkit.relyingparty.resetPassword_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct IdentitytoolkitRelyingpartyResetPasswordArgs {
    /// Request body.
    pub body: IdentitytoolkitRelyingpartyResetPasswordRequest,
}

/// Arguments for [`identitytoolkit.relyingparty.sendVerificationCode_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct IdentitytoolkitRelyingpartySendVerificationCodeArgs {
    /// Request body.
    pub body: IdentitytoolkitRelyingpartySendVerificationCodeRequest,
}

/// Arguments for [`identitytoolkit.relyingparty.setAccountInfo_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct IdentitytoolkitRelyingpartySetAccountInfoArgs {
    /// Request body.
    pub body: IdentitytoolkitRelyingpartySetAccountInfoRequest,
}

/// Arguments for [`identitytoolkit.relyingparty.setProjectConfig_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct IdentitytoolkitRelyingpartySetProjectConfigArgs {
    /// Request body.
    pub body: IdentitytoolkitRelyingpartySetProjectConfigRequest,
}

/// Arguments for [`identitytoolkit.relyingparty.signOutUser_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct IdentitytoolkitRelyingpartySignOutUserArgs {
    /// Request body.
    pub body: IdentitytoolkitRelyingpartySignOutUserRequest,
}

/// Arguments for [`identitytoolkit.relyingparty.signupNewUser_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct IdentitytoolkitRelyingpartySignupNewUserArgs {
    /// Request body.
    pub body: IdentitytoolkitRelyingpartySignupNewUserRequest,
}

/// Arguments for [`identitytoolkit.relyingparty.uploadAccount_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct IdentitytoolkitRelyingpartyUploadAccountArgs {
    /// Request body.
    pub body: IdentitytoolkitRelyingpartyUploadAccountRequest,
}

/// Arguments for [`identitytoolkit.relyingparty.verifyAssertion_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct IdentitytoolkitRelyingpartyVerifyAssertionArgs {
    /// Request body.
    pub body: IdentitytoolkitRelyingpartyVerifyAssertionRequest,
}

/// Arguments for [`identitytoolkit.relyingparty.verifyCustomToken_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct IdentitytoolkitRelyingpartyVerifyCustomTokenArgs {
    /// Request body.
    pub body: IdentitytoolkitRelyingpartyVerifyCustomTokenRequest,
}

/// Arguments for [`identitytoolkit.relyingparty.verifyPassword_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct IdentitytoolkitRelyingpartyVerifyPasswordArgs {
    /// Request body.
    pub body: IdentitytoolkitRelyingpartyVerifyPasswordRequest,
}

/// Arguments for [`identitytoolkit.relyingparty.verifyPhoneNumber_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct IdentitytoolkitRelyingpartyVerifyPhoneNumberArgs {
    /// Request body.
    pub body: IdentitytoolkitRelyingpartyVerifyPhoneNumberRequest,
}

// =============================================================================
// CLIENT FUNCTIONS (per-endpoint)
// =============================================================================

// -----------------------------------------------------------------------------
// POST createAuthUri
// -----------------------------------------------------------------------------

/// POST createAuthUri.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = identitytoolkit_relyingparty_create_auth_uri_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn identitytoolkit_relyingparty_create_auth_uri_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &IdentitytoolkitRelyingpartyCreateAuthUriArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<CreateAuthUriResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url =
        format!("https://www.googleapis.com/identitytoolkit/v3/relyingparty/createAuthUri",);

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: CreateAuthUriResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST deleteAccount
// -----------------------------------------------------------------------------

/// POST deleteAccount.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = identitytoolkit_relyingparty_delete_account_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn identitytoolkit_relyingparty_delete_account_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &IdentitytoolkitRelyingpartyDeleteAccountArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<DeleteAccountResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url =
        format!("https://www.googleapis.com/identitytoolkit/v3/relyingparty/deleteAccount",);

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: DeleteAccountResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST downloadAccount
// -----------------------------------------------------------------------------

/// POST downloadAccount.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = identitytoolkit_relyingparty_download_account_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn identitytoolkit_relyingparty_download_account_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &IdentitytoolkitRelyingpartyDownloadAccountArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<DownloadAccountResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url =
        format!("https://www.googleapis.com/identitytoolkit/v3/relyingparty/downloadAccount",);

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: DownloadAccountResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST emailLinkSignin
// -----------------------------------------------------------------------------

/// POST emailLinkSignin.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = identitytoolkit_relyingparty_email_link_signin_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn identitytoolkit_relyingparty_email_link_signin_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &IdentitytoolkitRelyingpartyEmailLinkSigninArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<EmailLinkSigninResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url =
        format!("https://www.googleapis.com/identitytoolkit/v3/relyingparty/emailLinkSignin",);

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: EmailLinkSigninResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST getAccountInfo
// -----------------------------------------------------------------------------

/// POST getAccountInfo.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = identitytoolkit_relyingparty_get_account_info_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn identitytoolkit_relyingparty_get_account_info_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &IdentitytoolkitRelyingpartyGetAccountInfoArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GetAccountInfoResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url =
        format!("https://www.googleapis.com/identitytoolkit/v3/relyingparty/getAccountInfo",);

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GetAccountInfoResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST getOobConfirmationCode
// -----------------------------------------------------------------------------

/// POST getOobConfirmationCode.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = identitytoolkit_relyingparty_get_oob_confirmation_code_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn identitytoolkit_relyingparty_get_oob_confirmation_code_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &IdentitytoolkitRelyingpartyGetOobConfirmationCodeArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GetOobConfirmationCodeResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://www.googleapis.com/identitytoolkit/v3/relyingparty/getOobConfirmationCode",
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GetOobConfirmationCodeResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET getProjectConfig
// -----------------------------------------------------------------------------

/// GET getProjectConfig.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = identitytoolkit_relyingparty_get_project_config_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn identitytoolkit_relyingparty_get_project_config_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &IdentitytoolkitRelyingpartyGetProjectConfigArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<IdentitytoolkitRelyingpartyGetProjectConfigResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url =
        format!("https://www.googleapis.com/identitytoolkit/v3/relyingparty/getProjectConfig",);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.delegated_project_number {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("delegatedProjectNumber=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.project_number {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("projectNumber=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: IdentitytoolkitRelyingpartyGetProjectConfigResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET publicKeys
// -----------------------------------------------------------------------------

/// GET publicKeys.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = identitytoolkit_relyingparty_get_public_keys_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn identitytoolkit_relyingparty_get_public_keys_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &IdentitytoolkitRelyingpartyGetPublicKeysArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<IdentitytoolkitRelyingpartyGetPublicKeysResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url =
        format!("https://www.googleapis.com/identitytoolkit/v3/relyingparty/publicKeys",);

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: IdentitytoolkitRelyingpartyGetPublicKeysResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET getRecaptchaParam
// -----------------------------------------------------------------------------

/// GET getRecaptchaParam.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = identitytoolkit_relyingparty_get_recaptcha_param_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn identitytoolkit_relyingparty_get_recaptcha_param_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &IdentitytoolkitRelyingpartyGetRecaptchaParamArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GetRecaptchaParamResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url =
        format!("https://www.googleapis.com/identitytoolkit/v3/relyingparty/getRecaptchaParam",);

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GetRecaptchaParamResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST resetPassword
// -----------------------------------------------------------------------------

/// POST resetPassword.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = identitytoolkit_relyingparty_reset_password_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn identitytoolkit_relyingparty_reset_password_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &IdentitytoolkitRelyingpartyResetPasswordArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ResetPasswordResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url =
        format!("https://www.googleapis.com/identitytoolkit/v3/relyingparty/resetPassword",);

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ResetPasswordResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST sendVerificationCode
// -----------------------------------------------------------------------------

/// POST sendVerificationCode.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = identitytoolkit_relyingparty_send_verification_code_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn identitytoolkit_relyingparty_send_verification_code_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &IdentitytoolkitRelyingpartySendVerificationCodeArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<IdentitytoolkitRelyingpartySendVerificationCodeResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url =
        format!("https://www.googleapis.com/identitytoolkit/v3/relyingparty/sendVerificationCode",);

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: IdentitytoolkitRelyingpartySendVerificationCodeResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST setAccountInfo
// -----------------------------------------------------------------------------

/// POST setAccountInfo.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = identitytoolkit_relyingparty_set_account_info_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn identitytoolkit_relyingparty_set_account_info_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &IdentitytoolkitRelyingpartySetAccountInfoArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SetAccountInfoResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url =
        format!("https://www.googleapis.com/identitytoolkit/v3/relyingparty/setAccountInfo",);

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: SetAccountInfoResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST setProjectConfig
// -----------------------------------------------------------------------------

/// POST setProjectConfig.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = identitytoolkit_relyingparty_set_project_config_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn identitytoolkit_relyingparty_set_project_config_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &IdentitytoolkitRelyingpartySetProjectConfigArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<IdentitytoolkitRelyingpartySetProjectConfigResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url =
        format!("https://www.googleapis.com/identitytoolkit/v3/relyingparty/setProjectConfig",);

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: IdentitytoolkitRelyingpartySetProjectConfigResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST signOutUser
// -----------------------------------------------------------------------------

/// POST signOutUser.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = identitytoolkit_relyingparty_sign_out_user_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn identitytoolkit_relyingparty_sign_out_user_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &IdentitytoolkitRelyingpartySignOutUserArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<IdentitytoolkitRelyingpartySignOutUserResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url =
        format!("https://www.googleapis.com/identitytoolkit/v3/relyingparty/signOutUser",);

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: IdentitytoolkitRelyingpartySignOutUserResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST signupNewUser
// -----------------------------------------------------------------------------

/// POST signupNewUser.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = identitytoolkit_relyingparty_signup_new_user_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn identitytoolkit_relyingparty_signup_new_user_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &IdentitytoolkitRelyingpartySignupNewUserArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SignupNewUserResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url =
        format!("https://www.googleapis.com/identitytoolkit/v3/relyingparty/signupNewUser",);

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: SignupNewUserResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST uploadAccount
// -----------------------------------------------------------------------------

/// POST uploadAccount.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = identitytoolkit_relyingparty_upload_account_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn identitytoolkit_relyingparty_upload_account_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &IdentitytoolkitRelyingpartyUploadAccountArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<UploadAccountResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url =
        format!("https://www.googleapis.com/identitytoolkit/v3/relyingparty/uploadAccount",);

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: UploadAccountResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST verifyAssertion
// -----------------------------------------------------------------------------

/// POST verifyAssertion.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = identitytoolkit_relyingparty_verify_assertion_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn identitytoolkit_relyingparty_verify_assertion_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &IdentitytoolkitRelyingpartyVerifyAssertionArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<VerifyAssertionResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url =
        format!("https://www.googleapis.com/identitytoolkit/v3/relyingparty/verifyAssertion",);

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: VerifyAssertionResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST verifyCustomToken
// -----------------------------------------------------------------------------

/// POST verifyCustomToken.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = identitytoolkit_relyingparty_verify_custom_token_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn identitytoolkit_relyingparty_verify_custom_token_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &IdentitytoolkitRelyingpartyVerifyCustomTokenArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<VerifyCustomTokenResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url =
        format!("https://www.googleapis.com/identitytoolkit/v3/relyingparty/verifyCustomToken",);

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: VerifyCustomTokenResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST verifyPassword
// -----------------------------------------------------------------------------

/// POST verifyPassword.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = identitytoolkit_relyingparty_verify_password_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn identitytoolkit_relyingparty_verify_password_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &IdentitytoolkitRelyingpartyVerifyPasswordArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<VerifyPasswordResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url =
        format!("https://www.googleapis.com/identitytoolkit/v3/relyingparty/verifyPassword",);

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: VerifyPasswordResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST verifyPhoneNumber
// -----------------------------------------------------------------------------

/// POST verifyPhoneNumber.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = identitytoolkit_relyingparty_verify_phone_number_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn identitytoolkit_relyingparty_verify_phone_number_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &IdentitytoolkitRelyingpartyVerifyPhoneNumberArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<IdentitytoolkitRelyingpartyVerifyPhoneNumberResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url =
        format!("https://www.googleapis.com/identitytoolkit/v3/relyingparty/verifyPhoneNumber",);

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: IdentitytoolkitRelyingpartyVerifyPhoneNumberResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}
