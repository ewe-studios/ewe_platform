//! Auto-generated API module for gcp/fitness fitness.
//!
//! Generated by `cargo run --bin ewe_platform gen_api`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp_fitness_fitness `

#![cfg(feature = "gcp_fitness_fitness")]
#![allow(clippy::too_many_arguments, clippy::type_complexity)]
#![allow(
    clippy::missing_errors_doc,
    clippy::doc_markdown,
    clippy::useless_format
)]
#![allow(unused_imports)]

use foundation_core::valtron::{TaskIterator, TaskIteratorExt};
use foundation_core::wire::simple_http::client::{ClientRequestBuilder, SimpleHttpClient};
use foundation_macros::JsonHash;
use serde::{Deserialize, Serialize};

use super::shared::ApiResponse;

// =============================================================================
// TYPE DECLARATIONS
// =============================================================================

/// `Value` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Value {
    /// fpVal property.
    pub fp_val: Option<f64>,
    /// intVal property.
    pub int_val: Option<i64>,
    /// mapVal property.
    pub map_val: Option<Vec<ValueMapValEntry>>,
    /// stringVal property.
    pub string_val: Option<String>,
}

/// `ValueMapValEntry` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ValueMapValEntry {
    /// key property.
    pub key: Option<String>,
    /// value property.
    pub value: Option<MapValue>,
}

/// `DataType` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct DataType {
    /// field property.
    pub field: Option<Vec<DataTypeField>>,
    /// name property.
    pub name: Option<String>,
}

/// `DataTypeField` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct DataTypeField {
    /// format property.
    pub format: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// optional property.
    pub optional: Option<bool>,
}

/// `AggregateBucket` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AggregateBucket {
    /// activity property.
    pub activity: Option<i64>,
    /// dataset property.
    pub dataset: Option<Vec<Dataset>>,
    /// endTimeMillis property.
    pub end_time_millis: Option<String>,
    /// session property.
    pub session: Option<Session>,
    /// startTimeMillis property.
    pub start_time_millis: Option<String>,
    /// type property.
    pub r#type: Option<String>,
}

/// `ListDataSourcesResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ListDataSourcesResponse {
    /// dataSource property.
    pub data_source: Option<Vec<DataSource>>,
}

/// `AggregateBy` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AggregateBy {
    /// dataSourceId property.
    pub data_source_id: Option<String>,
    /// dataTypeName property.
    pub data_type_name: Option<String>,
}

/// `Session` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Session {
    /// activeTimeMillis property.
    pub active_time_millis: Option<String>,
    /// activityType property.
    pub activity_type: Option<i64>,
    /// application property.
    pub application: Option<Application>,
    /// description property.
    pub description: Option<String>,
    /// endTimeMillis property.
    pub end_time_millis: Option<String>,
    /// id property.
    pub id: Option<String>,
    /// modifiedTimeMillis property.
    pub modified_time_millis: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// startTimeMillis property.
    pub start_time_millis: Option<String>,
}

/// `BucketBySession` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct BucketBySession {
    /// minDurationMillis property.
    pub min_duration_millis: Option<String>,
}

/// `BucketByTimePeriod` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct BucketByTimePeriod {
    /// timeZoneId property.
    pub time_zone_id: Option<String>,
    /// type property.
    pub r#type: Option<String>,
    /// value property.
    pub value: Option<i64>,
}

/// `BucketByActivity` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct BucketByActivity {
    /// activityDataSourceId property.
    pub activity_data_source_id: Option<String>,
    /// minDurationMillis property.
    pub min_duration_millis: Option<String>,
}

/// `DataSource` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct DataSource {
    /// application property.
    pub application: Option<Application>,
    /// dataQualityStandard property.
    pub data_quality_standard: Option<Vec<String>>,
    /// dataStreamId property.
    pub data_stream_id: Option<String>,
    /// dataStreamName property.
    pub data_stream_name: Option<String>,
    /// dataType property.
    pub data_type: Option<DataType>,
    /// device property.
    pub device: Option<Device>,
    /// name property.
    pub name: Option<String>,
    /// type property.
    pub r#type: Option<String>,
}

/// `AggregateRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AggregateRequest {
    /// aggregateBy property.
    pub aggregate_by: Option<Vec<AggregateBy>>,
    /// bucketByActivitySegment property.
    pub bucket_by_activity_segment: Option<BucketByActivity>,
    /// bucketByActivityType property.
    pub bucket_by_activity_type: Option<BucketByActivity>,
    /// bucketBySession property.
    pub bucket_by_session: Option<BucketBySession>,
    /// bucketByTime property.
    pub bucket_by_time: Option<BucketByTime>,
    /// endTimeMillis property.
    pub end_time_millis: Option<String>,
    /// filteredDataQualityStandard property.
    pub filtered_data_quality_standard: Option<Vec<String>>,
    /// startTimeMillis property.
    pub start_time_millis: Option<String>,
}

/// `Dataset` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Dataset {
    /// dataSourceId property.
    pub data_source_id: Option<String>,
    /// maxEndTimeNs property.
    pub max_end_time_ns: Option<String>,
    /// minStartTimeNs property.
    pub min_start_time_ns: Option<String>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// point property.
    pub point: Option<Vec<DataPoint>>,
}

/// `MapValue` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct MapValue {
    /// fpVal property.
    pub fp_val: Option<f64>,
}

/// `ListSessionsResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ListSessionsResponse {
    /// deletedSession property.
    pub deleted_session: Option<Vec<Session>>,
    /// hasMoreData property.
    pub has_more_data: Option<bool>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// session property.
    pub session: Option<Vec<Session>>,
}

/// `Device` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Device {
    /// manufacturer property.
    pub manufacturer: Option<String>,
    /// model property.
    pub model: Option<String>,
    /// type property.
    pub r#type: Option<String>,
    /// uid property.
    pub uid: Option<String>,
    /// version property.
    pub version: Option<String>,
}

/// `Application` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Application {
    /// detailsUrl property.
    pub details_url: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// packageName property.
    pub package_name: Option<String>,
    /// version property.
    pub version: Option<String>,
}

/// `DataPoint` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct DataPoint {
    /// computationTimeMillis property.
    pub computation_time_millis: Option<String>,
    /// dataTypeName property.
    pub data_type_name: Option<String>,
    /// endTimeNanos property.
    pub end_time_nanos: Option<String>,
    /// modifiedTimeMillis property.
    pub modified_time_millis: Option<String>,
    /// originDataSourceId property.
    pub origin_data_source_id: Option<String>,
    /// rawTimestampNanos property.
    pub raw_timestamp_nanos: Option<String>,
    /// startTimeNanos property.
    pub start_time_nanos: Option<String>,
    /// value property.
    pub value: Option<Vec<Value>>,
}

/// `BucketByTime` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct BucketByTime {
    /// durationMillis property.
    pub duration_millis: Option<String>,
    /// period property.
    pub period: Option<BucketByTimePeriod>,
}

/// `ListDataPointChangesResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ListDataPointChangesResponse {
    /// dataSourceId property.
    pub data_source_id: Option<String>,
    /// deletedDataPoint property.
    pub deleted_data_point: Option<Vec<DataPoint>>,
    /// insertedDataPoint property.
    pub inserted_data_point: Option<Vec<DataPoint>>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
}

/// `AggregateResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AggregateResponse {
    /// bucket property.
    pub bucket: Option<Vec<AggregateBucket>>,
}

// =============================================================================
// ARGS TYPES (per-endpoint)
// =============================================================================

/// Arguments for [`fitness.users.dataSources.create_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct FitnessUsersDataSourcesCreateArgs {
    /// Path parameter: `userId`.
    pub user_id: String,
    /// Request body.
    pub body: DataSource,
}

/// Arguments for [`fitness.users.dataSources.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct FitnessUsersDataSourcesDeleteArgs {
    /// Path parameter: `userId`.
    pub user_id: String,
    /// Path parameter: `dataSourceId`.
    pub data_source_id: String,
}

/// Arguments for [`fitness.users.dataSources.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct FitnessUsersDataSourcesGetArgs {
    /// Path parameter: `userId`.
    pub user_id: String,
    /// Path parameter: `dataSourceId`.
    pub data_source_id: String,
}

/// Arguments for [`fitness.users.dataSources.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct FitnessUsersDataSourcesListArgs {
    /// Path parameter: `userId`.
    pub user_id: String,
    /// Query parameter: `dataTypeName`.
    pub data_type_name: Option<String>,
}

/// Arguments for [`fitness.users.dataSources.update_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct FitnessUsersDataSourcesUpdateArgs {
    /// Path parameter: `userId`.
    pub user_id: String,
    /// Path parameter: `dataSourceId`.
    pub data_source_id: String,
    /// Request body.
    pub body: DataSource,
}

/// Arguments for [`fitness.users.dataSources.dataPointChanges.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct FitnessUsersDataSourcesDataPointChangesListArgs {
    /// Path parameter: `userId`.
    pub user_id: String,
    /// Path parameter: `dataSourceId`.
    pub data_source_id: String,
    /// Query parameter: `limit`.
    pub limit: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

/// Arguments for [`fitness.users.dataSources.datasets.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct FitnessUsersDataSourcesDatasetsDeleteArgs {
    /// Path parameter: `userId`.
    pub user_id: String,
    /// Path parameter: `dataSourceId`.
    pub data_source_id: String,
    /// Path parameter: `datasetId`.
    pub dataset_id: String,
}

/// Arguments for [`fitness.users.dataSources.datasets.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct FitnessUsersDataSourcesDatasetsGetArgs {
    /// Path parameter: `userId`.
    pub user_id: String,
    /// Path parameter: `dataSourceId`.
    pub data_source_id: String,
    /// Path parameter: `datasetId`.
    pub dataset_id: String,
    /// Query parameter: `limit`.
    pub limit: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

/// Arguments for [`fitness.users.dataSources.datasets.patch_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct FitnessUsersDataSourcesDatasetsPatchArgs {
    /// Path parameter: `userId`.
    pub user_id: String,
    /// Path parameter: `dataSourceId`.
    pub data_source_id: String,
    /// Path parameter: `datasetId`.
    pub dataset_id: String,
    /// Request body.
    pub body: Dataset,
}

/// Arguments for [`fitness.users.dataset.aggregate_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct FitnessUsersDatasetAggregateArgs {
    /// Path parameter: `userId`.
    pub user_id: String,
    /// Request body.
    pub body: AggregateRequest,
}

/// Arguments for [`fitness.users.sessions.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct FitnessUsersSessionsDeleteArgs {
    /// Path parameter: `userId`.
    pub user_id: String,
    /// Path parameter: `sessionId`.
    pub session_id: String,
}

/// Arguments for [`fitness.users.sessions.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct FitnessUsersSessionsListArgs {
    /// Path parameter: `userId`.
    pub user_id: String,
    /// Query parameter: `activityType`.
    pub activity_type: Option<String>,
    /// Query parameter: `endTime`.
    pub end_time: Option<String>,
    /// Query parameter: `includeDeleted`.
    pub include_deleted: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `startTime`.
    pub start_time: Option<String>,
}

/// Arguments for [`fitness.users.sessions.update_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct FitnessUsersSessionsUpdateArgs {
    /// Path parameter: `userId`.
    pub user_id: String,
    /// Path parameter: `sessionId`.
    pub session_id: String,
    /// Request body.
    pub body: Session,
}

// =============================================================================
// CLIENT FUNCTIONS (per-endpoint)
// =============================================================================

// -----------------------------------------------------------------------------
// POST {userId}/dataSources
// -----------------------------------------------------------------------------

/// POST {userId}/dataSources.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = fitness_users_data_sources_create_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn fitness_users_data_sources_create_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &FitnessUsersDataSourcesCreateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<DataSource>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://fitness.googleapis.com/fitness/v1/users/{}/dataSources",
        args.user_id,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: DataSource =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE {userId}/dataSources/{dataSourceId}
// -----------------------------------------------------------------------------

/// DELETE {userId}/dataSources/{dataSourceId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = fitness_users_data_sources_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn fitness_users_data_sources_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &FitnessUsersDataSourcesDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<DataSource>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://fitness.googleapis.com/fitness/v1/users/{}/dataSources/{}",
        args.user_id, args.data_source_id,
    );

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: DataSource =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET {userId}/dataSources/{dataSourceId}
// -----------------------------------------------------------------------------

/// GET {userId}/dataSources/{dataSourceId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = fitness_users_data_sources_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn fitness_users_data_sources_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &FitnessUsersDataSourcesGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<DataSource>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://fitness.googleapis.com/fitness/v1/users/{}/dataSources/{}",
        args.user_id, args.data_source_id,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: DataSource =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET {userId}/dataSources
// -----------------------------------------------------------------------------

/// GET {userId}/dataSources.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = fitness_users_data_sources_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn fitness_users_data_sources_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &FitnessUsersDataSourcesListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListDataSourcesResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://fitness.googleapis.com/fitness/v1/users/{}/dataSources",
        args.user_id,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.data_type_name {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("dataTypeName=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ListDataSourcesResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PUT {userId}/dataSources/{dataSourceId}
// -----------------------------------------------------------------------------

/// PUT {userId}/dataSources/{dataSourceId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = fitness_users_data_sources_update_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn fitness_users_data_sources_update_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &FitnessUsersDataSourcesUpdateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<DataSource>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://fitness.googleapis.com/fitness/v1/users/{}/dataSources/{}",
        args.user_id, args.data_source_id,
    );

    let mut builder = client
        .put(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: DataSource =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET {userId}/dataSources/{dataSourceId}/dataPointChanges
// -----------------------------------------------------------------------------

/// GET {userId}/dataSources/{dataSourceId}/dataPointChanges.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = fitness_users_data_sources_data_point_changes_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn fitness_users_data_sources_data_point_changes_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &FitnessUsersDataSourcesDataPointChangesListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListDataPointChangesResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://fitness.googleapis.com/fitness/v1/users/{}/dataSources/{}/dataPointChanges",
        args.user_id, args.data_source_id,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.limit {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("limit=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ListDataPointChangesResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE {userId}/dataSources/{dataSourceId}/datasets/{datasetId}
// -----------------------------------------------------------------------------

/// DELETE {userId}/dataSources/{dataSourceId}/datasets/{datasetId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = fitness_users_data_sources_datasets_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn fitness_users_data_sources_datasets_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &FitnessUsersDataSourcesDatasetsDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://fitness.googleapis.com/fitness/v1/users/{}/dataSources/{}/datasets/{}",
        args.user_id, args.data_source_id, args.dataset_id,
    );

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream: _,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET {userId}/dataSources/{dataSourceId}/datasets/{datasetId}
// -----------------------------------------------------------------------------

/// GET {userId}/dataSources/{dataSourceId}/datasets/{datasetId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = fitness_users_data_sources_datasets_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn fitness_users_data_sources_datasets_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &FitnessUsersDataSourcesDatasetsGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Dataset>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://fitness.googleapis.com/fitness/v1/users/{}/dataSources/{}/datasets/{}",
        args.user_id, args.data_source_id, args.dataset_id,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.limit {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("limit=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Dataset =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PATCH {userId}/dataSources/{dataSourceId}/datasets/{datasetId}
// -----------------------------------------------------------------------------

/// PATCH {userId}/dataSources/{dataSourceId}/datasets/{datasetId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = fitness_users_data_sources_datasets_patch_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn fitness_users_data_sources_datasets_patch_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &FitnessUsersDataSourcesDatasetsPatchArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Dataset>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://fitness.googleapis.com/fitness/v1/users/{}/dataSources/{}/datasets/{}",
        args.user_id, args.data_source_id, args.dataset_id,
    );

    let mut builder = client
        .patch(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Dataset =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST {userId}/dataset:aggregate
// -----------------------------------------------------------------------------

/// POST {userId}/dataset:aggregate.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = fitness_users_dataset_aggregate_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn fitness_users_dataset_aggregate_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &FitnessUsersDatasetAggregateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<AggregateResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://fitness.googleapis.com/fitness/v1/users/{}/dataset:aggregate",
        args.user_id,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: AggregateResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE {userId}/sessions/{sessionId}
// -----------------------------------------------------------------------------

/// DELETE {userId}/sessions/{sessionId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = fitness_users_sessions_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn fitness_users_sessions_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &FitnessUsersSessionsDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://fitness.googleapis.com/fitness/v1/users/{}/sessions/{}",
        args.user_id, args.session_id,
    );

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream: _,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET {userId}/sessions
// -----------------------------------------------------------------------------

/// GET {userId}/sessions.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = fitness_users_sessions_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn fitness_users_sessions_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &FitnessUsersSessionsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListSessionsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://fitness.googleapis.com/fitness/v1/users/{}/sessions",
        args.user_id,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.activity_type {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("activityType=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.end_time {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("endTime=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.include_deleted {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("includeDeleted=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.start_time {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("startTime=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ListSessionsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PUT {userId}/sessions/{sessionId}
// -----------------------------------------------------------------------------

/// PUT {userId}/sessions/{sessionId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = fitness_users_sessions_update_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn fitness_users_sessions_update_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &FitnessUsersSessionsUpdateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Session>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://fitness.googleapis.com/fitness/v1/users/{}/sessions/{}",
        args.user_id, args.session_id,
    );

    let mut builder = client
        .put(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Session =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}
