//! Auto-generated API module for gcp/iap v1.
//!
//! Generated by `cargo run --bin ewe_platform gen_api`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp_iap_group `

#![cfg(feature = "gcp_iap_group")]
#![allow(clippy::too_many_arguments, clippy::type_complexity)]
#![allow(
    clippy::missing_errors_doc,
    clippy::doc_markdown,
    clippy::useless_format
)]
#![allow(unused_imports)]

use foundation_core::valtron::{TaskIterator, TaskIteratorExt};
use foundation_core::wire::simple_http::client::{ClientRequestBuilder, SimpleHttpClient};
use foundation_macros::JsonHash;
use serde::{Deserialize, Serialize};

use super::shared::ApiResponse;

// =============================================================================
// TYPE DECLARATIONS
// =============================================================================

/// `AccessSettings` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct AccessSettings {
    /// allowedDomainsSettings property.
    pub allowed_domains_settings: Option<AllowedDomainsSettings>,
    /// corsSettings property.
    pub cors_settings: Option<CorsSettings>,
    /// gcipSettings property.
    pub gcip_settings: Option<GcipSettings>,
    /// identitySources property.
    pub identity_sources: Option<Vec<String>>,
    /// oauthSettings property.
    pub oauth_settings: Option<OAuthSettings>,
    /// policyDelegationSettings property.
    pub policy_delegation_settings: Option<PolicyDelegationSettings>,
    /// reauthSettings property.
    pub reauth_settings: Option<ReauthSettings>,
    /// workforceIdentitySettings property.
    pub workforce_identity_settings: Option<WorkforceIdentitySettings>,
}

/// `PolicyName` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct PolicyName {
    /// id property.
    pub id: Option<String>,
    /// region property.
    pub region: Option<String>,
    /// type property.
    pub r#type: Option<String>,
}

/// `IapSettings` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct IapSettings {
    /// accessSettings property.
    pub access_settings: Option<AccessSettings>,
    /// applicationSettings property.
    pub application_settings: Option<ApplicationSettings>,
    /// name property.
    pub name: Option<String>,
}

/// `Expr` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Expr {
    /// description property.
    pub description: Option<String>,
    /// expression property.
    pub expression: Option<String>,
    /// location property.
    pub location: Option<String>,
    /// title property.
    pub title: Option<String>,
}

/// `Resource` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Resource {
    /// expectedNextState property.
    pub expected_next_state: Option<serde_json::Value>,
    /// labels property.
    pub labels: Option<serde_json::Value>,
    /// locations property.
    pub locations: Option<Vec<String>>,
    /// name property.
    pub name: Option<String>,
    /// nextStateOfTags property.
    pub next_state_of_tags: Option<NextStateOfTags>,
    /// service property.
    pub service: Option<String>,
    /// type property.
    pub r#type: Option<String>,
}

/// `TagsPartialState` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct TagsPartialState {
    /// tagKeysToRemove property.
    pub tag_keys_to_remove: Option<Vec<String>>,
    /// tagsToUpsert property.
    pub tags_to_upsert: Option<serde_json::Value>,
}

/// `Policy` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Policy {
    /// bindings property.
    pub bindings: Option<Vec<Binding>>,
    /// etag property.
    pub etag: Option<String>,
    /// version property.
    pub version: Option<i64>,
}

/// `TagsFullState` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct TagsFullState {
    /// tags property.
    pub tags: Option<serde_json::Value>,
}

/// `ValidateIapAttributeExpressionResponse` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ValidateIapAttributeExpressionResponse {}

/// `CsmSettings` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct CsmSettings {
    /// rctokenAud property.
    pub rctoken_aud: Option<String>,
}

/// `ReauthSettings` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ReauthSettings {
    /// maxAge property.
    pub max_age: Option<String>,
    /// method property.
    pub method: Option<String>,
    /// policyType property.
    pub policy_type: Option<String>,
}

/// `AccessDeniedPageSettings` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct AccessDeniedPageSettings {
    /// accessDeniedPageUri property.
    pub access_denied_page_uri: Option<String>,
    /// generateTroubleshootingUri property.
    pub generate_troubleshooting_uri: Option<bool>,
    /// remediationTokenGenerationEnabled property.
    pub remediation_token_generation_enabled: Option<bool>,
}

/// `TestIamPermissionsResponse` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct TestIamPermissionsResponse {
    /// permissions property.
    pub permissions: Option<Vec<String>>,
}

/// `AttributePropagationSettings` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct AttributePropagationSettings {
    /// enable property.
    pub enable: Option<bool>,
    /// expression property.
    pub expression: Option<String>,
    /// outputCredentials property.
    pub output_credentials: Option<Vec<String>>,
}

/// `CorsSettings` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct CorsSettings {
    /// allowHttpOptions property.
    pub allow_http_options: Option<bool>,
}

/// `PolicyDelegationSettings` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct PolicyDelegationSettings {
    /// iamPermission property.
    pub iam_permission: Option<String>,
    /// iamServiceName property.
    pub iam_service_name: Option<String>,
    /// policyName property.
    pub policy_name: Option<PolicyName>,
    /// resource property.
    pub resource: Option<Resource>,
}

/// `NextStateOfTags` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct NextStateOfTags {
    /// tagsFullState property.
    pub tags_full_state: Option<TagsFullState>,
    /// tagsFullStateForChildResource property.
    pub tags_full_state_for_child_resource: Option<TagsFullStateForChildResource>,
    /// tagsPartialState property.
    pub tags_partial_state: Option<TagsPartialState>,
}

/// `Binding` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Binding {
    /// condition property.
    pub condition: Option<Expr>,
    /// members property.
    pub members: Option<Vec<String>>,
    /// role property.
    pub role: Option<String>,
}

/// `TagsFullStateForChildResource` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct TagsFullStateForChildResource {
    /// tags property.
    pub tags: Option<serde_json::Value>,
}

/// `GcipSettings` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct GcipSettings {
    /// loginPageUri property.
    pub login_page_uri: Option<String>,
    /// tenantIds property.
    pub tenant_ids: Option<Vec<String>>,
}

/// `OAuth2` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct OAuth2 {
    /// clientId property.
    pub client_id: Option<String>,
    /// clientSecret property.
    pub client_secret: Option<String>,
    /// clientSecretSha256 property.
    pub client_secret_sha256: Option<String>,
}

/// `OAuthSettings` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct OAuthSettings {
    /// clientId property.
    pub client_id: Option<String>,
    /// clientSecret property.
    pub client_secret: Option<String>,
    /// clientSecretSha256 property.
    pub client_secret_sha256: Option<String>,
    /// loginHint property.
    pub login_hint: Option<String>,
    /// programmaticClients property.
    pub programmatic_clients: Option<Vec<String>>,
}

/// `WorkforceIdentitySettings` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct WorkforceIdentitySettings {
    /// oauth2 property.
    pub oauth2: Option<OAuth2>,
    /// workforcePools property.
    pub workforce_pools: Option<Vec<String>>,
}

/// `ApplicationSettings` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ApplicationSettings {
    /// accessDeniedPageSettings property.
    pub access_denied_page_settings: Option<AccessDeniedPageSettings>,
    /// attributePropagationSettings property.
    pub attribute_propagation_settings: Option<AttributePropagationSettings>,
    /// cookieDomain property.
    pub cookie_domain: Option<String>,
    /// csmSettings property.
    pub csm_settings: Option<CsmSettings>,
}

/// `AllowedDomainsSettings` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct AllowedDomainsSettings {
    /// domains property.
    pub domains: Option<Vec<String>>,
    /// enable property.
    pub enable: Option<bool>,
}

// =============================================================================
// ARGS TYPES (per-endpoint)
// =============================================================================

/// Arguments for [`iap.getIamPolicy_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct IapGetIamPolicyArgs {
    /// Path parameter: `resource`.
    pub resource: String,
}

/// Arguments for [`iap.getIapSettings_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct IapGetIapSettingsArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`iap.setIamPolicy_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct IapSetIamPolicyArgs {
    /// Path parameter: `resource`.
    pub resource: String,
}

/// Arguments for [`iap.testIamPermissions_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct IapTestIamPermissionsArgs {
    /// Path parameter: `resource`.
    pub resource: String,
}

/// Arguments for [`iap.updateIapSettings_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct IapUpdateIapSettingsArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `updateMask`.
    pub update_mask: Option<String>,
}

/// Arguments for [`iap.validateAttributeExpression_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct IapValidateAttributeExpressionArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `expression`.
    pub expression: Option<String>,
}

// =============================================================================
// CLIENT FUNCTIONS (per-endpoint)
// =============================================================================

// -----------------------------------------------------------------------------
// POST v1/{v1Id}:getIamPolicy
// -----------------------------------------------------------------------------

/// POST v1/{v1Id}:getIamPolicy.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = iap_get_iam_policy_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn iap_get_iam_policy_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://iap.googleapis.com/v1/{{v1Id}}:getIamPolicy",);

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Policy =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/{v1Id}:iapSettings
// -----------------------------------------------------------------------------

/// GET v1/{v1Id}:iapSettings.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = iap_get_iap_settings_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn iap_get_iap_settings_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<IapSettings>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://iap.googleapis.com/v1/{{v1Id}}:iapSettings",);

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: IapSettings =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/{v1Id}:setIamPolicy
// -----------------------------------------------------------------------------

/// POST v1/{v1Id}:setIamPolicy.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = iap_set_iam_policy_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn iap_set_iam_policy_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://iap.googleapis.com/v1/{{v1Id}}:setIamPolicy",);

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Policy =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/{v1Id}:testIamPermissions
// -----------------------------------------------------------------------------

/// POST v1/{v1Id}:testIamPermissions.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = iap_test_iam_permissions_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn iap_test_iam_permissions_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TestIamPermissionsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://iap.googleapis.com/v1/{{v1Id}}:testIamPermissions",);

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: TestIamPermissionsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PATCH v1/{v1Id}:iapSettings
// -----------------------------------------------------------------------------

/// PATCH v1/{v1Id}:iapSettings.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = iap_update_iap_settings_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn iap_update_iap_settings_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<IapSettings>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://iap.googleapis.com/v1/{{v1Id}}:iapSettings",);

    let mut builder = client
        .patch(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: IapSettings =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/{v1Id}:validateAttributeExpression
// -----------------------------------------------------------------------------

/// POST v1/{v1Id}:validateAttributeExpression.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = iap_validate_attribute_expression_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn iap_validate_attribute_expression_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<ValidateIapAttributeExpressionResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url =
        format!("https://iap.googleapis.com/v1/{{v1Id}}:validateAttributeExpression",);

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ValidateIapAttributeExpressionResponse = serde_json::from_str(&body)
                    .map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}
