//! Auto-generated API module for gcp/cloudkms proposals.
//!
//! Generated by `cargo run --bin ewe_platform gen_api`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp_cloudkms_proposals `

#![cfg(feature = "gcp_cloudkms_proposals")]
#![allow(clippy::too_many_arguments, clippy::type_complexity)]
#![allow(
    clippy::missing_errors_doc,
    clippy::doc_markdown,
    clippy::useless_format
)]

use foundation_core::valtron::{execute, StreamIterator, TaskIterator, TaskIteratorExt};
use foundation_core::wire::simple_http::client::{ClientRequestBuilder, SimpleHttpClient};
use foundation_macros::JsonHash;
use serde::{Deserialize, Serialize};

// Import shared types used by this module
use super::shared::Operation;

use super::shared::{ApiError, ApiPending, ApiResponse};

// =============================================================================
// TYPE DECLARATIONS
// =============================================================================

/// `Status` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Status {
    /// code property.
    pub code: Option<i64>,
    /// details property.
    pub details: Option<Vec<serde_json::Value>>,
    /// message property.
    pub message: Option<String>,
}

/// `ApproveSingleTenantHsmInstanceProposalResponse` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ApproveSingleTenantHsmInstanceProposalResponse {}

/// `DeleteSingleTenantHsmInstance` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct DeleteSingleTenantHsmInstance {}

/// `DisableSingleTenantHsmInstance` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct DisableSingleTenantHsmInstance {}

/// `Challenge` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Challenge {
    /// challenge property.
    pub challenge: Option<String>,
    /// publicKeyPem property.
    pub public_key_pem: Option<String>,
}

/// `EnableSingleTenantHsmInstance` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct EnableSingleTenantHsmInstance {}

/// `SingleTenantHsmInstanceProposal` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct SingleTenantHsmInstanceProposal {
    /// addQuorumMember property.
    pub add_quorum_member: Option<AddQuorumMember>,
    /// createTime property.
    pub create_time: Option<String>,
    /// deleteSingleTenantHsmInstance property.
    pub delete_single_tenant_hsm_instance: Option<DeleteSingleTenantHsmInstance>,
    /// deleteTime property.
    pub delete_time: Option<String>,
    /// disableSingleTenantHsmInstance property.
    pub disable_single_tenant_hsm_instance: Option<DisableSingleTenantHsmInstance>,
    /// enableSingleTenantHsmInstance property.
    pub enable_single_tenant_hsm_instance: Option<EnableSingleTenantHsmInstance>,
    /// expireTime property.
    pub expire_time: Option<String>,
    /// failureReason property.
    pub failure_reason: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// purgeTime property.
    pub purge_time: Option<String>,
    /// quorumParameters property.
    pub quorum_parameters: Option<QuorumParameters>,
    /// refreshSingleTenantHsmInstance property.
    pub refresh_single_tenant_hsm_instance: Option<RefreshSingleTenantHsmInstance>,
    /// registerTwoFactorAuthKeys property.
    pub register_two_factor_auth_keys: Option<RegisterTwoFactorAuthKeys>,
    /// removeQuorumMember property.
    pub remove_quorum_member: Option<RemoveQuorumMember>,
    /// requiredActionQuorumParameters property.
    pub required_action_quorum_parameters: Option<RequiredActionQuorumParameters>,
    /// state property.
    pub state: Option<String>,
    /// ttl property.
    pub ttl: Option<String>,
}

/// `RefreshSingleTenantHsmInstance` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct RefreshSingleTenantHsmInstance {}

/// `RegisterTwoFactorAuthKeys` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct RegisterTwoFactorAuthKeys {
    /// requiredApproverCount property.
    pub required_approver_count: Option<i64>,
    /// twoFactorPublicKeyPems property.
    pub two_factor_public_key_pems: Option<Vec<String>>,
}

/// `QuorumParameters` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct QuorumParameters {
    /// approvedTwoFactorPublicKeyPems property.
    pub approved_two_factor_public_key_pems: Option<Vec<String>>,
    /// challenges property.
    pub challenges: Option<Vec<Challenge>>,
    /// requiredApproverCount property.
    pub required_approver_count: Option<i64>,
}

/// `RemoveQuorumMember` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct RemoveQuorumMember {
    /// twoFactorPublicKeyPem property.
    pub two_factor_public_key_pem: Option<String>,
}

/// `AddQuorumMember` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct AddQuorumMember {
    /// twoFactorPublicKeyPem property.
    pub two_factor_public_key_pem: Option<String>,
}

/// `RequiredActionQuorumParameters` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct RequiredActionQuorumParameters {
    /// approvedTwoFactorPublicKeyPems property.
    pub approved_two_factor_public_key_pems: Option<Vec<String>>,
    /// quorumChallenges property.
    pub quorum_challenges: Option<Vec<Challenge>>,
    /// requiredApproverCount property.
    pub required_approver_count: Option<i64>,
    /// requiredChallenges property.
    pub required_challenges: Option<Vec<Challenge>>,
}

/// `Empty` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Empty {}

/// `ListSingleTenantHsmInstanceProposalsResponse` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ListSingleTenantHsmInstanceProposalsResponse {
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// singleTenantHsmInstanceProposals property.
    pub single_tenant_hsm_instance_proposals: Option<Vec<SingleTenantHsmInstanceProposal>>,
    /// totalSize property.
    pub total_size: Option<i64>,
}

// =============================================================================
// ARGS TYPES (per-endpoint)
// =============================================================================

/// Arguments for [`cloudkms.projects.locations.singleTenantHsmInstances.proposals.approve_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudkmsProjectsLocationsSingleTenantHsmInstancesProposalsApproveArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`cloudkms.projects.locations.singleTenantHsmInstances.proposals.create_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudkmsProjectsLocationsSingleTenantHsmInstancesProposalsCreateArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `singleTenantHsmInstanceProposalId`.
    pub single_tenant_hsm_instance_proposal_id: Option<String>,
}

/// Arguments for [`cloudkms.projects.locations.singleTenantHsmInstances.proposals.delete_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudkmsProjectsLocationsSingleTenantHsmInstancesProposalsDeleteArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`cloudkms.projects.locations.singleTenantHsmInstances.proposals.execute_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudkmsProjectsLocationsSingleTenantHsmInstancesProposalsExecuteArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`cloudkms.projects.locations.singleTenantHsmInstances.proposals.get_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudkmsProjectsLocationsSingleTenantHsmInstancesProposalsGetArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`cloudkms.projects.locations.singleTenantHsmInstances.proposals.list_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudkmsProjectsLocationsSingleTenantHsmInstancesProposalsListArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `showDeleted`.
    pub show_deleted: Option<String>,
}

// =============================================================================
// CLIENT FUNCTIONS (per-endpoint)
// =============================================================================

// -----------------------------------------------------------------------------
// POST v1/projects/{projectsId}/locations/{locationsId}/singleTenantHsmInstances/{singleTenantHsmInstancesId}/proposals/{proposalsId}:approve
// -----------------------------------------------------------------------------

/// POST v1/projects/{projectsId}/locations/{locationsId}/singleTenantHsmInstances/{singleTenantHsmInstancesId}/proposals/{proposalsId}:approve.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = cloudkms_projects_locations_single_tenant_hsm_instances_proposals_approve_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn cloudkms_projects_locations_single_tenant_hsm_instances_proposals_approve_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<ApproveSingleTenantHsmInstanceProposalResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://cloudkms.googleapis.com/v1/projects/{{projectsId}}/locations/{{locationsId}}/singleTenantHsmInstances/{{singleTenantHsmInstancesId}}/proposals/{{proposalsId}}:approve",
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ApproveSingleTenantHsmInstanceProposalResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/projects/{projectsId}/locations/{locationsId}/singleTenantHsmInstances/{singleTenantHsmInstancesId}/proposals
// -----------------------------------------------------------------------------

/// POST v1/projects/{projectsId}/locations/{locationsId}/singleTenantHsmInstances/{singleTenantHsmInstancesId}/proposals.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = cloudkms_projects_locations_single_tenant_hsm_instances_proposals_create_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn cloudkms_projects_locations_single_tenant_hsm_instances_proposals_create_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://cloudkms.googleapis.com/v1/projects/{{projectsId}}/locations/{{locationsId}}/singleTenantHsmInstances/{{singleTenantHsmInstancesId}}/proposals",
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE v1/projects/{projectsId}/locations/{locationsId}/singleTenantHsmInstances/{singleTenantHsmInstancesId}/proposals/{proposalsId}
// -----------------------------------------------------------------------------

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/singleTenantHsmInstances/{singleTenantHsmInstancesId}/proposals/{proposalsId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = cloudkms_projects_locations_single_tenant_hsm_instances_proposals_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn cloudkms_projects_locations_single_tenant_hsm_instances_proposals_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://cloudkms.googleapis.com/v1/projects/{{projectsId}}/locations/{{locationsId}}/singleTenantHsmInstances/{{singleTenantHsmInstancesId}}/proposals/{{proposalsId}}",
    );

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Empty =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/projects/{projectsId}/locations/{locationsId}/singleTenantHsmInstances/{singleTenantHsmInstancesId}/proposals/{proposalsId}:execute
// -----------------------------------------------------------------------------

/// POST v1/projects/{projectsId}/locations/{locationsId}/singleTenantHsmInstances/{singleTenantHsmInstancesId}/proposals/{proposalsId}:execute.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = cloudkms_projects_locations_single_tenant_hsm_instances_proposals_execute_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn cloudkms_projects_locations_single_tenant_hsm_instances_proposals_execute_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://cloudkms.googleapis.com/v1/projects/{{projectsId}}/locations/{{locationsId}}/singleTenantHsmInstances/{{singleTenantHsmInstancesId}}/proposals/{{proposalsId}}:execute",
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/projects/{projectsId}/locations/{locationsId}/singleTenantHsmInstances/{singleTenantHsmInstancesId}/proposals/{proposalsId}
// -----------------------------------------------------------------------------

/// GET v1/projects/{projectsId}/locations/{locationsId}/singleTenantHsmInstances/{singleTenantHsmInstancesId}/proposals/{proposalsId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = cloudkms_projects_locations_single_tenant_hsm_instances_proposals_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn cloudkms_projects_locations_single_tenant_hsm_instances_proposals_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SingleTenantHsmInstanceProposal>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://cloudkms.googleapis.com/v1/projects/{{projectsId}}/locations/{{locationsId}}/singleTenantHsmInstances/{{singleTenantHsmInstancesId}}/proposals/{{proposalsId}}",
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: SingleTenantHsmInstanceProposal =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/projects/{projectsId}/locations/{locationsId}/singleTenantHsmInstances/{singleTenantHsmInstancesId}/proposals
// -----------------------------------------------------------------------------

/// GET v1/projects/{projectsId}/locations/{locationsId}/singleTenantHsmInstances/{singleTenantHsmInstancesId}/proposals.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = cloudkms_projects_locations_single_tenant_hsm_instances_proposals_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn cloudkms_projects_locations_single_tenant_hsm_instances_proposals_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<ListSingleTenantHsmInstanceProposalsResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://cloudkms.googleapis.com/v1/projects/{{projectsId}}/locations/{{locationsId}}/singleTenantHsmInstances/{{singleTenantHsmInstancesId}}/proposals",
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ListSingleTenantHsmInstanceProposalsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}
