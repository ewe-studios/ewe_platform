//! Auto-generated API module for gcp/analyticsdata properties.
//!
//! Generated by `cargo run --bin ewe_platform gen_api`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp_analyticsdata_properties `

#![cfg(feature = "gcp_analyticsdata_properties")]
#![allow(clippy::too_many_arguments, clippy::type_complexity)]
#![allow(
    clippy::missing_errors_doc,
    clippy::doc_markdown,
    clippy::useless_format
)]

use foundation_core::valtron::{execute, StreamIterator, TaskIterator, TaskIteratorExt};
use foundation_core::wire::simple_http::client::{ClientRequestBuilder, SimpleHttpClient};
use foundation_macros::JsonHash;
use serde::{Deserialize, Serialize};

use super::shared::{ApiError, ApiPending, ApiResponse};

// =============================================================================
// TYPE DECLARATIONS
// =============================================================================

/// `RunRealtimeReportResponse` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct RunRealtimeReportResponse {
    /// dimensionHeaders property.
    pub dimension_headers: Option<Vec<DimensionHeader>>,
    /// kind property.
    pub kind: Option<String>,
    /// maximums property.
    pub maximums: Option<Vec<Row>>,
    /// metricHeaders property.
    pub metric_headers: Option<Vec<MetricHeader>>,
    /// minimums property.
    pub minimums: Option<Vec<Row>>,
    /// propertyQuota property.
    pub property_quota: Option<PropertyQuota>,
    /// rowCount property.
    pub row_count: Option<i64>,
    /// rows property.
    pub rows: Option<Vec<Row>>,
    /// totals property.
    pub totals: Option<Vec<Row>>,
}

/// `BatchRunReportsResponse` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct BatchRunReportsResponse {
    /// kind property.
    pub kind: Option<String>,
    /// reports property.
    pub reports: Option<Vec<RunReportResponse>>,
}

/// `ActiveMetricRestriction` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ActiveMetricRestriction {
    /// metricName property.
    pub metric_name: Option<String>,
    /// restrictedMetricTypes property.
    pub restricted_metric_types: Option<Vec<String>>,
}

/// `MetricHeader` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct MetricHeader {
    /// name property.
    pub name: Option<String>,
    /// type property.
    pub r#type: Option<String>,
}

/// `SchemaRestrictionResponse` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct SchemaRestrictionResponse {
    /// activeMetricRestrictions property.
    pub active_metric_restrictions: Option<Vec<ActiveMetricRestriction>>,
}

/// `DimensionMetadata` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct DimensionMetadata {
    /// apiName property.
    pub api_name: Option<String>,
    /// category property.
    pub category: Option<String>,
    /// customDefinition property.
    pub custom_definition: Option<bool>,
    /// deprecatedApiNames property.
    pub deprecated_api_names: Option<Vec<String>>,
    /// description property.
    pub description: Option<String>,
    /// uiName property.
    pub ui_name: Option<String>,
}

/// `PivotHeader` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct PivotHeader {
    /// pivotDimensionHeaders property.
    pub pivot_dimension_headers: Option<Vec<PivotDimensionHeader>>,
    /// rowCount property.
    pub row_count: Option<i64>,
}

/// `BatchRunPivotReportsResponse` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct BatchRunPivotReportsResponse {
    /// kind property.
    pub kind: Option<String>,
    /// pivotReports property.
    pub pivot_reports: Option<Vec<RunPivotReportResponse>>,
}

/// `MetricValue` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct MetricValue {
    /// value property.
    pub value: Option<String>,
}

/// `DimensionHeader` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct DimensionHeader {
    /// name property.
    pub name: Option<String>,
}

/// `PropertyQuota` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct PropertyQuota {
    /// concurrentRequests property.
    pub concurrent_requests: Option<QuotaStatus>,
    /// potentiallyThresholdedRequestsPerHour property.
    pub potentially_thresholded_requests_per_hour: Option<QuotaStatus>,
    /// serverErrorsPerProjectPerHour property.
    pub server_errors_per_project_per_hour: Option<QuotaStatus>,
    /// tokensPerDay property.
    pub tokens_per_day: Option<QuotaStatus>,
    /// tokensPerHour property.
    pub tokens_per_hour: Option<QuotaStatus>,
    /// tokensPerProjectPerHour property.
    pub tokens_per_project_per_hour: Option<QuotaStatus>,
}

/// `DimensionCompatibility` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct DimensionCompatibility {
    /// compatibility property.
    pub compatibility: Option<String>,
    /// dimensionMetadata property.
    pub dimension_metadata: Option<DimensionMetadata>,
}

/// `MetricCompatibility` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct MetricCompatibility {
    /// compatibility property.
    pub compatibility: Option<String>,
    /// metricMetadata property.
    pub metric_metadata: Option<MetricMetadata>,
}

/// `RunReportResponse` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct RunReportResponse {
    /// dimensionHeaders property.
    pub dimension_headers: Option<Vec<DimensionHeader>>,
    /// kind property.
    pub kind: Option<String>,
    /// maximums property.
    pub maximums: Option<Vec<Row>>,
    /// metadata property.
    pub metadata: Option<ResponseMetaData>,
    /// metricHeaders property.
    pub metric_headers: Option<Vec<MetricHeader>>,
    /// minimums property.
    pub minimums: Option<Vec<Row>>,
    /// propertyQuota property.
    pub property_quota: Option<PropertyQuota>,
    /// rowCount property.
    pub row_count: Option<i64>,
    /// rows property.
    pub rows: Option<Vec<Row>>,
    /// totals property.
    pub totals: Option<Vec<Row>>,
}

/// `RunPivotReportResponse` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct RunPivotReportResponse {
    /// aggregates property.
    pub aggregates: Option<Vec<Row>>,
    /// dimensionHeaders property.
    pub dimension_headers: Option<Vec<DimensionHeader>>,
    /// kind property.
    pub kind: Option<String>,
    /// metadata property.
    pub metadata: Option<ResponseMetaData>,
    /// metricHeaders property.
    pub metric_headers: Option<Vec<MetricHeader>>,
    /// pivotHeaders property.
    pub pivot_headers: Option<Vec<PivotHeader>>,
    /// propertyQuota property.
    pub property_quota: Option<PropertyQuota>,
    /// rows property.
    pub rows: Option<Vec<Row>>,
}

/// `PivotDimensionHeader` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct PivotDimensionHeader {
    /// dimensionValues property.
    pub dimension_values: Option<Vec<DimensionValue>>,
}

/// `ResponseMetaData` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ResponseMetaData {
    /// currencyCode property.
    pub currency_code: Option<String>,
    /// dataLossFromOtherRow property.
    pub data_loss_from_other_row: Option<bool>,
    /// emptyReason property.
    pub empty_reason: Option<String>,
    /// samplingMetadatas property.
    pub sampling_metadatas: Option<Vec<SamplingMetadata>>,
    /// schemaRestrictionResponse property.
    pub schema_restriction_response: Option<SchemaRestrictionResponse>,
    /// subjectToThresholding property.
    pub subject_to_thresholding: Option<bool>,
    /// timeZone property.
    pub time_zone: Option<String>,
}

/// `QuotaStatus` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct QuotaStatus {
    /// consumed property.
    pub consumed: Option<i64>,
    /// remaining property.
    pub remaining: Option<i64>,
}

/// `DimensionValue` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct DimensionValue {
    /// value property.
    pub value: Option<String>,
}

/// `Row` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Row {
    /// dimensionValues property.
    pub dimension_values: Option<Vec<DimensionValue>>,
    /// metricValues property.
    pub metric_values: Option<Vec<MetricValue>>,
}

/// `MetricMetadata` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct MetricMetadata {
    /// apiName property.
    pub api_name: Option<String>,
    /// blockedReasons property.
    pub blocked_reasons: Option<Vec<String>>,
    /// category property.
    pub category: Option<String>,
    /// customDefinition property.
    pub custom_definition: Option<bool>,
    /// deprecatedApiNames property.
    pub deprecated_api_names: Option<Vec<String>>,
    /// description property.
    pub description: Option<String>,
    /// expression property.
    pub expression: Option<String>,
    /// type property.
    pub r#type: Option<String>,
    /// uiName property.
    pub ui_name: Option<String>,
}

/// `SamplingMetadata` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct SamplingMetadata {
    /// samplesReadCount property.
    pub samples_read_count: Option<String>,
    /// samplingSpaceSize property.
    pub sampling_space_size: Option<String>,
}

/// `CheckCompatibilityResponse` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct CheckCompatibilityResponse {
    /// dimensionCompatibilities property.
    pub dimension_compatibilities: Option<Vec<DimensionCompatibility>>,
    /// metricCompatibilities property.
    pub metric_compatibilities: Option<Vec<MetricCompatibility>>,
}

// =============================================================================
// ARGS TYPES (per-endpoint)
// =============================================================================

/// Arguments for [`analyticsdata.properties.batchRunPivotReports_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AnalyticsdataPropertiesBatchRunPivotReportsArgs {
    /// Path parameter: `property`.
    pub property: String,
}

/// Arguments for [`analyticsdata.properties.batchRunReports_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AnalyticsdataPropertiesBatchRunReportsArgs {
    /// Path parameter: `property`.
    pub property: String,
}

/// Arguments for [`analyticsdata.properties.checkCompatibility_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AnalyticsdataPropertiesCheckCompatibilityArgs {
    /// Path parameter: `property`.
    pub property: String,
}

/// Arguments for [`analyticsdata.properties.runPivotReport_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AnalyticsdataPropertiesRunPivotReportArgs {
    /// Path parameter: `property`.
    pub property: String,
}

/// Arguments for [`analyticsdata.properties.runRealtimeReport_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AnalyticsdataPropertiesRunRealtimeReportArgs {
    /// Path parameter: `property`.
    pub property: String,
}

/// Arguments for [`analyticsdata.properties.runReport_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AnalyticsdataPropertiesRunReportArgs {
    /// Path parameter: `property`.
    pub property: String,
}

// =============================================================================
// CLIENT FUNCTIONS (per-endpoint)
// =============================================================================

// -----------------------------------------------------------------------------
// POST v1beta/properties/{propertiesId}:batchRunPivotReports
// -----------------------------------------------------------------------------

/// POST v1beta/properties/{propertiesId}:batchRunPivotReports.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = analyticsdata_properties_batch_run_pivot_reports_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn analyticsdata_properties_batch_run_pivot_reports_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<BatchRunPivotReportsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://analyticsdata.googleapis.com/v1beta/properties/{{propertiesId}}:batchRunPivotReports",
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: BatchRunPivotReportsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1beta/properties/{propertiesId}:batchRunReports
// -----------------------------------------------------------------------------

/// POST v1beta/properties/{propertiesId}:batchRunReports.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = analyticsdata_properties_batch_run_reports_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn analyticsdata_properties_batch_run_reports_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<BatchRunReportsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://analyticsdata.googleapis.com/v1beta/properties/{{propertiesId}}:batchRunReports",
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: BatchRunReportsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1beta/properties/{propertiesId}:checkCompatibility
// -----------------------------------------------------------------------------

/// POST v1beta/properties/{propertiesId}:checkCompatibility.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = analyticsdata_properties_check_compatibility_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn analyticsdata_properties_check_compatibility_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<CheckCompatibilityResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://analyticsdata.googleapis.com/v1beta/properties/{{propertiesId}}:checkCompatibility",
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: CheckCompatibilityResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1beta/properties/{propertiesId}:runPivotReport
// -----------------------------------------------------------------------------

/// POST v1beta/properties/{propertiesId}:runPivotReport.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = analyticsdata_properties_run_pivot_report_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn analyticsdata_properties_run_pivot_report_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<RunPivotReportResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://analyticsdata.googleapis.com/v1beta/properties/{{propertiesId}}:runPivotReport",
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: RunPivotReportResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1beta/properties/{propertiesId}:runRealtimeReport
// -----------------------------------------------------------------------------

/// POST v1beta/properties/{propertiesId}:runRealtimeReport.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = analyticsdata_properties_run_realtime_report_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn analyticsdata_properties_run_realtime_report_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<RunRealtimeReportResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://analyticsdata.googleapis.com/v1beta/properties/{{propertiesId}}:runRealtimeReport",
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: RunRealtimeReportResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1beta/properties/{propertiesId}:runReport
// -----------------------------------------------------------------------------

/// POST v1beta/properties/{propertiesId}:runReport.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = analyticsdata_properties_run_report_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn analyticsdata_properties_run_report_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<RunReportResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://analyticsdata.googleapis.com/v1beta/properties/{{propertiesId}}:runReport",
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: RunReportResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}
