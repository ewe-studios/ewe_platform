//! Auto-generated API module for gcp/paymentsresellersubscription subscriptions.
//!
//! Generated by `cargo run --bin ewe_platform gen_api`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp_paymentsresellersubscription_subscriptions `

#![cfg(feature = "gcp_paymentsresellersubscription_subscriptions")]
#![allow(clippy::too_many_arguments, clippy::type_complexity)]
#![allow(
    clippy::missing_errors_doc,
    clippy::doc_markdown,
    clippy::useless_format
)]
#![allow(unused_imports)]

use foundation_core::valtron::{TaskIterator, TaskIteratorExt};
use foundation_core::wire::simple_http::client::{ClientRequestBuilder, SimpleHttpClient};
use foundation_macros::JsonHash;
use serde::{Deserialize, Serialize};

// Import shared types used by this module
use super::shared::Subscription;

use super::shared::ApiResponse;

// =============================================================================
// TYPE DECLARATIONS
// =============================================================================

/// `SubscriptionLineItemBundleDetails` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct SubscriptionLineItemBundleDetails {
    /// bundleElementDetails property.
    pub bundle_element_details: Option<Vec<SubscriptionLineItemBundleDetailsBundleElementDetails>>,
}

/// `SubscriptionMigrationDetails` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct SubscriptionMigrationDetails {
    /// migratedSubscriptionId property.
    pub migrated_subscription_id: Option<String>,
}

/// `SubscriptionLineItemOneTimeRecurrenceDetails` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct SubscriptionLineItemOneTimeRecurrenceDetails {
    /// servicePeriod property.
    pub service_period: Option<ServicePeriod>,
}

/// `Duration` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Duration {
    /// count property.
    pub count: Option<i64>,
    /// unit property.
    pub unit: Option<String>,
}

/// `FiniteBillingCycleDetails` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct FiniteBillingCycleDetails {
    /// billingCycleCountLimit property.
    pub billing_cycle_count_limit: Option<String>,
}

/// `CancelSubscriptionResponse` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct CancelSubscriptionResponse {
    /// subscription property.
    pub subscription: Option<Subscription>,
}

/// `ProductPayload` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ProductPayload {
    /// googleHomePayload property.
    pub google_home_payload: Option<GoogleHomePayload>,
    /// googleOnePayload property.
    pub google_one_payload: Option<GoogleOnePayload>,
    /// youtubePayload property.
    pub youtube_payload: Option<YoutubePayload>,
}

/// `Amount` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Amount {
    /// amountMicros property.
    pub amount_micros: Option<String>,
    /// currencyCode property.
    pub currency_code: Option<String>,
}

/// `GoogleHomePayload` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct GoogleHomePayload {
    /// attachedToGoogleStructure property.
    pub attached_to_google_structure: Option<bool>,
    /// googleStructureId property.
    pub google_structure_id: Option<String>,
    /// partnerStructureId property.
    pub partner_structure_id: Option<String>,
}

/// `SubscriptionLineItemBundleDetailsBundleElementDetails` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct SubscriptionLineItemBundleDetailsBundleElementDetails {
    /// product property.
    pub product: Option<String>,
    /// userAccountLinkedTime property.
    pub user_account_linked_time: Option<String>,
}

/// `SubscriptionLineItem` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct SubscriptionLineItem {
    /// amount property.
    pub amount: Option<Amount>,
    /// bundleDetails property.
    pub bundle_details: Option<SubscriptionLineItemBundleDetails>,
    /// description property.
    pub description: Option<String>,
    /// finiteBillingCycleDetails property.
    pub finite_billing_cycle_details: Option<FiniteBillingCycleDetails>,
    /// lineItemFreeTrialEndTime property.
    pub line_item_free_trial_end_time: Option<String>,
    /// lineItemIndex property.
    pub line_item_index: Option<i64>,
    /// lineItemPromotionSpecs property.
    pub line_item_promotion_specs: Option<Vec<SubscriptionPromotionSpec>>,
    /// name property.
    pub name: Option<String>,
    /// oneTimeRecurrenceDetails property.
    pub one_time_recurrence_details: Option<SubscriptionLineItemOneTimeRecurrenceDetails>,
    /// product property.
    pub product: Option<String>,
    /// productPayload property.
    pub product_payload: Option<ProductPayload>,
    /// recurrenceType property.
    pub recurrence_type: Option<String>,
    /// state property.
    pub state: Option<String>,
}

/// `SubscriptionUpgradeDowngradeDetails` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct SubscriptionUpgradeDowngradeDetails {
    /// billingCycleSpec property.
    pub billing_cycle_spec: Option<String>,
    /// previousSubscriptionId property.
    pub previous_subscription_id: Option<String>,
}

/// `ResumeSubscriptionResponse` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ResumeSubscriptionResponse {
    /// subscription property.
    pub subscription: Option<Subscription>,
}

/// `SubscriptionPromotionSpec` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct SubscriptionPromotionSpec {
    /// freeTrialDuration property.
    pub free_trial_duration: Option<Duration>,
    /// introductoryPricingDetails property.
    pub introductory_pricing_details: Option<PromotionIntroductoryPricingDetails>,
    /// promotion property.
    pub promotion: Option<String>,
    /// type property.
    pub r#type: Option<String>,
}

/// `SuspendSubscriptionResponse` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct SuspendSubscriptionResponse {
    /// subscription property.
    pub subscription: Option<Subscription>,
}

/// `PromotionIntroductoryPricingDetailsIntroductoryPricingSpec` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct PromotionIntroductoryPricingDetailsIntroductoryPricingSpec {
    /// discountAmount property.
    pub discount_amount: Option<Amount>,
    /// discountRatioMicros property.
    pub discount_ratio_micros: Option<String>,
    /// recurrenceCount property.
    pub recurrence_count: Option<i64>,
    /// regionCode property.
    pub region_code: Option<String>,
}

/// `GoogleOnePayload` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct GoogleOnePayload {
    /// campaigns property.
    pub campaigns: Option<Vec<String>>,
    /// offering property.
    pub offering: Option<String>,
    /// salesChannel property.
    pub sales_channel: Option<String>,
    /// storeId property.
    pub store_id: Option<String>,
}

/// `ServicePeriod` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ServicePeriod {
    /// endTime property.
    pub end_time: Option<String>,
    /// startTime property.
    pub start_time: Option<String>,
}

/// `YoutubePayload` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct YoutubePayload {
    /// accessEndTime property.
    pub access_end_time: Option<String>,
    /// partnerEligibilityIds property.
    pub partner_eligibility_ids: Option<Vec<String>>,
    /// partnerPlanType property.
    pub partner_plan_type: Option<String>,
}

/// `ExtendSubscriptionResponse` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ExtendSubscriptionResponse {
    /// cycleEndTime property.
    pub cycle_end_time: Option<String>,
    /// freeTrialEndTime property.
    pub free_trial_end_time: Option<String>,
    /// renewalTime property.
    pub renewal_time: Option<String>,
}

/// `SubscriptionCancellationDetails` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct SubscriptionCancellationDetails {
    /// reason property.
    pub reason: Option<String>,
}

/// `UndoCancelSubscriptionResponse` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct UndoCancelSubscriptionResponse {
    /// subscription property.
    pub subscription: Option<Subscription>,
}

/// `Location` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Location {
    /// postalCode property.
    pub postal_code: Option<String>,
    /// regionCode property.
    pub region_code: Option<String>,
}

/// `EntitleSubscriptionResponse` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct EntitleSubscriptionResponse {
    /// subscription property.
    pub subscription: Option<Subscription>,
}

/// `PromotionIntroductoryPricingDetails` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct PromotionIntroductoryPricingDetails {
    /// introductoryPricingSpecs property.
    pub introductory_pricing_specs:
        Option<Vec<PromotionIntroductoryPricingDetailsIntroductoryPricingSpec>>,
}

// =============================================================================
// ARGS TYPES (per-endpoint)
// =============================================================================

/// Arguments for [`paymentsresellersubscription.partners.subscriptions.cancel_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PaymentsresellersubscriptionPartnersSubscriptionsCancelArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`paymentsresellersubscription.partners.subscriptions.create_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PaymentsresellersubscriptionPartnersSubscriptionsCreateArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `subscriptionId`.
    pub subscription_id: Option<String>,
}

/// Arguments for [`paymentsresellersubscription.partners.subscriptions.entitle_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PaymentsresellersubscriptionPartnersSubscriptionsEntitleArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`paymentsresellersubscription.partners.subscriptions.extend_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PaymentsresellersubscriptionPartnersSubscriptionsExtendArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`paymentsresellersubscription.partners.subscriptions.get_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PaymentsresellersubscriptionPartnersSubscriptionsGetArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`paymentsresellersubscription.partners.subscriptions.resume_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PaymentsresellersubscriptionPartnersSubscriptionsResumeArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`paymentsresellersubscription.partners.subscriptions.suspend_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PaymentsresellersubscriptionPartnersSubscriptionsSuspendArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`paymentsresellersubscription.partners.subscriptions.undoCancel_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PaymentsresellersubscriptionPartnersSubscriptionsUndoCancelArgs {
    /// Path parameter: `name`.
    pub name: String,
}

// =============================================================================
// CLIENT FUNCTIONS (per-endpoint)
// =============================================================================

// -----------------------------------------------------------------------------
// POST v1/partners/{partnersId}/subscriptions/{subscriptionsId}:cancel
// -----------------------------------------------------------------------------

/// POST v1/partners/{partnersId}/subscriptions/{subscriptionsId}:cancel.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = paymentsresellersubscription_partners_subscriptions_cancel_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn paymentsresellersubscription_partners_subscriptions_cancel_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<CancelSubscriptionResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://paymentsresellersubscription.googleapis.com/v1/partners/{{partnersId}}/subscriptions/{{subscriptionsId}}:cancel",
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: CancelSubscriptionResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/partners/{partnersId}/subscriptions
// -----------------------------------------------------------------------------

/// POST v1/partners/{partnersId}/subscriptions.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = paymentsresellersubscription_partners_subscriptions_create_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn paymentsresellersubscription_partners_subscriptions_create_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Subscription>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://paymentsresellersubscription.googleapis.com/v1/partners/{{partnersId}}/subscriptions",
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Subscription =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/partners/{partnersId}/subscriptions/{subscriptionsId}:entitle
// -----------------------------------------------------------------------------

/// POST v1/partners/{partnersId}/subscriptions/{subscriptionsId}:entitle.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = paymentsresellersubscription_partners_subscriptions_entitle_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn paymentsresellersubscription_partners_subscriptions_entitle_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<EntitleSubscriptionResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://paymentsresellersubscription.googleapis.com/v1/partners/{{partnersId}}/subscriptions/{{subscriptionsId}}:entitle",
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: EntitleSubscriptionResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/partners/{partnersId}/subscriptions/{subscriptionsId}:extend
// -----------------------------------------------------------------------------

/// POST v1/partners/{partnersId}/subscriptions/{subscriptionsId}:extend.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = paymentsresellersubscription_partners_subscriptions_extend_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn paymentsresellersubscription_partners_subscriptions_extend_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ExtendSubscriptionResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://paymentsresellersubscription.googleapis.com/v1/partners/{{partnersId}}/subscriptions/{{subscriptionsId}}:extend",
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ExtendSubscriptionResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/partners/{partnersId}/subscriptions/{subscriptionsId}
// -----------------------------------------------------------------------------

/// GET v1/partners/{partnersId}/subscriptions/{subscriptionsId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = paymentsresellersubscription_partners_subscriptions_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn paymentsresellersubscription_partners_subscriptions_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Subscription>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://paymentsresellersubscription.googleapis.com/v1/partners/{{partnersId}}/subscriptions/{{subscriptionsId}}",
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Subscription =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/partners/{partnersId}/subscriptions/{subscriptionsId}:resume
// -----------------------------------------------------------------------------

/// POST v1/partners/{partnersId}/subscriptions/{subscriptionsId}:resume.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = paymentsresellersubscription_partners_subscriptions_resume_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn paymentsresellersubscription_partners_subscriptions_resume_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ResumeSubscriptionResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://paymentsresellersubscription.googleapis.com/v1/partners/{{partnersId}}/subscriptions/{{subscriptionsId}}:resume",
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ResumeSubscriptionResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/partners/{partnersId}/subscriptions/{subscriptionsId}:suspend
// -----------------------------------------------------------------------------

/// POST v1/partners/{partnersId}/subscriptions/{subscriptionsId}:suspend.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = paymentsresellersubscription_partners_subscriptions_suspend_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn paymentsresellersubscription_partners_subscriptions_suspend_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SuspendSubscriptionResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://paymentsresellersubscription.googleapis.com/v1/partners/{{partnersId}}/subscriptions/{{subscriptionsId}}:suspend",
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: SuspendSubscriptionResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/partners/{partnersId}/subscriptions/{subscriptionsId}:undoCancel
// -----------------------------------------------------------------------------

/// POST v1/partners/{partnersId}/subscriptions/{subscriptionsId}:undoCancel.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = paymentsresellersubscription_partners_subscriptions_undo_cancel_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn paymentsresellersubscription_partners_subscriptions_undo_cancel_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<UndoCancelSubscriptionResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://paymentsresellersubscription.googleapis.com/v1/partners/{{partnersId}}/subscriptions/{{subscriptionsId}}:undoCancel",
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: UndoCancelSubscriptionResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}
