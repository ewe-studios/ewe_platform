//! Auto-generated API module for gcp/paymentsresellersubscription products.
//!
//! Generated by `cargo run --bin ewe_platform gen_api`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp_paymentsresellersubscription_products `

#![cfg(feature = "gcp_paymentsresellersubscription_products")]
#![allow(clippy::too_many_arguments, clippy::type_complexity)]
#![allow(
    clippy::missing_errors_doc,
    clippy::doc_markdown,
    clippy::useless_format
)]
#![allow(unused_imports)]

use foundation_core::valtron::{TaskIterator, TaskIteratorExt};
use foundation_core::wire::simple_http::client::{ClientRequestBuilder, SimpleHttpClient};
use foundation_macros::JsonHash;
use serde::{Deserialize, Serialize};

use super::shared::ApiResponse;

// =============================================================================
// TYPE DECLARATIONS
// =============================================================================

/// `Amount` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Amount {
    /// amountMicros property.
    pub amount_micros: Option<String>,
    /// currencyCode property.
    pub currency_code: Option<String>,
}

/// `ProductBundleDetailsBundleElement` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ProductBundleDetailsBundleElement {
    /// product property.
    pub product: Option<String>,
}

/// `GoogleTypeLocalizedText` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct GoogleTypeLocalizedText {
    /// languageCode property.
    pub language_code: Option<String>,
    /// text property.
    pub text: Option<String>,
}

/// `ListProductsResponse` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ListProductsResponse {
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// products property.
    pub products: Option<Vec<Product>>,
}

/// `Product` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Product {
    /// bundleDetails property.
    pub bundle_details: Option<ProductBundleDetails>,
    /// finiteBillingCycleDetails property.
    pub finite_billing_cycle_details: Option<FiniteBillingCycleDetails>,
    /// name property.
    pub name: Option<String>,
    /// priceConfigs property.
    pub price_configs: Option<Vec<ProductPriceConfig>>,
    /// productType property.
    pub product_type: Option<String>,
    /// regionCodes property.
    pub region_codes: Option<Vec<String>>,
    /// subscriptionBillingCycleDuration property.
    pub subscription_billing_cycle_duration: Option<Duration>,
    /// titles property.
    pub titles: Option<Vec<GoogleTypeLocalizedText>>,
}

/// `ProductPriceConfig` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ProductPriceConfig {
    /// amount property.
    pub amount: Option<Amount>,
    /// regionCode property.
    pub region_code: Option<String>,
}

/// `ProductBundleDetails` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ProductBundleDetails {
    /// bundleElements property.
    pub bundle_elements: Option<Vec<ProductBundleDetailsBundleElement>>,
    /// entitlementMode property.
    pub entitlement_mode: Option<String>,
}

/// `FiniteBillingCycleDetails` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct FiniteBillingCycleDetails {
    /// billingCycleCountLimit property.
    pub billing_cycle_count_limit: Option<String>,
}

/// `Duration` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Duration {
    /// count property.
    pub count: Option<i64>,
    /// unit property.
    pub unit: Option<String>,
}

// =============================================================================
// ARGS TYPES (per-endpoint)
// =============================================================================

/// Arguments for [`paymentsresellersubscription.partners.products.list_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PaymentsresellersubscriptionPartnersProductsListArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

// =============================================================================
// CLIENT FUNCTIONS (per-endpoint)
// =============================================================================

// -----------------------------------------------------------------------------
// GET v1/partners/{partnersId}/products
// -----------------------------------------------------------------------------

/// GET v1/partners/{partnersId}/products.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = paymentsresellersubscription_partners_products_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn paymentsresellersubscription_partners_products_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListProductsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://paymentsresellersubscription.googleapis.com/v1/partners/{{partnersId}}/products",
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ListProductsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}
