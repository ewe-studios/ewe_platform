//! Auto-generated API module for gcp/paymentsresellersubscription paymentsresellersubscription.
//!
//! Generated by `cargo run --bin ewe_platform gen_api`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp_paymentsresellersubscription_paymentsresellersubscription `

#![cfg(feature = "gcp_paymentsresellersubscription_paymentsresellersubscription")]
#![allow(clippy::too_many_arguments, clippy::type_complexity)]
#![allow(
    clippy::missing_errors_doc,
    clippy::doc_markdown,
    clippy::useless_format
)]
#![allow(unused_imports)]

use foundation_core::valtron::{TaskIterator, TaskIteratorExt};
use foundation_core::wire::simple_http::client::{ClientRequestBuilder, SimpleHttpClient};
use foundation_macros::JsonHash;
use serde::{Deserialize, Serialize};

use super::shared::ApiResponse;

// =============================================================================
// TYPE DECLARATIONS
// =============================================================================

/// `ProductPayload` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ProductPayload {
    /// googleHomePayload property.
    pub google_home_payload: Option<GoogleHomePayload>,
    /// googleOnePayload property.
    pub google_one_payload: Option<GoogleOnePayload>,
    /// youtubePayload property.
    pub youtube_payload: Option<YoutubePayload>,
}

/// `SuspendSubscriptionRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SuspendSubscriptionRequest {}

/// `EntitleSubscriptionIntent` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct EntitleSubscriptionIntent {
    /// name property.
    pub name: Option<String>,
}

/// `ListPromotionsResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ListPromotionsResponse {
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// promotions property.
    pub promotions: Option<Vec<Promotion>>,
}

/// `ExtendSubscriptionRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ExtendSubscriptionRequest {
    /// extension property.
    pub extension: Option<Extension>,
    /// requestId property.
    pub request_id: Option<String>,
}

/// `GenerateUserSessionResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GenerateUserSessionResponse {
    /// userSession property.
    pub user_session: Option<UserSession>,
}

/// `ResumeSubscriptionRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ResumeSubscriptionRequest {
    /// cycleOptions property.
    pub cycle_options: Option<CycleOptions>,
    /// resumeMode property.
    pub resume_mode: Option<String>,
}

/// `UndoCancelSubscriptionResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct UndoCancelSubscriptionResponse {
    /// subscription property.
    pub subscription: Option<Subscription>,
}

/// `PromotionIntroductoryPricingDetailsIntroductoryPricingSpec` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct PromotionIntroductoryPricingDetailsIntroductoryPricingSpec {
    /// discountAmount property.
    pub discount_amount: Option<Amount>,
    /// discountRatioMicros property.
    pub discount_ratio_micros: Option<String>,
    /// recurrenceCount property.
    pub recurrence_count: Option<i64>,
    /// regionCode property.
    pub region_code: Option<String>,
}

/// `IntentPayloadIntentOptions` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct IntentPayloadIntentOptions {
    /// enableOfferOverride property.
    pub enable_offer_override: Option<bool>,
}

/// `SubscriptionLineItem` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SubscriptionLineItem {
    /// amount property.
    pub amount: Option<Amount>,
    /// bundleDetails property.
    pub bundle_details: Option<SubscriptionLineItemBundleDetails>,
    /// description property.
    pub description: Option<String>,
    /// finiteBillingCycleDetails property.
    pub finite_billing_cycle_details: Option<FiniteBillingCycleDetails>,
    /// lineItemFreeTrialEndTime property.
    pub line_item_free_trial_end_time: Option<String>,
    /// lineItemIndex property.
    pub line_item_index: Option<i64>,
    /// lineItemPromotionSpecs property.
    pub line_item_promotion_specs: Option<Vec<SubscriptionPromotionSpec>>,
    /// name property.
    pub name: Option<String>,
    /// oneTimeRecurrenceDetails property.
    pub one_time_recurrence_details: Option<SubscriptionLineItemOneTimeRecurrenceDetails>,
    /// product property.
    pub product: Option<String>,
    /// productPayload property.
    pub product_payload: Option<ProductPayload>,
    /// recurrenceType property.
    pub recurrence_type: Option<String>,
    /// state property.
    pub state: Option<String>,
}

/// `CancelSubscriptionResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct CancelSubscriptionResponse {
    /// subscription property.
    pub subscription: Option<Subscription>,
}

/// `GoogleTypeLocalizedText` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleTypeLocalizedText {
    /// languageCode property.
    pub language_code: Option<String>,
    /// text property.
    pub text: Option<String>,
}

/// `IntentPayload` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct IntentPayload {
    /// createIntent property.
    pub create_intent: Option<CreateSubscriptionIntent>,
    /// entitleIntent property.
    pub entitle_intent: Option<EntitleSubscriptionIntent>,
    /// intentOptions property.
    pub intent_options: Option<IntentPayloadIntentOptions>,
}

/// `Amount` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Amount {
    /// amountMicros property.
    pub amount_micros: Option<String>,
    /// currencyCode property.
    pub currency_code: Option<String>,
}

/// `Duration` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Duration {
    /// count property.
    pub count: Option<i64>,
    /// unit property.
    pub unit: Option<String>,
}

/// `YoutubePayload` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct YoutubePayload {
    /// accessEndTime property.
    pub access_end_time: Option<String>,
    /// partnerEligibilityIds property.
    pub partner_eligibility_ids: Option<Vec<String>>,
    /// partnerPlanType property.
    pub partner_plan_type: Option<String>,
}

/// `SubscriptionCancellationDetails` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SubscriptionCancellationDetails {
    /// reason property.
    pub reason: Option<String>,
}

/// `SubscriptionMigrationDetails` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SubscriptionMigrationDetails {
    /// migratedSubscriptionId property.
    pub migrated_subscription_id: Option<String>,
}

/// `Location` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Location {
    /// postalCode property.
    pub postal_code: Option<String>,
    /// regionCode property.
    pub region_code: Option<String>,
}

/// `SubscriptionPromotionSpec` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SubscriptionPromotionSpec {
    /// freeTrialDuration property.
    pub free_trial_duration: Option<Duration>,
    /// introductoryPricingDetails property.
    pub introductory_pricing_details: Option<PromotionIntroductoryPricingDetails>,
    /// promotion property.
    pub promotion: Option<String>,
    /// type property.
    pub r#type: Option<String>,
}

/// `SubscriptionUpgradeDowngradeDetails` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SubscriptionUpgradeDowngradeDetails {
    /// billingCycleSpec property.
    pub billing_cycle_spec: Option<String>,
    /// previousSubscriptionId property.
    pub previous_subscription_id: Option<String>,
}

/// `Product` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Product {
    /// bundleDetails property.
    pub bundle_details: Option<ProductBundleDetails>,
    /// finiteBillingCycleDetails property.
    pub finite_billing_cycle_details: Option<FiniteBillingCycleDetails>,
    /// name property.
    pub name: Option<String>,
    /// priceConfigs property.
    pub price_configs: Option<Vec<ProductPriceConfig>>,
    /// productType property.
    pub product_type: Option<String>,
    /// regionCodes property.
    pub region_codes: Option<Vec<String>>,
    /// subscriptionBillingCycleDuration property.
    pub subscription_billing_cycle_duration: Option<Duration>,
    /// titles property.
    pub titles: Option<Vec<GoogleTypeLocalizedText>>,
}

/// `ProductBundleDetails` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ProductBundleDetails {
    /// bundleElements property.
    pub bundle_elements: Option<Vec<ProductBundleDetailsBundleElement>>,
    /// entitlementMode property.
    pub entitlement_mode: Option<String>,
}

/// `ProductPriceConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ProductPriceConfig {
    /// amount property.
    pub amount: Option<Amount>,
    /// regionCode property.
    pub region_code: Option<String>,
}

/// `Promotion` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Promotion {
    /// applicableProducts property.
    pub applicable_products: Option<Vec<String>>,
    /// endTime property.
    pub end_time: Option<String>,
    /// freeTrialDuration property.
    pub free_trial_duration: Option<Duration>,
    /// introductoryPricingDetails property.
    pub introductory_pricing_details: Option<PromotionIntroductoryPricingDetails>,
    /// name property.
    pub name: Option<String>,
    /// promotionType property.
    pub promotion_type: Option<String>,
    /// regionCodes property.
    pub region_codes: Option<Vec<String>>,
    /// startTime property.
    pub start_time: Option<String>,
    /// titles property.
    pub titles: Option<Vec<GoogleTypeLocalizedText>>,
}

/// `ProductBundleDetailsBundleElement` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ProductBundleDetailsBundleElement {
    /// product property.
    pub product: Option<String>,
}

/// `ResumeSubscriptionResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ResumeSubscriptionResponse {
    /// subscription property.
    pub subscription: Option<Subscription>,
}

/// `CancelSubscriptionRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct CancelSubscriptionRequest {
    /// cancelImmediately property.
    pub cancel_immediately: Option<bool>,
    /// cancellationReason property.
    pub cancellation_reason: Option<String>,
}

/// `EntitleSubscriptionResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct EntitleSubscriptionResponse {
    /// subscription property.
    pub subscription: Option<Subscription>,
}

/// `ListProductsResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ListProductsResponse {
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// products property.
    pub products: Option<Vec<Product>>,
}

/// `Subscription` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Subscription {
    /// cancellationDetails property.
    pub cancellation_details: Option<SubscriptionCancellationDetails>,
    /// createTime property.
    pub create_time: Option<String>,
    /// cycleEndTime property.
    pub cycle_end_time: Option<String>,
    /// endUserEntitled property.
    pub end_user_entitled: Option<bool>,
    /// freeTrialEndTime property.
    pub free_trial_end_time: Option<String>,
    /// lineItems property.
    pub line_items: Option<Vec<SubscriptionLineItem>>,
    /// migrationDetails property.
    pub migration_details: Option<SubscriptionMigrationDetails>,
    /// name property.
    pub name: Option<String>,
    /// partnerUserToken property.
    pub partner_user_token: Option<String>,
    /// processingState property.
    pub processing_state: Option<String>,
    /// products property.
    pub products: Option<Vec<String>>,
    /// promotionSpecs property.
    pub promotion_specs: Option<Vec<SubscriptionPromotionSpec>>,
    /// promotions property.
    pub promotions: Option<Vec<String>>,
    /// purchaseTime property.
    pub purchase_time: Option<String>,
    /// redirectUri property.
    pub redirect_uri: Option<String>,
    /// renewalTime property.
    pub renewal_time: Option<String>,
    /// serviceLocation property.
    pub service_location: Option<Location>,
    /// state property.
    pub state: Option<String>,
    /// updateTime property.
    pub update_time: Option<String>,
    /// upgradeDowngradeDetails property.
    pub upgrade_downgrade_details: Option<SubscriptionUpgradeDowngradeDetails>,
}

/// `FindEligiblePromotionsRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct FindEligiblePromotionsRequest {
    /// filter property.
    pub filter: Option<String>,
    /// pageSize property.
    pub page_size: Option<i64>,
    /// pageToken property.
    pub page_token: Option<String>,
}

/// `CycleOptions` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct CycleOptions {
    /// initialCycleDuration property.
    pub initial_cycle_duration: Option<Duration>,
}

/// `SubscriptionLineItemOneTimeRecurrenceDetails` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SubscriptionLineItemOneTimeRecurrenceDetails {
    /// servicePeriod property.
    pub service_period: Option<ServicePeriod>,
}

/// `UndoCancelSubscriptionRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct UndoCancelSubscriptionRequest {}

/// `GoogleHomePayload` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleHomePayload {
    /// attachedToGoogleStructure property.
    pub attached_to_google_structure: Option<bool>,
    /// googleStructureId property.
    pub google_structure_id: Option<String>,
    /// partnerStructureId property.
    pub partner_structure_id: Option<String>,
}

/// `ExtendSubscriptionResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ExtendSubscriptionResponse {
    /// cycleEndTime property.
    pub cycle_end_time: Option<String>,
    /// freeTrialEndTime property.
    pub free_trial_end_time: Option<String>,
    /// renewalTime property.
    pub renewal_time: Option<String>,
}

/// `Extension` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Extension {
    /// duration property.
    pub duration: Option<Duration>,
    /// partnerUserToken property.
    pub partner_user_token: Option<String>,
}

/// `EntitleSubscriptionRequestLineItemEntitlementDetails` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct EntitleSubscriptionRequestLineItemEntitlementDetails {
    /// lineItemIndex property.
    pub line_item_index: Option<i64>,
    /// products property.
    pub products: Option<Vec<String>>,
}

/// `PromotionIntroductoryPricingDetails` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct PromotionIntroductoryPricingDetails {
    /// introductoryPricingSpecs property.
    pub introductory_pricing_specs:
        Option<Vec<PromotionIntroductoryPricingDetailsIntroductoryPricingSpec>>,
}

/// `ServicePeriod` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ServicePeriod {
    /// endTime property.
    pub end_time: Option<String>,
    /// startTime property.
    pub start_time: Option<String>,
}

/// `SubscriptionLineItemBundleDetailsBundleElementDetails` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SubscriptionLineItemBundleDetailsBundleElementDetails {
    /// product property.
    pub product: Option<String>,
    /// userAccountLinkedTime property.
    pub user_account_linked_time: Option<String>,
}

/// `GoogleOnePayload` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleOnePayload {
    /// campaigns property.
    pub campaigns: Option<Vec<String>>,
    /// offering property.
    pub offering: Option<String>,
    /// salesChannel property.
    pub sales_channel: Option<String>,
    /// storeId property.
    pub store_id: Option<String>,
}

/// `GenerateUserSessionRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GenerateUserSessionRequest {
    /// intentPayload property.
    pub intent_payload: Option<IntentPayload>,
}

/// `UserSession` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct UserSession {
    /// expireTime property.
    pub expire_time: Option<String>,
    /// token property.
    pub token: Option<String>,
}

/// `SubscriptionLineItemBundleDetails` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SubscriptionLineItemBundleDetails {
    /// bundleElementDetails property.
    pub bundle_element_details: Option<Vec<SubscriptionLineItemBundleDetailsBundleElementDetails>>,
}

/// `CreateSubscriptionIntent` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct CreateSubscriptionIntent {
    /// cycleOptions property.
    pub cycle_options: Option<CycleOptions>,
    /// parent property.
    pub parent: Option<String>,
    /// subscription property.
    pub subscription: Option<Subscription>,
    /// subscriptionId property.
    pub subscription_id: Option<String>,
}

/// `EntitleSubscriptionRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct EntitleSubscriptionRequest {
    /// lineItemEntitlementDetails property.
    pub line_item_entitlement_details:
        Option<Vec<EntitleSubscriptionRequestLineItemEntitlementDetails>>,
}

/// `FindEligiblePromotionsResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct FindEligiblePromotionsResponse {
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// promotions property.
    pub promotions: Option<Vec<Promotion>>,
}

/// `SuspendSubscriptionResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SuspendSubscriptionResponse {
    /// subscription property.
    pub subscription: Option<Subscription>,
}

/// `FiniteBillingCycleDetails` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct FiniteBillingCycleDetails {
    /// billingCycleCountLimit property.
    pub billing_cycle_count_limit: Option<String>,
}

// =============================================================================
// ARGS TYPES (per-endpoint)
// =============================================================================

/// Arguments for [`paymentsresellersubscription.partners.products.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct PaymentsresellersubscriptionPartnersProductsListArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

/// Arguments for [`paymentsresellersubscription.partners.promotions.findEligible_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct PaymentsresellersubscriptionPartnersPromotionsFindEligibleArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Request body.
    pub body: FindEligiblePromotionsRequest,
}

/// Arguments for [`paymentsresellersubscription.partners.promotions.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct PaymentsresellersubscriptionPartnersPromotionsListArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

/// Arguments for [`paymentsresellersubscription.partners.subscriptions.cancel_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct PaymentsresellersubscriptionPartnersSubscriptionsCancelArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Request body.
    pub body: CancelSubscriptionRequest,
}

/// Arguments for [`paymentsresellersubscription.partners.subscriptions.create_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct PaymentsresellersubscriptionPartnersSubscriptionsCreateArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `subscriptionId`.
    pub subscription_id: Option<String>,
    /// Request body.
    pub body: Subscription,
}

/// Arguments for [`paymentsresellersubscription.partners.subscriptions.entitle_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct PaymentsresellersubscriptionPartnersSubscriptionsEntitleArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Request body.
    pub body: EntitleSubscriptionRequest,
}

/// Arguments for [`paymentsresellersubscription.partners.subscriptions.extend_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct PaymentsresellersubscriptionPartnersSubscriptionsExtendArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Request body.
    pub body: ExtendSubscriptionRequest,
}

/// Arguments for [`paymentsresellersubscription.partners.subscriptions.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct PaymentsresellersubscriptionPartnersSubscriptionsGetArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`paymentsresellersubscription.partners.subscriptions.provision_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct PaymentsresellersubscriptionPartnersSubscriptionsProvisionArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `cycleOptions.initialCycleDuration.count`.
    pub cycle_options_initial_cycle_duration_count: Option<String>,
    /// Query parameter: `cycleOptions.initialCycleDuration.unit`.
    pub cycle_options_initial_cycle_duration_unit: Option<String>,
    /// Query parameter: `subscriptionId`.
    pub subscription_id: Option<String>,
    /// Request body.
    pub body: Subscription,
}

/// Arguments for [`paymentsresellersubscription.partners.subscriptions.resume_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct PaymentsresellersubscriptionPartnersSubscriptionsResumeArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Request body.
    pub body: ResumeSubscriptionRequest,
}

/// Arguments for [`paymentsresellersubscription.partners.subscriptions.suspend_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct PaymentsresellersubscriptionPartnersSubscriptionsSuspendArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Request body.
    pub body: SuspendSubscriptionRequest,
}

/// Arguments for [`paymentsresellersubscription.partners.subscriptions.undoCancel_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct PaymentsresellersubscriptionPartnersSubscriptionsUndoCancelArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Request body.
    pub body: UndoCancelSubscriptionRequest,
}

/// Arguments for [`paymentsresellersubscription.partners.subscriptions.lineItems.patch_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct PaymentsresellersubscriptionPartnersSubscriptionsLineItemsPatchArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `updateMask`.
    pub update_mask: Option<String>,
    /// Request body.
    pub body: SubscriptionLineItem,
}

/// Arguments for [`paymentsresellersubscription.partners.userSessions.generate_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct PaymentsresellersubscriptionPartnersUserSessionsGenerateArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Request body.
    pub body: GenerateUserSessionRequest,
}

// =============================================================================
// CLIENT FUNCTIONS (per-endpoint)
// =============================================================================

// -----------------------------------------------------------------------------
// GET v1/{+parent}/products
// -----------------------------------------------------------------------------

/// GET v1/{+parent}/products.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = paymentsresellersubscription_partners_products_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn paymentsresellersubscription_partners_products_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &PaymentsresellersubscriptionPartnersProductsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListProductsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://paymentsresellersubscription.googleapis.com/v1/{}/products",
        args.parent,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ListProductsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/{+parent}/promotions:findEligible
// -----------------------------------------------------------------------------

/// POST v1/{+parent}/promotions:findEligible.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = paymentsresellersubscription_partners_promotions_find_eligible_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn paymentsresellersubscription_partners_promotions_find_eligible_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &PaymentsresellersubscriptionPartnersPromotionsFindEligibleArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<FindEligiblePromotionsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://paymentsresellersubscription.googleapis.com/v1/{}/promotions:findEligible",
        args.parent,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: FindEligiblePromotionsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/{+parent}/promotions
// -----------------------------------------------------------------------------

/// GET v1/{+parent}/promotions.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = paymentsresellersubscription_partners_promotions_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn paymentsresellersubscription_partners_promotions_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &PaymentsresellersubscriptionPartnersPromotionsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListPromotionsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://paymentsresellersubscription.googleapis.com/v1/{}/promotions",
        args.parent,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ListPromotionsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/{+name}:cancel
// -----------------------------------------------------------------------------

/// POST v1/{+name}:cancel.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = paymentsresellersubscription_partners_subscriptions_cancel_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn paymentsresellersubscription_partners_subscriptions_cancel_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &PaymentsresellersubscriptionPartnersSubscriptionsCancelArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<CancelSubscriptionResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://paymentsresellersubscription.googleapis.com/v1/{}:cancel",
        args.name,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: CancelSubscriptionResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/{+parent}/subscriptions
// -----------------------------------------------------------------------------

/// POST v1/{+parent}/subscriptions.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = paymentsresellersubscription_partners_subscriptions_create_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn paymentsresellersubscription_partners_subscriptions_create_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &PaymentsresellersubscriptionPartnersSubscriptionsCreateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Subscription>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://paymentsresellersubscription.googleapis.com/v1/{}/subscriptions",
        args.parent,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.subscription_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("subscriptionId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Subscription =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/{+name}:entitle
// -----------------------------------------------------------------------------

/// POST v1/{+name}:entitle.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = paymentsresellersubscription_partners_subscriptions_entitle_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn paymentsresellersubscription_partners_subscriptions_entitle_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &PaymentsresellersubscriptionPartnersSubscriptionsEntitleArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<EntitleSubscriptionResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://paymentsresellersubscription.googleapis.com/v1/{}:entitle",
        args.name,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: EntitleSubscriptionResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/{+name}:extend
// -----------------------------------------------------------------------------

/// POST v1/{+name}:extend.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = paymentsresellersubscription_partners_subscriptions_extend_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn paymentsresellersubscription_partners_subscriptions_extend_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &PaymentsresellersubscriptionPartnersSubscriptionsExtendArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ExtendSubscriptionResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://paymentsresellersubscription.googleapis.com/v1/{}:extend",
        args.name,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ExtendSubscriptionResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/{+name}
// -----------------------------------------------------------------------------

/// GET v1/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = paymentsresellersubscription_partners_subscriptions_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn paymentsresellersubscription_partners_subscriptions_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &PaymentsresellersubscriptionPartnersSubscriptionsGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Subscription>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://paymentsresellersubscription.googleapis.com/v1/{}",
        args.name,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Subscription =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/{+parent}/subscriptions:provision
// -----------------------------------------------------------------------------

/// POST v1/{+parent}/subscriptions:provision.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = paymentsresellersubscription_partners_subscriptions_provision_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn paymentsresellersubscription_partners_subscriptions_provision_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &PaymentsresellersubscriptionPartnersSubscriptionsProvisionArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Subscription>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://paymentsresellersubscription.googleapis.com/v1/{}/subscriptions:provision",
        args.parent,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.cycle_options_initial_cycle_duration_count {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("cycleOptions_initialCycleDuration_count=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.cycle_options_initial_cycle_duration_unit {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("cycleOptions_initialCycleDuration_unit=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.subscription_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("subscriptionId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Subscription =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/{+name}:resume
// -----------------------------------------------------------------------------

/// POST v1/{+name}:resume.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = paymentsresellersubscription_partners_subscriptions_resume_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn paymentsresellersubscription_partners_subscriptions_resume_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &PaymentsresellersubscriptionPartnersSubscriptionsResumeArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ResumeSubscriptionResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://paymentsresellersubscription.googleapis.com/v1/{}:resume",
        args.name,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ResumeSubscriptionResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/{+name}:suspend
// -----------------------------------------------------------------------------

/// POST v1/{+name}:suspend.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = paymentsresellersubscription_partners_subscriptions_suspend_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn paymentsresellersubscription_partners_subscriptions_suspend_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &PaymentsresellersubscriptionPartnersSubscriptionsSuspendArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SuspendSubscriptionResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://paymentsresellersubscription.googleapis.com/v1/{}:suspend",
        args.name,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: SuspendSubscriptionResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/{+name}:undoCancel
// -----------------------------------------------------------------------------

/// POST v1/{+name}:undoCancel.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = paymentsresellersubscription_partners_subscriptions_undo_cancel_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn paymentsresellersubscription_partners_subscriptions_undo_cancel_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &PaymentsresellersubscriptionPartnersSubscriptionsUndoCancelArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<UndoCancelSubscriptionResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://paymentsresellersubscription.googleapis.com/v1/{}:undoCancel",
        args.name,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: UndoCancelSubscriptionResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PATCH v1/{+name}
// -----------------------------------------------------------------------------

/// PATCH v1/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = paymentsresellersubscription_partners_subscriptions_line_items_patch_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn paymentsresellersubscription_partners_subscriptions_line_items_patch_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &PaymentsresellersubscriptionPartnersSubscriptionsLineItemsPatchArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SubscriptionLineItem>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://paymentsresellersubscription.googleapis.com/v1/{}",
        args.name,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.update_mask {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("updateMask=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .patch(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: SubscriptionLineItem =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/{+parent}/userSessions:generate
// -----------------------------------------------------------------------------

/// POST v1/{+parent}/userSessions:generate.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = paymentsresellersubscription_partners_user_sessions_generate_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn paymentsresellersubscription_partners_user_sessions_generate_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &PaymentsresellersubscriptionPartnersUserSessionsGenerateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GenerateUserSessionResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://paymentsresellersubscription.googleapis.com/v1/{}/userSessions:generate",
        args.parent,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GenerateUserSessionResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}
