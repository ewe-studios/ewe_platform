//! Auto-generated API module for gcp/health health.
//!
//! Generated by `cargo run --bin ewe_platform gen_api`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp_health_health `

#![cfg(feature = "gcp_health_health")]
#![allow(clippy::too_many_arguments, clippy::type_complexity)]
#![allow(
    clippy::missing_errors_doc,
    clippy::doc_markdown,
    clippy::useless_format
)]
#![allow(unused_imports)]

use foundation_core::valtron::{TaskIterator, TaskIteratorExt};
use foundation_core::wire::simple_http::client::{ClientRequestBuilder, SimpleHttpClient};
use foundation_macros::JsonHash;
use serde::{Deserialize, Serialize};

// Import shared types used by this module
use super::shared::Operation;

use super::shared::ApiResponse;

// =============================================================================
// TYPE DECLARATIONS
// =============================================================================

/// `Date` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Date {
    /// day property.
    pub day: Option<i64>,
    /// month property.
    pub month: Option<i64>,
    /// year property.
    pub year: Option<i64>,
}

/// `VolumeQuantityRollup` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct VolumeQuantityRollup {
    /// millilitersSum property.
    pub milliliters_sum: Option<f64>,
    /// userProvidedUnitLast property.
    pub user_provided_unit_last: Option<String>,
}

/// `RollUpDataPointsRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct RollUpDataPointsRequest {
    /// dataSourceFamily property.
    pub data_source_family: Option<String>,
    /// pageSize property.
    pub page_size: Option<i64>,
    /// pageToken property.
    pub page_token: Option<String>,
    /// range property.
    pub range: Option<Interval>,
    /// windowSize property.
    pub window_size: Option<String>,
}

/// `ActivityLevelRollupValue` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ActivityLevelRollupValue {
    /// activityLevelRollupsByActivityLevelType property.
    pub activity_level_rollups_by_activity_level_type:
        Option<Vec<ActivityLevelRollupByActivityLevelType>>,
}

/// `HeartRate` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct HeartRate {
    /// beatsPerMinute property.
    pub beats_per_minute: Option<String>,
    /// metadata property.
    pub metadata: Option<HeartRateMetadata>,
    /// sampleTime property.
    pub sample_time: Option<ObservationSampleTime>,
}

/// `ActiveMinutesByActivityLevel` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ActiveMinutesByActivityLevel {
    /// activeMinutes property.
    pub active_minutes: Option<String>,
    /// activityLevel property.
    pub activity_level: Option<String>,
}

/// `HeartRateVariabilityPersonalRangeRollupValue` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct HeartRateVariabilityPersonalRangeRollupValue {
    /// averageHeartRateVariabilityMillisecondsMax property.
    pub average_heart_rate_variability_milliseconds_max: Option<f64>,
    /// averageHeartRateVariabilityMillisecondsMin property.
    pub average_heart_rate_variability_milliseconds_min: Option<f64>,
}

/// `RollupDataPoint` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct RollupDataPoint {
    /// activeMinutes property.
    pub active_minutes: Option<ActiveMinutesRollupValue>,
    /// activeZoneMinutes property.
    pub active_zone_minutes: Option<ActiveZoneMinutesRollupValue>,
    /// activityLevel property.
    pub activity_level: Option<ActivityLevelRollupValue>,
    /// altitude property.
    pub altitude: Option<AltitudeRollupValue>,
    /// bodyFat property.
    pub body_fat: Option<BodyFatRollupValue>,
    /// caloriesInHeartRateZone property.
    pub calories_in_heart_rate_zone: Option<CaloriesInHeartRateZoneRollupValue>,
    /// distance property.
    pub distance: Option<DistanceRollupValue>,
    /// endTime property.
    pub end_time: Option<String>,
    /// floors property.
    pub floors: Option<FloorsRollupValue>,
    /// heartRate property.
    pub heart_rate: Option<HeartRateRollupValue>,
    /// hydrationLog property.
    pub hydration_log: Option<HydrationLogRollupValue>,
    /// runVo2Max property.
    pub run_vo2_max: Option<RunVO2MaxRollupValue>,
    /// sedentaryPeriod property.
    pub sedentary_period: Option<SedentaryPeriodRollupValue>,
    /// startTime property.
    pub start_time: Option<String>,
    /// steps property.
    pub steps: Option<StepsRollupValue>,
    /// timeInHeartRateZone property.
    pub time_in_heart_rate_zone: Option<TimeInHeartRateZoneRollupValue>,
    /// totalCalories property.
    pub total_calories: Option<TotalCaloriesRollupValue>,
    /// weight property.
    pub weight: Option<WeightRollupValue>,
}

/// `ReconciledDataPoint` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ReconciledDataPoint {
    /// activeMinutes property.
    pub active_minutes: Option<ActiveMinutes>,
    /// activeZoneMinutes property.
    pub active_zone_minutes: Option<ActiveZoneMinutes>,
    /// activityLevel property.
    pub activity_level: Option<ActivityLevel>,
    /// altitude property.
    pub altitude: Option<Altitude>,
    /// bodyFat property.
    pub body_fat: Option<BodyFat>,
    /// dailyHeartRateVariability property.
    pub daily_heart_rate_variability: Option<DailyHeartRateVariability>,
    /// dailyHeartRateZones property.
    pub daily_heart_rate_zones: Option<DailyHeartRateZones>,
    /// dailyOxygenSaturation property.
    pub daily_oxygen_saturation: Option<DailyOxygenSaturation>,
    /// dailyRespiratoryRate property.
    pub daily_respiratory_rate: Option<DailyRespiratoryRate>,
    /// dailyRestingHeartRate property.
    pub daily_resting_heart_rate: Option<DailyRestingHeartRate>,
    /// dailySleepTemperatureDerivations property.
    pub daily_sleep_temperature_derivations: Option<DailySleepTemperatureDerivations>,
    /// dailyVo2Max property.
    pub daily_vo2_max: Option<DailyVO2Max>,
    /// dataPointName property.
    pub data_point_name: Option<String>,
    /// distance property.
    pub distance: Option<Distance>,
    /// exercise property.
    pub exercise: Option<Exercise>,
    /// floors property.
    pub floors: Option<Floors>,
    /// heartRate property.
    pub heart_rate: Option<HeartRate>,
    /// heartRateVariability property.
    pub heart_rate_variability: Option<HeartRateVariability>,
    /// hydrationLog property.
    pub hydration_log: Option<HydrationLog>,
    /// oxygenSaturation property.
    pub oxygen_saturation: Option<OxygenSaturation>,
    /// respiratoryRateSleepSummary property.
    pub respiratory_rate_sleep_summary: Option<RespiratoryRateSleepSummary>,
    /// runVo2Max property.
    pub run_vo2_max: Option<RunVO2Max>,
    /// sedentaryPeriod property.
    pub sedentary_period: Option<SedentaryPeriod>,
    /// sleep property.
    pub sleep: Option<Sleep>,
    /// steps property.
    pub steps: Option<Steps>,
    /// timeInHeartRateZone property.
    pub time_in_heart_rate_zone: Option<TimeInHeartRateZone>,
    /// vo2Max property.
    pub vo2_max: Option<VO2Max>,
    /// weight property.
    pub weight: Option<Weight>,
}

/// `ExerciseEvent` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ExerciseEvent {
    /// eventTime property.
    pub event_time: Option<String>,
    /// eventUtcOffset property.
    pub event_utc_offset: Option<String>,
    /// exerciseEventType property.
    pub exercise_event_type: Option<String>,
}

/// `AltitudeRollupValue` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AltitudeRollupValue {
    /// gainMillimetersSum property.
    pub gain_millimeters_sum: Option<String>,
}

/// `BodyFatRollupValue` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct BodyFatRollupValue {
    /// bodyFatPercentageAvg property.
    pub body_fat_percentage_avg: Option<f64>,
}

/// `DailyHeartRateVariability` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct DailyHeartRateVariability {
    /// averageHeartRateVariabilityMilliseconds property.
    pub average_heart_rate_variability_milliseconds: Option<f64>,
    /// date property.
    pub date: Option<Date>,
    /// deepSleepRootMeanSquareOfSuccessiveDifferencesMilliseconds property.
    pub deep_sleep_root_mean_square_of_successive_differences_milliseconds: Option<f64>,
    /// entropy property.
    pub entropy: Option<f64>,
    /// nonRemHeartRateBeatsPerMinute property.
    pub non_rem_heart_rate_beats_per_minute: Option<String>,
}

/// `DailyRestingHeartRateMetadata` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct DailyRestingHeartRateMetadata {
    /// calculationMethod property.
    pub calculation_method: Option<String>,
}

/// `ExerciseMetadata` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ExerciseMetadata {
    /// hasGps property.
    pub has_gps: Option<bool>,
    /// poolLengthMillimeters property.
    pub pool_length_millimeters: Option<String>,
}

/// `SleepMetadata` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SleepMetadata {
    /// externalId property.
    pub external_id: Option<String>,
    /// manuallyEdited property.
    pub manually_edited: Option<bool>,
    /// nap property.
    pub nap: Option<bool>,
    /// processed property.
    pub processed: Option<bool>,
    /// stagesStatus property.
    pub stages_status: Option<String>,
}

/// `DailyRestingHeartRate` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct DailyRestingHeartRate {
    /// beatsPerMinute property.
    pub beats_per_minute: Option<String>,
    /// dailyRestingHeartRateMetadata property.
    pub daily_resting_heart_rate_metadata: Option<DailyRestingHeartRateMetadata>,
    /// date property.
    pub date: Option<Date>,
}

/// `OxygenSaturation` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct OxygenSaturation {
    /// percentage property.
    pub percentage: Option<f64>,
    /// sampleTime property.
    pub sample_time: Option<ObservationSampleTime>,
}

/// `DailyRespiratoryRate` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct DailyRespiratoryRate {
    /// breathsPerMinute property.
    pub breaths_per_minute: Option<f64>,
    /// date property.
    pub date: Option<Date>,
}

/// `Distance` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Distance {
    /// interval property.
    pub interval: Option<ObservationTimeInterval>,
    /// millimeters property.
    pub millimeters: Option<String>,
}

/// `CaloriesInHeartRateZoneRollupValue` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct CaloriesInHeartRateZoneRollupValue {
    /// caloriesInHeartRateZones property.
    pub calories_in_heart_rate_zones: Option<Vec<CaloriesInHeartRateZoneValue>>,
}

/// `Device` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Device {
    /// displayName property.
    pub display_name: Option<String>,
    /// formFactor property.
    pub form_factor: Option<String>,
    /// manufacturer property.
    pub manufacturer: Option<String>,
}

/// `ListDataPointsResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ListDataPointsResponse {
    /// dataPoints property.
    pub data_points: Option<Vec<DataPoint>>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
}

/// `Status` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Status {
    /// code property.
    pub code: Option<i64>,
    /// details property.
    pub details: Option<Vec<serde_json::Value>>,
    /// message property.
    pub message: Option<String>,
}

/// `Identity` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Identity {
    /// healthUserId property.
    pub health_user_id: Option<String>,
    /// legacyUserId property.
    pub legacy_user_id: Option<String>,
    /// name property.
    pub name: Option<String>,
}

/// `ActiveZoneMinutesRollupValue` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ActiveZoneMinutesRollupValue {
    /// sumInCardioHeartZone property.
    pub sum_in_cardio_heart_zone: Option<String>,
    /// sumInFatBurnHeartZone property.
    pub sum_in_fat_burn_heart_zone: Option<String>,
    /// sumInPeakHeartZone property.
    pub sum_in_peak_heart_zone: Option<String>,
}

/// `ActiveZoneMinutes` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ActiveZoneMinutes {
    /// activeZoneMinutes property.
    pub active_zone_minutes: Option<String>,
    /// heartRateZone property.
    pub heart_rate_zone: Option<String>,
    /// interval property.
    pub interval: Option<ObservationTimeInterval>,
}

/// `Settings` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Settings {
    /// autoStrideEnabled property.
    pub auto_stride_enabled: Option<bool>,
    /// distanceUnit property.
    pub distance_unit: Option<String>,
    /// glucoseUnit property.
    pub glucose_unit: Option<String>,
    /// heightUnit property.
    pub height_unit: Option<String>,
    /// languageLocale property.
    pub language_locale: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// strideLengthRunningType property.
    pub stride_length_running_type: Option<String>,
    /// strideLengthWalkingType property.
    pub stride_length_walking_type: Option<String>,
    /// swimUnit property.
    pub swim_unit: Option<String>,
    /// temperatureUnit property.
    pub temperature_unit: Option<String>,
    /// timeZone property.
    pub time_zone: Option<String>,
    /// utcOffset property.
    pub utc_offset: Option<String>,
    /// waterUnit property.
    pub water_unit: Option<String>,
    /// weightUnit property.
    pub weight_unit: Option<String>,
}

/// `Profile` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Profile {
    /// age property.
    pub age: Option<i64>,
    /// autoRunningStrideLengthMm property.
    pub auto_running_stride_length_mm: Option<i64>,
    /// autoWalkingStrideLengthMm property.
    pub auto_walking_stride_length_mm: Option<i64>,
    /// membershipStartDate property.
    pub membership_start_date: Option<Date>,
    /// name property.
    pub name: Option<String>,
    /// userConfiguredRunningStrideLengthMm property.
    pub user_configured_running_stride_length_mm: Option<i64>,
    /// userConfiguredWalkingStrideLengthMm property.
    pub user_configured_walking_stride_length_mm: Option<i64>,
}

/// `SleepStage` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SleepStage {
    /// createTime property.
    pub create_time: Option<String>,
    /// endTime property.
    pub end_time: Option<String>,
    /// endUtcOffset property.
    pub end_utc_offset: Option<String>,
    /// startTime property.
    pub start_time: Option<String>,
    /// startUtcOffset property.
    pub start_utc_offset: Option<String>,
    /// type property.
    pub r#type: Option<String>,
    /// updateTime property.
    pub update_time: Option<String>,
}

/// `FloorsRollupValue` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct FloorsRollupValue {
    /// countSum property.
    pub count_sum: Option<String>,
}

/// `Exercise` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Exercise {
    /// activeDuration property.
    pub active_duration: Option<String>,
    /// createTime property.
    pub create_time: Option<String>,
    /// displayName property.
    pub display_name: Option<String>,
    /// exerciseEvents property.
    pub exercise_events: Option<Vec<ExerciseEvent>>,
    /// exerciseMetadata property.
    pub exercise_metadata: Option<ExerciseMetadata>,
    /// exerciseType property.
    pub exercise_type: Option<String>,
    /// interval property.
    pub interval: Option<SessionTimeInterval>,
    /// metricsSummary property.
    pub metrics_summary: Option<MetricsSummary>,
    /// notes property.
    pub notes: Option<String>,
    /// splitSummaries property.
    pub split_summaries: Option<Vec<SplitSummary>>,
    /// splits property.
    pub splits: Option<Vec<SplitSummary>>,
    /// updateTime property.
    pub update_time: Option<String>,
}

/// `Altitude` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Altitude {
    /// gainMillimeters property.
    pub gain_millimeters: Option<String>,
    /// interval property.
    pub interval: Option<ObservationTimeInterval>,
}

/// `HeartRateMetadata` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct HeartRateMetadata {
    /// motionContext property.
    pub motion_context: Option<String>,
    /// sensorLocation property.
    pub sensor_location: Option<String>,
}

/// `SedentaryPeriod` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SedentaryPeriod {
    /// interval property.
    pub interval: Option<ObservationTimeInterval>,
}

/// `DataSource` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct DataSource {
    /// application property.
    pub application: Option<Application>,
    /// device property.
    pub device: Option<Device>,
    /// platform property.
    pub platform: Option<String>,
    /// recordingMethod property.
    pub recording_method: Option<String>,
}

/// `HeartRateVariability` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct HeartRateVariability {
    /// rootMeanSquareOfSuccessiveDifferencesMilliseconds property.
    pub root_mean_square_of_successive_differences_milliseconds: Option<f64>,
    /// sampleTime property.
    pub sample_time: Option<ObservationSampleTime>,
    /// standardDeviationMilliseconds property.
    pub standard_deviation_milliseconds: Option<f64>,
}

/// `TimeInHeartRateZone` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct TimeInHeartRateZone {
    /// heartRateZoneType property.
    pub heart_rate_zone_type: Option<String>,
    /// interval property.
    pub interval: Option<ObservationTimeInterval>,
}

/// `VO2Max` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct VO2Max {
    /// measurementMethod property.
    pub measurement_method: Option<String>,
    /// sampleTime property.
    pub sample_time: Option<ObservationSampleTime>,
    /// vo2Max property.
    pub vo2_max: Option<f64>,
}

/// `CivilDateTime` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct CivilDateTime {
    /// date property.
    pub date: Option<Date>,
    /// time property.
    pub time: Option<TimeOfDay>,
}

/// `ObservationSampleTime` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ObservationSampleTime {
    /// civilTime property.
    pub civil_time: Option<CivilDateTime>,
    /// physicalTime property.
    pub physical_time: Option<String>,
    /// utcOffset property.
    pub utc_offset: Option<String>,
}

/// `ActiveMinutes` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ActiveMinutes {
    /// activeMinutesByActivityLevel property.
    pub active_minutes_by_activity_level: Option<Vec<ActiveMinutesByActivityLevel>>,
    /// interval property.
    pub interval: Option<ObservationTimeInterval>,
}

/// `TimeOfDay` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct TimeOfDay {
    /// hours property.
    pub hours: Option<i64>,
    /// minutes property.
    pub minutes: Option<i64>,
    /// nanos property.
    pub nanos: Option<i64>,
    /// seconds property.
    pub seconds: Option<i64>,
}

/// `ObservationTimeInterval` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ObservationTimeInterval {
    /// civilEndTime property.
    pub civil_end_time: Option<CivilDateTime>,
    /// civilStartTime property.
    pub civil_start_time: Option<CivilDateTime>,
    /// endTime property.
    pub end_time: Option<String>,
    /// endUtcOffset property.
    pub end_utc_offset: Option<String>,
    /// startTime property.
    pub start_time: Option<String>,
    /// startUtcOffset property.
    pub start_utc_offset: Option<String>,
}

/// `DailyRollupDataPoint` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct DailyRollupDataPoint {
    /// activeMinutes property.
    pub active_minutes: Option<ActiveMinutesRollupValue>,
    /// activeZoneMinutes property.
    pub active_zone_minutes: Option<ActiveZoneMinutesRollupValue>,
    /// activityLevel property.
    pub activity_level: Option<ActivityLevelRollupValue>,
    /// altitude property.
    pub altitude: Option<AltitudeRollupValue>,
    /// bodyFat property.
    pub body_fat: Option<BodyFatRollupValue>,
    /// caloriesInHeartRateZone property.
    pub calories_in_heart_rate_zone: Option<CaloriesInHeartRateZoneRollupValue>,
    /// civilEndTime property.
    pub civil_end_time: Option<CivilDateTime>,
    /// civilStartTime property.
    pub civil_start_time: Option<CivilDateTime>,
    /// distance property.
    pub distance: Option<DistanceRollupValue>,
    /// floors property.
    pub floors: Option<FloorsRollupValue>,
    /// heartRate property.
    pub heart_rate: Option<HeartRateRollupValue>,
    /// heartRateVariabilityPersonalRange property.
    pub heart_rate_variability_personal_range: Option<HeartRateVariabilityPersonalRangeRollupValue>,
    /// hydrationLog property.
    pub hydration_log: Option<HydrationLogRollupValue>,
    /// restingHeartRatePersonalRange property.
    pub resting_heart_rate_personal_range: Option<RestingHeartRatePersonalRangeRollupValue>,
    /// runVo2Max property.
    pub run_vo2_max: Option<RunVO2MaxRollupValue>,
    /// sedentaryPeriod property.
    pub sedentary_period: Option<SedentaryPeriodRollupValue>,
    /// steps property.
    pub steps: Option<StepsRollupValue>,
    /// timeInHeartRateZone property.
    pub time_in_heart_rate_zone: Option<TimeInHeartRateZoneRollupValue>,
    /// totalCalories property.
    pub total_calories: Option<TotalCaloriesRollupValue>,
    /// weight property.
    pub weight: Option<WeightRollupValue>,
}

/// `HydrationLog` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct HydrationLog {
    /// amountConsumed property.
    pub amount_consumed: Option<VolumeQuantity>,
    /// interval property.
    pub interval: Option<SessionTimeInterval>,
}

/// `RestingHeartRatePersonalRangeRollupValue` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct RestingHeartRatePersonalRangeRollupValue {
    /// beatsPerMinuteMax property.
    pub beats_per_minute_max: Option<f64>,
    /// beatsPerMinuteMin property.
    pub beats_per_minute_min: Option<f64>,
}

/// `HeartRateRollupValue` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct HeartRateRollupValue {
    /// beatsPerMinuteAvg property.
    pub beats_per_minute_avg: Option<f64>,
    /// beatsPerMinuteMax property.
    pub beats_per_minute_max: Option<f64>,
    /// beatsPerMinuteMin property.
    pub beats_per_minute_min: Option<f64>,
}

/// `SedentaryPeriodRollupValue` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SedentaryPeriodRollupValue {
    /// durationSum property.
    pub duration_sum: Option<String>,
}

/// `DailyRollUpDataPointsRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct DailyRollUpDataPointsRequest {
    /// dataSourceFamily property.
    pub data_source_family: Option<String>,
    /// pageSize property.
    pub page_size: Option<i64>,
    /// pageToken property.
    pub page_token: Option<String>,
    /// range property.
    pub range: Option<CivilTimeInterval>,
    /// windowSizeDays property.
    pub window_size_days: Option<i64>,
}

/// `TimeInHeartRateZoneValue` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct TimeInHeartRateZoneValue {
    /// duration property.
    pub duration: Option<String>,
    /// heartRateZone property.
    pub heart_rate_zone: Option<String>,
}

/// `ActiveMinutesRollupByActivityLevel` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ActiveMinutesRollupByActivityLevel {
    /// activeMinutesSum property.
    pub active_minutes_sum: Option<String>,
    /// activityLevel property.
    pub activity_level: Option<String>,
}

/// `DailyVO2Max` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct DailyVO2Max {
    /// cardioFitnessLevel property.
    pub cardio_fitness_level: Option<String>,
    /// date property.
    pub date: Option<Date>,
    /// estimated property.
    pub estimated: Option<bool>,
    /// vo2Max property.
    pub vo2_max: Option<f64>,
    /// vo2MaxCovariance property.
    pub vo2_max_covariance: Option<f64>,
}

/// `CivilTimeInterval` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct CivilTimeInterval {
    /// end property.
    pub end: Option<CivilDateTime>,
    /// start property.
    pub start: Option<CivilDateTime>,
}

/// `RunVO2Max` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct RunVO2Max {
    /// runVo2Max property.
    pub run_vo2_max: Option<f64>,
    /// sampleTime property.
    pub sample_time: Option<ObservationSampleTime>,
}

/// `DailyRollUpDataPointsResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct DailyRollUpDataPointsResponse {
    /// rollupDataPoints property.
    pub rollup_data_points: Option<Vec<DailyRollupDataPoint>>,
}

/// `ActiveMinutesRollupValue` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ActiveMinutesRollupValue {
    /// activeMinutesRollupByActivityLevel property.
    pub active_minutes_rollup_by_activity_level: Option<Vec<ActiveMinutesRollupByActivityLevel>>,
}

/// `Interval` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Interval {
    /// endTime property.
    pub end_time: Option<String>,
    /// startTime property.
    pub start_time: Option<String>,
}

/// `Weight` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Weight {
    /// notes property.
    pub notes: Option<String>,
    /// sampleTime property.
    pub sample_time: Option<ObservationSampleTime>,
    /// weightGrams property.
    pub weight_grams: Option<f64>,
}

/// `ReconcileDataPointsResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ReconcileDataPointsResponse {
    /// dataPoints property.
    pub data_points: Option<Vec<ReconciledDataPoint>>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
}

/// `Floors` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Floors {
    /// count property.
    pub count: Option<String>,
    /// interval property.
    pub interval: Option<ObservationTimeInterval>,
}

/// `ActivityLevelRollupByActivityLevelType` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ActivityLevelRollupByActivityLevelType {
    /// activityLevelType property.
    pub activity_level_type: Option<String>,
    /// totalDuration property.
    pub total_duration: Option<String>,
}

/// `RollUpDataPointsResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct RollUpDataPointsResponse {
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// rollupDataPoints property.
    pub rollup_data_points: Option<Vec<RollupDataPoint>>,
}

/// `TotalCaloriesRollupValue` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct TotalCaloriesRollupValue {
    /// kcalSum property.
    pub kcal_sum: Option<f64>,
}

/// `DailyHeartRateZones` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct DailyHeartRateZones {
    /// date property.
    pub date: Option<Date>,
    /// heartRateZones property.
    pub heart_rate_zones: Option<Vec<HeartRateZone>>,
}

/// `MobilityMetrics` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct MobilityMetrics {
    /// avgCadenceStepsPerMinute property.
    pub avg_cadence_steps_per_minute: Option<f64>,
    /// avgGroundContactTimeDuration property.
    pub avg_ground_contact_time_duration: Option<String>,
    /// avgStrideLengthMillimeters property.
    pub avg_stride_length_millimeters: Option<String>,
    /// avgVerticalOscillationMillimeters property.
    pub avg_vertical_oscillation_millimeters: Option<String>,
    /// avgVerticalRatio property.
    pub avg_vertical_ratio: Option<f64>,
}

/// `BatchDeleteDataPointsRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct BatchDeleteDataPointsRequest {
    /// names property.
    pub names: Option<Vec<String>>,
}

/// `VolumeQuantity` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct VolumeQuantity {
    /// milliliters property.
    pub milliliters: Option<f64>,
    /// userProvidedUnit property.
    pub user_provided_unit: Option<String>,
}

/// `BodyFat` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct BodyFat {
    /// percentage property.
    pub percentage: Option<f64>,
    /// sampleTime property.
    pub sample_time: Option<ObservationSampleTime>,
}

/// `Sleep` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Sleep {
    /// createTime property.
    pub create_time: Option<String>,
    /// interval property.
    pub interval: Option<SessionTimeInterval>,
    /// metadata property.
    pub metadata: Option<SleepMetadata>,
    /// outOfBedSegments property.
    pub out_of_bed_segments: Option<Vec<OutOfBedSegment>>,
    /// stages property.
    pub stages: Option<Vec<SleepStage>>,
    /// summary property.
    pub summary: Option<SleepSummary>,
    /// type property.
    pub r#type: Option<String>,
    /// updateTime property.
    pub update_time: Option<String>,
}

/// `CaloriesInHeartRateZoneValue` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct CaloriesInHeartRateZoneValue {
    /// heartRateZone property.
    pub heart_rate_zone: Option<String>,
    /// kcal property.
    pub kcal: Option<f64>,
}

/// `RespiratoryRateSleepSummaryStatistics` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct RespiratoryRateSleepSummaryStatistics {
    /// breathsPerMinute property.
    pub breaths_per_minute: Option<f64>,
    /// signalToNoise property.
    pub signal_to_noise: Option<f64>,
    /// standardDeviation property.
    pub standard_deviation: Option<f64>,
}

/// `TimeInHeartRateZones` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct TimeInHeartRateZones {
    /// lightTime property.
    pub light_time: Option<String>,
    /// moderateTime property.
    pub moderate_time: Option<String>,
    /// peakTime property.
    pub peak_time: Option<String>,
    /// vigorousTime property.
    pub vigorous_time: Option<String>,
}

/// `DataPoint` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct DataPoint {
    /// activeMinutes property.
    pub active_minutes: Option<ActiveMinutes>,
    /// activeZoneMinutes property.
    pub active_zone_minutes: Option<ActiveZoneMinutes>,
    /// activityLevel property.
    pub activity_level: Option<ActivityLevel>,
    /// altitude property.
    pub altitude: Option<Altitude>,
    /// bodyFat property.
    pub body_fat: Option<BodyFat>,
    /// dailyHeartRateVariability property.
    pub daily_heart_rate_variability: Option<DailyHeartRateVariability>,
    /// dailyHeartRateZones property.
    pub daily_heart_rate_zones: Option<DailyHeartRateZones>,
    /// dailyOxygenSaturation property.
    pub daily_oxygen_saturation: Option<DailyOxygenSaturation>,
    /// dailyRespiratoryRate property.
    pub daily_respiratory_rate: Option<DailyRespiratoryRate>,
    /// dailyRestingHeartRate property.
    pub daily_resting_heart_rate: Option<DailyRestingHeartRate>,
    /// dailySleepTemperatureDerivations property.
    pub daily_sleep_temperature_derivations: Option<DailySleepTemperatureDerivations>,
    /// dailyVo2Max property.
    pub daily_vo2_max: Option<DailyVO2Max>,
    /// dataSource property.
    pub data_source: Option<DataSource>,
    /// distance property.
    pub distance: Option<Distance>,
    /// exercise property.
    pub exercise: Option<Exercise>,
    /// floors property.
    pub floors: Option<Floors>,
    /// heartRate property.
    pub heart_rate: Option<HeartRate>,
    /// heartRateVariability property.
    pub heart_rate_variability: Option<HeartRateVariability>,
    /// hydrationLog property.
    pub hydration_log: Option<HydrationLog>,
    /// name property.
    pub name: Option<String>,
    /// oxygenSaturation property.
    pub oxygen_saturation: Option<OxygenSaturation>,
    /// respiratoryRateSleepSummary property.
    pub respiratory_rate_sleep_summary: Option<RespiratoryRateSleepSummary>,
    /// runVo2Max property.
    pub run_vo2_max: Option<RunVO2Max>,
    /// sedentaryPeriod property.
    pub sedentary_period: Option<SedentaryPeriod>,
    /// sleep property.
    pub sleep: Option<Sleep>,
    /// steps property.
    pub steps: Option<Steps>,
    /// timeInHeartRateZone property.
    pub time_in_heart_rate_zone: Option<TimeInHeartRateZone>,
    /// vo2Max property.
    pub vo2_max: Option<VO2Max>,
    /// weight property.
    pub weight: Option<Weight>,
}

/// `OutOfBedSegment` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct OutOfBedSegment {
    /// endTime property.
    pub end_time: Option<String>,
    /// endUtcOffset property.
    pub end_utc_offset: Option<String>,
    /// startTime property.
    pub start_time: Option<String>,
    /// startUtcOffset property.
    pub start_utc_offset: Option<String>,
}

/// `TimeInHeartRateZoneRollupValue` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct TimeInHeartRateZoneRollupValue {
    /// timeInHeartRateZones property.
    pub time_in_heart_rate_zones: Option<Vec<TimeInHeartRateZoneValue>>,
}

/// `SplitSummary` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SplitSummary {
    /// activeDuration property.
    pub active_duration: Option<String>,
    /// endTime property.
    pub end_time: Option<String>,
    /// endUtcOffset property.
    pub end_utc_offset: Option<String>,
    /// metricsSummary property.
    pub metrics_summary: Option<MetricsSummary>,
    /// splitType property.
    pub split_type: Option<String>,
    /// startTime property.
    pub start_time: Option<String>,
    /// startUtcOffset property.
    pub start_utc_offset: Option<String>,
}

/// `SessionTimeInterval` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SessionTimeInterval {
    /// civilEndTime property.
    pub civil_end_time: Option<CivilDateTime>,
    /// civilStartTime property.
    pub civil_start_time: Option<CivilDateTime>,
    /// endTime property.
    pub end_time: Option<String>,
    /// endUtcOffset property.
    pub end_utc_offset: Option<String>,
    /// startTime property.
    pub start_time: Option<String>,
    /// startUtcOffset property.
    pub start_utc_offset: Option<String>,
}

/// `RunVO2MaxRollupValue` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct RunVO2MaxRollupValue {
    /// rateAvg property.
    pub rate_avg: Option<f64>,
    /// rateMax property.
    pub rate_max: Option<f64>,
    /// rateMin property.
    pub rate_min: Option<f64>,
}

/// `Steps` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Steps {
    /// count property.
    pub count: Option<String>,
    /// interval property.
    pub interval: Option<ObservationTimeInterval>,
}

/// `Application` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Application {
    /// googleWebClientId property.
    pub google_web_client_id: Option<String>,
    /// packageName property.
    pub package_name: Option<String>,
    /// webClientId property.
    pub web_client_id: Option<String>,
}

/// `WeightRollupValue` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct WeightRollupValue {
    /// weightGramsAvg property.
    pub weight_grams_avg: Option<f64>,
}

/// `ExportExerciseTcxResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ExportExerciseTcxResponse {
    /// tcxData property.
    pub tcx_data: Option<String>,
}

/// `HydrationLogRollupValue` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct HydrationLogRollupValue {
    /// amountConsumed property.
    pub amount_consumed: Option<VolumeQuantityRollup>,
}

/// `DailySleepTemperatureDerivations` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct DailySleepTemperatureDerivations {
    /// baselineTemperatureCelsius property.
    pub baseline_temperature_celsius: Option<f64>,
    /// date property.
    pub date: Option<Date>,
    /// nightlyTemperatureCelsius property.
    pub nightly_temperature_celsius: Option<f64>,
    /// relativeNightlyStddev30dCelsius property.
    pub relative_nightly_stddev30d_celsius: Option<f64>,
}

/// `StageSummary` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct StageSummary {
    /// count property.
    pub count: Option<String>,
    /// minutes property.
    pub minutes: Option<String>,
    /// type property.
    pub r#type: Option<String>,
}

/// `ActivityLevel` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ActivityLevel {
    /// activityLevelType property.
    pub activity_level_type: Option<String>,
    /// interval property.
    pub interval: Option<ObservationTimeInterval>,
}

/// `MetricsSummary` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct MetricsSummary {
    /// activeZoneMinutes property.
    pub active_zone_minutes: Option<String>,
    /// averageHeartRateBeatsPerMinute property.
    pub average_heart_rate_beats_per_minute: Option<String>,
    /// averagePaceSecondsPerMeter property.
    pub average_pace_seconds_per_meter: Option<f64>,
    /// averageSpeedMillimetersPerSecond property.
    pub average_speed_millimeters_per_second: Option<f64>,
    /// caloriesKcal property.
    pub calories_kcal: Option<f64>,
    /// distanceMillimeters property.
    pub distance_millimeters: Option<f64>,
    /// elevationGainMillimeters property.
    pub elevation_gain_millimeters: Option<f64>,
    /// heartRateZoneDurations property.
    pub heart_rate_zone_durations: Option<TimeInHeartRateZones>,
    /// mobilityMetrics property.
    pub mobility_metrics: Option<MobilityMetrics>,
    /// runVo2Max property.
    pub run_vo2_max: Option<f64>,
    /// steps property.
    pub steps: Option<String>,
    /// totalSwimLengths property.
    pub total_swim_lengths: Option<f64>,
}

/// `StepsRollupValue` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct StepsRollupValue {
    /// countSum property.
    pub count_sum: Option<String>,
}

/// `DailyOxygenSaturation` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct DailyOxygenSaturation {
    /// averagePercentage property.
    pub average_percentage: Option<f64>,
    /// date property.
    pub date: Option<Date>,
    /// lowerBoundPercentage property.
    pub lower_bound_percentage: Option<f64>,
    /// standardDeviationPercentage property.
    pub standard_deviation_percentage: Option<f64>,
    /// upperBoundPercentage property.
    pub upper_bound_percentage: Option<f64>,
}

/// `SleepSummary` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SleepSummary {
    /// minutesAfterWakeUp property.
    pub minutes_after_wake_up: Option<String>,
    /// minutesAsleep property.
    pub minutes_asleep: Option<String>,
    /// minutesAwake property.
    pub minutes_awake: Option<String>,
    /// minutesInSleepPeriod property.
    pub minutes_in_sleep_period: Option<String>,
    /// minutesToFallAsleep property.
    pub minutes_to_fall_asleep: Option<String>,
    /// stagesSummary property.
    pub stages_summary: Option<Vec<StageSummary>>,
}

/// `HeartRateZone` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct HeartRateZone {
    /// heartRateZoneType property.
    pub heart_rate_zone_type: Option<String>,
    /// maxBeatsPerMinute property.
    pub max_beats_per_minute: Option<String>,
    /// minBeatsPerMinute property.
    pub min_beats_per_minute: Option<String>,
}

/// `RespiratoryRateSleepSummary` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct RespiratoryRateSleepSummary {
    /// deepSleepStats property.
    pub deep_sleep_stats: Option<RespiratoryRateSleepSummaryStatistics>,
    /// fullSleepStats property.
    pub full_sleep_stats: Option<RespiratoryRateSleepSummaryStatistics>,
    /// lightSleepStats property.
    pub light_sleep_stats: Option<RespiratoryRateSleepSummaryStatistics>,
    /// remSleepStats property.
    pub rem_sleep_stats: Option<RespiratoryRateSleepSummaryStatistics>,
    /// sampleTime property.
    pub sample_time: Option<ObservationSampleTime>,
}

/// `DistanceRollupValue` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct DistanceRollupValue {
    /// millimetersSum property.
    pub millimeters_sum: Option<String>,
}

// =============================================================================
// ARGS TYPES (per-endpoint)
// =============================================================================

/// Arguments for [`health.users.getIdentity_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct HealthUsersGetIdentityArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`health.users.getProfile_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct HealthUsersGetProfileArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`health.users.getSettings_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct HealthUsersGetSettingsArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`health.users.updateProfile_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct HealthUsersUpdateProfileArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `updateMask`.
    pub update_mask: Option<String>,
    /// Request body.
    pub body: Profile,
}

/// Arguments for [`health.users.updateSettings_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct HealthUsersUpdateSettingsArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `updateMask`.
    pub update_mask: Option<String>,
    /// Request body.
    pub body: Settings,
}

/// Arguments for [`health.users.dataTypes.dataPoints.batchDelete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct HealthUsersDataTypesDataPointsBatchDeleteArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Request body.
    pub body: BatchDeleteDataPointsRequest,
}

/// Arguments for [`health.users.dataTypes.dataPoints.create_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct HealthUsersDataTypesDataPointsCreateArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Request body.
    pub body: DataPoint,
}

/// Arguments for [`health.users.dataTypes.dataPoints.dailyRollUp_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct HealthUsersDataTypesDataPointsDailyRollUpArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Request body.
    pub body: DailyRollUpDataPointsRequest,
}

/// Arguments for [`health.users.dataTypes.dataPoints.exportExerciseTcx_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct HealthUsersDataTypesDataPointsExportExerciseTcxArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `partialData`.
    pub partial_data: Option<String>,
}

/// Arguments for [`health.users.dataTypes.dataPoints.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct HealthUsersDataTypesDataPointsListArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

/// Arguments for [`health.users.dataTypes.dataPoints.patch_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct HealthUsersDataTypesDataPointsPatchArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Request body.
    pub body: DataPoint,
}

/// Arguments for [`health.users.dataTypes.dataPoints.reconcile_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct HealthUsersDataTypesDataPointsReconcileArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `dataSourceFamily`.
    pub data_source_family: Option<String>,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

/// Arguments for [`health.users.dataTypes.dataPoints.rollUp_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct HealthUsersDataTypesDataPointsRollUpArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Request body.
    pub body: RollUpDataPointsRequest,
}

// =============================================================================
// CLIENT FUNCTIONS (per-endpoint)
// =============================================================================

// -----------------------------------------------------------------------------
// GET v4/{+name}
// -----------------------------------------------------------------------------

/// GET v4/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = health_users_get_identity_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn health_users_get_identity_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &HealthUsersGetIdentityArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Identity>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://health.googleapis.com/v4/{}", args.name,);

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Identity =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v4/{+name}
// -----------------------------------------------------------------------------

/// GET v4/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = health_users_get_profile_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn health_users_get_profile_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &HealthUsersGetProfileArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Profile>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://health.googleapis.com/v4/{}", args.name,);

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Profile =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v4/{+name}
// -----------------------------------------------------------------------------

/// GET v4/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = health_users_get_settings_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn health_users_get_settings_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &HealthUsersGetSettingsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Settings>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://health.googleapis.com/v4/{}", args.name,);

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Settings =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PATCH v4/{+name}
// -----------------------------------------------------------------------------

/// PATCH v4/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = health_users_update_profile_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn health_users_update_profile_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &HealthUsersUpdateProfileArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Profile>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://health.googleapis.com/v4/{}", args.name,);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.update_mask {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("updateMask=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .patch(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Profile =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PATCH v4/{+name}
// -----------------------------------------------------------------------------

/// PATCH v4/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = health_users_update_settings_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn health_users_update_settings_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &HealthUsersUpdateSettingsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Settings>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://health.googleapis.com/v4/{}", args.name,);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.update_mask {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("updateMask=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .patch(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Settings =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v4/{+parent}/dataPoints:batchDelete
// -----------------------------------------------------------------------------

/// POST v4/{+parent}/dataPoints:batchDelete.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = health_users_data_types_data_points_batch_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn health_users_data_types_data_points_batch_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &HealthUsersDataTypesDataPointsBatchDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://health.googleapis.com/v4/{}/dataPoints:batchDelete",
        args.parent,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v4/{+parent}/dataPoints
// -----------------------------------------------------------------------------

/// POST v4/{+parent}/dataPoints.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = health_users_data_types_data_points_create_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn health_users_data_types_data_points_create_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &HealthUsersDataTypesDataPointsCreateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://health.googleapis.com/v4/{}/dataPoints",
        args.parent,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v4/{+parent}/dataPoints:dailyRollUp
// -----------------------------------------------------------------------------

/// POST v4/{+parent}/dataPoints:dailyRollUp.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = health_users_data_types_data_points_daily_roll_up_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn health_users_data_types_data_points_daily_roll_up_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &HealthUsersDataTypesDataPointsDailyRollUpArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<DailyRollUpDataPointsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://health.googleapis.com/v4/{}/dataPoints:dailyRollUp",
        args.parent,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: DailyRollUpDataPointsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v4/{+name}:exportExerciseTcx
// -----------------------------------------------------------------------------

/// GET v4/{+name}:exportExerciseTcx.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = health_users_data_types_data_points_export_exercise_tcx_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn health_users_data_types_data_points_export_exercise_tcx_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &HealthUsersDataTypesDataPointsExportExerciseTcxArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ExportExerciseTcxResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://health.googleapis.com/v4/{}:exportExerciseTcx",
        args.name,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.partial_data {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("partialData=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ExportExerciseTcxResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v4/{+parent}/dataPoints
// -----------------------------------------------------------------------------

/// GET v4/{+parent}/dataPoints.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = health_users_data_types_data_points_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn health_users_data_types_data_points_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &HealthUsersDataTypesDataPointsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListDataPointsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://health.googleapis.com/v4/{}/dataPoints",
        args.parent,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ListDataPointsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PATCH v4/{+name}
// -----------------------------------------------------------------------------

/// PATCH v4/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = health_users_data_types_data_points_patch_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn health_users_data_types_data_points_patch_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &HealthUsersDataTypesDataPointsPatchArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://health.googleapis.com/v4/{}", args.name,);

    let mut builder = client
        .patch(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v4/{+parent}/dataPoints:reconcile
// -----------------------------------------------------------------------------

/// GET v4/{+parent}/dataPoints:reconcile.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = health_users_data_types_data_points_reconcile_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn health_users_data_types_data_points_reconcile_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &HealthUsersDataTypesDataPointsReconcileArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ReconcileDataPointsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://health.googleapis.com/v4/{}/dataPoints:reconcile",
        args.parent,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.data_source_family {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("dataSourceFamily=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ReconcileDataPointsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v4/{+parent}/dataPoints:rollUp
// -----------------------------------------------------------------------------

/// POST v4/{+parent}/dataPoints:rollUp.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = health_users_data_types_data_points_roll_up_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn health_users_data_types_data_points_roll_up_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &HealthUsersDataTypesDataPointsRollUpArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<RollUpDataPointsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://health.googleapis.com/v4/{}/dataPoints:rollUp",
        args.parent,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: RollUpDataPointsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}
