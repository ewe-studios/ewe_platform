//! Auto-generated API module for gcp/contactcenterinsights settings.
//!
//! Generated by `cargo run --bin ewe_platform gen_api`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp_contactcenterinsights_settings `

#![cfg(feature = "gcp_contactcenterinsights_settings")]
#![allow(clippy::too_many_arguments, clippy::type_complexity)]
#![allow(
    clippy::missing_errors_doc,
    clippy::doc_markdown,
    clippy::useless_format
)]

use foundation_core::valtron::{execute, StreamIterator, TaskIterator, TaskIteratorExt};
use foundation_core::wire::simple_http::client::{ClientRequestBuilder, SimpleHttpClient};
use foundation_macros::JsonHash;
use serde::{Deserialize, Serialize};

use super::shared::{ApiError, ApiPending, ApiResponse};

// =============================================================================
// TYPE DECLARATIONS
// =============================================================================

/// `GoogleCloudContactcenterinsightsV1AnnotatorSelector` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudContactcenterinsightsV1AnnotatorSelector {
    /// issueModels property.
    pub issue_models: Option<Vec<String>>,
    /// phraseMatchers property.
    pub phrase_matchers: Option<Vec<String>>,
    /// qaConfig property.
    pub qa_config: Option<GoogleCloudContactcenterinsightsV1AnnotatorSelectorQaConfig>,
    /// runAutoLabelingAnnotator property.
    pub run_auto_labeling_annotator: Option<bool>,
    /// runEntityAnnotator property.
    pub run_entity_annotator: Option<bool>,
    /// runIntentAnnotator property.
    pub run_intent_annotator: Option<bool>,
    /// runInterruptionAnnotator property.
    pub run_interruption_annotator: Option<bool>,
    /// runIssueModelAnnotator property.
    pub run_issue_model_annotator: Option<bool>,
    /// runPhraseMatcherAnnotator property.
    pub run_phrase_matcher_annotator: Option<bool>,
    /// runQaAnnotator property.
    pub run_qa_annotator: Option<bool>,
    /// runSentimentAnnotator property.
    pub run_sentiment_annotator: Option<bool>,
    /// runSilenceAnnotator property.
    pub run_silence_annotator: Option<bool>,
    /// runSummarizationAnnotator property.
    pub run_summarization_annotator: Option<bool>,
    /// summarizationConfig property.
    pub summarization_config:
        Option<GoogleCloudContactcenterinsightsV1AnnotatorSelectorSummarizationConfig>,
}

/// `GoogleCloudContactcenterinsightsV1Settings` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudContactcenterinsightsV1Settings {
    /// analysisConfig property.
    pub analysis_config: Option<GoogleCloudContactcenterinsightsV1SettingsAnalysisConfig>,
    /// conversationTtl property.
    pub conversation_ttl: Option<String>,
    /// createTime property.
    pub create_time: Option<String>,
    /// languageCode property.
    pub language_code: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// pubsubNotificationSettings property.
    pub pubsub_notification_settings: Option<serde_json::Value>,
    /// redactionConfig property.
    pub redaction_config: Option<GoogleCloudContactcenterinsightsV1RedactionConfig>,
    /// screenRecordingBucketUri property.
    pub screen_recording_bucket_uri: Option<String>,
    /// speechConfig property.
    pub speech_config: Option<GoogleCloudContactcenterinsightsV1SpeechConfig>,
    /// timeZone property.
    pub time_zone: Option<String>,
    /// updateTime property.
    pub update_time: Option<String>,
}

/// `GoogleCloudContactcenterinsightsV1SpeechConfig` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudContactcenterinsightsV1SpeechConfig {
    /// disableWordTimeOffsets property.
    pub disable_word_time_offsets: Option<bool>,
    /// speechRecognizer property.
    pub speech_recognizer: Option<String>,
}

/// `GoogleCloudContactcenterinsightsV1AnnotatorSelectorQaConfig` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudContactcenterinsightsV1AnnotatorSelectorQaConfig {
    /// scorecardList property.
    pub scorecard_list:
        Option<GoogleCloudContactcenterinsightsV1AnnotatorSelectorQaConfigScorecardList>,
}

/// `GoogleCloudContactcenterinsightsV1AnnotatorSelectorQaConfigScorecardList` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudContactcenterinsightsV1AnnotatorSelectorQaConfigScorecardList {
    /// qaScorecardRevisions property.
    pub qa_scorecard_revisions: Option<Vec<String>>,
}

/// `GoogleCloudContactcenterinsightsV1AnnotatorSelectorSummarizationConfig` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudContactcenterinsightsV1AnnotatorSelectorSummarizationConfig {
    /// conversationProfile property.
    pub conversation_profile: Option<String>,
    /// generator property.
    pub generator: Option<String>,
    /// summarizationModel property.
    pub summarization_model: Option<String>,
}

/// `GoogleCloudContactcenterinsightsV1SettingsAnalysisConfig` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudContactcenterinsightsV1SettingsAnalysisConfig {
    /// annotatorSelector property.
    pub annotator_selector: Option<GoogleCloudContactcenterinsightsV1AnnotatorSelector>,
    /// runtimeIntegrationAnalysisPercentage property.
    pub runtime_integration_analysis_percentage: Option<f64>,
    /// uploadConversationAnalysisPercentage property.
    pub upload_conversation_analysis_percentage: Option<f64>,
}

/// `GoogleCloudContactcenterinsightsV1RedactionConfig` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudContactcenterinsightsV1RedactionConfig {
    /// deidentifyTemplate property.
    pub deidentify_template: Option<String>,
    /// inspectTemplate property.
    pub inspect_template: Option<String>,
}

// =============================================================================
// ARGS TYPES (per-endpoint)
// =============================================================================

/// Arguments for [`contactcenterinsights.projects.locations.getSettings_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContactcenterinsightsProjectsLocationsGetSettingsArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`contactcenterinsights.projects.locations.updateSettings_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContactcenterinsightsProjectsLocationsUpdateSettingsArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `updateMask`.
    pub update_mask: Option<String>,
}

// =============================================================================
// CLIENT FUNCTIONS (per-endpoint)
// =============================================================================

// -----------------------------------------------------------------------------
// GET v1/projects/{projectsId}/locations/{locationsId}/settings
// -----------------------------------------------------------------------------

/// GET v1/projects/{projectsId}/locations/{locationsId}/settings.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = contactcenterinsights_projects_locations_get_settings_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn contactcenterinsights_projects_locations_get_settings_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudContactcenterinsightsV1Settings>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://contactcenterinsights.googleapis.com/v1/projects/{{projectsId}}/locations/{{locationsId}}/settings",
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleCloudContactcenterinsightsV1Settings =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PATCH v1/projects/{projectsId}/locations/{locationsId}/settings
// -----------------------------------------------------------------------------

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/settings.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = contactcenterinsights_projects_locations_update_settings_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn contactcenterinsights_projects_locations_update_settings_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudContactcenterinsightsV1Settings>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://contactcenterinsights.googleapis.com/v1/projects/{{projectsId}}/locations/{{locationsId}}/settings",
    );

    let mut builder = client
        .patch(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleCloudContactcenterinsightsV1Settings =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}
