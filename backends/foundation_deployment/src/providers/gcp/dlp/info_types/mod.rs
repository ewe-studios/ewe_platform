//! Auto-generated API module for gcp/dlp infoTypes.
//!
//! Generated by `cargo run --bin ewe_platform gen_api`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp_dlp_info_types `

#![cfg(feature = "gcp_dlp_info_types")]
#![allow(clippy::too_many_arguments, clippy::type_complexity)]
#![allow(
    clippy::missing_errors_doc,
    clippy::doc_markdown,
    clippy::useless_format
)]

use foundation_core::valtron::{execute, StreamIterator, TaskIterator, TaskIteratorExt};
use foundation_core::wire::simple_http::client::{ClientRequestBuilder, SimpleHttpClient};
use foundation_macros::JsonHash;
use serde::{Deserialize, Serialize};

use super::shared::{ApiError, ApiPending, ApiResponse};

// =============================================================================
// TYPE DECLARATIONS
// =============================================================================

/// `GooglePrivacyDlpV2InfoTypeDescription` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct GooglePrivacyDlpV2InfoTypeDescription {
    /// categories property.
    pub categories: Option<Vec<GooglePrivacyDlpV2InfoTypeCategory>>,
    /// description property.
    pub description: Option<String>,
    /// displayName property.
    pub display_name: Option<String>,
    /// example property.
    pub example: Option<String>,
    /// launchStatus property.
    pub launch_status: Option<String>,
    /// locationSupport property.
    pub location_support: Option<GooglePrivacyDlpV2LocationSupport>,
    /// name property.
    pub name: Option<String>,
    /// sensitivityScore property.
    pub sensitivity_score: Option<GooglePrivacyDlpV2SensitivityScore>,
    /// specificInfoTypes property.
    pub specific_info_types: Option<Vec<String>>,
    /// supportedBy property.
    pub supported_by: Option<Vec<String>>,
    /// versions property.
    pub versions: Option<Vec<GooglePrivacyDlpV2VersionDescription>>,
}

/// `GooglePrivacyDlpV2ListInfoTypesResponse` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct GooglePrivacyDlpV2ListInfoTypesResponse {
    /// infoTypes property.
    pub info_types: Option<Vec<GooglePrivacyDlpV2InfoTypeDescription>>,
}

/// `GooglePrivacyDlpV2LocationSupport` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct GooglePrivacyDlpV2LocationSupport {
    /// locations property.
    pub locations: Option<Vec<String>>,
    /// regionalizationScope property.
    pub regionalization_scope: Option<String>,
}

/// `GooglePrivacyDlpV2SensitivityScore` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct GooglePrivacyDlpV2SensitivityScore {
    /// score property.
    pub score: Option<String>,
}

/// `GooglePrivacyDlpV2VersionDescription` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct GooglePrivacyDlpV2VersionDescription {
    /// description property.
    pub description: Option<String>,
    /// version property.
    pub version: Option<String>,
}

/// `GooglePrivacyDlpV2InfoTypeCategory` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct GooglePrivacyDlpV2InfoTypeCategory {
    /// industryCategory property.
    pub industry_category: Option<String>,
    /// locationCategory property.
    pub location_category: Option<String>,
    /// typeCategory property.
    pub type_category: Option<String>,
}

// =============================================================================
// ARGS TYPES (per-endpoint)
// =============================================================================

/// Arguments for [`dlp.infoTypes.list_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DlpInfoTypesListArgs {
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `languageCode`.
    pub language_code: Option<String>,
    /// Query parameter: `locationId`.
    pub location_id: Option<String>,
    /// Query parameter: `parent`.
    pub parent: Option<String>,
}

/// Arguments for [`dlp.locations.infoTypes.list_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DlpLocationsInfoTypesListArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `languageCode`.
    pub language_code: Option<String>,
    /// Query parameter: `locationId`.
    pub location_id: Option<String>,
}

/// Arguments for [`dlp.organizations.locations.infoTypes.list_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DlpOrganizationsLocationsInfoTypesListArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `languageCode`.
    pub language_code: Option<String>,
    /// Query parameter: `locationId`.
    pub location_id: Option<String>,
}

/// Arguments for [`dlp.projects.locations.infoTypes.list_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DlpProjectsLocationsInfoTypesListArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `languageCode`.
    pub language_code: Option<String>,
    /// Query parameter: `locationId`.
    pub location_id: Option<String>,
}

// =============================================================================
// CLIENT FUNCTIONS (per-endpoint)
// =============================================================================

// -----------------------------------------------------------------------------
// GET v2/infoTypes
// -----------------------------------------------------------------------------

/// GET v2/infoTypes.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dlp_info_types_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dlp_info_types_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GooglePrivacyDlpV2ListInfoTypesResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://dlp.googleapis.com/v2/infoTypes",);

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2ListInfoTypesResponse = serde_json::from_str(&body)
                    .map_err(
                    |e: serde_json::Error| super::shared::ApiError::ParseFailed(e.to_string()),
                )?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v2/locations/{locationsId}/infoTypes
// -----------------------------------------------------------------------------

/// GET v2/locations/{locationsId}/infoTypes.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dlp_locations_info_types_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dlp_locations_info_types_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GooglePrivacyDlpV2ListInfoTypesResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url =
        format!("https://dlp.googleapis.com/v2/locations/{{locationsId}}/infoTypes",);

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2ListInfoTypesResponse = serde_json::from_str(&body)
                    .map_err(
                    |e: serde_json::Error| super::shared::ApiError::ParseFailed(e.to_string()),
                )?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v2/organizations/{organizationsId}/locations/{locationsId}/infoTypes
// -----------------------------------------------------------------------------

/// GET v2/organizations/{organizationsId}/locations/{locationsId}/infoTypes.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dlp_organizations_locations_info_types_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dlp_organizations_locations_info_types_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GooglePrivacyDlpV2ListInfoTypesResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dlp.googleapis.com/v2/organizations/{{organizationsId}}/locations/{{locationsId}}/infoTypes",
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2ListInfoTypesResponse = serde_json::from_str(&body)
                    .map_err(
                    |e: serde_json::Error| super::shared::ApiError::ParseFailed(e.to_string()),
                )?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v2/projects/{projectsId}/locations/{locationsId}/infoTypes
// -----------------------------------------------------------------------------

/// GET v2/projects/{projectsId}/locations/{locationsId}/infoTypes.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dlp_projects_locations_info_types_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dlp_projects_locations_info_types_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GooglePrivacyDlpV2ListInfoTypesResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dlp.googleapis.com/v2/projects/{{projectsId}}/locations/{{locationsId}}/infoTypes",
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GooglePrivacyDlpV2ListInfoTypesResponse = serde_json::from_str(&body)
                    .map_err(
                    |e: serde_json::Error| super::shared::ApiError::ParseFailed(e.to_string()),
                )?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}
