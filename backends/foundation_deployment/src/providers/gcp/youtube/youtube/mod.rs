//! Auto-generated API module for gcp/youtube youtube.
//!
//! Generated by `cargo run --bin ewe_platform gen_api`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp_youtube_youtube `

#![cfg(feature = "gcp_youtube_youtube")]
#![allow(clippy::too_many_arguments, clippy::type_complexity)]
#![allow(
    clippy::missing_errors_doc,
    clippy::doc_markdown,
    clippy::useless_format
)]
#![allow(unused_imports)]

use foundation_core::valtron::{TaskIterator, TaskIteratorExt};
use foundation_core::wire::simple_http::client::{ClientRequestBuilder, SimpleHttpClient};
use foundation_macros::JsonHash;
use serde::{Deserialize, Serialize};

use super::shared::ApiResponse;

// =============================================================================
// TYPE DECLARATIONS
// =============================================================================

/// `ThirdPartyLink` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ThirdPartyLink {
    /// etag property.
    pub etag: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// linkingToken property.
    pub linking_token: Option<String>,
    /// snippet property.
    pub snippet: Option<ThirdPartyLinkSnippet>,
    /// status property.
    pub status: Option<ThirdPartyLinkStatus>,
}

/// `PlaylistItemListResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct PlaylistItemListResponse {
    /// etag property.
    pub etag: Option<String>,
    /// eventId property.
    pub event_id: Option<String>,
    /// items property.
    pub items: Option<Vec<PlaylistItem>>,
    /// kind property.
    pub kind: Option<String>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// pageInfo property.
    pub page_info: Option<PageInfo>,
    /// prevPageToken property.
    pub prev_page_token: Option<String>,
    /// tokenPagination property.
    pub token_pagination: Option<TokenPagination>,
    /// visitorId property.
    pub visitor_id: Option<String>,
}

/// `GeoPoint` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GeoPoint {
    /// altitude property.
    pub altitude: Option<f64>,
    /// latitude property.
    pub latitude: Option<f64>,
    /// longitude property.
    pub longitude: Option<f64>,
}

/// `I18NLanguage` response type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct I18NLanguage {
    /// Raw JSON value - full schema generated from `OpenAPI`
    #[serde(flatten)]
    pub data: std::collections::HashMap<String, serde_json::Value>,
}

/// `I18NLanguageSnippet` response type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct I18NLanguageSnippet {
    /// Raw JSON value - full schema generated from `OpenAPI`
    #[serde(flatten)]
    pub data: std::collections::HashMap<String, serde_json::Value>,
}

/// `LiveChatMessageAuthorDetails` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct LiveChatMessageAuthorDetails {
    /// channelId property.
    pub channel_id: Option<String>,
    /// channelUrl property.
    pub channel_url: Option<String>,
    /// displayName property.
    pub display_name: Option<String>,
    /// isChatModerator property.
    pub is_chat_moderator: Option<bool>,
    /// isChatOwner property.
    pub is_chat_owner: Option<bool>,
    /// isChatSponsor property.
    pub is_chat_sponsor: Option<bool>,
    /// isVerified property.
    pub is_verified: Option<bool>,
    /// profileImageUrl property.
    pub profile_image_url: Option<String>,
}

/// `SubscriptionSubscriberSnippet` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SubscriptionSubscriberSnippet {
    /// channelId property.
    pub channel_id: Option<String>,
    /// description property.
    pub description: Option<String>,
    /// thumbnails property.
    pub thumbnails: Option<ThumbnailDetails>,
    /// title property.
    pub title: Option<String>,
}

/// `LiveChatMessageListResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct LiveChatMessageListResponse {
    /// activePollItem property.
    pub active_poll_item: Option<LiveChatMessage>,
    /// etag property.
    pub etag: Option<String>,
    /// eventId property.
    pub event_id: Option<String>,
    /// items property.
    pub items: Option<Vec<LiveChatMessage>>,
    /// kind property.
    pub kind: Option<String>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// offlineAt property.
    pub offline_at: Option<String>,
    /// pageInfo property.
    pub page_info: Option<PageInfo>,
    /// pollingIntervalMillis property.
    pub polling_interval_millis: Option<i64>,
    /// tokenPagination property.
    pub token_pagination: Option<TokenPagination>,
    /// visitorId property.
    pub visitor_id: Option<String>,
}

/// `LiveBroadcastMonetizationDetails` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct LiveBroadcastMonetizationDetails {
    /// cuepointSchedule property.
    pub cuepoint_schedule: Option<CuepointSchedule>,
}

/// `VideoPlayer` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct VideoPlayer {
    /// embedHeight property.
    pub embed_height: Option<String>,
    /// embedHtml property.
    pub embed_html: Option<String>,
    /// embedWidth property.
    pub embed_width: Option<String>,
}

/// `SubscriptionListResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SubscriptionListResponse {
    /// etag property.
    pub etag: Option<String>,
    /// eventId property.
    pub event_id: Option<String>,
    /// items property.
    pub items: Option<Vec<Subscription>>,
    /// kind property.
    pub kind: Option<String>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// pageInfo property.
    pub page_info: Option<PageInfo>,
    /// prevPageToken property.
    pub prev_page_token: Option<String>,
    /// tokenPagination property.
    pub token_pagination: Option<TokenPagination>,
    /// visitorId property.
    pub visitor_id: Option<String>,
}

/// `VideoStatistics` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct VideoStatistics {
    /// commentCount property.
    pub comment_count: Option<String>,
    /// dislikeCount property.
    pub dislike_count: Option<String>,
    /// favoriteCount property.
    pub favorite_count: Option<String>,
    /// likeCount property.
    pub like_count: Option<String>,
    /// viewCount property.
    pub view_count: Option<String>,
}

/// `MembershipsLevel` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct MembershipsLevel {
    /// etag property.
    pub etag: Option<String>,
    /// id property.
    pub id: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// snippet property.
    pub snippet: Option<MembershipsLevelSnippet>,
}

/// `VideoAbuseReport` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct VideoAbuseReport {
    /// comments property.
    pub comments: Option<String>,
    /// language property.
    pub language: Option<String>,
    /// reasonId property.
    pub reason_id: Option<String>,
    /// secondaryReasonId property.
    pub secondary_reason_id: Option<String>,
    /// videoId property.
    pub video_id: Option<String>,
}

/// `VideoCategory` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct VideoCategory {
    /// etag property.
    pub etag: Option<String>,
    /// id property.
    pub id: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// snippet property.
    pub snippet: Option<VideoCategorySnippet>,
}

/// `MembershipsDuration` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct MembershipsDuration {
    /// memberSince property.
    pub member_since: Option<String>,
    /// memberTotalDurationMonths property.
    pub member_total_duration_months: Option<i64>,
}

/// `SuperStickerMetadata` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SuperStickerMetadata {
    /// altText property.
    pub alt_text: Option<String>,
    /// altTextLanguage property.
    pub alt_text_language: Option<String>,
    /// stickerId property.
    pub sticker_id: Option<String>,
}

/// `MonitorStreamInfo` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct MonitorStreamInfo {
    /// broadcastStreamDelayMs property.
    pub broadcast_stream_delay_ms: Option<i64>,
    /// embedHtml property.
    pub embed_html: Option<String>,
    /// enableMonitorStream property.
    pub enable_monitor_stream: Option<bool>,
}

/// `Comment` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Comment {
    /// etag property.
    pub etag: Option<String>,
    /// id property.
    pub id: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// snippet property.
    pub snippet: Option<CommentSnippet>,
}

/// `PlaylistLocalization` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct PlaylistLocalization {
    /// description property.
    pub description: Option<String>,
    /// title property.
    pub title: Option<String>,
}

/// `MembershipsLevelSnippet` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct MembershipsLevelSnippet {
    /// creatorChannelId property.
    pub creator_channel_id: Option<String>,
    /// levelDetails property.
    pub level_details: Option<LevelDetails>,
}

/// `TokenPagination` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct TokenPagination {}

/// `Entity` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Entity {
    /// id property.
    pub id: Option<String>,
    /// typeId property.
    pub type_id: Option<String>,
    /// url property.
    pub url: Option<String>,
}

/// `SearchResultSnippet` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SearchResultSnippet {
    /// channelId property.
    pub channel_id: Option<String>,
    /// channelTitle property.
    pub channel_title: Option<String>,
    /// description property.
    pub description: Option<String>,
    /// liveBroadcastContent property.
    pub live_broadcast_content: Option<String>,
    /// publishedAt property.
    pub published_at: Option<String>,
    /// thumbnails property.
    pub thumbnails: Option<ThumbnailDetails>,
    /// title property.
    pub title: Option<String>,
}

/// `ChannelToStoreLinkDetailsBillingDetails` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ChannelToStoreLinkDetailsBillingDetails {
    /// billingStatus property.
    pub billing_status: Option<String>,
}

/// `CaptionListResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct CaptionListResponse {
    /// etag property.
    pub etag: Option<String>,
    /// eventId property.
    pub event_id: Option<String>,
    /// items property.
    pub items: Option<Vec<Caption>>,
    /// kind property.
    pub kind: Option<String>,
    /// visitorId property.
    pub visitor_id: Option<String>,
}

/// `PlaylistImage` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct PlaylistImage {
    /// id property.
    pub id: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// snippet property.
    pub snippet: Option<PlaylistImageSnippet>,
}

/// `ChannelContentDetails` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ChannelContentDetails {
    /// relatedPlaylists property.
    pub related_playlists: Option<std::collections::HashMap<String, serde_json::Value>>,
}

/// `ThumbnailDetails` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ThumbnailDetails {
    /// default property.
    pub default: Option<Thumbnail>,
    /// high property.
    pub high: Option<Thumbnail>,
    /// maxres property.
    pub maxres: Option<Thumbnail>,
    /// medium property.
    pub medium: Option<Thumbnail>,
    /// standard property.
    pub standard: Option<Thumbnail>,
}

/// `ActivityContentDetailsPromotedItem` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ActivityContentDetailsPromotedItem {
    /// adTag property.
    pub ad_tag: Option<String>,
    /// clickTrackingUrl property.
    pub click_tracking_url: Option<String>,
    /// creativeViewUrl property.
    pub creative_view_url: Option<String>,
    /// ctaType property.
    pub cta_type: Option<String>,
    /// customCtaButtonText property.
    pub custom_cta_button_text: Option<String>,
    /// descriptionText property.
    pub description_text: Option<String>,
    /// destinationUrl property.
    pub destination_url: Option<String>,
    /// forecastingUrl property.
    pub forecasting_url: Option<Vec<String>>,
    /// impressionUrl property.
    pub impression_url: Option<Vec<String>>,
    /// videoId property.
    pub video_id: Option<String>,
}

/// `LiveBroadcastSnippet` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct LiveBroadcastSnippet {
    /// actualEndTime property.
    pub actual_end_time: Option<String>,
    /// actualStartTime property.
    pub actual_start_time: Option<String>,
    /// channelId property.
    pub channel_id: Option<String>,
    /// description property.
    pub description: Option<String>,
    /// isDefaultBroadcast property.
    pub is_default_broadcast: Option<bool>,
    /// liveChatId property.
    pub live_chat_id: Option<String>,
    /// publishedAt property.
    pub published_at: Option<String>,
    /// scheduledEndTime property.
    pub scheduled_end_time: Option<String>,
    /// scheduledStartTime property.
    pub scheduled_start_time: Option<String>,
    /// thumbnails property.
    pub thumbnails: Option<ThumbnailDetails>,
    /// title property.
    pub title: Option<String>,
}

/// `ActivityContentDetailsPlaylistItem` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ActivityContentDetailsPlaylistItem {
    /// playlistId property.
    pub playlist_id: Option<String>,
    /// playlistItemId property.
    pub playlist_item_id: Option<String>,
    /// resourceId property.
    pub resource_id: Option<ResourceId>,
}

/// `PlaylistListResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct PlaylistListResponse {
    /// etag property.
    pub etag: Option<String>,
    /// eventId property.
    pub event_id: Option<String>,
    /// items property.
    pub items: Option<Vec<Playlist>>,
    /// kind property.
    pub kind: Option<String>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// pageInfo property.
    pub page_info: Option<PageInfo>,
    /// prevPageToken property.
    pub prev_page_token: Option<String>,
    /// tokenPagination property.
    pub token_pagination: Option<TokenPagination>,
    /// visitorId property.
    pub visitor_id: Option<String>,
}

/// `ActivityContentDetailsUpload` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ActivityContentDetailsUpload {
    /// videoId property.
    pub video_id: Option<String>,
}

/// `ChannelListResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ChannelListResponse {
    /// etag property.
    pub etag: Option<String>,
    /// eventId property.
    pub event_id: Option<String>,
    /// items property.
    pub items: Option<Vec<Channel>>,
    /// kind property.
    pub kind: Option<String>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// pageInfo property.
    pub page_info: Option<PageInfo>,
    /// prevPageToken property.
    pub prev_page_token: Option<String>,
    /// tokenPagination property.
    pub token_pagination: Option<TokenPagination>,
    /// visitorId property.
    pub visitor_id: Option<String>,
}

/// `LiveBroadcastContentDetails` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct LiveBroadcastContentDetails {
    /// boundStreamId property.
    pub bound_stream_id: Option<String>,
    /// boundStreamLastUpdateTimeMs property.
    pub bound_stream_last_update_time_ms: Option<String>,
    /// closedCaptionsType property.
    pub closed_captions_type: Option<String>,
    /// enableAutoStart property.
    pub enable_auto_start: Option<bool>,
    /// enableAutoStop property.
    pub enable_auto_stop: Option<bool>,
    /// enableClosedCaptions property.
    pub enable_closed_captions: Option<bool>,
    /// enableContentEncryption property.
    pub enable_content_encryption: Option<bool>,
    /// enableDvr property.
    pub enable_dvr: Option<bool>,
    /// enableEmbed property.
    pub enable_embed: Option<bool>,
    /// enableLowLatency property.
    pub enable_low_latency: Option<bool>,
    /// latencyPreference property.
    pub latency_preference: Option<String>,
    /// mesh property.
    pub mesh: Option<String>,
    /// monitorStream property.
    pub monitor_stream: Option<MonitorStreamInfo>,
    /// projection property.
    pub projection: Option<String>,
    /// recordFromStart property.
    pub record_from_start: Option<bool>,
    /// startWithSlate property.
    pub start_with_slate: Option<bool>,
    /// stereoLayout property.
    pub stereo_layout: Option<String>,
}

/// `LiveChatSuperChatDetails` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct LiveChatSuperChatDetails {
    /// amountDisplayString property.
    pub amount_display_string: Option<String>,
    /// amountMicros property.
    pub amount_micros: Option<String>,
    /// currency property.
    pub currency: Option<String>,
    /// tier property.
    pub tier: Option<i64>,
    /// userComment property.
    pub user_comment: Option<String>,
}

/// `ImageSettings` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ImageSettings {
    /// backgroundImageUrl property.
    pub background_image_url: Option<LocalizedProperty>,
    /// bannerExternalUrl property.
    pub banner_external_url: Option<String>,
    /// bannerImageUrl property.
    pub banner_image_url: Option<String>,
    /// bannerMobileExtraHdImageUrl property.
    pub banner_mobile_extra_hd_image_url: Option<String>,
    /// bannerMobileHdImageUrl property.
    pub banner_mobile_hd_image_url: Option<String>,
    /// bannerMobileImageUrl property.
    pub banner_mobile_image_url: Option<String>,
    /// bannerMobileLowImageUrl property.
    pub banner_mobile_low_image_url: Option<String>,
    /// bannerMobileMediumHdImageUrl property.
    pub banner_mobile_medium_hd_image_url: Option<String>,
    /// bannerTabletExtraHdImageUrl property.
    pub banner_tablet_extra_hd_image_url: Option<String>,
    /// bannerTabletHdImageUrl property.
    pub banner_tablet_hd_image_url: Option<String>,
    /// bannerTabletImageUrl property.
    pub banner_tablet_image_url: Option<String>,
    /// bannerTabletLowImageUrl property.
    pub banner_tablet_low_image_url: Option<String>,
    /// bannerTvHighImageUrl property.
    pub banner_tv_high_image_url: Option<String>,
    /// bannerTvImageUrl property.
    pub banner_tv_image_url: Option<String>,
    /// bannerTvLowImageUrl property.
    pub banner_tv_low_image_url: Option<String>,
    /// bannerTvMediumImageUrl property.
    pub banner_tv_medium_image_url: Option<String>,
    /// largeBrandedBannerImageImapScript property.
    pub large_branded_banner_image_imap_script: Option<LocalizedProperty>,
    /// largeBrandedBannerImageUrl property.
    pub large_branded_banner_image_url: Option<LocalizedProperty>,
    /// smallBrandedBannerImageImapScript property.
    pub small_branded_banner_image_imap_script: Option<LocalizedProperty>,
    /// smallBrandedBannerImageUrl property.
    pub small_branded_banner_image_url: Option<LocalizedProperty>,
    /// trackingImageUrl property.
    pub tracking_image_url: Option<String>,
    /// watchIconImageUrl property.
    pub watch_icon_image_url: Option<String>,
}

/// `ActivityContentDetailsLike` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ActivityContentDetailsLike {
    /// resourceId property.
    pub resource_id: Option<ResourceId>,
}

/// `CommentThreadReplies` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct CommentThreadReplies {
    /// comments property.
    pub comments: Option<Vec<Comment>>,
}

/// `ContentRating` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ContentRating {
    /// acbRating property.
    pub acb_rating: Option<String>,
    /// agcomRating property.
    pub agcom_rating: Option<String>,
    /// anatelRating property.
    pub anatel_rating: Option<String>,
    /// bbfcRating property.
    pub bbfc_rating: Option<String>,
    /// bfvcRating property.
    pub bfvc_rating: Option<String>,
    /// bmukkRating property.
    pub bmukk_rating: Option<String>,
    /// catvRating property.
    pub catv_rating: Option<String>,
    /// catvfrRating property.
    pub catvfr_rating: Option<String>,
    /// cbfcRating property.
    pub cbfc_rating: Option<String>,
    /// cccRating property.
    pub ccc_rating: Option<String>,
    /// cceRating property.
    pub cce_rating: Option<String>,
    /// chfilmRating property.
    pub chfilm_rating: Option<String>,
    /// chvrsRating property.
    pub chvrs_rating: Option<String>,
    /// cicfRating property.
    pub cicf_rating: Option<String>,
    /// cnaRating property.
    pub cna_rating: Option<String>,
    /// cncRating property.
    pub cnc_rating: Option<String>,
    /// csaRating property.
    pub csa_rating: Option<String>,
    /// cscfRating property.
    pub cscf_rating: Option<String>,
    /// czfilmRating property.
    pub czfilm_rating: Option<String>,
    /// djctqRating property.
    pub djctq_rating: Option<String>,
    /// djctqRatingReasons property.
    pub djctq_rating_reasons: Option<Vec<String>>,
    /// ecbmctRating property.
    pub ecbmct_rating: Option<String>,
    /// eefilmRating property.
    pub eefilm_rating: Option<String>,
    /// egfilmRating property.
    pub egfilm_rating: Option<String>,
    /// eirinRating property.
    pub eirin_rating: Option<String>,
    /// fcbmRating property.
    pub fcbm_rating: Option<String>,
    /// fcoRating property.
    pub fco_rating: Option<String>,
    /// fmocRating property.
    pub fmoc_rating: Option<String>,
    /// fpbRating property.
    pub fpb_rating: Option<String>,
    /// fpbRatingReasons property.
    pub fpb_rating_reasons: Option<Vec<String>>,
    /// fskRating property.
    pub fsk_rating: Option<String>,
    /// grfilmRating property.
    pub grfilm_rating: Option<String>,
    /// icaaRating property.
    pub icaa_rating: Option<String>,
    /// ifcoRating property.
    pub ifco_rating: Option<String>,
    /// ilfilmRating property.
    pub ilfilm_rating: Option<String>,
    /// incaaRating property.
    pub incaa_rating: Option<String>,
    /// kfcbRating property.
    pub kfcb_rating: Option<String>,
    /// kijkwijzerRating property.
    pub kijkwijzer_rating: Option<String>,
    /// kmrbRating property.
    pub kmrb_rating: Option<String>,
    /// lsfRating property.
    pub lsf_rating: Option<String>,
    /// mccaaRating property.
    pub mccaa_rating: Option<String>,
    /// mccypRating property.
    pub mccyp_rating: Option<String>,
    /// mcstRating property.
    pub mcst_rating: Option<String>,
    /// mdaRating property.
    pub mda_rating: Option<String>,
    /// medietilsynetRating property.
    pub medietilsynet_rating: Option<String>,
    /// mekuRating property.
    pub meku_rating: Option<String>,
    /// menaMpaaRating property.
    pub mena_mpaa_rating: Option<String>,
    /// mibacRating property.
    pub mibac_rating: Option<String>,
    /// mocRating property.
    pub moc_rating: Option<String>,
    /// moctwRating property.
    pub moctw_rating: Option<String>,
    /// mpaaRating property.
    pub mpaa_rating: Option<String>,
    /// mpaatRating property.
    pub mpaat_rating: Option<String>,
    /// mtrcbRating property.
    pub mtrcb_rating: Option<String>,
    /// nbcRating property.
    pub nbc_rating: Option<String>,
    /// nbcplRating property.
    pub nbcpl_rating: Option<String>,
    /// nfrcRating property.
    pub nfrc_rating: Option<String>,
    /// nfvcbRating property.
    pub nfvcb_rating: Option<String>,
    /// nkclvRating property.
    pub nkclv_rating: Option<String>,
    /// nmcRating property.
    pub nmc_rating: Option<String>,
    /// oflcRating property.
    pub oflc_rating: Option<String>,
    /// pefilmRating property.
    pub pefilm_rating: Option<String>,
    /// rcnofRating property.
    pub rcnof_rating: Option<String>,
    /// resorteviolenciaRating property.
    pub resorteviolencia_rating: Option<String>,
    /// rtcRating property.
    pub rtc_rating: Option<String>,
    /// rteRating property.
    pub rte_rating: Option<String>,
    /// russiaRating property.
    pub russia_rating: Option<String>,
    /// skfilmRating property.
    pub skfilm_rating: Option<String>,
    /// smaisRating property.
    pub smais_rating: Option<String>,
    /// smsaRating property.
    pub smsa_rating: Option<String>,
    /// tvpgRating property.
    pub tvpg_rating: Option<String>,
    /// ytRating property.
    pub yt_rating: Option<String>,
}

/// `WatchSettings` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct WatchSettings {
    /// backgroundColor property.
    pub background_color: Option<String>,
    /// featuredPlaylistId property.
    pub featured_playlist_id: Option<String>,
    /// textColor property.
    pub text_color: Option<String>,
}

/// `ThumbnailSetResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ThumbnailSetResponse {
    /// etag property.
    pub etag: Option<String>,
    /// eventId property.
    pub event_id: Option<String>,
    /// items property.
    pub items: Option<Vec<ThumbnailDetails>>,
    /// kind property.
    pub kind: Option<String>,
    /// visitorId property.
    pub visitor_id: Option<String>,
}

/// `VideoCategorySnippet` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct VideoCategorySnippet {
    /// assignable property.
    pub assignable: Option<bool>,
    /// channelId property.
    pub channel_id: Option<String>,
    /// title property.
    pub title: Option<String>,
}

/// `VideoProcessingDetailsProcessingProgress` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct VideoProcessingDetailsProcessingProgress {
    /// partsProcessed property.
    pub parts_processed: Option<String>,
    /// partsTotal property.
    pub parts_total: Option<String>,
    /// timeLeftMs property.
    pub time_left_ms: Option<String>,
}

/// `VideoAbuseReportReasonListResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct VideoAbuseReportReasonListResponse {
    /// etag property.
    pub etag: Option<String>,
    /// eventId property.
    pub event_id: Option<String>,
    /// items property.
    pub items: Option<Vec<VideoAbuseReportReason>>,
    /// kind property.
    pub kind: Option<String>,
    /// visitorId property.
    pub visitor_id: Option<String>,
}

/// `SuperChatEvent` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SuperChatEvent {
    /// etag property.
    pub etag: Option<String>,
    /// id property.
    pub id: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// snippet property.
    pub snippet: Option<SuperChatEventSnippet>,
}

/// `LiveStreamContentDetails` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct LiveStreamContentDetails {
    /// closedCaptionsIngestionUrl property.
    pub closed_captions_ingestion_url: Option<String>,
    /// isReusable property.
    pub is_reusable: Option<bool>,
}

/// `CommentThreadListResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct CommentThreadListResponse {
    /// etag property.
    pub etag: Option<String>,
    /// eventId property.
    pub event_id: Option<String>,
    /// items property.
    pub items: Option<Vec<CommentThread>>,
    /// kind property.
    pub kind: Option<String>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// pageInfo property.
    pub page_info: Option<PageInfo>,
    /// tokenPagination property.
    pub token_pagination: Option<TokenPagination>,
    /// visitorId property.
    pub visitor_id: Option<String>,
}

/// `VideoProjectDetails` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct VideoProjectDetails {}

/// `CommentThreadSnippet` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct CommentThreadSnippet {
    /// canReply property.
    pub can_reply: Option<bool>,
    /// channelId property.
    pub channel_id: Option<String>,
    /// isPublic property.
    pub is_public: Option<bool>,
    /// postId property.
    pub post_id: Option<String>,
    /// topLevelComment property.
    pub top_level_comment: Option<Comment>,
    /// totalReplyCount property.
    pub total_reply_count: Option<i64>,
    /// videoId property.
    pub video_id: Option<String>,
}

/// `VideoStatus` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct VideoStatus {
    /// containsSyntheticMedia property.
    pub contains_synthetic_media: Option<bool>,
    /// embeddable property.
    pub embeddable: Option<bool>,
    /// failureReason property.
    pub failure_reason: Option<String>,
    /// license property.
    pub license: Option<String>,
    /// madeForKids property.
    pub made_for_kids: Option<bool>,
    /// privacyStatus property.
    pub privacy_status: Option<String>,
    /// publicStatsViewable property.
    pub public_stats_viewable: Option<bool>,
    /// publishAt property.
    pub publish_at: Option<String>,
    /// rejectionReason property.
    pub rejection_reason: Option<String>,
    /// selfDeclaredMadeForKids property.
    pub self_declared_made_for_kids: Option<bool>,
    /// uploadStatus property.
    pub upload_status: Option<String>,
}

/// `SubscriptionContentDetails` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SubscriptionContentDetails {
    /// activityType property.
    pub activity_type: Option<String>,
    /// newItemCount property.
    pub new_item_count: Option<i64>,
    /// totalItemCount property.
    pub total_item_count: Option<i64>,
}

/// `PlaylistItemStatus` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct PlaylistItemStatus {
    /// privacyStatus property.
    pub privacy_status: Option<String>,
}

/// `ChannelSection` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ChannelSection {
    /// contentDetails property.
    pub content_details: Option<ChannelSectionContentDetails>,
    /// etag property.
    pub etag: Option<String>,
    /// id property.
    pub id: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// localizations property.
    pub localizations: Option<serde_json::Value>,
    /// snippet property.
    pub snippet: Option<ChannelSectionSnippet>,
    /// targeting property.
    pub targeting: Option<ChannelSectionTargeting>,
}

/// `PlaylistItemContentDetails` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct PlaylistItemContentDetails {
    /// endAt property.
    pub end_at: Option<String>,
    /// note property.
    pub note: Option<String>,
    /// startAt property.
    pub start_at: Option<String>,
    /// videoId property.
    pub video_id: Option<String>,
    /// videoPublishedAt property.
    pub video_published_at: Option<String>,
}

/// `LevelDetails` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct LevelDetails {
    /// displayName property.
    pub display_name: Option<String>,
}

/// `InvideoTiming` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct InvideoTiming {
    /// durationMs property.
    pub duration_ms: Option<String>,
    /// offsetMs property.
    pub offset_ms: Option<String>,
    /// type property.
    pub r#type: Option<String>,
}

/// `LiveBroadcastStatistics` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct LiveBroadcastStatistics {
    /// concurrentViewers property.
    pub concurrent_viewers: Option<String>,
}

/// `VideoMonetizationDetails` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct VideoMonetizationDetails {
    /// access property.
    pub access: Option<AccessPolicy>,
}

/// `LiveChatTextMessageDetails` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct LiveChatTextMessageDetails {
    /// messageText property.
    pub message_text: Option<String>,
}

/// `LiveChatPollDetailsPollMetadataPollOption` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct LiveChatPollDetailsPollMetadataPollOption {
    /// optionText property.
    pub option_text: Option<String>,
    /// tally property.
    pub tally: Option<String>,
}

/// `ChannelSettings` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ChannelSettings {
    /// country property.
    pub country: Option<String>,
    /// defaultLanguage property.
    pub default_language: Option<String>,
    /// defaultTab property.
    pub default_tab: Option<String>,
    /// description property.
    pub description: Option<String>,
    /// featuredChannelsTitle property.
    pub featured_channels_title: Option<String>,
    /// featuredChannelsUrls property.
    pub featured_channels_urls: Option<Vec<String>>,
    /// keywords property.
    pub keywords: Option<String>,
    /// moderateComments property.
    pub moderate_comments: Option<bool>,
    /// profileColor property.
    pub profile_color: Option<String>,
    /// showBrowseView property.
    pub show_browse_view: Option<bool>,
    /// showRelatedChannels property.
    pub show_related_channels: Option<bool>,
    /// title property.
    pub title: Option<String>,
    /// trackingAnalyticsAccountId property.
    pub tracking_analytics_account_id: Option<String>,
    /// unsubscribedTrailer property.
    pub unsubscribed_trailer: Option<String>,
}

/// `ActivityContentDetailsBulletin` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ActivityContentDetailsBulletin {
    /// resourceId property.
    pub resource_id: Option<ResourceId>,
}

/// `LocalizedString` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct LocalizedString {
    /// language property.
    pub language: Option<String>,
    /// value property.
    pub value: Option<String>,
}

/// `LiveChatMessageDeletedDetails` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct LiveChatMessageDeletedDetails {
    /// deletedMessageId property.
    pub deleted_message_id: Option<String>,
}

/// `ChannelSectionLocalization` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ChannelSectionLocalization {
    /// title property.
    pub title: Option<String>,
}

/// `I18nLanguageListResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct I18nLanguageListResponse {
    /// etag property.
    pub etag: Option<String>,
    /// eventId property.
    pub event_id: Option<String>,
    /// items property.
    pub items: Option<Vec<I18NLanguage>>,
    /// kind property.
    pub kind: Option<String>,
    /// visitorId property.
    pub visitor_id: Option<String>,
}

/// `LiveChatModerator` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct LiveChatModerator {
    /// etag property.
    pub etag: Option<String>,
    /// id property.
    pub id: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// snippet property.
    pub snippet: Option<LiveChatModeratorSnippet>,
}

/// `VideoPaidProductPlacementDetails` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct VideoPaidProductPlacementDetails {
    /// hasPaidProductPlacement property.
    pub has_paid_product_placement: Option<bool>,
}

/// `ChannelConversionPings` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ChannelConversionPings {
    /// pings property.
    pub pings: Option<Vec<ChannelConversionPing>>,
}

/// `ChannelStatistics` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ChannelStatistics {
    /// commentCount property.
    pub comment_count: Option<String>,
    /// hiddenSubscriberCount property.
    pub hidden_subscriber_count: Option<bool>,
    /// subscriberCount property.
    pub subscriber_count: Option<String>,
    /// videoCount property.
    pub video_count: Option<String>,
    /// viewCount property.
    pub view_count: Option<String>,
}

/// `LiveBroadcast` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct LiveBroadcast {
    /// contentDetails property.
    pub content_details: Option<LiveBroadcastContentDetails>,
    /// etag property.
    pub etag: Option<String>,
    /// id property.
    pub id: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// monetizationDetails property.
    pub monetization_details: Option<LiveBroadcastMonetizationDetails>,
    /// snippet property.
    pub snippet: Option<LiveBroadcastSnippet>,
    /// statistics property.
    pub statistics: Option<LiveBroadcastStatistics>,
    /// status property.
    pub status: Option<LiveBroadcastStatus>,
}

/// `I18NRegion` response type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct I18NRegion {
    /// Raw JSON value - full schema generated from `OpenAPI`
    #[serde(flatten)]
    pub data: std::collections::HashMap<String, serde_json::Value>,
}

/// `LiveChatGiftDetails` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct LiveChatGiftDetails {
    /// altText property.
    pub alt_text: Option<String>,
    /// comboCount property.
    pub combo_count: Option<i64>,
    /// giftDuration property.
    pub gift_duration: Option<String>,
    /// giftName property.
    pub gift_name: Option<String>,
    /// giftUrl property.
    pub gift_url: Option<String>,
    /// hasVisualEffect property.
    pub has_visual_effect: Option<bool>,
    /// jewelsAmount property.
    pub jewels_amount: Option<i64>,
    /// language property.
    pub language: Option<String>,
}

/// `VideoSuggestions` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct VideoSuggestions {
    /// editorSuggestions property.
    pub editor_suggestions: Option<Vec<String>>,
    /// processingErrors property.
    pub processing_errors: Option<Vec<String>>,
    /// processingHints property.
    pub processing_hints: Option<Vec<String>>,
    /// processingWarnings property.
    pub processing_warnings: Option<Vec<String>>,
    /// tagSuggestions property.
    pub tag_suggestions: Option<Vec<VideoSuggestionsTagSuggestion>>,
}

/// `PropertyValue` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct PropertyValue {
    /// property property.
    pub property: Option<String>,
    /// value property.
    pub value: Option<String>,
}

/// `PlaylistItem` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct PlaylistItem {
    /// contentDetails property.
    pub content_details: Option<PlaylistItemContentDetails>,
    /// etag property.
    pub etag: Option<String>,
    /// id property.
    pub id: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// snippet property.
    pub snippet: Option<PlaylistItemSnippet>,
    /// status property.
    pub status: Option<PlaylistItemStatus>,
}

/// `VideoContentDetails` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct VideoContentDetails {
    /// caption property.
    pub caption: Option<String>,
    /// contentRating property.
    pub content_rating: Option<ContentRating>,
    /// countryRestriction property.
    pub country_restriction: Option<AccessPolicy>,
    /// definition property.
    pub definition: Option<String>,
    /// dimension property.
    pub dimension: Option<String>,
    /// duration property.
    pub duration: Option<String>,
    /// hasCustomThumbnail property.
    pub has_custom_thumbnail: Option<bool>,
    /// licensedContent property.
    pub licensed_content: Option<bool>,
    /// projection property.
    pub projection: Option<String>,
    /// regionRestriction property.
    pub region_restriction: Option<VideoContentDetailsRegionRestriction>,
}

/// `ChannelSectionTargeting` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ChannelSectionTargeting {
    /// countries property.
    pub countries: Option<Vec<String>>,
    /// languages property.
    pub languages: Option<Vec<String>>,
    /// regions property.
    pub regions: Option<Vec<String>>,
}

/// `ChannelProfileDetails` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ChannelProfileDetails {
    /// channelId property.
    pub channel_id: Option<String>,
    /// channelUrl property.
    pub channel_url: Option<String>,
    /// displayName property.
    pub display_name: Option<String>,
    /// profileImageUrl property.
    pub profile_image_url: Option<String>,
}

/// `AbuseType` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AbuseType {
    /// id property.
    pub id: Option<String>,
}

/// `ActivitySnippet` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ActivitySnippet {
    /// channelId property.
    pub channel_id: Option<String>,
    /// channelTitle property.
    pub channel_title: Option<String>,
    /// description property.
    pub description: Option<String>,
    /// groupId property.
    pub group_id: Option<String>,
    /// publishedAt property.
    pub published_at: Option<String>,
    /// thumbnails property.
    pub thumbnails: Option<ThumbnailDetails>,
    /// title property.
    pub title: Option<String>,
    /// type property.
    pub r#type: Option<String>,
}

/// `VideoTrainability` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct VideoTrainability {
    /// etag property.
    pub etag: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// permitted property.
    pub permitted: Option<Vec<String>>,
    /// videoId property.
    pub video_id: Option<String>,
}

/// `LiveChatBan` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct LiveChatBan {
    /// etag property.
    pub etag: Option<String>,
    /// id property.
    pub id: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// snippet property.
    pub snippet: Option<LiveChatBanSnippet>,
}

/// `LiveStreamListResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct LiveStreamListResponse {
    /// etag property.
    pub etag: Option<String>,
    /// eventId property.
    pub event_id: Option<String>,
    /// items property.
    pub items: Option<Vec<LiveStream>>,
    /// kind property.
    pub kind: Option<String>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// pageInfo property.
    pub page_info: Option<PageInfo>,
    /// prevPageToken property.
    pub prev_page_token: Option<String>,
    /// tokenPagination property.
    pub token_pagination: Option<TokenPagination>,
    /// visitorId property.
    pub visitor_id: Option<String>,
}

/// `PlaylistItemSnippet` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct PlaylistItemSnippet {
    /// channelId property.
    pub channel_id: Option<String>,
    /// channelTitle property.
    pub channel_title: Option<String>,
    /// description property.
    pub description: Option<String>,
    /// playlistId property.
    pub playlist_id: Option<String>,
    /// position property.
    pub position: Option<i64>,
    /// publishedAt property.
    pub published_at: Option<String>,
    /// resourceId property.
    pub resource_id: Option<ResourceId>,
    /// thumbnails property.
    pub thumbnails: Option<ThumbnailDetails>,
    /// title property.
    pub title: Option<String>,
    /// videoOwnerChannelId property.
    pub video_owner_channel_id: Option<String>,
    /// videoOwnerChannelTitle property.
    pub video_owner_channel_title: Option<String>,
}

/// `VideoAgeGating` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct VideoAgeGating {
    /// alcoholContent property.
    pub alcohol_content: Option<bool>,
    /// restricted property.
    pub restricted: Option<bool>,
    /// videoGameRating property.
    pub video_game_rating: Option<String>,
}

/// `VideoLiveStreamingDetails` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct VideoLiveStreamingDetails {
    /// activeLiveChatId property.
    pub active_live_chat_id: Option<String>,
    /// actualEndTime property.
    pub actual_end_time: Option<String>,
    /// actualStartTime property.
    pub actual_start_time: Option<String>,
    /// concurrentViewers property.
    pub concurrent_viewers: Option<String>,
    /// scheduledEndTime property.
    pub scheduled_end_time: Option<String>,
    /// scheduledStartTime property.
    pub scheduled_start_time: Option<String>,
}

/// `VideoRecordingDetails` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct VideoRecordingDetails {
    /// location property.
    pub location: Option<GeoPoint>,
    /// locationDescription property.
    pub location_description: Option<String>,
    /// recordingDate property.
    pub recording_date: Option<String>,
}

/// `VideoSuggestionsTagSuggestion` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct VideoSuggestionsTagSuggestion {
    /// categoryRestricts property.
    pub category_restricts: Option<Vec<String>>,
    /// tag property.
    pub tag: Option<String>,
}

/// `LiveChatNewSponsorDetails` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct LiveChatNewSponsorDetails {
    /// isUpgrade property.
    pub is_upgrade: Option<bool>,
    /// memberLevelName property.
    pub member_level_name: Option<String>,
}

/// `LocalizedProperty` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct LocalizedProperty {
    /// default property.
    pub default: Option<String>,
    /// defaultLanguage property.
    pub default_language: Option<LanguageTag>,
    /// localized property.
    pub localized: Option<Vec<LocalizedString>>,
}

/// `I18NRegionSnippet` response type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct I18NRegionSnippet {
    /// Raw JSON value - full schema generated from `OpenAPI`
    #[serde(flatten)]
    pub data: std::collections::HashMap<String, serde_json::Value>,
}

/// `ActivityListResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ActivityListResponse {
    /// etag property.
    pub etag: Option<String>,
    /// eventId property.
    pub event_id: Option<String>,
    /// items property.
    pub items: Option<Vec<Activity>>,
    /// kind property.
    pub kind: Option<String>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// pageInfo property.
    pub page_info: Option<PageInfo>,
    /// prevPageToken property.
    pub prev_page_token: Option<String>,
    /// tokenPagination property.
    pub token_pagination: Option<TokenPagination>,
    /// visitorId property.
    pub visitor_id: Option<String>,
}

/// `LiveBroadcastListResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct LiveBroadcastListResponse {
    /// etag property.
    pub etag: Option<String>,
    /// eventId property.
    pub event_id: Option<String>,
    /// items property.
    pub items: Option<Vec<LiveBroadcast>>,
    /// kind property.
    pub kind: Option<String>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// pageInfo property.
    pub page_info: Option<PageInfo>,
    /// prevPageToken property.
    pub prev_page_token: Option<String>,
    /// tokenPagination property.
    pub token_pagination: Option<TokenPagination>,
    /// visitorId property.
    pub visitor_id: Option<String>,
}

/// `SubscriptionSnippet` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SubscriptionSnippet {
    /// channelId property.
    pub channel_id: Option<String>,
    /// description property.
    pub description: Option<String>,
    /// publishedAt property.
    pub published_at: Option<String>,
    /// resourceId property.
    pub resource_id: Option<ResourceId>,
    /// thumbnails property.
    pub thumbnails: Option<ThumbnailDetails>,
    /// title property.
    pub title: Option<String>,
}

/// `CommentSnippetAuthorChannelId` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct CommentSnippetAuthorChannelId {
    /// value property.
    pub value: Option<String>,
}

/// `ChannelBrandingSettings` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ChannelBrandingSettings {
    /// channel property.
    pub channel: Option<ChannelSettings>,
    /// hints property.
    pub hints: Option<Vec<PropertyValue>>,
    /// image property.
    pub image: Option<ImageSettings>,
    /// watch property.
    pub watch: Option<WatchSettings>,
}

/// `LiveChatMessageRetractedDetails` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct LiveChatMessageRetractedDetails {
    /// retractedMessageId property.
    pub retracted_message_id: Option<String>,
}

/// `Activity` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Activity {
    /// contentDetails property.
    pub content_details: Option<ActivityContentDetails>,
    /// etag property.
    pub etag: Option<String>,
    /// id property.
    pub id: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// snippet property.
    pub snippet: Option<ActivitySnippet>,
}

/// `ThirdPartyLinkSnippet` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ThirdPartyLinkSnippet {
    /// channelToStoreLink property.
    pub channel_to_store_link: Option<ChannelToStoreLinkDetails>,
    /// type property.
    pub r#type: Option<String>,
}

/// `PlaylistSnippet` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct PlaylistSnippet {
    /// channelId property.
    pub channel_id: Option<String>,
    /// channelTitle property.
    pub channel_title: Option<String>,
    /// defaultLanguage property.
    pub default_language: Option<String>,
    /// description property.
    pub description: Option<String>,
    /// localized property.
    pub localized: Option<PlaylistLocalization>,
    /// publishedAt property.
    pub published_at: Option<String>,
    /// tags property.
    pub tags: Option<Vec<String>>,
    /// thumbnailVideoId property.
    pub thumbnail_video_id: Option<String>,
    /// thumbnails property.
    pub thumbnails: Option<ThumbnailDetails>,
    /// title property.
    pub title: Option<String>,
}

/// `RelatedEntity` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct RelatedEntity {
    /// entity property.
    pub entity: Option<Entity>,
}

/// `Playlist` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Playlist {
    /// contentDetails property.
    pub content_details: Option<PlaylistContentDetails>,
    /// etag property.
    pub etag: Option<String>,
    /// id property.
    pub id: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// localizations property.
    pub localizations: Option<serde_json::Value>,
    /// player property.
    pub player: Option<PlaylistPlayer>,
    /// snippet property.
    pub snippet: Option<PlaylistSnippet>,
    /// status property.
    pub status: Option<PlaylistStatus>,
}

/// `ActivityContentDetails` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ActivityContentDetails {
    /// bulletin property.
    pub bulletin: Option<ActivityContentDetailsBulletin>,
    /// channelItem property.
    pub channel_item: Option<ActivityContentDetailsChannelItem>,
    /// comment property.
    pub comment: Option<ActivityContentDetailsComment>,
    /// favorite property.
    pub favorite: Option<ActivityContentDetailsFavorite>,
    /// like property.
    pub like: Option<ActivityContentDetailsLike>,
    /// playlistItem property.
    pub playlist_item: Option<ActivityContentDetailsPlaylistItem>,
    /// promotedItem property.
    pub promoted_item: Option<ActivityContentDetailsPromotedItem>,
    /// recommendation property.
    pub recommendation: Option<ActivityContentDetailsRecommendation>,
    /// social property.
    pub social: Option<ActivityContentDetailsSocial>,
    /// subscription property.
    pub subscription: Option<ActivityContentDetailsSubscription>,
    /// upload property.
    pub upload: Option<ActivityContentDetailsUpload>,
}

/// `ChannelSectionListResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ChannelSectionListResponse {
    /// etag property.
    pub etag: Option<String>,
    /// eventId property.
    pub event_id: Option<String>,
    /// items property.
    pub items: Option<Vec<ChannelSection>>,
    /// kind property.
    pub kind: Option<String>,
    /// visitorId property.
    pub visitor_id: Option<String>,
}

/// `VideoListResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct VideoListResponse {
    /// etag property.
    pub etag: Option<String>,
    /// eventId property.
    pub event_id: Option<String>,
    /// items property.
    pub items: Option<Vec<Video>>,
    /// kind property.
    pub kind: Option<String>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// pageInfo property.
    pub page_info: Option<PageInfo>,
    /// prevPageToken property.
    pub prev_page_token: Option<String>,
    /// tokenPagination property.
    pub token_pagination: Option<TokenPagination>,
    /// visitorId property.
    pub visitor_id: Option<String>,
}

/// `VideoLocalization` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct VideoLocalization {
    /// description property.
    pub description: Option<String>,
    /// title property.
    pub title: Option<String>,
}

/// `ChannelStatus` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ChannelStatus {
    /// isChannelMonetizationEnabled property.
    pub is_channel_monetization_enabled: Option<bool>,
    /// isLinked property.
    pub is_linked: Option<bool>,
    /// longUploadsStatus property.
    pub long_uploads_status: Option<String>,
    /// madeForKids property.
    pub made_for_kids: Option<bool>,
    /// privacyStatus property.
    pub privacy_status: Option<String>,
    /// selfDeclaredMadeForKids property.
    pub self_declared_made_for_kids: Option<bool>,
}

/// `TestItemTestItemSnippet` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct TestItemTestItemSnippet {}

/// `ChannelLocalization` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ChannelLocalization {
    /// description property.
    pub description: Option<String>,
    /// title property.
    pub title: Option<String>,
}

/// `InvideoPosition` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct InvideoPosition {
    /// cornerPosition property.
    pub corner_position: Option<String>,
    /// type property.
    pub r#type: Option<String>,
}

/// `ActivityContentDetailsSocial` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ActivityContentDetailsSocial {
    /// author property.
    pub author: Option<String>,
    /// imageUrl property.
    pub image_url: Option<String>,
    /// referenceUrl property.
    pub reference_url: Option<String>,
    /// resourceId property.
    pub resource_id: Option<ResourceId>,
    /// type property.
    pub r#type: Option<String>,
}

/// `SearchResult` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SearchResult {
    /// etag property.
    pub etag: Option<String>,
    /// id property.
    pub id: Option<ResourceId>,
    /// kind property.
    pub kind: Option<String>,
    /// snippet property.
    pub snippet: Option<SearchResultSnippet>,
}

/// `TestItem` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct TestItem {
    /// etag property.
    pub etag: Option<String>,
    /// featuredPart property.
    pub featured_part: Option<bool>,
    /// gaia property.
    pub gaia: Option<String>,
    /// id property.
    pub id: Option<String>,
    /// snippet property.
    pub snippet: Option<TestItemTestItemSnippet>,
}

/// `LiveStreamSnippet` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct LiveStreamSnippet {
    /// channelId property.
    pub channel_id: Option<String>,
    /// description property.
    pub description: Option<String>,
    /// isDefaultStream property.
    pub is_default_stream: Option<bool>,
    /// publishedAt property.
    pub published_at: Option<String>,
    /// title property.
    pub title: Option<String>,
}

/// `LiveChatMembershipGiftingDetails` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct LiveChatMembershipGiftingDetails {
    /// giftMembershipsCount property.
    pub gift_memberships_count: Option<i64>,
    /// giftMembershipsLevelName property.
    pub gift_memberships_level_name: Option<String>,
}

/// `VideoCategoryListResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct VideoCategoryListResponse {
    /// etag property.
    pub etag: Option<String>,
    /// eventId property.
    pub event_id: Option<String>,
    /// items property.
    pub items: Option<Vec<VideoCategory>>,
    /// kind property.
    pub kind: Option<String>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// pageInfo property.
    pub page_info: Option<PageInfo>,
    /// prevPageToken property.
    pub prev_page_token: Option<String>,
    /// tokenPagination property.
    pub token_pagination: Option<TokenPagination>,
    /// visitorId property.
    pub visitor_id: Option<String>,
}

/// `Channel` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Channel {
    /// auditDetails property.
    pub audit_details: Option<ChannelAuditDetails>,
    /// brandingSettings property.
    pub branding_settings: Option<ChannelBrandingSettings>,
    /// contentDetails property.
    pub content_details: Option<ChannelContentDetails>,
    /// contentOwnerDetails property.
    pub content_owner_details: Option<ChannelContentOwnerDetails>,
    /// conversionPings property.
    pub conversion_pings: Option<ChannelConversionPings>,
    /// etag property.
    pub etag: Option<String>,
    /// id property.
    pub id: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// localizations property.
    pub localizations: Option<serde_json::Value>,
    /// snippet property.
    pub snippet: Option<ChannelSnippet>,
    /// statistics property.
    pub statistics: Option<ChannelStatistics>,
    /// status property.
    pub status: Option<ChannelStatus>,
    /// topicDetails property.
    pub topic_details: Option<ChannelTopicDetails>,
}

/// `CommentListResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct CommentListResponse {
    /// etag property.
    pub etag: Option<String>,
    /// eventId property.
    pub event_id: Option<String>,
    /// items property.
    pub items: Option<Vec<Comment>>,
    /// kind property.
    pub kind: Option<String>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// pageInfo property.
    pub page_info: Option<PageInfo>,
    /// tokenPagination property.
    pub token_pagination: Option<TokenPagination>,
    /// visitorId property.
    pub visitor_id: Option<String>,
}

/// `CdnSettings` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct CdnSettings {
    /// format property.
    pub format: Option<String>,
    /// frameRate property.
    pub frame_rate: Option<String>,
    /// ingestionInfo property.
    pub ingestion_info: Option<IngestionInfo>,
    /// ingestionType property.
    pub ingestion_type: Option<String>,
    /// resolution property.
    pub resolution: Option<String>,
}

/// `SearchListResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SearchListResponse {
    /// etag property.
    pub etag: Option<String>,
    /// eventId property.
    pub event_id: Option<String>,
    /// items property.
    pub items: Option<Vec<SearchResult>>,
    /// kind property.
    pub kind: Option<String>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// pageInfo property.
    pub page_info: Option<PageInfo>,
    /// prevPageToken property.
    pub prev_page_token: Option<String>,
    /// regionCode property.
    pub region_code: Option<String>,
    /// tokenPagination property.
    pub token_pagination: Option<TokenPagination>,
    /// visitorId property.
    pub visitor_id: Option<String>,
}

/// `ActivityContentDetailsRecommendation` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ActivityContentDetailsRecommendation {
    /// reason property.
    pub reason: Option<String>,
    /// resourceId property.
    pub resource_id: Option<ResourceId>,
    /// seedResourceId property.
    pub seed_resource_id: Option<ResourceId>,
}

/// `VideoSnippet` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct VideoSnippet {
    /// categoryId property.
    pub category_id: Option<String>,
    /// channelId property.
    pub channel_id: Option<String>,
    /// channelTitle property.
    pub channel_title: Option<String>,
    /// defaultAudioLanguage property.
    pub default_audio_language: Option<String>,
    /// defaultLanguage property.
    pub default_language: Option<String>,
    /// description property.
    pub description: Option<String>,
    /// liveBroadcastContent property.
    pub live_broadcast_content: Option<String>,
    /// localized property.
    pub localized: Option<VideoLocalization>,
    /// publishedAt property.
    pub published_at: Option<String>,
    /// tags property.
    pub tags: Option<Vec<String>>,
    /// thumbnails property.
    pub thumbnails: Option<ThumbnailDetails>,
    /// title property.
    pub title: Option<String>,
}

/// `LiveChatModeratorSnippet` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct LiveChatModeratorSnippet {
    /// liveChatId property.
    pub live_chat_id: Option<String>,
    /// moderatorDetails property.
    pub moderator_details: Option<ChannelProfileDetails>,
}

/// `VideoAbuseReportReasonSnippet` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct VideoAbuseReportReasonSnippet {
    /// label property.
    pub label: Option<String>,
    /// secondaryReasons property.
    pub secondary_reasons: Option<Vec<VideoAbuseReportSecondaryReason>>,
}

/// `VideoGetRatingResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct VideoGetRatingResponse {
    /// etag property.
    pub etag: Option<String>,
    /// eventId property.
    pub event_id: Option<String>,
    /// items property.
    pub items: Option<Vec<VideoRating>>,
    /// kind property.
    pub kind: Option<String>,
    /// visitorId property.
    pub visitor_id: Option<String>,
}

/// `PageInfo` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct PageInfo {
    /// resultsPerPage property.
    pub results_per_page: Option<i64>,
    /// totalResults property.
    pub total_results: Option<i64>,
}

/// `LiveChatModeratorListResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct LiveChatModeratorListResponse {
    /// etag property.
    pub etag: Option<String>,
    /// eventId property.
    pub event_id: Option<String>,
    /// items property.
    pub items: Option<Vec<LiveChatModerator>>,
    /// kind property.
    pub kind: Option<String>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// pageInfo property.
    pub page_info: Option<PageInfo>,
    /// prevPageToken property.
    pub prev_page_token: Option<String>,
    /// tokenPagination property.
    pub token_pagination: Option<TokenPagination>,
    /// visitorId property.
    pub visitor_id: Option<String>,
}

/// `Caption` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Caption {
    /// etag property.
    pub etag: Option<String>,
    /// id property.
    pub id: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// snippet property.
    pub snippet: Option<CaptionSnippet>,
}

/// `ActivityContentDetailsChannelItem` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ActivityContentDetailsChannelItem {
    /// resourceId property.
    pub resource_id: Option<ResourceId>,
}

/// `MembershipsDurationAtLevel` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct MembershipsDurationAtLevel {
    /// level property.
    pub level: Option<String>,
    /// memberSince property.
    pub member_since: Option<String>,
    /// memberTotalDurationMonths property.
    pub member_total_duration_months: Option<i64>,
}

/// `PlaylistImageListResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct PlaylistImageListResponse {
    /// items property.
    pub items: Option<Vec<PlaylistImage>>,
    /// kind property.
    pub kind: Option<String>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// pageInfo property.
    pub page_info: Option<PageInfo>,
    /// prevPageToken property.
    pub prev_page_token: Option<String>,
}

/// `VideoTopicDetails` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct VideoTopicDetails {
    /// relevantTopicIds property.
    pub relevant_topic_ids: Option<Vec<String>>,
    /// topicCategories property.
    pub topic_categories: Option<Vec<String>>,
    /// topicIds property.
    pub topic_ids: Option<Vec<String>>,
}

/// `ThirdPartyLinkStatus` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ThirdPartyLinkStatus {
    /// linkStatus property.
    pub link_status: Option<String>,
}

/// `VideoAbuseReportSecondaryReason` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct VideoAbuseReportSecondaryReason {
    /// id property.
    pub id: Option<String>,
    /// label property.
    pub label: Option<String>,
}

/// `VideoContentDetailsRegionRestriction` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct VideoContentDetailsRegionRestriction {
    /// allowed property.
    pub allowed: Option<Vec<String>>,
    /// blocked property.
    pub blocked: Option<Vec<String>>,
}

/// `ActivityContentDetailsComment` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ActivityContentDetailsComment {
    /// resourceId property.
    pub resource_id: Option<ResourceId>,
}

/// `MemberSnippet` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct MemberSnippet {
    /// creatorChannelId property.
    pub creator_channel_id: Option<String>,
    /// memberDetails property.
    pub member_details: Option<ChannelProfileDetails>,
    /// membershipsDetails property.
    pub memberships_details: Option<MembershipsDetails>,
}

/// `VideoFileDetails` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct VideoFileDetails {
    /// audioStreams property.
    pub audio_streams: Option<Vec<VideoFileDetailsAudioStream>>,
    /// bitrateBps property.
    pub bitrate_bps: Option<String>,
    /// container property.
    pub container: Option<String>,
    /// creationTime property.
    pub creation_time: Option<String>,
    /// durationMs property.
    pub duration_ms: Option<String>,
    /// fileName property.
    pub file_name: Option<String>,
    /// fileSize property.
    pub file_size: Option<String>,
    /// fileType property.
    pub file_type: Option<String>,
    /// videoStreams property.
    pub video_streams: Option<Vec<VideoFileDetailsVideoStream>>,
}

/// `ChannelToStoreLinkDetails` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ChannelToStoreLinkDetails {
    /// billingDetails property.
    pub billing_details: Option<ChannelToStoreLinkDetailsBillingDetails>,
    /// merchantAffiliateProgramDetails property.
    pub merchant_affiliate_program_details:
        Option<ChannelToStoreLinkDetailsMerchantAffiliateProgramDetails>,
    /// merchantId property.
    pub merchant_id: Option<String>,
    /// storeName property.
    pub store_name: Option<String>,
    /// storeUrl property.
    pub store_url: Option<String>,
}

/// `LiveChatSuperStickerDetails` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct LiveChatSuperStickerDetails {
    /// amountDisplayString property.
    pub amount_display_string: Option<String>,
    /// amountMicros property.
    pub amount_micros: Option<String>,
    /// currency property.
    pub currency: Option<String>,
    /// superStickerMetadata property.
    pub super_sticker_metadata: Option<SuperStickerMetadata>,
    /// tier property.
    pub tier: Option<i64>,
}

/// `CuepointSchedule` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct CuepointSchedule {
    /// enabled property.
    pub enabled: Option<bool>,
    /// pauseAdsUntil property.
    pub pause_ads_until: Option<String>,
    /// repeatIntervalSecs property.
    pub repeat_interval_secs: Option<i64>,
    /// scheduleStrategy property.
    pub schedule_strategy: Option<String>,
}

/// `ChannelConversionPing` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ChannelConversionPing {
    /// context property.
    pub context: Option<String>,
    /// conversionUrl property.
    pub conversion_url: Option<String>,
}

/// `PlaylistContentDetails` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct PlaylistContentDetails {
    /// itemCount property.
    pub item_count: Option<i64>,
}

/// `LanguageTag` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct LanguageTag {
    /// value property.
    pub value: Option<String>,
}

/// `LiveStream` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct LiveStream {
    /// cdn property.
    pub cdn: Option<CdnSettings>,
    /// contentDetails property.
    pub content_details: Option<LiveStreamContentDetails>,
    /// etag property.
    pub etag: Option<String>,
    /// id property.
    pub id: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// snippet property.
    pub snippet: Option<LiveStreamSnippet>,
    /// status property.
    pub status: Option<LiveStreamStatus>,
}

/// `AbuseReport` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AbuseReport {
    /// abuseTypes property.
    pub abuse_types: Option<Vec<AbuseType>>,
    /// description property.
    pub description: Option<String>,
    /// relatedEntities property.
    pub related_entities: Option<Vec<RelatedEntity>>,
    /// subject property.
    pub subject: Option<Entity>,
}

/// `MembershipsDetails` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct MembershipsDetails {
    /// accessibleLevels property.
    pub accessible_levels: Option<Vec<String>>,
    /// highestAccessibleLevel property.
    pub highest_accessible_level: Option<String>,
    /// highestAccessibleLevelDisplayName property.
    pub highest_accessible_level_display_name: Option<String>,
    /// membershipsDuration property.
    pub memberships_duration: Option<MembershipsDuration>,
    /// membershipsDurationAtLevels property.
    pub memberships_duration_at_levels: Option<Vec<MembershipsDurationAtLevel>>,
}

/// `I18nRegionListResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct I18nRegionListResponse {
    /// etag property.
    pub etag: Option<String>,
    /// eventId property.
    pub event_id: Option<String>,
    /// items property.
    pub items: Option<Vec<I18NRegion>>,
    /// kind property.
    pub kind: Option<String>,
    /// visitorId property.
    pub visitor_id: Option<String>,
}

/// `ActivityContentDetailsSubscription` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ActivityContentDetailsSubscription {
    /// resourceId property.
    pub resource_id: Option<ResourceId>,
}

/// `ResourceId` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ResourceId {
    /// channelId property.
    pub channel_id: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// playlistId property.
    pub playlist_id: Option<String>,
    /// videoId property.
    pub video_id: Option<String>,
}

/// `VideoFileDetailsVideoStream` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct VideoFileDetailsVideoStream {
    /// aspectRatio property.
    pub aspect_ratio: Option<f64>,
    /// bitrateBps property.
    pub bitrate_bps: Option<String>,
    /// codec property.
    pub codec: Option<String>,
    /// frameRateFps property.
    pub frame_rate_fps: Option<f64>,
    /// heightPixels property.
    pub height_pixels: Option<i64>,
    /// rotation property.
    pub rotation: Option<String>,
    /// vendor property.
    pub vendor: Option<String>,
    /// widthPixels property.
    pub width_pixels: Option<i64>,
}

/// `LiveChatPollDetails` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct LiveChatPollDetails {
    /// metadata property.
    pub metadata: Option<LiveChatPollDetailsPollMetadata>,
    /// status property.
    pub status: Option<String>,
}

/// `CaptionSnippet` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct CaptionSnippet {
    /// audioTrackType property.
    pub audio_track_type: Option<String>,
    /// failureReason property.
    pub failure_reason: Option<String>,
    /// isAutoSynced property.
    pub is_auto_synced: Option<bool>,
    /// isCC property.
    pub is_cc: Option<bool>,
    /// isDraft property.
    pub is_draft: Option<bool>,
    /// isEasyReader property.
    pub is_easy_reader: Option<bool>,
    /// isLarge property.
    pub is_large: Option<bool>,
    /// language property.
    pub language: Option<String>,
    /// lastUpdated property.
    pub last_updated: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// status property.
    pub status: Option<String>,
    /// trackKind property.
    pub track_kind: Option<String>,
    /// videoId property.
    pub video_id: Option<String>,
}

/// `LiveChatUserBannedMessageDetails` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct LiveChatUserBannedMessageDetails {
    /// banDurationSeconds property.
    pub ban_duration_seconds: Option<String>,
    /// banType property.
    pub ban_type: Option<String>,
    /// bannedUserDetails property.
    pub banned_user_details: Option<ChannelProfileDetails>,
}

/// `VideoFileDetailsAudioStream` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct VideoFileDetailsAudioStream {
    /// bitrateBps property.
    pub bitrate_bps: Option<String>,
    /// channelCount property.
    pub channel_count: Option<i64>,
    /// codec property.
    pub codec: Option<String>,
    /// vendor property.
    pub vendor: Option<String>,
}

/// `IngestionInfo` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct IngestionInfo {
    /// backupIngestionAddress property.
    pub backup_ingestion_address: Option<String>,
    /// ingestionAddress property.
    pub ingestion_address: Option<String>,
    /// rtmpsBackupIngestionAddress property.
    pub rtmps_backup_ingestion_address: Option<String>,
    /// rtmpsIngestionAddress property.
    pub rtmps_ingestion_address: Option<String>,
    /// streamName property.
    pub stream_name: Option<String>,
}

/// `Member` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Member {
    /// etag property.
    pub etag: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// snippet property.
    pub snippet: Option<MemberSnippet>,
}

/// `PlaylistImageSnippet` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct PlaylistImageSnippet {
    /// height property.
    pub height: Option<i64>,
    /// playlistId property.
    pub playlist_id: Option<String>,
    /// type property.
    pub r#type: Option<String>,
    /// width property.
    pub width: Option<i64>,
}

/// `ChannelContentOwnerDetails` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ChannelContentOwnerDetails {
    /// contentOwner property.
    pub content_owner: Option<String>,
    /// timeLinked property.
    pub time_linked: Option<String>,
}

/// `Cuepoint` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Cuepoint {
    /// cueType property.
    pub cue_type: Option<String>,
    /// durationSecs property.
    pub duration_secs: Option<i64>,
    /// etag property.
    pub etag: Option<String>,
    /// id property.
    pub id: Option<String>,
    /// insertionOffsetTimeMs property.
    pub insertion_offset_time_ms: Option<String>,
    /// walltimeMs property.
    pub walltime_ms: Option<String>,
}

/// `SuperChatEventListResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SuperChatEventListResponse {
    /// etag property.
    pub etag: Option<String>,
    /// eventId property.
    pub event_id: Option<String>,
    /// items property.
    pub items: Option<Vec<SuperChatEvent>>,
    /// kind property.
    pub kind: Option<String>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// pageInfo property.
    pub page_info: Option<PageInfo>,
    /// tokenPagination property.
    pub token_pagination: Option<TokenPagination>,
    /// visitorId property.
    pub visitor_id: Option<String>,
}

/// `ChannelSectionSnippet` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ChannelSectionSnippet {
    /// channelId property.
    pub channel_id: Option<String>,
    /// defaultLanguage property.
    pub default_language: Option<String>,
    /// localized property.
    pub localized: Option<ChannelSectionLocalization>,
    /// position property.
    pub position: Option<i64>,
    /// style property.
    pub style: Option<String>,
    /// title property.
    pub title: Option<String>,
    /// type property.
    pub r#type: Option<String>,
}

/// `Thumbnail` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Thumbnail {
    /// height property.
    pub height: Option<i64>,
    /// url property.
    pub url: Option<String>,
    /// width property.
    pub width: Option<i64>,
}

/// `ActivityContentDetailsFavorite` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ActivityContentDetailsFavorite {
    /// resourceId property.
    pub resource_id: Option<ResourceId>,
}

/// `LiveChatMessage` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct LiveChatMessage {
    /// authorDetails property.
    pub author_details: Option<LiveChatMessageAuthorDetails>,
    /// etag property.
    pub etag: Option<String>,
    /// id property.
    pub id: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// snippet property.
    pub snippet: Option<LiveChatMessageSnippet>,
}

/// `LiveChatPollDetailsPollMetadata` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct LiveChatPollDetailsPollMetadata {
    /// options property.
    pub options: Option<Vec<LiveChatPollDetailsPollMetadataPollOption>>,
    /// questionText property.
    pub question_text: Option<String>,
}

/// `AccessPolicy` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AccessPolicy {
    /// allowed property.
    pub allowed: Option<bool>,
    /// exception property.
    pub exception: Option<Vec<String>>,
}

/// `LiveStreamHealthStatus` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct LiveStreamHealthStatus {
    /// configurationIssues property.
    pub configuration_issues: Option<Vec<LiveStreamConfigurationIssue>>,
    /// lastUpdateTimeSeconds property.
    pub last_update_time_seconds: Option<String>,
    /// status property.
    pub status: Option<String>,
}

/// `VideoProcessingDetails` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct VideoProcessingDetails {
    /// editorSuggestionsAvailability property.
    pub editor_suggestions_availability: Option<String>,
    /// fileDetailsAvailability property.
    pub file_details_availability: Option<String>,
    /// processingFailureReason property.
    pub processing_failure_reason: Option<String>,
    /// processingIssuesAvailability property.
    pub processing_issues_availability: Option<String>,
    /// processingProgress property.
    pub processing_progress: Option<VideoProcessingDetailsProcessingProgress>,
    /// processingStatus property.
    pub processing_status: Option<String>,
    /// tagSuggestionsAvailability property.
    pub tag_suggestions_availability: Option<String>,
    /// thumbnailsAvailability property.
    pub thumbnails_availability: Option<String>,
}

/// `Video` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Video {
    /// ageGating property.
    pub age_gating: Option<VideoAgeGating>,
    /// contentDetails property.
    pub content_details: Option<VideoContentDetails>,
    /// etag property.
    pub etag: Option<String>,
    /// fileDetails property.
    pub file_details: Option<VideoFileDetails>,
    /// id property.
    pub id: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// liveStreamingDetails property.
    pub live_streaming_details: Option<VideoLiveStreamingDetails>,
    /// localizations property.
    pub localizations: Option<serde_json::Value>,
    /// monetizationDetails property.
    pub monetization_details: Option<VideoMonetizationDetails>,
    /// paidProductPlacementDetails property.
    pub paid_product_placement_details: Option<VideoPaidProductPlacementDetails>,
    /// player property.
    pub player: Option<VideoPlayer>,
    /// processingDetails property.
    pub processing_details: Option<VideoProcessingDetails>,
    /// projectDetails property.
    pub project_details: Option<VideoProjectDetails>,
    /// recordingDetails property.
    pub recording_details: Option<VideoRecordingDetails>,
    /// snippet property.
    pub snippet: Option<VideoSnippet>,
    /// statistics property.
    pub statistics: Option<VideoStatistics>,
    /// status property.
    pub status: Option<VideoStatus>,
    /// suggestions property.
    pub suggestions: Option<VideoSuggestions>,
    /// topicDetails property.
    pub topic_details: Option<VideoTopicDetails>,
}

/// `LiveChatFanFundingEventDetails` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct LiveChatFanFundingEventDetails {
    /// amountDisplayString property.
    pub amount_display_string: Option<String>,
    /// amountMicros property.
    pub amount_micros: Option<String>,
    /// currency property.
    pub currency: Option<String>,
    /// userComment property.
    pub user_comment: Option<String>,
}

/// `InvideoBranding` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct InvideoBranding {
    /// imageBytes property.
    pub image_bytes: Option<String>,
    /// imageUrl property.
    pub image_url: Option<String>,
    /// position property.
    pub position: Option<InvideoPosition>,
    /// targetChannelId property.
    pub target_channel_id: Option<String>,
    /// timing property.
    pub timing: Option<InvideoTiming>,
}

/// `ChannelTopicDetails` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ChannelTopicDetails {
    /// topicCategories property.
    pub topic_categories: Option<Vec<String>>,
    /// topicIds property.
    pub topic_ids: Option<Vec<String>>,
}

/// `SuperChatEventSnippet` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SuperChatEventSnippet {
    /// amountMicros property.
    pub amount_micros: Option<String>,
    /// channelId property.
    pub channel_id: Option<String>,
    /// commentText property.
    pub comment_text: Option<String>,
    /// createdAt property.
    pub created_at: Option<String>,
    /// currency property.
    pub currency: Option<String>,
    /// displayString property.
    pub display_string: Option<String>,
    /// isSuperStickerEvent property.
    pub is_super_sticker_event: Option<bool>,
    /// messageType property.
    pub message_type: Option<i64>,
    /// superStickerMetadata property.
    pub super_sticker_metadata: Option<SuperStickerMetadata>,
    /// supporterDetails property.
    pub supporter_details: Option<ChannelProfileDetails>,
}

/// `ChannelSectionContentDetails` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ChannelSectionContentDetails {
    /// channels property.
    pub channels: Option<Vec<String>>,
    /// playlists property.
    pub playlists: Option<Vec<String>>,
}

/// `VideoRating` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct VideoRating {
    /// rating property.
    pub rating: Option<String>,
    /// videoId property.
    pub video_id: Option<String>,
}

/// `ChannelSnippet` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ChannelSnippet {
    /// country property.
    pub country: Option<String>,
    /// customUrl property.
    pub custom_url: Option<String>,
    /// defaultLanguage property.
    pub default_language: Option<String>,
    /// description property.
    pub description: Option<String>,
    /// localized property.
    pub localized: Option<ChannelLocalization>,
    /// publishedAt property.
    pub published_at: Option<String>,
    /// thumbnails property.
    pub thumbnails: Option<ThumbnailDetails>,
    /// title property.
    pub title: Option<String>,
}

/// `PlaylistStatus` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct PlaylistStatus {
    /// podcastStatus property.
    pub podcast_status: Option<String>,
    /// privacyStatus property.
    pub privacy_status: Option<String>,
}

/// `VideoAbuseReportReason` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct VideoAbuseReportReason {
    /// etag property.
    pub etag: Option<String>,
    /// id property.
    pub id: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// snippet property.
    pub snippet: Option<VideoAbuseReportReasonSnippet>,
}

/// `CommentThread` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct CommentThread {
    /// etag property.
    pub etag: Option<String>,
    /// id property.
    pub id: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// replies property.
    pub replies: Option<CommentThreadReplies>,
    /// snippet property.
    pub snippet: Option<CommentThreadSnippet>,
}

/// `LiveChatGiftMembershipReceivedDetails` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct LiveChatGiftMembershipReceivedDetails {
    /// associatedMembershipGiftingMessageId property.
    pub associated_membership_gifting_message_id: Option<String>,
    /// gifterChannelId property.
    pub gifter_channel_id: Option<String>,
    /// memberLevelName property.
    pub member_level_name: Option<String>,
}

/// `ChannelBannerResource` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ChannelBannerResource {
    /// etag property.
    pub etag: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// url property.
    pub url: Option<String>,
}

/// `Subscription` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Subscription {
    /// contentDetails property.
    pub content_details: Option<SubscriptionContentDetails>,
    /// etag property.
    pub etag: Option<String>,
    /// id property.
    pub id: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// snippet property.
    pub snippet: Option<SubscriptionSnippet>,
    /// subscriberSnippet property.
    pub subscriber_snippet: Option<SubscriptionSubscriberSnippet>,
}

/// `LiveChatBanSnippet` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct LiveChatBanSnippet {
    /// banDurationSeconds property.
    pub ban_duration_seconds: Option<String>,
    /// bannedUserDetails property.
    pub banned_user_details: Option<ChannelProfileDetails>,
    /// liveChatId property.
    pub live_chat_id: Option<String>,
    /// type property.
    pub r#type: Option<String>,
}

/// `ChannelAuditDetails` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ChannelAuditDetails {
    /// communityGuidelinesGoodStanding property.
    pub community_guidelines_good_standing: Option<bool>,
    /// contentIdClaimsGoodStanding property.
    pub content_id_claims_good_standing: Option<bool>,
    /// copyrightStrikesGoodStanding property.
    pub copyright_strikes_good_standing: Option<bool>,
}

/// `CommentSnippet` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct CommentSnippet {
    /// authorChannelId property.
    pub author_channel_id: Option<CommentSnippetAuthorChannelId>,
    /// authorChannelUrl property.
    pub author_channel_url: Option<String>,
    /// authorDisplayName property.
    pub author_display_name: Option<String>,
    /// authorProfileImageUrl property.
    pub author_profile_image_url: Option<String>,
    /// canRate property.
    pub can_rate: Option<bool>,
    /// channelId property.
    pub channel_id: Option<String>,
    /// likeCount property.
    pub like_count: Option<i64>,
    /// moderationStatus property.
    pub moderation_status: Option<String>,
    /// parentId property.
    pub parent_id: Option<String>,
    /// postId property.
    pub post_id: Option<String>,
    /// publishedAt property.
    pub published_at: Option<String>,
    /// textDisplay property.
    pub text_display: Option<String>,
    /// textOriginal property.
    pub text_original: Option<String>,
    /// updatedAt property.
    pub updated_at: Option<String>,
    /// videoId property.
    pub video_id: Option<String>,
    /// viewerRating property.
    pub viewer_rating: Option<String>,
}

/// `LiveBroadcastStatus` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct LiveBroadcastStatus {
    /// lifeCycleStatus property.
    pub life_cycle_status: Option<String>,
    /// liveBroadcastPriority property.
    pub live_broadcast_priority: Option<String>,
    /// madeForKids property.
    pub made_for_kids: Option<bool>,
    /// privacyStatus property.
    pub privacy_status: Option<String>,
    /// recordingStatus property.
    pub recording_status: Option<String>,
    /// selfDeclaredMadeForKids property.
    pub self_declared_made_for_kids: Option<bool>,
}

/// `LiveChatMessageSnippet` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct LiveChatMessageSnippet {
    /// authorChannelId property.
    pub author_channel_id: Option<String>,
    /// displayMessage property.
    pub display_message: Option<String>,
    /// fanFundingEventDetails property.
    pub fan_funding_event_details: Option<LiveChatFanFundingEventDetails>,
    /// giftDetails property.
    pub gift_details: Option<LiveChatGiftDetails>,
    /// giftMembershipReceivedDetails property.
    pub gift_membership_received_details: Option<LiveChatGiftMembershipReceivedDetails>,
    /// hasDisplayContent property.
    pub has_display_content: Option<bool>,
    /// liveChatId property.
    pub live_chat_id: Option<String>,
    /// memberMilestoneChatDetails property.
    pub member_milestone_chat_details: Option<LiveChatMemberMilestoneChatDetails>,
    /// membershipGiftingDetails property.
    pub membership_gifting_details: Option<LiveChatMembershipGiftingDetails>,
    /// messageDeletedDetails property.
    pub message_deleted_details: Option<LiveChatMessageDeletedDetails>,
    /// messageRetractedDetails property.
    pub message_retracted_details: Option<LiveChatMessageRetractedDetails>,
    /// newSponsorDetails property.
    pub new_sponsor_details: Option<LiveChatNewSponsorDetails>,
    /// pollDetails property.
    pub poll_details: Option<LiveChatPollDetails>,
    /// publishedAt property.
    pub published_at: Option<String>,
    /// superChatDetails property.
    pub super_chat_details: Option<LiveChatSuperChatDetails>,
    /// superStickerDetails property.
    pub super_sticker_details: Option<LiveChatSuperStickerDetails>,
    /// textMessageDetails property.
    pub text_message_details: Option<LiveChatTextMessageDetails>,
    /// type property.
    pub r#type: Option<String>,
    /// userBannedDetails property.
    pub user_banned_details: Option<LiveChatUserBannedMessageDetails>,
}

/// `MemberListResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct MemberListResponse {
    /// etag property.
    pub etag: Option<String>,
    /// eventId property.
    pub event_id: Option<String>,
    /// items property.
    pub items: Option<Vec<Member>>,
    /// kind property.
    pub kind: Option<String>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// pageInfo property.
    pub page_info: Option<PageInfo>,
    /// tokenPagination property.
    pub token_pagination: Option<TokenPagination>,
    /// visitorId property.
    pub visitor_id: Option<String>,
}

/// `LiveStreamConfigurationIssue` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct LiveStreamConfigurationIssue {
    /// description property.
    pub description: Option<String>,
    /// reason property.
    pub reason: Option<String>,
    /// severity property.
    pub severity: Option<String>,
    /// type property.
    pub r#type: Option<String>,
}

/// `ChannelToStoreLinkDetailsMerchantAffiliateProgramDetails` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ChannelToStoreLinkDetailsMerchantAffiliateProgramDetails {
    /// status property.
    pub status: Option<String>,
}

/// `MembershipsLevelListResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct MembershipsLevelListResponse {
    /// etag property.
    pub etag: Option<String>,
    /// eventId property.
    pub event_id: Option<String>,
    /// items property.
    pub items: Option<Vec<MembershipsLevel>>,
    /// kind property.
    pub kind: Option<String>,
    /// visitorId property.
    pub visitor_id: Option<String>,
}

/// `PlaylistPlayer` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct PlaylistPlayer {
    /// embedHtml property.
    pub embed_html: Option<String>,
}

/// `LiveChatMemberMilestoneChatDetails` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct LiveChatMemberMilestoneChatDetails {
    /// memberLevelName property.
    pub member_level_name: Option<String>,
    /// memberMonth property.
    pub member_month: Option<i64>,
    /// userComment property.
    pub user_comment: Option<String>,
}

/// `ThirdPartyLinkListResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ThirdPartyLinkListResponse {
    /// etag property.
    pub etag: Option<String>,
    /// items property.
    pub items: Option<Vec<ThirdPartyLink>>,
    /// kind property.
    pub kind: Option<String>,
}

/// `LiveStreamStatus` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct LiveStreamStatus {
    /// healthStatus property.
    pub health_status: Option<LiveStreamHealthStatus>,
    /// streamStatus property.
    pub stream_status: Option<String>,
}

// =============================================================================
// ARGS TYPES (per-endpoint)
// =============================================================================

/// Arguments for [`youtube.abuseReports.insert_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct YoutubeAbuseReportsInsertArgs {
    /// Query parameter: `part`.
    pub part: Option<String>,
    /// Request body.
    pub body: AbuseReport,
}

/// Arguments for [`youtube.activities.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct YoutubeActivitiesListArgs {
    /// Query parameter: `channelId`.
    pub channel_id: Option<String>,
    /// Query parameter: `home`.
    pub home: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `mine`.
    pub mine: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `part`.
    pub part: Option<String>,
    /// Query parameter: `publishedAfter`.
    pub published_after: Option<String>,
    /// Query parameter: `publishedBefore`.
    pub published_before: Option<String>,
    /// Query parameter: `regionCode`.
    pub region_code: Option<String>,
}

/// Arguments for [`youtube.captions.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct YoutubeCaptionsDeleteArgs {
    /// Query parameter: `id`.
    pub id: Option<String>,
    /// Query parameter: `onBehalfOf`.
    pub on_behalf_of: Option<String>,
    /// Query parameter: `onBehalfOfContentOwner`.
    pub on_behalf_of_content_owner: Option<String>,
}

/// Arguments for [`youtube.captions.download_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct YoutubeCaptionsDownloadArgs {
    /// Path parameter: `id`.
    pub id: String,
    /// Query parameter: `onBehalfOf`.
    pub on_behalf_of: Option<String>,
    /// Query parameter: `onBehalfOfContentOwner`.
    pub on_behalf_of_content_owner: Option<String>,
    /// Query parameter: `tfmt`.
    pub tfmt: Option<String>,
    /// Query parameter: `tlang`.
    pub tlang: Option<String>,
}

/// Arguments for [`youtube.captions.insert_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct YoutubeCaptionsInsertArgs {
    /// Query parameter: `onBehalfOf`.
    pub on_behalf_of: Option<String>,
    /// Query parameter: `onBehalfOfContentOwner`.
    pub on_behalf_of_content_owner: Option<String>,
    /// Query parameter: `part`.
    pub part: Option<String>,
    /// Query parameter: `sync`.
    pub sync: Option<String>,
    /// Request body.
    pub body: Caption,
}

/// Arguments for [`youtube.captions.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct YoutubeCaptionsListArgs {
    /// Query parameter: `id`.
    pub id: Option<String>,
    /// Query parameter: `onBehalfOf`.
    pub on_behalf_of: Option<String>,
    /// Query parameter: `onBehalfOfContentOwner`.
    pub on_behalf_of_content_owner: Option<String>,
    /// Query parameter: `part`.
    pub part: Option<String>,
    /// Query parameter: `videoId`.
    pub video_id: Option<String>,
}

/// Arguments for [`youtube.captions.update_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct YoutubeCaptionsUpdateArgs {
    /// Query parameter: `onBehalfOf`.
    pub on_behalf_of: Option<String>,
    /// Query parameter: `onBehalfOfContentOwner`.
    pub on_behalf_of_content_owner: Option<String>,
    /// Query parameter: `part`.
    pub part: Option<String>,
    /// Query parameter: `sync`.
    pub sync: Option<String>,
    /// Request body.
    pub body: Caption,
}

/// Arguments for [`youtube.channelBanners.insert_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct YoutubeChannelBannersInsertArgs {
    /// Query parameter: `channelId`.
    pub channel_id: Option<String>,
    /// Query parameter: `onBehalfOfContentOwner`.
    pub on_behalf_of_content_owner: Option<String>,
    /// Query parameter: `onBehalfOfContentOwnerChannel`.
    pub on_behalf_of_content_owner_channel: Option<String>,
    /// Request body.
    pub body: ChannelBannerResource,
}

/// Arguments for [`youtube.channelSections.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct YoutubeChannelSectionsDeleteArgs {
    /// Query parameter: `id`.
    pub id: Option<String>,
    /// Query parameter: `onBehalfOfContentOwner`.
    pub on_behalf_of_content_owner: Option<String>,
}

/// Arguments for [`youtube.channelSections.insert_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct YoutubeChannelSectionsInsertArgs {
    /// Query parameter: `onBehalfOfContentOwner`.
    pub on_behalf_of_content_owner: Option<String>,
    /// Query parameter: `onBehalfOfContentOwnerChannel`.
    pub on_behalf_of_content_owner_channel: Option<String>,
    /// Query parameter: `part`.
    pub part: Option<String>,
    /// Request body.
    pub body: ChannelSection,
}

/// Arguments for [`youtube.channelSections.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct YoutubeChannelSectionsListArgs {
    /// Query parameter: `channelId`.
    pub channel_id: Option<String>,
    /// Query parameter: `hl`.
    pub hl: Option<String>,
    /// Query parameter: `id`.
    pub id: Option<String>,
    /// Query parameter: `mine`.
    pub mine: Option<String>,
    /// Query parameter: `onBehalfOfContentOwner`.
    pub on_behalf_of_content_owner: Option<String>,
    /// Query parameter: `part`.
    pub part: Option<String>,
}

/// Arguments for [`youtube.channelSections.update_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct YoutubeChannelSectionsUpdateArgs {
    /// Query parameter: `onBehalfOfContentOwner`.
    pub on_behalf_of_content_owner: Option<String>,
    /// Query parameter: `part`.
    pub part: Option<String>,
    /// Request body.
    pub body: ChannelSection,
}

/// Arguments for [`youtube.channels.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct YoutubeChannelsListArgs {
    /// Query parameter: `categoryId`.
    pub category_id: Option<String>,
    /// Query parameter: `forHandle`.
    pub for_handle: Option<String>,
    /// Query parameter: `forUsername`.
    pub for_username: Option<String>,
    /// Query parameter: `hl`.
    pub hl: Option<String>,
    /// Query parameter: `id`.
    pub id: Option<String>,
    /// Query parameter: `managedByMe`.
    pub managed_by_me: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `mine`.
    pub mine: Option<String>,
    /// Query parameter: `mySubscribers`.
    pub my_subscribers: Option<String>,
    /// Query parameter: `onBehalfOfContentOwner`.
    pub on_behalf_of_content_owner: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `part`.
    pub part: Option<String>,
}

/// Arguments for [`youtube.channels.update_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct YoutubeChannelsUpdateArgs {
    /// Query parameter: `onBehalfOfContentOwner`.
    pub on_behalf_of_content_owner: Option<String>,
    /// Query parameter: `part`.
    pub part: Option<String>,
    /// Request body.
    pub body: Channel,
}

/// Arguments for [`youtube.commentThreads.insert_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct YoutubeCommentThreadsInsertArgs {
    /// Query parameter: `part`.
    pub part: Option<String>,
    /// Request body.
    pub body: CommentThread,
}

/// Arguments for [`youtube.commentThreads.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct YoutubeCommentThreadsListArgs {
    /// Query parameter: `allThreadsRelatedToChannelId`.
    pub all_threads_related_to_channel_id: Option<String>,
    /// Query parameter: `channelId`.
    pub channel_id: Option<String>,
    /// Query parameter: `id`.
    pub id: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `moderationStatus`.
    pub moderation_status: Option<String>,
    /// Query parameter: `order`.
    pub order: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `part`.
    pub part: Option<String>,
    /// Query parameter: `postId`.
    pub post_id: Option<String>,
    /// Query parameter: `searchTerms`.
    pub search_terms: Option<String>,
    /// Query parameter: `textFormat`.
    pub text_format: Option<String>,
    /// Query parameter: `videoId`.
    pub video_id: Option<String>,
}

/// Arguments for [`youtube.comments.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct YoutubeCommentsDeleteArgs {
    /// Query parameter: `id`.
    pub id: Option<String>,
}

/// Arguments for [`youtube.comments.insert_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct YoutubeCommentsInsertArgs {
    /// Query parameter: `part`.
    pub part: Option<String>,
    /// Request body.
    pub body: Comment,
}

/// Arguments for [`youtube.comments.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct YoutubeCommentsListArgs {
    /// Query parameter: `id`.
    pub id: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `parentId`.
    pub parent_id: Option<String>,
    /// Query parameter: `part`.
    pub part: Option<String>,
    /// Query parameter: `textFormat`.
    pub text_format: Option<String>,
}

/// Arguments for [`youtube.comments.markAsSpam_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct YoutubeCommentsMarkAsSpamArgs {
    /// Query parameter: `id`.
    pub id: Option<String>,
}

/// Arguments for [`youtube.comments.setModerationStatus_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct YoutubeCommentsSetModerationStatusArgs {
    /// Query parameter: `banAuthor`.
    pub ban_author: Option<String>,
    /// Query parameter: `id`.
    pub id: Option<String>,
    /// Query parameter: `moderationStatus`.
    pub moderation_status: Option<String>,
}

/// Arguments for [`youtube.comments.update_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct YoutubeCommentsUpdateArgs {
    /// Query parameter: `part`.
    pub part: Option<String>,
    /// Request body.
    pub body: Comment,
}

/// Arguments for [`youtube.i18nLanguages.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct YoutubeI18NLanguagesListArgs {
    /// Query parameter: `hl`.
    pub hl: Option<String>,
    /// Query parameter: `part`.
    pub part: Option<String>,
}

/// Arguments for [`youtube.i18nRegions.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct YoutubeI18NRegionsListArgs {
    /// Query parameter: `hl`.
    pub hl: Option<String>,
    /// Query parameter: `part`.
    pub part: Option<String>,
}

/// Arguments for [`youtube.liveBroadcasts.bind_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct YoutubeLiveBroadcastsBindArgs {
    /// Query parameter: `id`.
    pub id: Option<String>,
    /// Query parameter: `onBehalfOfContentOwner`.
    pub on_behalf_of_content_owner: Option<String>,
    /// Query parameter: `onBehalfOfContentOwnerChannel`.
    pub on_behalf_of_content_owner_channel: Option<String>,
    /// Query parameter: `part`.
    pub part: Option<String>,
    /// Query parameter: `streamId`.
    pub stream_id: Option<String>,
}

/// Arguments for [`youtube.liveBroadcasts.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct YoutubeLiveBroadcastsDeleteArgs {
    /// Query parameter: `id`.
    pub id: Option<String>,
    /// Query parameter: `onBehalfOfContentOwner`.
    pub on_behalf_of_content_owner: Option<String>,
    /// Query parameter: `onBehalfOfContentOwnerChannel`.
    pub on_behalf_of_content_owner_channel: Option<String>,
}

/// Arguments for [`youtube.liveBroadcasts.insert_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct YoutubeLiveBroadcastsInsertArgs {
    /// Query parameter: `onBehalfOfContentOwner`.
    pub on_behalf_of_content_owner: Option<String>,
    /// Query parameter: `onBehalfOfContentOwnerChannel`.
    pub on_behalf_of_content_owner_channel: Option<String>,
    /// Query parameter: `part`.
    pub part: Option<String>,
    /// Request body.
    pub body: LiveBroadcast,
}

/// Arguments for [`youtube.liveBroadcasts.insertCuepoint_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct YoutubeLiveBroadcastsInsertCuepointArgs {
    /// Query parameter: `id`.
    pub id: Option<String>,
    /// Query parameter: `onBehalfOfContentOwner`.
    pub on_behalf_of_content_owner: Option<String>,
    /// Query parameter: `onBehalfOfContentOwnerChannel`.
    pub on_behalf_of_content_owner_channel: Option<String>,
    /// Query parameter: `part`.
    pub part: Option<String>,
    /// Request body.
    pub body: Cuepoint,
}

/// Arguments for [`youtube.liveBroadcasts.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct YoutubeLiveBroadcastsListArgs {
    /// Query parameter: `broadcastStatus`.
    pub broadcast_status: Option<String>,
    /// Query parameter: `broadcastType`.
    pub broadcast_type: Option<String>,
    /// Query parameter: `id`.
    pub id: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `mine`.
    pub mine: Option<String>,
    /// Query parameter: `onBehalfOfContentOwner`.
    pub on_behalf_of_content_owner: Option<String>,
    /// Query parameter: `onBehalfOfContentOwnerChannel`.
    pub on_behalf_of_content_owner_channel: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `part`.
    pub part: Option<String>,
}

/// Arguments for [`youtube.liveBroadcasts.transition_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct YoutubeLiveBroadcastsTransitionArgs {
    /// Query parameter: `broadcastStatus`.
    pub broadcast_status: Option<String>,
    /// Query parameter: `id`.
    pub id: Option<String>,
    /// Query parameter: `onBehalfOfContentOwner`.
    pub on_behalf_of_content_owner: Option<String>,
    /// Query parameter: `onBehalfOfContentOwnerChannel`.
    pub on_behalf_of_content_owner_channel: Option<String>,
    /// Query parameter: `part`.
    pub part: Option<String>,
}

/// Arguments for [`youtube.liveBroadcasts.update_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct YoutubeLiveBroadcastsUpdateArgs {
    /// Query parameter: `onBehalfOfContentOwner`.
    pub on_behalf_of_content_owner: Option<String>,
    /// Query parameter: `onBehalfOfContentOwnerChannel`.
    pub on_behalf_of_content_owner_channel: Option<String>,
    /// Query parameter: `part`.
    pub part: Option<String>,
    /// Request body.
    pub body: LiveBroadcast,
}

/// Arguments for [`youtube.liveChatBans.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct YoutubeLiveChatBansDeleteArgs {
    /// Query parameter: `id`.
    pub id: Option<String>,
}

/// Arguments for [`youtube.liveChatBans.insert_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct YoutubeLiveChatBansInsertArgs {
    /// Query parameter: `part`.
    pub part: Option<String>,
    /// Request body.
    pub body: LiveChatBan,
}

/// Arguments for [`youtube.liveChatMessages.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct YoutubeLiveChatMessagesDeleteArgs {
    /// Query parameter: `id`.
    pub id: Option<String>,
}

/// Arguments for [`youtube.liveChatMessages.insert_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct YoutubeLiveChatMessagesInsertArgs {
    /// Query parameter: `part`.
    pub part: Option<String>,
    /// Request body.
    pub body: LiveChatMessage,
}

/// Arguments for [`youtube.liveChatMessages.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct YoutubeLiveChatMessagesListArgs {
    /// Query parameter: `hl`.
    pub hl: Option<String>,
    /// Query parameter: `liveChatId`.
    pub live_chat_id: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `part`.
    pub part: Option<String>,
    /// Query parameter: `profileImageSize`.
    pub profile_image_size: Option<String>,
}

/// Arguments for [`youtube.liveChatMessages.transition_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct YoutubeLiveChatMessagesTransitionArgs {
    /// Query parameter: `id`.
    pub id: Option<String>,
    /// Query parameter: `status`.
    pub status: Option<String>,
}

/// Arguments for [`youtube.liveChatModerators.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct YoutubeLiveChatModeratorsDeleteArgs {
    /// Query parameter: `id`.
    pub id: Option<String>,
}

/// Arguments for [`youtube.liveChatModerators.insert_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct YoutubeLiveChatModeratorsInsertArgs {
    /// Query parameter: `part`.
    pub part: Option<String>,
    /// Request body.
    pub body: LiveChatModerator,
}

/// Arguments for [`youtube.liveChatModerators.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct YoutubeLiveChatModeratorsListArgs {
    /// Query parameter: `liveChatId`.
    pub live_chat_id: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `part`.
    pub part: Option<String>,
}

/// Arguments for [`youtube.liveStreams.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct YoutubeLiveStreamsDeleteArgs {
    /// Query parameter: `id`.
    pub id: Option<String>,
    /// Query parameter: `onBehalfOfContentOwner`.
    pub on_behalf_of_content_owner: Option<String>,
    /// Query parameter: `onBehalfOfContentOwnerChannel`.
    pub on_behalf_of_content_owner_channel: Option<String>,
}

/// Arguments for [`youtube.liveStreams.insert_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct YoutubeLiveStreamsInsertArgs {
    /// Query parameter: `onBehalfOfContentOwner`.
    pub on_behalf_of_content_owner: Option<String>,
    /// Query parameter: `onBehalfOfContentOwnerChannel`.
    pub on_behalf_of_content_owner_channel: Option<String>,
    /// Query parameter: `part`.
    pub part: Option<String>,
    /// Request body.
    pub body: LiveStream,
}

/// Arguments for [`youtube.liveStreams.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct YoutubeLiveStreamsListArgs {
    /// Query parameter: `id`.
    pub id: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `mine`.
    pub mine: Option<String>,
    /// Query parameter: `onBehalfOfContentOwner`.
    pub on_behalf_of_content_owner: Option<String>,
    /// Query parameter: `onBehalfOfContentOwnerChannel`.
    pub on_behalf_of_content_owner_channel: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `part`.
    pub part: Option<String>,
}

/// Arguments for [`youtube.liveStreams.update_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct YoutubeLiveStreamsUpdateArgs {
    /// Query parameter: `onBehalfOfContentOwner`.
    pub on_behalf_of_content_owner: Option<String>,
    /// Query parameter: `onBehalfOfContentOwnerChannel`.
    pub on_behalf_of_content_owner_channel: Option<String>,
    /// Query parameter: `part`.
    pub part: Option<String>,
    /// Request body.
    pub body: LiveStream,
}

/// Arguments for [`youtube.members.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct YoutubeMembersListArgs {
    /// Query parameter: `filterByMemberChannelId`.
    pub filter_by_member_channel_id: Option<String>,
    /// Query parameter: `hasAccessToLevel`.
    pub has_access_to_level: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `mode`.
    pub mode: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `part`.
    pub part: Option<String>,
}

/// Arguments for [`youtube.membershipsLevels.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct YoutubeMembershipsLevelsListArgs {
    /// Query parameter: `part`.
    pub part: Option<String>,
}

/// Arguments for [`youtube.playlistImages.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct YoutubePlaylistImagesDeleteArgs {
    /// Query parameter: `id`.
    pub id: Option<String>,
    /// Query parameter: `onBehalfOfContentOwner`.
    pub on_behalf_of_content_owner: Option<String>,
}

/// Arguments for [`youtube.playlistImages.insert_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct YoutubePlaylistImagesInsertArgs {
    /// Query parameter: `onBehalfOfContentOwner`.
    pub on_behalf_of_content_owner: Option<String>,
    /// Query parameter: `onBehalfOfContentOwnerChannel`.
    pub on_behalf_of_content_owner_channel: Option<String>,
    /// Query parameter: `part`.
    pub part: Option<String>,
    /// Request body.
    pub body: PlaylistImage,
}

/// Arguments for [`youtube.playlistImages.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct YoutubePlaylistImagesListArgs {
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `onBehalfOfContentOwner`.
    pub on_behalf_of_content_owner: Option<String>,
    /// Query parameter: `onBehalfOfContentOwnerChannel`.
    pub on_behalf_of_content_owner_channel: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `parent`.
    pub parent: Option<String>,
    /// Query parameter: `part`.
    pub part: Option<String>,
}

/// Arguments for [`youtube.playlistImages.update_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct YoutubePlaylistImagesUpdateArgs {
    /// Query parameter: `onBehalfOfContentOwner`.
    pub on_behalf_of_content_owner: Option<String>,
    /// Query parameter: `part`.
    pub part: Option<String>,
    /// Request body.
    pub body: PlaylistImage,
}

/// Arguments for [`youtube.playlistItems.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct YoutubePlaylistItemsDeleteArgs {
    /// Query parameter: `id`.
    pub id: Option<String>,
    /// Query parameter: `onBehalfOfContentOwner`.
    pub on_behalf_of_content_owner: Option<String>,
}

/// Arguments for [`youtube.playlistItems.insert_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct YoutubePlaylistItemsInsertArgs {
    /// Query parameter: `onBehalfOfContentOwner`.
    pub on_behalf_of_content_owner: Option<String>,
    /// Query parameter: `part`.
    pub part: Option<String>,
    /// Request body.
    pub body: PlaylistItem,
}

/// Arguments for [`youtube.playlistItems.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct YoutubePlaylistItemsListArgs {
    /// Query parameter: `id`.
    pub id: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `onBehalfOfContentOwner`.
    pub on_behalf_of_content_owner: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `part`.
    pub part: Option<String>,
    /// Query parameter: `playlistId`.
    pub playlist_id: Option<String>,
    /// Query parameter: `videoId`.
    pub video_id: Option<String>,
}

/// Arguments for [`youtube.playlistItems.update_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct YoutubePlaylistItemsUpdateArgs {
    /// Query parameter: `onBehalfOfContentOwner`.
    pub on_behalf_of_content_owner: Option<String>,
    /// Query parameter: `part`.
    pub part: Option<String>,
    /// Request body.
    pub body: PlaylistItem,
}

/// Arguments for [`youtube.playlists.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct YoutubePlaylistsDeleteArgs {
    /// Query parameter: `id`.
    pub id: Option<String>,
    /// Query parameter: `onBehalfOfContentOwner`.
    pub on_behalf_of_content_owner: Option<String>,
}

/// Arguments for [`youtube.playlists.insert_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct YoutubePlaylistsInsertArgs {
    /// Query parameter: `onBehalfOfContentOwner`.
    pub on_behalf_of_content_owner: Option<String>,
    /// Query parameter: `onBehalfOfContentOwnerChannel`.
    pub on_behalf_of_content_owner_channel: Option<String>,
    /// Query parameter: `part`.
    pub part: Option<String>,
    /// Request body.
    pub body: Playlist,
}

/// Arguments for [`youtube.playlists.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct YoutubePlaylistsListArgs {
    /// Query parameter: `channelId`.
    pub channel_id: Option<String>,
    /// Query parameter: `hl`.
    pub hl: Option<String>,
    /// Query parameter: `id`.
    pub id: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `mine`.
    pub mine: Option<String>,
    /// Query parameter: `onBehalfOfContentOwner`.
    pub on_behalf_of_content_owner: Option<String>,
    /// Query parameter: `onBehalfOfContentOwnerChannel`.
    pub on_behalf_of_content_owner_channel: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `part`.
    pub part: Option<String>,
}

/// Arguments for [`youtube.playlists.update_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct YoutubePlaylistsUpdateArgs {
    /// Query parameter: `onBehalfOfContentOwner`.
    pub on_behalf_of_content_owner: Option<String>,
    /// Query parameter: `part`.
    pub part: Option<String>,
    /// Request body.
    pub body: Playlist,
}

/// Arguments for [`youtube.search.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct YoutubeSearchListArgs {
    /// Query parameter: `channelId`.
    pub channel_id: Option<String>,
    /// Query parameter: `channelType`.
    pub channel_type: Option<String>,
    /// Query parameter: `eventType`.
    pub event_type: Option<String>,
    /// Query parameter: `forContentOwner`.
    pub for_content_owner: Option<String>,
    /// Query parameter: `forDeveloper`.
    pub for_developer: Option<String>,
    /// Query parameter: `forMine`.
    pub for_mine: Option<String>,
    /// Query parameter: `location`.
    pub location: Option<String>,
    /// Query parameter: `locationRadius`.
    pub location_radius: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `onBehalfOfContentOwner`.
    pub on_behalf_of_content_owner: Option<String>,
    /// Query parameter: `order`.
    pub order: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `part`.
    pub part: Option<String>,
    /// Query parameter: `publishedAfter`.
    pub published_after: Option<String>,
    /// Query parameter: `publishedBefore`.
    pub published_before: Option<String>,
    /// Query parameter: `q`.
    pub q: Option<String>,
    /// Query parameter: `regionCode`.
    pub region_code: Option<String>,
    /// Query parameter: `relevanceLanguage`.
    pub relevance_language: Option<String>,
    /// Query parameter: `safeSearch`.
    pub safe_search: Option<String>,
    /// Query parameter: `topicId`.
    pub topic_id: Option<String>,
    /// Query parameter: `type`.
    pub r#type: Option<String>,
    /// Query parameter: `videoCaption`.
    pub video_caption: Option<String>,
    /// Query parameter: `videoCategoryId`.
    pub video_category_id: Option<String>,
    /// Query parameter: `videoDefinition`.
    pub video_definition: Option<String>,
    /// Query parameter: `videoDimension`.
    pub video_dimension: Option<String>,
    /// Query parameter: `videoDuration`.
    pub video_duration: Option<String>,
    /// Query parameter: `videoEmbeddable`.
    pub video_embeddable: Option<String>,
    /// Query parameter: `videoLicense`.
    pub video_license: Option<String>,
    /// Query parameter: `videoPaidProductPlacement`.
    pub video_paid_product_placement: Option<String>,
    /// Query parameter: `videoSyndicated`.
    pub video_syndicated: Option<String>,
    /// Query parameter: `videoType`.
    pub video_type: Option<String>,
}

/// Arguments for [`youtube.subscriptions.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct YoutubeSubscriptionsDeleteArgs {
    /// Query parameter: `id`.
    pub id: Option<String>,
}

/// Arguments for [`youtube.subscriptions.insert_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct YoutubeSubscriptionsInsertArgs {
    /// Query parameter: `part`.
    pub part: Option<String>,
    /// Request body.
    pub body: Subscription,
}

/// Arguments for [`youtube.subscriptions.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct YoutubeSubscriptionsListArgs {
    /// Query parameter: `channelId`.
    pub channel_id: Option<String>,
    /// Query parameter: `forChannelId`.
    pub for_channel_id: Option<String>,
    /// Query parameter: `id`.
    pub id: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `mine`.
    pub mine: Option<String>,
    /// Query parameter: `myRecentSubscribers`.
    pub my_recent_subscribers: Option<String>,
    /// Query parameter: `mySubscribers`.
    pub my_subscribers: Option<String>,
    /// Query parameter: `onBehalfOfContentOwner`.
    pub on_behalf_of_content_owner: Option<String>,
    /// Query parameter: `onBehalfOfContentOwnerChannel`.
    pub on_behalf_of_content_owner_channel: Option<String>,
    /// Query parameter: `order`.
    pub order: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `part`.
    pub part: Option<String>,
}

/// Arguments for [`youtube.superChatEvents.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct YoutubeSuperChatEventsListArgs {
    /// Query parameter: `hl`.
    pub hl: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `part`.
    pub part: Option<String>,
}

/// Arguments for [`youtube.tests.insert_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct YoutubeTestsInsertArgs {
    /// Query parameter: `externalChannelId`.
    pub external_channel_id: Option<String>,
    /// Query parameter: `onBehalfOfContentOwnerChannel`.
    pub on_behalf_of_content_owner_channel: Option<String>,
    /// Query parameter: `part`.
    pub part: Option<String>,
    /// Request body.
    pub body: TestItem,
}

/// Arguments for [`youtube.thirdPartyLinks.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct YoutubeThirdPartyLinksDeleteArgs {
    /// Query parameter: `externalChannelId`.
    pub external_channel_id: Option<String>,
    /// Query parameter: `linkingToken`.
    pub linking_token: Option<String>,
    /// Query parameter: `part`.
    pub part: Option<String>,
    /// Query parameter: `type`.
    pub r#type: Option<String>,
}

/// Arguments for [`youtube.thirdPartyLinks.insert_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct YoutubeThirdPartyLinksInsertArgs {
    /// Query parameter: `externalChannelId`.
    pub external_channel_id: Option<String>,
    /// Query parameter: `part`.
    pub part: Option<String>,
    /// Request body.
    pub body: ThirdPartyLink,
}

/// Arguments for [`youtube.thirdPartyLinks.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct YoutubeThirdPartyLinksListArgs {
    /// Query parameter: `externalChannelId`.
    pub external_channel_id: Option<String>,
    /// Query parameter: `linkingToken`.
    pub linking_token: Option<String>,
    /// Query parameter: `part`.
    pub part: Option<String>,
    /// Query parameter: `type`.
    pub r#type: Option<String>,
}

/// Arguments for [`youtube.thirdPartyLinks.update_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct YoutubeThirdPartyLinksUpdateArgs {
    /// Query parameter: `externalChannelId`.
    pub external_channel_id: Option<String>,
    /// Query parameter: `part`.
    pub part: Option<String>,
    /// Request body.
    pub body: ThirdPartyLink,
}

/// Arguments for [`youtube.thumbnails.set_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct YoutubeThumbnailsSetArgs {
    /// Query parameter: `onBehalfOfContentOwner`.
    pub on_behalf_of_content_owner: Option<String>,
    /// Query parameter: `videoId`.
    pub video_id: Option<String>,
}

/// Arguments for [`youtube.videoAbuseReportReasons.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct YoutubeVideoAbuseReportReasonsListArgs {
    /// Query parameter: `hl`.
    pub hl: Option<String>,
    /// Query parameter: `part`.
    pub part: Option<String>,
}

/// Arguments for [`youtube.videoCategories.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct YoutubeVideoCategoriesListArgs {
    /// Query parameter: `hl`.
    pub hl: Option<String>,
    /// Query parameter: `id`.
    pub id: Option<String>,
    /// Query parameter: `part`.
    pub part: Option<String>,
    /// Query parameter: `regionCode`.
    pub region_code: Option<String>,
}

/// Arguments for [`youtube.videoTrainability.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct YoutubeVideoTrainabilityGetArgs {
    /// Query parameter: `id`.
    pub id: Option<String>,
}

/// Arguments for [`youtube.videos.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct YoutubeVideosDeleteArgs {
    /// Query parameter: `id`.
    pub id: Option<String>,
    /// Query parameter: `onBehalfOfContentOwner`.
    pub on_behalf_of_content_owner: Option<String>,
}

/// Arguments for [`youtube.videos.getRating_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct YoutubeVideosGetRatingArgs {
    /// Query parameter: `id`.
    pub id: Option<String>,
    /// Query parameter: `onBehalfOfContentOwner`.
    pub on_behalf_of_content_owner: Option<String>,
}

/// Arguments for [`youtube.videos.insert_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct YoutubeVideosInsertArgs {
    /// Query parameter: `autoLevels`.
    pub auto_levels: Option<String>,
    /// Query parameter: `notifySubscribers`.
    pub notify_subscribers: Option<String>,
    /// Query parameter: `onBehalfOfContentOwner`.
    pub on_behalf_of_content_owner: Option<String>,
    /// Query parameter: `onBehalfOfContentOwnerChannel`.
    pub on_behalf_of_content_owner_channel: Option<String>,
    /// Query parameter: `part`.
    pub part: Option<String>,
    /// Query parameter: `stabilize`.
    pub stabilize: Option<String>,
    /// Request body.
    pub body: Video,
}

/// Arguments for [`youtube.videos.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct YoutubeVideosListArgs {
    /// Query parameter: `chart`.
    pub chart: Option<String>,
    /// Query parameter: `hl`.
    pub hl: Option<String>,
    /// Query parameter: `id`.
    pub id: Option<String>,
    /// Query parameter: `locale`.
    pub locale: Option<String>,
    /// Query parameter: `maxHeight`.
    pub max_height: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `maxWidth`.
    pub max_width: Option<String>,
    /// Query parameter: `myRating`.
    pub my_rating: Option<String>,
    /// Query parameter: `onBehalfOfContentOwner`.
    pub on_behalf_of_content_owner: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `part`.
    pub part: Option<String>,
    /// Query parameter: `regionCode`.
    pub region_code: Option<String>,
    /// Query parameter: `videoCategoryId`.
    pub video_category_id: Option<String>,
}

/// Arguments for [`youtube.videos.rate_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct YoutubeVideosRateArgs {
    /// Query parameter: `id`.
    pub id: Option<String>,
    /// Query parameter: `rating`.
    pub rating: Option<String>,
}

/// Arguments for [`youtube.videos.reportAbuse_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct YoutubeVideosReportAbuseArgs {
    /// Query parameter: `onBehalfOfContentOwner`.
    pub on_behalf_of_content_owner: Option<String>,
    /// Request body.
    pub body: VideoAbuseReport,
}

/// Arguments for [`youtube.videos.update_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct YoutubeVideosUpdateArgs {
    /// Query parameter: `onBehalfOfContentOwner`.
    pub on_behalf_of_content_owner: Option<String>,
    /// Query parameter: `part`.
    pub part: Option<String>,
    /// Request body.
    pub body: Video,
}

/// Arguments for [`youtube.watermarks.set_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct YoutubeWatermarksSetArgs {
    /// Query parameter: `channelId`.
    pub channel_id: Option<String>,
    /// Query parameter: `onBehalfOfContentOwner`.
    pub on_behalf_of_content_owner: Option<String>,
    /// Request body.
    pub body: InvideoBranding,
}

/// Arguments for [`youtube.watermarks.unset_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct YoutubeWatermarksUnsetArgs {
    /// Query parameter: `channelId`.
    pub channel_id: Option<String>,
    /// Query parameter: `onBehalfOfContentOwner`.
    pub on_behalf_of_content_owner: Option<String>,
}

/// Arguments for [`youtube.youtube.v3.updateCommentThreads_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct YoutubeYoutubeV3UpdateCommentThreadsArgs {
    /// Query parameter: `part`.
    pub part: Option<String>,
    /// Request body.
    pub body: CommentThread,
}

/// Arguments for [`youtube.youtube.v3.liveChat.messages.stream_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct YoutubeYoutubeV3LiveChatMessagesStreamArgs {
    /// Query parameter: `hl`.
    pub hl: Option<String>,
    /// Query parameter: `liveChatId`.
    pub live_chat_id: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `part`.
    pub part: Option<String>,
    /// Query parameter: `profileImageSize`.
    pub profile_image_size: Option<String>,
}

// =============================================================================
// CLIENT FUNCTIONS (per-endpoint)
// =============================================================================

// -----------------------------------------------------------------------------
// POST youtube/v3/abuseReports
// -----------------------------------------------------------------------------

/// POST youtube/v3/abuseReports.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = youtube_abuse_reports_insert_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn youtube_abuse_reports_insert_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &YoutubeAbuseReportsInsertArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<AbuseReport>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://youtube.googleapis.com/youtube/v3/abuseReports",);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.part {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("part=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: AbuseReport =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET youtube/v3/activities
// -----------------------------------------------------------------------------

/// GET youtube/v3/activities.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = youtube_activities_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn youtube_activities_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &YoutubeActivitiesListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ActivityListResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://youtube.googleapis.com/youtube/v3/activities",);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.channel_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("channelId=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.home {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("home=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.mine {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("mine=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.part {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("part=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.published_after {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("publishedAfter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.published_before {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("publishedBefore=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.region_code {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("regionCode=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ActivityListResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE youtube/v3/captions
// -----------------------------------------------------------------------------

/// DELETE youtube/v3/captions.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = youtube_captions_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn youtube_captions_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &YoutubeCaptionsDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://youtube.googleapis.com/youtube/v3/captions",);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("id=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.on_behalf_of {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("onBehalfOf=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.on_behalf_of_content_owner {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("onBehalfOfContentOwner=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream: _,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET youtube/v3/captions/{id}
// -----------------------------------------------------------------------------

/// GET youtube/v3/captions/{id}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = youtube_captions_download_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn youtube_captions_download_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &YoutubeCaptionsDownloadArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://youtube.googleapis.com/youtube/v3/captions/{}",
        args.id,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.on_behalf_of {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("onBehalfOf=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.on_behalf_of_content_owner {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("onBehalfOfContentOwner=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.tfmt {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("tfmt=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.tlang {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("tlang=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream: _,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST youtube/v3/captions
// -----------------------------------------------------------------------------

/// POST youtube/v3/captions.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = youtube_captions_insert_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn youtube_captions_insert_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &YoutubeCaptionsInsertArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Caption>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://youtube.googleapis.com/youtube/v3/captions",);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.on_behalf_of {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("onBehalfOf=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.on_behalf_of_content_owner {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("onBehalfOfContentOwner=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.part {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("part=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.sync {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("sync=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Caption =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET youtube/v3/captions
// -----------------------------------------------------------------------------

/// GET youtube/v3/captions.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = youtube_captions_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn youtube_captions_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &YoutubeCaptionsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<CaptionListResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://youtube.googleapis.com/youtube/v3/captions",);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("id=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.on_behalf_of {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("onBehalfOf=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.on_behalf_of_content_owner {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("onBehalfOfContentOwner=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.part {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("part=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.video_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("videoId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: CaptionListResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PUT youtube/v3/captions
// -----------------------------------------------------------------------------

/// PUT youtube/v3/captions.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = youtube_captions_update_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn youtube_captions_update_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &YoutubeCaptionsUpdateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Caption>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://youtube.googleapis.com/youtube/v3/captions",);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.on_behalf_of {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("onBehalfOf=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.on_behalf_of_content_owner {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("onBehalfOfContentOwner=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.part {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("part=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.sync {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("sync=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .put(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Caption =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST youtube/v3/channelBanners/insert
// -----------------------------------------------------------------------------

/// POST youtube/v3/channelBanners/insert.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = youtube_channel_banners_insert_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn youtube_channel_banners_insert_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &YoutubeChannelBannersInsertArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ChannelBannerResource>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://youtube.googleapis.com/youtube/v3/channelBanners/insert",);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.channel_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("channelId=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.on_behalf_of_content_owner {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("onBehalfOfContentOwner=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.on_behalf_of_content_owner_channel {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("onBehalfOfContentOwnerChannel=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ChannelBannerResource =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE youtube/v3/channelSections
// -----------------------------------------------------------------------------

/// DELETE youtube/v3/channelSections.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = youtube_channel_sections_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn youtube_channel_sections_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &YoutubeChannelSectionsDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://youtube.googleapis.com/youtube/v3/channelSections",);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("id=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.on_behalf_of_content_owner {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("onBehalfOfContentOwner=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream: _,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST youtube/v3/channelSections
// -----------------------------------------------------------------------------

/// POST youtube/v3/channelSections.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = youtube_channel_sections_insert_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn youtube_channel_sections_insert_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &YoutubeChannelSectionsInsertArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ChannelSection>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://youtube.googleapis.com/youtube/v3/channelSections",);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.on_behalf_of_content_owner {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("onBehalfOfContentOwner=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.on_behalf_of_content_owner_channel {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("onBehalfOfContentOwnerChannel=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.part {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("part=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ChannelSection =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET youtube/v3/channelSections
// -----------------------------------------------------------------------------

/// GET youtube/v3/channelSections.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = youtube_channel_sections_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn youtube_channel_sections_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &YoutubeChannelSectionsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ChannelSectionListResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://youtube.googleapis.com/youtube/v3/channelSections",);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.channel_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("channelId=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.hl {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("hl=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("id=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.mine {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("mine=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.on_behalf_of_content_owner {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("onBehalfOfContentOwner=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.part {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("part=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ChannelSectionListResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PUT youtube/v3/channelSections
// -----------------------------------------------------------------------------

/// PUT youtube/v3/channelSections.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = youtube_channel_sections_update_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn youtube_channel_sections_update_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &YoutubeChannelSectionsUpdateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ChannelSection>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://youtube.googleapis.com/youtube/v3/channelSections",);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.on_behalf_of_content_owner {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("onBehalfOfContentOwner=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.part {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("part=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .put(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ChannelSection =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET youtube/v3/channels
// -----------------------------------------------------------------------------

/// GET youtube/v3/channels.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = youtube_channels_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn youtube_channels_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &YoutubeChannelsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ChannelListResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://youtube.googleapis.com/youtube/v3/channels",);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.category_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("categoryId=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.for_handle {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("forHandle=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.for_username {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("forUsername=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.hl {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("hl=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("id=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.managed_by_me {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("managedByMe=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.mine {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("mine=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.my_subscribers {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("mySubscribers=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.on_behalf_of_content_owner {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("onBehalfOfContentOwner=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.part {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("part=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ChannelListResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PUT youtube/v3/channels
// -----------------------------------------------------------------------------

/// PUT youtube/v3/channels.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = youtube_channels_update_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn youtube_channels_update_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &YoutubeChannelsUpdateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Channel>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://youtube.googleapis.com/youtube/v3/channels",);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.on_behalf_of_content_owner {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("onBehalfOfContentOwner=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.part {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("part=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .put(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Channel =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST youtube/v3/commentThreads
// -----------------------------------------------------------------------------

/// POST youtube/v3/commentThreads.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = youtube_comment_threads_insert_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn youtube_comment_threads_insert_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &YoutubeCommentThreadsInsertArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<CommentThread>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://youtube.googleapis.com/youtube/v3/commentThreads",);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.part {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("part=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: CommentThread =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET youtube/v3/commentThreads
// -----------------------------------------------------------------------------

/// GET youtube/v3/commentThreads.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = youtube_comment_threads_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn youtube_comment_threads_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &YoutubeCommentThreadsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<CommentThreadListResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://youtube.googleapis.com/youtube/v3/commentThreads",);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.all_threads_related_to_channel_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("allThreadsRelatedToChannelId=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.channel_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("channelId=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("id=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.moderation_status {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("moderationStatus=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.order {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("order=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.part {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("part=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.post_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("postId=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.search_terms {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("searchTerms=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.text_format {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("textFormat=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.video_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("videoId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: CommentThreadListResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE youtube/v3/comments
// -----------------------------------------------------------------------------

/// DELETE youtube/v3/comments.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = youtube_comments_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn youtube_comments_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &YoutubeCommentsDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://youtube.googleapis.com/youtube/v3/comments",);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("id=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream: _,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST youtube/v3/comments
// -----------------------------------------------------------------------------

/// POST youtube/v3/comments.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = youtube_comments_insert_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn youtube_comments_insert_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &YoutubeCommentsInsertArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Comment>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://youtube.googleapis.com/youtube/v3/comments",);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.part {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("part=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Comment =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET youtube/v3/comments
// -----------------------------------------------------------------------------

/// GET youtube/v3/comments.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = youtube_comments_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn youtube_comments_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &YoutubeCommentsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<CommentListResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://youtube.googleapis.com/youtube/v3/comments",);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("id=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.parent_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("parentId=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.part {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("part=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.text_format {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("textFormat=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: CommentListResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST youtube/v3/comments/markAsSpam
// -----------------------------------------------------------------------------

/// POST youtube/v3/comments/markAsSpam.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = youtube_comments_mark_as_spam_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn youtube_comments_mark_as_spam_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &YoutubeCommentsMarkAsSpamArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://youtube.googleapis.com/youtube/v3/comments/markAsSpam",);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("id=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream: _,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST youtube/v3/comments/setModerationStatus
// -----------------------------------------------------------------------------

/// POST youtube/v3/comments/setModerationStatus.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = youtube_comments_set_moderation_status_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn youtube_comments_set_moderation_status_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &YoutubeCommentsSetModerationStatusArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url =
        format!("https://youtube.googleapis.com/youtube/v3/comments/setModerationStatus",);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.ban_author {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("banAuthor=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("id=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.moderation_status {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("moderationStatus=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream: _,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PUT youtube/v3/comments
// -----------------------------------------------------------------------------

/// PUT youtube/v3/comments.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = youtube_comments_update_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn youtube_comments_update_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &YoutubeCommentsUpdateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Comment>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://youtube.googleapis.com/youtube/v3/comments",);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.part {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("part=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .put(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Comment =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET youtube/v3/i18nLanguages
// -----------------------------------------------------------------------------

/// GET youtube/v3/i18nLanguages.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = youtube_i18n_languages_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn youtube_i18n_languages_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &YoutubeI18NLanguagesListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<I18nLanguageListResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://youtube.googleapis.com/youtube/v3/i18nLanguages",);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.hl {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("hl=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.part {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("part=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: I18nLanguageListResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET youtube/v3/i18nRegions
// -----------------------------------------------------------------------------

/// GET youtube/v3/i18nRegions.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = youtube_i18n_regions_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn youtube_i18n_regions_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &YoutubeI18NRegionsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<I18nRegionListResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://youtube.googleapis.com/youtube/v3/i18nRegions",);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.hl {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("hl=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.part {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("part=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: I18nRegionListResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST youtube/v3/liveBroadcasts/bind
// -----------------------------------------------------------------------------

/// POST youtube/v3/liveBroadcasts/bind.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = youtube_live_broadcasts_bind_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn youtube_live_broadcasts_bind_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &YoutubeLiveBroadcastsBindArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<LiveBroadcast>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://youtube.googleapis.com/youtube/v3/liveBroadcasts/bind",);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("id=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.on_behalf_of_content_owner {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("onBehalfOfContentOwner=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.on_behalf_of_content_owner_channel {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("onBehalfOfContentOwnerChannel=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.part {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("part=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.stream_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("streamId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: LiveBroadcast =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE youtube/v3/liveBroadcasts
// -----------------------------------------------------------------------------

/// DELETE youtube/v3/liveBroadcasts.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = youtube_live_broadcasts_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn youtube_live_broadcasts_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &YoutubeLiveBroadcastsDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://youtube.googleapis.com/youtube/v3/liveBroadcasts",);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("id=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.on_behalf_of_content_owner {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("onBehalfOfContentOwner=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.on_behalf_of_content_owner_channel {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("onBehalfOfContentOwnerChannel=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream: _,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST youtube/v3/liveBroadcasts
// -----------------------------------------------------------------------------

/// POST youtube/v3/liveBroadcasts.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = youtube_live_broadcasts_insert_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn youtube_live_broadcasts_insert_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &YoutubeLiveBroadcastsInsertArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<LiveBroadcast>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://youtube.googleapis.com/youtube/v3/liveBroadcasts",);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.on_behalf_of_content_owner {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("onBehalfOfContentOwner=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.on_behalf_of_content_owner_channel {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("onBehalfOfContentOwnerChannel=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.part {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("part=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: LiveBroadcast =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST youtube/v3/liveBroadcasts/cuepoint
// -----------------------------------------------------------------------------

/// POST youtube/v3/liveBroadcasts/cuepoint.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = youtube_live_broadcasts_insert_cuepoint_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn youtube_live_broadcasts_insert_cuepoint_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &YoutubeLiveBroadcastsInsertCuepointArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Cuepoint>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url =
        format!("https://youtube.googleapis.com/youtube/v3/liveBroadcasts/cuepoint",);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("id=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.on_behalf_of_content_owner {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("onBehalfOfContentOwner=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.on_behalf_of_content_owner_channel {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("onBehalfOfContentOwnerChannel=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.part {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("part=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Cuepoint =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET youtube/v3/liveBroadcasts
// -----------------------------------------------------------------------------

/// GET youtube/v3/liveBroadcasts.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = youtube_live_broadcasts_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn youtube_live_broadcasts_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &YoutubeLiveBroadcastsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<LiveBroadcastListResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://youtube.googleapis.com/youtube/v3/liveBroadcasts",);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.broadcast_status {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("broadcastStatus=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.broadcast_type {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("broadcastType=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("id=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.mine {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("mine=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.on_behalf_of_content_owner {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("onBehalfOfContentOwner=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.on_behalf_of_content_owner_channel {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("onBehalfOfContentOwnerChannel=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.part {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("part=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: LiveBroadcastListResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST youtube/v3/liveBroadcasts/transition
// -----------------------------------------------------------------------------

/// POST youtube/v3/liveBroadcasts/transition.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = youtube_live_broadcasts_transition_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn youtube_live_broadcasts_transition_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &YoutubeLiveBroadcastsTransitionArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<LiveBroadcast>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url =
        format!("https://youtube.googleapis.com/youtube/v3/liveBroadcasts/transition",);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.broadcast_status {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("broadcastStatus=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("id=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.on_behalf_of_content_owner {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("onBehalfOfContentOwner=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.on_behalf_of_content_owner_channel {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("onBehalfOfContentOwnerChannel=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.part {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("part=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: LiveBroadcast =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PUT youtube/v3/liveBroadcasts
// -----------------------------------------------------------------------------

/// PUT youtube/v3/liveBroadcasts.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = youtube_live_broadcasts_update_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn youtube_live_broadcasts_update_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &YoutubeLiveBroadcastsUpdateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<LiveBroadcast>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://youtube.googleapis.com/youtube/v3/liveBroadcasts",);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.on_behalf_of_content_owner {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("onBehalfOfContentOwner=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.on_behalf_of_content_owner_channel {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("onBehalfOfContentOwnerChannel=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.part {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("part=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .put(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: LiveBroadcast =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE youtube/v3/liveChat/bans
// -----------------------------------------------------------------------------

/// DELETE youtube/v3/liveChat/bans.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = youtube_live_chat_bans_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn youtube_live_chat_bans_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &YoutubeLiveChatBansDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://youtube.googleapis.com/youtube/v3/liveChat/bans",);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("id=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream: _,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST youtube/v3/liveChat/bans
// -----------------------------------------------------------------------------

/// POST youtube/v3/liveChat/bans.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = youtube_live_chat_bans_insert_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn youtube_live_chat_bans_insert_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &YoutubeLiveChatBansInsertArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<LiveChatBan>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://youtube.googleapis.com/youtube/v3/liveChat/bans",);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.part {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("part=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: LiveChatBan =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE youtube/v3/liveChat/messages
// -----------------------------------------------------------------------------

/// DELETE youtube/v3/liveChat/messages.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = youtube_live_chat_messages_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn youtube_live_chat_messages_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &YoutubeLiveChatMessagesDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://youtube.googleapis.com/youtube/v3/liveChat/messages",);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("id=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream: _,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST youtube/v3/liveChat/messages
// -----------------------------------------------------------------------------

/// POST youtube/v3/liveChat/messages.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = youtube_live_chat_messages_insert_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn youtube_live_chat_messages_insert_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &YoutubeLiveChatMessagesInsertArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<LiveChatMessage>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://youtube.googleapis.com/youtube/v3/liveChat/messages",);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.part {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("part=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: LiveChatMessage =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET youtube/v3/liveChat/messages
// -----------------------------------------------------------------------------

/// GET youtube/v3/liveChat/messages.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = youtube_live_chat_messages_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn youtube_live_chat_messages_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &YoutubeLiveChatMessagesListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<LiveChatMessageListResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://youtube.googleapis.com/youtube/v3/liveChat/messages",);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.hl {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("hl=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.live_chat_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("liveChatId=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.part {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("part=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.profile_image_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("profileImageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: LiveChatMessageListResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST youtube/v3/liveChat/messages/transition
// -----------------------------------------------------------------------------

/// POST youtube/v3/liveChat/messages/transition.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = youtube_live_chat_messages_transition_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn youtube_live_chat_messages_transition_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &YoutubeLiveChatMessagesTransitionArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<LiveChatMessage>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url =
        format!("https://youtube.googleapis.com/youtube/v3/liveChat/messages/transition",);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("id=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.status {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("status=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: LiveChatMessage =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE youtube/v3/liveChat/moderators
// -----------------------------------------------------------------------------

/// DELETE youtube/v3/liveChat/moderators.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = youtube_live_chat_moderators_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn youtube_live_chat_moderators_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &YoutubeLiveChatModeratorsDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://youtube.googleapis.com/youtube/v3/liveChat/moderators",);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("id=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream: _,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST youtube/v3/liveChat/moderators
// -----------------------------------------------------------------------------

/// POST youtube/v3/liveChat/moderators.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = youtube_live_chat_moderators_insert_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn youtube_live_chat_moderators_insert_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &YoutubeLiveChatModeratorsInsertArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<LiveChatModerator>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://youtube.googleapis.com/youtube/v3/liveChat/moderators",);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.part {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("part=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: LiveChatModerator =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET youtube/v3/liveChat/moderators
// -----------------------------------------------------------------------------

/// GET youtube/v3/liveChat/moderators.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = youtube_live_chat_moderators_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn youtube_live_chat_moderators_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &YoutubeLiveChatModeratorsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<LiveChatModeratorListResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://youtube.googleapis.com/youtube/v3/liveChat/moderators",);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.live_chat_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("liveChatId=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.part {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("part=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: LiveChatModeratorListResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE youtube/v3/liveStreams
// -----------------------------------------------------------------------------

/// DELETE youtube/v3/liveStreams.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = youtube_live_streams_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn youtube_live_streams_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &YoutubeLiveStreamsDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://youtube.googleapis.com/youtube/v3/liveStreams",);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("id=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.on_behalf_of_content_owner {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("onBehalfOfContentOwner=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.on_behalf_of_content_owner_channel {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("onBehalfOfContentOwnerChannel=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream: _,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST youtube/v3/liveStreams
// -----------------------------------------------------------------------------

/// POST youtube/v3/liveStreams.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = youtube_live_streams_insert_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn youtube_live_streams_insert_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &YoutubeLiveStreamsInsertArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<LiveStream>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://youtube.googleapis.com/youtube/v3/liveStreams",);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.on_behalf_of_content_owner {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("onBehalfOfContentOwner=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.on_behalf_of_content_owner_channel {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("onBehalfOfContentOwnerChannel=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.part {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("part=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: LiveStream =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET youtube/v3/liveStreams
// -----------------------------------------------------------------------------

/// GET youtube/v3/liveStreams.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = youtube_live_streams_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn youtube_live_streams_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &YoutubeLiveStreamsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<LiveStreamListResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://youtube.googleapis.com/youtube/v3/liveStreams",);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("id=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.mine {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("mine=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.on_behalf_of_content_owner {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("onBehalfOfContentOwner=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.on_behalf_of_content_owner_channel {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("onBehalfOfContentOwnerChannel=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.part {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("part=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: LiveStreamListResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PUT youtube/v3/liveStreams
// -----------------------------------------------------------------------------

/// PUT youtube/v3/liveStreams.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = youtube_live_streams_update_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn youtube_live_streams_update_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &YoutubeLiveStreamsUpdateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<LiveStream>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://youtube.googleapis.com/youtube/v3/liveStreams",);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.on_behalf_of_content_owner {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("onBehalfOfContentOwner=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.on_behalf_of_content_owner_channel {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("onBehalfOfContentOwnerChannel=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.part {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("part=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .put(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: LiveStream =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET youtube/v3/members
// -----------------------------------------------------------------------------

/// GET youtube/v3/members.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = youtube_members_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn youtube_members_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &YoutubeMembersListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<MemberListResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://youtube.googleapis.com/youtube/v3/members",);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter_by_member_channel_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filterByMemberChannelId=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.has_access_to_level {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("hasAccessToLevel=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.mode {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("mode=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.part {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("part=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: MemberListResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET youtube/v3/membershipsLevels
// -----------------------------------------------------------------------------

/// GET youtube/v3/membershipsLevels.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = youtube_memberships_levels_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn youtube_memberships_levels_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &YoutubeMembershipsLevelsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<MembershipsLevelListResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://youtube.googleapis.com/youtube/v3/membershipsLevels",);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.part {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("part=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: MembershipsLevelListResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE youtube/v3/playlistImages
// -----------------------------------------------------------------------------

/// DELETE youtube/v3/playlistImages.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = youtube_playlist_images_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn youtube_playlist_images_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &YoutubePlaylistImagesDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://youtube.googleapis.com/youtube/v3/playlistImages",);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("id=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.on_behalf_of_content_owner {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("onBehalfOfContentOwner=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream: _,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST youtube/v3/playlistImages
// -----------------------------------------------------------------------------

/// POST youtube/v3/playlistImages.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = youtube_playlist_images_insert_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn youtube_playlist_images_insert_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &YoutubePlaylistImagesInsertArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<PlaylistImage>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://youtube.googleapis.com/youtube/v3/playlistImages",);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.on_behalf_of_content_owner {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("onBehalfOfContentOwner=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.on_behalf_of_content_owner_channel {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("onBehalfOfContentOwnerChannel=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.part {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("part=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: PlaylistImage =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET youtube/v3/playlistImages
// -----------------------------------------------------------------------------

/// GET youtube/v3/playlistImages.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = youtube_playlist_images_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn youtube_playlist_images_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &YoutubePlaylistImagesListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<PlaylistImageListResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://youtube.googleapis.com/youtube/v3/playlistImages",);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.on_behalf_of_content_owner {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("onBehalfOfContentOwner=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.on_behalf_of_content_owner_channel {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("onBehalfOfContentOwnerChannel=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.parent {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("parent=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.part {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("part=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: PlaylistImageListResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PUT youtube/v3/playlistImages
// -----------------------------------------------------------------------------

/// PUT youtube/v3/playlistImages.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = youtube_playlist_images_update_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn youtube_playlist_images_update_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &YoutubePlaylistImagesUpdateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<PlaylistImage>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://youtube.googleapis.com/youtube/v3/playlistImages",);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.on_behalf_of_content_owner {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("onBehalfOfContentOwner=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.part {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("part=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .put(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: PlaylistImage =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE youtube/v3/playlistItems
// -----------------------------------------------------------------------------

/// DELETE youtube/v3/playlistItems.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = youtube_playlist_items_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn youtube_playlist_items_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &YoutubePlaylistItemsDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://youtube.googleapis.com/youtube/v3/playlistItems",);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("id=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.on_behalf_of_content_owner {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("onBehalfOfContentOwner=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream: _,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST youtube/v3/playlistItems
// -----------------------------------------------------------------------------

/// POST youtube/v3/playlistItems.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = youtube_playlist_items_insert_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn youtube_playlist_items_insert_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &YoutubePlaylistItemsInsertArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<PlaylistItem>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://youtube.googleapis.com/youtube/v3/playlistItems",);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.on_behalf_of_content_owner {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("onBehalfOfContentOwner=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.part {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("part=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: PlaylistItem =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET youtube/v3/playlistItems
// -----------------------------------------------------------------------------

/// GET youtube/v3/playlistItems.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = youtube_playlist_items_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn youtube_playlist_items_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &YoutubePlaylistItemsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<PlaylistItemListResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://youtube.googleapis.com/youtube/v3/playlistItems",);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("id=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.on_behalf_of_content_owner {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("onBehalfOfContentOwner=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.part {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("part=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.playlist_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("playlistId=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.video_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("videoId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: PlaylistItemListResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PUT youtube/v3/playlistItems
// -----------------------------------------------------------------------------

/// PUT youtube/v3/playlistItems.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = youtube_playlist_items_update_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn youtube_playlist_items_update_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &YoutubePlaylistItemsUpdateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<PlaylistItem>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://youtube.googleapis.com/youtube/v3/playlistItems",);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.on_behalf_of_content_owner {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("onBehalfOfContentOwner=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.part {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("part=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .put(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: PlaylistItem =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE youtube/v3/playlists
// -----------------------------------------------------------------------------

/// DELETE youtube/v3/playlists.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = youtube_playlists_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn youtube_playlists_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &YoutubePlaylistsDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://youtube.googleapis.com/youtube/v3/playlists",);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("id=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.on_behalf_of_content_owner {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("onBehalfOfContentOwner=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream: _,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST youtube/v3/playlists
// -----------------------------------------------------------------------------

/// POST youtube/v3/playlists.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = youtube_playlists_insert_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn youtube_playlists_insert_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &YoutubePlaylistsInsertArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Playlist>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://youtube.googleapis.com/youtube/v3/playlists",);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.on_behalf_of_content_owner {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("onBehalfOfContentOwner=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.on_behalf_of_content_owner_channel {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("onBehalfOfContentOwnerChannel=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.part {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("part=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Playlist =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET youtube/v3/playlists
// -----------------------------------------------------------------------------

/// GET youtube/v3/playlists.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = youtube_playlists_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn youtube_playlists_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &YoutubePlaylistsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<PlaylistListResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://youtube.googleapis.com/youtube/v3/playlists",);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.channel_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("channelId=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.hl {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("hl=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("id=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.mine {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("mine=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.on_behalf_of_content_owner {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("onBehalfOfContentOwner=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.on_behalf_of_content_owner_channel {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("onBehalfOfContentOwnerChannel=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.part {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("part=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: PlaylistListResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PUT youtube/v3/playlists
// -----------------------------------------------------------------------------

/// PUT youtube/v3/playlists.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = youtube_playlists_update_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn youtube_playlists_update_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &YoutubePlaylistsUpdateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Playlist>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://youtube.googleapis.com/youtube/v3/playlists",);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.on_behalf_of_content_owner {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("onBehalfOfContentOwner=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.part {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("part=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .put(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Playlist =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET youtube/v3/search
// -----------------------------------------------------------------------------

/// GET youtube/v3/search.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = youtube_search_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn youtube_search_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &YoutubeSearchListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SearchListResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://youtube.googleapis.com/youtube/v3/search",);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.channel_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("channelId=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.channel_type {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("channelType=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.event_type {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("eventType=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.for_content_owner {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("forContentOwner=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.for_developer {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("forDeveloper=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.for_mine {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("forMine=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.location {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("location=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.location_radius {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("locationRadius=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.on_behalf_of_content_owner {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("onBehalfOfContentOwner=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.order {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("order=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.part {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("part=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.published_after {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("publishedAfter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.published_before {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("publishedBefore=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.q {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("q=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.region_code {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("regionCode=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.relevance_language {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("relevanceLanguage=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.safe_search {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("safeSearch=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.topic_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("topicId=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.r#type {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("type=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.video_caption {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("videoCaption=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.video_category_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("videoCategoryId=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.video_definition {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("videoDefinition=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.video_dimension {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("videoDimension=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.video_duration {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("videoDuration=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.video_embeddable {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("videoEmbeddable=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.video_license {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("videoLicense=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.video_paid_product_placement {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("videoPaidProductPlacement=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.video_syndicated {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("videoSyndicated=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.video_type {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("videoType=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: SearchListResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE youtube/v3/subscriptions
// -----------------------------------------------------------------------------

/// DELETE youtube/v3/subscriptions.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = youtube_subscriptions_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn youtube_subscriptions_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &YoutubeSubscriptionsDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://youtube.googleapis.com/youtube/v3/subscriptions",);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("id=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream: _,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST youtube/v3/subscriptions
// -----------------------------------------------------------------------------

/// POST youtube/v3/subscriptions.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = youtube_subscriptions_insert_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn youtube_subscriptions_insert_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &YoutubeSubscriptionsInsertArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Subscription>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://youtube.googleapis.com/youtube/v3/subscriptions",);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.part {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("part=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Subscription =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET youtube/v3/subscriptions
// -----------------------------------------------------------------------------

/// GET youtube/v3/subscriptions.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = youtube_subscriptions_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn youtube_subscriptions_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &YoutubeSubscriptionsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SubscriptionListResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://youtube.googleapis.com/youtube/v3/subscriptions",);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.channel_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("channelId=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.for_channel_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("forChannelId=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("id=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.mine {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("mine=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.my_recent_subscribers {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("myRecentSubscribers=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.my_subscribers {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("mySubscribers=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.on_behalf_of_content_owner {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("onBehalfOfContentOwner=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.on_behalf_of_content_owner_channel {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("onBehalfOfContentOwnerChannel=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.order {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("order=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.part {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("part=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: SubscriptionListResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET youtube/v3/superChatEvents
// -----------------------------------------------------------------------------

/// GET youtube/v3/superChatEvents.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = youtube_super_chat_events_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn youtube_super_chat_events_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &YoutubeSuperChatEventsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SuperChatEventListResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://youtube.googleapis.com/youtube/v3/superChatEvents",);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.hl {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("hl=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.part {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("part=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: SuperChatEventListResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST youtube/v3/tests
// -----------------------------------------------------------------------------

/// POST youtube/v3/tests.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = youtube_tests_insert_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn youtube_tests_insert_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &YoutubeTestsInsertArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TestItem>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://youtube.googleapis.com/youtube/v3/tests",);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.external_channel_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("externalChannelId=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.on_behalf_of_content_owner_channel {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("onBehalfOfContentOwnerChannel=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.part {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("part=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: TestItem =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE youtube/v3/thirdPartyLinks
// -----------------------------------------------------------------------------

/// DELETE youtube/v3/thirdPartyLinks.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = youtube_third_party_links_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn youtube_third_party_links_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &YoutubeThirdPartyLinksDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://youtube.googleapis.com/youtube/v3/thirdPartyLinks",);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.external_channel_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("externalChannelId=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.linking_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("linkingToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.part {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("part=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.r#type {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("type=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream: _,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST youtube/v3/thirdPartyLinks
// -----------------------------------------------------------------------------

/// POST youtube/v3/thirdPartyLinks.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = youtube_third_party_links_insert_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn youtube_third_party_links_insert_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &YoutubeThirdPartyLinksInsertArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ThirdPartyLink>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://youtube.googleapis.com/youtube/v3/thirdPartyLinks",);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.external_channel_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("externalChannelId=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.part {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("part=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ThirdPartyLink =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET youtube/v3/thirdPartyLinks
// -----------------------------------------------------------------------------

/// GET youtube/v3/thirdPartyLinks.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = youtube_third_party_links_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn youtube_third_party_links_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &YoutubeThirdPartyLinksListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ThirdPartyLinkListResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://youtube.googleapis.com/youtube/v3/thirdPartyLinks",);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.external_channel_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("externalChannelId=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.linking_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("linkingToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.part {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("part=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.r#type {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("type=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ThirdPartyLinkListResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PUT youtube/v3/thirdPartyLinks
// -----------------------------------------------------------------------------

/// PUT youtube/v3/thirdPartyLinks.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = youtube_third_party_links_update_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn youtube_third_party_links_update_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &YoutubeThirdPartyLinksUpdateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ThirdPartyLink>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://youtube.googleapis.com/youtube/v3/thirdPartyLinks",);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.external_channel_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("externalChannelId=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.part {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("part=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .put(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ThirdPartyLink =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST youtube/v3/thumbnails/set
// -----------------------------------------------------------------------------

/// POST youtube/v3/thumbnails/set.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = youtube_thumbnails_set_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn youtube_thumbnails_set_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &YoutubeThumbnailsSetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ThumbnailSetResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://youtube.googleapis.com/youtube/v3/thumbnails/set",);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.on_behalf_of_content_owner {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("onBehalfOfContentOwner=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.video_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("videoId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ThumbnailSetResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET youtube/v3/videoAbuseReportReasons
// -----------------------------------------------------------------------------

/// GET youtube/v3/videoAbuseReportReasons.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = youtube_video_abuse_report_reasons_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn youtube_video_abuse_report_reasons_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &YoutubeVideoAbuseReportReasonsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<VideoAbuseReportReasonListResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url =
        format!("https://youtube.googleapis.com/youtube/v3/videoAbuseReportReasons",);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.hl {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("hl=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.part {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("part=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: VideoAbuseReportReasonListResponse = serde_json::from_str(&body)
                    .map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET youtube/v3/videoCategories
// -----------------------------------------------------------------------------

/// GET youtube/v3/videoCategories.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = youtube_video_categories_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn youtube_video_categories_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &YoutubeVideoCategoriesListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<VideoCategoryListResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://youtube.googleapis.com/youtube/v3/videoCategories",);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.hl {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("hl=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("id=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.part {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("part=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.region_code {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("regionCode=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: VideoCategoryListResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET youtube/v3/videoTrainability
// -----------------------------------------------------------------------------

/// GET youtube/v3/videoTrainability.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = youtube_video_trainability_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn youtube_video_trainability_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &YoutubeVideoTrainabilityGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<VideoTrainability>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://youtube.googleapis.com/youtube/v3/videoTrainability",);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("id=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: VideoTrainability =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE youtube/v3/videos
// -----------------------------------------------------------------------------

/// DELETE youtube/v3/videos.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = youtube_videos_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn youtube_videos_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &YoutubeVideosDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://youtube.googleapis.com/youtube/v3/videos",);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("id=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.on_behalf_of_content_owner {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("onBehalfOfContentOwner=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream: _,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET youtube/v3/videos/getRating
// -----------------------------------------------------------------------------

/// GET youtube/v3/videos/getRating.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = youtube_videos_get_rating_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn youtube_videos_get_rating_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &YoutubeVideosGetRatingArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<VideoGetRatingResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://youtube.googleapis.com/youtube/v3/videos/getRating",);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("id=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.on_behalf_of_content_owner {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("onBehalfOfContentOwner=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: VideoGetRatingResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST youtube/v3/videos
// -----------------------------------------------------------------------------

/// POST youtube/v3/videos.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = youtube_videos_insert_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn youtube_videos_insert_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &YoutubeVideosInsertArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Video>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://youtube.googleapis.com/youtube/v3/videos",);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.auto_levels {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("autoLevels=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.notify_subscribers {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("notifySubscribers=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.on_behalf_of_content_owner {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("onBehalfOfContentOwner=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.on_behalf_of_content_owner_channel {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("onBehalfOfContentOwnerChannel=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.part {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("part=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.stabilize {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("stabilize=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Video =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET youtube/v3/videos
// -----------------------------------------------------------------------------

/// GET youtube/v3/videos.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = youtube_videos_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn youtube_videos_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &YoutubeVideosListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<VideoListResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://youtube.googleapis.com/youtube/v3/videos",);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.chart {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("chart=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.hl {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("hl=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("id=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.locale {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("locale=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_height {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxHeight=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_width {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxWidth=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.my_rating {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("myRating=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.on_behalf_of_content_owner {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("onBehalfOfContentOwner=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.part {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("part=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.region_code {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("regionCode=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.video_category_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("videoCategoryId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: VideoListResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST youtube/v3/videos/rate
// -----------------------------------------------------------------------------

/// POST youtube/v3/videos/rate.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = youtube_videos_rate_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn youtube_videos_rate_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &YoutubeVideosRateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://youtube.googleapis.com/youtube/v3/videos/rate",);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("id=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.rating {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("rating=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream: _,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST youtube/v3/videos/reportAbuse
// -----------------------------------------------------------------------------

/// POST youtube/v3/videos/reportAbuse.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = youtube_videos_report_abuse_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn youtube_videos_report_abuse_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &YoutubeVideosReportAbuseArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://youtube.googleapis.com/youtube/v3/videos/reportAbuse",);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.on_behalf_of_content_owner {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("onBehalfOfContentOwner=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream: _,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PUT youtube/v3/videos
// -----------------------------------------------------------------------------

/// PUT youtube/v3/videos.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = youtube_videos_update_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn youtube_videos_update_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &YoutubeVideosUpdateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Video>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://youtube.googleapis.com/youtube/v3/videos",);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.on_behalf_of_content_owner {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("onBehalfOfContentOwner=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.part {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("part=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .put(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Video =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST youtube/v3/watermarks/set
// -----------------------------------------------------------------------------

/// POST youtube/v3/watermarks/set.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = youtube_watermarks_set_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn youtube_watermarks_set_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &YoutubeWatermarksSetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://youtube.googleapis.com/youtube/v3/watermarks/set",);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.channel_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("channelId=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.on_behalf_of_content_owner {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("onBehalfOfContentOwner=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream: _,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST youtube/v3/watermarks/unset
// -----------------------------------------------------------------------------

/// POST youtube/v3/watermarks/unset.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = youtube_watermarks_unset_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn youtube_watermarks_unset_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &YoutubeWatermarksUnsetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://youtube.googleapis.com/youtube/v3/watermarks/unset",);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.channel_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("channelId=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.on_behalf_of_content_owner {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("onBehalfOfContentOwner=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream: _,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PUT youtube/v3/commentThreads
// -----------------------------------------------------------------------------

/// PUT youtube/v3/commentThreads.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = youtube_youtube_v3_update_comment_threads_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn youtube_youtube_v3_update_comment_threads_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &YoutubeYoutubeV3UpdateCommentThreadsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<CommentThread>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://youtube.googleapis.com/youtube/v3/commentThreads",);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.part {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("part=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .put(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: CommentThread =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET youtube/v3/liveChat/messages/stream
// -----------------------------------------------------------------------------

/// GET youtube/v3/liveChat/messages/stream.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = youtube_youtube_v3_live_chat_messages_stream_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn youtube_youtube_v3_live_chat_messages_stream_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &YoutubeYoutubeV3LiveChatMessagesStreamArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<LiveChatMessageListResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url =
        format!("https://youtube.googleapis.com/youtube/v3/liveChat/messages/stream",);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.hl {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("hl=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.live_chat_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("liveChatId=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.part {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("part=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.profile_image_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("profileImageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: LiveChatMessageListResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}
