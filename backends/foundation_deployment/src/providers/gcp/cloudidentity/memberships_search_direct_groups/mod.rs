//! Auto-generated API module for gcp/cloudidentity memberships:searchDirectGroups.
//!
//! Generated by `cargo run --bin ewe_platform gen_api`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp_cloudidentity_memberships_search_direct_groups `

#![cfg(feature = "gcp_cloudidentity_memberships_search_direct_groups")]
#![allow(clippy::too_many_arguments, clippy::type_complexity)]
#![allow(
    clippy::missing_errors_doc,
    clippy::doc_markdown,
    clippy::useless_format
)]

use foundation_core::valtron::{execute, StreamIterator, TaskIterator, TaskIteratorExt};
use foundation_core::wire::simple_http::client::{ClientRequestBuilder, SimpleHttpClient};
use foundation_macros::JsonHash;
use serde::{Deserialize, Serialize};

use super::shared::{ApiError, ApiPending, ApiResponse};

// =============================================================================
// TYPE DECLARATIONS
// =============================================================================

/// `EntityKey` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct EntityKey {
    /// id property.
    pub id: Option<String>,
    /// namespace property.
    pub namespace: Option<String>,
}

/// `MembershipRoleRestrictionEvaluation` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct MembershipRoleRestrictionEvaluation {
    /// state property.
    pub state: Option<String>,
}

/// `MembershipRole` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct MembershipRole {
    /// expiryDetail property.
    pub expiry_detail: Option<ExpiryDetail>,
    /// name property.
    pub name: Option<String>,
    /// restrictionEvaluations property.
    pub restriction_evaluations: Option<RestrictionEvaluations>,
}

/// `RestrictionEvaluations` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct RestrictionEvaluations {
    /// memberRestrictionEvaluation property.
    pub member_restriction_evaluation: Option<MembershipRoleRestrictionEvaluation>,
}

/// `MembershipRelation` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct MembershipRelation {
    /// description property.
    pub description: Option<String>,
    /// displayName property.
    pub display_name: Option<String>,
    /// group property.
    pub group: Option<String>,
    /// groupKey property.
    pub group_key: Option<EntityKey>,
    /// labels property.
    pub labels: Option<serde_json::Value>,
    /// membership property.
    pub membership: Option<String>,
    /// roles property.
    pub roles: Option<Vec<MembershipRole>>,
}

/// `SearchDirectGroupsResponse` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct SearchDirectGroupsResponse {
    /// memberships property.
    pub memberships: Option<Vec<MembershipRelation>>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
}

/// `ExpiryDetail` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ExpiryDetail {
    /// expireTime property.
    pub expire_time: Option<String>,
}

// =============================================================================
// ARGS TYPES (per-endpoint)
// =============================================================================

/// Arguments for [`cloudidentity.groups.memberships.searchDirectGroups_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudidentityGroupsMembershipsSearchDirectGroupsArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `query`.
    pub query: Option<String>,
}

// =============================================================================
// CLIENT FUNCTIONS (per-endpoint)
// =============================================================================

// -----------------------------------------------------------------------------
// GET v1/groups/{groupsId}/memberships:searchDirectGroups
// -----------------------------------------------------------------------------

/// GET v1/groups/{groupsId}/memberships:searchDirectGroups.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = cloudidentity_groups_memberships_search_direct_groups_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn cloudidentity_groups_memberships_search_direct_groups_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SearchDirectGroupsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://cloudidentity.googleapis.com/v1/groups/{{groupsId}}/memberships:searchDirectGroups",
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: SearchDirectGroupsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}
