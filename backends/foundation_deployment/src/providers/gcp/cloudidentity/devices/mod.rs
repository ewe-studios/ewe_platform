//! Auto-generated API module for gcp/cloudidentity devices.
//!
//! Generated by `cargo run --bin ewe_platform gen_api`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp_cloudidentity_devices `

#![cfg(feature = "gcp_cloudidentity_devices")]
#![allow(clippy::too_many_arguments, clippy::type_complexity)]
#![allow(
    clippy::missing_errors_doc,
    clippy::doc_markdown,
    clippy::useless_format
)]
#![allow(unused_imports)]

use foundation_core::valtron::{TaskIterator, TaskIteratorExt};
use foundation_core::wire::simple_http::client::{ClientRequestBuilder, SimpleHttpClient};
use foundation_macros::JsonHash;
use serde::{Deserialize, Serialize};

// Import shared types used by this module
use super::shared::Operation;

use super::shared::ApiResponse;

// =============================================================================
// TYPE DECLARATIONS
// =============================================================================

/// `GoogleAppsCloudidentityDevicesV1Device` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct GoogleAppsCloudidentityDevicesV1Device {
    /// androidSpecificAttributes property.
    pub android_specific_attributes: Option<GoogleAppsCloudidentityDevicesV1AndroidAttributes>,
    /// assetTag property.
    pub asset_tag: Option<String>,
    /// basebandVersion property.
    pub baseband_version: Option<String>,
    /// bootloaderVersion property.
    pub bootloader_version: Option<String>,
    /// brand property.
    pub brand: Option<String>,
    /// buildNumber property.
    pub build_number: Option<String>,
    /// compromisedState property.
    pub compromised_state: Option<String>,
    /// createTime property.
    pub create_time: Option<String>,
    /// deviceId property.
    pub device_id: Option<String>,
    /// deviceType property.
    pub device_type: Option<String>,
    /// enabledDeveloperOptions property.
    pub enabled_developer_options: Option<bool>,
    /// enabledUsbDebugging property.
    pub enabled_usb_debugging: Option<bool>,
    /// encryptionState property.
    pub encryption_state: Option<String>,
    /// endpointVerificationSpecificAttributes property.
    pub endpoint_verification_specific_attributes:
        Option<GoogleAppsCloudidentityDevicesV1EndpointVerificationSpecificAttributes>,
    /// hostname property.
    pub hostname: Option<String>,
    /// imei property.
    pub imei: Option<String>,
    /// kernelVersion property.
    pub kernel_version: Option<String>,
    /// lastSyncTime property.
    pub last_sync_time: Option<String>,
    /// managementState property.
    pub management_state: Option<String>,
    /// manufacturer property.
    pub manufacturer: Option<String>,
    /// meid property.
    pub meid: Option<String>,
    /// model property.
    pub model: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// networkOperator property.
    pub network_operator: Option<String>,
    /// osVersion property.
    pub os_version: Option<String>,
    /// otherAccounts property.
    pub other_accounts: Option<Vec<String>>,
    /// ownerType property.
    pub owner_type: Option<String>,
    /// releaseVersion property.
    pub release_version: Option<String>,
    /// securityPatchTime property.
    pub security_patch_time: Option<String>,
    /// serialNumber property.
    pub serial_number: Option<String>,
    /// unifiedDeviceId property.
    pub unified_device_id: Option<String>,
    /// wifiMacAddresses property.
    pub wifi_mac_addresses: Option<Vec<String>>,
}

/// `GoogleAppsCloudidentityDevicesV1AndroidAttributes` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct GoogleAppsCloudidentityDevicesV1AndroidAttributes {
    /// ctsProfileMatch property.
    pub cts_profile_match: Option<bool>,
    /// enabledUnknownSources property.
    pub enabled_unknown_sources: Option<bool>,
    /// hasPotentiallyHarmfulApps property.
    pub has_potentially_harmful_apps: Option<bool>,
    /// ownerProfileAccount property.
    pub owner_profile_account: Option<bool>,
    /// ownershipPrivilege property.
    pub ownership_privilege: Option<String>,
    /// supportsWorkProfile property.
    pub supports_work_profile: Option<bool>,
    /// verifiedBoot property.
    pub verified_boot: Option<bool>,
    /// verifyAppsEnabled property.
    pub verify_apps_enabled: Option<bool>,
}

/// `GoogleAppsCloudidentityDevicesV1BrowserAttributes` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct GoogleAppsCloudidentityDevicesV1BrowserAttributes {
    /// chromeBrowserInfo property.
    pub chrome_browser_info: Option<GoogleAppsCloudidentityDevicesV1BrowserInfo>,
    /// chromeProfileId property.
    pub chrome_profile_id: Option<String>,
    /// lastProfileSyncTime property.
    pub last_profile_sync_time: Option<String>,
}

/// `GoogleAppsCloudidentityDevicesV1CertificateAttributes` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct GoogleAppsCloudidentityDevicesV1CertificateAttributes {
    /// certificateTemplate property.
    pub certificate_template: Option<GoogleAppsCloudidentityDevicesV1CertificateTemplate>,
    /// fingerprint property.
    pub fingerprint: Option<String>,
    /// issuer property.
    pub issuer: Option<String>,
    /// serialNumber property.
    pub serial_number: Option<String>,
    /// subject property.
    pub subject: Option<String>,
    /// thumbprint property.
    pub thumbprint: Option<String>,
    /// validationState property.
    pub validation_state: Option<String>,
    /// validityExpirationTime property.
    pub validity_expiration_time: Option<String>,
    /// validityStartTime property.
    pub validity_start_time: Option<String>,
}

/// `GoogleAppsCloudidentityDevicesV1BrowserInfo` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct GoogleAppsCloudidentityDevicesV1BrowserInfo {
    /// browserManagementState property.
    pub browser_management_state: Option<String>,
    /// browserVersion property.
    pub browser_version: Option<String>,
    /// isBuiltInDnsClientEnabled property.
    pub is_built_in_dns_client_enabled: Option<bool>,
    /// isBulkDataEntryAnalysisEnabled property.
    pub is_bulk_data_entry_analysis_enabled: Option<bool>,
    /// isChromeCleanupEnabled property.
    pub is_chrome_cleanup_enabled: Option<bool>,
    /// isChromeRemoteDesktopAppBlocked property.
    pub is_chrome_remote_desktop_app_blocked: Option<bool>,
    /// isFileDownloadAnalysisEnabled property.
    pub is_file_download_analysis_enabled: Option<bool>,
    /// isFileUploadAnalysisEnabled property.
    pub is_file_upload_analysis_enabled: Option<bool>,
    /// isRealtimeUrlCheckEnabled property.
    pub is_realtime_url_check_enabled: Option<bool>,
    /// isSecurityEventAnalysisEnabled property.
    pub is_security_event_analysis_enabled: Option<bool>,
    /// isSiteIsolationEnabled property.
    pub is_site_isolation_enabled: Option<bool>,
    /// isThirdPartyBlockingEnabled property.
    pub is_third_party_blocking_enabled: Option<bool>,
    /// passwordProtectionWarningTrigger property.
    pub password_protection_warning_trigger: Option<String>,
    /// safeBrowsingProtectionLevel property.
    pub safe_browsing_protection_level: Option<String>,
}

/// `Status` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Status {
    /// code property.
    pub code: Option<i64>,
    /// details property.
    pub details: Option<Vec<serde_json::Value>>,
    /// message property.
    pub message: Option<String>,
}

/// `GoogleAppsCloudidentityDevicesV1ListDevicesResponse` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct GoogleAppsCloudidentityDevicesV1ListDevicesResponse {
    /// devices property.
    pub devices: Option<Vec<GoogleAppsCloudidentityDevicesV1Device>>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
}

/// `GoogleAppsCloudidentityDevicesV1CertificateTemplate` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct GoogleAppsCloudidentityDevicesV1CertificateTemplate {
    /// id property.
    pub id: Option<String>,
    /// majorVersion property.
    pub major_version: Option<i64>,
    /// minorVersion property.
    pub minor_version: Option<i64>,
}

/// `GoogleAppsCloudidentityDevicesV1EndpointVerificationSpecificAttributes` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct GoogleAppsCloudidentityDevicesV1EndpointVerificationSpecificAttributes {
    /// additionalSignals property.
    pub additional_signals: Option<serde_json::Value>,
    /// browserAttributes property.
    pub browser_attributes: Option<Vec<GoogleAppsCloudidentityDevicesV1BrowserAttributes>>,
    /// certificateAttributes property.
    pub certificate_attributes: Option<Vec<GoogleAppsCloudidentityDevicesV1CertificateAttributes>>,
}

// =============================================================================
// ARGS TYPES (per-endpoint)
// =============================================================================

/// Arguments for [`cloudidentity.devices.cancelWipe_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudidentityDevicesCancelWipeArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`cloudidentity.devices.create_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudidentityDevicesCreateArgs {
    /// Query parameter: `customer`.
    pub customer: Option<String>,
}

/// Arguments for [`cloudidentity.devices.delete_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudidentityDevicesDeleteArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `customer`.
    pub customer: Option<String>,
}

/// Arguments for [`cloudidentity.devices.get_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudidentityDevicesGetArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `customer`.
    pub customer: Option<String>,
}

/// Arguments for [`cloudidentity.devices.list_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudidentityDevicesListArgs {
    /// Query parameter: `customer`.
    pub customer: Option<String>,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `view`.
    pub view: Option<String>,
}

/// Arguments for [`cloudidentity.devices.wipe_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudidentityDevicesWipeArgs {
    /// Path parameter: `name`.
    pub name: String,
}

// =============================================================================
// CLIENT FUNCTIONS (per-endpoint)
// =============================================================================

// -----------------------------------------------------------------------------
// POST v1/devices/{devicesId}:cancelWipe
// -----------------------------------------------------------------------------

/// POST v1/devices/{devicesId}:cancelWipe.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = cloudidentity_devices_cancel_wipe_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn cloudidentity_devices_cancel_wipe_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url =
        format!("https://cloudidentity.googleapis.com/v1/devices/{{devicesId}}:cancelWipe",);

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/devices
// -----------------------------------------------------------------------------

/// POST v1/devices.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = cloudidentity_devices_create_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn cloudidentity_devices_create_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://cloudidentity.googleapis.com/v1/devices",);

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE v1/devices/{devicesId}
// -----------------------------------------------------------------------------

/// DELETE v1/devices/{devicesId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = cloudidentity_devices_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn cloudidentity_devices_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://cloudidentity.googleapis.com/v1/devices/{{devicesId}}",);

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/devices/{devicesId}
// -----------------------------------------------------------------------------

/// GET v1/devices/{devicesId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = cloudidentity_devices_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn cloudidentity_devices_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleAppsCloudidentityDevicesV1Device>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://cloudidentity.googleapis.com/v1/devices/{{devicesId}}",);

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleAppsCloudidentityDevicesV1Device = serde_json::from_str(&body)
                    .map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/devices
// -----------------------------------------------------------------------------

/// GET v1/devices.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = cloudidentity_devices_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn cloudidentity_devices_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleAppsCloudidentityDevicesV1ListDevicesResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://cloudidentity.googleapis.com/v1/devices",);

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleAppsCloudidentityDevicesV1ListDevicesResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/devices/{devicesId}:wipe
// -----------------------------------------------------------------------------

/// POST v1/devices/{devicesId}:wipe.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = cloudidentity_devices_wipe_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn cloudidentity_devices_wipe_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url =
        format!("https://cloudidentity.googleapis.com/v1/devices/{{devicesId}}:wipe",);

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}
