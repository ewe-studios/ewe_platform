//! Auto-generated API module for gcp/servicecontrol servicecontrol.
//!
//! Generated by `cargo run --bin ewe_platform gen_api`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp_servicecontrol_servicecontrol `

#![cfg(feature = "gcp_servicecontrol_servicecontrol")]
#![allow(clippy::too_many_arguments, clippy::type_complexity)]
#![allow(
    clippy::missing_errors_doc,
    clippy::doc_markdown,
    clippy::useless_format
)]
#![allow(unused_imports)]

use foundation_core::valtron::{TaskIterator, TaskIteratorExt};
use foundation_core::wire::simple_http::client::{ClientRequestBuilder, SimpleHttpClient};
use foundation_macros::JsonHash;
use serde::{Deserialize, Serialize};

use super::shared::ApiResponse;

// =============================================================================
// TYPE DECLARATIONS
// =============================================================================

/// `Resource` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Resource {
    /// annotations property.
    pub annotations: Option<serde_json::Value>,
    /// createTime property.
    pub create_time: Option<String>,
    /// deleteTime property.
    pub delete_time: Option<String>,
    /// displayName property.
    pub display_name: Option<String>,
    /// etag property.
    pub etag: Option<String>,
    /// labels property.
    pub labels: Option<serde_json::Value>,
    /// location property.
    pub location: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// service property.
    pub service: Option<String>,
    /// type property.
    pub r#type: Option<String>,
    /// uid property.
    pub uid: Option<String>,
    /// updateTime property.
    pub update_time: Option<String>,
}

/// `Peer` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Peer {
    /// ip property.
    pub ip: Option<String>,
    /// labels property.
    pub labels: Option<serde_json::Value>,
    /// port property.
    pub port: Option<String>,
    /// principal property.
    pub principal: Option<String>,
    /// regionCode property.
    pub region_code: Option<String>,
}

/// `CheckRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct CheckRequest {
    /// attributes property.
    pub attributes: Option<AttributeContext>,
    /// flags property.
    pub flags: Option<String>,
    /// resources property.
    pub resources: Option<Vec<ResourceInfo>>,
    /// serviceConfigId property.
    pub service_config_id: Option<String>,
}

/// `ResourceInfo` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ResourceInfo {
    /// container property.
    pub container: Option<String>,
    /// location property.
    pub location: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// permission property.
    pub permission: Option<String>,
    /// type property.
    pub r#type: Option<String>,
}

/// `ReportResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ReportResponse {
    /// extensions property.
    pub extensions: Option<serde_json::Value>,
}

/// `AttributeContext` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AttributeContext {
    /// api property.
    pub api: Option<Api>,
    /// destination property.
    pub destination: Option<Peer>,
    /// extensions property.
    pub extensions: Option<Vec<serde_json::Value>>,
    /// origin property.
    pub origin: Option<Peer>,
    /// request property.
    pub request: Option<Request>,
    /// resource property.
    pub resource: Option<Resource>,
    /// response property.
    pub response: Option<Response>,
    /// source property.
    pub source: Option<Peer>,
}

/// `ReportRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ReportRequest {
    /// operations property.
    pub operations: Option<Vec<AttributeContext>>,
    /// serviceConfigId property.
    pub service_config_id: Option<String>,
}

/// `Response` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Response {
    /// backendLatency property.
    pub backend_latency: Option<String>,
    /// code property.
    pub code: Option<String>,
    /// headers property.
    pub headers: Option<serde_json::Value>,
    /// size property.
    pub size: Option<String>,
    /// time property.
    pub time: Option<String>,
}

/// `Status` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Status {
    /// code property.
    pub code: Option<i64>,
    /// details property.
    pub details: Option<Vec<serde_json::Value>>,
    /// message property.
    pub message: Option<String>,
}

/// `CheckResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct CheckResponse {
    /// dynamicMetadata property.
    pub dynamic_metadata: Option<serde_json::Value>,
    /// headers property.
    pub headers: Option<serde_json::Value>,
    /// status property.
    pub status: Option<Status>,
}

/// `Auth` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Auth {
    /// accessLevels property.
    pub access_levels: Option<Vec<String>>,
    /// audiences property.
    pub audiences: Option<Vec<String>>,
    /// claims property.
    pub claims: Option<serde_json::Value>,
    /// oauth property.
    pub oauth: Option<Oauth>,
    /// presenter property.
    pub presenter: Option<String>,
    /// principal property.
    pub principal: Option<String>,
}

/// `Request` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Request {
    /// auth property.
    pub auth: Option<Auth>,
    /// headers property.
    pub headers: Option<serde_json::Value>,
    /// host property.
    pub host: Option<String>,
    /// id property.
    pub id: Option<String>,
    /// method property.
    pub method: Option<String>,
    /// origin property.
    pub origin: Option<String>,
    /// path property.
    pub path: Option<String>,
    /// protocol property.
    pub protocol: Option<String>,
    /// query property.
    pub query: Option<String>,
    /// reason property.
    pub reason: Option<String>,
    /// scheme property.
    pub scheme: Option<String>,
    /// size property.
    pub size: Option<String>,
    /// time property.
    pub time: Option<String>,
}

/// `Api` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Api {
    /// operation property.
    pub operation: Option<String>,
    /// protocol property.
    pub protocol: Option<String>,
    /// service property.
    pub service: Option<String>,
    /// version property.
    pub version: Option<String>,
}

/// `Oauth` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Oauth {
    /// clientId property.
    pub client_id: Option<String>,
}

// =============================================================================
// ARGS TYPES (per-endpoint)
// =============================================================================

/// Arguments for [`servicecontrol.services.check_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ServicecontrolServicesCheckArgs {
    /// Path parameter: `serviceName`.
    pub service_name: String,
    /// Request body.
    pub body: CheckRequest,
}

/// Arguments for [`servicecontrol.services.report_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ServicecontrolServicesReportArgs {
    /// Path parameter: `serviceName`.
    pub service_name: String,
    /// Request body.
    pub body: ReportRequest,
}

// =============================================================================
// CLIENT FUNCTIONS (per-endpoint)
// =============================================================================

// -----------------------------------------------------------------------------
// POST v2/services/{serviceName}:check
// -----------------------------------------------------------------------------

/// POST v2/services/{serviceName}:check.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = servicecontrol_services_check_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn servicecontrol_services_check_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ServicecontrolServicesCheckArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<CheckResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://servicecontrol.googleapis.com/v2/services/{}:check",
        args.service_name,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: CheckResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v2/services/{serviceName}:report
// -----------------------------------------------------------------------------

/// POST v2/services/{serviceName}:report.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = servicecontrol_services_report_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn servicecontrol_services_report_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ServicecontrolServicesReportArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ReportResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://servicecontrol.googleapis.com/v2/services/{}:report",
        args.service_name,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ReportResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}
