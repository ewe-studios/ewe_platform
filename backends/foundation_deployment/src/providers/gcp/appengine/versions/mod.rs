//! Auto-generated API module for gcp/appengine versions.
//!
//! Generated by `cargo run --bin ewe_platform gen_api`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp_appengine_versions `

#![cfg(feature = "gcp_appengine_versions")]
#![allow(clippy::too_many_arguments, clippy::type_complexity)]
#![allow(
    clippy::missing_errors_doc,
    clippy::doc_markdown,
    clippy::useless_format
)]
#![allow(unused_imports)]

use foundation_core::valtron::{TaskIterator, TaskIteratorExt};
use foundation_core::wire::simple_http::client::{ClientRequestBuilder, SimpleHttpClient};
use foundation_macros::JsonHash;
use serde::{Deserialize, Serialize};

// Import shared types used by this module
use super::shared::Operation;

use super::shared::ApiResponse;

// =============================================================================
// TYPE DECLARATIONS
// =============================================================================

/// `VpcAccessConnector` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct VpcAccessConnector {
    /// egressSetting property.
    pub egress_setting: Option<String>,
    /// name property.
    pub name: Option<String>,
}

/// `LivenessCheck` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct LivenessCheck {
    /// checkInterval property.
    pub check_interval: Option<String>,
    /// failureThreshold property.
    pub failure_threshold: Option<i64>,
    /// host property.
    pub host: Option<String>,
    /// initialDelay property.
    pub initial_delay: Option<String>,
    /// path property.
    pub path: Option<String>,
    /// successThreshold property.
    pub success_threshold: Option<i64>,
    /// timeout property.
    pub timeout: Option<String>,
}

/// `NetworkUtilization` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct NetworkUtilization {
    /// targetReceivedBytesPerSecond property.
    pub target_received_bytes_per_second: Option<i64>,
    /// targetReceivedPacketsPerSecond property.
    pub target_received_packets_per_second: Option<i64>,
    /// targetSentBytesPerSecond property.
    pub target_sent_bytes_per_second: Option<i64>,
    /// targetSentPacketsPerSecond property.
    pub target_sent_packets_per_second: Option<i64>,
}

/// `Status` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Status {
    /// code property.
    pub code: Option<i64>,
    /// details property.
    pub details: Option<Vec<serde_json::Value>>,
    /// message property.
    pub message: Option<String>,
}

/// `ApiEndpointHandler` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ApiEndpointHandler {
    /// scriptPath property.
    pub script_path: Option<String>,
}

/// `StaticFilesHandler` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct StaticFilesHandler {
    /// applicationReadable property.
    pub application_readable: Option<bool>,
    /// expiration property.
    pub expiration: Option<String>,
    /// httpHeaders property.
    pub http_headers: Option<serde_json::Value>,
    /// mimeType property.
    pub mime_type: Option<String>,
    /// path property.
    pub path: Option<String>,
    /// requireMatchingFile property.
    pub require_matching_file: Option<bool>,
    /// uploadPathRegex property.
    pub upload_path_regex: Option<String>,
}

/// `FlexibleRuntimeSettings` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct FlexibleRuntimeSettings {
    /// operatingSystem property.
    pub operating_system: Option<String>,
    /// runtimeVersion property.
    pub runtime_version: Option<String>,
}

/// `DiskUtilization` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct DiskUtilization {
    /// targetReadBytesPerSecond property.
    pub target_read_bytes_per_second: Option<i64>,
    /// targetReadOpsPerSecond property.
    pub target_read_ops_per_second: Option<i64>,
    /// targetWriteBytesPerSecond property.
    pub target_write_bytes_per_second: Option<i64>,
    /// targetWriteOpsPerSecond property.
    pub target_write_ops_per_second: Option<i64>,
}

/// `BasicScaling` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct BasicScaling {
    /// idleTimeout property.
    pub idle_timeout: Option<String>,
    /// maxInstances property.
    pub max_instances: Option<i64>,
}

/// `ZipInfo` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ZipInfo {
    /// filesCount property.
    pub files_count: Option<i64>,
    /// sourceUrl property.
    pub source_url: Option<String>,
}

/// `CpuUtilization` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct CpuUtilization {
    /// aggregationWindowLength property.
    pub aggregation_window_length: Option<String>,
    /// targetUtilization property.
    pub target_utilization: Option<f64>,
}

/// `Version` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Version {
    /// apiConfig property.
    pub api_config: Option<ApiConfigHandler>,
    /// appEngineApis property.
    pub app_engine_apis: Option<bool>,
    /// automaticScaling property.
    pub automatic_scaling: Option<AutomaticScaling>,
    /// basicScaling property.
    pub basic_scaling: Option<BasicScaling>,
    /// betaSettings property.
    pub beta_settings: Option<serde_json::Value>,
    /// buildEnvVariables property.
    pub build_env_variables: Option<serde_json::Value>,
    /// createTime property.
    pub create_time: Option<String>,
    /// createdBy property.
    pub created_by: Option<String>,
    /// defaultExpiration property.
    pub default_expiration: Option<String>,
    /// deployment property.
    pub deployment: Option<Deployment>,
    /// diskUsageBytes property.
    pub disk_usage_bytes: Option<String>,
    /// endpointsApiService property.
    pub endpoints_api_service: Option<EndpointsApiService>,
    /// entrypoint property.
    pub entrypoint: Option<Entrypoint>,
    /// env property.
    pub env: Option<String>,
    /// envVariables property.
    pub env_variables: Option<serde_json::Value>,
    /// errorHandlers property.
    pub error_handlers: Option<Vec<ErrorHandler>>,
    /// flexibleRuntimeSettings property.
    pub flexible_runtime_settings: Option<FlexibleRuntimeSettings>,
    /// generatedCustomerMetadata property.
    pub generated_customer_metadata: Option<serde_json::Value>,
    /// handlers property.
    pub handlers: Option<Vec<UrlMap>>,
    /// healthCheck property.
    pub health_check: Option<HealthCheck>,
    /// id property.
    pub id: Option<String>,
    /// inboundServices property.
    pub inbound_services: Option<Vec<String>>,
    /// instanceClass property.
    pub instance_class: Option<String>,
    /// libraries property.
    pub libraries: Option<Vec<Library>>,
    /// livenessCheck property.
    pub liveness_check: Option<LivenessCheck>,
    /// manualScaling property.
    pub manual_scaling: Option<ManualScaling>,
    /// name property.
    pub name: Option<String>,
    /// network property.
    pub network: Option<Network>,
    /// nobuildFilesRegex property.
    pub nobuild_files_regex: Option<String>,
    /// readinessCheck property.
    pub readiness_check: Option<ReadinessCheck>,
    /// resources property.
    pub resources: Option<Resources>,
    /// runtime property.
    pub runtime: Option<String>,
    /// runtimeApiVersion property.
    pub runtime_api_version: Option<String>,
    /// runtimeChannel property.
    pub runtime_channel: Option<String>,
    /// runtimeMainExecutablePath property.
    pub runtime_main_executable_path: Option<String>,
    /// serviceAccount property.
    pub service_account: Option<String>,
    /// servingStatus property.
    pub serving_status: Option<String>,
    /// threadsafe property.
    pub threadsafe: Option<bool>,
    /// versionUrl property.
    pub version_url: Option<String>,
    /// vm property.
    pub vm: Option<bool>,
    /// vpcAccessConnector property.
    pub vpc_access_connector: Option<VpcAccessConnector>,
    /// zones property.
    pub zones: Option<Vec<String>>,
}

/// `Entrypoint` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Entrypoint {
    /// shell property.
    pub shell: Option<String>,
}

/// `ScriptHandler` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ScriptHandler {
    /// scriptPath property.
    pub script_path: Option<String>,
}

/// `ReadinessCheck` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ReadinessCheck {
    /// appStartTimeout property.
    pub app_start_timeout: Option<String>,
    /// checkInterval property.
    pub check_interval: Option<String>,
    /// failureThreshold property.
    pub failure_threshold: Option<i64>,
    /// host property.
    pub host: Option<String>,
    /// path property.
    pub path: Option<String>,
    /// successThreshold property.
    pub success_threshold: Option<i64>,
    /// timeout property.
    pub timeout: Option<String>,
}

/// `Resources` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Resources {
    /// cpu property.
    pub cpu: Option<f64>,
    /// diskGb property.
    pub disk_gb: Option<f64>,
    /// kmsKeyReference property.
    pub kms_key_reference: Option<String>,
    /// memoryGb property.
    pub memory_gb: Option<f64>,
    /// volumes property.
    pub volumes: Option<Vec<Volume>>,
}

/// `ListVersionsResponse` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ListVersionsResponse {
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// versions property.
    pub versions: Option<Vec<Version>>,
}

/// `Deployment` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Deployment {
    /// cloudBuildOptions property.
    pub cloud_build_options: Option<CloudBuildOptions>,
    /// container property.
    pub container: Option<ContainerInfo>,
    /// files property.
    pub files: Option<serde_json::Value>,
    /// zip property.
    pub zip: Option<ZipInfo>,
}

/// `ManualScaling` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ManualScaling {
    /// instances property.
    pub instances: Option<i64>,
}

/// `AutomaticScaling` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct AutomaticScaling {
    /// coolDownPeriod property.
    pub cool_down_period: Option<String>,
    /// cpuUtilization property.
    pub cpu_utilization: Option<CpuUtilization>,
    /// diskUtilization property.
    pub disk_utilization: Option<DiskUtilization>,
    /// maxConcurrentRequests property.
    pub max_concurrent_requests: Option<i64>,
    /// maxIdleInstances property.
    pub max_idle_instances: Option<i64>,
    /// maxPendingLatency property.
    pub max_pending_latency: Option<String>,
    /// maxTotalInstances property.
    pub max_total_instances: Option<i64>,
    /// minIdleInstances property.
    pub min_idle_instances: Option<i64>,
    /// minPendingLatency property.
    pub min_pending_latency: Option<String>,
    /// minTotalInstances property.
    pub min_total_instances: Option<i64>,
    /// networkUtilization property.
    pub network_utilization: Option<NetworkUtilization>,
    /// requestUtilization property.
    pub request_utilization: Option<RequestUtilization>,
    /// standardSchedulerSettings property.
    pub standard_scheduler_settings: Option<StandardSchedulerSettings>,
}

/// `Volume` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Volume {
    /// name property.
    pub name: Option<String>,
    /// sizeGb property.
    pub size_gb: Option<f64>,
    /// volumeType property.
    pub volume_type: Option<String>,
}

/// `StandardSchedulerSettings` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct StandardSchedulerSettings {
    /// maxInstances property.
    pub max_instances: Option<i64>,
    /// minInstances property.
    pub min_instances: Option<i64>,
    /// targetCpuUtilization property.
    pub target_cpu_utilization: Option<f64>,
    /// targetThroughputUtilization property.
    pub target_throughput_utilization: Option<f64>,
}

/// `RequestUtilization` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct RequestUtilization {
    /// targetConcurrentRequests property.
    pub target_concurrent_requests: Option<i64>,
    /// targetRequestCountPerSecond property.
    pub target_request_count_per_second: Option<i64>,
}

/// `ContainerInfo` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ContainerInfo {
    /// image property.
    pub image: Option<String>,
}

/// `Network` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Network {
    /// forwardedPorts property.
    pub forwarded_ports: Option<Vec<String>>,
    /// instanceIpMode property.
    pub instance_ip_mode: Option<String>,
    /// instanceTag property.
    pub instance_tag: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// sessionAffinity property.
    pub session_affinity: Option<bool>,
    /// subnetworkName property.
    pub subnetwork_name: Option<String>,
}

/// `CloudBuildOptions` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct CloudBuildOptions {
    /// appYamlPath property.
    pub app_yaml_path: Option<String>,
    /// cloudBuildTimeout property.
    pub cloud_build_timeout: Option<String>,
}

/// `ErrorHandler` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ErrorHandler {
    /// errorCode property.
    pub error_code: Option<String>,
    /// mimeType property.
    pub mime_type: Option<String>,
    /// staticFile property.
    pub static_file: Option<String>,
}

/// `UrlMap` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct UrlMap {
    /// apiEndpoint property.
    pub api_endpoint: Option<ApiEndpointHandler>,
    /// authFailAction property.
    pub auth_fail_action: Option<String>,
    /// login property.
    pub login: Option<String>,
    /// redirectHttpResponseCode property.
    pub redirect_http_response_code: Option<String>,
    /// script property.
    pub script: Option<ScriptHandler>,
    /// securityLevel property.
    pub security_level: Option<String>,
    /// staticFiles property.
    pub static_files: Option<StaticFilesHandler>,
    /// urlRegex property.
    pub url_regex: Option<String>,
}

/// `HealthCheck` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct HealthCheck {
    /// checkInterval property.
    pub check_interval: Option<String>,
    /// disableHealthCheck property.
    pub disable_health_check: Option<bool>,
    /// healthyThreshold property.
    pub healthy_threshold: Option<i64>,
    /// host property.
    pub host: Option<String>,
    /// restartThreshold property.
    pub restart_threshold: Option<i64>,
    /// timeout property.
    pub timeout: Option<String>,
    /// unhealthyThreshold property.
    pub unhealthy_threshold: Option<i64>,
}

/// `EndpointsApiService` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct EndpointsApiService {
    /// configId property.
    pub config_id: Option<String>,
    /// disableTraceSampling property.
    pub disable_trace_sampling: Option<bool>,
    /// name property.
    pub name: Option<String>,
    /// rolloutStrategy property.
    pub rollout_strategy: Option<String>,
}

/// `ApiConfigHandler` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ApiConfigHandler {
    /// authFailAction property.
    pub auth_fail_action: Option<String>,
    /// login property.
    pub login: Option<String>,
    /// script property.
    pub script: Option<String>,
    /// securityLevel property.
    pub security_level: Option<String>,
    /// url property.
    pub url: Option<String>,
}

/// `Library` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Library {
    /// name property.
    pub name: Option<String>,
    /// version property.
    pub version: Option<String>,
}

// =============================================================================
// ARGS TYPES (per-endpoint)
// =============================================================================

/// Arguments for [`appengine.apps.services.versions.create_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AppengineAppsServicesVersionsCreateArgs {
    /// Path parameter: `appsId`.
    pub apps_id: String,
    /// Path parameter: `servicesId`.
    pub services_id: String,
}

/// Arguments for [`appengine.apps.services.versions.delete_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AppengineAppsServicesVersionsDeleteArgs {
    /// Path parameter: `appsId`.
    pub apps_id: String,
    /// Path parameter: `servicesId`.
    pub services_id: String,
    /// Path parameter: `versionsId`.
    pub versions_id: String,
}

/// Arguments for [`appengine.apps.services.versions.exportAppImage_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AppengineAppsServicesVersionsExportAppImageArgs {
    /// Path parameter: `appsId`.
    pub apps_id: String,
    /// Path parameter: `servicesId`.
    pub services_id: String,
    /// Path parameter: `versionsId`.
    pub versions_id: String,
}

/// Arguments for [`appengine.apps.services.versions.get_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AppengineAppsServicesVersionsGetArgs {
    /// Path parameter: `appsId`.
    pub apps_id: String,
    /// Path parameter: `servicesId`.
    pub services_id: String,
    /// Path parameter: `versionsId`.
    pub versions_id: String,
    /// Query parameter: `view`.
    pub view: Option<String>,
}

/// Arguments for [`appengine.apps.services.versions.list_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AppengineAppsServicesVersionsListArgs {
    /// Path parameter: `appsId`.
    pub apps_id: String,
    /// Path parameter: `servicesId`.
    pub services_id: String,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `view`.
    pub view: Option<String>,
}

/// Arguments for [`appengine.apps.services.versions.patch_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AppengineAppsServicesVersionsPatchArgs {
    /// Path parameter: `appsId`.
    pub apps_id: String,
    /// Path parameter: `servicesId`.
    pub services_id: String,
    /// Path parameter: `versionsId`.
    pub versions_id: String,
    /// Query parameter: `updateMask`.
    pub update_mask: Option<String>,
}

/// Arguments for [`appengine.projects.locations.applications.services.versions.delete_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AppengineProjectsLocationsApplicationsServicesVersionsDeleteArgs {
    /// Path parameter: `projectsId`.
    pub projects_id: String,
    /// Path parameter: `locationsId`.
    pub locations_id: String,
    /// Path parameter: `applicationsId`.
    pub applications_id: String,
    /// Path parameter: `servicesId`.
    pub services_id: String,
    /// Path parameter: `versionsId`.
    pub versions_id: String,
}

/// Arguments for [`appengine.projects.locations.applications.services.versions.exportAppImage_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AppengineProjectsLocationsApplicationsServicesVersionsExportAppImageArgs {
    /// Path parameter: `projectsId`.
    pub projects_id: String,
    /// Path parameter: `locationsId`.
    pub locations_id: String,
    /// Path parameter: `applicationsId`.
    pub applications_id: String,
    /// Path parameter: `servicesId`.
    pub services_id: String,
    /// Path parameter: `versionsId`.
    pub versions_id: String,
}

/// Arguments for [`appengine.projects.locations.applications.services.versions.patch_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AppengineProjectsLocationsApplicationsServicesVersionsPatchArgs {
    /// Path parameter: `projectsId`.
    pub projects_id: String,
    /// Path parameter: `locationsId`.
    pub locations_id: String,
    /// Path parameter: `applicationsId`.
    pub applications_id: String,
    /// Path parameter: `servicesId`.
    pub services_id: String,
    /// Path parameter: `versionsId`.
    pub versions_id: String,
    /// Query parameter: `updateMask`.
    pub update_mask: Option<String>,
}

// =============================================================================
// CLIENT FUNCTIONS (per-endpoint)
// =============================================================================

// -----------------------------------------------------------------------------
// POST v1/apps/{appsId}/services/{servicesId}/versions
// -----------------------------------------------------------------------------

/// POST v1/apps/{appsId}/services/{servicesId}/versions.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = appengine_apps_services_versions_create_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn appengine_apps_services_versions_create_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &AppengineAppsServicesVersionsCreateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://appengine.googleapis.com/v1/apps/{}/services/{}/versions",
        args.apps_id, args.services_id,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE v1/apps/{appsId}/services/{servicesId}/versions/{versionsId}
// -----------------------------------------------------------------------------

/// DELETE v1/apps/{appsId}/services/{servicesId}/versions/{versionsId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = appengine_apps_services_versions_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn appengine_apps_services_versions_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &AppengineAppsServicesVersionsDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://appengine.googleapis.com/v1/apps/{}/services/{}/versions/{}",
        args.apps_id, args.services_id, args.versions_id,
    );

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/apps/{appsId}/services/{servicesId}/versions/{versionsId}:exportAppImage
// -----------------------------------------------------------------------------

/// POST v1/apps/{appsId}/services/{servicesId}/versions/{versionsId}:exportAppImage.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = appengine_apps_services_versions_export_app_image_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn appengine_apps_services_versions_export_app_image_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &AppengineAppsServicesVersionsExportAppImageArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://appengine.googleapis.com/v1/apps/{}/services/{}/versions/{}:exportAppImage",
        args.apps_id, args.services_id, args.versions_id,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/apps/{appsId}/services/{servicesId}/versions/{versionsId}
// -----------------------------------------------------------------------------

/// GET v1/apps/{appsId}/services/{servicesId}/versions/{versionsId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = appengine_apps_services_versions_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn appengine_apps_services_versions_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &AppengineAppsServicesVersionsGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Version>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://appengine.googleapis.com/v1/apps/{}/services/{}/versions/{}",
        args.apps_id, args.services_id, args.versions_id,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Version =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/apps/{appsId}/services/{servicesId}/versions
// -----------------------------------------------------------------------------

/// GET v1/apps/{appsId}/services/{servicesId}/versions.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = appengine_apps_services_versions_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn appengine_apps_services_versions_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &AppengineAppsServicesVersionsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListVersionsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://appengine.googleapis.com/v1/apps/{}/services/{}/versions",
        args.apps_id, args.services_id,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ListVersionsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PATCH v1/apps/{appsId}/services/{servicesId}/versions/{versionsId}
// -----------------------------------------------------------------------------

/// PATCH v1/apps/{appsId}/services/{servicesId}/versions/{versionsId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = appengine_apps_services_versions_patch_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn appengine_apps_services_versions_patch_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &AppengineAppsServicesVersionsPatchArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://appengine.googleapis.com/v1/apps/{}/services/{}/versions/{}",
        args.apps_id, args.services_id, args.versions_id,
    );

    let mut builder = client
        .patch(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE v1/projects/{projectsId}/locations/{locationsId}/applications/{applicationsId}/services/{servicesId}/versions/{versionsId}
// -----------------------------------------------------------------------------

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/applications/{applicationsId}/services/{servicesId}/versions/{versionsId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = appengine_projects_locations_applications_services_versions_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn appengine_projects_locations_applications_services_versions_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &AppengineProjectsLocationsApplicationsServicesVersionsDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://appengine.googleapis.com/v1/projects/{}/locations/{}/applications/{}/services/{}/versions/{}",
        args.projects_id,
        args.locations_id,
        args.applications_id,
        args.services_id,
        args.versions_id,
    );

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/projects/{projectsId}/locations/{locationsId}/applications/{applicationsId}/services/{servicesId}/versions/{versionsId}:exportAppImage
// -----------------------------------------------------------------------------

/// POST v1/projects/{projectsId}/locations/{locationsId}/applications/{applicationsId}/services/{servicesId}/versions/{versionsId}:exportAppImage.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = appengine_projects_locations_applications_services_versions_export_app_image_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn appengine_projects_locations_applications_services_versions_export_app_image_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &AppengineProjectsLocationsApplicationsServicesVersionsExportAppImageArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://appengine.googleapis.com/v1/projects/{}/locations/{}/applications/{}/services/{}/versions/{}:exportAppImage",
        args.projects_id,
        args.locations_id,
        args.applications_id,
        args.services_id,
        args.versions_id,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PATCH v1/projects/{projectsId}/locations/{locationsId}/applications/{applicationsId}/services/{servicesId}/versions/{versionsId}
// -----------------------------------------------------------------------------

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/applications/{applicationsId}/services/{servicesId}/versions/{versionsId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = appengine_projects_locations_applications_services_versions_patch_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn appengine_projects_locations_applications_services_versions_patch_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &AppengineProjectsLocationsApplicationsServicesVersionsPatchArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://appengine.googleapis.com/v1/projects/{}/locations/{}/applications/{}/services/{}/versions/{}",
        args.projects_id,
        args.locations_id,
        args.applications_id,
        args.services_id,
        args.versions_id,
    );

    let mut builder = client
        .patch(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}
