//! Auto-generated API module for gcp/areainsights areainsights.
//!
//! Generated by `cargo run --bin ewe_platform gen_api`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp_areainsights_areainsights `

#![cfg(feature = "gcp_areainsights_areainsights")]
#![allow(clippy::too_many_arguments, clippy::type_complexity)]
#![allow(
    clippy::missing_errors_doc,
    clippy::doc_markdown,
    clippy::useless_format
)]
#![allow(unused_imports)]

use foundation_core::valtron::{TaskIterator, TaskIteratorExt};
use foundation_core::wire::simple_http::client::{ClientRequestBuilder, SimpleHttpClient};
use foundation_macros::JsonHash;
use serde::{Deserialize, Serialize};

use super::shared::ApiResponse;

// =============================================================================
// TYPE DECLARATIONS
// =============================================================================

/// `LocationFilter` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct LocationFilter {
    /// circle property.
    pub circle: Option<Circle>,
    /// customArea property.
    pub custom_area: Option<CustomArea>,
    /// region property.
    pub region: Option<Region>,
}

/// `Filter` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Filter {
    /// locationFilter property.
    pub location_filter: Option<LocationFilter>,
    /// operatingStatus property.
    pub operating_status: Option<Vec<String>>,
    /// priceLevels property.
    pub price_levels: Option<Vec<String>>,
    /// ratingFilter property.
    pub rating_filter: Option<RatingFilter>,
    /// typeFilter property.
    pub type_filter: Option<TypeFilter>,
}

/// `TypeFilter` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct TypeFilter {
    /// excludedPrimaryTypes property.
    pub excluded_primary_types: Option<Vec<String>>,
    /// excludedTypes property.
    pub excluded_types: Option<Vec<String>>,
    /// includedPrimaryTypes property.
    pub included_primary_types: Option<Vec<String>>,
    /// includedTypes property.
    pub included_types: Option<Vec<String>>,
}

/// `LatLng` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct LatLng {
    /// latitude property.
    pub latitude: Option<f64>,
    /// longitude property.
    pub longitude: Option<f64>,
}

/// `CustomArea` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct CustomArea {
    /// polygon property.
    pub polygon: Option<Polygon>,
}

/// `Circle` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Circle {
    /// latLng property.
    pub lat_lng: Option<LatLng>,
    /// place property.
    pub place: Option<String>,
    /// radius property.
    pub radius: Option<i64>,
}

/// `Region` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Region {
    /// place property.
    pub place: Option<String>,
}

/// `Polygon` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Polygon {
    /// coordinates property.
    pub coordinates: Option<Vec<LatLng>>,
}

/// `ComputeInsightsResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ComputeInsightsResponse {
    /// count property.
    pub count: Option<String>,
    /// placeInsights property.
    pub place_insights: Option<Vec<PlaceInsight>>,
}

/// `PlaceInsight` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct PlaceInsight {
    /// place property.
    pub place: Option<String>,
}

/// `ComputeInsightsRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ComputeInsightsRequest {
    /// filter property.
    pub filter: Option<Filter>,
    /// insights property.
    pub insights: Option<Vec<String>>,
}

/// `RatingFilter` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct RatingFilter {
    /// maxRating property.
    pub max_rating: Option<f64>,
    /// minRating property.
    pub min_rating: Option<f64>,
}

// =============================================================================
// ARGS TYPES (per-endpoint)
// =============================================================================

/// Arguments for [`areainsights.computeInsights_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct AreainsightsComputeInsightsArgs {
    /// Request body.
    pub body: ComputeInsightsRequest,
}

// =============================================================================
// CLIENT FUNCTIONS (per-endpoint)
// =============================================================================

// -----------------------------------------------------------------------------
// POST v1:computeInsights
// -----------------------------------------------------------------------------

/// POST v1:computeInsights.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = areainsights_compute_insights_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn areainsights_compute_insights_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &AreainsightsComputeInsightsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ComputeInsightsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://areainsights.googleapis.com/v1:computeInsights",);

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ComputeInsightsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}
