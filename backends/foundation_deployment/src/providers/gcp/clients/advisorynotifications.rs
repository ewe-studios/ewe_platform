//! Auto-generated API clients for Google Cloud Platform - advisorynotifications.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp`
//!

#![cfg(feature = "gcp")]

//! Feature flag: `gcp/advisorynotifications`

#![cfg(feature = "gcp/advisorynotifications")]

use foundation_core::valtron::{execute, StreamIterator, StreamIteratorExt};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};
use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;

/// GET v1/{+name}
/// Get notification settings.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `advisorynotifications_organizations_locations_get_settings_execute()` to send, or `advisorynotifications_organizations_locations_get_settings` for simplest API.

pub fn advisorynotifications_organizations_locations_get_settings_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {

    // Build URL
    let url = format!(
        "https://api.example.comv1/{+name}",
        name,
    );

    // Build request
    client.get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Get notification settings.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `advisorynotifications_organizations_locations_get_settings_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from advisorynotifications_organizations_locations_get_settings_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn advisorynotifications_organizations_locations_get_settings_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<GoogleCloudAdvisorynotificationsV1Settings>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAdvisorynotificationsV1Settings = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Get notification settings.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `advisorynotifications_organizations_locations_get_settings_builder()` + `advisorynotifications_organizations_locations_get_settings_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn advisorynotifications_organizations_locations_get_settings(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<GoogleCloudAdvisorynotificationsV1Settings>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let builder = advisorynotifications_organizations_locations_get_settings_builder(client, name)?;
    advisorynotifications_organizations_locations_get_settings_execute(builder)
}

/// GET v1/{+name}
/// Update notification settings.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `advisorynotifications_organizations_locations_update_settings_execute()` to send, or `advisorynotifications_organizations_locations_update_settings` for simplest API.

pub fn advisorynotifications_organizations_locations_update_settings_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudAdvisorynotificationsV1Settings
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {

    // Build URL
    let url = format!(
        "https://api.example.comv1/{+name}",
        name,
    );

    // Build request
    client.get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))

        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+name}
/// Update notification settings.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `advisorynotifications_organizations_locations_update_settings_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from advisorynotifications_organizations_locations_update_settings_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn advisorynotifications_organizations_locations_update_settings_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<GoogleCloudAdvisorynotificationsV1Settings>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAdvisorynotificationsV1Settings = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Update notification settings.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `advisorynotifications_organizations_locations_update_settings_builder()` + `advisorynotifications_organizations_locations_update_settings_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn advisorynotifications_organizations_locations_update_settings(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudAdvisorynotificationsV1Settings,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<GoogleCloudAdvisorynotificationsV1Settings>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let builder = advisorynotifications_organizations_locations_update_settings_builder(client, name, body)?;
    advisorynotifications_organizations_locations_update_settings_execute(builder)
}

/// GET v1/{+name}
/// Gets a notification.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `advisorynotifications_organizations_locations_notifications_get_execute()` to send, or `advisorynotifications_organizations_locations_notifications_get` for simplest API.

pub fn advisorynotifications_organizations_locations_notifications_get_builder(
    client: &SimpleHttpClient,
    name: &str,
    languageCode: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {

    // Build URL
    let url = format!(
        "https://api.example.comv1/{+name}",
        name,
    );

    // Build request
    client.get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))

        .query(
            "languageCode",
            languageCode.as_ref().map(|s| s.to_string()).unwrap_or_default().as_str(),
        )
}

/// GET v1/{+name}
/// Gets a notification.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `advisorynotifications_organizations_locations_notifications_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from advisorynotifications_organizations_locations_notifications_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn advisorynotifications_organizations_locations_notifications_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<GoogleCloudAdvisorynotificationsV1Notification>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAdvisorynotificationsV1Notification = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Gets a notification.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `advisorynotifications_organizations_locations_notifications_get_builder()` + `advisorynotifications_organizations_locations_notifications_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn advisorynotifications_organizations_locations_notifications_get(
    client: &SimpleHttpClient,
    name: &str,
    languageCode: Option<&str>,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<GoogleCloudAdvisorynotificationsV1Notification>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let builder = advisorynotifications_organizations_locations_notifications_get_builder(client, name, languageCode)?;
    advisorynotifications_organizations_locations_notifications_get_execute(builder)
}

/// GET v1/{+parent}/notifications
/// Lists notifications under a given parent.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `advisorynotifications_organizations_locations_notifications_list_execute()` to send, or `advisorynotifications_organizations_locations_notifications_list` for simplest API.

pub fn advisorynotifications_organizations_locations_notifications_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    languageCode: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    view: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {

    // Build URL
    let url = format!(
        "https://api.example.comv1/{+parent}/notifications",
        parent,
    );

    // Build request
    client.get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))

        .query(
            "languageCode",
            languageCode.as_ref().map(|s| s.to_string()).unwrap_or_default().as_str(),
        )

        .query(
            "pageSize",
            pageSize.as_ref().map(|s| s.to_string()).unwrap_or_default().as_str(),
        )

        .query(
            "pageToken",
            pageToken.as_ref().map(|s| s.to_string()).unwrap_or_default().as_str(),
        )

        .query(
            "view",
            view.as_ref().map(|s| s.to_string()).unwrap_or_default().as_str(),
        )
}

/// GET v1/{+parent}/notifications
/// Lists notifications under a given parent.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `advisorynotifications_organizations_locations_notifications_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from advisorynotifications_organizations_locations_notifications_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn advisorynotifications_organizations_locations_notifications_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<GoogleCloudAdvisorynotificationsV1ListNotificationsResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAdvisorynotificationsV1ListNotificationsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/notifications
/// Lists notifications under a given parent.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `advisorynotifications_organizations_locations_notifications_list_builder()` + `advisorynotifications_organizations_locations_notifications_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn advisorynotifications_organizations_locations_notifications_list(
    client: &SimpleHttpClient,
    parent: &str,
    languageCode: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    view: Option<&str>,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<GoogleCloudAdvisorynotificationsV1ListNotificationsResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let builder = advisorynotifications_organizations_locations_notifications_list_builder(client, parent, languageCode, pageSize, pageToken, view)?;
    advisorynotifications_organizations_locations_notifications_list_execute(builder)
}

/// GET v1/{+name}
/// Get notification settings.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `advisorynotifications_projects_locations_get_settings_execute()` to send, or `advisorynotifications_projects_locations_get_settings` for simplest API.

pub fn advisorynotifications_projects_locations_get_settings_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {

    // Build URL
    let url = format!(
        "https://api.example.comv1/{+name}",
        name,
    );

    // Build request
    client.get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Get notification settings.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `advisorynotifications_projects_locations_get_settings_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from advisorynotifications_projects_locations_get_settings_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn advisorynotifications_projects_locations_get_settings_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<GoogleCloudAdvisorynotificationsV1Settings>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAdvisorynotificationsV1Settings = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Get notification settings.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `advisorynotifications_projects_locations_get_settings_builder()` + `advisorynotifications_projects_locations_get_settings_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn advisorynotifications_projects_locations_get_settings(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<GoogleCloudAdvisorynotificationsV1Settings>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let builder = advisorynotifications_projects_locations_get_settings_builder(client, name)?;
    advisorynotifications_projects_locations_get_settings_execute(builder)
}

/// GET v1/{+name}
/// Update notification settings.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `advisorynotifications_projects_locations_update_settings_execute()` to send, or `advisorynotifications_projects_locations_update_settings` for simplest API.

pub fn advisorynotifications_projects_locations_update_settings_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudAdvisorynotificationsV1Settings
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {

    // Build URL
    let url = format!(
        "https://api.example.comv1/{+name}",
        name,
    );

    // Build request
    client.get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))

        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+name}
/// Update notification settings.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `advisorynotifications_projects_locations_update_settings_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from advisorynotifications_projects_locations_update_settings_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn advisorynotifications_projects_locations_update_settings_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<GoogleCloudAdvisorynotificationsV1Settings>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAdvisorynotificationsV1Settings = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Update notification settings.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `advisorynotifications_projects_locations_update_settings_builder()` + `advisorynotifications_projects_locations_update_settings_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn advisorynotifications_projects_locations_update_settings(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleCloudAdvisorynotificationsV1Settings,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<GoogleCloudAdvisorynotificationsV1Settings>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let builder = advisorynotifications_projects_locations_update_settings_builder(client, name, body)?;
    advisorynotifications_projects_locations_update_settings_execute(builder)
}

/// GET v1/{+name}
/// Gets a notification.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `advisorynotifications_projects_locations_notifications_get_execute()` to send, or `advisorynotifications_projects_locations_notifications_get` for simplest API.

pub fn advisorynotifications_projects_locations_notifications_get_builder(
    client: &SimpleHttpClient,
    name: &str,
    languageCode: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {

    // Build URL
    let url = format!(
        "https://api.example.comv1/{+name}",
        name,
    );

    // Build request
    client.get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))

        .query(
            "languageCode",
            languageCode.as_ref().map(|s| s.to_string()).unwrap_or_default().as_str(),
        )
}

/// GET v1/{+name}
/// Gets a notification.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `advisorynotifications_projects_locations_notifications_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from advisorynotifications_projects_locations_notifications_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn advisorynotifications_projects_locations_notifications_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<GoogleCloudAdvisorynotificationsV1Notification>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAdvisorynotificationsV1Notification = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Gets a notification.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `advisorynotifications_projects_locations_notifications_get_builder()` + `advisorynotifications_projects_locations_notifications_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn advisorynotifications_projects_locations_notifications_get(
    client: &SimpleHttpClient,
    name: &str,
    languageCode: Option<&str>,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<GoogleCloudAdvisorynotificationsV1Notification>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let builder = advisorynotifications_projects_locations_notifications_get_builder(client, name, languageCode)?;
    advisorynotifications_projects_locations_notifications_get_execute(builder)
}

/// GET v1/{+parent}/notifications
/// Lists notifications under a given parent.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `advisorynotifications_projects_locations_notifications_list_execute()` to send, or `advisorynotifications_projects_locations_notifications_list` for simplest API.

pub fn advisorynotifications_projects_locations_notifications_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    languageCode: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    view: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {

    // Build URL
    let url = format!(
        "https://api.example.comv1/{+parent}/notifications",
        parent,
    );

    // Build request
    client.get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))

        .query(
            "languageCode",
            languageCode.as_ref().map(|s| s.to_string()).unwrap_or_default().as_str(),
        )

        .query(
            "pageSize",
            pageSize.as_ref().map(|s| s.to_string()).unwrap_or_default().as_str(),
        )

        .query(
            "pageToken",
            pageToken.as_ref().map(|s| s.to_string()).unwrap_or_default().as_str(),
        )

        .query(
            "view",
            view.as_ref().map(|s| s.to_string()).unwrap_or_default().as_str(),
        )
}

/// GET v1/{+parent}/notifications
/// Lists notifications under a given parent.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `advisorynotifications_projects_locations_notifications_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from advisorynotifications_projects_locations_notifications_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn advisorynotifications_projects_locations_notifications_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<GoogleCloudAdvisorynotificationsV1ListNotificationsResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAdvisorynotificationsV1ListNotificationsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/notifications
/// Lists notifications under a given parent.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `advisorynotifications_projects_locations_notifications_list_builder()` + `advisorynotifications_projects_locations_notifications_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn advisorynotifications_projects_locations_notifications_list(
    client: &SimpleHttpClient,
    parent: &str,
    languageCode: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    view: Option<&str>,
) -> Result<
    impl StreamIterator<
        D = Result<ApiResponse<GoogleCloudAdvisorynotificationsV1ListNotificationsResponse>, ApiError>,
        P = ApiPending
    > + Send + 'static,
    ApiError,
> {

    let builder = advisorynotifications_projects_locations_notifications_list_builder(client, parent, languageCode, pageSize, pageToken, view)?;
    advisorynotifications_projects_locations_notifications_list_execute(builder)
}

