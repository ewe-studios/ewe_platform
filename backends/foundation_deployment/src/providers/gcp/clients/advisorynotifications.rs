//! Auto-generated API clients for gcp/advisorynotifications.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp`

#![cfg(feature = "gcp")]


use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{
    execute, BoxedSendExecutionAction, StreamIterator, StreamIteratorExt, TaskIterator,
    TaskIteratorExt,
};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, DnsResolver, RequestIntro, SimpleHttpClient,
    SystemDnsResolver,
};
use foundation_db::state::resource_identifier::ResourceIdentifier;
use foundation_macros::JsonHash;
use serde::Serialize;

/// GET v1/organizations/{organizationsId}/locations/{locationsId}/settings
/// Get notification settings.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `advisorynotifications_organizations_locations_get_settings_execute()` to send, or `advisorynotifications_organizations_locations_get_settings` for simplest API.

pub fn advisorynotifications_organizations_locations_get_settings_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://advisorynotifications.googleapis.com/v1/organizations/{}/locations/{locationsId}/settings",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/locations/{locationsId}/settings
/// Get notification settings.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `advisorynotifications_organizations_locations_get_settings_execute()` or `advisorynotifications_organizations_locations_get_settings`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `advisorynotifications_organizations_locations_get_settings_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn advisorynotifications_organizations_locations_get_settings_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudAdvisorynotificationsV1Settings>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAdvisorynotificationsV1Settings =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/organizations/{organizationsId}/locations/{locationsId}/settings
/// Get notification settings.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `advisorynotifications_organizations_locations_get_settings_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `advisorynotifications_organizations_locations_get_settings_task()`.
/// For the simplest API, use `advisorynotifications_organizations_locations_get_settings()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `advisorynotifications_organizations_locations_get_settings_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn advisorynotifications_organizations_locations_get_settings_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAdvisorynotificationsV1Settings>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = advisorynotifications_organizations_locations_get_settings_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`advisorynotifications_organizations_locations_get_settings`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AdvisorynotificationsOrganizationsLocationsGetSettingsArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/organizations/{organizationsId}/locations/{locationsId}/settings
/// Get notification settings.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `advisorynotifications_organizations_locations_get_settings_builder()` + `advisorynotifications_organizations_locations_get_settings_execute()`.
/// For task-level control, use `advisorynotifications_organizations_locations_get_settings_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn advisorynotifications_organizations_locations_get_settings(
    client: &SimpleHttpClient,
    args: &AdvisorynotificationsOrganizationsLocationsGetSettingsArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAdvisorynotificationsV1Settings>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        advisorynotifications_organizations_locations_get_settings_builder(client, &args.name)?;
    advisorynotifications_organizations_locations_get_settings_execute(builder)
}

/// PATCH v1/organizations/{organizationsId}/locations/{locationsId}/settings
/// Update notification settings.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `advisorynotifications_organizations_locations_update_settings_execute()` to send, or `advisorynotifications_organizations_locations_update_settings` for simplest API.

pub fn advisorynotifications_organizations_locations_update_settings_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://advisorynotifications.googleapis.com/v1/organizations/{}/locations/{locationsId}/settings",
        name,
    );

    // Build request
    let builder = client
        .patch(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v1/organizations/{organizationsId}/locations/{locationsId}/settings
/// Update notification settings.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `advisorynotifications_organizations_locations_update_settings_execute()` or `advisorynotifications_organizations_locations_update_settings`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `advisorynotifications_organizations_locations_update_settings_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn advisorynotifications_organizations_locations_update_settings_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudAdvisorynotificationsV1Settings>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAdvisorynotificationsV1Settings =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v1/organizations/{organizationsId}/locations/{locationsId}/settings
/// Update notification settings.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `advisorynotifications_organizations_locations_update_settings_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `advisorynotifications_organizations_locations_update_settings_task()`.
/// For the simplest API, use `advisorynotifications_organizations_locations_update_settings()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `advisorynotifications_organizations_locations_update_settings_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn advisorynotifications_organizations_locations_update_settings_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAdvisorynotificationsV1Settings>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = advisorynotifications_organizations_locations_update_settings_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`advisorynotifications_organizations_locations_update_settings`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AdvisorynotificationsOrganizationsLocationsUpdateSettingsArgs {
    /// Path parameter: name
    pub name: String,
}

/// PATCH v1/organizations/{organizationsId}/locations/{locationsId}/settings
/// Update notification settings.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `advisorynotifications_organizations_locations_update_settings_builder()` + `advisorynotifications_organizations_locations_update_settings_execute()`.
/// For task-level control, use `advisorynotifications_organizations_locations_update_settings_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn advisorynotifications_organizations_locations_update_settings(
    client: &SimpleHttpClient,
    args: &AdvisorynotificationsOrganizationsLocationsUpdateSettingsArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAdvisorynotificationsV1Settings>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        advisorynotifications_organizations_locations_update_settings_builder(client, &args.name)?;
    advisorynotifications_organizations_locations_update_settings_execute(builder)
}

/// GET v1/organizations/{organizationsId}/locations/{locationsId}/notifications/{notificationsId}
/// Gets a notification.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `advisorynotifications_organizations_locations_notifications_get_execute()` to send, or `advisorynotifications_organizations_locations_notifications_get` for simplest API.

pub fn advisorynotifications_organizations_locations_notifications_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    languageCode: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://advisorynotifications.googleapis.com/v1/organizations/{}/locations/{locationsId}/notifications/{notificationsId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = languageCode.as_ref() {
        query_parts.push(format!("languageCode={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/locations/{locationsId}/notifications/{notificationsId}
/// Gets a notification.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `advisorynotifications_organizations_locations_notifications_get_execute()` or `advisorynotifications_organizations_locations_notifications_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `advisorynotifications_organizations_locations_notifications_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn advisorynotifications_organizations_locations_notifications_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudAdvisorynotificationsV1Notification>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAdvisorynotificationsV1Notification =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/organizations/{organizationsId}/locations/{locationsId}/notifications/{notificationsId}
/// Gets a notification.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `advisorynotifications_organizations_locations_notifications_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `advisorynotifications_organizations_locations_notifications_get_task()`.
/// For the simplest API, use `advisorynotifications_organizations_locations_notifications_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `advisorynotifications_organizations_locations_notifications_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn advisorynotifications_organizations_locations_notifications_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAdvisorynotificationsV1Notification>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = advisorynotifications_organizations_locations_notifications_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`advisorynotifications_organizations_locations_notifications_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AdvisorynotificationsOrganizationsLocationsNotificationsGetArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: languageCode
    pub languageCode: Option<String>,
}

/// GET v1/organizations/{organizationsId}/locations/{locationsId}/notifications/{notificationsId}
/// Gets a notification.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `advisorynotifications_organizations_locations_notifications_get_builder()` + `advisorynotifications_organizations_locations_notifications_get_execute()`.
/// For task-level control, use `advisorynotifications_organizations_locations_notifications_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn advisorynotifications_organizations_locations_notifications_get(
    client: &SimpleHttpClient,
    args: &AdvisorynotificationsOrganizationsLocationsNotificationsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAdvisorynotificationsV1Notification>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = advisorynotifications_organizations_locations_notifications_get_builder(
        client,
        &args.name,
        &args.languageCode,
    )?;
    advisorynotifications_organizations_locations_notifications_get_execute(builder)
}

/// GET v1/organizations/{organizationsId}/locations/{locationsId}/notifications
/// Lists notifications under a given parent.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `advisorynotifications_organizations_locations_notifications_list_execute()` to send, or `advisorynotifications_organizations_locations_notifications_list` for simplest API.

pub fn advisorynotifications_organizations_locations_notifications_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    languageCode: &Option<String>,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
    view: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://advisorynotifications.googleapis.com/v1/organizations/{}/locations/{locationsId}/notifications",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = languageCode.as_ref() {
        query_parts.push(format!("languageCode={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = view.as_ref() {
        query_parts.push(format!("view={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/locations/{locationsId}/notifications
/// Lists notifications under a given parent.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `advisorynotifications_organizations_locations_notifications_list_execute()` or `advisorynotifications_organizations_locations_notifications_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `advisorynotifications_organizations_locations_notifications_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn advisorynotifications_organizations_locations_notifications_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudAdvisorynotificationsV1ListNotificationsResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAdvisorynotificationsV1ListNotificationsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/organizations/{organizationsId}/locations/{locationsId}/notifications
/// Lists notifications under a given parent.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `advisorynotifications_organizations_locations_notifications_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `advisorynotifications_organizations_locations_notifications_list_task()`.
/// For the simplest API, use `advisorynotifications_organizations_locations_notifications_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `advisorynotifications_organizations_locations_notifications_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn advisorynotifications_organizations_locations_notifications_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudAdvisorynotificationsV1ListNotificationsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = advisorynotifications_organizations_locations_notifications_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`advisorynotifications_organizations_locations_notifications_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AdvisorynotificationsOrganizationsLocationsNotificationsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: languageCode
    pub languageCode: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: view
    pub view: Option<String>,
}

/// GET v1/organizations/{organizationsId}/locations/{locationsId}/notifications
/// Lists notifications under a given parent.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `advisorynotifications_organizations_locations_notifications_list_builder()` + `advisorynotifications_organizations_locations_notifications_list_execute()`.
/// For task-level control, use `advisorynotifications_organizations_locations_notifications_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn advisorynotifications_organizations_locations_notifications_list(
    client: &SimpleHttpClient,
    args: &AdvisorynotificationsOrganizationsLocationsNotificationsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudAdvisorynotificationsV1ListNotificationsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = advisorynotifications_organizations_locations_notifications_list_builder(
        client,
        &args.parent,
        &args.languageCode,
        &args.pageSize,
        &args.pageToken,
        &args.view,
    )?;
    advisorynotifications_organizations_locations_notifications_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/settings
/// Get notification settings.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `advisorynotifications_projects_locations_get_settings_execute()` to send, or `advisorynotifications_projects_locations_get_settings` for simplest API.

pub fn advisorynotifications_projects_locations_get_settings_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://advisorynotifications.googleapis.com/v1/projects/{}/locations/{locationsId}/settings",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/settings
/// Get notification settings.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `advisorynotifications_projects_locations_get_settings_execute()` or `advisorynotifications_projects_locations_get_settings`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `advisorynotifications_projects_locations_get_settings_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn advisorynotifications_projects_locations_get_settings_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudAdvisorynotificationsV1Settings>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAdvisorynotificationsV1Settings =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/settings
/// Get notification settings.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `advisorynotifications_projects_locations_get_settings_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `advisorynotifications_projects_locations_get_settings_task()`.
/// For the simplest API, use `advisorynotifications_projects_locations_get_settings()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `advisorynotifications_projects_locations_get_settings_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn advisorynotifications_projects_locations_get_settings_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAdvisorynotificationsV1Settings>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = advisorynotifications_projects_locations_get_settings_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`advisorynotifications_projects_locations_get_settings`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AdvisorynotificationsProjectsLocationsGetSettingsArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/settings
/// Get notification settings.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `advisorynotifications_projects_locations_get_settings_builder()` + `advisorynotifications_projects_locations_get_settings_execute()`.
/// For task-level control, use `advisorynotifications_projects_locations_get_settings_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn advisorynotifications_projects_locations_get_settings(
    client: &SimpleHttpClient,
    args: &AdvisorynotificationsProjectsLocationsGetSettingsArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAdvisorynotificationsV1Settings>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        advisorynotifications_projects_locations_get_settings_builder(client, &args.name)?;
    advisorynotifications_projects_locations_get_settings_execute(builder)
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/settings
/// Update notification settings.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `advisorynotifications_projects_locations_update_settings_execute()` to send, or `advisorynotifications_projects_locations_update_settings` for simplest API.

pub fn advisorynotifications_projects_locations_update_settings_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://advisorynotifications.googleapis.com/v1/projects/{}/locations/{locationsId}/settings",
        name,
    );

    // Build request
    let builder = client
        .patch(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/settings
/// Update notification settings.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `advisorynotifications_projects_locations_update_settings_execute()` or `advisorynotifications_projects_locations_update_settings`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `advisorynotifications_projects_locations_update_settings_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn advisorynotifications_projects_locations_update_settings_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudAdvisorynotificationsV1Settings>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAdvisorynotificationsV1Settings =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/settings
/// Update notification settings.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `advisorynotifications_projects_locations_update_settings_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `advisorynotifications_projects_locations_update_settings_task()`.
/// For the simplest API, use `advisorynotifications_projects_locations_update_settings()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `advisorynotifications_projects_locations_update_settings_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn advisorynotifications_projects_locations_update_settings_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAdvisorynotificationsV1Settings>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = advisorynotifications_projects_locations_update_settings_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`advisorynotifications_projects_locations_update_settings`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AdvisorynotificationsProjectsLocationsUpdateSettingsArgs {
    /// Path parameter: name
    pub name: String,
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/settings
/// Update notification settings.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `advisorynotifications_projects_locations_update_settings_builder()` + `advisorynotifications_projects_locations_update_settings_execute()`.
/// For task-level control, use `advisorynotifications_projects_locations_update_settings_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn advisorynotifications_projects_locations_update_settings(
    client: &SimpleHttpClient,
    args: &AdvisorynotificationsProjectsLocationsUpdateSettingsArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAdvisorynotificationsV1Settings>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        advisorynotifications_projects_locations_update_settings_builder(client, &args.name)?;
    advisorynotifications_projects_locations_update_settings_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/notifications/{notificationsId}
/// Gets a notification.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `advisorynotifications_projects_locations_notifications_get_execute()` to send, or `advisorynotifications_projects_locations_notifications_get` for simplest API.

pub fn advisorynotifications_projects_locations_notifications_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    languageCode: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://advisorynotifications.googleapis.com/v1/projects/{}/locations/{locationsId}/notifications/{notificationsId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = languageCode.as_ref() {
        query_parts.push(format!("languageCode={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/notifications/{notificationsId}
/// Gets a notification.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `advisorynotifications_projects_locations_notifications_get_execute()` or `advisorynotifications_projects_locations_notifications_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `advisorynotifications_projects_locations_notifications_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn advisorynotifications_projects_locations_notifications_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudAdvisorynotificationsV1Notification>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAdvisorynotificationsV1Notification =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/notifications/{notificationsId}
/// Gets a notification.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `advisorynotifications_projects_locations_notifications_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `advisorynotifications_projects_locations_notifications_get_task()`.
/// For the simplest API, use `advisorynotifications_projects_locations_notifications_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `advisorynotifications_projects_locations_notifications_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn advisorynotifications_projects_locations_notifications_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAdvisorynotificationsV1Notification>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = advisorynotifications_projects_locations_notifications_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`advisorynotifications_projects_locations_notifications_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AdvisorynotificationsProjectsLocationsNotificationsGetArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: languageCode
    pub languageCode: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/notifications/{notificationsId}
/// Gets a notification.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `advisorynotifications_projects_locations_notifications_get_builder()` + `advisorynotifications_projects_locations_notifications_get_execute()`.
/// For task-level control, use `advisorynotifications_projects_locations_notifications_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn advisorynotifications_projects_locations_notifications_get(
    client: &SimpleHttpClient,
    args: &AdvisorynotificationsProjectsLocationsNotificationsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudAdvisorynotificationsV1Notification>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = advisorynotifications_projects_locations_notifications_get_builder(
        client,
        &args.name,
        &args.languageCode,
    )?;
    advisorynotifications_projects_locations_notifications_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/notifications
/// Lists notifications under a given parent.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `advisorynotifications_projects_locations_notifications_list_execute()` to send, or `advisorynotifications_projects_locations_notifications_list` for simplest API.

pub fn advisorynotifications_projects_locations_notifications_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    languageCode: &Option<String>,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
    view: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://advisorynotifications.googleapis.com/v1/projects/{}/locations/{locationsId}/notifications",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = languageCode.as_ref() {
        query_parts.push(format!("languageCode={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = view.as_ref() {
        query_parts.push(format!("view={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/notifications
/// Lists notifications under a given parent.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `advisorynotifications_projects_locations_notifications_list_execute()` or `advisorynotifications_projects_locations_notifications_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `advisorynotifications_projects_locations_notifications_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn advisorynotifications_projects_locations_notifications_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudAdvisorynotificationsV1ListNotificationsResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudAdvisorynotificationsV1ListNotificationsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/notifications
/// Lists notifications under a given parent.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `advisorynotifications_projects_locations_notifications_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `advisorynotifications_projects_locations_notifications_list_task()`.
/// For the simplest API, use `advisorynotifications_projects_locations_notifications_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `advisorynotifications_projects_locations_notifications_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn advisorynotifications_projects_locations_notifications_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudAdvisorynotificationsV1ListNotificationsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = advisorynotifications_projects_locations_notifications_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`advisorynotifications_projects_locations_notifications_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AdvisorynotificationsProjectsLocationsNotificationsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: languageCode
    pub languageCode: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: view
    pub view: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/notifications
/// Lists notifications under a given parent.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `advisorynotifications_projects_locations_notifications_list_builder()` + `advisorynotifications_projects_locations_notifications_list_execute()`.
/// For task-level control, use `advisorynotifications_projects_locations_notifications_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn advisorynotifications_projects_locations_notifications_list(
    client: &SimpleHttpClient,
    args: &AdvisorynotificationsProjectsLocationsNotificationsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudAdvisorynotificationsV1ListNotificationsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = advisorynotifications_projects_locations_notifications_list_builder(
        client,
        &args.parent,
        &args.languageCode,
        &args.pageSize,
        &args.pageToken,
        &args.view,
    )?;
    advisorynotifications_projects_locations_notifications_list_execute(builder)
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudAdvisorynotificationsV1Settings
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudAdvisorynotificationsV1Settings with AdvisorynotificationsOrganizationsLocationsGetSettingsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<AdvisorynotificationsOrganizationsLocationsGetSettingsArgs>
    for GoogleCloudAdvisorynotificationsV1Settings
{
    fn generate_resource_id(
        &self,
        input: &AdvisorynotificationsOrganizationsLocationsGetSettingsArgs,
    ) -> String {
        format!(
            "gcp::advisorynotifications::GoogleCloudAdvisorynotificationsV1Settings/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::advisorynotifications::GoogleCloudAdvisorynotificationsV1Settings"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudAdvisorynotificationsV1Settings
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudAdvisorynotificationsV1Settings with AdvisorynotificationsOrganizationsLocationsUpdateSettingsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<AdvisorynotificationsOrganizationsLocationsUpdateSettingsArgs>
    for GoogleCloudAdvisorynotificationsV1Settings
{
    fn generate_resource_id(
        &self,
        input: &AdvisorynotificationsOrganizationsLocationsUpdateSettingsArgs,
    ) -> String {
        format!(
            "gcp::advisorynotifications::GoogleCloudAdvisorynotificationsV1Settings/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::advisorynotifications::GoogleCloudAdvisorynotificationsV1Settings"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudAdvisorynotificationsV1Notification
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudAdvisorynotificationsV1Notification with AdvisorynotificationsOrganizationsLocationsNotificationsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<AdvisorynotificationsOrganizationsLocationsNotificationsGetArgs>
    for GoogleCloudAdvisorynotificationsV1Notification
{
    fn generate_resource_id(
        &self,
        input: &AdvisorynotificationsOrganizationsLocationsNotificationsGetArgs,
    ) -> String {
        format!(
            "gcp::advisorynotifications::GoogleCloudAdvisorynotificationsV1Notification/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::advisorynotifications::GoogleCloudAdvisorynotificationsV1Notification"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudAdvisorynotificationsV1ListNotificationsResponse
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudAdvisorynotificationsV1ListNotificationsResponse with AdvisorynotificationsOrganizationsLocationsNotificationsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<AdvisorynotificationsOrganizationsLocationsNotificationsListArgs>
    for GoogleCloudAdvisorynotificationsV1ListNotificationsResponse
{
    fn generate_resource_id(
        &self,
        input: &AdvisorynotificationsOrganizationsLocationsNotificationsListArgs,
    ) -> String {
        format!("gcp::advisorynotifications::GoogleCloudAdvisorynotificationsV1ListNotificationsResponse/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::advisorynotifications::GoogleCloudAdvisorynotificationsV1ListNotificationsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudAdvisorynotificationsV1Settings
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudAdvisorynotificationsV1Settings with AdvisorynotificationsProjectsLocationsGetSettingsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<AdvisorynotificationsProjectsLocationsGetSettingsArgs>
    for GoogleCloudAdvisorynotificationsV1Settings
{
    fn generate_resource_id(
        &self,
        input: &AdvisorynotificationsProjectsLocationsGetSettingsArgs,
    ) -> String {
        format!(
            "gcp::advisorynotifications::GoogleCloudAdvisorynotificationsV1Settings/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::advisorynotifications::GoogleCloudAdvisorynotificationsV1Settings"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudAdvisorynotificationsV1Settings
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudAdvisorynotificationsV1Settings with AdvisorynotificationsProjectsLocationsUpdateSettingsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<AdvisorynotificationsProjectsLocationsUpdateSettingsArgs>
    for GoogleCloudAdvisorynotificationsV1Settings
{
    fn generate_resource_id(
        &self,
        input: &AdvisorynotificationsProjectsLocationsUpdateSettingsArgs,
    ) -> String {
        format!(
            "gcp::advisorynotifications::GoogleCloudAdvisorynotificationsV1Settings/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::advisorynotifications::GoogleCloudAdvisorynotificationsV1Settings"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudAdvisorynotificationsV1Notification
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudAdvisorynotificationsV1Notification with AdvisorynotificationsProjectsLocationsNotificationsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<AdvisorynotificationsProjectsLocationsNotificationsGetArgs>
    for GoogleCloudAdvisorynotificationsV1Notification
{
    fn generate_resource_id(
        &self,
        input: &AdvisorynotificationsProjectsLocationsNotificationsGetArgs,
    ) -> String {
        format!(
            "gcp::advisorynotifications::GoogleCloudAdvisorynotificationsV1Notification/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::advisorynotifications::GoogleCloudAdvisorynotificationsV1Notification"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudAdvisorynotificationsV1ListNotificationsResponse
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudAdvisorynotificationsV1ListNotificationsResponse with AdvisorynotificationsProjectsLocationsNotificationsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<AdvisorynotificationsProjectsLocationsNotificationsListArgs>
    for GoogleCloudAdvisorynotificationsV1ListNotificationsResponse
{
    fn generate_resource_id(
        &self,
        input: &AdvisorynotificationsProjectsLocationsNotificationsListArgs,
    ) -> String {
        format!("gcp::advisorynotifications::GoogleCloudAdvisorynotificationsV1ListNotificationsResponse/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::advisorynotifications::GoogleCloudAdvisorynotificationsV1ListNotificationsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}
