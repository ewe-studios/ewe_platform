//! Auto-generated API clients for gcp/abusiveexperiencereport.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp`

#![cfg(feature = "gcp")]

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{
    execute, BoxedSendExecutionAction, StreamIterator, StreamIteratorExt, TaskIterator,
    TaskIteratorExt,
};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};
use foundation_db::state::resource_identifier::ResourceIdentifier;
use foundation_macros::JsonHash;
use serde::Serialize;

/// GET v1/sites/{sitesId}
/// Gets a site's Abusive Experience Report summary.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `abusiveexperiencereport_sites_get_execute()` to send, or `abusiveexperiencereport_sites_get` for simplest API.

pub fn abusiveexperiencereport_sites_get_builder(
    client: &SimpleHttpClient,
    name: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://abusiveexperiencereport.googleapis.com/v1/sites/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/sites/{sitesId}
/// Gets a site's Abusive Experience Report summary.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `abusiveexperiencereport_sites_get_execute()` or `abusiveexperiencereport_sites_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `abusiveexperiencereport_sites_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn abusiveexperiencereport_sites_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SiteSummaryResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SiteSummaryResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/sites/{sitesId}
/// Gets a site's Abusive Experience Report summary.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `abusiveexperiencereport_sites_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `abusiveexperiencereport_sites_get_task()`.
/// For the simplest API, use `abusiveexperiencereport_sites_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `abusiveexperiencereport_sites_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn abusiveexperiencereport_sites_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SiteSummaryResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = abusiveexperiencereport_sites_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`abusiveexperiencereport_sites_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AbusiveexperiencereportSitesGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/sites/{sitesId}
/// Gets a site's Abusive Experience Report summary.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `abusiveexperiencereport_sites_get_builder()` + `abusiveexperiencereport_sites_get_execute()`.
/// For task-level control, use `abusiveexperiencereport_sites_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn abusiveexperiencereport_sites_get(
    client: &SimpleHttpClient,
    args: &AbusiveexperiencereportSitesGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SiteSummaryResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = abusiveexperiencereport_sites_get_builder(client, &args.name)?;
    abusiveexperiencereport_sites_get_execute(builder)
}

/// GET v1/violatingSites
/// Lists sites that are failing in the Abusive Experience Report.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `abusiveexperiencereport_violating_sites_list_execute()` to send, or `abusiveexperiencereport_violating_sites_list` for simplest API.

pub fn abusiveexperiencereport_violating_sites_list_builder(
    client: &SimpleHttpClient,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://abusiveexperiencereport.googleapis.com/v1/violatingSites",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/violatingSites
/// Lists sites that are failing in the Abusive Experience Report.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `abusiveexperiencereport_violating_sites_list_execute()` or `abusiveexperiencereport_violating_sites_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `abusiveexperiencereport_violating_sites_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn abusiveexperiencereport_violating_sites_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ViolatingSitesResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ViolatingSitesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/violatingSites
/// Lists sites that are failing in the Abusive Experience Report.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `abusiveexperiencereport_violating_sites_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `abusiveexperiencereport_violating_sites_list_task()`.
/// For the simplest API, use `abusiveexperiencereport_violating_sites_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `abusiveexperiencereport_violating_sites_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn abusiveexperiencereport_violating_sites_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ViolatingSitesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = abusiveexperiencereport_violating_sites_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/violatingSites
/// Lists sites that are failing in the Abusive Experience Report.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `abusiveexperiencereport_violating_sites_list_builder()` + `abusiveexperiencereport_violating_sites_list_execute()`.
/// For task-level control, use `abusiveexperiencereport_violating_sites_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn abusiveexperiencereport_violating_sites_list(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ViolatingSitesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = abusiveexperiencereport_violating_sites_list_builder(client)?;
    abusiveexperiencereport_violating_sites_list_execute(builder)
}

// =============================================================================
// ResourceIdentifier implementation for SiteSummaryResponse
// =============================================================================

/// ResourceIdentifier implementation for SiteSummaryResponse with AbusiveexperiencereportSitesGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<AbusiveexperiencereportSitesGetArgs> for SiteSummaryResponse {
    fn generate_resource_id(&self, input: &AbusiveexperiencereportSitesGetArgs) -> String {
        format!(
            "gcp::abusiveexperiencereport::SiteSummaryResponse/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::abusiveexperiencereport::SiteSummaryResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ViolatingSitesResponse
// =============================================================================

/// ResourceIdentifier implementation for ViolatingSitesResponse with AbusiveexperiencereportViolatingSitesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<AbusiveexperiencereportViolatingSitesListArgs> for ViolatingSitesResponse {
    fn generate_resource_id(
        &self,
        input: &AbusiveexperiencereportViolatingSitesListArgs,
    ) -> String {
        "gcp::abusiveexperiencereport::ViolatingSitesResponse".to_string()
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::abusiveexperiencereport::ViolatingSitesResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}
