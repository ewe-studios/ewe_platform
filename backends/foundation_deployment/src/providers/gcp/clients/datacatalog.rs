//! Auto-generated API clients for gcp/datacatalog.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp`

#![cfg(feature = "gcp")]

pub mod types;

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{
    execute, BoxedSendExecutionAction, StreamIterator, StreamIteratorExt, TaskIterator,
    TaskIteratorExt,
};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};
use foundation_macros::JsonHash;
use serde::Serialize;

/// GET v1/catalog:search
/// Searches Data Catalog for multiple resources like entries and tags that match a query. This is a [Custom Method] (<https://cloud.google.`com/apis/design/custom_methods`>) that doesn't return all information on a resource, only its ID and high level fields. To get more information, you can subsequently call specific get methods. Note: Data Catalog search queries don't guarantee full recall. Results that match your query might not be returned, even in subsequent result pages. Additionally, returned (and not returned) results can vary if you repeat search queries. For more information, see [Data Catalog search syntax] (<https://cloud.google.`com/data-catalog/docs/how-to/search-reference`>).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `datacatalog_catalog_search_execute()` to send, or `datacatalog_catalog_search` for simplest API.

pub fn datacatalog_catalog_search_builder(
    client: &SimpleHttpClient,
    body: &GoogleCloudDatacatalogV1SearchCatalogRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://datacatalog.googleapis.com/v1/catalog:search",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/catalog:search
/// Searches Data Catalog for multiple resources like entries and tags that match a query. This is a [Custom Method] (<https://cloud.google.`com/apis/design/custom_methods`>) that doesn't return all information on a resource, only its ID and high level fields. To get more information, you can subsequently call specific get methods. Note: Data Catalog search queries don't guarantee full recall. Results that match your query might not be returned, even in subsequent result pages. Additionally, returned (and not returned) results can vary if you repeat search queries. For more information, see [Data Catalog search syntax] (<https://cloud.google.`com/data-catalog/docs/how-to/search-reference`>).
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `datacatalog_catalog_search_execute()` or `datacatalog_catalog_search`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `datacatalog_catalog_search_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datacatalog_catalog_search_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudDatacatalogV1SearchCatalogResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDatacatalogV1SearchCatalogResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/catalog:search
/// Searches Data Catalog for multiple resources like entries and tags that match a query. This is a [Custom Method] (<https://cloud.google.`com/apis/design/custom_methods`>) that doesn't return all information on a resource, only its ID and high level fields. To get more information, you can subsequently call specific get methods. Note: Data Catalog search queries don't guarantee full recall. Results that match your query might not be returned, even in subsequent result pages. Additionally, returned (and not returned) results can vary if you repeat search queries. For more information, see [Data Catalog search syntax] (<https://cloud.google.`com/data-catalog/docs/how-to/search-reference`>).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `datacatalog_catalog_search_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `datacatalog_catalog_search_task()`.
/// For the simplest API, use `datacatalog_catalog_search()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `datacatalog_catalog_search_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn datacatalog_catalog_search_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDatacatalogV1SearchCatalogResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = datacatalog_catalog_search_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`datacatalog_catalog_search`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DatacatalogCatalogSearchArgs {
    /// Request body.
    pub body: GoogleCloudDatacatalogV1SearchCatalogRequest,
}

/// GET v1/catalog:search
/// Searches Data Catalog for multiple resources like entries and tags that match a query. This is a [Custom Method] (<https://cloud.google.`com/apis/design/custom_methods`>) that doesn't return all information on a resource, only its ID and high level fields. To get more information, you can subsequently call specific get methods. Note: Data Catalog search queries don't guarantee full recall. Results that match your query might not be returned, even in subsequent result pages. Additionally, returned (and not returned) results can vary if you repeat search queries. For more information, see [Data Catalog search syntax] (<https://cloud.google.`com/data-catalog/docs/how-to/search-reference`>).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `datacatalog_catalog_search_builder()` + `datacatalog_catalog_search_execute()`.
/// For task-level control, use `datacatalog_catalog_search_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datacatalog_catalog_search(
    client: &SimpleHttpClient,
    args: &DatacatalogCatalogSearchArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDatacatalogV1SearchCatalogResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = datacatalog_catalog_search_builder(client, &args.body)?;
    datacatalog_catalog_search_execute(builder)
}

/// GET v1/entries:lookup
/// Gets an entry by its target resource name. The resource name comes from the source Google Cloud Platform service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `datacatalog_entries_lookup_execute()` to send, or `datacatalog_entries_lookup` for simplest API.

pub fn datacatalog_entries_lookup_builder(
    client: &SimpleHttpClient,
    fullyQualifiedName: Option<String>,
    linkedResource: Option<String>,
    location: Option<String>,
    project: Option<String>,
    sqlResource: Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://datacatalog.googleapis.com/v1/entries:lookup",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = fullyQualifiedName {
        query_parts.push(format!("fullyQualifiedName={}", val));
    }
    if let Some(val) = linkedResource {
        query_parts.push(format!("linkedResource={}", val));
    }
    if let Some(val) = location {
        query_parts.push(format!("location={}", val));
    }
    if let Some(val) = project {
        query_parts.push(format!("project={}", val));
    }
    if let Some(val) = sqlResource {
        query_parts.push(format!("sqlResource={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/entries:lookup
/// Gets an entry by its target resource name. The resource name comes from the source Google Cloud Platform service.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `datacatalog_entries_lookup_execute()` or `datacatalog_entries_lookup`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `datacatalog_entries_lookup_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datacatalog_entries_lookup_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudDatacatalogV1Entry>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDatacatalogV1Entry = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/entries:lookup
/// Gets an entry by its target resource name. The resource name comes from the source Google Cloud Platform service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `datacatalog_entries_lookup_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `datacatalog_entries_lookup_task()`.
/// For the simplest API, use `datacatalog_entries_lookup()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `datacatalog_entries_lookup_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn datacatalog_entries_lookup_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDatacatalogV1Entry>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = datacatalog_entries_lookup_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`datacatalog_entries_lookup`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DatacatalogEntriesLookupArgs {
    /// Query parameter: fullyQualifiedName
    pub fullyQualifiedName: Option<String>,
    /// Query parameter: linkedResource
    pub linkedResource: Option<String>,
    /// Query parameter: location
    pub location: Option<String>,
    /// Query parameter: project
    pub project: Option<String>,
    /// Query parameter: sqlResource
    pub sqlResource: Option<String>,
}

/// GET v1/entries:lookup
/// Gets an entry by its target resource name. The resource name comes from the source Google Cloud Platform service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `datacatalog_entries_lookup_builder()` + `datacatalog_entries_lookup_execute()`.
/// For task-level control, use `datacatalog_entries_lookup_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datacatalog_entries_lookup(
    client: &SimpleHttpClient,
    args: &DatacatalogEntriesLookupArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDatacatalogV1Entry>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = datacatalog_entries_lookup_builder(
        client,
        args.fullyQualifiedName.clone(),
        args.linkedResource.clone(),
        args.location.clone(),
        args.project.clone(),
        args.sqlResource.clone(),
    )?;
    datacatalog_entries_lookup_execute(builder)
}
