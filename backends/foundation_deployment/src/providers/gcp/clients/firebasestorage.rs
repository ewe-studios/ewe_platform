//! Auto-generated API clients for gcp/firebasestorage.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp`

#![cfg(feature = "gcp")]

pub mod types;

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{
    execute, BoxedSendExecutionAction, StreamIterator, StreamIteratorExt, TaskIterator,
    TaskIteratorExt,
};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};
use foundation_macros::JsonHash;
use serde::Serialize;

/// GET v1beta/projects/{projectsId}/defaultBucket
/// Unlinks and deletes the default bucket.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `firebasestorage_projects_delete_default_bucket_execute()` to send, or `firebasestorage_projects_delete_default_bucket` for simplest API.

pub fn firebasestorage_projects_delete_default_bucket_builder(
    client: &SimpleHttpClient,
    name: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url =
        format!("https://firebasestorage.googleapis.com/v1beta/projects/{}/defaultBucket",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1beta/projects/{projectsId}/defaultBucket
/// Unlinks and deletes the default bucket.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `firebasestorage_projects_delete_default_bucket_execute()` or `firebasestorage_projects_delete_default_bucket`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firebasestorage_projects_delete_default_bucket_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebasestorage_projects_delete_default_bucket_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1beta/projects/{projectsId}/defaultBucket
/// Unlinks and deletes the default bucket.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `firebasestorage_projects_delete_default_bucket_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `firebasestorage_projects_delete_default_bucket_task()`.
/// For the simplest API, use `firebasestorage_projects_delete_default_bucket()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firebasestorage_projects_delete_default_bucket_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn firebasestorage_projects_delete_default_bucket_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = firebasestorage_projects_delete_default_bucket_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`firebasestorage_projects_delete_default_bucket`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct FirebasestorageProjectsDeleteDefaultBucketArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1beta/projects/{projectsId}/defaultBucket
/// Unlinks and deletes the default bucket.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `firebasestorage_projects_delete_default_bucket_builder()` + `firebasestorage_projects_delete_default_bucket_execute()`.
/// For task-level control, use `firebasestorage_projects_delete_default_bucket_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebasestorage_projects_delete_default_bucket(
    client: &SimpleHttpClient,
    args: &FirebasestorageProjectsDeleteDefaultBucketArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = firebasestorage_projects_delete_default_bucket_builder(client, &args.name)?;
    firebasestorage_projects_delete_default_bucket_execute(builder)
}

/// GET v1beta/projects/{projectsId}/buckets
/// Lists the linked storage buckets for a project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `firebasestorage_projects_buckets_list_execute()` to send, or `firebasestorage_projects_buckets_list` for simplest API.

pub fn firebasestorage_projects_buckets_list_builder(
    client: &SimpleHttpClient,
    parent: &String,
    pageSize: &Option<i32>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url =
        format!("https://firebasestorage.googleapis.com/v1beta/projects/{}/buckets",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1beta/projects/{projectsId}/buckets
/// Lists the linked storage buckets for a project.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `firebasestorage_projects_buckets_list_execute()` or `firebasestorage_projects_buckets_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firebasestorage_projects_buckets_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebasestorage_projects_buckets_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListBucketsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListBucketsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1beta/projects/{projectsId}/buckets
/// Lists the linked storage buckets for a project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `firebasestorage_projects_buckets_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `firebasestorage_projects_buckets_list_task()`.
/// For the simplest API, use `firebasestorage_projects_buckets_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `firebasestorage_projects_buckets_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn firebasestorage_projects_buckets_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListBucketsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = firebasestorage_projects_buckets_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`firebasestorage_projects_buckets_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct FirebasestorageProjectsBucketsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1beta/projects/{projectsId}/buckets
/// Lists the linked storage buckets for a project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `firebasestorage_projects_buckets_list_builder()` + `firebasestorage_projects_buckets_list_execute()`.
/// For task-level control, use `firebasestorage_projects_buckets_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn firebasestorage_projects_buckets_list(
    client: &SimpleHttpClient,
    args: &FirebasestorageProjectsBucketsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListBucketsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = firebasestorage_projects_buckets_list_builder(
        client,
        &args.parent,
        &args.pageSize,
        &args.pageToken,
    )?;
    firebasestorage_projects_buckets_list_execute(builder)
}
