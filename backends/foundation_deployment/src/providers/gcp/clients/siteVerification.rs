//! Auto-generated API clients for gcp/siteVerification.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp`

#![cfg(feature = "gcp")]


use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{
    execute, BoxedSendExecutionAction, StreamIterator, StreamIteratorExt, TaskIterator,
    TaskIteratorExt,
};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, DnsResolver, RequestIntro, SimpleHttpClient,
    SystemDnsResolver,
};
use foundation_db::state::resource_identifier::ResourceIdentifier;
use foundation_macros::JsonHash;
use serde::Serialize;

/// DELETE webResource/{id}
/// Relinquish ownership of a website or domain.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `site_verification_web_resource_delete_execute()` to send, or `site_verification_web_resource_delete` for simplest API.

pub fn site_verification_web_resource_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    id: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://www.googleapis.com/siteVerification/v1/webResource/{}",
        id,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE webResource/{id}
/// Relinquish ownership of a website or domain.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `site_verification_web_resource_delete_execute()` or `site_verification_web_resource_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `site_verification_web_resource_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn site_verification_web_resource_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE webResource/{id}
/// Relinquish ownership of a website or domain.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `site_verification_web_resource_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `site_verification_web_resource_delete_task()`.
/// For the simplest API, use `site_verification_web_resource_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `site_verification_web_resource_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn site_verification_web_resource_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = site_verification_web_resource_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`site_verification_web_resource_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SiteVerificationWebResourceDeleteArgs {
    /// Path parameter: id
    pub id: String,
}

/// DELETE webResource/{id}
/// Relinquish ownership of a website or domain.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `site_verification_web_resource_delete_builder()` + `site_verification_web_resource_delete_execute()`.
/// For task-level control, use `site_verification_web_resource_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn site_verification_web_resource_delete(
    client: &SimpleHttpClient,
    args: &SiteVerificationWebResourceDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = site_verification_web_resource_delete_builder(client, &args.id)?;
    site_verification_web_resource_delete_execute(builder)
}

/// GET webResource/{id}
/// Get the most current data for a website or domain.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `site_verification_web_resource_get_execute()` to send, or `site_verification_web_resource_get` for simplest API.

pub fn site_verification_web_resource_get_builder<R>(
    client: &SimpleHttpClient<R>,
    id: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://www.googleapis.com/siteVerification/v1/webResource/{}",
        id,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET webResource/{id}
/// Get the most current data for a website or domain.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `site_verification_web_resource_get_execute()` or `site_verification_web_resource_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `site_verification_web_resource_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn site_verification_web_resource_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SiteVerificationWebResourceResource>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SiteVerificationWebResourceResource = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET webResource/{id}
/// Get the most current data for a website or domain.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `site_verification_web_resource_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `site_verification_web_resource_get_task()`.
/// For the simplest API, use `site_verification_web_resource_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `site_verification_web_resource_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn site_verification_web_resource_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SiteVerificationWebResourceResource>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = site_verification_web_resource_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`site_verification_web_resource_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SiteVerificationWebResourceGetArgs {
    /// Path parameter: id
    pub id: String,
}

/// GET webResource/{id}
/// Get the most current data for a website or domain.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `site_verification_web_resource_get_builder()` + `site_verification_web_resource_get_execute()`.
/// For task-level control, use `site_verification_web_resource_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn site_verification_web_resource_get(
    client: &SimpleHttpClient,
    args: &SiteVerificationWebResourceGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SiteVerificationWebResourceResource>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = site_verification_web_resource_get_builder(client, &args.id)?;
    site_verification_web_resource_get_execute(builder)
}

/// POST token
/// Get a verification token for placing on a website or domain.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `site_verification_web_resource_get_token_execute()` to send, or `site_verification_web_resource_get_token` for simplest API.

pub fn site_verification_web_resource_get_token_builder<R>(
    client: &SimpleHttpClient<R>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!("https://www.googleapis.com/siteVerification/v1/token",);

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST token
/// Get a verification token for placing on a website or domain.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `site_verification_web_resource_get_token_execute()` or `site_verification_web_resource_get_token`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `site_verification_web_resource_get_token_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn site_verification_web_resource_get_token_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SiteVerificationWebResourceGettokenResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SiteVerificationWebResourceGettokenResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST token
/// Get a verification token for placing on a website or domain.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `site_verification_web_resource_get_token_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `site_verification_web_resource_get_token_task()`.
/// For the simplest API, use `site_verification_web_resource_get_token()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `site_verification_web_resource_get_token_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn site_verification_web_resource_get_token_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SiteVerificationWebResourceGettokenResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = site_verification_web_resource_get_token_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST token
/// Get a verification token for placing on a website or domain.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `site_verification_web_resource_get_token_builder()` + `site_verification_web_resource_get_token_execute()`.
/// For task-level control, use `site_verification_web_resource_get_token_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn site_verification_web_resource_get_token(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SiteVerificationWebResourceGettokenResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = site_verification_web_resource_get_token_builder(client)?;
    site_verification_web_resource_get_token_execute(builder)
}

/// POST webResource
/// Attempt verification of a website or domain.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `site_verification_web_resource_insert_execute()` to send, or `site_verification_web_resource_insert` for simplest API.

pub fn site_verification_web_resource_insert_builder<R>(
    client: &SimpleHttpClient<R>,
    verificationMethod: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!("https://www.googleapis.com/siteVerification/v1/webResource",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = verificationMethod.as_ref() {
        query_parts.push(format!("verificationMethod={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST webResource
/// Attempt verification of a website or domain.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `site_verification_web_resource_insert_execute()` or `site_verification_web_resource_insert`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `site_verification_web_resource_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn site_verification_web_resource_insert_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SiteVerificationWebResourceResource>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SiteVerificationWebResourceResource = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST webResource
/// Attempt verification of a website or domain.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `site_verification_web_resource_insert_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `site_verification_web_resource_insert_task()`.
/// For the simplest API, use `site_verification_web_resource_insert()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `site_verification_web_resource_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn site_verification_web_resource_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SiteVerificationWebResourceResource>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = site_verification_web_resource_insert_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`site_verification_web_resource_insert`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SiteVerificationWebResourceInsertArgs {
    /// Query parameter: verificationMethod
    pub verificationMethod: Option<String>,
}

/// POST webResource
/// Attempt verification of a website or domain.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `site_verification_web_resource_insert_builder()` + `site_verification_web_resource_insert_execute()`.
/// For task-level control, use `site_verification_web_resource_insert_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn site_verification_web_resource_insert(
    client: &SimpleHttpClient,
    args: &SiteVerificationWebResourceInsertArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SiteVerificationWebResourceResource>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = site_verification_web_resource_insert_builder(client, &args.verificationMethod)?;
    site_verification_web_resource_insert_execute(builder)
}

/// GET webResource
/// Get the list of your verified websites and domains.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `site_verification_web_resource_list_execute()` to send, or `site_verification_web_resource_list` for simplest API.

pub fn site_verification_web_resource_list_builder<R>(
    client: &SimpleHttpClient<R>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!("https://www.googleapis.com/siteVerification/v1/webResource",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET webResource
/// Get the list of your verified websites and domains.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `site_verification_web_resource_list_execute()` or `site_verification_web_resource_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `site_verification_web_resource_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn site_verification_web_resource_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SiteVerificationWebResourceListResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SiteVerificationWebResourceListResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET webResource
/// Get the list of your verified websites and domains.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `site_verification_web_resource_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `site_verification_web_resource_list_task()`.
/// For the simplest API, use `site_verification_web_resource_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `site_verification_web_resource_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn site_verification_web_resource_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SiteVerificationWebResourceListResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = site_verification_web_resource_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET webResource
/// Get the list of your verified websites and domains.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `site_verification_web_resource_list_builder()` + `site_verification_web_resource_list_execute()`.
/// For task-level control, use `site_verification_web_resource_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn site_verification_web_resource_list(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SiteVerificationWebResourceListResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = site_verification_web_resource_list_builder(client)?;
    site_verification_web_resource_list_execute(builder)
}

/// PATCH webResource/{id}
/// Modify the list of owners for your website or domain. This method supports patch semantics.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `site_verification_web_resource_patch_execute()` to send, or `site_verification_web_resource_patch` for simplest API.

pub fn site_verification_web_resource_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    id: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://www.googleapis.com/siteVerification/v1/webResource/{}",
        id,
    );

    // Build request
    let builder = client
        .patch(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH webResource/{id}
/// Modify the list of owners for your website or domain. This method supports patch semantics.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `site_verification_web_resource_patch_execute()` or `site_verification_web_resource_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `site_verification_web_resource_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn site_verification_web_resource_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SiteVerificationWebResourceResource>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SiteVerificationWebResourceResource = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH webResource/{id}
/// Modify the list of owners for your website or domain. This method supports patch semantics.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `site_verification_web_resource_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `site_verification_web_resource_patch_task()`.
/// For the simplest API, use `site_verification_web_resource_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `site_verification_web_resource_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn site_verification_web_resource_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SiteVerificationWebResourceResource>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = site_verification_web_resource_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`site_verification_web_resource_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SiteVerificationWebResourcePatchArgs {
    /// Path parameter: id
    pub id: String,
}

/// PATCH webResource/{id}
/// Modify the list of owners for your website or domain. This method supports patch semantics.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `site_verification_web_resource_patch_builder()` + `site_verification_web_resource_patch_execute()`.
/// For task-level control, use `site_verification_web_resource_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn site_verification_web_resource_patch(
    client: &SimpleHttpClient,
    args: &SiteVerificationWebResourcePatchArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SiteVerificationWebResourceResource>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = site_verification_web_resource_patch_builder(client, &args.id)?;
    site_verification_web_resource_patch_execute(builder)
}

/// PUT webResource/{id}
/// Modify the list of owners for your website or domain.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `site_verification_web_resource_update_execute()` to send, or `site_verification_web_resource_update` for simplest API.

pub fn site_verification_web_resource_update_builder<R>(
    client: &SimpleHttpClient<R>,
    id: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://www.googleapis.com/siteVerification/v1/webResource/{}",
        id,
    );

    // Build request
    let builder = client
        .put(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PUT webResource/{id}
/// Modify the list of owners for your website or domain.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `site_verification_web_resource_update_execute()` or `site_verification_web_resource_update`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `site_verification_web_resource_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn site_verification_web_resource_update_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SiteVerificationWebResourceResource>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SiteVerificationWebResourceResource = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PUT webResource/{id}
/// Modify the list of owners for your website or domain.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `site_verification_web_resource_update_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `site_verification_web_resource_update_task()`.
/// For the simplest API, use `site_verification_web_resource_update()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `site_verification_web_resource_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn site_verification_web_resource_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SiteVerificationWebResourceResource>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = site_verification_web_resource_update_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`site_verification_web_resource_update`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SiteVerificationWebResourceUpdateArgs {
    /// Path parameter: id
    pub id: String,
}

/// PUT webResource/{id}
/// Modify the list of owners for your website or domain.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `site_verification_web_resource_update_builder()` + `site_verification_web_resource_update_execute()`.
/// For task-level control, use `site_verification_web_resource_update_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn site_verification_web_resource_update(
    client: &SimpleHttpClient,
    args: &SiteVerificationWebResourceUpdateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SiteVerificationWebResourceResource>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = site_verification_web_resource_update_builder(client, &args.id)?;
    site_verification_web_resource_update_execute(builder)
}

// =============================================================================
// ResourceIdentifier implementation for SiteVerificationWebResourceResource
// =============================================================================

/// ResourceIdentifier implementation for SiteVerificationWebResourceResource with SiteVerificationWebResourceGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<SiteVerificationWebResourceGetArgs>
    for SiteVerificationWebResourceResource
{
    fn generate_resource_id(&self, input: &SiteVerificationWebResourceGetArgs) -> String {
        format!(
            "gcp::siteVerification::SiteVerificationWebResourceResource/{}",
            input.id
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::siteVerification::SiteVerificationWebResourceResource"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for SiteVerificationWebResourceResource
// =============================================================================

/// ResourceIdentifier implementation for SiteVerificationWebResourceResource with SiteVerificationWebResourceInsertArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<SiteVerificationWebResourceInsertArgs>
    for SiteVerificationWebResourceResource
{
    fn generate_resource_id(&self, input: &SiteVerificationWebResourceInsertArgs) -> String {
        "gcp::siteVerification::SiteVerificationWebResourceResource".to_string()
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::siteVerification::SiteVerificationWebResourceResource"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for SiteVerificationWebResourceResource
// =============================================================================

/// ResourceIdentifier implementation for SiteVerificationWebResourceResource with SiteVerificationWebResourcePatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<SiteVerificationWebResourcePatchArgs>
    for SiteVerificationWebResourceResource
{
    fn generate_resource_id(&self, input: &SiteVerificationWebResourcePatchArgs) -> String {
        format!(
            "gcp::siteVerification::SiteVerificationWebResourceResource/{}",
            input.id
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::siteVerification::SiteVerificationWebResourceResource"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for SiteVerificationWebResourceResource
// =============================================================================

/// ResourceIdentifier implementation for SiteVerificationWebResourceResource with SiteVerificationWebResourceUpdateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<SiteVerificationWebResourceUpdateArgs>
    for SiteVerificationWebResourceResource
{
    fn generate_resource_id(&self, input: &SiteVerificationWebResourceUpdateArgs) -> String {
        format!(
            "gcp::siteVerification::SiteVerificationWebResourceResource/{}",
            input.id
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::siteVerification::SiteVerificationWebResourceResource"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}
