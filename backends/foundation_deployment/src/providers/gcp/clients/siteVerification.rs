//! Auto-generated API clients for gcp/siteVerification.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp/siteVerification`

#![cfg(feature = "gcp/siteVerification")]

pub mod types;

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{execute, StreamIterator, StreamIteratorExt, TaskIteratorExt};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};

/// GET webResource/{id}
/// Relinquish ownership of a website or domain.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `site_verification_web_resource_delete_execute()` to send, or `site_verification_web_resource_delete` for simplest API.

pub fn site_verification_web_resource_delete_builder(
    client: &SimpleHttpClient,
    id: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://www.googleapis.com/siteVerification/v1/webResource/{}",
        id,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET webResource/{id}
/// Relinquish ownership of a website or domain.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `site_verification_web_resource_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from site_verification_web_resource_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn site_verification_web_resource_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET webResource/{id}
/// Relinquish ownership of a website or domain.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `site_verification_web_resource_delete_builder()` + `site_verification_web_resource_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn site_verification_web_resource_delete(
    client: &SimpleHttpClient,
    id: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = site_verification_web_resource_delete_builder(client, id)?;
    site_verification_web_resource_delete_execute(builder)
}

/// GET webResource/{id}
/// Get the most current data for a website or domain.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `site_verification_web_resource_get_execute()` to send, or `site_verification_web_resource_get` for simplest API.

pub fn site_verification_web_resource_get_builder(
    client: &SimpleHttpClient,
    id: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://www.googleapis.com/siteVerification/v1/webResource/{}",
        id,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET webResource/{id}
/// Get the most current data for a website or domain.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `site_verification_web_resource_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from site_verification_web_resource_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn site_verification_web_resource_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SiteVerificationWebResourceResource>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SiteVerificationWebResourceResource = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET webResource/{id}
/// Get the most current data for a website or domain.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `site_verification_web_resource_get_builder()` + `site_verification_web_resource_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn site_verification_web_resource_get(
    client: &SimpleHttpClient,
    id: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SiteVerificationWebResourceResource>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = site_verification_web_resource_get_builder(client, id)?;
    site_verification_web_resource_get_execute(builder)
}

/// GET token
/// Get a verification token for placing on a website or domain.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `site_verification_web_resource_get_token_execute()` to send, or `site_verification_web_resource_get_token` for simplest API.

pub fn site_verification_web_resource_get_token_builder(
    client: &SimpleHttpClient,
    body: &SiteVerificationWebResourceGettokenRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://www.googleapis.com/siteVerification/v1/token",);

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET token
/// Get a verification token for placing on a website or domain.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `site_verification_web_resource_get_token_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from site_verification_web_resource_get_token_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn site_verification_web_resource_get_token_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SiteVerificationWebResourceGettokenResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SiteVerificationWebResourceGettokenResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET token
/// Get a verification token for placing on a website or domain.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `site_verification_web_resource_get_token_builder()` + `site_verification_web_resource_get_token_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn site_verification_web_resource_get_token(
    client: &SimpleHttpClient,
    body: &SiteVerificationWebResourceGettokenRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SiteVerificationWebResourceGettokenResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = site_verification_web_resource_get_token_builder(client, body)?;
    site_verification_web_resource_get_token_execute(builder)
}

/// GET webResource
/// Attempt verification of a website or domain.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `site_verification_web_resource_insert_execute()` to send, or `site_verification_web_resource_insert` for simplest API.

pub fn site_verification_web_resource_insert_builder(
    client: &SimpleHttpClient,
    verificationMethod: &str,
    body: &SiteVerificationWebResourceResource,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://www.googleapis.com/siteVerification/v1/webResource",
        verificationMethod,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET webResource
/// Attempt verification of a website or domain.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `site_verification_web_resource_insert_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from site_verification_web_resource_insert_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn site_verification_web_resource_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SiteVerificationWebResourceResource>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SiteVerificationWebResourceResource = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET webResource
/// Attempt verification of a website or domain.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `site_verification_web_resource_insert_builder()` + `site_verification_web_resource_insert_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn site_verification_web_resource_insert(
    client: &SimpleHttpClient,
    verificationMethod: &str,
    body: &SiteVerificationWebResourceResource,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SiteVerificationWebResourceResource>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = site_verification_web_resource_insert_builder(client, verificationMethod, body)?;
    site_verification_web_resource_insert_execute(builder)
}

/// GET webResource
/// Get the list of your verified websites and domains.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `site_verification_web_resource_list_execute()` to send, or `site_verification_web_resource_list` for simplest API.

pub fn site_verification_web_resource_list_builder(
    client: &SimpleHttpClient,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://www.googleapis.com/siteVerification/v1/webResource",);

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET webResource
/// Get the list of your verified websites and domains.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `site_verification_web_resource_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from site_verification_web_resource_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn site_verification_web_resource_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SiteVerificationWebResourceListResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SiteVerificationWebResourceListResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET webResource
/// Get the list of your verified websites and domains.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `site_verification_web_resource_list_builder()` + `site_verification_web_resource_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn site_verification_web_resource_list(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SiteVerificationWebResourceListResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = site_verification_web_resource_list_builder(client)?;
    site_verification_web_resource_list_execute(builder)
}

/// GET webResource/{id}
/// Modify the list of owners for your website or domain. This method supports patch semantics.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `site_verification_web_resource_patch_execute()` to send, or `site_verification_web_resource_patch` for simplest API.

pub fn site_verification_web_resource_patch_builder(
    client: &SimpleHttpClient,
    id: &str,
    body: &SiteVerificationWebResourceResource,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://www.googleapis.com/siteVerification/v1/webResource/{}",
        id,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET webResource/{id}
/// Modify the list of owners for your website or domain. This method supports patch semantics.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `site_verification_web_resource_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from site_verification_web_resource_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn site_verification_web_resource_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SiteVerificationWebResourceResource>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SiteVerificationWebResourceResource = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET webResource/{id}
/// Modify the list of owners for your website or domain. This method supports patch semantics.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `site_verification_web_resource_patch_builder()` + `site_verification_web_resource_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn site_verification_web_resource_patch(
    client: &SimpleHttpClient,
    id: &str,
    body: &SiteVerificationWebResourceResource,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SiteVerificationWebResourceResource>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = site_verification_web_resource_patch_builder(client, id, body)?;
    site_verification_web_resource_patch_execute(builder)
}

/// GET webResource/{id}
/// Modify the list of owners for your website or domain.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `site_verification_web_resource_update_execute()` to send, or `site_verification_web_resource_update` for simplest API.

pub fn site_verification_web_resource_update_builder(
    client: &SimpleHttpClient,
    id: &str,
    body: &SiteVerificationWebResourceResource,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://www.googleapis.com/siteVerification/v1/webResource/{}",
        id,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET webResource/{id}
/// Modify the list of owners for your website or domain.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `site_verification_web_resource_update_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from site_verification_web_resource_update_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn site_verification_web_resource_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SiteVerificationWebResourceResource>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SiteVerificationWebResourceResource = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET webResource/{id}
/// Modify the list of owners for your website or domain.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `site_verification_web_resource_update_builder()` + `site_verification_web_resource_update_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn site_verification_web_resource_update(
    client: &SimpleHttpClient,
    id: &str,
    body: &SiteVerificationWebResourceResource,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SiteVerificationWebResourceResource>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = site_verification_web_resource_update_builder(client, id, body)?;
    site_verification_web_resource_update_execute(builder)
}
