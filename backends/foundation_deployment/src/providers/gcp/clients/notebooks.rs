//! Auto-generated API clients for Google Cloud Platform - notebooks.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp`


#![cfg(feature = "gcp")]

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{execute, StreamIterator, StreamIteratorExt};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};

/// GET v2/{+name}
/// Gets information about a location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `notebooks_projects_locations_get_execute()` to send, or `notebooks_projects_locations_get` for simplest API.

pub fn notebooks_projects_locations_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Gets information about a location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `notebooks_projects_locations_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from notebooks_projects_locations_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn notebooks_projects_locations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Location>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Location = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Gets information about a location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `notebooks_projects_locations_get_builder()` + `notebooks_projects_locations_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn notebooks_projects_locations_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Location>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = notebooks_projects_locations_get_builder(client, name)?;
    notebooks_projects_locations_get_execute(builder)
}

/// GET v2/{+name}/locations
/// Lists information about the supported locations for this service. This method can be called in two ways: * **List all public locations:** Use the path GET /v1/locations. * **List project-visible locations:** Use the path GET /v1/`projects/{project_id}/locations`. This may include public locations as well as private or other locations specifically visible to the project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `notebooks_projects_locations_list_execute()` to send, or `notebooks_projects_locations_list` for simplest API.

pub fn notebooks_projects_locations_list_builder(
    client: &SimpleHttpClient,
    name: &str,
    extraLocationTypes: Option<&str>,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}/locations", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "extraLocationTypes",
            extraLocationTypes
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v2/{+name}/locations
/// Lists information about the supported locations for this service. This method can be called in two ways: * **List all public locations:** Use the path GET /v1/locations. * **List project-visible locations:** Use the path GET /v1/`projects/{project_id}/locations`. This may include public locations as well as private or other locations specifically visible to the project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `notebooks_projects_locations_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from notebooks_projects_locations_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn notebooks_projects_locations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListLocationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListLocationsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}/locations
/// Lists information about the supported locations for this service. This method can be called in two ways: * **List all public locations:** Use the path GET /v1/locations. * **List project-visible locations:** Use the path GET /v1/`projects/{project_id}/locations`. This may include public locations as well as private or other locations specifically visible to the project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `notebooks_projects_locations_list_builder()` + `notebooks_projects_locations_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn notebooks_projects_locations_list(
    client: &SimpleHttpClient,
    name: &str,
    extraLocationTypes: Option<&str>,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListLocationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = notebooks_projects_locations_list_builder(
        client,
        name,
        extraLocationTypes,
        filter,
        pageSize,
        pageToken,
    )?;
    notebooks_projects_locations_list_execute(builder)
}

/// GET v2/{+name}:checkAuthorization
/// Initiated by Cloud Console for Oauth consent flow for Workbench Instances. Do not use this method directly. Design doc: `go/wbi-euc`:auth-dd
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `notebooks_projects_locations_instances_check_authorization_execute()` to send, or `notebooks_projects_locations_instances_check_authorization` for simplest API.

pub fn notebooks_projects_locations_instances_check_authorization_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &CheckAuthorizationRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}:checkAuthorization", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+name}:checkAuthorization
/// Initiated by Cloud Console for Oauth consent flow for Workbench Instances. Do not use this method directly. Design doc: `go/wbi-euc`:auth-dd
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `notebooks_projects_locations_instances_check_authorization_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from notebooks_projects_locations_instances_check_authorization_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn notebooks_projects_locations_instances_check_authorization_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<CheckAuthorizationResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: CheckAuthorizationResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}:checkAuthorization
/// Initiated by Cloud Console for Oauth consent flow for Workbench Instances. Do not use this method directly. Design doc: `go/wbi-euc`:auth-dd
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `notebooks_projects_locations_instances_check_authorization_builder()` + `notebooks_projects_locations_instances_check_authorization_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn notebooks_projects_locations_instances_check_authorization(
    client: &SimpleHttpClient,
    name: &str,
    body: &CheckAuthorizationRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<CheckAuthorizationResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        notebooks_projects_locations_instances_check_authorization_builder(client, name, body)?;
    notebooks_projects_locations_instances_check_authorization_execute(builder)
}

/// GET v2/{+notebookInstance}:checkUpgradability
/// Checks whether a notebook instance is upgradable.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `notebooks_projects_locations_instances_check_upgradability_execute()` to send, or `notebooks_projects_locations_instances_check_upgradability` for simplest API.

pub fn notebooks_projects_locations_instances_check_upgradability_builder(
    client: &SimpleHttpClient,
    notebookInstance: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv2/{+notebookInstance}:checkUpgradability",
        notebookInstance,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+notebookInstance}:checkUpgradability
/// Checks whether a notebook instance is upgradable.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `notebooks_projects_locations_instances_check_upgradability_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from notebooks_projects_locations_instances_check_upgradability_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn notebooks_projects_locations_instances_check_upgradability_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<CheckInstanceUpgradabilityResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: CheckInstanceUpgradabilityResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+notebookInstance}:checkUpgradability
/// Checks whether a notebook instance is upgradable.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `notebooks_projects_locations_instances_check_upgradability_builder()` + `notebooks_projects_locations_instances_check_upgradability_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn notebooks_projects_locations_instances_check_upgradability(
    client: &SimpleHttpClient,
    notebookInstance: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<CheckInstanceUpgradabilityResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = notebooks_projects_locations_instances_check_upgradability_builder(
        client,
        notebookInstance,
    )?;
    notebooks_projects_locations_instances_check_upgradability_execute(builder)
}

/// GET v2/{+parent}/instances
/// Creates a new Instance in a given project and location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `notebooks_projects_locations_instances_create_execute()` to send, or `notebooks_projects_locations_instances_create` for simplest API.

pub fn notebooks_projects_locations_instances_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    instanceId: Option<&str>,
    requestId: Option<&str>,
    body: &Instance,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+parent}/instances", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "instanceId",
            instanceId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+parent}/instances
/// Creates a new Instance in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `notebooks_projects_locations_instances_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from notebooks_projects_locations_instances_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn notebooks_projects_locations_instances_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+parent}/instances
/// Creates a new Instance in a given project and location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `notebooks_projects_locations_instances_create_builder()` + `notebooks_projects_locations_instances_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn notebooks_projects_locations_instances_create(
    client: &SimpleHttpClient,
    parent: &str,
    instanceId: Option<&str>,
    requestId: Option<&str>,
    body: &Instance,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = notebooks_projects_locations_instances_create_builder(
        client, parent, instanceId, requestId, body,
    )?;
    notebooks_projects_locations_instances_create_execute(builder)
}

/// GET v2/{+name}
/// Deletes a single Instance.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `notebooks_projects_locations_instances_delete_execute()` to send, or `notebooks_projects_locations_instances_delete` for simplest API.

pub fn notebooks_projects_locations_instances_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
    requestId: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v2/{+name}
/// Deletes a single Instance.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `notebooks_projects_locations_instances_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from notebooks_projects_locations_instances_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn notebooks_projects_locations_instances_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Deletes a single Instance.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `notebooks_projects_locations_instances_delete_builder()` + `notebooks_projects_locations_instances_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn notebooks_projects_locations_instances_delete(
    client: &SimpleHttpClient,
    name: &str,
    requestId: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = notebooks_projects_locations_instances_delete_builder(client, name, requestId)?;
    notebooks_projects_locations_instances_delete_execute(builder)
}

/// GET v2/{+name}:diagnose
/// Creates a Diagnostic File and runs Diagnostic Tool given an Instance.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `notebooks_projects_locations_instances_diagnose_execute()` to send, or `notebooks_projects_locations_instances_diagnose` for simplest API.

pub fn notebooks_projects_locations_instances_diagnose_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &DiagnoseInstanceRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}:diagnose", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+name}:diagnose
/// Creates a Diagnostic File and runs Diagnostic Tool given an Instance.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `notebooks_projects_locations_instances_diagnose_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from notebooks_projects_locations_instances_diagnose_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn notebooks_projects_locations_instances_diagnose_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}:diagnose
/// Creates a Diagnostic File and runs Diagnostic Tool given an Instance.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `notebooks_projects_locations_instances_diagnose_builder()` + `notebooks_projects_locations_instances_diagnose_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn notebooks_projects_locations_instances_diagnose(
    client: &SimpleHttpClient,
    name: &str,
    body: &DiagnoseInstanceRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = notebooks_projects_locations_instances_diagnose_builder(client, name, body)?;
    notebooks_projects_locations_instances_diagnose_execute(builder)
}

/// GET v2/{+name}:generateAccessToken
/// Called by VM to return an EUC for the instance owner. Do not use this method directly. Design doc: `go/wbi-euc`:dd
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `notebooks_projects_locations_instances_generate_access_token_execute()` to send, or `notebooks_projects_locations_instances_generate_access_token` for simplest API.

pub fn notebooks_projects_locations_instances_generate_access_token_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GenerateAccessTokenRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv2/{+name}:generateAccessToken",
        name,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+name}:generateAccessToken
/// Called by VM to return an EUC for the instance owner. Do not use this method directly. Design doc: `go/wbi-euc`:dd
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `notebooks_projects_locations_instances_generate_access_token_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from notebooks_projects_locations_instances_generate_access_token_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn notebooks_projects_locations_instances_generate_access_token_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GenerateAccessTokenResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GenerateAccessTokenResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}:generateAccessToken
/// Called by VM to return an EUC for the instance owner. Do not use this method directly. Design doc: `go/wbi-euc`:dd
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `notebooks_projects_locations_instances_generate_access_token_builder()` + `notebooks_projects_locations_instances_generate_access_token_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn notebooks_projects_locations_instances_generate_access_token(
    client: &SimpleHttpClient,
    name: &str,
    body: &GenerateAccessTokenRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GenerateAccessTokenResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        notebooks_projects_locations_instances_generate_access_token_builder(client, name, body)?;
    notebooks_projects_locations_instances_generate_access_token_execute(builder)
}

/// GET v2/{+name}
/// Gets details of a single Instance.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `notebooks_projects_locations_instances_get_execute()` to send, or `notebooks_projects_locations_instances_get` for simplest API.

pub fn notebooks_projects_locations_instances_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Gets details of a single Instance.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `notebooks_projects_locations_instances_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from notebooks_projects_locations_instances_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn notebooks_projects_locations_instances_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Instance>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Instance = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Gets details of a single Instance.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `notebooks_projects_locations_instances_get_builder()` + `notebooks_projects_locations_instances_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn notebooks_projects_locations_instances_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Instance>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = notebooks_projects_locations_instances_get_builder(client, name)?;
    notebooks_projects_locations_instances_get_execute(builder)
}

/// GET v2/{+name}/instances:getConfig
/// Returns various configuration parameters.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `notebooks_projects_locations_instances_get_config_execute()` to send, or `notebooks_projects_locations_instances_get_config` for simplest API.

pub fn notebooks_projects_locations_instances_get_config_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv2/{+name}/instances:getConfig",
        name,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}/instances:getConfig
/// Returns various configuration parameters.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `notebooks_projects_locations_instances_get_config_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from notebooks_projects_locations_instances_get_config_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn notebooks_projects_locations_instances_get_config_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Config>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Config = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}/instances:getConfig
/// Returns various configuration parameters.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `notebooks_projects_locations_instances_get_config_builder()` + `notebooks_projects_locations_instances_get_config_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn notebooks_projects_locations_instances_get_config(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Config>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = notebooks_projects_locations_instances_get_config_builder(client, name)?;
    notebooks_projects_locations_instances_get_config_execute(builder)
}

/// GET v2/{+resource}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `notebooks_projects_locations_instances_get_iam_policy_execute()` to send, or `notebooks_projects_locations_instances_get_iam_policy` for simplest API.

pub fn notebooks_projects_locations_instances_get_iam_policy_builder(
    client: &SimpleHttpClient,
    resource: &str,
    options_requestedPolicyVersion: Option<i32>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv2/{+resource}:getIamPolicy",
        resource,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "options_requestedPolicyVersion",
            options_requestedPolicyVersion
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v2/{+resource}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `notebooks_projects_locations_instances_get_iam_policy_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from notebooks_projects_locations_instances_get_iam_policy_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn notebooks_projects_locations_instances_get_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+resource}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `notebooks_projects_locations_instances_get_iam_policy_builder()` + `notebooks_projects_locations_instances_get_iam_policy_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn notebooks_projects_locations_instances_get_iam_policy(
    client: &SimpleHttpClient,
    resource: &str,
    options_requestedPolicyVersion: Option<i32>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = notebooks_projects_locations_instances_get_iam_policy_builder(
        client,
        resource,
        options_requestedPolicyVersion,
    )?;
    notebooks_projects_locations_instances_get_iam_policy_execute(builder)
}

/// GET v2/{+parent}/instances
/// Lists instances in a given project and location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `notebooks_projects_locations_instances_list_execute()` to send, or `notebooks_projects_locations_instances_list` for simplest API.

pub fn notebooks_projects_locations_instances_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+parent}/instances", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v2/{+parent}/instances
/// Lists instances in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `notebooks_projects_locations_instances_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from notebooks_projects_locations_instances_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn notebooks_projects_locations_instances_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListInstancesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListInstancesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+parent}/instances
/// Lists instances in a given project and location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `notebooks_projects_locations_instances_list_builder()` + `notebooks_projects_locations_instances_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn notebooks_projects_locations_instances_list(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListInstancesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = notebooks_projects_locations_instances_list_builder(
        client, parent, filter, orderBy, pageSize, pageToken,
    )?;
    notebooks_projects_locations_instances_list_execute(builder)
}

/// GET v2/{+name}
/// UpdateInstance updates an Instance.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `notebooks_projects_locations_instances_patch_execute()` to send, or `notebooks_projects_locations_instances_patch` for simplest API.

pub fn notebooks_projects_locations_instances_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    requestId: Option<&str>,
    updateMask: Option<&str>,
    body: &Instance,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "updateMask",
            updateMask
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+name}
/// UpdateInstance updates an Instance.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `notebooks_projects_locations_instances_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from notebooks_projects_locations_instances_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn notebooks_projects_locations_instances_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// UpdateInstance updates an Instance.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `notebooks_projects_locations_instances_patch_builder()` + `notebooks_projects_locations_instances_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn notebooks_projects_locations_instances_patch(
    client: &SimpleHttpClient,
    name: &str,
    requestId: Option<&str>,
    updateMask: Option<&str>,
    body: &Instance,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = notebooks_projects_locations_instances_patch_builder(
        client, name, requestId, updateMask, body,
    )?;
    notebooks_projects_locations_instances_patch_execute(builder)
}

/// GET v2/{+name}:reportInfoSystem
/// Allows notebook instances to report their latest instance information to the Notebooks API server. The server will merge the reported information to the instance metadata store. Do not use this method directly.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `notebooks_projects_locations_instances_report_info_system_execute()` to send, or `notebooks_projects_locations_instances_report_info_system` for simplest API.

pub fn notebooks_projects_locations_instances_report_info_system_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &ReportInstanceInfoSystemRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}:reportInfoSystem", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+name}:reportInfoSystem
/// Allows notebook instances to report their latest instance information to the Notebooks API server. The server will merge the reported information to the instance metadata store. Do not use this method directly.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `notebooks_projects_locations_instances_report_info_system_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from notebooks_projects_locations_instances_report_info_system_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn notebooks_projects_locations_instances_report_info_system_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}:reportInfoSystem
/// Allows notebook instances to report their latest instance information to the Notebooks API server. The server will merge the reported information to the instance metadata store. Do not use this method directly.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `notebooks_projects_locations_instances_report_info_system_builder()` + `notebooks_projects_locations_instances_report_info_system_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn notebooks_projects_locations_instances_report_info_system(
    client: &SimpleHttpClient,
    name: &str,
    body: &ReportInstanceInfoSystemRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        notebooks_projects_locations_instances_report_info_system_builder(client, name, body)?;
    notebooks_projects_locations_instances_report_info_system_execute(builder)
}

/// GET v2/{+name}:reset
/// Resets a notebook instance.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `notebooks_projects_locations_instances_reset_execute()` to send, or `notebooks_projects_locations_instances_reset` for simplest API.

pub fn notebooks_projects_locations_instances_reset_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &ResetInstanceRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}:reset", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+name}:reset
/// Resets a notebook instance.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `notebooks_projects_locations_instances_reset_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from notebooks_projects_locations_instances_reset_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn notebooks_projects_locations_instances_reset_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}:reset
/// Resets a notebook instance.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `notebooks_projects_locations_instances_reset_builder()` + `notebooks_projects_locations_instances_reset_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn notebooks_projects_locations_instances_reset(
    client: &SimpleHttpClient,
    name: &str,
    body: &ResetInstanceRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = notebooks_projects_locations_instances_reset_builder(client, name, body)?;
    notebooks_projects_locations_instances_reset_execute(builder)
}

/// GET v2/{+notebookInstance}:resizeDisk
/// Resize a notebook instance disk to a higher capacity.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `notebooks_projects_locations_instances_resize_disk_execute()` to send, or `notebooks_projects_locations_instances_resize_disk` for simplest API.

pub fn notebooks_projects_locations_instances_resize_disk_builder(
    client: &SimpleHttpClient,
    notebookInstance: &str,
    body: &ResizeDiskRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv2/{+notebookInstance}:resizeDisk",
        notebookInstance,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+notebookInstance}:resizeDisk
/// Resize a notebook instance disk to a higher capacity.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `notebooks_projects_locations_instances_resize_disk_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from notebooks_projects_locations_instances_resize_disk_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn notebooks_projects_locations_instances_resize_disk_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+notebookInstance}:resizeDisk
/// Resize a notebook instance disk to a higher capacity.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `notebooks_projects_locations_instances_resize_disk_builder()` + `notebooks_projects_locations_instances_resize_disk_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn notebooks_projects_locations_instances_resize_disk(
    client: &SimpleHttpClient,
    notebookInstance: &str,
    body: &ResizeDiskRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        notebooks_projects_locations_instances_resize_disk_builder(client, notebookInstance, body)?;
    notebooks_projects_locations_instances_resize_disk_execute(builder)
}

/// GET v2/{+name}:restore
/// RestoreInstance restores an Instance from a BackupSource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `notebooks_projects_locations_instances_restore_execute()` to send, or `notebooks_projects_locations_instances_restore` for simplest API.

pub fn notebooks_projects_locations_instances_restore_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &RestoreInstanceRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}:restore", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+name}:restore
/// RestoreInstance restores an Instance from a BackupSource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `notebooks_projects_locations_instances_restore_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from notebooks_projects_locations_instances_restore_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn notebooks_projects_locations_instances_restore_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}:restore
/// RestoreInstance restores an Instance from a BackupSource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `notebooks_projects_locations_instances_restore_builder()` + `notebooks_projects_locations_instances_restore_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn notebooks_projects_locations_instances_restore(
    client: &SimpleHttpClient,
    name: &str,
    body: &RestoreInstanceRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = notebooks_projects_locations_instances_restore_builder(client, name, body)?;
    notebooks_projects_locations_instances_restore_execute(builder)
}

/// GET v2/{+name}:rollback
/// Rollbacks a notebook instance to the previous version.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `notebooks_projects_locations_instances_rollback_execute()` to send, or `notebooks_projects_locations_instances_rollback` for simplest API.

pub fn notebooks_projects_locations_instances_rollback_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &RollbackInstanceRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}:rollback", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+name}:rollback
/// Rollbacks a notebook instance to the previous version.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `notebooks_projects_locations_instances_rollback_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from notebooks_projects_locations_instances_rollback_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn notebooks_projects_locations_instances_rollback_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}:rollback
/// Rollbacks a notebook instance to the previous version.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `notebooks_projects_locations_instances_rollback_builder()` + `notebooks_projects_locations_instances_rollback_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn notebooks_projects_locations_instances_rollback(
    client: &SimpleHttpClient,
    name: &str,
    body: &RollbackInstanceRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = notebooks_projects_locations_instances_rollback_builder(client, name, body)?;
    notebooks_projects_locations_instances_rollback_execute(builder)
}

/// GET v2/{+resource}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `notebooks_projects_locations_instances_set_iam_policy_execute()` to send, or `notebooks_projects_locations_instances_set_iam_policy` for simplest API.

pub fn notebooks_projects_locations_instances_set_iam_policy_builder(
    client: &SimpleHttpClient,
    resource: &str,
    body: &SetIamPolicyRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv2/{+resource}:setIamPolicy",
        resource,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+resource}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `notebooks_projects_locations_instances_set_iam_policy_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from notebooks_projects_locations_instances_set_iam_policy_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn notebooks_projects_locations_instances_set_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+resource}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `notebooks_projects_locations_instances_set_iam_policy_builder()` + `notebooks_projects_locations_instances_set_iam_policy_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn notebooks_projects_locations_instances_set_iam_policy(
    client: &SimpleHttpClient,
    resource: &str,
    body: &SetIamPolicyRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        notebooks_projects_locations_instances_set_iam_policy_builder(client, resource, body)?;
    notebooks_projects_locations_instances_set_iam_policy_execute(builder)
}

/// GET v2/{+name}:start
/// Starts a notebook instance.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `notebooks_projects_locations_instances_start_execute()` to send, or `notebooks_projects_locations_instances_start` for simplest API.

pub fn notebooks_projects_locations_instances_start_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &StartInstanceRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}:start", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+name}:start
/// Starts a notebook instance.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `notebooks_projects_locations_instances_start_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from notebooks_projects_locations_instances_start_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn notebooks_projects_locations_instances_start_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}:start
/// Starts a notebook instance.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `notebooks_projects_locations_instances_start_builder()` + `notebooks_projects_locations_instances_start_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn notebooks_projects_locations_instances_start(
    client: &SimpleHttpClient,
    name: &str,
    body: &StartInstanceRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = notebooks_projects_locations_instances_start_builder(client, name, body)?;
    notebooks_projects_locations_instances_start_execute(builder)
}

/// GET v2/{+name}:stop
/// Stops a notebook instance.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `notebooks_projects_locations_instances_stop_execute()` to send, or `notebooks_projects_locations_instances_stop` for simplest API.

pub fn notebooks_projects_locations_instances_stop_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &StopInstanceRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}:stop", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+name}:stop
/// Stops a notebook instance.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `notebooks_projects_locations_instances_stop_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from notebooks_projects_locations_instances_stop_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn notebooks_projects_locations_instances_stop_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}:stop
/// Stops a notebook instance.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `notebooks_projects_locations_instances_stop_builder()` + `notebooks_projects_locations_instances_stop_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn notebooks_projects_locations_instances_stop(
    client: &SimpleHttpClient,
    name: &str,
    body: &StopInstanceRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = notebooks_projects_locations_instances_stop_builder(client, name, body)?;
    notebooks_projects_locations_instances_stop_execute(builder)
}

/// GET v2/{+resource}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `notebooks_projects_locations_instances_test_iam_permissions_execute()` to send, or `notebooks_projects_locations_instances_test_iam_permissions` for simplest API.

pub fn notebooks_projects_locations_instances_test_iam_permissions_builder(
    client: &SimpleHttpClient,
    resource: &str,
    body: &TestIamPermissionsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv2/{+resource}:testIamPermissions",
        resource,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+resource}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `notebooks_projects_locations_instances_test_iam_permissions_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from notebooks_projects_locations_instances_test_iam_permissions_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn notebooks_projects_locations_instances_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TestIamPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+resource}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `notebooks_projects_locations_instances_test_iam_permissions_builder()` + `notebooks_projects_locations_instances_test_iam_permissions_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn notebooks_projects_locations_instances_test_iam_permissions(
    client: &SimpleHttpClient,
    resource: &str,
    body: &TestIamPermissionsRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = notebooks_projects_locations_instances_test_iam_permissions_builder(
        client, resource, body,
    )?;
    notebooks_projects_locations_instances_test_iam_permissions_execute(builder)
}

/// GET v2/{+name}:upgrade
/// Upgrades a notebook instance to the latest version.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `notebooks_projects_locations_instances_upgrade_execute()` to send, or `notebooks_projects_locations_instances_upgrade` for simplest API.

pub fn notebooks_projects_locations_instances_upgrade_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &UpgradeInstanceRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}:upgrade", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+name}:upgrade
/// Upgrades a notebook instance to the latest version.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `notebooks_projects_locations_instances_upgrade_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from notebooks_projects_locations_instances_upgrade_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn notebooks_projects_locations_instances_upgrade_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}:upgrade
/// Upgrades a notebook instance to the latest version.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `notebooks_projects_locations_instances_upgrade_builder()` + `notebooks_projects_locations_instances_upgrade_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn notebooks_projects_locations_instances_upgrade(
    client: &SimpleHttpClient,
    name: &str,
    body: &UpgradeInstanceRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = notebooks_projects_locations_instances_upgrade_builder(client, name, body)?;
    notebooks_projects_locations_instances_upgrade_execute(builder)
}

/// GET v2/{+name}:upgradeSystem
/// Allows notebook instances to upgrade themselves. Do not use this method directly.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `notebooks_projects_locations_instances_upgrade_system_execute()` to send, or `notebooks_projects_locations_instances_upgrade_system` for simplest API.

pub fn notebooks_projects_locations_instances_upgrade_system_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &UpgradeInstanceSystemRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}:upgradeSystem", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+name}:upgradeSystem
/// Allows notebook instances to upgrade themselves. Do not use this method directly.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `notebooks_projects_locations_instances_upgrade_system_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from notebooks_projects_locations_instances_upgrade_system_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn notebooks_projects_locations_instances_upgrade_system_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}:upgradeSystem
/// Allows notebook instances to upgrade themselves. Do not use this method directly.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `notebooks_projects_locations_instances_upgrade_system_builder()` + `notebooks_projects_locations_instances_upgrade_system_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn notebooks_projects_locations_instances_upgrade_system(
    client: &SimpleHttpClient,
    name: &str,
    body: &UpgradeInstanceSystemRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        notebooks_projects_locations_instances_upgrade_system_builder(client, name, body)?;
    notebooks_projects_locations_instances_upgrade_system_execute(builder)
}

/// GET v2/{+name}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `notebooks_projects_locations_operations_cancel_execute()` to send, or `notebooks_projects_locations_operations_cancel` for simplest API.

pub fn notebooks_projects_locations_operations_cancel_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &CancelOperationRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}:cancel", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+name}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `notebooks_projects_locations_operations_cancel_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from notebooks_projects_locations_operations_cancel_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn notebooks_projects_locations_operations_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `notebooks_projects_locations_operations_cancel_builder()` + `notebooks_projects_locations_operations_cancel_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn notebooks_projects_locations_operations_cancel(
    client: &SimpleHttpClient,
    name: &str,
    body: &CancelOperationRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = notebooks_projects_locations_operations_cancel_builder(client, name, body)?;
    notebooks_projects_locations_operations_cancel_execute(builder)
}

/// GET v2/{+name}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `notebooks_projects_locations_operations_delete_execute()` to send, or `notebooks_projects_locations_operations_delete` for simplest API.

pub fn notebooks_projects_locations_operations_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `notebooks_projects_locations_operations_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from notebooks_projects_locations_operations_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn notebooks_projects_locations_operations_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `notebooks_projects_locations_operations_delete_builder()` + `notebooks_projects_locations_operations_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn notebooks_projects_locations_operations_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = notebooks_projects_locations_operations_delete_builder(client, name)?;
    notebooks_projects_locations_operations_delete_execute(builder)
}

/// GET v2/{+name}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `notebooks_projects_locations_operations_get_execute()` to send, or `notebooks_projects_locations_operations_get` for simplest API.

pub fn notebooks_projects_locations_operations_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `notebooks_projects_locations_operations_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from notebooks_projects_locations_operations_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn notebooks_projects_locations_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `notebooks_projects_locations_operations_get_builder()` + `notebooks_projects_locations_operations_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn notebooks_projects_locations_operations_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = notebooks_projects_locations_operations_get_builder(client, name)?;
    notebooks_projects_locations_operations_get_execute(builder)
}

/// GET v2/{+name}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `notebooks_projects_locations_operations_list_execute()` to send, or `notebooks_projects_locations_operations_list` for simplest API.

pub fn notebooks_projects_locations_operations_list_builder(
    client: &SimpleHttpClient,
    name: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}/operations", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "returnPartialSuccess",
            returnPartialSuccess
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v2/{+name}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `notebooks_projects_locations_operations_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from notebooks_projects_locations_operations_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn notebooks_projects_locations_operations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListOperationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListOperationsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `notebooks_projects_locations_operations_list_builder()` + `notebooks_projects_locations_operations_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn notebooks_projects_locations_operations_list(
    client: &SimpleHttpClient,
    name: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListOperationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = notebooks_projects_locations_operations_list_builder(
        client,
        name,
        filter,
        pageSize,
        pageToken,
        returnPartialSuccess,
    )?;
    notebooks_projects_locations_operations_list_execute(builder)
}
