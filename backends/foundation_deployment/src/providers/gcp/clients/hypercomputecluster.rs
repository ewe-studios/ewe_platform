//! Auto-generated API clients for gcp/hypercomputecluster.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp`

#![cfg(feature = "gcp")]


use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{
    execute, BoxedSendExecutionAction, StreamIterator, StreamIteratorExt, TaskIterator,
    TaskIteratorExt,
};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, DnsResolver, RequestIntro, SimpleHttpClient,
    SystemDnsResolver,
};
use foundation_db::state::resource_identifier::ResourceIdentifier;
use foundation_macros::JsonHash;
use serde::Serialize;

/// GET v1/projects/{projectsId}/locations/{locationsId}
/// Gets information about a location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `hypercomputecluster_projects_locations_get_execute()` to send, or `hypercomputecluster_projects_locations_get` for simplest API.

pub fn hypercomputecluster_projects_locations_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://hypercomputecluster.googleapis.com/v1/projects/{}/locations/{locationsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}
/// Gets information about a location.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `hypercomputecluster_projects_locations_get_execute()` or `hypercomputecluster_projects_locations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `hypercomputecluster_projects_locations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn hypercomputecluster_projects_locations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Location>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Location = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}
/// Gets information about a location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `hypercomputecluster_projects_locations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `hypercomputecluster_projects_locations_get_task()`.
/// For the simplest API, use `hypercomputecluster_projects_locations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `hypercomputecluster_projects_locations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn hypercomputecluster_projects_locations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Location>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = hypercomputecluster_projects_locations_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`hypercomputecluster_projects_locations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct HypercomputeclusterProjectsLocationsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}
/// Gets information about a location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `hypercomputecluster_projects_locations_get_builder()` + `hypercomputecluster_projects_locations_get_execute()`.
/// For task-level control, use `hypercomputecluster_projects_locations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn hypercomputecluster_projects_locations_get(
    client: &SimpleHttpClient,
    args: &HypercomputeclusterProjectsLocationsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Location>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = hypercomputecluster_projects_locations_get_builder(client, &args.name)?;
    hypercomputecluster_projects_locations_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations
/// Lists information about the supported locations for this service. This method lists locations based on the resource scope provided in the [ListLocationsRequest.name] field: * **Global locations**: If name is empty, the method lists the public locations available to all projects. * **Project-specific locations**: If name follows the format `projects/{project}`, the method lists locations visible to that specific project. This includes public, private, or other project-specific locations enabled for the project. For `gRPC` and client library implementations, the resource name is passed as the name field. For direct service calls, the resource name is incorporated into the request path based on the specific service implementation and version.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `hypercomputecluster_projects_locations_list_execute()` to send, or `hypercomputecluster_projects_locations_list` for simplest API.

pub fn hypercomputecluster_projects_locations_list_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    extraLocationTypes: &Option<String>,
    filter: &Option<String>,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://hypercomputecluster.googleapis.com/v1/projects/{}/locations",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = extraLocationTypes.as_ref() {
        query_parts.push(format!("extraLocationTypes={}", val));
    }
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations
/// Lists information about the supported locations for this service. This method lists locations based on the resource scope provided in the [ListLocationsRequest.name] field: * **Global locations**: If name is empty, the method lists the public locations available to all projects. * **Project-specific locations**: If name follows the format `projects/{project}`, the method lists locations visible to that specific project. This includes public, private, or other project-specific locations enabled for the project. For `gRPC` and client library implementations, the resource name is passed as the name field. For direct service calls, the resource name is incorporated into the request path based on the specific service implementation and version.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `hypercomputecluster_projects_locations_list_execute()` or `hypercomputecluster_projects_locations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `hypercomputecluster_projects_locations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn hypercomputecluster_projects_locations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListLocationsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListLocationsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations
/// Lists information about the supported locations for this service. This method lists locations based on the resource scope provided in the [ListLocationsRequest.name] field: * **Global locations**: If name is empty, the method lists the public locations available to all projects. * **Project-specific locations**: If name follows the format `projects/{project}`, the method lists locations visible to that specific project. This includes public, private, or other project-specific locations enabled for the project. For `gRPC` and client library implementations, the resource name is passed as the name field. For direct service calls, the resource name is incorporated into the request path based on the specific service implementation and version.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `hypercomputecluster_projects_locations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `hypercomputecluster_projects_locations_list_task()`.
/// For the simplest API, use `hypercomputecluster_projects_locations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `hypercomputecluster_projects_locations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn hypercomputecluster_projects_locations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListLocationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = hypercomputecluster_projects_locations_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`hypercomputecluster_projects_locations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct HypercomputeclusterProjectsLocationsListArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: extraLocationTypes
    pub extraLocationTypes: Option<String>,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/projects/{projectsId}/locations
/// Lists information about the supported locations for this service. This method lists locations based on the resource scope provided in the [ListLocationsRequest.name] field: * **Global locations**: If name is empty, the method lists the public locations available to all projects. * **Project-specific locations**: If name follows the format `projects/{project}`, the method lists locations visible to that specific project. This includes public, private, or other project-specific locations enabled for the project. For `gRPC` and client library implementations, the resource name is passed as the name field. For direct service calls, the resource name is incorporated into the request path based on the specific service implementation and version.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `hypercomputecluster_projects_locations_list_builder()` + `hypercomputecluster_projects_locations_list_execute()`.
/// For task-level control, use `hypercomputecluster_projects_locations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn hypercomputecluster_projects_locations_list(
    client: &SimpleHttpClient,
    args: &HypercomputeclusterProjectsLocationsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListLocationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = hypercomputecluster_projects_locations_list_builder(
        client,
        &args.name,
        &args.extraLocationTypes,
        &args.filter,
        &args.pageSize,
        &args.pageToken,
    )?;
    hypercomputecluster_projects_locations_list_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/clusters
/// Creates a new Cluster in a given project and location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `hypercomputecluster_projects_locations_clusters_create_execute()` to send, or `hypercomputecluster_projects_locations_clusters_create` for simplest API.

pub fn hypercomputecluster_projects_locations_clusters_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    clusterId: &Option<String>,
    requestId: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://hypercomputecluster.googleapis.com/v1/projects/{}/locations/{locationsId}/clusters",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = clusterId.as_ref() {
        query_parts.push(format!("clusterId={}", val));
    }
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/clusters
/// Creates a new Cluster in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `hypercomputecluster_projects_locations_clusters_create_execute()` or `hypercomputecluster_projects_locations_clusters_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `hypercomputecluster_projects_locations_clusters_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn hypercomputecluster_projects_locations_clusters_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/clusters
/// Creates a new Cluster in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `hypercomputecluster_projects_locations_clusters_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `hypercomputecluster_projects_locations_clusters_create_task()`.
/// For the simplest API, use `hypercomputecluster_projects_locations_clusters_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `hypercomputecluster_projects_locations_clusters_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn hypercomputecluster_projects_locations_clusters_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = hypercomputecluster_projects_locations_clusters_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`hypercomputecluster_projects_locations_clusters_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct HypercomputeclusterProjectsLocationsClustersCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: clusterId
    pub clusterId: Option<String>,
    /// Query parameter: requestId
    pub requestId: Option<String>,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/clusters
/// Creates a new Cluster in a given project and location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `hypercomputecluster_projects_locations_clusters_create_builder()` + `hypercomputecluster_projects_locations_clusters_create_execute()`.
/// For task-level control, use `hypercomputecluster_projects_locations_clusters_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn hypercomputecluster_projects_locations_clusters_create(
    client: &SimpleHttpClient,
    args: &HypercomputeclusterProjectsLocationsClustersCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = hypercomputecluster_projects_locations_clusters_create_builder(
        client,
        &args.parent,
        &args.clusterId,
        &args.requestId,
    )?;
    hypercomputecluster_projects_locations_clusters_create_execute(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/clusters/{clustersId}
/// Deletes a single Cluster.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `hypercomputecluster_projects_locations_clusters_delete_execute()` to send, or `hypercomputecluster_projects_locations_clusters_delete` for simplest API.

pub fn hypercomputecluster_projects_locations_clusters_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    requestId: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://hypercomputecluster.googleapis.com/v1/projects/{}/locations/{locationsId}/clusters/{clustersId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .delete(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/clusters/{clustersId}
/// Deletes a single Cluster.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `hypercomputecluster_projects_locations_clusters_delete_execute()` or `hypercomputecluster_projects_locations_clusters_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `hypercomputecluster_projects_locations_clusters_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn hypercomputecluster_projects_locations_clusters_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/clusters/{clustersId}
/// Deletes a single Cluster.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `hypercomputecluster_projects_locations_clusters_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `hypercomputecluster_projects_locations_clusters_delete_task()`.
/// For the simplest API, use `hypercomputecluster_projects_locations_clusters_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `hypercomputecluster_projects_locations_clusters_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn hypercomputecluster_projects_locations_clusters_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = hypercomputecluster_projects_locations_clusters_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`hypercomputecluster_projects_locations_clusters_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct HypercomputeclusterProjectsLocationsClustersDeleteArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: requestId
    pub requestId: Option<String>,
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/clusters/{clustersId}
/// Deletes a single Cluster.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `hypercomputecluster_projects_locations_clusters_delete_builder()` + `hypercomputecluster_projects_locations_clusters_delete_execute()`.
/// For task-level control, use `hypercomputecluster_projects_locations_clusters_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn hypercomputecluster_projects_locations_clusters_delete(
    client: &SimpleHttpClient,
    args: &HypercomputeclusterProjectsLocationsClustersDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = hypercomputecluster_projects_locations_clusters_delete_builder(
        client,
        &args.name,
        &args.requestId,
    )?;
    hypercomputecluster_projects_locations_clusters_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/clusters/{clustersId}
/// Gets details of a single Cluster.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `hypercomputecluster_projects_locations_clusters_get_execute()` to send, or `hypercomputecluster_projects_locations_clusters_get` for simplest API.

pub fn hypercomputecluster_projects_locations_clusters_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://hypercomputecluster.googleapis.com/v1/projects/{}/locations/{locationsId}/clusters/{clustersId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/clusters/{clustersId}
/// Gets details of a single Cluster.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `hypercomputecluster_projects_locations_clusters_get_execute()` or `hypercomputecluster_projects_locations_clusters_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `hypercomputecluster_projects_locations_clusters_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn hypercomputecluster_projects_locations_clusters_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Cluster>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Cluster = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/clusters/{clustersId}
/// Gets details of a single Cluster.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `hypercomputecluster_projects_locations_clusters_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `hypercomputecluster_projects_locations_clusters_get_task()`.
/// For the simplest API, use `hypercomputecluster_projects_locations_clusters_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `hypercomputecluster_projects_locations_clusters_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn hypercomputecluster_projects_locations_clusters_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Cluster>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = hypercomputecluster_projects_locations_clusters_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`hypercomputecluster_projects_locations_clusters_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct HypercomputeclusterProjectsLocationsClustersGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/clusters/{clustersId}
/// Gets details of a single Cluster.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `hypercomputecluster_projects_locations_clusters_get_builder()` + `hypercomputecluster_projects_locations_clusters_get_execute()`.
/// For task-level control, use `hypercomputecluster_projects_locations_clusters_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn hypercomputecluster_projects_locations_clusters_get(
    client: &SimpleHttpClient,
    args: &HypercomputeclusterProjectsLocationsClustersGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Cluster>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = hypercomputecluster_projects_locations_clusters_get_builder(client, &args.name)?;
    hypercomputecluster_projects_locations_clusters_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/clusters
/// Lists Clusters in a given project and location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `hypercomputecluster_projects_locations_clusters_list_execute()` to send, or `hypercomputecluster_projects_locations_clusters_list` for simplest API.

pub fn hypercomputecluster_projects_locations_clusters_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    filter: &Option<String>,
    orderBy: &Option<String>,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://hypercomputecluster.googleapis.com/v1/projects/{}/locations/{locationsId}/clusters",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/clusters
/// Lists Clusters in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `hypercomputecluster_projects_locations_clusters_list_execute()` or `hypercomputecluster_projects_locations_clusters_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `hypercomputecluster_projects_locations_clusters_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn hypercomputecluster_projects_locations_clusters_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListClustersResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListClustersResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/clusters
/// Lists Clusters in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `hypercomputecluster_projects_locations_clusters_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `hypercomputecluster_projects_locations_clusters_list_task()`.
/// For the simplest API, use `hypercomputecluster_projects_locations_clusters_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `hypercomputecluster_projects_locations_clusters_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn hypercomputecluster_projects_locations_clusters_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListClustersResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = hypercomputecluster_projects_locations_clusters_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`hypercomputecluster_projects_locations_clusters_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct HypercomputeclusterProjectsLocationsClustersListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: orderBy
    pub orderBy: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/clusters
/// Lists Clusters in a given project and location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `hypercomputecluster_projects_locations_clusters_list_builder()` + `hypercomputecluster_projects_locations_clusters_list_execute()`.
/// For task-level control, use `hypercomputecluster_projects_locations_clusters_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn hypercomputecluster_projects_locations_clusters_list(
    client: &SimpleHttpClient,
    args: &HypercomputeclusterProjectsLocationsClustersListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListClustersResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = hypercomputecluster_projects_locations_clusters_list_builder(
        client,
        &args.parent,
        &args.filter,
        &args.orderBy,
        &args.pageSize,
        &args.pageToken,
    )?;
    hypercomputecluster_projects_locations_clusters_list_execute(builder)
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/clusters/{clustersId}
/// Updates the parameters of a single Cluster.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `hypercomputecluster_projects_locations_clusters_patch_execute()` to send, or `hypercomputecluster_projects_locations_clusters_patch` for simplest API.

pub fn hypercomputecluster_projects_locations_clusters_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    requestId: &Option<String>,
    updateMask: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://hypercomputecluster.googleapis.com/v1/projects/{}/locations/{locationsId}/clusters/{clustersId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/clusters/{clustersId}
/// Updates the parameters of a single Cluster.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `hypercomputecluster_projects_locations_clusters_patch_execute()` or `hypercomputecluster_projects_locations_clusters_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `hypercomputecluster_projects_locations_clusters_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn hypercomputecluster_projects_locations_clusters_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/clusters/{clustersId}
/// Updates the parameters of a single Cluster.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `hypercomputecluster_projects_locations_clusters_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `hypercomputecluster_projects_locations_clusters_patch_task()`.
/// For the simplest API, use `hypercomputecluster_projects_locations_clusters_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `hypercomputecluster_projects_locations_clusters_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn hypercomputecluster_projects_locations_clusters_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = hypercomputecluster_projects_locations_clusters_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`hypercomputecluster_projects_locations_clusters_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct HypercomputeclusterProjectsLocationsClustersPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: requestId
    pub requestId: Option<String>,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/clusters/{clustersId}
/// Updates the parameters of a single Cluster.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `hypercomputecluster_projects_locations_clusters_patch_builder()` + `hypercomputecluster_projects_locations_clusters_patch_execute()`.
/// For task-level control, use `hypercomputecluster_projects_locations_clusters_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn hypercomputecluster_projects_locations_clusters_patch(
    client: &SimpleHttpClient,
    args: &HypercomputeclusterProjectsLocationsClustersPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = hypercomputecluster_projects_locations_clusters_patch_builder(
        client,
        &args.name,
        &args.requestId,
        &args.updateMask,
    )?;
    hypercomputecluster_projects_locations_clusters_patch_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn''t support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `hypercomputecluster_projects_locations_operations_cancel_execute()` to send, or `hypercomputecluster_projects_locations_operations_cancel` for simplest API.

pub fn hypercomputecluster_projects_locations_operations_cancel_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://hypercomputecluster.googleapis.com/v1/projects/{}/locations/{locationsId}/operations/{operationsId}:cancel",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn''t support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `hypercomputecluster_projects_locations_operations_cancel_execute()` or `hypercomputecluster_projects_locations_operations_cancel`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `hypercomputecluster_projects_locations_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn hypercomputecluster_projects_locations_operations_cancel_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn''t support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `hypercomputecluster_projects_locations_operations_cancel_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `hypercomputecluster_projects_locations_operations_cancel_task()`.
/// For the simplest API, use `hypercomputecluster_projects_locations_operations_cancel()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `hypercomputecluster_projects_locations_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn hypercomputecluster_projects_locations_operations_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = hypercomputecluster_projects_locations_operations_cancel_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`hypercomputecluster_projects_locations_operations_cancel`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct HypercomputeclusterProjectsLocationsOperationsCancelArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn''t support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `hypercomputecluster_projects_locations_operations_cancel_builder()` + `hypercomputecluster_projects_locations_operations_cancel_execute()`.
/// For task-level control, use `hypercomputecluster_projects_locations_operations_cancel_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn hypercomputecluster_projects_locations_operations_cancel(
    client: &SimpleHttpClient,
    args: &HypercomputeclusterProjectsLocationsOperationsCancelArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        hypercomputecluster_projects_locations_operations_cancel_builder(client, &args.name)?;
    hypercomputecluster_projects_locations_operations_cancel_execute(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn''t support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `hypercomputecluster_projects_locations_operations_delete_execute()` to send, or `hypercomputecluster_projects_locations_operations_delete` for simplest API.

pub fn hypercomputecluster_projects_locations_operations_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://hypercomputecluster.googleapis.com/v1/projects/{}/locations/{locationsId}/operations/{operationsId}",
        name,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn''t support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `hypercomputecluster_projects_locations_operations_delete_execute()` or `hypercomputecluster_projects_locations_operations_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `hypercomputecluster_projects_locations_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn hypercomputecluster_projects_locations_operations_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn''t support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `hypercomputecluster_projects_locations_operations_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `hypercomputecluster_projects_locations_operations_delete_task()`.
/// For the simplest API, use `hypercomputecluster_projects_locations_operations_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `hypercomputecluster_projects_locations_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn hypercomputecluster_projects_locations_operations_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = hypercomputecluster_projects_locations_operations_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`hypercomputecluster_projects_locations_operations_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct HypercomputeclusterProjectsLocationsOperationsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn''t support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `hypercomputecluster_projects_locations_operations_delete_builder()` + `hypercomputecluster_projects_locations_operations_delete_execute()`.
/// For task-level control, use `hypercomputecluster_projects_locations_operations_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn hypercomputecluster_projects_locations_operations_delete(
    client: &SimpleHttpClient,
    args: &HypercomputeclusterProjectsLocationsOperationsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        hypercomputecluster_projects_locations_operations_delete_builder(client, &args.name)?;
    hypercomputecluster_projects_locations_operations_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `hypercomputecluster_projects_locations_operations_get_execute()` to send, or `hypercomputecluster_projects_locations_operations_get` for simplest API.

pub fn hypercomputecluster_projects_locations_operations_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://hypercomputecluster.googleapis.com/v1/projects/{}/locations/{locationsId}/operations/{operationsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `hypercomputecluster_projects_locations_operations_get_execute()` or `hypercomputecluster_projects_locations_operations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `hypercomputecluster_projects_locations_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn hypercomputecluster_projects_locations_operations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `hypercomputecluster_projects_locations_operations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `hypercomputecluster_projects_locations_operations_get_task()`.
/// For the simplest API, use `hypercomputecluster_projects_locations_operations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `hypercomputecluster_projects_locations_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn hypercomputecluster_projects_locations_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = hypercomputecluster_projects_locations_operations_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`hypercomputecluster_projects_locations_operations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct HypercomputeclusterProjectsLocationsOperationsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `hypercomputecluster_projects_locations_operations_get_builder()` + `hypercomputecluster_projects_locations_operations_get_execute()`.
/// For task-level control, use `hypercomputecluster_projects_locations_operations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn hypercomputecluster_projects_locations_operations_get(
    client: &SimpleHttpClient,
    args: &HypercomputeclusterProjectsLocationsOperationsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        hypercomputecluster_projects_locations_operations_get_builder(client, &args.name)?;
    hypercomputecluster_projects_locations_operations_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn''t support this method, it returns UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `hypercomputecluster_projects_locations_operations_list_execute()` to send, or `hypercomputecluster_projects_locations_operations_list` for simplest API.

pub fn hypercomputecluster_projects_locations_operations_list_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    filter: &Option<String>,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
    returnPartialSuccess: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://hypercomputecluster.googleapis.com/v1/projects/{}/locations/{locationsId}/operations",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn''t support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `hypercomputecluster_projects_locations_operations_list_execute()` or `hypercomputecluster_projects_locations_operations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `hypercomputecluster_projects_locations_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn hypercomputecluster_projects_locations_operations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListOperationsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListOperationsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn''t support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `hypercomputecluster_projects_locations_operations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `hypercomputecluster_projects_locations_operations_list_task()`.
/// For the simplest API, use `hypercomputecluster_projects_locations_operations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `hypercomputecluster_projects_locations_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn hypercomputecluster_projects_locations_operations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListOperationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = hypercomputecluster_projects_locations_operations_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`hypercomputecluster_projects_locations_operations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct HypercomputeclusterProjectsLocationsOperationsListArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn''t support this method, it returns UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `hypercomputecluster_projects_locations_operations_list_builder()` + `hypercomputecluster_projects_locations_operations_list_execute()`.
/// For task-level control, use `hypercomputecluster_projects_locations_operations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn hypercomputecluster_projects_locations_operations_list(
    client: &SimpleHttpClient,
    args: &HypercomputeclusterProjectsLocationsOperationsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListOperationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = hypercomputecluster_projects_locations_operations_list_builder(
        client,
        &args.name,
        &args.filter,
        &args.pageSize,
        &args.pageToken,
        &args.returnPartialSuccess,
    )?;
    hypercomputecluster_projects_locations_operations_list_execute(builder)
}

// =============================================================================
// ResourceIdentifier implementation for Location
// =============================================================================

/// ResourceIdentifier implementation for Location with HypercomputeclusterProjectsLocationsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<HypercomputeclusterProjectsLocationsGetArgs> for Location {
    fn generate_resource_id(&self, input: &HypercomputeclusterProjectsLocationsGetArgs) -> String {
        format!("gcp::hypercomputecluster::Location/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::hypercomputecluster::Location"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListLocationsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListLocationsResponse with HypercomputeclusterProjectsLocationsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<HypercomputeclusterProjectsLocationsListArgs> for ListLocationsResponse {
    fn generate_resource_id(&self, input: &HypercomputeclusterProjectsLocationsListArgs) -> String {
        format!(
            "gcp::hypercomputecluster::ListLocationsResponse/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::hypercomputecluster::ListLocationsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with HypercomputeclusterProjectsLocationsClustersCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<HypercomputeclusterProjectsLocationsClustersCreateArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &HypercomputeclusterProjectsLocationsClustersCreateArgs,
    ) -> String {
        format!("gcp::hypercomputecluster::Operation/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::hypercomputecluster::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with HypercomputeclusterProjectsLocationsClustersDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<HypercomputeclusterProjectsLocationsClustersDeleteArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &HypercomputeclusterProjectsLocationsClustersDeleteArgs,
    ) -> String {
        format!("gcp::hypercomputecluster::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::hypercomputecluster::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Cluster
// =============================================================================

/// ResourceIdentifier implementation for Cluster with HypercomputeclusterProjectsLocationsClustersGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<HypercomputeclusterProjectsLocationsClustersGetArgs> for Cluster {
    fn generate_resource_id(
        &self,
        input: &HypercomputeclusterProjectsLocationsClustersGetArgs,
    ) -> String {
        format!("gcp::hypercomputecluster::Cluster/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::hypercomputecluster::Cluster"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListClustersResponse
// =============================================================================

/// ResourceIdentifier implementation for ListClustersResponse with HypercomputeclusterProjectsLocationsClustersListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<HypercomputeclusterProjectsLocationsClustersListArgs>
    for ListClustersResponse
{
    fn generate_resource_id(
        &self,
        input: &HypercomputeclusterProjectsLocationsClustersListArgs,
    ) -> String {
        format!(
            "gcp::hypercomputecluster::ListClustersResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::hypercomputecluster::ListClustersResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with HypercomputeclusterProjectsLocationsClustersPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<HypercomputeclusterProjectsLocationsClustersPatchArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &HypercomputeclusterProjectsLocationsClustersPatchArgs,
    ) -> String {
        format!("gcp::hypercomputecluster::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::hypercomputecluster::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Empty
// =============================================================================

/// ResourceIdentifier implementation for Empty with HypercomputeclusterProjectsLocationsOperationsCancelArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<HypercomputeclusterProjectsLocationsOperationsCancelArgs> for Empty {
    fn generate_resource_id(
        &self,
        input: &HypercomputeclusterProjectsLocationsOperationsCancelArgs,
    ) -> String {
        format!("gcp::hypercomputecluster::Empty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::hypercomputecluster::Empty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Empty
// =============================================================================

/// ResourceIdentifier implementation for Empty with HypercomputeclusterProjectsLocationsOperationsDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<HypercomputeclusterProjectsLocationsOperationsDeleteArgs> for Empty {
    fn generate_resource_id(
        &self,
        input: &HypercomputeclusterProjectsLocationsOperationsDeleteArgs,
    ) -> String {
        format!("gcp::hypercomputecluster::Empty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::hypercomputecluster::Empty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with HypercomputeclusterProjectsLocationsOperationsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<HypercomputeclusterProjectsLocationsOperationsGetArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &HypercomputeclusterProjectsLocationsOperationsGetArgs,
    ) -> String {
        format!("gcp::hypercomputecluster::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::hypercomputecluster::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListOperationsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListOperationsResponse with HypercomputeclusterProjectsLocationsOperationsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<HypercomputeclusterProjectsLocationsOperationsListArgs>
    for ListOperationsResponse
{
    fn generate_resource_id(
        &self,
        input: &HypercomputeclusterProjectsLocationsOperationsListArgs,
    ) -> String {
        format!(
            "gcp::hypercomputecluster::ListOperationsResponse/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::hypercomputecluster::ListOperationsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}
