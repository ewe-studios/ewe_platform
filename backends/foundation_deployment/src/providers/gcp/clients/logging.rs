//! Auto-generated API clients for gcp/logging.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp`

#![cfg(feature = "gcp")]


use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{
    execute, BoxedSendExecutionAction, StreamIterator, StreamIteratorExt, TaskIterator,
    TaskIteratorExt,
};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, DnsResolver, RequestIntro, SimpleHttpClient,
    SystemDnsResolver,
};
use foundation_db::state::resource_identifier::ResourceIdentifier;
use foundation_macros::JsonHash;
use serde::Serialize;

/// GET v2/billingAccounts/{billingAccountsId}/cmekSettings
/// Gets the Logging CMEK settings for the given resource.Note: CMEK for the Log Router can be configured for Google Cloud projects, folders, organizations, and billing accounts. Once configured for an organization, it applies to all projects and folders in the Google Cloud organization.See Configure CMEK for Cloud Logging (<https://docs.cloud.google.`com/logging/docs/routing/managed-encryption`>) for more information.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_billing_accounts_get_cmek_settings_execute()` to send, or `logging_billing_accounts_get_cmek_settings` for simplest API.

pub fn logging_billing_accounts_get_cmek_settings_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/billingAccounts/{}/cmekSettings",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/billingAccounts/{billingAccountsId}/cmekSettings
/// Gets the Logging CMEK settings for the given resource.Note: CMEK for the Log Router can be configured for Google Cloud projects, folders, organizations, and billing accounts. Once configured for an organization, it applies to all projects and folders in the Google Cloud organization.See Configure CMEK for Cloud Logging (<https://docs.cloud.google.`com/logging/docs/routing/managed-encryption`>) for more information.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_billing_accounts_get_cmek_settings_execute()` or `logging_billing_accounts_get_cmek_settings`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_billing_accounts_get_cmek_settings_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_billing_accounts_get_cmek_settings_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<CmekSettings>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: CmekSettings = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/billingAccounts/{billingAccountsId}/cmekSettings
/// Gets the Logging CMEK settings for the given resource.Note: CMEK for the Log Router can be configured for Google Cloud projects, folders, organizations, and billing accounts. Once configured for an organization, it applies to all projects and folders in the Google Cloud organization.See Configure CMEK for Cloud Logging (<https://docs.cloud.google.`com/logging/docs/routing/managed-encryption`>) for more information.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_billing_accounts_get_cmek_settings_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_billing_accounts_get_cmek_settings_task()`.
/// For the simplest API, use `logging_billing_accounts_get_cmek_settings()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_billing_accounts_get_cmek_settings_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_billing_accounts_get_cmek_settings_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CmekSettings>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = logging_billing_accounts_get_cmek_settings_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_billing_accounts_get_cmek_settings`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingBillingAccountsGetCmekSettingsArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v2/billingAccounts/{billingAccountsId}/cmekSettings
/// Gets the Logging CMEK settings for the given resource.Note: CMEK for the Log Router can be configured for Google Cloud projects, folders, organizations, and billing accounts. Once configured for an organization, it applies to all projects and folders in the Google Cloud organization.See Configure CMEK for Cloud Logging (<https://docs.cloud.google.`com/logging/docs/routing/managed-encryption`>) for more information.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_billing_accounts_get_cmek_settings_builder()` + `logging_billing_accounts_get_cmek_settings_execute()`.
/// For task-level control, use `logging_billing_accounts_get_cmek_settings_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_billing_accounts_get_cmek_settings(
    client: &SimpleHttpClient,
    args: &LoggingBillingAccountsGetCmekSettingsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CmekSettings>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = logging_billing_accounts_get_cmek_settings_builder(client, &args.name)?;
    logging_billing_accounts_get_cmek_settings_execute(builder)
}

/// GET v2/billingAccounts/{billingAccountsId}/settings
/// Gets the settings for the given resource.Note: Settings can be retrieved for Google Cloud projects, folders, organizations, and billing accounts.See View default resource settings for Logging (<https://docs.cloud.google.`com/logging/docs/default-settings`#view-org-settings>) for more information.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_billing_accounts_get_settings_execute()` to send, or `logging_billing_accounts_get_settings` for simplest API.

pub fn logging_billing_accounts_get_settings_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/billingAccounts/{}/settings",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/billingAccounts/{billingAccountsId}/settings
/// Gets the settings for the given resource.Note: Settings can be retrieved for Google Cloud projects, folders, organizations, and billing accounts.See View default resource settings for Logging (<https://docs.cloud.google.`com/logging/docs/default-settings`#view-org-settings>) for more information.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_billing_accounts_get_settings_execute()` or `logging_billing_accounts_get_settings`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_billing_accounts_get_settings_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_billing_accounts_get_settings_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Settings>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Settings = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/billingAccounts/{billingAccountsId}/settings
/// Gets the settings for the given resource.Note: Settings can be retrieved for Google Cloud projects, folders, organizations, and billing accounts.See View default resource settings for Logging (<https://docs.cloud.google.`com/logging/docs/default-settings`#view-org-settings>) for more information.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_billing_accounts_get_settings_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_billing_accounts_get_settings_task()`.
/// For the simplest API, use `logging_billing_accounts_get_settings()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_billing_accounts_get_settings_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_billing_accounts_get_settings_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Settings>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = logging_billing_accounts_get_settings_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_billing_accounts_get_settings`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingBillingAccountsGetSettingsArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v2/billingAccounts/{billingAccountsId}/settings
/// Gets the settings for the given resource.Note: Settings can be retrieved for Google Cloud projects, folders, organizations, and billing accounts.See View default resource settings for Logging (<https://docs.cloud.google.`com/logging/docs/default-settings`#view-org-settings>) for more information.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_billing_accounts_get_settings_builder()` + `logging_billing_accounts_get_settings_execute()`.
/// For task-level control, use `logging_billing_accounts_get_settings_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_billing_accounts_get_settings(
    client: &SimpleHttpClient,
    args: &LoggingBillingAccountsGetSettingsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Settings>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_billing_accounts_get_settings_builder(client, &args.name)?;
    logging_billing_accounts_get_settings_execute(builder)
}

/// POST v2/billingAccounts/{billingAccountsId}/exclusions
/// Creates a new exclusion in the _Default sink in a specified parent resource. Only log entries belonging to that resource can be excluded. You can have up to 10 exclusions in a resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_billing_accounts_exclusions_create_execute()` to send, or `logging_billing_accounts_exclusions_create` for simplest API.

pub fn logging_billing_accounts_exclusions_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/billingAccounts/{}/exclusions",
        parent,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v2/billingAccounts/{billingAccountsId}/exclusions
/// Creates a new exclusion in the _Default sink in a specified parent resource. Only log entries belonging to that resource can be excluded. You can have up to 10 exclusions in a resource.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_billing_accounts_exclusions_create_execute()` or `logging_billing_accounts_exclusions_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_billing_accounts_exclusions_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_billing_accounts_exclusions_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<LogExclusion>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LogExclusion = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v2/billingAccounts/{billingAccountsId}/exclusions
/// Creates a new exclusion in the _Default sink in a specified parent resource. Only log entries belonging to that resource can be excluded. You can have up to 10 exclusions in a resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_billing_accounts_exclusions_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_billing_accounts_exclusions_create_task()`.
/// For the simplest API, use `logging_billing_accounts_exclusions_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_billing_accounts_exclusions_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_billing_accounts_exclusions_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogExclusion>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = logging_billing_accounts_exclusions_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_billing_accounts_exclusions_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingBillingAccountsExclusionsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// POST v2/billingAccounts/{billingAccountsId}/exclusions
/// Creates a new exclusion in the _Default sink in a specified parent resource. Only log entries belonging to that resource can be excluded. You can have up to 10 exclusions in a resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_billing_accounts_exclusions_create_builder()` + `logging_billing_accounts_exclusions_create_execute()`.
/// For task-level control, use `logging_billing_accounts_exclusions_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_billing_accounts_exclusions_create(
    client: &SimpleHttpClient,
    args: &LoggingBillingAccountsExclusionsCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogExclusion>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = logging_billing_accounts_exclusions_create_builder(client, &args.parent)?;
    logging_billing_accounts_exclusions_create_execute(builder)
}

/// DELETE v2/billingAccounts/{billingAccountsId}/exclusions/{exclusionsId}
/// Deletes an exclusion in the _Default sink.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_billing_accounts_exclusions_delete_execute()` to send, or `logging_billing_accounts_exclusions_delete` for simplest API.

pub fn logging_billing_accounts_exclusions_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/billingAccounts/{}/exclusions/{exclusionsId}",
        name,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v2/billingAccounts/{billingAccountsId}/exclusions/{exclusionsId}
/// Deletes an exclusion in the _Default sink.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_billing_accounts_exclusions_delete_execute()` or `logging_billing_accounts_exclusions_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_billing_accounts_exclusions_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_billing_accounts_exclusions_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v2/billingAccounts/{billingAccountsId}/exclusions/{exclusionsId}
/// Deletes an exclusion in the _Default sink.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_billing_accounts_exclusions_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_billing_accounts_exclusions_delete_task()`.
/// For the simplest API, use `logging_billing_accounts_exclusions_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_billing_accounts_exclusions_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_billing_accounts_exclusions_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = logging_billing_accounts_exclusions_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_billing_accounts_exclusions_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingBillingAccountsExclusionsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// DELETE v2/billingAccounts/{billingAccountsId}/exclusions/{exclusionsId}
/// Deletes an exclusion in the _Default sink.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_billing_accounts_exclusions_delete_builder()` + `logging_billing_accounts_exclusions_delete_execute()`.
/// For task-level control, use `logging_billing_accounts_exclusions_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_billing_accounts_exclusions_delete(
    client: &SimpleHttpClient,
    args: &LoggingBillingAccountsExclusionsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_billing_accounts_exclusions_delete_builder(client, &args.name)?;
    logging_billing_accounts_exclusions_delete_execute(builder)
}

/// GET v2/billingAccounts/{billingAccountsId}/exclusions/{exclusionsId}
/// Gets the description of an exclusion in the _Default sink.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_billing_accounts_exclusions_get_execute()` to send, or `logging_billing_accounts_exclusions_get` for simplest API.

pub fn logging_billing_accounts_exclusions_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/billingAccounts/{}/exclusions/{exclusionsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/billingAccounts/{billingAccountsId}/exclusions/{exclusionsId}
/// Gets the description of an exclusion in the _Default sink.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_billing_accounts_exclusions_get_execute()` or `logging_billing_accounts_exclusions_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_billing_accounts_exclusions_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_billing_accounts_exclusions_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<LogExclusion>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LogExclusion = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/billingAccounts/{billingAccountsId}/exclusions/{exclusionsId}
/// Gets the description of an exclusion in the _Default sink.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_billing_accounts_exclusions_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_billing_accounts_exclusions_get_task()`.
/// For the simplest API, use `logging_billing_accounts_exclusions_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_billing_accounts_exclusions_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_billing_accounts_exclusions_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogExclusion>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = logging_billing_accounts_exclusions_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_billing_accounts_exclusions_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingBillingAccountsExclusionsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v2/billingAccounts/{billingAccountsId}/exclusions/{exclusionsId}
/// Gets the description of an exclusion in the _Default sink.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_billing_accounts_exclusions_get_builder()` + `logging_billing_accounts_exclusions_get_execute()`.
/// For task-level control, use `logging_billing_accounts_exclusions_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_billing_accounts_exclusions_get(
    client: &SimpleHttpClient,
    args: &LoggingBillingAccountsExclusionsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogExclusion>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = logging_billing_accounts_exclusions_get_builder(client, &args.name)?;
    logging_billing_accounts_exclusions_get_execute(builder)
}

/// GET v2/billingAccounts/{billingAccountsId}/exclusions
/// Lists all the exclusions on the _Default sink in a parent resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_billing_accounts_exclusions_list_execute()` to send, or `logging_billing_accounts_exclusions_list` for simplest API.

pub fn logging_billing_accounts_exclusions_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/billingAccounts/{}/exclusions",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/billingAccounts/{billingAccountsId}/exclusions
/// Lists all the exclusions on the _Default sink in a parent resource.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_billing_accounts_exclusions_list_execute()` or `logging_billing_accounts_exclusions_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_billing_accounts_exclusions_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_billing_accounts_exclusions_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListExclusionsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListExclusionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/billingAccounts/{billingAccountsId}/exclusions
/// Lists all the exclusions on the _Default sink in a parent resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_billing_accounts_exclusions_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_billing_accounts_exclusions_list_task()`.
/// For the simplest API, use `logging_billing_accounts_exclusions_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_billing_accounts_exclusions_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_billing_accounts_exclusions_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListExclusionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = logging_billing_accounts_exclusions_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_billing_accounts_exclusions_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingBillingAccountsExclusionsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v2/billingAccounts/{billingAccountsId}/exclusions
/// Lists all the exclusions on the _Default sink in a parent resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_billing_accounts_exclusions_list_builder()` + `logging_billing_accounts_exclusions_list_execute()`.
/// For task-level control, use `logging_billing_accounts_exclusions_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_billing_accounts_exclusions_list(
    client: &SimpleHttpClient,
    args: &LoggingBillingAccountsExclusionsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListExclusionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = logging_billing_accounts_exclusions_list_builder(
        client,
        &args.parent,
        &args.pageSize,
        &args.pageToken,
    )?;
    logging_billing_accounts_exclusions_list_execute(builder)
}

/// PATCH v2/billingAccounts/{billingAccountsId}/exclusions/{exclusionsId}
/// Changes one or more properties of an existing exclusion in the _Default sink.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_billing_accounts_exclusions_patch_execute()` to send, or `logging_billing_accounts_exclusions_patch` for simplest API.

pub fn logging_billing_accounts_exclusions_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    updateMask: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/billingAccounts/{}/exclusions/{exclusionsId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v2/billingAccounts/{billingAccountsId}/exclusions/{exclusionsId}
/// Changes one or more properties of an existing exclusion in the _Default sink.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_billing_accounts_exclusions_patch_execute()` or `logging_billing_accounts_exclusions_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_billing_accounts_exclusions_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_billing_accounts_exclusions_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<LogExclusion>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LogExclusion = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v2/billingAccounts/{billingAccountsId}/exclusions/{exclusionsId}
/// Changes one or more properties of an existing exclusion in the _Default sink.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_billing_accounts_exclusions_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_billing_accounts_exclusions_patch_task()`.
/// For the simplest API, use `logging_billing_accounts_exclusions_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_billing_accounts_exclusions_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_billing_accounts_exclusions_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogExclusion>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = logging_billing_accounts_exclusions_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_billing_accounts_exclusions_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingBillingAccountsExclusionsPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
}

/// PATCH v2/billingAccounts/{billingAccountsId}/exclusions/{exclusionsId}
/// Changes one or more properties of an existing exclusion in the _Default sink.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_billing_accounts_exclusions_patch_builder()` + `logging_billing_accounts_exclusions_patch_execute()`.
/// For task-level control, use `logging_billing_accounts_exclusions_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_billing_accounts_exclusions_patch(
    client: &SimpleHttpClient,
    args: &LoggingBillingAccountsExclusionsPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogExclusion>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        logging_billing_accounts_exclusions_patch_builder(client, &args.name, &args.updateMask)?;
    logging_billing_accounts_exclusions_patch_execute(builder)
}

/// GET v2/billingAccounts/{billingAccountsId}/locations/{locationsId}
/// Gets information about a location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_billing_accounts_locations_get_execute()` to send, or `logging_billing_accounts_locations_get` for simplest API.

pub fn logging_billing_accounts_locations_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/billingAccounts/{}/locations/{locationsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/billingAccounts/{billingAccountsId}/locations/{locationsId}
/// Gets information about a location.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_billing_accounts_locations_get_execute()` or `logging_billing_accounts_locations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_billing_accounts_locations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_billing_accounts_locations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Location>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Location = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/billingAccounts/{billingAccountsId}/locations/{locationsId}
/// Gets information about a location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_billing_accounts_locations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_billing_accounts_locations_get_task()`.
/// For the simplest API, use `logging_billing_accounts_locations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_billing_accounts_locations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_billing_accounts_locations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Location>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = logging_billing_accounts_locations_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_billing_accounts_locations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingBillingAccountsLocationsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v2/billingAccounts/{billingAccountsId}/locations/{locationsId}
/// Gets information about a location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_billing_accounts_locations_get_builder()` + `logging_billing_accounts_locations_get_execute()`.
/// For task-level control, use `logging_billing_accounts_locations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_billing_accounts_locations_get(
    client: &SimpleHttpClient,
    args: &LoggingBillingAccountsLocationsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Location>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_billing_accounts_locations_get_builder(client, &args.name)?;
    logging_billing_accounts_locations_get_execute(builder)
}

/// GET v2/billingAccounts/{billingAccountsId}/locations
/// Lists information about the supported locations for this service.This method lists locations based on the resource scope provided in the ListLocationsRequest.name field: Global locations: If name is empty, the method lists the public locations available to all projects. Project-specific locations: If name follows the format `projects/{project}`, the method lists locations visible to that specific project. This includes public, private, or other project-specific locations enabled for the project.For `gRPC` and client library implementations, the resource name is passed as the name field. For direct service calls, the resource name is incorporated into the request path based on the specific service implementation and version.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_billing_accounts_locations_list_execute()` to send, or `logging_billing_accounts_locations_list` for simplest API.

pub fn logging_billing_accounts_locations_list_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    extraLocationTypes: &Option<String>,
    filter: &Option<String>,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/billingAccounts/{}/locations",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = extraLocationTypes.as_ref() {
        query_parts.push(format!("extraLocationTypes={}", val));
    }
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/billingAccounts/{billingAccountsId}/locations
/// Lists information about the supported locations for this service.This method lists locations based on the resource scope provided in the ListLocationsRequest.name field: Global locations: If name is empty, the method lists the public locations available to all projects. Project-specific locations: If name follows the format `projects/{project}`, the method lists locations visible to that specific project. This includes public, private, or other project-specific locations enabled for the project.For `gRPC` and client library implementations, the resource name is passed as the name field. For direct service calls, the resource name is incorporated into the request path based on the specific service implementation and version.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_billing_accounts_locations_list_execute()` or `logging_billing_accounts_locations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_billing_accounts_locations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_billing_accounts_locations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListLocationsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListLocationsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/billingAccounts/{billingAccountsId}/locations
/// Lists information about the supported locations for this service.This method lists locations based on the resource scope provided in the ListLocationsRequest.name field: Global locations: If name is empty, the method lists the public locations available to all projects. Project-specific locations: If name follows the format `projects/{project}`, the method lists locations visible to that specific project. This includes public, private, or other project-specific locations enabled for the project.For `gRPC` and client library implementations, the resource name is passed as the name field. For direct service calls, the resource name is incorporated into the request path based on the specific service implementation and version.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_billing_accounts_locations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_billing_accounts_locations_list_task()`.
/// For the simplest API, use `logging_billing_accounts_locations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_billing_accounts_locations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_billing_accounts_locations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListLocationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = logging_billing_accounts_locations_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_billing_accounts_locations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingBillingAccountsLocationsListArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: extraLocationTypes
    pub extraLocationTypes: Option<String>,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v2/billingAccounts/{billingAccountsId}/locations
/// Lists information about the supported locations for this service.This method lists locations based on the resource scope provided in the ListLocationsRequest.name field: Global locations: If name is empty, the method lists the public locations available to all projects. Project-specific locations: If name follows the format `projects/{project}`, the method lists locations visible to that specific project. This includes public, private, or other project-specific locations enabled for the project.For `gRPC` and client library implementations, the resource name is passed as the name field. For direct service calls, the resource name is incorporated into the request path based on the specific service implementation and version.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_billing_accounts_locations_list_builder()` + `logging_billing_accounts_locations_list_execute()`.
/// For task-level control, use `logging_billing_accounts_locations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_billing_accounts_locations_list(
    client: &SimpleHttpClient,
    args: &LoggingBillingAccountsLocationsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListLocationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = logging_billing_accounts_locations_list_builder(
        client,
        &args.name,
        &args.extraLocationTypes,
        &args.filter,
        &args.pageSize,
        &args.pageToken,
    )?;
    logging_billing_accounts_locations_list_execute(builder)
}

/// POST v2/billingAccounts/{billingAccountsId}/locations/{locationsId}/buckets
/// Creates a log bucket that can be used to store log entries. After a bucket has been created, the bucket''s location cannot be changed.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_billing_accounts_locations_buckets_create_execute()` to send, or `logging_billing_accounts_locations_buckets_create` for simplest API.

pub fn logging_billing_accounts_locations_buckets_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    bucketId: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/billingAccounts/{}/locations/{locationsId}/buckets",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = bucketId.as_ref() {
        query_parts.push(format!("bucketId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v2/billingAccounts/{billingAccountsId}/locations/{locationsId}/buckets
/// Creates a log bucket that can be used to store log entries. After a bucket has been created, the bucket''s location cannot be changed.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_billing_accounts_locations_buckets_create_execute()` or `logging_billing_accounts_locations_buckets_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_billing_accounts_locations_buckets_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_billing_accounts_locations_buckets_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<LogBucket>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LogBucket = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v2/billingAccounts/{billingAccountsId}/locations/{locationsId}/buckets
/// Creates a log bucket that can be used to store log entries. After a bucket has been created, the bucket''s location cannot be changed.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_billing_accounts_locations_buckets_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_billing_accounts_locations_buckets_create_task()`.
/// For the simplest API, use `logging_billing_accounts_locations_buckets_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_billing_accounts_locations_buckets_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_billing_accounts_locations_buckets_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogBucket>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = logging_billing_accounts_locations_buckets_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_billing_accounts_locations_buckets_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingBillingAccountsLocationsBucketsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: bucketId
    pub bucketId: Option<String>,
}

/// POST v2/billingAccounts/{billingAccountsId}/locations/{locationsId}/buckets
/// Creates a log bucket that can be used to store log entries. After a bucket has been created, the bucket''s location cannot be changed.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_billing_accounts_locations_buckets_create_builder()` + `logging_billing_accounts_locations_buckets_create_execute()`.
/// For task-level control, use `logging_billing_accounts_locations_buckets_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_billing_accounts_locations_buckets_create(
    client: &SimpleHttpClient,
    args: &LoggingBillingAccountsLocationsBucketsCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogBucket>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_billing_accounts_locations_buckets_create_builder(
        client,
        &args.parent,
        &args.bucketId,
    )?;
    logging_billing_accounts_locations_buckets_create_execute(builder)
}

/// POST v2/billingAccounts/{billingAccountsId}/locations/{locationsId}/buckets:createAsync
/// Creates a log bucket asynchronously that can be used to store log entries.After a bucket has been created, the bucket''s location cannot be changed.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_billing_accounts_locations_buckets_create_async_execute()` to send, or `logging_billing_accounts_locations_buckets_create_async` for simplest API.

pub fn logging_billing_accounts_locations_buckets_create_async_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    bucketId: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/billingAccounts/{}/locations/{locationsId}/buckets:createAsync",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = bucketId.as_ref() {
        query_parts.push(format!("bucketId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v2/billingAccounts/{billingAccountsId}/locations/{locationsId}/buckets:createAsync
/// Creates a log bucket asynchronously that can be used to store log entries.After a bucket has been created, the bucket''s location cannot be changed.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_billing_accounts_locations_buckets_create_async_execute()` or `logging_billing_accounts_locations_buckets_create_async`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_billing_accounts_locations_buckets_create_async_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_billing_accounts_locations_buckets_create_async_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v2/billingAccounts/{billingAccountsId}/locations/{locationsId}/buckets:createAsync
/// Creates a log bucket asynchronously that can be used to store log entries.After a bucket has been created, the bucket''s location cannot be changed.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_billing_accounts_locations_buckets_create_async_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_billing_accounts_locations_buckets_create_async_task()`.
/// For the simplest API, use `logging_billing_accounts_locations_buckets_create_async()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_billing_accounts_locations_buckets_create_async_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_billing_accounts_locations_buckets_create_async_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = logging_billing_accounts_locations_buckets_create_async_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_billing_accounts_locations_buckets_create_async`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingBillingAccountsLocationsBucketsCreateAsyncArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: bucketId
    pub bucketId: Option<String>,
}

/// POST v2/billingAccounts/{billingAccountsId}/locations/{locationsId}/buckets:createAsync
/// Creates a log bucket asynchronously that can be used to store log entries.After a bucket has been created, the bucket''s location cannot be changed.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_billing_accounts_locations_buckets_create_async_builder()` + `logging_billing_accounts_locations_buckets_create_async_execute()`.
/// For task-level control, use `logging_billing_accounts_locations_buckets_create_async_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_billing_accounts_locations_buckets_create_async(
    client: &SimpleHttpClient,
    args: &LoggingBillingAccountsLocationsBucketsCreateAsyncArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_billing_accounts_locations_buckets_create_async_builder(
        client,
        &args.parent,
        &args.bucketId,
    )?;
    logging_billing_accounts_locations_buckets_create_async_execute(builder)
}

/// DELETE v2/billingAccounts/{billingAccountsId}/locations/{locationsId}/buckets/{bucketsId}
/// Deletes a log bucket.Changes the bucket''s lifecycle_state to the DELETE_REQUESTED state. After 7 days, the bucket will be purged and all log entries in the bucket will be permanently deleted.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_billing_accounts_locations_buckets_delete_execute()` to send, or `logging_billing_accounts_locations_buckets_delete` for simplest API.

pub fn logging_billing_accounts_locations_buckets_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/billingAccounts/{}/locations/{locationsId}/buckets/{bucketsId}",
        name,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v2/billingAccounts/{billingAccountsId}/locations/{locationsId}/buckets/{bucketsId}
/// Deletes a log bucket.Changes the bucket''s lifecycle_state to the DELETE_REQUESTED state. After 7 days, the bucket will be purged and all log entries in the bucket will be permanently deleted.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_billing_accounts_locations_buckets_delete_execute()` or `logging_billing_accounts_locations_buckets_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_billing_accounts_locations_buckets_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_billing_accounts_locations_buckets_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v2/billingAccounts/{billingAccountsId}/locations/{locationsId}/buckets/{bucketsId}
/// Deletes a log bucket.Changes the bucket''s lifecycle_state to the DELETE_REQUESTED state. After 7 days, the bucket will be purged and all log entries in the bucket will be permanently deleted.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_billing_accounts_locations_buckets_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_billing_accounts_locations_buckets_delete_task()`.
/// For the simplest API, use `logging_billing_accounts_locations_buckets_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_billing_accounts_locations_buckets_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_billing_accounts_locations_buckets_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = logging_billing_accounts_locations_buckets_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_billing_accounts_locations_buckets_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingBillingAccountsLocationsBucketsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// DELETE v2/billingAccounts/{billingAccountsId}/locations/{locationsId}/buckets/{bucketsId}
/// Deletes a log bucket.Changes the bucket''s lifecycle_state to the DELETE_REQUESTED state. After 7 days, the bucket will be purged and all log entries in the bucket will be permanently deleted.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_billing_accounts_locations_buckets_delete_builder()` + `logging_billing_accounts_locations_buckets_delete_execute()`.
/// For task-level control, use `logging_billing_accounts_locations_buckets_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_billing_accounts_locations_buckets_delete(
    client: &SimpleHttpClient,
    args: &LoggingBillingAccountsLocationsBucketsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_billing_accounts_locations_buckets_delete_builder(client, &args.name)?;
    logging_billing_accounts_locations_buckets_delete_execute(builder)
}

/// GET v2/billingAccounts/{billingAccountsId}/locations/{locationsId}/buckets/{bucketsId}
/// Gets a log bucket.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_billing_accounts_locations_buckets_get_execute()` to send, or `logging_billing_accounts_locations_buckets_get` for simplest API.

pub fn logging_billing_accounts_locations_buckets_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/billingAccounts/{}/locations/{locationsId}/buckets/{bucketsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/billingAccounts/{billingAccountsId}/locations/{locationsId}/buckets/{bucketsId}
/// Gets a log bucket.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_billing_accounts_locations_buckets_get_execute()` or `logging_billing_accounts_locations_buckets_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_billing_accounts_locations_buckets_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_billing_accounts_locations_buckets_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<LogBucket>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LogBucket = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/billingAccounts/{billingAccountsId}/locations/{locationsId}/buckets/{bucketsId}
/// Gets a log bucket.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_billing_accounts_locations_buckets_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_billing_accounts_locations_buckets_get_task()`.
/// For the simplest API, use `logging_billing_accounts_locations_buckets_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_billing_accounts_locations_buckets_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_billing_accounts_locations_buckets_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogBucket>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = logging_billing_accounts_locations_buckets_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_billing_accounts_locations_buckets_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingBillingAccountsLocationsBucketsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v2/billingAccounts/{billingAccountsId}/locations/{locationsId}/buckets/{bucketsId}
/// Gets a log bucket.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_billing_accounts_locations_buckets_get_builder()` + `logging_billing_accounts_locations_buckets_get_execute()`.
/// For task-level control, use `logging_billing_accounts_locations_buckets_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_billing_accounts_locations_buckets_get(
    client: &SimpleHttpClient,
    args: &LoggingBillingAccountsLocationsBucketsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogBucket>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_billing_accounts_locations_buckets_get_builder(client, &args.name)?;
    logging_billing_accounts_locations_buckets_get_execute(builder)
}

/// GET v2/billingAccounts/{billingAccountsId}/locations/{locationsId}/buckets
/// Lists log buckets.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_billing_accounts_locations_buckets_list_execute()` to send, or `logging_billing_accounts_locations_buckets_list` for simplest API.

pub fn logging_billing_accounts_locations_buckets_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/billingAccounts/{}/locations/{locationsId}/buckets",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/billingAccounts/{billingAccountsId}/locations/{locationsId}/buckets
/// Lists log buckets.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_billing_accounts_locations_buckets_list_execute()` or `logging_billing_accounts_locations_buckets_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_billing_accounts_locations_buckets_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_billing_accounts_locations_buckets_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListBucketsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListBucketsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/billingAccounts/{billingAccountsId}/locations/{locationsId}/buckets
/// Lists log buckets.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_billing_accounts_locations_buckets_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_billing_accounts_locations_buckets_list_task()`.
/// For the simplest API, use `logging_billing_accounts_locations_buckets_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_billing_accounts_locations_buckets_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_billing_accounts_locations_buckets_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListBucketsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = logging_billing_accounts_locations_buckets_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_billing_accounts_locations_buckets_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingBillingAccountsLocationsBucketsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v2/billingAccounts/{billingAccountsId}/locations/{locationsId}/buckets
/// Lists log buckets.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_billing_accounts_locations_buckets_list_builder()` + `logging_billing_accounts_locations_buckets_list_execute()`.
/// For task-level control, use `logging_billing_accounts_locations_buckets_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_billing_accounts_locations_buckets_list(
    client: &SimpleHttpClient,
    args: &LoggingBillingAccountsLocationsBucketsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListBucketsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = logging_billing_accounts_locations_buckets_list_builder(
        client,
        &args.parent,
        &args.pageSize,
        &args.pageToken,
    )?;
    logging_billing_accounts_locations_buckets_list_execute(builder)
}

/// PATCH v2/billingAccounts/{billingAccountsId}/locations/{locationsId}/buckets/{bucketsId}
/// Updates a log bucket.If the bucket has a lifecycle_state of DELETE_REQUESTED, then FAILED_PRECONDITION will be returned.After a bucket has been created, the bucket''s location cannot be changed.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_billing_accounts_locations_buckets_patch_execute()` to send, or `logging_billing_accounts_locations_buckets_patch` for simplest API.

pub fn logging_billing_accounts_locations_buckets_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    updateMask: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/billingAccounts/{}/locations/{locationsId}/buckets/{bucketsId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v2/billingAccounts/{billingAccountsId}/locations/{locationsId}/buckets/{bucketsId}
/// Updates a log bucket.If the bucket has a lifecycle_state of DELETE_REQUESTED, then FAILED_PRECONDITION will be returned.After a bucket has been created, the bucket''s location cannot be changed.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_billing_accounts_locations_buckets_patch_execute()` or `logging_billing_accounts_locations_buckets_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_billing_accounts_locations_buckets_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_billing_accounts_locations_buckets_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<LogBucket>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LogBucket = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v2/billingAccounts/{billingAccountsId}/locations/{locationsId}/buckets/{bucketsId}
/// Updates a log bucket.If the bucket has a lifecycle_state of DELETE_REQUESTED, then FAILED_PRECONDITION will be returned.After a bucket has been created, the bucket''s location cannot be changed.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_billing_accounts_locations_buckets_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_billing_accounts_locations_buckets_patch_task()`.
/// For the simplest API, use `logging_billing_accounts_locations_buckets_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_billing_accounts_locations_buckets_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_billing_accounts_locations_buckets_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogBucket>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = logging_billing_accounts_locations_buckets_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_billing_accounts_locations_buckets_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingBillingAccountsLocationsBucketsPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
}

/// PATCH v2/billingAccounts/{billingAccountsId}/locations/{locationsId}/buckets/{bucketsId}
/// Updates a log bucket.If the bucket has a lifecycle_state of DELETE_REQUESTED, then FAILED_PRECONDITION will be returned.After a bucket has been created, the bucket''s location cannot be changed.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_billing_accounts_locations_buckets_patch_builder()` + `logging_billing_accounts_locations_buckets_patch_execute()`.
/// For task-level control, use `logging_billing_accounts_locations_buckets_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_billing_accounts_locations_buckets_patch(
    client: &SimpleHttpClient,
    args: &LoggingBillingAccountsLocationsBucketsPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogBucket>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_billing_accounts_locations_buckets_patch_builder(
        client,
        &args.name,
        &args.updateMask,
    )?;
    logging_billing_accounts_locations_buckets_patch_execute(builder)
}

/// POST v2/billingAccounts/{billingAccountsId}/locations/{locationsId}/buckets/{bucketsId}:undelete
/// Undeletes a log bucket. A bucket that has been deleted can be undeleted within the grace period of 7 days.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_billing_accounts_locations_buckets_undelete_execute()` to send, or `logging_billing_accounts_locations_buckets_undelete` for simplest API.

pub fn logging_billing_accounts_locations_buckets_undelete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/billingAccounts/{}/locations/{locationsId}/buckets/{bucketsId}:undelete",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v2/billingAccounts/{billingAccountsId}/locations/{locationsId}/buckets/{bucketsId}:undelete
/// Undeletes a log bucket. A bucket that has been deleted can be undeleted within the grace period of 7 days.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_billing_accounts_locations_buckets_undelete_execute()` or `logging_billing_accounts_locations_buckets_undelete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_billing_accounts_locations_buckets_undelete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_billing_accounts_locations_buckets_undelete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v2/billingAccounts/{billingAccountsId}/locations/{locationsId}/buckets/{bucketsId}:undelete
/// Undeletes a log bucket. A bucket that has been deleted can be undeleted within the grace period of 7 days.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_billing_accounts_locations_buckets_undelete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_billing_accounts_locations_buckets_undelete_task()`.
/// For the simplest API, use `logging_billing_accounts_locations_buckets_undelete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_billing_accounts_locations_buckets_undelete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_billing_accounts_locations_buckets_undelete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = logging_billing_accounts_locations_buckets_undelete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_billing_accounts_locations_buckets_undelete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingBillingAccountsLocationsBucketsUndeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v2/billingAccounts/{billingAccountsId}/locations/{locationsId}/buckets/{bucketsId}:undelete
/// Undeletes a log bucket. A bucket that has been deleted can be undeleted within the grace period of 7 days.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_billing_accounts_locations_buckets_undelete_builder()` + `logging_billing_accounts_locations_buckets_undelete_execute()`.
/// For task-level control, use `logging_billing_accounts_locations_buckets_undelete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_billing_accounts_locations_buckets_undelete(
    client: &SimpleHttpClient,
    args: &LoggingBillingAccountsLocationsBucketsUndeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_billing_accounts_locations_buckets_undelete_builder(client, &args.name)?;
    logging_billing_accounts_locations_buckets_undelete_execute(builder)
}

/// POST v2/billingAccounts/{billingAccountsId}/locations/{locationsId}/buckets/{bucketsId}:updateAsync
/// Updates a log bucket asynchronously.If the bucket has a lifecycle_state of DELETE_REQUESTED, then FAILED_PRECONDITION will be returned.After a bucket has been created, the bucket''s location cannot be changed.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_billing_accounts_locations_buckets_update_async_execute()` to send, or `logging_billing_accounts_locations_buckets_update_async` for simplest API.

pub fn logging_billing_accounts_locations_buckets_update_async_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    updateMask: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/billingAccounts/{}/locations/{locationsId}/buckets/{bucketsId}:updateAsync",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v2/billingAccounts/{billingAccountsId}/locations/{locationsId}/buckets/{bucketsId}:updateAsync
/// Updates a log bucket asynchronously.If the bucket has a lifecycle_state of DELETE_REQUESTED, then FAILED_PRECONDITION will be returned.After a bucket has been created, the bucket''s location cannot be changed.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_billing_accounts_locations_buckets_update_async_execute()` or `logging_billing_accounts_locations_buckets_update_async`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_billing_accounts_locations_buckets_update_async_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_billing_accounts_locations_buckets_update_async_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v2/billingAccounts/{billingAccountsId}/locations/{locationsId}/buckets/{bucketsId}:updateAsync
/// Updates a log bucket asynchronously.If the bucket has a lifecycle_state of DELETE_REQUESTED, then FAILED_PRECONDITION will be returned.After a bucket has been created, the bucket''s location cannot be changed.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_billing_accounts_locations_buckets_update_async_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_billing_accounts_locations_buckets_update_async_task()`.
/// For the simplest API, use `logging_billing_accounts_locations_buckets_update_async()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_billing_accounts_locations_buckets_update_async_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_billing_accounts_locations_buckets_update_async_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = logging_billing_accounts_locations_buckets_update_async_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_billing_accounts_locations_buckets_update_async`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingBillingAccountsLocationsBucketsUpdateAsyncArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
}

/// POST v2/billingAccounts/{billingAccountsId}/locations/{locationsId}/buckets/{bucketsId}:updateAsync
/// Updates a log bucket asynchronously.If the bucket has a lifecycle_state of DELETE_REQUESTED, then FAILED_PRECONDITION will be returned.After a bucket has been created, the bucket''s location cannot be changed.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_billing_accounts_locations_buckets_update_async_builder()` + `logging_billing_accounts_locations_buckets_update_async_execute()`.
/// For task-level control, use `logging_billing_accounts_locations_buckets_update_async_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_billing_accounts_locations_buckets_update_async(
    client: &SimpleHttpClient,
    args: &LoggingBillingAccountsLocationsBucketsUpdateAsyncArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_billing_accounts_locations_buckets_update_async_builder(
        client,
        &args.name,
        &args.updateMask,
    )?;
    logging_billing_accounts_locations_buckets_update_async_execute(builder)
}

/// POST v2/billingAccounts/{billingAccountsId}/locations/{locationsId}/buckets/{bucketsId}/links
/// Asynchronously creates a linked dataset in BigQuery which makes it possible to use BigQuery to read the logs stored in the log bucket. A log bucket may currently only contain one link.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_billing_accounts_locations_buckets_links_create_execute()` to send, or `logging_billing_accounts_locations_buckets_links_create` for simplest API.

pub fn logging_billing_accounts_locations_buckets_links_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    linkId: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/billingAccounts/{}/locations/{locationsId}/buckets/{bucketsId}/links",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = linkId.as_ref() {
        query_parts.push(format!("linkId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v2/billingAccounts/{billingAccountsId}/locations/{locationsId}/buckets/{bucketsId}/links
/// Asynchronously creates a linked dataset in BigQuery which makes it possible to use BigQuery to read the logs stored in the log bucket. A log bucket may currently only contain one link.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_billing_accounts_locations_buckets_links_create_execute()` or `logging_billing_accounts_locations_buckets_links_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_billing_accounts_locations_buckets_links_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_billing_accounts_locations_buckets_links_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v2/billingAccounts/{billingAccountsId}/locations/{locationsId}/buckets/{bucketsId}/links
/// Asynchronously creates a linked dataset in BigQuery which makes it possible to use BigQuery to read the logs stored in the log bucket. A log bucket may currently only contain one link.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_billing_accounts_locations_buckets_links_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_billing_accounts_locations_buckets_links_create_task()`.
/// For the simplest API, use `logging_billing_accounts_locations_buckets_links_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_billing_accounts_locations_buckets_links_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_billing_accounts_locations_buckets_links_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = logging_billing_accounts_locations_buckets_links_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_billing_accounts_locations_buckets_links_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingBillingAccountsLocationsBucketsLinksCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: linkId
    pub linkId: Option<String>,
}

/// POST v2/billingAccounts/{billingAccountsId}/locations/{locationsId}/buckets/{bucketsId}/links
/// Asynchronously creates a linked dataset in BigQuery which makes it possible to use BigQuery to read the logs stored in the log bucket. A log bucket may currently only contain one link.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_billing_accounts_locations_buckets_links_create_builder()` + `logging_billing_accounts_locations_buckets_links_create_execute()`.
/// For task-level control, use `logging_billing_accounts_locations_buckets_links_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_billing_accounts_locations_buckets_links_create(
    client: &SimpleHttpClient,
    args: &LoggingBillingAccountsLocationsBucketsLinksCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_billing_accounts_locations_buckets_links_create_builder(
        client,
        &args.parent,
        &args.linkId,
    )?;
    logging_billing_accounts_locations_buckets_links_create_execute(builder)
}

/// DELETE v2/billingAccounts/{billingAccountsId}/locations/{locationsId}/buckets/{bucketsId}/links/{linksId}
/// Deletes a link. This will also delete the corresponding BigQuery linked dataset.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_billing_accounts_locations_buckets_links_delete_execute()` to send, or `logging_billing_accounts_locations_buckets_links_delete` for simplest API.

pub fn logging_billing_accounts_locations_buckets_links_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/billingAccounts/{}/locations/{locationsId}/buckets/{bucketsId}/links/{linksId}",
        name,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v2/billingAccounts/{billingAccountsId}/locations/{locationsId}/buckets/{bucketsId}/links/{linksId}
/// Deletes a link. This will also delete the corresponding BigQuery linked dataset.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_billing_accounts_locations_buckets_links_delete_execute()` or `logging_billing_accounts_locations_buckets_links_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_billing_accounts_locations_buckets_links_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_billing_accounts_locations_buckets_links_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v2/billingAccounts/{billingAccountsId}/locations/{locationsId}/buckets/{bucketsId}/links/{linksId}
/// Deletes a link. This will also delete the corresponding BigQuery linked dataset.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_billing_accounts_locations_buckets_links_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_billing_accounts_locations_buckets_links_delete_task()`.
/// For the simplest API, use `logging_billing_accounts_locations_buckets_links_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_billing_accounts_locations_buckets_links_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_billing_accounts_locations_buckets_links_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = logging_billing_accounts_locations_buckets_links_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_billing_accounts_locations_buckets_links_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingBillingAccountsLocationsBucketsLinksDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// DELETE v2/billingAccounts/{billingAccountsId}/locations/{locationsId}/buckets/{bucketsId}/links/{linksId}
/// Deletes a link. This will also delete the corresponding BigQuery linked dataset.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_billing_accounts_locations_buckets_links_delete_builder()` + `logging_billing_accounts_locations_buckets_links_delete_execute()`.
/// For task-level control, use `logging_billing_accounts_locations_buckets_links_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_billing_accounts_locations_buckets_links_delete(
    client: &SimpleHttpClient,
    args: &LoggingBillingAccountsLocationsBucketsLinksDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        logging_billing_accounts_locations_buckets_links_delete_builder(client, &args.name)?;
    logging_billing_accounts_locations_buckets_links_delete_execute(builder)
}

/// GET v2/billingAccounts/{billingAccountsId}/locations/{locationsId}/buckets/{bucketsId}/links/{linksId}
/// Gets a link.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_billing_accounts_locations_buckets_links_get_execute()` to send, or `logging_billing_accounts_locations_buckets_links_get` for simplest API.

pub fn logging_billing_accounts_locations_buckets_links_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/billingAccounts/{}/locations/{locationsId}/buckets/{bucketsId}/links/{linksId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/billingAccounts/{billingAccountsId}/locations/{locationsId}/buckets/{bucketsId}/links/{linksId}
/// Gets a link.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_billing_accounts_locations_buckets_links_get_execute()` or `logging_billing_accounts_locations_buckets_links_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_billing_accounts_locations_buckets_links_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_billing_accounts_locations_buckets_links_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Link>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Link = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/billingAccounts/{billingAccountsId}/locations/{locationsId}/buckets/{bucketsId}/links/{linksId}
/// Gets a link.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_billing_accounts_locations_buckets_links_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_billing_accounts_locations_buckets_links_get_task()`.
/// For the simplest API, use `logging_billing_accounts_locations_buckets_links_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_billing_accounts_locations_buckets_links_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_billing_accounts_locations_buckets_links_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Link>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = logging_billing_accounts_locations_buckets_links_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_billing_accounts_locations_buckets_links_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingBillingAccountsLocationsBucketsLinksGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v2/billingAccounts/{billingAccountsId}/locations/{locationsId}/buckets/{bucketsId}/links/{linksId}
/// Gets a link.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_billing_accounts_locations_buckets_links_get_builder()` + `logging_billing_accounts_locations_buckets_links_get_execute()`.
/// For task-level control, use `logging_billing_accounts_locations_buckets_links_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_billing_accounts_locations_buckets_links_get(
    client: &SimpleHttpClient,
    args: &LoggingBillingAccountsLocationsBucketsLinksGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Link>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_billing_accounts_locations_buckets_links_get_builder(client, &args.name)?;
    logging_billing_accounts_locations_buckets_links_get_execute(builder)
}

/// GET v2/billingAccounts/{billingAccountsId}/locations/{locationsId}/buckets/{bucketsId}/links
/// Lists links.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_billing_accounts_locations_buckets_links_list_execute()` to send, or `logging_billing_accounts_locations_buckets_links_list` for simplest API.

pub fn logging_billing_accounts_locations_buckets_links_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/billingAccounts/{}/locations/{locationsId}/buckets/{bucketsId}/links",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/billingAccounts/{billingAccountsId}/locations/{locationsId}/buckets/{bucketsId}/links
/// Lists links.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_billing_accounts_locations_buckets_links_list_execute()` or `logging_billing_accounts_locations_buckets_links_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_billing_accounts_locations_buckets_links_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_billing_accounts_locations_buckets_links_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListLinksResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListLinksResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/billingAccounts/{billingAccountsId}/locations/{locationsId}/buckets/{bucketsId}/links
/// Lists links.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_billing_accounts_locations_buckets_links_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_billing_accounts_locations_buckets_links_list_task()`.
/// For the simplest API, use `logging_billing_accounts_locations_buckets_links_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_billing_accounts_locations_buckets_links_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_billing_accounts_locations_buckets_links_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListLinksResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = logging_billing_accounts_locations_buckets_links_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_billing_accounts_locations_buckets_links_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingBillingAccountsLocationsBucketsLinksListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v2/billingAccounts/{billingAccountsId}/locations/{locationsId}/buckets/{bucketsId}/links
/// Lists links.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_billing_accounts_locations_buckets_links_list_builder()` + `logging_billing_accounts_locations_buckets_links_list_execute()`.
/// For task-level control, use `logging_billing_accounts_locations_buckets_links_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_billing_accounts_locations_buckets_links_list(
    client: &SimpleHttpClient,
    args: &LoggingBillingAccountsLocationsBucketsLinksListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListLinksResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = logging_billing_accounts_locations_buckets_links_list_builder(
        client,
        &args.parent,
        &args.pageSize,
        &args.pageToken,
    )?;
    logging_billing_accounts_locations_buckets_links_list_execute(builder)
}

/// POST v2/billingAccounts/{billingAccountsId}/locations/{locationsId}/buckets/{bucketsId}/views
/// Creates a view over log entries in a log bucket. A bucket may contain a maximum of 30 views.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_billing_accounts_locations_buckets_views_create_execute()` to send, or `logging_billing_accounts_locations_buckets_views_create` for simplest API.

pub fn logging_billing_accounts_locations_buckets_views_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    viewId: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/billingAccounts/{}/locations/{locationsId}/buckets/{bucketsId}/views",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = viewId.as_ref() {
        query_parts.push(format!("viewId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v2/billingAccounts/{billingAccountsId}/locations/{locationsId}/buckets/{bucketsId}/views
/// Creates a view over log entries in a log bucket. A bucket may contain a maximum of 30 views.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_billing_accounts_locations_buckets_views_create_execute()` or `logging_billing_accounts_locations_buckets_views_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_billing_accounts_locations_buckets_views_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_billing_accounts_locations_buckets_views_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<LogView>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LogView = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v2/billingAccounts/{billingAccountsId}/locations/{locationsId}/buckets/{bucketsId}/views
/// Creates a view over log entries in a log bucket. A bucket may contain a maximum of 30 views.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_billing_accounts_locations_buckets_views_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_billing_accounts_locations_buckets_views_create_task()`.
/// For the simplest API, use `logging_billing_accounts_locations_buckets_views_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_billing_accounts_locations_buckets_views_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_billing_accounts_locations_buckets_views_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogView>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = logging_billing_accounts_locations_buckets_views_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_billing_accounts_locations_buckets_views_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingBillingAccountsLocationsBucketsViewsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: viewId
    pub viewId: Option<String>,
}

/// POST v2/billingAccounts/{billingAccountsId}/locations/{locationsId}/buckets/{bucketsId}/views
/// Creates a view over log entries in a log bucket. A bucket may contain a maximum of 30 views.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_billing_accounts_locations_buckets_views_create_builder()` + `logging_billing_accounts_locations_buckets_views_create_execute()`.
/// For task-level control, use `logging_billing_accounts_locations_buckets_views_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_billing_accounts_locations_buckets_views_create(
    client: &SimpleHttpClient,
    args: &LoggingBillingAccountsLocationsBucketsViewsCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogView>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_billing_accounts_locations_buckets_views_create_builder(
        client,
        &args.parent,
        &args.viewId,
    )?;
    logging_billing_accounts_locations_buckets_views_create_execute(builder)
}

/// DELETE v2/billingAccounts/{billingAccountsId}/locations/{locationsId}/buckets/{bucketsId}/views/{viewsId}
/// Deletes a view on a log bucket. If an UNAVAILABLE error is returned, this indicates that system is not in a state where it can delete the view. If this occurs, please try again in a few minutes.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_billing_accounts_locations_buckets_views_delete_execute()` to send, or `logging_billing_accounts_locations_buckets_views_delete` for simplest API.

pub fn logging_billing_accounts_locations_buckets_views_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/billingAccounts/{}/locations/{locationsId}/buckets/{bucketsId}/views/{viewsId}",
        name,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v2/billingAccounts/{billingAccountsId}/locations/{locationsId}/buckets/{bucketsId}/views/{viewsId}
/// Deletes a view on a log bucket. If an UNAVAILABLE error is returned, this indicates that system is not in a state where it can delete the view. If this occurs, please try again in a few minutes.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_billing_accounts_locations_buckets_views_delete_execute()` or `logging_billing_accounts_locations_buckets_views_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_billing_accounts_locations_buckets_views_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_billing_accounts_locations_buckets_views_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v2/billingAccounts/{billingAccountsId}/locations/{locationsId}/buckets/{bucketsId}/views/{viewsId}
/// Deletes a view on a log bucket. If an UNAVAILABLE error is returned, this indicates that system is not in a state where it can delete the view. If this occurs, please try again in a few minutes.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_billing_accounts_locations_buckets_views_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_billing_accounts_locations_buckets_views_delete_task()`.
/// For the simplest API, use `logging_billing_accounts_locations_buckets_views_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_billing_accounts_locations_buckets_views_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_billing_accounts_locations_buckets_views_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = logging_billing_accounts_locations_buckets_views_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_billing_accounts_locations_buckets_views_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingBillingAccountsLocationsBucketsViewsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// DELETE v2/billingAccounts/{billingAccountsId}/locations/{locationsId}/buckets/{bucketsId}/views/{viewsId}
/// Deletes a view on a log bucket. If an UNAVAILABLE error is returned, this indicates that system is not in a state where it can delete the view. If this occurs, please try again in a few minutes.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_billing_accounts_locations_buckets_views_delete_builder()` + `logging_billing_accounts_locations_buckets_views_delete_execute()`.
/// For task-level control, use `logging_billing_accounts_locations_buckets_views_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_billing_accounts_locations_buckets_views_delete(
    client: &SimpleHttpClient,
    args: &LoggingBillingAccountsLocationsBucketsViewsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        logging_billing_accounts_locations_buckets_views_delete_builder(client, &args.name)?;
    logging_billing_accounts_locations_buckets_views_delete_execute(builder)
}

/// GET v2/billingAccounts/{billingAccountsId}/locations/{locationsId}/buckets/{bucketsId}/views/{viewsId}
/// Gets a view on a log bucket.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_billing_accounts_locations_buckets_views_get_execute()` to send, or `logging_billing_accounts_locations_buckets_views_get` for simplest API.

pub fn logging_billing_accounts_locations_buckets_views_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/billingAccounts/{}/locations/{locationsId}/buckets/{bucketsId}/views/{viewsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/billingAccounts/{billingAccountsId}/locations/{locationsId}/buckets/{bucketsId}/views/{viewsId}
/// Gets a view on a log bucket.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_billing_accounts_locations_buckets_views_get_execute()` or `logging_billing_accounts_locations_buckets_views_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_billing_accounts_locations_buckets_views_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_billing_accounts_locations_buckets_views_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<LogView>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LogView = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/billingAccounts/{billingAccountsId}/locations/{locationsId}/buckets/{bucketsId}/views/{viewsId}
/// Gets a view on a log bucket.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_billing_accounts_locations_buckets_views_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_billing_accounts_locations_buckets_views_get_task()`.
/// For the simplest API, use `logging_billing_accounts_locations_buckets_views_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_billing_accounts_locations_buckets_views_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_billing_accounts_locations_buckets_views_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogView>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = logging_billing_accounts_locations_buckets_views_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_billing_accounts_locations_buckets_views_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingBillingAccountsLocationsBucketsViewsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v2/billingAccounts/{billingAccountsId}/locations/{locationsId}/buckets/{bucketsId}/views/{viewsId}
/// Gets a view on a log bucket.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_billing_accounts_locations_buckets_views_get_builder()` + `logging_billing_accounts_locations_buckets_views_get_execute()`.
/// For task-level control, use `logging_billing_accounts_locations_buckets_views_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_billing_accounts_locations_buckets_views_get(
    client: &SimpleHttpClient,
    args: &LoggingBillingAccountsLocationsBucketsViewsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogView>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_billing_accounts_locations_buckets_views_get_builder(client, &args.name)?;
    logging_billing_accounts_locations_buckets_views_get_execute(builder)
}

/// GET v2/billingAccounts/{billingAccountsId}/locations/{locationsId}/buckets/{bucketsId}/views
/// Lists views on a log bucket.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_billing_accounts_locations_buckets_views_list_execute()` to send, or `logging_billing_accounts_locations_buckets_views_list` for simplest API.

pub fn logging_billing_accounts_locations_buckets_views_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/billingAccounts/{}/locations/{locationsId}/buckets/{bucketsId}/views",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/billingAccounts/{billingAccountsId}/locations/{locationsId}/buckets/{bucketsId}/views
/// Lists views on a log bucket.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_billing_accounts_locations_buckets_views_list_execute()` or `logging_billing_accounts_locations_buckets_views_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_billing_accounts_locations_buckets_views_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_billing_accounts_locations_buckets_views_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListViewsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListViewsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/billingAccounts/{billingAccountsId}/locations/{locationsId}/buckets/{bucketsId}/views
/// Lists views on a log bucket.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_billing_accounts_locations_buckets_views_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_billing_accounts_locations_buckets_views_list_task()`.
/// For the simplest API, use `logging_billing_accounts_locations_buckets_views_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_billing_accounts_locations_buckets_views_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_billing_accounts_locations_buckets_views_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListViewsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = logging_billing_accounts_locations_buckets_views_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_billing_accounts_locations_buckets_views_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingBillingAccountsLocationsBucketsViewsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v2/billingAccounts/{billingAccountsId}/locations/{locationsId}/buckets/{bucketsId}/views
/// Lists views on a log bucket.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_billing_accounts_locations_buckets_views_list_builder()` + `logging_billing_accounts_locations_buckets_views_list_execute()`.
/// For task-level control, use `logging_billing_accounts_locations_buckets_views_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_billing_accounts_locations_buckets_views_list(
    client: &SimpleHttpClient,
    args: &LoggingBillingAccountsLocationsBucketsViewsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListViewsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = logging_billing_accounts_locations_buckets_views_list_builder(
        client,
        &args.parent,
        &args.pageSize,
        &args.pageToken,
    )?;
    logging_billing_accounts_locations_buckets_views_list_execute(builder)
}

/// PATCH v2/billingAccounts/{billingAccountsId}/locations/{locationsId}/buckets/{bucketsId}/views/{viewsId}
/// Updates a view on a log bucket. This method replaces the value of the filter field from the existing view with the corresponding value from the new view. If an UNAVAILABLE error is returned, this indicates that system is not in a state where it can update the view. If this occurs, please try again in a few minutes.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_billing_accounts_locations_buckets_views_patch_execute()` to send, or `logging_billing_accounts_locations_buckets_views_patch` for simplest API.

pub fn logging_billing_accounts_locations_buckets_views_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    updateMask: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/billingAccounts/{}/locations/{locationsId}/buckets/{bucketsId}/views/{viewsId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v2/billingAccounts/{billingAccountsId}/locations/{locationsId}/buckets/{bucketsId}/views/{viewsId}
/// Updates a view on a log bucket. This method replaces the value of the filter field from the existing view with the corresponding value from the new view. If an UNAVAILABLE error is returned, this indicates that system is not in a state where it can update the view. If this occurs, please try again in a few minutes.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_billing_accounts_locations_buckets_views_patch_execute()` or `logging_billing_accounts_locations_buckets_views_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_billing_accounts_locations_buckets_views_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_billing_accounts_locations_buckets_views_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<LogView>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LogView = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v2/billingAccounts/{billingAccountsId}/locations/{locationsId}/buckets/{bucketsId}/views/{viewsId}
/// Updates a view on a log bucket. This method replaces the value of the filter field from the existing view with the corresponding value from the new view. If an UNAVAILABLE error is returned, this indicates that system is not in a state where it can update the view. If this occurs, please try again in a few minutes.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_billing_accounts_locations_buckets_views_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_billing_accounts_locations_buckets_views_patch_task()`.
/// For the simplest API, use `logging_billing_accounts_locations_buckets_views_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_billing_accounts_locations_buckets_views_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_billing_accounts_locations_buckets_views_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogView>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = logging_billing_accounts_locations_buckets_views_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_billing_accounts_locations_buckets_views_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingBillingAccountsLocationsBucketsViewsPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
}

/// PATCH v2/billingAccounts/{billingAccountsId}/locations/{locationsId}/buckets/{bucketsId}/views/{viewsId}
/// Updates a view on a log bucket. This method replaces the value of the filter field from the existing view with the corresponding value from the new view. If an UNAVAILABLE error is returned, this indicates that system is not in a state where it can update the view. If this occurs, please try again in a few minutes.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_billing_accounts_locations_buckets_views_patch_builder()` + `logging_billing_accounts_locations_buckets_views_patch_execute()`.
/// For task-level control, use `logging_billing_accounts_locations_buckets_views_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_billing_accounts_locations_buckets_views_patch(
    client: &SimpleHttpClient,
    args: &LoggingBillingAccountsLocationsBucketsViewsPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogView>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_billing_accounts_locations_buckets_views_patch_builder(
        client,
        &args.name,
        &args.updateMask,
    )?;
    logging_billing_accounts_locations_buckets_views_patch_execute(builder)
}

/// GET v2/billingAccounts/{billingAccountsId}/locations/{locationsId}/buckets/{bucketsId}/views/{viewsId}/logs
/// Lists the logs in projects, organizations, folders, or billing accounts. Only logs that have entries are listed.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_billing_accounts_locations_buckets_views_logs_list_execute()` to send, or `logging_billing_accounts_locations_buckets_views_logs_list` for simplest API.

pub fn logging_billing_accounts_locations_buckets_views_logs_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
    resourceNames: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/billingAccounts/{}/locations/{locationsId}/buckets/{bucketsId}/views/{viewsId}/logs",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = resourceNames.as_ref() {
        query_parts.push(format!("resourceNames={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/billingAccounts/{billingAccountsId}/locations/{locationsId}/buckets/{bucketsId}/views/{viewsId}/logs
/// Lists the logs in projects, organizations, folders, or billing accounts. Only logs that have entries are listed.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_billing_accounts_locations_buckets_views_logs_list_execute()` or `logging_billing_accounts_locations_buckets_views_logs_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_billing_accounts_locations_buckets_views_logs_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_billing_accounts_locations_buckets_views_logs_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListLogsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListLogsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/billingAccounts/{billingAccountsId}/locations/{locationsId}/buckets/{bucketsId}/views/{viewsId}/logs
/// Lists the logs in projects, organizations, folders, or billing accounts. Only logs that have entries are listed.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_billing_accounts_locations_buckets_views_logs_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_billing_accounts_locations_buckets_views_logs_list_task()`.
/// For the simplest API, use `logging_billing_accounts_locations_buckets_views_logs_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_billing_accounts_locations_buckets_views_logs_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_billing_accounts_locations_buckets_views_logs_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListLogsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = logging_billing_accounts_locations_buckets_views_logs_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_billing_accounts_locations_buckets_views_logs_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingBillingAccountsLocationsBucketsViewsLogsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: resourceNames
    pub resourceNames: Option<String>,
}

/// GET v2/billingAccounts/{billingAccountsId}/locations/{locationsId}/buckets/{bucketsId}/views/{viewsId}/logs
/// Lists the logs in projects, organizations, folders, or billing accounts. Only logs that have entries are listed.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_billing_accounts_locations_buckets_views_logs_list_builder()` + `logging_billing_accounts_locations_buckets_views_logs_list_execute()`.
/// For task-level control, use `logging_billing_accounts_locations_buckets_views_logs_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_billing_accounts_locations_buckets_views_logs_list(
    client: &SimpleHttpClient,
    args: &LoggingBillingAccountsLocationsBucketsViewsLogsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListLogsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = logging_billing_accounts_locations_buckets_views_logs_list_builder(
        client,
        &args.parent,
        &args.pageSize,
        &args.pageToken,
        &args.resourceNames,
    )?;
    logging_billing_accounts_locations_buckets_views_logs_list_execute(builder)
}

/// POST v2/billingAccounts/{billingAccountsId}/locations/{locationsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn''t support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_billing_accounts_locations_operations_cancel_execute()` to send, or `logging_billing_accounts_locations_operations_cancel` for simplest API.

pub fn logging_billing_accounts_locations_operations_cancel_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/billingAccounts/{}/locations/{locationsId}/operations/{operationsId}:cancel",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v2/billingAccounts/{billingAccountsId}/locations/{locationsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn''t support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_billing_accounts_locations_operations_cancel_execute()` or `logging_billing_accounts_locations_operations_cancel`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_billing_accounts_locations_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_billing_accounts_locations_operations_cancel_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v2/billingAccounts/{billingAccountsId}/locations/{locationsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn''t support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_billing_accounts_locations_operations_cancel_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_billing_accounts_locations_operations_cancel_task()`.
/// For the simplest API, use `logging_billing_accounts_locations_operations_cancel()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_billing_accounts_locations_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_billing_accounts_locations_operations_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = logging_billing_accounts_locations_operations_cancel_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_billing_accounts_locations_operations_cancel`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingBillingAccountsLocationsOperationsCancelArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v2/billingAccounts/{billingAccountsId}/locations/{locationsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn''t support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_billing_accounts_locations_operations_cancel_builder()` + `logging_billing_accounts_locations_operations_cancel_execute()`.
/// For task-level control, use `logging_billing_accounts_locations_operations_cancel_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_billing_accounts_locations_operations_cancel(
    client: &SimpleHttpClient,
    args: &LoggingBillingAccountsLocationsOperationsCancelArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_billing_accounts_locations_operations_cancel_builder(client, &args.name)?;
    logging_billing_accounts_locations_operations_cancel_execute(builder)
}

/// GET v2/billingAccounts/{billingAccountsId}/locations/{locationsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_billing_accounts_locations_operations_get_execute()` to send, or `logging_billing_accounts_locations_operations_get` for simplest API.

pub fn logging_billing_accounts_locations_operations_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/billingAccounts/{}/locations/{locationsId}/operations/{operationsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/billingAccounts/{billingAccountsId}/locations/{locationsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_billing_accounts_locations_operations_get_execute()` or `logging_billing_accounts_locations_operations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_billing_accounts_locations_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_billing_accounts_locations_operations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/billingAccounts/{billingAccountsId}/locations/{locationsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_billing_accounts_locations_operations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_billing_accounts_locations_operations_get_task()`.
/// For the simplest API, use `logging_billing_accounts_locations_operations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_billing_accounts_locations_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_billing_accounts_locations_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = logging_billing_accounts_locations_operations_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_billing_accounts_locations_operations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingBillingAccountsLocationsOperationsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v2/billingAccounts/{billingAccountsId}/locations/{locationsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_billing_accounts_locations_operations_get_builder()` + `logging_billing_accounts_locations_operations_get_execute()`.
/// For task-level control, use `logging_billing_accounts_locations_operations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_billing_accounts_locations_operations_get(
    client: &SimpleHttpClient,
    args: &LoggingBillingAccountsLocationsOperationsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_billing_accounts_locations_operations_get_builder(client, &args.name)?;
    logging_billing_accounts_locations_operations_get_execute(builder)
}

/// GET v2/billingAccounts/{billingAccountsId}/locations/{locationsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn''t support this method, it returns UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_billing_accounts_locations_operations_list_execute()` to send, or `logging_billing_accounts_locations_operations_list` for simplest API.

pub fn logging_billing_accounts_locations_operations_list_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    filter: &Option<String>,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
    returnPartialSuccess: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/billingAccounts/{}/locations/{locationsId}/operations",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/billingAccounts/{billingAccountsId}/locations/{locationsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn''t support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_billing_accounts_locations_operations_list_execute()` or `logging_billing_accounts_locations_operations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_billing_accounts_locations_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_billing_accounts_locations_operations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListOperationsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListOperationsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/billingAccounts/{billingAccountsId}/locations/{locationsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn''t support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_billing_accounts_locations_operations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_billing_accounts_locations_operations_list_task()`.
/// For the simplest API, use `logging_billing_accounts_locations_operations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_billing_accounts_locations_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_billing_accounts_locations_operations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListOperationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = logging_billing_accounts_locations_operations_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_billing_accounts_locations_operations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingBillingAccountsLocationsOperationsListArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<String>,
}

/// GET v2/billingAccounts/{billingAccountsId}/locations/{locationsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn''t support this method, it returns UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_billing_accounts_locations_operations_list_builder()` + `logging_billing_accounts_locations_operations_list_execute()`.
/// For task-level control, use `logging_billing_accounts_locations_operations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_billing_accounts_locations_operations_list(
    client: &SimpleHttpClient,
    args: &LoggingBillingAccountsLocationsOperationsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListOperationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = logging_billing_accounts_locations_operations_list_builder(
        client,
        &args.name,
        &args.filter,
        &args.pageSize,
        &args.pageToken,
        &args.returnPartialSuccess,
    )?;
    logging_billing_accounts_locations_operations_list_execute(builder)
}

/// GET v2/billingAccounts/{billingAccountsId}/locations/{locationsId}/recentQueries
/// Lists the RecentQueries that were created by the user making the request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_billing_accounts_locations_recent_queries_list_execute()` to send, or `logging_billing_accounts_locations_recent_queries_list` for simplest API.

pub fn logging_billing_accounts_locations_recent_queries_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    filter: &Option<String>,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/billingAccounts/{}/locations/{locationsId}/recentQueries",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/billingAccounts/{billingAccountsId}/locations/{locationsId}/recentQueries
/// Lists the RecentQueries that were created by the user making the request.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_billing_accounts_locations_recent_queries_list_execute()` or `logging_billing_accounts_locations_recent_queries_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_billing_accounts_locations_recent_queries_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_billing_accounts_locations_recent_queries_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListRecentQueriesResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListRecentQueriesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/billingAccounts/{billingAccountsId}/locations/{locationsId}/recentQueries
/// Lists the RecentQueries that were created by the user making the request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_billing_accounts_locations_recent_queries_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_billing_accounts_locations_recent_queries_list_task()`.
/// For the simplest API, use `logging_billing_accounts_locations_recent_queries_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_billing_accounts_locations_recent_queries_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_billing_accounts_locations_recent_queries_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListRecentQueriesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = logging_billing_accounts_locations_recent_queries_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_billing_accounts_locations_recent_queries_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingBillingAccountsLocationsRecentQueriesListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v2/billingAccounts/{billingAccountsId}/locations/{locationsId}/recentQueries
/// Lists the RecentQueries that were created by the user making the request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_billing_accounts_locations_recent_queries_list_builder()` + `logging_billing_accounts_locations_recent_queries_list_execute()`.
/// For task-level control, use `logging_billing_accounts_locations_recent_queries_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_billing_accounts_locations_recent_queries_list(
    client: &SimpleHttpClient,
    args: &LoggingBillingAccountsLocationsRecentQueriesListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListRecentQueriesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = logging_billing_accounts_locations_recent_queries_list_builder(
        client,
        &args.parent,
        &args.filter,
        &args.pageSize,
        &args.pageToken,
    )?;
    logging_billing_accounts_locations_recent_queries_list_execute(builder)
}

/// POST v2/billingAccounts/{billingAccountsId}/locations/{locationsId}/savedQueries
/// Creates a new SavedQuery for the user making the request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_billing_accounts_locations_saved_queries_create_execute()` to send, or `logging_billing_accounts_locations_saved_queries_create` for simplest API.

pub fn logging_billing_accounts_locations_saved_queries_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    savedQueryId: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/billingAccounts/{}/locations/{locationsId}/savedQueries",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = savedQueryId.as_ref() {
        query_parts.push(format!("savedQueryId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v2/billingAccounts/{billingAccountsId}/locations/{locationsId}/savedQueries
/// Creates a new SavedQuery for the user making the request.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_billing_accounts_locations_saved_queries_create_execute()` or `logging_billing_accounts_locations_saved_queries_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_billing_accounts_locations_saved_queries_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_billing_accounts_locations_saved_queries_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SavedQuery>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SavedQuery = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v2/billingAccounts/{billingAccountsId}/locations/{locationsId}/savedQueries
/// Creates a new SavedQuery for the user making the request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_billing_accounts_locations_saved_queries_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_billing_accounts_locations_saved_queries_create_task()`.
/// For the simplest API, use `logging_billing_accounts_locations_saved_queries_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_billing_accounts_locations_saved_queries_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_billing_accounts_locations_saved_queries_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SavedQuery>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = logging_billing_accounts_locations_saved_queries_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_billing_accounts_locations_saved_queries_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingBillingAccountsLocationsSavedQueriesCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: savedQueryId
    pub savedQueryId: Option<String>,
}

/// POST v2/billingAccounts/{billingAccountsId}/locations/{locationsId}/savedQueries
/// Creates a new SavedQuery for the user making the request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_billing_accounts_locations_saved_queries_create_builder()` + `logging_billing_accounts_locations_saved_queries_create_execute()`.
/// For task-level control, use `logging_billing_accounts_locations_saved_queries_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_billing_accounts_locations_saved_queries_create(
    client: &SimpleHttpClient,
    args: &LoggingBillingAccountsLocationsSavedQueriesCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SavedQuery>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_billing_accounts_locations_saved_queries_create_builder(
        client,
        &args.parent,
        &args.savedQueryId,
    )?;
    logging_billing_accounts_locations_saved_queries_create_execute(builder)
}

/// DELETE v2/billingAccounts/{billingAccountsId}/locations/{locationsId}/savedQueries/{savedQueriesId}
/// Deletes an existing SavedQuery that was created by the user making the request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_billing_accounts_locations_saved_queries_delete_execute()` to send, or `logging_billing_accounts_locations_saved_queries_delete` for simplest API.

pub fn logging_billing_accounts_locations_saved_queries_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/billingAccounts/{}/locations/{locationsId}/savedQueries/{savedQueriesId}",
        name,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v2/billingAccounts/{billingAccountsId}/locations/{locationsId}/savedQueries/{savedQueriesId}
/// Deletes an existing SavedQuery that was created by the user making the request.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_billing_accounts_locations_saved_queries_delete_execute()` or `logging_billing_accounts_locations_saved_queries_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_billing_accounts_locations_saved_queries_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_billing_accounts_locations_saved_queries_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v2/billingAccounts/{billingAccountsId}/locations/{locationsId}/savedQueries/{savedQueriesId}
/// Deletes an existing SavedQuery that was created by the user making the request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_billing_accounts_locations_saved_queries_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_billing_accounts_locations_saved_queries_delete_task()`.
/// For the simplest API, use `logging_billing_accounts_locations_saved_queries_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_billing_accounts_locations_saved_queries_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_billing_accounts_locations_saved_queries_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = logging_billing_accounts_locations_saved_queries_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_billing_accounts_locations_saved_queries_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingBillingAccountsLocationsSavedQueriesDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// DELETE v2/billingAccounts/{billingAccountsId}/locations/{locationsId}/savedQueries/{savedQueriesId}
/// Deletes an existing SavedQuery that was created by the user making the request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_billing_accounts_locations_saved_queries_delete_builder()` + `logging_billing_accounts_locations_saved_queries_delete_execute()`.
/// For task-level control, use `logging_billing_accounts_locations_saved_queries_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_billing_accounts_locations_saved_queries_delete(
    client: &SimpleHttpClient,
    args: &LoggingBillingAccountsLocationsSavedQueriesDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        logging_billing_accounts_locations_saved_queries_delete_builder(client, &args.name)?;
    logging_billing_accounts_locations_saved_queries_delete_execute(builder)
}

/// GET v2/billingAccounts/{billingAccountsId}/locations/{locationsId}/savedQueries/{savedQueriesId}
/// Returns all data associated with the requested query.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_billing_accounts_locations_saved_queries_get_execute()` to send, or `logging_billing_accounts_locations_saved_queries_get` for simplest API.

pub fn logging_billing_accounts_locations_saved_queries_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/billingAccounts/{}/locations/{locationsId}/savedQueries/{savedQueriesId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/billingAccounts/{billingAccountsId}/locations/{locationsId}/savedQueries/{savedQueriesId}
/// Returns all data associated with the requested query.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_billing_accounts_locations_saved_queries_get_execute()` or `logging_billing_accounts_locations_saved_queries_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_billing_accounts_locations_saved_queries_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_billing_accounts_locations_saved_queries_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SavedQuery>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SavedQuery = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/billingAccounts/{billingAccountsId}/locations/{locationsId}/savedQueries/{savedQueriesId}
/// Returns all data associated with the requested query.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_billing_accounts_locations_saved_queries_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_billing_accounts_locations_saved_queries_get_task()`.
/// For the simplest API, use `logging_billing_accounts_locations_saved_queries_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_billing_accounts_locations_saved_queries_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_billing_accounts_locations_saved_queries_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SavedQuery>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = logging_billing_accounts_locations_saved_queries_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_billing_accounts_locations_saved_queries_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingBillingAccountsLocationsSavedQueriesGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v2/billingAccounts/{billingAccountsId}/locations/{locationsId}/savedQueries/{savedQueriesId}
/// Returns all data associated with the requested query.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_billing_accounts_locations_saved_queries_get_builder()` + `logging_billing_accounts_locations_saved_queries_get_execute()`.
/// For task-level control, use `logging_billing_accounts_locations_saved_queries_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_billing_accounts_locations_saved_queries_get(
    client: &SimpleHttpClient,
    args: &LoggingBillingAccountsLocationsSavedQueriesGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SavedQuery>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_billing_accounts_locations_saved_queries_get_builder(client, &args.name)?;
    logging_billing_accounts_locations_saved_queries_get_execute(builder)
}

/// GET v2/billingAccounts/{billingAccountsId}/locations/{locationsId}/savedQueries
/// Lists the SavedQueries that were created by the user making the request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_billing_accounts_locations_saved_queries_list_execute()` to send, or `logging_billing_accounts_locations_saved_queries_list` for simplest API.

pub fn logging_billing_accounts_locations_saved_queries_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    filter: &Option<String>,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/billingAccounts/{}/locations/{locationsId}/savedQueries",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/billingAccounts/{billingAccountsId}/locations/{locationsId}/savedQueries
/// Lists the SavedQueries that were created by the user making the request.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_billing_accounts_locations_saved_queries_list_execute()` or `logging_billing_accounts_locations_saved_queries_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_billing_accounts_locations_saved_queries_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_billing_accounts_locations_saved_queries_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListSavedQueriesResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListSavedQueriesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/billingAccounts/{billingAccountsId}/locations/{locationsId}/savedQueries
/// Lists the SavedQueries that were created by the user making the request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_billing_accounts_locations_saved_queries_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_billing_accounts_locations_saved_queries_list_task()`.
/// For the simplest API, use `logging_billing_accounts_locations_saved_queries_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_billing_accounts_locations_saved_queries_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_billing_accounts_locations_saved_queries_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListSavedQueriesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = logging_billing_accounts_locations_saved_queries_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_billing_accounts_locations_saved_queries_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingBillingAccountsLocationsSavedQueriesListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v2/billingAccounts/{billingAccountsId}/locations/{locationsId}/savedQueries
/// Lists the SavedQueries that were created by the user making the request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_billing_accounts_locations_saved_queries_list_builder()` + `logging_billing_accounts_locations_saved_queries_list_execute()`.
/// For task-level control, use `logging_billing_accounts_locations_saved_queries_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_billing_accounts_locations_saved_queries_list(
    client: &SimpleHttpClient,
    args: &LoggingBillingAccountsLocationsSavedQueriesListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListSavedQueriesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = logging_billing_accounts_locations_saved_queries_list_builder(
        client,
        &args.parent,
        &args.filter,
        &args.pageSize,
        &args.pageToken,
    )?;
    logging_billing_accounts_locations_saved_queries_list_execute(builder)
}

/// PATCH v2/billingAccounts/{billingAccountsId}/locations/{locationsId}/savedQueries/{savedQueriesId}
/// Updates an existing SavedQuery.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_billing_accounts_locations_saved_queries_patch_execute()` to send, or `logging_billing_accounts_locations_saved_queries_patch` for simplest API.

pub fn logging_billing_accounts_locations_saved_queries_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    updateMask: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/billingAccounts/{}/locations/{locationsId}/savedQueries/{savedQueriesId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v2/billingAccounts/{billingAccountsId}/locations/{locationsId}/savedQueries/{savedQueriesId}
/// Updates an existing SavedQuery.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_billing_accounts_locations_saved_queries_patch_execute()` or `logging_billing_accounts_locations_saved_queries_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_billing_accounts_locations_saved_queries_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_billing_accounts_locations_saved_queries_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SavedQuery>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SavedQuery = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v2/billingAccounts/{billingAccountsId}/locations/{locationsId}/savedQueries/{savedQueriesId}
/// Updates an existing SavedQuery.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_billing_accounts_locations_saved_queries_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_billing_accounts_locations_saved_queries_patch_task()`.
/// For the simplest API, use `logging_billing_accounts_locations_saved_queries_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_billing_accounts_locations_saved_queries_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_billing_accounts_locations_saved_queries_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SavedQuery>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = logging_billing_accounts_locations_saved_queries_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_billing_accounts_locations_saved_queries_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingBillingAccountsLocationsSavedQueriesPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
}

/// PATCH v2/billingAccounts/{billingAccountsId}/locations/{locationsId}/savedQueries/{savedQueriesId}
/// Updates an existing SavedQuery.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_billing_accounts_locations_saved_queries_patch_builder()` + `logging_billing_accounts_locations_saved_queries_patch_execute()`.
/// For task-level control, use `logging_billing_accounts_locations_saved_queries_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_billing_accounts_locations_saved_queries_patch(
    client: &SimpleHttpClient,
    args: &LoggingBillingAccountsLocationsSavedQueriesPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SavedQuery>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_billing_accounts_locations_saved_queries_patch_builder(
        client,
        &args.name,
        &args.updateMask,
    )?;
    logging_billing_accounts_locations_saved_queries_patch_execute(builder)
}

/// DELETE v2/billingAccounts/{billingAccountsId}/logs/{logsId}
/// Deletes all the log entries in a log for the global _Default Log Bucket. The log reappears if it receives new entries. Log entries written shortly before the delete operation might not be deleted. Entries received after the delete operation with a timestamp before the operation will be deleted.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_billing_accounts_logs_delete_execute()` to send, or `logging_billing_accounts_logs_delete` for simplest API.

pub fn logging_billing_accounts_logs_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    logName: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/billingAccounts/{}/logs/{logsId}",
        logName,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v2/billingAccounts/{billingAccountsId}/logs/{logsId}
/// Deletes all the log entries in a log for the global _Default Log Bucket. The log reappears if it receives new entries. Log entries written shortly before the delete operation might not be deleted. Entries received after the delete operation with a timestamp before the operation will be deleted.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_billing_accounts_logs_delete_execute()` or `logging_billing_accounts_logs_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_billing_accounts_logs_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_billing_accounts_logs_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v2/billingAccounts/{billingAccountsId}/logs/{logsId}
/// Deletes all the log entries in a log for the global _Default Log Bucket. The log reappears if it receives new entries. Log entries written shortly before the delete operation might not be deleted. Entries received after the delete operation with a timestamp before the operation will be deleted.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_billing_accounts_logs_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_billing_accounts_logs_delete_task()`.
/// For the simplest API, use `logging_billing_accounts_logs_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_billing_accounts_logs_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_billing_accounts_logs_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = logging_billing_accounts_logs_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_billing_accounts_logs_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingBillingAccountsLogsDeleteArgs {
    /// Path parameter: logName
    pub logName: String,
}

/// DELETE v2/billingAccounts/{billingAccountsId}/logs/{logsId}
/// Deletes all the log entries in a log for the global _Default Log Bucket. The log reappears if it receives new entries. Log entries written shortly before the delete operation might not be deleted. Entries received after the delete operation with a timestamp before the operation will be deleted.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_billing_accounts_logs_delete_builder()` + `logging_billing_accounts_logs_delete_execute()`.
/// For task-level control, use `logging_billing_accounts_logs_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_billing_accounts_logs_delete(
    client: &SimpleHttpClient,
    args: &LoggingBillingAccountsLogsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_billing_accounts_logs_delete_builder(client, &args.logName)?;
    logging_billing_accounts_logs_delete_execute(builder)
}

/// GET v2/billingAccounts/{billingAccountsId}/logs
/// Lists the logs in projects, organizations, folders, or billing accounts. Only logs that have entries are listed.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_billing_accounts_logs_list_execute()` to send, or `logging_billing_accounts_logs_list` for simplest API.

pub fn logging_billing_accounts_logs_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
    resourceNames: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/billingAccounts/{}/logs",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = resourceNames.as_ref() {
        query_parts.push(format!("resourceNames={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/billingAccounts/{billingAccountsId}/logs
/// Lists the logs in projects, organizations, folders, or billing accounts. Only logs that have entries are listed.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_billing_accounts_logs_list_execute()` or `logging_billing_accounts_logs_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_billing_accounts_logs_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_billing_accounts_logs_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListLogsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListLogsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/billingAccounts/{billingAccountsId}/logs
/// Lists the logs in projects, organizations, folders, or billing accounts. Only logs that have entries are listed.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_billing_accounts_logs_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_billing_accounts_logs_list_task()`.
/// For the simplest API, use `logging_billing_accounts_logs_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_billing_accounts_logs_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_billing_accounts_logs_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListLogsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = logging_billing_accounts_logs_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_billing_accounts_logs_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingBillingAccountsLogsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: resourceNames
    pub resourceNames: Option<String>,
}

/// GET v2/billingAccounts/{billingAccountsId}/logs
/// Lists the logs in projects, organizations, folders, or billing accounts. Only logs that have entries are listed.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_billing_accounts_logs_list_builder()` + `logging_billing_accounts_logs_list_execute()`.
/// For task-level control, use `logging_billing_accounts_logs_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_billing_accounts_logs_list(
    client: &SimpleHttpClient,
    args: &LoggingBillingAccountsLogsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListLogsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = logging_billing_accounts_logs_list_builder(
        client,
        &args.parent,
        &args.pageSize,
        &args.pageToken,
        &args.resourceNames,
    )?;
    logging_billing_accounts_logs_list_execute(builder)
}

/// POST v2/billingAccounts/{billingAccountsId}/sinks
/// Creates a sink that exports specified log entries to a destination. The export begins upon ingress, unless the sink''s writer_identity is not permitted to write to the destination. A sink can export log entries only from the resource owning the sink.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_billing_accounts_sinks_create_execute()` to send, or `logging_billing_accounts_sinks_create` for simplest API.

pub fn logging_billing_accounts_sinks_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    customWriterIdentity: &Option<String>,
    uniqueWriterIdentity: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/billingAccounts/{}/sinks",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = customWriterIdentity.as_ref() {
        query_parts.push(format!("customWriterIdentity={}", val));
    }
    if let Some(val) = uniqueWriterIdentity.as_ref() {
        query_parts.push(format!("uniqueWriterIdentity={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v2/billingAccounts/{billingAccountsId}/sinks
/// Creates a sink that exports specified log entries to a destination. The export begins upon ingress, unless the sink''s writer_identity is not permitted to write to the destination. A sink can export log entries only from the resource owning the sink.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_billing_accounts_sinks_create_execute()` or `logging_billing_accounts_sinks_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_billing_accounts_sinks_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_billing_accounts_sinks_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<LogSink>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LogSink = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v2/billingAccounts/{billingAccountsId}/sinks
/// Creates a sink that exports specified log entries to a destination. The export begins upon ingress, unless the sink''s writer_identity is not permitted to write to the destination. A sink can export log entries only from the resource owning the sink.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_billing_accounts_sinks_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_billing_accounts_sinks_create_task()`.
/// For the simplest API, use `logging_billing_accounts_sinks_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_billing_accounts_sinks_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_billing_accounts_sinks_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogSink>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = logging_billing_accounts_sinks_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_billing_accounts_sinks_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingBillingAccountsSinksCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: customWriterIdentity
    pub customWriterIdentity: Option<String>,
    /// Query parameter: uniqueWriterIdentity
    pub uniqueWriterIdentity: Option<String>,
}

/// POST v2/billingAccounts/{billingAccountsId}/sinks
/// Creates a sink that exports specified log entries to a destination. The export begins upon ingress, unless the sink''s writer_identity is not permitted to write to the destination. A sink can export log entries only from the resource owning the sink.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_billing_accounts_sinks_create_builder()` + `logging_billing_accounts_sinks_create_execute()`.
/// For task-level control, use `logging_billing_accounts_sinks_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_billing_accounts_sinks_create(
    client: &SimpleHttpClient,
    args: &LoggingBillingAccountsSinksCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogSink>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_billing_accounts_sinks_create_builder(
        client,
        &args.parent,
        &args.customWriterIdentity,
        &args.uniqueWriterIdentity,
    )?;
    logging_billing_accounts_sinks_create_execute(builder)
}

/// DELETE v2/billingAccounts/{billingAccountsId}/sinks/{sinksId}
/// Deletes a sink. If the sink has a unique writer_identity, then that service account is also deleted.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_billing_accounts_sinks_delete_execute()` to send, or `logging_billing_accounts_sinks_delete` for simplest API.

pub fn logging_billing_accounts_sinks_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    sinkName: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/billingAccounts/{}/sinks/{sinksId}",
        sinkName,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v2/billingAccounts/{billingAccountsId}/sinks/{sinksId}
/// Deletes a sink. If the sink has a unique writer_identity, then that service account is also deleted.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_billing_accounts_sinks_delete_execute()` or `logging_billing_accounts_sinks_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_billing_accounts_sinks_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_billing_accounts_sinks_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v2/billingAccounts/{billingAccountsId}/sinks/{sinksId}
/// Deletes a sink. If the sink has a unique writer_identity, then that service account is also deleted.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_billing_accounts_sinks_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_billing_accounts_sinks_delete_task()`.
/// For the simplest API, use `logging_billing_accounts_sinks_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_billing_accounts_sinks_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_billing_accounts_sinks_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = logging_billing_accounts_sinks_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_billing_accounts_sinks_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingBillingAccountsSinksDeleteArgs {
    /// Path parameter: sinkName
    pub sinkName: String,
}

/// DELETE v2/billingAccounts/{billingAccountsId}/sinks/{sinksId}
/// Deletes a sink. If the sink has a unique writer_identity, then that service account is also deleted.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_billing_accounts_sinks_delete_builder()` + `logging_billing_accounts_sinks_delete_execute()`.
/// For task-level control, use `logging_billing_accounts_sinks_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_billing_accounts_sinks_delete(
    client: &SimpleHttpClient,
    args: &LoggingBillingAccountsSinksDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_billing_accounts_sinks_delete_builder(client, &args.sinkName)?;
    logging_billing_accounts_sinks_delete_execute(builder)
}

/// GET v2/billingAccounts/{billingAccountsId}/sinks/{sinksId}
/// Gets a sink.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_billing_accounts_sinks_get_execute()` to send, or `logging_billing_accounts_sinks_get` for simplest API.

pub fn logging_billing_accounts_sinks_get_builder<R>(
    client: &SimpleHttpClient<R>,
    sinkName: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/billingAccounts/{}/sinks/{sinksId}",
        sinkName,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/billingAccounts/{billingAccountsId}/sinks/{sinksId}
/// Gets a sink.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_billing_accounts_sinks_get_execute()` or `logging_billing_accounts_sinks_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_billing_accounts_sinks_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_billing_accounts_sinks_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<LogSink>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LogSink = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/billingAccounts/{billingAccountsId}/sinks/{sinksId}
/// Gets a sink.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_billing_accounts_sinks_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_billing_accounts_sinks_get_task()`.
/// For the simplest API, use `logging_billing_accounts_sinks_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_billing_accounts_sinks_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_billing_accounts_sinks_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogSink>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = logging_billing_accounts_sinks_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_billing_accounts_sinks_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingBillingAccountsSinksGetArgs {
    /// Path parameter: sinkName
    pub sinkName: String,
}

/// GET v2/billingAccounts/{billingAccountsId}/sinks/{sinksId}
/// Gets a sink.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_billing_accounts_sinks_get_builder()` + `logging_billing_accounts_sinks_get_execute()`.
/// For task-level control, use `logging_billing_accounts_sinks_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_billing_accounts_sinks_get(
    client: &SimpleHttpClient,
    args: &LoggingBillingAccountsSinksGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogSink>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_billing_accounts_sinks_get_builder(client, &args.sinkName)?;
    logging_billing_accounts_sinks_get_execute(builder)
}

/// GET v2/billingAccounts/{billingAccountsId}/sinks
/// Lists sinks.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_billing_accounts_sinks_list_execute()` to send, or `logging_billing_accounts_sinks_list` for simplest API.

pub fn logging_billing_accounts_sinks_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    filter: &Option<String>,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/billingAccounts/{}/sinks",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/billingAccounts/{billingAccountsId}/sinks
/// Lists sinks.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_billing_accounts_sinks_list_execute()` or `logging_billing_accounts_sinks_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_billing_accounts_sinks_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_billing_accounts_sinks_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListSinksResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListSinksResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/billingAccounts/{billingAccountsId}/sinks
/// Lists sinks.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_billing_accounts_sinks_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_billing_accounts_sinks_list_task()`.
/// For the simplest API, use `logging_billing_accounts_sinks_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_billing_accounts_sinks_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_billing_accounts_sinks_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListSinksResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = logging_billing_accounts_sinks_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_billing_accounts_sinks_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingBillingAccountsSinksListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v2/billingAccounts/{billingAccountsId}/sinks
/// Lists sinks.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_billing_accounts_sinks_list_builder()` + `logging_billing_accounts_sinks_list_execute()`.
/// For task-level control, use `logging_billing_accounts_sinks_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_billing_accounts_sinks_list(
    client: &SimpleHttpClient,
    args: &LoggingBillingAccountsSinksListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListSinksResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = logging_billing_accounts_sinks_list_builder(
        client,
        &args.parent,
        &args.filter,
        &args.pageSize,
        &args.pageToken,
    )?;
    logging_billing_accounts_sinks_list_execute(builder)
}

/// PATCH v2/billingAccounts/{billingAccountsId}/sinks/{sinksId}
/// Updates a sink. This method replaces the values of the destination and filter fields of the existing sink with the corresponding values from the new sink.The updated sink might also have a new writer_identity; see the unique_writer_identity field.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_billing_accounts_sinks_patch_execute()` to send, or `logging_billing_accounts_sinks_patch` for simplest API.

pub fn logging_billing_accounts_sinks_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    sinkName: &String,
    customWriterIdentity: &Option<String>,
    uniqueWriterIdentity: &Option<String>,
    updateMask: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/billingAccounts/{}/sinks/{sinksId}",
        sinkName,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = customWriterIdentity.as_ref() {
        query_parts.push(format!("customWriterIdentity={}", val));
    }
    if let Some(val) = uniqueWriterIdentity.as_ref() {
        query_parts.push(format!("uniqueWriterIdentity={}", val));
    }
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v2/billingAccounts/{billingAccountsId}/sinks/{sinksId}
/// Updates a sink. This method replaces the values of the destination and filter fields of the existing sink with the corresponding values from the new sink.The updated sink might also have a new writer_identity; see the unique_writer_identity field.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_billing_accounts_sinks_patch_execute()` or `logging_billing_accounts_sinks_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_billing_accounts_sinks_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_billing_accounts_sinks_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<LogSink>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LogSink = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v2/billingAccounts/{billingAccountsId}/sinks/{sinksId}
/// Updates a sink. This method replaces the values of the destination and filter fields of the existing sink with the corresponding values from the new sink.The updated sink might also have a new writer_identity; see the unique_writer_identity field.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_billing_accounts_sinks_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_billing_accounts_sinks_patch_task()`.
/// For the simplest API, use `logging_billing_accounts_sinks_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_billing_accounts_sinks_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_billing_accounts_sinks_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogSink>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = logging_billing_accounts_sinks_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_billing_accounts_sinks_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingBillingAccountsSinksPatchArgs {
    /// Path parameter: sinkName
    pub sinkName: String,
    /// Query parameter: customWriterIdentity
    pub customWriterIdentity: Option<String>,
    /// Query parameter: uniqueWriterIdentity
    pub uniqueWriterIdentity: Option<String>,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
}

/// PATCH v2/billingAccounts/{billingAccountsId}/sinks/{sinksId}
/// Updates a sink. This method replaces the values of the destination and filter fields of the existing sink with the corresponding values from the new sink.The updated sink might also have a new writer_identity; see the unique_writer_identity field.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_billing_accounts_sinks_patch_builder()` + `logging_billing_accounts_sinks_patch_execute()`.
/// For task-level control, use `logging_billing_accounts_sinks_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_billing_accounts_sinks_patch(
    client: &SimpleHttpClient,
    args: &LoggingBillingAccountsSinksPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogSink>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_billing_accounts_sinks_patch_builder(
        client,
        &args.sinkName,
        &args.customWriterIdentity,
        &args.uniqueWriterIdentity,
        &args.updateMask,
    )?;
    logging_billing_accounts_sinks_patch_execute(builder)
}

/// PUT v2/billingAccounts/{billingAccountsId}/sinks/{sinksId}
/// Updates a sink. This method replaces the values of the destination and filter fields of the existing sink with the corresponding values from the new sink.The updated sink might also have a new writer_identity; see the unique_writer_identity field.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_billing_accounts_sinks_update_execute()` to send, or `logging_billing_accounts_sinks_update` for simplest API.

pub fn logging_billing_accounts_sinks_update_builder<R>(
    client: &SimpleHttpClient<R>,
    sinkName: &String,
    customWriterIdentity: &Option<String>,
    uniqueWriterIdentity: &Option<String>,
    updateMask: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/billingAccounts/{}/sinks/{sinksId}",
        sinkName,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = customWriterIdentity.as_ref() {
        query_parts.push(format!("customWriterIdentity={}", val));
    }
    if let Some(val) = uniqueWriterIdentity.as_ref() {
        query_parts.push(format!("uniqueWriterIdentity={}", val));
    }
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .put(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PUT v2/billingAccounts/{billingAccountsId}/sinks/{sinksId}
/// Updates a sink. This method replaces the values of the destination and filter fields of the existing sink with the corresponding values from the new sink.The updated sink might also have a new writer_identity; see the unique_writer_identity field.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_billing_accounts_sinks_update_execute()` or `logging_billing_accounts_sinks_update`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_billing_accounts_sinks_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_billing_accounts_sinks_update_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<LogSink>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LogSink = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PUT v2/billingAccounts/{billingAccountsId}/sinks/{sinksId}
/// Updates a sink. This method replaces the values of the destination and filter fields of the existing sink with the corresponding values from the new sink.The updated sink might also have a new writer_identity; see the unique_writer_identity field.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_billing_accounts_sinks_update_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_billing_accounts_sinks_update_task()`.
/// For the simplest API, use `logging_billing_accounts_sinks_update()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_billing_accounts_sinks_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_billing_accounts_sinks_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogSink>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = logging_billing_accounts_sinks_update_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_billing_accounts_sinks_update`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingBillingAccountsSinksUpdateArgs {
    /// Path parameter: sinkName
    pub sinkName: String,
    /// Query parameter: customWriterIdentity
    pub customWriterIdentity: Option<String>,
    /// Query parameter: uniqueWriterIdentity
    pub uniqueWriterIdentity: Option<String>,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
}

/// PUT v2/billingAccounts/{billingAccountsId}/sinks/{sinksId}
/// Updates a sink. This method replaces the values of the destination and filter fields of the existing sink with the corresponding values from the new sink.The updated sink might also have a new writer_identity; see the unique_writer_identity field.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_billing_accounts_sinks_update_builder()` + `logging_billing_accounts_sinks_update_execute()`.
/// For task-level control, use `logging_billing_accounts_sinks_update_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_billing_accounts_sinks_update(
    client: &SimpleHttpClient,
    args: &LoggingBillingAccountsSinksUpdateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogSink>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_billing_accounts_sinks_update_builder(
        client,
        &args.sinkName,
        &args.customWriterIdentity,
        &args.uniqueWriterIdentity,
        &args.updateMask,
    )?;
    logging_billing_accounts_sinks_update_execute(builder)
}

/// POST v2/entries:copy
/// Copies a set of log entries from a log bucket to a Cloud Storage bucket.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_entries_copy_execute()` to send, or `logging_entries_copy` for simplest API.

pub fn logging_entries_copy_builder<R>(
    client: &SimpleHttpClient<R>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!("https://logging.googleapis.com/v2/entries:copy",);

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v2/entries:copy
/// Copies a set of log entries from a log bucket to a Cloud Storage bucket.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_entries_copy_execute()` or `logging_entries_copy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_entries_copy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_entries_copy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v2/entries:copy
/// Copies a set of log entries from a log bucket to a Cloud Storage bucket.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_entries_copy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_entries_copy_task()`.
/// For the simplest API, use `logging_entries_copy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_entries_copy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_entries_copy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = logging_entries_copy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST v2/entries:copy
/// Copies a set of log entries from a log bucket to a Cloud Storage bucket.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_entries_copy_builder()` + `logging_entries_copy_execute()`.
/// For task-level control, use `logging_entries_copy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_entries_copy(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_entries_copy_builder(client)?;
    logging_entries_copy_execute(builder)
}

/// POST v2/entries:list
/// Lists log entries. Use this method to retrieve log entries that originated from a `project/folder/organization/billing` account. For ways to export log entries, see Routing overview (<https://docs.cloud.google.`com/logging/docs/routing/overview`>).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_entries_list_execute()` to send, or `logging_entries_list` for simplest API.

pub fn logging_entries_list_builder<R>(
    client: &SimpleHttpClient<R>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!("https://logging.googleapis.com/v2/entries:list",);

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v2/entries:list
/// Lists log entries. Use this method to retrieve log entries that originated from a `project/folder/organization/billing` account. For ways to export log entries, see Routing overview (<https://docs.cloud.google.`com/logging/docs/routing/overview`>).
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_entries_list_execute()` or `logging_entries_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_entries_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_entries_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListLogEntriesResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListLogEntriesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v2/entries:list
/// Lists log entries. Use this method to retrieve log entries that originated from a `project/folder/organization/billing` account. For ways to export log entries, see Routing overview (<https://docs.cloud.google.`com/logging/docs/routing/overview`>).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_entries_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_entries_list_task()`.
/// For the simplest API, use `logging_entries_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_entries_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_entries_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListLogEntriesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = logging_entries_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST v2/entries:list
/// Lists log entries. Use this method to retrieve log entries that originated from a `project/folder/organization/billing` account. For ways to export log entries, see Routing overview (<https://docs.cloud.google.`com/logging/docs/routing/overview`>).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_entries_list_builder()` + `logging_entries_list_execute()`.
/// For task-level control, use `logging_entries_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_entries_list(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListLogEntriesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = logging_entries_list_builder(client)?;
    logging_entries_list_execute(builder)
}

/// POST v2/entries:tail
/// Streaming read of log entries as they are received. Until the stream is terminated, it will continue reading logs.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_entries_tail_execute()` to send, or `logging_entries_tail` for simplest API.

pub fn logging_entries_tail_builder<R>(
    client: &SimpleHttpClient<R>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!("https://logging.googleapis.com/v2/entries:tail",);

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v2/entries:tail
/// Streaming read of log entries as they are received. Until the stream is terminated, it will continue reading logs.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_entries_tail_execute()` or `logging_entries_tail`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_entries_tail_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_entries_tail_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TailLogEntriesResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TailLogEntriesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v2/entries:tail
/// Streaming read of log entries as they are received. Until the stream is terminated, it will continue reading logs.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_entries_tail_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_entries_tail_task()`.
/// For the simplest API, use `logging_entries_tail()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_entries_tail_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_entries_tail_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TailLogEntriesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = logging_entries_tail_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST v2/entries:tail
/// Streaming read of log entries as they are received. Until the stream is terminated, it will continue reading logs.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_entries_tail_builder()` + `logging_entries_tail_execute()`.
/// For task-level control, use `logging_entries_tail_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_entries_tail(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TailLogEntriesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = logging_entries_tail_builder(client)?;
    logging_entries_tail_execute(builder)
}

/// POST v2/entries:write
/// Writes log entries to Logging. This API method is the only way to send log entries to Logging. This method is used, directly or indirectly, by the Logging agent (fluentd) and all logging libraries configured to use Logging. A single request may contain log entries for a maximum of 1000 different resource names (projects, organizations, billing accounts or folders), where the resource name for a log entry is determined from its `logName` field.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_entries_write_execute()` to send, or `logging_entries_write` for simplest API.

pub fn logging_entries_write_builder<R>(
    client: &SimpleHttpClient<R>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!("https://logging.googleapis.com/v2/entries:write",);

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v2/entries:write
/// Writes log entries to Logging. This API method is the only way to send log entries to Logging. This method is used, directly or indirectly, by the Logging agent (fluentd) and all logging libraries configured to use Logging. A single request may contain log entries for a maximum of 1000 different resource names (projects, organizations, billing accounts or folders), where the resource name for a log entry is determined from its `logName` field.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_entries_write_execute()` or `logging_entries_write`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_entries_write_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_entries_write_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<WriteLogEntriesResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: WriteLogEntriesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v2/entries:write
/// Writes log entries to Logging. This API method is the only way to send log entries to Logging. This method is used, directly or indirectly, by the Logging agent (fluentd) and all logging libraries configured to use Logging. A single request may contain log entries for a maximum of 1000 different resource names (projects, organizations, billing accounts or folders), where the resource name for a log entry is determined from its `logName` field.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_entries_write_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_entries_write_task()`.
/// For the simplest API, use `logging_entries_write()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_entries_write_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_entries_write_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<WriteLogEntriesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = logging_entries_write_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST v2/entries:write
/// Writes log entries to Logging. This API method is the only way to send log entries to Logging. This method is used, directly or indirectly, by the Logging agent (fluentd) and all logging libraries configured to use Logging. A single request may contain log entries for a maximum of 1000 different resource names (projects, organizations, billing accounts or folders), where the resource name for a log entry is determined from its `logName` field.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_entries_write_builder()` + `logging_entries_write_execute()`.
/// For task-level control, use `logging_entries_write_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_entries_write(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<WriteLogEntriesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = logging_entries_write_builder(client)?;
    logging_entries_write_execute(builder)
}

/// POST v2/{v2Id}/{v2Id1}/exclusions
/// Creates a new exclusion in the _Default sink in a specified parent resource. Only log entries belonging to that resource can be excluded. You can have up to 10 exclusions in a resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_exclusions_create_execute()` to send, or `logging_exclusions_create` for simplest API.

pub fn logging_exclusions_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/{}/{v2Id1}/exclusions",
        parent,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v2/{v2Id}/{v2Id1}/exclusions
/// Creates a new exclusion in the _Default sink in a specified parent resource. Only log entries belonging to that resource can be excluded. You can have up to 10 exclusions in a resource.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_exclusions_create_execute()` or `logging_exclusions_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_exclusions_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_exclusions_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<LogExclusion>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LogExclusion = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v2/{v2Id}/{v2Id1}/exclusions
/// Creates a new exclusion in the _Default sink in a specified parent resource. Only log entries belonging to that resource can be excluded. You can have up to 10 exclusions in a resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_exclusions_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_exclusions_create_task()`.
/// For the simplest API, use `logging_exclusions_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_exclusions_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_exclusions_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogExclusion>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = logging_exclusions_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_exclusions_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingExclusionsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// POST v2/{v2Id}/{v2Id1}/exclusions
/// Creates a new exclusion in the _Default sink in a specified parent resource. Only log entries belonging to that resource can be excluded. You can have up to 10 exclusions in a resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_exclusions_create_builder()` + `logging_exclusions_create_execute()`.
/// For task-level control, use `logging_exclusions_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_exclusions_create(
    client: &SimpleHttpClient,
    args: &LoggingExclusionsCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogExclusion>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = logging_exclusions_create_builder(client, &args.parent)?;
    logging_exclusions_create_execute(builder)
}

/// DELETE v2/{v2Id}/{v2Id1}/exclusions/{exclusionsId}
/// Deletes an exclusion in the _Default sink.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_exclusions_delete_execute()` to send, or `logging_exclusions_delete` for simplest API.

pub fn logging_exclusions_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/{}/{v2Id1}/exclusions/{exclusionsId}",
        name,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v2/{v2Id}/{v2Id1}/exclusions/{exclusionsId}
/// Deletes an exclusion in the _Default sink.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_exclusions_delete_execute()` or `logging_exclusions_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_exclusions_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_exclusions_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v2/{v2Id}/{v2Id1}/exclusions/{exclusionsId}
/// Deletes an exclusion in the _Default sink.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_exclusions_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_exclusions_delete_task()`.
/// For the simplest API, use `logging_exclusions_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_exclusions_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_exclusions_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = logging_exclusions_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_exclusions_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingExclusionsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// DELETE v2/{v2Id}/{v2Id1}/exclusions/{exclusionsId}
/// Deletes an exclusion in the _Default sink.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_exclusions_delete_builder()` + `logging_exclusions_delete_execute()`.
/// For task-level control, use `logging_exclusions_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_exclusions_delete(
    client: &SimpleHttpClient,
    args: &LoggingExclusionsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_exclusions_delete_builder(client, &args.name)?;
    logging_exclusions_delete_execute(builder)
}

/// GET v2/{v2Id}/{v2Id1}/exclusions/{exclusionsId}
/// Gets the description of an exclusion in the _Default sink.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_exclusions_get_execute()` to send, or `logging_exclusions_get` for simplest API.

pub fn logging_exclusions_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/{}/{v2Id1}/exclusions/{exclusionsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/{v2Id}/{v2Id1}/exclusions/{exclusionsId}
/// Gets the description of an exclusion in the _Default sink.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_exclusions_get_execute()` or `logging_exclusions_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_exclusions_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_exclusions_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<LogExclusion>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LogExclusion = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/{v2Id}/{v2Id1}/exclusions/{exclusionsId}
/// Gets the description of an exclusion in the _Default sink.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_exclusions_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_exclusions_get_task()`.
/// For the simplest API, use `logging_exclusions_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_exclusions_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_exclusions_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogExclusion>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = logging_exclusions_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_exclusions_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingExclusionsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v2/{v2Id}/{v2Id1}/exclusions/{exclusionsId}
/// Gets the description of an exclusion in the _Default sink.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_exclusions_get_builder()` + `logging_exclusions_get_execute()`.
/// For task-level control, use `logging_exclusions_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_exclusions_get(
    client: &SimpleHttpClient,
    args: &LoggingExclusionsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogExclusion>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = logging_exclusions_get_builder(client, &args.name)?;
    logging_exclusions_get_execute(builder)
}

/// GET v2/{v2Id}/{v2Id1}/exclusions
/// Lists all the exclusions on the _Default sink in a parent resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_exclusions_list_execute()` to send, or `logging_exclusions_list` for simplest API.

pub fn logging_exclusions_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/{}/{v2Id1}/exclusions",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/{v2Id}/{v2Id1}/exclusions
/// Lists all the exclusions on the _Default sink in a parent resource.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_exclusions_list_execute()` or `logging_exclusions_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_exclusions_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_exclusions_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListExclusionsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListExclusionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/{v2Id}/{v2Id1}/exclusions
/// Lists all the exclusions on the _Default sink in a parent resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_exclusions_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_exclusions_list_task()`.
/// For the simplest API, use `logging_exclusions_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_exclusions_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_exclusions_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListExclusionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = logging_exclusions_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_exclusions_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingExclusionsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v2/{v2Id}/{v2Id1}/exclusions
/// Lists all the exclusions on the _Default sink in a parent resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_exclusions_list_builder()` + `logging_exclusions_list_execute()`.
/// For task-level control, use `logging_exclusions_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_exclusions_list(
    client: &SimpleHttpClient,
    args: &LoggingExclusionsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListExclusionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        logging_exclusions_list_builder(client, &args.parent, &args.pageSize, &args.pageToken)?;
    logging_exclusions_list_execute(builder)
}

/// PATCH v2/{v2Id}/{v2Id1}/exclusions/{exclusionsId}
/// Changes one or more properties of an existing exclusion in the _Default sink.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_exclusions_patch_execute()` to send, or `logging_exclusions_patch` for simplest API.

pub fn logging_exclusions_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    updateMask: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/{}/{v2Id1}/exclusions/{exclusionsId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v2/{v2Id}/{v2Id1}/exclusions/{exclusionsId}
/// Changes one or more properties of an existing exclusion in the _Default sink.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_exclusions_patch_execute()` or `logging_exclusions_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_exclusions_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_exclusions_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<LogExclusion>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LogExclusion = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v2/{v2Id}/{v2Id1}/exclusions/{exclusionsId}
/// Changes one or more properties of an existing exclusion in the _Default sink.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_exclusions_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_exclusions_patch_task()`.
/// For the simplest API, use `logging_exclusions_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_exclusions_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_exclusions_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogExclusion>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = logging_exclusions_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_exclusions_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingExclusionsPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
}

/// PATCH v2/{v2Id}/{v2Id1}/exclusions/{exclusionsId}
/// Changes one or more properties of an existing exclusion in the _Default sink.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_exclusions_patch_builder()` + `logging_exclusions_patch_execute()`.
/// For task-level control, use `logging_exclusions_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_exclusions_patch(
    client: &SimpleHttpClient,
    args: &LoggingExclusionsPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogExclusion>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = logging_exclusions_patch_builder(client, &args.name, &args.updateMask)?;
    logging_exclusions_patch_execute(builder)
}

/// GET v2/folders/{foldersId}/cmekSettings
/// Gets the Logging CMEK settings for the given resource.Note: CMEK for the Log Router can be configured for Google Cloud projects, folders, organizations, and billing accounts. Once configured for an organization, it applies to all projects and folders in the Google Cloud organization.See Configure CMEK for Cloud Logging (<https://docs.cloud.google.`com/logging/docs/routing/managed-encryption`>) for more information.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_folders_get_cmek_settings_execute()` to send, or `logging_folders_get_cmek_settings` for simplest API.

pub fn logging_folders_get_cmek_settings_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/folders/{}/cmekSettings",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/folders/{foldersId}/cmekSettings
/// Gets the Logging CMEK settings for the given resource.Note: CMEK for the Log Router can be configured for Google Cloud projects, folders, organizations, and billing accounts. Once configured for an organization, it applies to all projects and folders in the Google Cloud organization.See Configure CMEK for Cloud Logging (<https://docs.cloud.google.`com/logging/docs/routing/managed-encryption`>) for more information.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_folders_get_cmek_settings_execute()` or `logging_folders_get_cmek_settings`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_folders_get_cmek_settings_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_folders_get_cmek_settings_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<CmekSettings>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: CmekSettings = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/folders/{foldersId}/cmekSettings
/// Gets the Logging CMEK settings for the given resource.Note: CMEK for the Log Router can be configured for Google Cloud projects, folders, organizations, and billing accounts. Once configured for an organization, it applies to all projects and folders in the Google Cloud organization.See Configure CMEK for Cloud Logging (<https://docs.cloud.google.`com/logging/docs/routing/managed-encryption`>) for more information.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_folders_get_cmek_settings_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_folders_get_cmek_settings_task()`.
/// For the simplest API, use `logging_folders_get_cmek_settings()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_folders_get_cmek_settings_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_folders_get_cmek_settings_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CmekSettings>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = logging_folders_get_cmek_settings_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_folders_get_cmek_settings`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingFoldersGetCmekSettingsArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v2/folders/{foldersId}/cmekSettings
/// Gets the Logging CMEK settings for the given resource.Note: CMEK for the Log Router can be configured for Google Cloud projects, folders, organizations, and billing accounts. Once configured for an organization, it applies to all projects and folders in the Google Cloud organization.See Configure CMEK for Cloud Logging (<https://docs.cloud.google.`com/logging/docs/routing/managed-encryption`>) for more information.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_folders_get_cmek_settings_builder()` + `logging_folders_get_cmek_settings_execute()`.
/// For task-level control, use `logging_folders_get_cmek_settings_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_folders_get_cmek_settings(
    client: &SimpleHttpClient,
    args: &LoggingFoldersGetCmekSettingsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CmekSettings>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = logging_folders_get_cmek_settings_builder(client, &args.name)?;
    logging_folders_get_cmek_settings_execute(builder)
}

/// GET v2/folders/{foldersId}/settings
/// Gets the settings for the given resource.Note: Settings can be retrieved for Google Cloud projects, folders, organizations, and billing accounts.See View default resource settings for Logging (<https://docs.cloud.google.`com/logging/docs/default-settings`#view-org-settings>) for more information.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_folders_get_settings_execute()` to send, or `logging_folders_get_settings` for simplest API.

pub fn logging_folders_get_settings_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/folders/{}/settings",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/folders/{foldersId}/settings
/// Gets the settings for the given resource.Note: Settings can be retrieved for Google Cloud projects, folders, organizations, and billing accounts.See View default resource settings for Logging (<https://docs.cloud.google.`com/logging/docs/default-settings`#view-org-settings>) for more information.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_folders_get_settings_execute()` or `logging_folders_get_settings`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_folders_get_settings_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_folders_get_settings_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Settings>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Settings = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/folders/{foldersId}/settings
/// Gets the settings for the given resource.Note: Settings can be retrieved for Google Cloud projects, folders, organizations, and billing accounts.See View default resource settings for Logging (<https://docs.cloud.google.`com/logging/docs/default-settings`#view-org-settings>) for more information.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_folders_get_settings_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_folders_get_settings_task()`.
/// For the simplest API, use `logging_folders_get_settings()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_folders_get_settings_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_folders_get_settings_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Settings>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = logging_folders_get_settings_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_folders_get_settings`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingFoldersGetSettingsArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v2/folders/{foldersId}/settings
/// Gets the settings for the given resource.Note: Settings can be retrieved for Google Cloud projects, folders, organizations, and billing accounts.See View default resource settings for Logging (<https://docs.cloud.google.`com/logging/docs/default-settings`#view-org-settings>) for more information.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_folders_get_settings_builder()` + `logging_folders_get_settings_execute()`.
/// For task-level control, use `logging_folders_get_settings_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_folders_get_settings(
    client: &SimpleHttpClient,
    args: &LoggingFoldersGetSettingsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Settings>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_folders_get_settings_builder(client, &args.name)?;
    logging_folders_get_settings_execute(builder)
}

/// PATCH v2/folders/{foldersId}/settings
/// Updates the settings for the given resource. This method applies to all feature configurations for organization and folders.UpdateSettings fails when any of the following are `true`: The value of storage_location either isn''t supported by Logging or violates the location OrgPolicy. The default_sink_config field is set, but it has an unspecified filter write mode. The value of kms_key_name is invalid. The associated service account doesn''t have the required `roles/cloudkms`.`cryptoKeyEncrypterDecrypter` role assigned for the key. Access to the key is disabled.See Configure default settings for organizations and folders (<https://docs.cloud.google.`com/logging/docs/default-settings`>) for more information.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_folders_update_settings_execute()` to send, or `logging_folders_update_settings` for simplest API.

pub fn logging_folders_update_settings_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    updateMask: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/folders/{}/settings",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v2/folders/{foldersId}/settings
/// Updates the settings for the given resource. This method applies to all feature configurations for organization and folders.UpdateSettings fails when any of the following are `true`: The value of storage_location either isn''t supported by Logging or violates the location OrgPolicy. The default_sink_config field is set, but it has an unspecified filter write mode. The value of kms_key_name is invalid. The associated service account doesn''t have the required `roles/cloudkms`.`cryptoKeyEncrypterDecrypter` role assigned for the key. Access to the key is disabled.See Configure default settings for organizations and folders (<https://docs.cloud.google.`com/logging/docs/default-settings`>) for more information.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_folders_update_settings_execute()` or `logging_folders_update_settings`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_folders_update_settings_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_folders_update_settings_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Settings>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Settings = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v2/folders/{foldersId}/settings
/// Updates the settings for the given resource. This method applies to all feature configurations for organization and folders.UpdateSettings fails when any of the following are `true`: The value of storage_location either isn''t supported by Logging or violates the location OrgPolicy. The default_sink_config field is set, but it has an unspecified filter write mode. The value of kms_key_name is invalid. The associated service account doesn''t have the required `roles/cloudkms`.`cryptoKeyEncrypterDecrypter` role assigned for the key. Access to the key is disabled.See Configure default settings for organizations and folders (<https://docs.cloud.google.`com/logging/docs/default-settings`>) for more information.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_folders_update_settings_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_folders_update_settings_task()`.
/// For the simplest API, use `logging_folders_update_settings()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_folders_update_settings_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_folders_update_settings_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Settings>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = logging_folders_update_settings_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_folders_update_settings`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingFoldersUpdateSettingsArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
}

/// PATCH v2/folders/{foldersId}/settings
/// Updates the settings for the given resource. This method applies to all feature configurations for organization and folders.UpdateSettings fails when any of the following are `true`: The value of storage_location either isn''t supported by Logging or violates the location OrgPolicy. The default_sink_config field is set, but it has an unspecified filter write mode. The value of kms_key_name is invalid. The associated service account doesn''t have the required `roles/cloudkms`.`cryptoKeyEncrypterDecrypter` role assigned for the key. Access to the key is disabled.See Configure default settings for organizations and folders (<https://docs.cloud.google.`com/logging/docs/default-settings`>) for more information.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_folders_update_settings_builder()` + `logging_folders_update_settings_execute()`.
/// For task-level control, use `logging_folders_update_settings_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_folders_update_settings(
    client: &SimpleHttpClient,
    args: &LoggingFoldersUpdateSettingsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Settings>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_folders_update_settings_builder(client, &args.name, &args.updateMask)?;
    logging_folders_update_settings_execute(builder)
}

/// POST v2/folders/{foldersId}/exclusions
/// Creates a new exclusion in the _Default sink in a specified parent resource. Only log entries belonging to that resource can be excluded. You can have up to 10 exclusions in a resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_folders_exclusions_create_execute()` to send, or `logging_folders_exclusions_create` for simplest API.

pub fn logging_folders_exclusions_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/folders/{}/exclusions",
        parent,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v2/folders/{foldersId}/exclusions
/// Creates a new exclusion in the _Default sink in a specified parent resource. Only log entries belonging to that resource can be excluded. You can have up to 10 exclusions in a resource.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_folders_exclusions_create_execute()` or `logging_folders_exclusions_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_folders_exclusions_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_folders_exclusions_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<LogExclusion>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LogExclusion = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v2/folders/{foldersId}/exclusions
/// Creates a new exclusion in the _Default sink in a specified parent resource. Only log entries belonging to that resource can be excluded. You can have up to 10 exclusions in a resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_folders_exclusions_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_folders_exclusions_create_task()`.
/// For the simplest API, use `logging_folders_exclusions_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_folders_exclusions_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_folders_exclusions_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogExclusion>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = logging_folders_exclusions_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_folders_exclusions_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingFoldersExclusionsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// POST v2/folders/{foldersId}/exclusions
/// Creates a new exclusion in the _Default sink in a specified parent resource. Only log entries belonging to that resource can be excluded. You can have up to 10 exclusions in a resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_folders_exclusions_create_builder()` + `logging_folders_exclusions_create_execute()`.
/// For task-level control, use `logging_folders_exclusions_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_folders_exclusions_create(
    client: &SimpleHttpClient,
    args: &LoggingFoldersExclusionsCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogExclusion>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = logging_folders_exclusions_create_builder(client, &args.parent)?;
    logging_folders_exclusions_create_execute(builder)
}

/// DELETE v2/folders/{foldersId}/exclusions/{exclusionsId}
/// Deletes an exclusion in the _Default sink.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_folders_exclusions_delete_execute()` to send, or `logging_folders_exclusions_delete` for simplest API.

pub fn logging_folders_exclusions_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/folders/{}/exclusions/{exclusionsId}",
        name,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v2/folders/{foldersId}/exclusions/{exclusionsId}
/// Deletes an exclusion in the _Default sink.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_folders_exclusions_delete_execute()` or `logging_folders_exclusions_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_folders_exclusions_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_folders_exclusions_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v2/folders/{foldersId}/exclusions/{exclusionsId}
/// Deletes an exclusion in the _Default sink.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_folders_exclusions_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_folders_exclusions_delete_task()`.
/// For the simplest API, use `logging_folders_exclusions_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_folders_exclusions_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_folders_exclusions_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = logging_folders_exclusions_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_folders_exclusions_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingFoldersExclusionsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// DELETE v2/folders/{foldersId}/exclusions/{exclusionsId}
/// Deletes an exclusion in the _Default sink.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_folders_exclusions_delete_builder()` + `logging_folders_exclusions_delete_execute()`.
/// For task-level control, use `logging_folders_exclusions_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_folders_exclusions_delete(
    client: &SimpleHttpClient,
    args: &LoggingFoldersExclusionsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_folders_exclusions_delete_builder(client, &args.name)?;
    logging_folders_exclusions_delete_execute(builder)
}

/// GET v2/folders/{foldersId}/exclusions/{exclusionsId}
/// Gets the description of an exclusion in the _Default sink.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_folders_exclusions_get_execute()` to send, or `logging_folders_exclusions_get` for simplest API.

pub fn logging_folders_exclusions_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/folders/{}/exclusions/{exclusionsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/folders/{foldersId}/exclusions/{exclusionsId}
/// Gets the description of an exclusion in the _Default sink.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_folders_exclusions_get_execute()` or `logging_folders_exclusions_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_folders_exclusions_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_folders_exclusions_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<LogExclusion>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LogExclusion = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/folders/{foldersId}/exclusions/{exclusionsId}
/// Gets the description of an exclusion in the _Default sink.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_folders_exclusions_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_folders_exclusions_get_task()`.
/// For the simplest API, use `logging_folders_exclusions_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_folders_exclusions_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_folders_exclusions_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogExclusion>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = logging_folders_exclusions_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_folders_exclusions_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingFoldersExclusionsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v2/folders/{foldersId}/exclusions/{exclusionsId}
/// Gets the description of an exclusion in the _Default sink.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_folders_exclusions_get_builder()` + `logging_folders_exclusions_get_execute()`.
/// For task-level control, use `logging_folders_exclusions_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_folders_exclusions_get(
    client: &SimpleHttpClient,
    args: &LoggingFoldersExclusionsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogExclusion>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = logging_folders_exclusions_get_builder(client, &args.name)?;
    logging_folders_exclusions_get_execute(builder)
}

/// GET v2/folders/{foldersId}/exclusions
/// Lists all the exclusions on the _Default sink in a parent resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_folders_exclusions_list_execute()` to send, or `logging_folders_exclusions_list` for simplest API.

pub fn logging_folders_exclusions_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/folders/{}/exclusions",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/folders/{foldersId}/exclusions
/// Lists all the exclusions on the _Default sink in a parent resource.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_folders_exclusions_list_execute()` or `logging_folders_exclusions_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_folders_exclusions_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_folders_exclusions_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListExclusionsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListExclusionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/folders/{foldersId}/exclusions
/// Lists all the exclusions on the _Default sink in a parent resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_folders_exclusions_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_folders_exclusions_list_task()`.
/// For the simplest API, use `logging_folders_exclusions_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_folders_exclusions_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_folders_exclusions_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListExclusionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = logging_folders_exclusions_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_folders_exclusions_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingFoldersExclusionsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v2/folders/{foldersId}/exclusions
/// Lists all the exclusions on the _Default sink in a parent resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_folders_exclusions_list_builder()` + `logging_folders_exclusions_list_execute()`.
/// For task-level control, use `logging_folders_exclusions_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_folders_exclusions_list(
    client: &SimpleHttpClient,
    args: &LoggingFoldersExclusionsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListExclusionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = logging_folders_exclusions_list_builder(
        client,
        &args.parent,
        &args.pageSize,
        &args.pageToken,
    )?;
    logging_folders_exclusions_list_execute(builder)
}

/// PATCH v2/folders/{foldersId}/exclusions/{exclusionsId}
/// Changes one or more properties of an existing exclusion in the _Default sink.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_folders_exclusions_patch_execute()` to send, or `logging_folders_exclusions_patch` for simplest API.

pub fn logging_folders_exclusions_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    updateMask: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/folders/{}/exclusions/{exclusionsId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v2/folders/{foldersId}/exclusions/{exclusionsId}
/// Changes one or more properties of an existing exclusion in the _Default sink.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_folders_exclusions_patch_execute()` or `logging_folders_exclusions_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_folders_exclusions_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_folders_exclusions_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<LogExclusion>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LogExclusion = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v2/folders/{foldersId}/exclusions/{exclusionsId}
/// Changes one or more properties of an existing exclusion in the _Default sink.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_folders_exclusions_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_folders_exclusions_patch_task()`.
/// For the simplest API, use `logging_folders_exclusions_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_folders_exclusions_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_folders_exclusions_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogExclusion>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = logging_folders_exclusions_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_folders_exclusions_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingFoldersExclusionsPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
}

/// PATCH v2/folders/{foldersId}/exclusions/{exclusionsId}
/// Changes one or more properties of an existing exclusion in the _Default sink.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_folders_exclusions_patch_builder()` + `logging_folders_exclusions_patch_execute()`.
/// For task-level control, use `logging_folders_exclusions_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_folders_exclusions_patch(
    client: &SimpleHttpClient,
    args: &LoggingFoldersExclusionsPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogExclusion>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = logging_folders_exclusions_patch_builder(client, &args.name, &args.updateMask)?;
    logging_folders_exclusions_patch_execute(builder)
}

/// GET v2/folders/{foldersId}/locations/{locationsId}
/// Gets information about a location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_folders_locations_get_execute()` to send, or `logging_folders_locations_get` for simplest API.

pub fn logging_folders_locations_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/folders/{}/locations/{locationsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/folders/{foldersId}/locations/{locationsId}
/// Gets information about a location.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_folders_locations_get_execute()` or `logging_folders_locations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_folders_locations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_folders_locations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Location>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Location = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/folders/{foldersId}/locations/{locationsId}
/// Gets information about a location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_folders_locations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_folders_locations_get_task()`.
/// For the simplest API, use `logging_folders_locations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_folders_locations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_folders_locations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Location>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = logging_folders_locations_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_folders_locations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingFoldersLocationsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v2/folders/{foldersId}/locations/{locationsId}
/// Gets information about a location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_folders_locations_get_builder()` + `logging_folders_locations_get_execute()`.
/// For task-level control, use `logging_folders_locations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_folders_locations_get(
    client: &SimpleHttpClient,
    args: &LoggingFoldersLocationsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Location>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_folders_locations_get_builder(client, &args.name)?;
    logging_folders_locations_get_execute(builder)
}

/// GET v2/folders/{foldersId}/locations
/// Lists information about the supported locations for this service.This method lists locations based on the resource scope provided in the ListLocationsRequest.name field: Global locations: If name is empty, the method lists the public locations available to all projects. Project-specific locations: If name follows the format `projects/{project}`, the method lists locations visible to that specific project. This includes public, private, or other project-specific locations enabled for the project.For `gRPC` and client library implementations, the resource name is passed as the name field. For direct service calls, the resource name is incorporated into the request path based on the specific service implementation and version.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_folders_locations_list_execute()` to send, or `logging_folders_locations_list` for simplest API.

pub fn logging_folders_locations_list_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    extraLocationTypes: &Option<String>,
    filter: &Option<String>,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/folders/{}/locations",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = extraLocationTypes.as_ref() {
        query_parts.push(format!("extraLocationTypes={}", val));
    }
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/folders/{foldersId}/locations
/// Lists information about the supported locations for this service.This method lists locations based on the resource scope provided in the ListLocationsRequest.name field: Global locations: If name is empty, the method lists the public locations available to all projects. Project-specific locations: If name follows the format `projects/{project}`, the method lists locations visible to that specific project. This includes public, private, or other project-specific locations enabled for the project.For `gRPC` and client library implementations, the resource name is passed as the name field. For direct service calls, the resource name is incorporated into the request path based on the specific service implementation and version.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_folders_locations_list_execute()` or `logging_folders_locations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_folders_locations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_folders_locations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListLocationsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListLocationsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/folders/{foldersId}/locations
/// Lists information about the supported locations for this service.This method lists locations based on the resource scope provided in the ListLocationsRequest.name field: Global locations: If name is empty, the method lists the public locations available to all projects. Project-specific locations: If name follows the format `projects/{project}`, the method lists locations visible to that specific project. This includes public, private, or other project-specific locations enabled for the project.For `gRPC` and client library implementations, the resource name is passed as the name field. For direct service calls, the resource name is incorporated into the request path based on the specific service implementation and version.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_folders_locations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_folders_locations_list_task()`.
/// For the simplest API, use `logging_folders_locations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_folders_locations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_folders_locations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListLocationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = logging_folders_locations_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_folders_locations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingFoldersLocationsListArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: extraLocationTypes
    pub extraLocationTypes: Option<String>,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v2/folders/{foldersId}/locations
/// Lists information about the supported locations for this service.This method lists locations based on the resource scope provided in the ListLocationsRequest.name field: Global locations: If name is empty, the method lists the public locations available to all projects. Project-specific locations: If name follows the format `projects/{project}`, the method lists locations visible to that specific project. This includes public, private, or other project-specific locations enabled for the project.For `gRPC` and client library implementations, the resource name is passed as the name field. For direct service calls, the resource name is incorporated into the request path based on the specific service implementation and version.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_folders_locations_list_builder()` + `logging_folders_locations_list_execute()`.
/// For task-level control, use `logging_folders_locations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_folders_locations_list(
    client: &SimpleHttpClient,
    args: &LoggingFoldersLocationsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListLocationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = logging_folders_locations_list_builder(
        client,
        &args.name,
        &args.extraLocationTypes,
        &args.filter,
        &args.pageSize,
        &args.pageToken,
    )?;
    logging_folders_locations_list_execute(builder)
}

/// POST v2/folders/{foldersId}/locations/{locationsId}/buckets
/// Creates a log bucket that can be used to store log entries. After a bucket has been created, the bucket''s location cannot be changed.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_folders_locations_buckets_create_execute()` to send, or `logging_folders_locations_buckets_create` for simplest API.

pub fn logging_folders_locations_buckets_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    bucketId: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/folders/{}/locations/{locationsId}/buckets",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = bucketId.as_ref() {
        query_parts.push(format!("bucketId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v2/folders/{foldersId}/locations/{locationsId}/buckets
/// Creates a log bucket that can be used to store log entries. After a bucket has been created, the bucket''s location cannot be changed.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_folders_locations_buckets_create_execute()` or `logging_folders_locations_buckets_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_folders_locations_buckets_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_folders_locations_buckets_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<LogBucket>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LogBucket = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v2/folders/{foldersId}/locations/{locationsId}/buckets
/// Creates a log bucket that can be used to store log entries. After a bucket has been created, the bucket''s location cannot be changed.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_folders_locations_buckets_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_folders_locations_buckets_create_task()`.
/// For the simplest API, use `logging_folders_locations_buckets_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_folders_locations_buckets_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_folders_locations_buckets_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogBucket>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = logging_folders_locations_buckets_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_folders_locations_buckets_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingFoldersLocationsBucketsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: bucketId
    pub bucketId: Option<String>,
}

/// POST v2/folders/{foldersId}/locations/{locationsId}/buckets
/// Creates a log bucket that can be used to store log entries. After a bucket has been created, the bucket''s location cannot be changed.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_folders_locations_buckets_create_builder()` + `logging_folders_locations_buckets_create_execute()`.
/// For task-level control, use `logging_folders_locations_buckets_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_folders_locations_buckets_create(
    client: &SimpleHttpClient,
    args: &LoggingFoldersLocationsBucketsCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogBucket>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        logging_folders_locations_buckets_create_builder(client, &args.parent, &args.bucketId)?;
    logging_folders_locations_buckets_create_execute(builder)
}

/// POST v2/folders/{foldersId}/locations/{locationsId}/buckets:createAsync
/// Creates a log bucket asynchronously that can be used to store log entries.After a bucket has been created, the bucket''s location cannot be changed.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_folders_locations_buckets_create_async_execute()` to send, or `logging_folders_locations_buckets_create_async` for simplest API.

pub fn logging_folders_locations_buckets_create_async_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    bucketId: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/folders/{}/locations/{locationsId}/buckets:createAsync",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = bucketId.as_ref() {
        query_parts.push(format!("bucketId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v2/folders/{foldersId}/locations/{locationsId}/buckets:createAsync
/// Creates a log bucket asynchronously that can be used to store log entries.After a bucket has been created, the bucket''s location cannot be changed.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_folders_locations_buckets_create_async_execute()` or `logging_folders_locations_buckets_create_async`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_folders_locations_buckets_create_async_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_folders_locations_buckets_create_async_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v2/folders/{foldersId}/locations/{locationsId}/buckets:createAsync
/// Creates a log bucket asynchronously that can be used to store log entries.After a bucket has been created, the bucket''s location cannot be changed.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_folders_locations_buckets_create_async_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_folders_locations_buckets_create_async_task()`.
/// For the simplest API, use `logging_folders_locations_buckets_create_async()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_folders_locations_buckets_create_async_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_folders_locations_buckets_create_async_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = logging_folders_locations_buckets_create_async_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_folders_locations_buckets_create_async`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingFoldersLocationsBucketsCreateAsyncArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: bucketId
    pub bucketId: Option<String>,
}

/// POST v2/folders/{foldersId}/locations/{locationsId}/buckets:createAsync
/// Creates a log bucket asynchronously that can be used to store log entries.After a bucket has been created, the bucket''s location cannot be changed.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_folders_locations_buckets_create_async_builder()` + `logging_folders_locations_buckets_create_async_execute()`.
/// For task-level control, use `logging_folders_locations_buckets_create_async_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_folders_locations_buckets_create_async(
    client: &SimpleHttpClient,
    args: &LoggingFoldersLocationsBucketsCreateAsyncArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_folders_locations_buckets_create_async_builder(
        client,
        &args.parent,
        &args.bucketId,
    )?;
    logging_folders_locations_buckets_create_async_execute(builder)
}

/// DELETE v2/folders/{foldersId}/locations/{locationsId}/buckets/{bucketsId}
/// Deletes a log bucket.Changes the bucket''s lifecycle_state to the DELETE_REQUESTED state. After 7 days, the bucket will be purged and all log entries in the bucket will be permanently deleted.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_folders_locations_buckets_delete_execute()` to send, or `logging_folders_locations_buckets_delete` for simplest API.

pub fn logging_folders_locations_buckets_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/folders/{}/locations/{locationsId}/buckets/{bucketsId}",
        name,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v2/folders/{foldersId}/locations/{locationsId}/buckets/{bucketsId}
/// Deletes a log bucket.Changes the bucket''s lifecycle_state to the DELETE_REQUESTED state. After 7 days, the bucket will be purged and all log entries in the bucket will be permanently deleted.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_folders_locations_buckets_delete_execute()` or `logging_folders_locations_buckets_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_folders_locations_buckets_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_folders_locations_buckets_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v2/folders/{foldersId}/locations/{locationsId}/buckets/{bucketsId}
/// Deletes a log bucket.Changes the bucket''s lifecycle_state to the DELETE_REQUESTED state. After 7 days, the bucket will be purged and all log entries in the bucket will be permanently deleted.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_folders_locations_buckets_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_folders_locations_buckets_delete_task()`.
/// For the simplest API, use `logging_folders_locations_buckets_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_folders_locations_buckets_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_folders_locations_buckets_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = logging_folders_locations_buckets_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_folders_locations_buckets_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingFoldersLocationsBucketsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// DELETE v2/folders/{foldersId}/locations/{locationsId}/buckets/{bucketsId}
/// Deletes a log bucket.Changes the bucket''s lifecycle_state to the DELETE_REQUESTED state. After 7 days, the bucket will be purged and all log entries in the bucket will be permanently deleted.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_folders_locations_buckets_delete_builder()` + `logging_folders_locations_buckets_delete_execute()`.
/// For task-level control, use `logging_folders_locations_buckets_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_folders_locations_buckets_delete(
    client: &SimpleHttpClient,
    args: &LoggingFoldersLocationsBucketsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_folders_locations_buckets_delete_builder(client, &args.name)?;
    logging_folders_locations_buckets_delete_execute(builder)
}

/// GET v2/folders/{foldersId}/locations/{locationsId}/buckets/{bucketsId}
/// Gets a log bucket.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_folders_locations_buckets_get_execute()` to send, or `logging_folders_locations_buckets_get` for simplest API.

pub fn logging_folders_locations_buckets_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/folders/{}/locations/{locationsId}/buckets/{bucketsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/folders/{foldersId}/locations/{locationsId}/buckets/{bucketsId}
/// Gets a log bucket.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_folders_locations_buckets_get_execute()` or `logging_folders_locations_buckets_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_folders_locations_buckets_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_folders_locations_buckets_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<LogBucket>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LogBucket = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/folders/{foldersId}/locations/{locationsId}/buckets/{bucketsId}
/// Gets a log bucket.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_folders_locations_buckets_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_folders_locations_buckets_get_task()`.
/// For the simplest API, use `logging_folders_locations_buckets_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_folders_locations_buckets_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_folders_locations_buckets_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogBucket>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = logging_folders_locations_buckets_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_folders_locations_buckets_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingFoldersLocationsBucketsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v2/folders/{foldersId}/locations/{locationsId}/buckets/{bucketsId}
/// Gets a log bucket.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_folders_locations_buckets_get_builder()` + `logging_folders_locations_buckets_get_execute()`.
/// For task-level control, use `logging_folders_locations_buckets_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_folders_locations_buckets_get(
    client: &SimpleHttpClient,
    args: &LoggingFoldersLocationsBucketsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogBucket>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_folders_locations_buckets_get_builder(client, &args.name)?;
    logging_folders_locations_buckets_get_execute(builder)
}

/// GET v2/folders/{foldersId}/locations/{locationsId}/buckets
/// Lists log buckets.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_folders_locations_buckets_list_execute()` to send, or `logging_folders_locations_buckets_list` for simplest API.

pub fn logging_folders_locations_buckets_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/folders/{}/locations/{locationsId}/buckets",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/folders/{foldersId}/locations/{locationsId}/buckets
/// Lists log buckets.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_folders_locations_buckets_list_execute()` or `logging_folders_locations_buckets_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_folders_locations_buckets_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_folders_locations_buckets_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListBucketsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListBucketsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/folders/{foldersId}/locations/{locationsId}/buckets
/// Lists log buckets.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_folders_locations_buckets_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_folders_locations_buckets_list_task()`.
/// For the simplest API, use `logging_folders_locations_buckets_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_folders_locations_buckets_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_folders_locations_buckets_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListBucketsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = logging_folders_locations_buckets_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_folders_locations_buckets_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingFoldersLocationsBucketsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v2/folders/{foldersId}/locations/{locationsId}/buckets
/// Lists log buckets.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_folders_locations_buckets_list_builder()` + `logging_folders_locations_buckets_list_execute()`.
/// For task-level control, use `logging_folders_locations_buckets_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_folders_locations_buckets_list(
    client: &SimpleHttpClient,
    args: &LoggingFoldersLocationsBucketsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListBucketsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = logging_folders_locations_buckets_list_builder(
        client,
        &args.parent,
        &args.pageSize,
        &args.pageToken,
    )?;
    logging_folders_locations_buckets_list_execute(builder)
}

/// PATCH v2/folders/{foldersId}/locations/{locationsId}/buckets/{bucketsId}
/// Updates a log bucket.If the bucket has a lifecycle_state of DELETE_REQUESTED, then FAILED_PRECONDITION will be returned.After a bucket has been created, the bucket''s location cannot be changed.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_folders_locations_buckets_patch_execute()` to send, or `logging_folders_locations_buckets_patch` for simplest API.

pub fn logging_folders_locations_buckets_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    updateMask: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/folders/{}/locations/{locationsId}/buckets/{bucketsId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v2/folders/{foldersId}/locations/{locationsId}/buckets/{bucketsId}
/// Updates a log bucket.If the bucket has a lifecycle_state of DELETE_REQUESTED, then FAILED_PRECONDITION will be returned.After a bucket has been created, the bucket''s location cannot be changed.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_folders_locations_buckets_patch_execute()` or `logging_folders_locations_buckets_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_folders_locations_buckets_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_folders_locations_buckets_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<LogBucket>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LogBucket = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v2/folders/{foldersId}/locations/{locationsId}/buckets/{bucketsId}
/// Updates a log bucket.If the bucket has a lifecycle_state of DELETE_REQUESTED, then FAILED_PRECONDITION will be returned.After a bucket has been created, the bucket''s location cannot be changed.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_folders_locations_buckets_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_folders_locations_buckets_patch_task()`.
/// For the simplest API, use `logging_folders_locations_buckets_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_folders_locations_buckets_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_folders_locations_buckets_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogBucket>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = logging_folders_locations_buckets_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_folders_locations_buckets_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingFoldersLocationsBucketsPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
}

/// PATCH v2/folders/{foldersId}/locations/{locationsId}/buckets/{bucketsId}
/// Updates a log bucket.If the bucket has a lifecycle_state of DELETE_REQUESTED, then FAILED_PRECONDITION will be returned.After a bucket has been created, the bucket''s location cannot be changed.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_folders_locations_buckets_patch_builder()` + `logging_folders_locations_buckets_patch_execute()`.
/// For task-level control, use `logging_folders_locations_buckets_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_folders_locations_buckets_patch(
    client: &SimpleHttpClient,
    args: &LoggingFoldersLocationsBucketsPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogBucket>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        logging_folders_locations_buckets_patch_builder(client, &args.name, &args.updateMask)?;
    logging_folders_locations_buckets_patch_execute(builder)
}

/// POST v2/folders/{foldersId}/locations/{locationsId}/buckets/{bucketsId}:undelete
/// Undeletes a log bucket. A bucket that has been deleted can be undeleted within the grace period of 7 days.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_folders_locations_buckets_undelete_execute()` to send, or `logging_folders_locations_buckets_undelete` for simplest API.

pub fn logging_folders_locations_buckets_undelete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/folders/{}/locations/{locationsId}/buckets/{bucketsId}:undelete",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v2/folders/{foldersId}/locations/{locationsId}/buckets/{bucketsId}:undelete
/// Undeletes a log bucket. A bucket that has been deleted can be undeleted within the grace period of 7 days.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_folders_locations_buckets_undelete_execute()` or `logging_folders_locations_buckets_undelete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_folders_locations_buckets_undelete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_folders_locations_buckets_undelete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v2/folders/{foldersId}/locations/{locationsId}/buckets/{bucketsId}:undelete
/// Undeletes a log bucket. A bucket that has been deleted can be undeleted within the grace period of 7 days.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_folders_locations_buckets_undelete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_folders_locations_buckets_undelete_task()`.
/// For the simplest API, use `logging_folders_locations_buckets_undelete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_folders_locations_buckets_undelete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_folders_locations_buckets_undelete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = logging_folders_locations_buckets_undelete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_folders_locations_buckets_undelete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingFoldersLocationsBucketsUndeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v2/folders/{foldersId}/locations/{locationsId}/buckets/{bucketsId}:undelete
/// Undeletes a log bucket. A bucket that has been deleted can be undeleted within the grace period of 7 days.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_folders_locations_buckets_undelete_builder()` + `logging_folders_locations_buckets_undelete_execute()`.
/// For task-level control, use `logging_folders_locations_buckets_undelete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_folders_locations_buckets_undelete(
    client: &SimpleHttpClient,
    args: &LoggingFoldersLocationsBucketsUndeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_folders_locations_buckets_undelete_builder(client, &args.name)?;
    logging_folders_locations_buckets_undelete_execute(builder)
}

/// POST v2/folders/{foldersId}/locations/{locationsId}/buckets/{bucketsId}:updateAsync
/// Updates a log bucket asynchronously.If the bucket has a lifecycle_state of DELETE_REQUESTED, then FAILED_PRECONDITION will be returned.After a bucket has been created, the bucket''s location cannot be changed.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_folders_locations_buckets_update_async_execute()` to send, or `logging_folders_locations_buckets_update_async` for simplest API.

pub fn logging_folders_locations_buckets_update_async_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    updateMask: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/folders/{}/locations/{locationsId}/buckets/{bucketsId}:updateAsync",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v2/folders/{foldersId}/locations/{locationsId}/buckets/{bucketsId}:updateAsync
/// Updates a log bucket asynchronously.If the bucket has a lifecycle_state of DELETE_REQUESTED, then FAILED_PRECONDITION will be returned.After a bucket has been created, the bucket''s location cannot be changed.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_folders_locations_buckets_update_async_execute()` or `logging_folders_locations_buckets_update_async`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_folders_locations_buckets_update_async_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_folders_locations_buckets_update_async_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v2/folders/{foldersId}/locations/{locationsId}/buckets/{bucketsId}:updateAsync
/// Updates a log bucket asynchronously.If the bucket has a lifecycle_state of DELETE_REQUESTED, then FAILED_PRECONDITION will be returned.After a bucket has been created, the bucket''s location cannot be changed.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_folders_locations_buckets_update_async_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_folders_locations_buckets_update_async_task()`.
/// For the simplest API, use `logging_folders_locations_buckets_update_async()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_folders_locations_buckets_update_async_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_folders_locations_buckets_update_async_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = logging_folders_locations_buckets_update_async_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_folders_locations_buckets_update_async`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingFoldersLocationsBucketsUpdateAsyncArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
}

/// POST v2/folders/{foldersId}/locations/{locationsId}/buckets/{bucketsId}:updateAsync
/// Updates a log bucket asynchronously.If the bucket has a lifecycle_state of DELETE_REQUESTED, then FAILED_PRECONDITION will be returned.After a bucket has been created, the bucket''s location cannot be changed.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_folders_locations_buckets_update_async_builder()` + `logging_folders_locations_buckets_update_async_execute()`.
/// For task-level control, use `logging_folders_locations_buckets_update_async_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_folders_locations_buckets_update_async(
    client: &SimpleHttpClient,
    args: &LoggingFoldersLocationsBucketsUpdateAsyncArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_folders_locations_buckets_update_async_builder(
        client,
        &args.name,
        &args.updateMask,
    )?;
    logging_folders_locations_buckets_update_async_execute(builder)
}

/// POST v2/folders/{foldersId}/locations/{locationsId}/buckets/{bucketsId}/links
/// Asynchronously creates a linked dataset in BigQuery which makes it possible to use BigQuery to read the logs stored in the log bucket. A log bucket may currently only contain one link.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_folders_locations_buckets_links_create_execute()` to send, or `logging_folders_locations_buckets_links_create` for simplest API.

pub fn logging_folders_locations_buckets_links_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    linkId: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/folders/{}/locations/{locationsId}/buckets/{bucketsId}/links",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = linkId.as_ref() {
        query_parts.push(format!("linkId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v2/folders/{foldersId}/locations/{locationsId}/buckets/{bucketsId}/links
/// Asynchronously creates a linked dataset in BigQuery which makes it possible to use BigQuery to read the logs stored in the log bucket. A log bucket may currently only contain one link.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_folders_locations_buckets_links_create_execute()` or `logging_folders_locations_buckets_links_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_folders_locations_buckets_links_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_folders_locations_buckets_links_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v2/folders/{foldersId}/locations/{locationsId}/buckets/{bucketsId}/links
/// Asynchronously creates a linked dataset in BigQuery which makes it possible to use BigQuery to read the logs stored in the log bucket. A log bucket may currently only contain one link.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_folders_locations_buckets_links_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_folders_locations_buckets_links_create_task()`.
/// For the simplest API, use `logging_folders_locations_buckets_links_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_folders_locations_buckets_links_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_folders_locations_buckets_links_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = logging_folders_locations_buckets_links_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_folders_locations_buckets_links_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingFoldersLocationsBucketsLinksCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: linkId
    pub linkId: Option<String>,
}

/// POST v2/folders/{foldersId}/locations/{locationsId}/buckets/{bucketsId}/links
/// Asynchronously creates a linked dataset in BigQuery which makes it possible to use BigQuery to read the logs stored in the log bucket. A log bucket may currently only contain one link.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_folders_locations_buckets_links_create_builder()` + `logging_folders_locations_buckets_links_create_execute()`.
/// For task-level control, use `logging_folders_locations_buckets_links_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_folders_locations_buckets_links_create(
    client: &SimpleHttpClient,
    args: &LoggingFoldersLocationsBucketsLinksCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        logging_folders_locations_buckets_links_create_builder(client, &args.parent, &args.linkId)?;
    logging_folders_locations_buckets_links_create_execute(builder)
}

/// DELETE v2/folders/{foldersId}/locations/{locationsId}/buckets/{bucketsId}/links/{linksId}
/// Deletes a link. This will also delete the corresponding BigQuery linked dataset.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_folders_locations_buckets_links_delete_execute()` to send, or `logging_folders_locations_buckets_links_delete` for simplest API.

pub fn logging_folders_locations_buckets_links_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/folders/{}/locations/{locationsId}/buckets/{bucketsId}/links/{linksId}",
        name,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v2/folders/{foldersId}/locations/{locationsId}/buckets/{bucketsId}/links/{linksId}
/// Deletes a link. This will also delete the corresponding BigQuery linked dataset.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_folders_locations_buckets_links_delete_execute()` or `logging_folders_locations_buckets_links_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_folders_locations_buckets_links_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_folders_locations_buckets_links_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v2/folders/{foldersId}/locations/{locationsId}/buckets/{bucketsId}/links/{linksId}
/// Deletes a link. This will also delete the corresponding BigQuery linked dataset.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_folders_locations_buckets_links_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_folders_locations_buckets_links_delete_task()`.
/// For the simplest API, use `logging_folders_locations_buckets_links_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_folders_locations_buckets_links_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_folders_locations_buckets_links_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = logging_folders_locations_buckets_links_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_folders_locations_buckets_links_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingFoldersLocationsBucketsLinksDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// DELETE v2/folders/{foldersId}/locations/{locationsId}/buckets/{bucketsId}/links/{linksId}
/// Deletes a link. This will also delete the corresponding BigQuery linked dataset.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_folders_locations_buckets_links_delete_builder()` + `logging_folders_locations_buckets_links_delete_execute()`.
/// For task-level control, use `logging_folders_locations_buckets_links_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_folders_locations_buckets_links_delete(
    client: &SimpleHttpClient,
    args: &LoggingFoldersLocationsBucketsLinksDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_folders_locations_buckets_links_delete_builder(client, &args.name)?;
    logging_folders_locations_buckets_links_delete_execute(builder)
}

/// GET v2/folders/{foldersId}/locations/{locationsId}/buckets/{bucketsId}/links/{linksId}
/// Gets a link.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_folders_locations_buckets_links_get_execute()` to send, or `logging_folders_locations_buckets_links_get` for simplest API.

pub fn logging_folders_locations_buckets_links_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/folders/{}/locations/{locationsId}/buckets/{bucketsId}/links/{linksId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/folders/{foldersId}/locations/{locationsId}/buckets/{bucketsId}/links/{linksId}
/// Gets a link.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_folders_locations_buckets_links_get_execute()` or `logging_folders_locations_buckets_links_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_folders_locations_buckets_links_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_folders_locations_buckets_links_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Link>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Link = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/folders/{foldersId}/locations/{locationsId}/buckets/{bucketsId}/links/{linksId}
/// Gets a link.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_folders_locations_buckets_links_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_folders_locations_buckets_links_get_task()`.
/// For the simplest API, use `logging_folders_locations_buckets_links_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_folders_locations_buckets_links_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_folders_locations_buckets_links_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Link>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = logging_folders_locations_buckets_links_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_folders_locations_buckets_links_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingFoldersLocationsBucketsLinksGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v2/folders/{foldersId}/locations/{locationsId}/buckets/{bucketsId}/links/{linksId}
/// Gets a link.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_folders_locations_buckets_links_get_builder()` + `logging_folders_locations_buckets_links_get_execute()`.
/// For task-level control, use `logging_folders_locations_buckets_links_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_folders_locations_buckets_links_get(
    client: &SimpleHttpClient,
    args: &LoggingFoldersLocationsBucketsLinksGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Link>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_folders_locations_buckets_links_get_builder(client, &args.name)?;
    logging_folders_locations_buckets_links_get_execute(builder)
}

/// GET v2/folders/{foldersId}/locations/{locationsId}/buckets/{bucketsId}/links
/// Lists links.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_folders_locations_buckets_links_list_execute()` to send, or `logging_folders_locations_buckets_links_list` for simplest API.

pub fn logging_folders_locations_buckets_links_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/folders/{}/locations/{locationsId}/buckets/{bucketsId}/links",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/folders/{foldersId}/locations/{locationsId}/buckets/{bucketsId}/links
/// Lists links.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_folders_locations_buckets_links_list_execute()` or `logging_folders_locations_buckets_links_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_folders_locations_buckets_links_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_folders_locations_buckets_links_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListLinksResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListLinksResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/folders/{foldersId}/locations/{locationsId}/buckets/{bucketsId}/links
/// Lists links.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_folders_locations_buckets_links_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_folders_locations_buckets_links_list_task()`.
/// For the simplest API, use `logging_folders_locations_buckets_links_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_folders_locations_buckets_links_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_folders_locations_buckets_links_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListLinksResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = logging_folders_locations_buckets_links_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_folders_locations_buckets_links_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingFoldersLocationsBucketsLinksListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v2/folders/{foldersId}/locations/{locationsId}/buckets/{bucketsId}/links
/// Lists links.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_folders_locations_buckets_links_list_builder()` + `logging_folders_locations_buckets_links_list_execute()`.
/// For task-level control, use `logging_folders_locations_buckets_links_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_folders_locations_buckets_links_list(
    client: &SimpleHttpClient,
    args: &LoggingFoldersLocationsBucketsLinksListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListLinksResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = logging_folders_locations_buckets_links_list_builder(
        client,
        &args.parent,
        &args.pageSize,
        &args.pageToken,
    )?;
    logging_folders_locations_buckets_links_list_execute(builder)
}

/// POST v2/folders/{foldersId}/locations/{locationsId}/buckets/{bucketsId}/views
/// Creates a view over log entries in a log bucket. A bucket may contain a maximum of 30 views.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_folders_locations_buckets_views_create_execute()` to send, or `logging_folders_locations_buckets_views_create` for simplest API.

pub fn logging_folders_locations_buckets_views_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    viewId: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/folders/{}/locations/{locationsId}/buckets/{bucketsId}/views",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = viewId.as_ref() {
        query_parts.push(format!("viewId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v2/folders/{foldersId}/locations/{locationsId}/buckets/{bucketsId}/views
/// Creates a view over log entries in a log bucket. A bucket may contain a maximum of 30 views.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_folders_locations_buckets_views_create_execute()` or `logging_folders_locations_buckets_views_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_folders_locations_buckets_views_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_folders_locations_buckets_views_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<LogView>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LogView = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v2/folders/{foldersId}/locations/{locationsId}/buckets/{bucketsId}/views
/// Creates a view over log entries in a log bucket. A bucket may contain a maximum of 30 views.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_folders_locations_buckets_views_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_folders_locations_buckets_views_create_task()`.
/// For the simplest API, use `logging_folders_locations_buckets_views_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_folders_locations_buckets_views_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_folders_locations_buckets_views_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogView>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = logging_folders_locations_buckets_views_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_folders_locations_buckets_views_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingFoldersLocationsBucketsViewsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: viewId
    pub viewId: Option<String>,
}

/// POST v2/folders/{foldersId}/locations/{locationsId}/buckets/{bucketsId}/views
/// Creates a view over log entries in a log bucket. A bucket may contain a maximum of 30 views.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_folders_locations_buckets_views_create_builder()` + `logging_folders_locations_buckets_views_create_execute()`.
/// For task-level control, use `logging_folders_locations_buckets_views_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_folders_locations_buckets_views_create(
    client: &SimpleHttpClient,
    args: &LoggingFoldersLocationsBucketsViewsCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogView>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        logging_folders_locations_buckets_views_create_builder(client, &args.parent, &args.viewId)?;
    logging_folders_locations_buckets_views_create_execute(builder)
}

/// DELETE v2/folders/{foldersId}/locations/{locationsId}/buckets/{bucketsId}/views/{viewsId}
/// Deletes a view on a log bucket. If an UNAVAILABLE error is returned, this indicates that system is not in a state where it can delete the view. If this occurs, please try again in a few minutes.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_folders_locations_buckets_views_delete_execute()` to send, or `logging_folders_locations_buckets_views_delete` for simplest API.

pub fn logging_folders_locations_buckets_views_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/folders/{}/locations/{locationsId}/buckets/{bucketsId}/views/{viewsId}",
        name,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v2/folders/{foldersId}/locations/{locationsId}/buckets/{bucketsId}/views/{viewsId}
/// Deletes a view on a log bucket. If an UNAVAILABLE error is returned, this indicates that system is not in a state where it can delete the view. If this occurs, please try again in a few minutes.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_folders_locations_buckets_views_delete_execute()` or `logging_folders_locations_buckets_views_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_folders_locations_buckets_views_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_folders_locations_buckets_views_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v2/folders/{foldersId}/locations/{locationsId}/buckets/{bucketsId}/views/{viewsId}
/// Deletes a view on a log bucket. If an UNAVAILABLE error is returned, this indicates that system is not in a state where it can delete the view. If this occurs, please try again in a few minutes.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_folders_locations_buckets_views_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_folders_locations_buckets_views_delete_task()`.
/// For the simplest API, use `logging_folders_locations_buckets_views_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_folders_locations_buckets_views_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_folders_locations_buckets_views_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = logging_folders_locations_buckets_views_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_folders_locations_buckets_views_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingFoldersLocationsBucketsViewsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// DELETE v2/folders/{foldersId}/locations/{locationsId}/buckets/{bucketsId}/views/{viewsId}
/// Deletes a view on a log bucket. If an UNAVAILABLE error is returned, this indicates that system is not in a state where it can delete the view. If this occurs, please try again in a few minutes.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_folders_locations_buckets_views_delete_builder()` + `logging_folders_locations_buckets_views_delete_execute()`.
/// For task-level control, use `logging_folders_locations_buckets_views_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_folders_locations_buckets_views_delete(
    client: &SimpleHttpClient,
    args: &LoggingFoldersLocationsBucketsViewsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_folders_locations_buckets_views_delete_builder(client, &args.name)?;
    logging_folders_locations_buckets_views_delete_execute(builder)
}

/// GET v2/folders/{foldersId}/locations/{locationsId}/buckets/{bucketsId}/views/{viewsId}
/// Gets a view on a log bucket.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_folders_locations_buckets_views_get_execute()` to send, or `logging_folders_locations_buckets_views_get` for simplest API.

pub fn logging_folders_locations_buckets_views_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/folders/{}/locations/{locationsId}/buckets/{bucketsId}/views/{viewsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/folders/{foldersId}/locations/{locationsId}/buckets/{bucketsId}/views/{viewsId}
/// Gets a view on a log bucket.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_folders_locations_buckets_views_get_execute()` or `logging_folders_locations_buckets_views_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_folders_locations_buckets_views_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_folders_locations_buckets_views_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<LogView>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LogView = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/folders/{foldersId}/locations/{locationsId}/buckets/{bucketsId}/views/{viewsId}
/// Gets a view on a log bucket.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_folders_locations_buckets_views_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_folders_locations_buckets_views_get_task()`.
/// For the simplest API, use `logging_folders_locations_buckets_views_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_folders_locations_buckets_views_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_folders_locations_buckets_views_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogView>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = logging_folders_locations_buckets_views_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_folders_locations_buckets_views_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingFoldersLocationsBucketsViewsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v2/folders/{foldersId}/locations/{locationsId}/buckets/{bucketsId}/views/{viewsId}
/// Gets a view on a log bucket.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_folders_locations_buckets_views_get_builder()` + `logging_folders_locations_buckets_views_get_execute()`.
/// For task-level control, use `logging_folders_locations_buckets_views_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_folders_locations_buckets_views_get(
    client: &SimpleHttpClient,
    args: &LoggingFoldersLocationsBucketsViewsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogView>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_folders_locations_buckets_views_get_builder(client, &args.name)?;
    logging_folders_locations_buckets_views_get_execute(builder)
}

/// POST v2/folders/{foldersId}/locations/{locationsId}/buckets/{bucketsId}/views/{viewsId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_folders_locations_buckets_views_get_iam_policy_execute()` to send, or `logging_folders_locations_buckets_views_get_iam_policy` for simplest API.

pub fn logging_folders_locations_buckets_views_get_iam_policy_builder<R>(
    client: &SimpleHttpClient<R>,
    resource: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/folders/{}/locations/{locationsId}/buckets/{bucketsId}/views/{viewsId}:getIamPolicy",
        resource,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v2/folders/{foldersId}/locations/{locationsId}/buckets/{bucketsId}/views/{viewsId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_folders_locations_buckets_views_get_iam_policy_execute()` or `logging_folders_locations_buckets_views_get_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_folders_locations_buckets_views_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_folders_locations_buckets_views_get_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v2/folders/{foldersId}/locations/{locationsId}/buckets/{bucketsId}/views/{viewsId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_folders_locations_buckets_views_get_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_folders_locations_buckets_views_get_iam_policy_task()`.
/// For the simplest API, use `logging_folders_locations_buckets_views_get_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_folders_locations_buckets_views_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_folders_locations_buckets_views_get_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = logging_folders_locations_buckets_views_get_iam_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_folders_locations_buckets_views_get_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingFoldersLocationsBucketsViewsGetIamPolicyArgs {
    /// Path parameter: resource
    pub resource: String,
}

/// POST v2/folders/{foldersId}/locations/{locationsId}/buckets/{bucketsId}/views/{viewsId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_folders_locations_buckets_views_get_iam_policy_builder()` + `logging_folders_locations_buckets_views_get_iam_policy_execute()`.
/// For task-level control, use `logging_folders_locations_buckets_views_get_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_folders_locations_buckets_views_get_iam_policy(
    client: &SimpleHttpClient,
    args: &LoggingFoldersLocationsBucketsViewsGetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        logging_folders_locations_buckets_views_get_iam_policy_builder(client, &args.resource)?;
    logging_folders_locations_buckets_views_get_iam_policy_execute(builder)
}

/// GET v2/folders/{foldersId}/locations/{locationsId}/buckets/{bucketsId}/views
/// Lists views on a log bucket.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_folders_locations_buckets_views_list_execute()` to send, or `logging_folders_locations_buckets_views_list` for simplest API.

pub fn logging_folders_locations_buckets_views_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/folders/{}/locations/{locationsId}/buckets/{bucketsId}/views",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/folders/{foldersId}/locations/{locationsId}/buckets/{bucketsId}/views
/// Lists views on a log bucket.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_folders_locations_buckets_views_list_execute()` or `logging_folders_locations_buckets_views_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_folders_locations_buckets_views_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_folders_locations_buckets_views_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListViewsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListViewsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/folders/{foldersId}/locations/{locationsId}/buckets/{bucketsId}/views
/// Lists views on a log bucket.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_folders_locations_buckets_views_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_folders_locations_buckets_views_list_task()`.
/// For the simplest API, use `logging_folders_locations_buckets_views_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_folders_locations_buckets_views_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_folders_locations_buckets_views_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListViewsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = logging_folders_locations_buckets_views_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_folders_locations_buckets_views_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingFoldersLocationsBucketsViewsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v2/folders/{foldersId}/locations/{locationsId}/buckets/{bucketsId}/views
/// Lists views on a log bucket.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_folders_locations_buckets_views_list_builder()` + `logging_folders_locations_buckets_views_list_execute()`.
/// For task-level control, use `logging_folders_locations_buckets_views_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_folders_locations_buckets_views_list(
    client: &SimpleHttpClient,
    args: &LoggingFoldersLocationsBucketsViewsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListViewsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = logging_folders_locations_buckets_views_list_builder(
        client,
        &args.parent,
        &args.pageSize,
        &args.pageToken,
    )?;
    logging_folders_locations_buckets_views_list_execute(builder)
}

/// PATCH v2/folders/{foldersId}/locations/{locationsId}/buckets/{bucketsId}/views/{viewsId}
/// Updates a view on a log bucket. This method replaces the value of the filter field from the existing view with the corresponding value from the new view. If an UNAVAILABLE error is returned, this indicates that system is not in a state where it can update the view. If this occurs, please try again in a few minutes.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_folders_locations_buckets_views_patch_execute()` to send, or `logging_folders_locations_buckets_views_patch` for simplest API.

pub fn logging_folders_locations_buckets_views_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    updateMask: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/folders/{}/locations/{locationsId}/buckets/{bucketsId}/views/{viewsId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v2/folders/{foldersId}/locations/{locationsId}/buckets/{bucketsId}/views/{viewsId}
/// Updates a view on a log bucket. This method replaces the value of the filter field from the existing view with the corresponding value from the new view. If an UNAVAILABLE error is returned, this indicates that system is not in a state where it can update the view. If this occurs, please try again in a few minutes.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_folders_locations_buckets_views_patch_execute()` or `logging_folders_locations_buckets_views_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_folders_locations_buckets_views_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_folders_locations_buckets_views_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<LogView>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LogView = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v2/folders/{foldersId}/locations/{locationsId}/buckets/{bucketsId}/views/{viewsId}
/// Updates a view on a log bucket. This method replaces the value of the filter field from the existing view with the corresponding value from the new view. If an UNAVAILABLE error is returned, this indicates that system is not in a state where it can update the view. If this occurs, please try again in a few minutes.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_folders_locations_buckets_views_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_folders_locations_buckets_views_patch_task()`.
/// For the simplest API, use `logging_folders_locations_buckets_views_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_folders_locations_buckets_views_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_folders_locations_buckets_views_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogView>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = logging_folders_locations_buckets_views_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_folders_locations_buckets_views_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingFoldersLocationsBucketsViewsPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
}

/// PATCH v2/folders/{foldersId}/locations/{locationsId}/buckets/{bucketsId}/views/{viewsId}
/// Updates a view on a log bucket. This method replaces the value of the filter field from the existing view with the corresponding value from the new view. If an UNAVAILABLE error is returned, this indicates that system is not in a state where it can update the view. If this occurs, please try again in a few minutes.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_folders_locations_buckets_views_patch_builder()` + `logging_folders_locations_buckets_views_patch_execute()`.
/// For task-level control, use `logging_folders_locations_buckets_views_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_folders_locations_buckets_views_patch(
    client: &SimpleHttpClient,
    args: &LoggingFoldersLocationsBucketsViewsPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogView>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_folders_locations_buckets_views_patch_builder(
        client,
        &args.name,
        &args.updateMask,
    )?;
    logging_folders_locations_buckets_views_patch_execute(builder)
}

/// POST v2/folders/{foldersId}/locations/{locationsId}/buckets/{bucketsId}/views/{viewsId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_folders_locations_buckets_views_set_iam_policy_execute()` to send, or `logging_folders_locations_buckets_views_set_iam_policy` for simplest API.

pub fn logging_folders_locations_buckets_views_set_iam_policy_builder<R>(
    client: &SimpleHttpClient<R>,
    resource: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/folders/{}/locations/{locationsId}/buckets/{bucketsId}/views/{viewsId}:setIamPolicy",
        resource,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v2/folders/{foldersId}/locations/{locationsId}/buckets/{bucketsId}/views/{viewsId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_folders_locations_buckets_views_set_iam_policy_execute()` or `logging_folders_locations_buckets_views_set_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_folders_locations_buckets_views_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_folders_locations_buckets_views_set_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v2/folders/{foldersId}/locations/{locationsId}/buckets/{bucketsId}/views/{viewsId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_folders_locations_buckets_views_set_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_folders_locations_buckets_views_set_iam_policy_task()`.
/// For the simplest API, use `logging_folders_locations_buckets_views_set_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_folders_locations_buckets_views_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_folders_locations_buckets_views_set_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = logging_folders_locations_buckets_views_set_iam_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_folders_locations_buckets_views_set_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingFoldersLocationsBucketsViewsSetIamPolicyArgs {
    /// Path parameter: resource
    pub resource: String,
}

/// POST v2/folders/{foldersId}/locations/{locationsId}/buckets/{bucketsId}/views/{viewsId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_folders_locations_buckets_views_set_iam_policy_builder()` + `logging_folders_locations_buckets_views_set_iam_policy_execute()`.
/// For task-level control, use `logging_folders_locations_buckets_views_set_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_folders_locations_buckets_views_set_iam_policy(
    client: &SimpleHttpClient,
    args: &LoggingFoldersLocationsBucketsViewsSetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        logging_folders_locations_buckets_views_set_iam_policy_builder(client, &args.resource)?;
    logging_folders_locations_buckets_views_set_iam_policy_execute(builder)
}

/// POST v2/folders/{foldersId}/locations/{locationsId}/buckets/{bucketsId}/views/{viewsId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error.Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_folders_locations_buckets_views_test_iam_permissions_execute()` to send, or `logging_folders_locations_buckets_views_test_iam_permissions` for simplest API.

pub fn logging_folders_locations_buckets_views_test_iam_permissions_builder<R>(
    client: &SimpleHttpClient<R>,
    resource: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/folders/{}/locations/{locationsId}/buckets/{bucketsId}/views/{viewsId}:testIamPermissions",
        resource,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v2/folders/{foldersId}/locations/{locationsId}/buckets/{bucketsId}/views/{viewsId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error.Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_folders_locations_buckets_views_test_iam_permissions_execute()` or `logging_folders_locations_buckets_views_test_iam_permissions`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_folders_locations_buckets_views_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_folders_locations_buckets_views_test_iam_permissions_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TestIamPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v2/folders/{foldersId}/locations/{locationsId}/buckets/{bucketsId}/views/{viewsId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error.Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_folders_locations_buckets_views_test_iam_permissions_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_folders_locations_buckets_views_test_iam_permissions_task()`.
/// For the simplest API, use `logging_folders_locations_buckets_views_test_iam_permissions()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_folders_locations_buckets_views_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_folders_locations_buckets_views_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = logging_folders_locations_buckets_views_test_iam_permissions_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_folders_locations_buckets_views_test_iam_permissions`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingFoldersLocationsBucketsViewsTestIamPermissionsArgs {
    /// Path parameter: resource
    pub resource: String,
}

/// POST v2/folders/{foldersId}/locations/{locationsId}/buckets/{bucketsId}/views/{viewsId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error.Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_folders_locations_buckets_views_test_iam_permissions_builder()` + `logging_folders_locations_buckets_views_test_iam_permissions_execute()`.
/// For task-level control, use `logging_folders_locations_buckets_views_test_iam_permissions_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_folders_locations_buckets_views_test_iam_permissions(
    client: &SimpleHttpClient,
    args: &LoggingFoldersLocationsBucketsViewsTestIamPermissionsArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = logging_folders_locations_buckets_views_test_iam_permissions_builder(
        client,
        &args.resource,
    )?;
    logging_folders_locations_buckets_views_test_iam_permissions_execute(builder)
}

/// GET v2/folders/{foldersId}/locations/{locationsId}/buckets/{bucketsId}/views/{viewsId}/logs
/// Lists the logs in projects, organizations, folders, or billing accounts. Only logs that have entries are listed.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_folders_locations_buckets_views_logs_list_execute()` to send, or `logging_folders_locations_buckets_views_logs_list` for simplest API.

pub fn logging_folders_locations_buckets_views_logs_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
    resourceNames: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/folders/{}/locations/{locationsId}/buckets/{bucketsId}/views/{viewsId}/logs",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = resourceNames.as_ref() {
        query_parts.push(format!("resourceNames={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/folders/{foldersId}/locations/{locationsId}/buckets/{bucketsId}/views/{viewsId}/logs
/// Lists the logs in projects, organizations, folders, or billing accounts. Only logs that have entries are listed.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_folders_locations_buckets_views_logs_list_execute()` or `logging_folders_locations_buckets_views_logs_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_folders_locations_buckets_views_logs_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_folders_locations_buckets_views_logs_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListLogsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListLogsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/folders/{foldersId}/locations/{locationsId}/buckets/{bucketsId}/views/{viewsId}/logs
/// Lists the logs in projects, organizations, folders, or billing accounts. Only logs that have entries are listed.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_folders_locations_buckets_views_logs_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_folders_locations_buckets_views_logs_list_task()`.
/// For the simplest API, use `logging_folders_locations_buckets_views_logs_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_folders_locations_buckets_views_logs_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_folders_locations_buckets_views_logs_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListLogsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = logging_folders_locations_buckets_views_logs_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_folders_locations_buckets_views_logs_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingFoldersLocationsBucketsViewsLogsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: resourceNames
    pub resourceNames: Option<String>,
}

/// GET v2/folders/{foldersId}/locations/{locationsId}/buckets/{bucketsId}/views/{viewsId}/logs
/// Lists the logs in projects, organizations, folders, or billing accounts. Only logs that have entries are listed.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_folders_locations_buckets_views_logs_list_builder()` + `logging_folders_locations_buckets_views_logs_list_execute()`.
/// For task-level control, use `logging_folders_locations_buckets_views_logs_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_folders_locations_buckets_views_logs_list(
    client: &SimpleHttpClient,
    args: &LoggingFoldersLocationsBucketsViewsLogsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListLogsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = logging_folders_locations_buckets_views_logs_list_builder(
        client,
        &args.parent,
        &args.pageSize,
        &args.pageToken,
        &args.resourceNames,
    )?;
    logging_folders_locations_buckets_views_logs_list_execute(builder)
}

/// POST v2/folders/{foldersId}/locations/{locationsId}/logScopes
/// Creates a log scope.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_folders_locations_log_scopes_create_execute()` to send, or `logging_folders_locations_log_scopes_create` for simplest API.

pub fn logging_folders_locations_log_scopes_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    logScopeId: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/folders/{}/locations/{locationsId}/logScopes",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = logScopeId.as_ref() {
        query_parts.push(format!("logScopeId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v2/folders/{foldersId}/locations/{locationsId}/logScopes
/// Creates a log scope.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_folders_locations_log_scopes_create_execute()` or `logging_folders_locations_log_scopes_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_folders_locations_log_scopes_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_folders_locations_log_scopes_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<LogScope>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LogScope = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v2/folders/{foldersId}/locations/{locationsId}/logScopes
/// Creates a log scope.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_folders_locations_log_scopes_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_folders_locations_log_scopes_create_task()`.
/// For the simplest API, use `logging_folders_locations_log_scopes_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_folders_locations_log_scopes_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_folders_locations_log_scopes_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogScope>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = logging_folders_locations_log_scopes_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_folders_locations_log_scopes_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingFoldersLocationsLogScopesCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: logScopeId
    pub logScopeId: Option<String>,
}

/// POST v2/folders/{foldersId}/locations/{locationsId}/logScopes
/// Creates a log scope.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_folders_locations_log_scopes_create_builder()` + `logging_folders_locations_log_scopes_create_execute()`.
/// For task-level control, use `logging_folders_locations_log_scopes_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_folders_locations_log_scopes_create(
    client: &SimpleHttpClient,
    args: &LoggingFoldersLocationsLogScopesCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogScope>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_folders_locations_log_scopes_create_builder(
        client,
        &args.parent,
        &args.logScopeId,
    )?;
    logging_folders_locations_log_scopes_create_execute(builder)
}

/// DELETE v2/folders/{foldersId}/locations/{locationsId}/logScopes/{logScopesId}
/// Deletes a log scope.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_folders_locations_log_scopes_delete_execute()` to send, or `logging_folders_locations_log_scopes_delete` for simplest API.

pub fn logging_folders_locations_log_scopes_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/folders/{}/locations/{locationsId}/logScopes/{logScopesId}",
        name,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v2/folders/{foldersId}/locations/{locationsId}/logScopes/{logScopesId}
/// Deletes a log scope.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_folders_locations_log_scopes_delete_execute()` or `logging_folders_locations_log_scopes_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_folders_locations_log_scopes_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_folders_locations_log_scopes_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v2/folders/{foldersId}/locations/{locationsId}/logScopes/{logScopesId}
/// Deletes a log scope.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_folders_locations_log_scopes_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_folders_locations_log_scopes_delete_task()`.
/// For the simplest API, use `logging_folders_locations_log_scopes_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_folders_locations_log_scopes_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_folders_locations_log_scopes_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = logging_folders_locations_log_scopes_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_folders_locations_log_scopes_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingFoldersLocationsLogScopesDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// DELETE v2/folders/{foldersId}/locations/{locationsId}/logScopes/{logScopesId}
/// Deletes a log scope.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_folders_locations_log_scopes_delete_builder()` + `logging_folders_locations_log_scopes_delete_execute()`.
/// For task-level control, use `logging_folders_locations_log_scopes_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_folders_locations_log_scopes_delete(
    client: &SimpleHttpClient,
    args: &LoggingFoldersLocationsLogScopesDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_folders_locations_log_scopes_delete_builder(client, &args.name)?;
    logging_folders_locations_log_scopes_delete_execute(builder)
}

/// GET v2/folders/{foldersId}/locations/{locationsId}/logScopes/{logScopesId}
/// Gets a log scope.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_folders_locations_log_scopes_get_execute()` to send, or `logging_folders_locations_log_scopes_get` for simplest API.

pub fn logging_folders_locations_log_scopes_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/folders/{}/locations/{locationsId}/logScopes/{logScopesId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/folders/{foldersId}/locations/{locationsId}/logScopes/{logScopesId}
/// Gets a log scope.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_folders_locations_log_scopes_get_execute()` or `logging_folders_locations_log_scopes_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_folders_locations_log_scopes_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_folders_locations_log_scopes_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<LogScope>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LogScope = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/folders/{foldersId}/locations/{locationsId}/logScopes/{logScopesId}
/// Gets a log scope.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_folders_locations_log_scopes_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_folders_locations_log_scopes_get_task()`.
/// For the simplest API, use `logging_folders_locations_log_scopes_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_folders_locations_log_scopes_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_folders_locations_log_scopes_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogScope>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = logging_folders_locations_log_scopes_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_folders_locations_log_scopes_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingFoldersLocationsLogScopesGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v2/folders/{foldersId}/locations/{locationsId}/logScopes/{logScopesId}
/// Gets a log scope.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_folders_locations_log_scopes_get_builder()` + `logging_folders_locations_log_scopes_get_execute()`.
/// For task-level control, use `logging_folders_locations_log_scopes_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_folders_locations_log_scopes_get(
    client: &SimpleHttpClient,
    args: &LoggingFoldersLocationsLogScopesGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogScope>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_folders_locations_log_scopes_get_builder(client, &args.name)?;
    logging_folders_locations_log_scopes_get_execute(builder)
}

/// GET v2/folders/{foldersId}/locations/{locationsId}/logScopes
/// Lists log scopes.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_folders_locations_log_scopes_list_execute()` to send, or `logging_folders_locations_log_scopes_list` for simplest API.

pub fn logging_folders_locations_log_scopes_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/folders/{}/locations/{locationsId}/logScopes",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/folders/{foldersId}/locations/{locationsId}/logScopes
/// Lists log scopes.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_folders_locations_log_scopes_list_execute()` or `logging_folders_locations_log_scopes_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_folders_locations_log_scopes_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_folders_locations_log_scopes_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListLogScopesResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListLogScopesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/folders/{foldersId}/locations/{locationsId}/logScopes
/// Lists log scopes.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_folders_locations_log_scopes_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_folders_locations_log_scopes_list_task()`.
/// For the simplest API, use `logging_folders_locations_log_scopes_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_folders_locations_log_scopes_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_folders_locations_log_scopes_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListLogScopesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = logging_folders_locations_log_scopes_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_folders_locations_log_scopes_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingFoldersLocationsLogScopesListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v2/folders/{foldersId}/locations/{locationsId}/logScopes
/// Lists log scopes.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_folders_locations_log_scopes_list_builder()` + `logging_folders_locations_log_scopes_list_execute()`.
/// For task-level control, use `logging_folders_locations_log_scopes_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_folders_locations_log_scopes_list(
    client: &SimpleHttpClient,
    args: &LoggingFoldersLocationsLogScopesListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListLogScopesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = logging_folders_locations_log_scopes_list_builder(
        client,
        &args.parent,
        &args.pageSize,
        &args.pageToken,
    )?;
    logging_folders_locations_log_scopes_list_execute(builder)
}

/// PATCH v2/folders/{foldersId}/locations/{locationsId}/logScopes/{logScopesId}
/// Updates a log scope.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_folders_locations_log_scopes_patch_execute()` to send, or `logging_folders_locations_log_scopes_patch` for simplest API.

pub fn logging_folders_locations_log_scopes_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    updateMask: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/folders/{}/locations/{locationsId}/logScopes/{logScopesId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v2/folders/{foldersId}/locations/{locationsId}/logScopes/{logScopesId}
/// Updates a log scope.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_folders_locations_log_scopes_patch_execute()` or `logging_folders_locations_log_scopes_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_folders_locations_log_scopes_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_folders_locations_log_scopes_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<LogScope>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LogScope = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v2/folders/{foldersId}/locations/{locationsId}/logScopes/{logScopesId}
/// Updates a log scope.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_folders_locations_log_scopes_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_folders_locations_log_scopes_patch_task()`.
/// For the simplest API, use `logging_folders_locations_log_scopes_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_folders_locations_log_scopes_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_folders_locations_log_scopes_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogScope>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = logging_folders_locations_log_scopes_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_folders_locations_log_scopes_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingFoldersLocationsLogScopesPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
}

/// PATCH v2/folders/{foldersId}/locations/{locationsId}/logScopes/{logScopesId}
/// Updates a log scope.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_folders_locations_log_scopes_patch_builder()` + `logging_folders_locations_log_scopes_patch_execute()`.
/// For task-level control, use `logging_folders_locations_log_scopes_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_folders_locations_log_scopes_patch(
    client: &SimpleHttpClient,
    args: &LoggingFoldersLocationsLogScopesPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogScope>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        logging_folders_locations_log_scopes_patch_builder(client, &args.name, &args.updateMask)?;
    logging_folders_locations_log_scopes_patch_execute(builder)
}

/// POST v2/folders/{foldersId}/locations/{locationsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn''t support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_folders_locations_operations_cancel_execute()` to send, or `logging_folders_locations_operations_cancel` for simplest API.

pub fn logging_folders_locations_operations_cancel_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/folders/{}/locations/{locationsId}/operations/{operationsId}:cancel",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v2/folders/{foldersId}/locations/{locationsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn''t support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_folders_locations_operations_cancel_execute()` or `logging_folders_locations_operations_cancel`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_folders_locations_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_folders_locations_operations_cancel_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v2/folders/{foldersId}/locations/{locationsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn''t support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_folders_locations_operations_cancel_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_folders_locations_operations_cancel_task()`.
/// For the simplest API, use `logging_folders_locations_operations_cancel()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_folders_locations_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_folders_locations_operations_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = logging_folders_locations_operations_cancel_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_folders_locations_operations_cancel`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingFoldersLocationsOperationsCancelArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v2/folders/{foldersId}/locations/{locationsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn''t support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_folders_locations_operations_cancel_builder()` + `logging_folders_locations_operations_cancel_execute()`.
/// For task-level control, use `logging_folders_locations_operations_cancel_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_folders_locations_operations_cancel(
    client: &SimpleHttpClient,
    args: &LoggingFoldersLocationsOperationsCancelArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_folders_locations_operations_cancel_builder(client, &args.name)?;
    logging_folders_locations_operations_cancel_execute(builder)
}

/// GET v2/folders/{foldersId}/locations/{locationsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_folders_locations_operations_get_execute()` to send, or `logging_folders_locations_operations_get` for simplest API.

pub fn logging_folders_locations_operations_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/folders/{}/locations/{locationsId}/operations/{operationsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/folders/{foldersId}/locations/{locationsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_folders_locations_operations_get_execute()` or `logging_folders_locations_operations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_folders_locations_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_folders_locations_operations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/folders/{foldersId}/locations/{locationsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_folders_locations_operations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_folders_locations_operations_get_task()`.
/// For the simplest API, use `logging_folders_locations_operations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_folders_locations_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_folders_locations_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = logging_folders_locations_operations_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_folders_locations_operations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingFoldersLocationsOperationsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v2/folders/{foldersId}/locations/{locationsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_folders_locations_operations_get_builder()` + `logging_folders_locations_operations_get_execute()`.
/// For task-level control, use `logging_folders_locations_operations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_folders_locations_operations_get(
    client: &SimpleHttpClient,
    args: &LoggingFoldersLocationsOperationsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_folders_locations_operations_get_builder(client, &args.name)?;
    logging_folders_locations_operations_get_execute(builder)
}

/// GET v2/folders/{foldersId}/locations/{locationsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn''t support this method, it returns UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_folders_locations_operations_list_execute()` to send, or `logging_folders_locations_operations_list` for simplest API.

pub fn logging_folders_locations_operations_list_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    filter: &Option<String>,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
    returnPartialSuccess: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/folders/{}/locations/{locationsId}/operations",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/folders/{foldersId}/locations/{locationsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn''t support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_folders_locations_operations_list_execute()` or `logging_folders_locations_operations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_folders_locations_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_folders_locations_operations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListOperationsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListOperationsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/folders/{foldersId}/locations/{locationsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn''t support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_folders_locations_operations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_folders_locations_operations_list_task()`.
/// For the simplest API, use `logging_folders_locations_operations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_folders_locations_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_folders_locations_operations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListOperationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = logging_folders_locations_operations_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_folders_locations_operations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingFoldersLocationsOperationsListArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<String>,
}

/// GET v2/folders/{foldersId}/locations/{locationsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn''t support this method, it returns UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_folders_locations_operations_list_builder()` + `logging_folders_locations_operations_list_execute()`.
/// For task-level control, use `logging_folders_locations_operations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_folders_locations_operations_list(
    client: &SimpleHttpClient,
    args: &LoggingFoldersLocationsOperationsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListOperationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = logging_folders_locations_operations_list_builder(
        client,
        &args.name,
        &args.filter,
        &args.pageSize,
        &args.pageToken,
        &args.returnPartialSuccess,
    )?;
    logging_folders_locations_operations_list_execute(builder)
}

/// GET v2/folders/{foldersId}/locations/{locationsId}/recentQueries
/// Lists the RecentQueries that were created by the user making the request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_folders_locations_recent_queries_list_execute()` to send, or `logging_folders_locations_recent_queries_list` for simplest API.

pub fn logging_folders_locations_recent_queries_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    filter: &Option<String>,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/folders/{}/locations/{locationsId}/recentQueries",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/folders/{foldersId}/locations/{locationsId}/recentQueries
/// Lists the RecentQueries that were created by the user making the request.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_folders_locations_recent_queries_list_execute()` or `logging_folders_locations_recent_queries_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_folders_locations_recent_queries_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_folders_locations_recent_queries_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListRecentQueriesResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListRecentQueriesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/folders/{foldersId}/locations/{locationsId}/recentQueries
/// Lists the RecentQueries that were created by the user making the request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_folders_locations_recent_queries_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_folders_locations_recent_queries_list_task()`.
/// For the simplest API, use `logging_folders_locations_recent_queries_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_folders_locations_recent_queries_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_folders_locations_recent_queries_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListRecentQueriesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = logging_folders_locations_recent_queries_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_folders_locations_recent_queries_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingFoldersLocationsRecentQueriesListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v2/folders/{foldersId}/locations/{locationsId}/recentQueries
/// Lists the RecentQueries that were created by the user making the request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_folders_locations_recent_queries_list_builder()` + `logging_folders_locations_recent_queries_list_execute()`.
/// For task-level control, use `logging_folders_locations_recent_queries_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_folders_locations_recent_queries_list(
    client: &SimpleHttpClient,
    args: &LoggingFoldersLocationsRecentQueriesListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListRecentQueriesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = logging_folders_locations_recent_queries_list_builder(
        client,
        &args.parent,
        &args.filter,
        &args.pageSize,
        &args.pageToken,
    )?;
    logging_folders_locations_recent_queries_list_execute(builder)
}

/// POST v2/folders/{foldersId}/locations/{locationsId}/savedQueries
/// Creates a new SavedQuery for the user making the request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_folders_locations_saved_queries_create_execute()` to send, or `logging_folders_locations_saved_queries_create` for simplest API.

pub fn logging_folders_locations_saved_queries_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    savedQueryId: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/folders/{}/locations/{locationsId}/savedQueries",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = savedQueryId.as_ref() {
        query_parts.push(format!("savedQueryId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v2/folders/{foldersId}/locations/{locationsId}/savedQueries
/// Creates a new SavedQuery for the user making the request.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_folders_locations_saved_queries_create_execute()` or `logging_folders_locations_saved_queries_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_folders_locations_saved_queries_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_folders_locations_saved_queries_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SavedQuery>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SavedQuery = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v2/folders/{foldersId}/locations/{locationsId}/savedQueries
/// Creates a new SavedQuery for the user making the request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_folders_locations_saved_queries_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_folders_locations_saved_queries_create_task()`.
/// For the simplest API, use `logging_folders_locations_saved_queries_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_folders_locations_saved_queries_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_folders_locations_saved_queries_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SavedQuery>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = logging_folders_locations_saved_queries_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_folders_locations_saved_queries_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingFoldersLocationsSavedQueriesCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: savedQueryId
    pub savedQueryId: Option<String>,
}

/// POST v2/folders/{foldersId}/locations/{locationsId}/savedQueries
/// Creates a new SavedQuery for the user making the request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_folders_locations_saved_queries_create_builder()` + `logging_folders_locations_saved_queries_create_execute()`.
/// For task-level control, use `logging_folders_locations_saved_queries_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_folders_locations_saved_queries_create(
    client: &SimpleHttpClient,
    args: &LoggingFoldersLocationsSavedQueriesCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SavedQuery>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_folders_locations_saved_queries_create_builder(
        client,
        &args.parent,
        &args.savedQueryId,
    )?;
    logging_folders_locations_saved_queries_create_execute(builder)
}

/// DELETE v2/folders/{foldersId}/locations/{locationsId}/savedQueries/{savedQueriesId}
/// Deletes an existing SavedQuery that was created by the user making the request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_folders_locations_saved_queries_delete_execute()` to send, or `logging_folders_locations_saved_queries_delete` for simplest API.

pub fn logging_folders_locations_saved_queries_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/folders/{}/locations/{locationsId}/savedQueries/{savedQueriesId}",
        name,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v2/folders/{foldersId}/locations/{locationsId}/savedQueries/{savedQueriesId}
/// Deletes an existing SavedQuery that was created by the user making the request.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_folders_locations_saved_queries_delete_execute()` or `logging_folders_locations_saved_queries_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_folders_locations_saved_queries_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_folders_locations_saved_queries_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v2/folders/{foldersId}/locations/{locationsId}/savedQueries/{savedQueriesId}
/// Deletes an existing SavedQuery that was created by the user making the request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_folders_locations_saved_queries_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_folders_locations_saved_queries_delete_task()`.
/// For the simplest API, use `logging_folders_locations_saved_queries_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_folders_locations_saved_queries_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_folders_locations_saved_queries_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = logging_folders_locations_saved_queries_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_folders_locations_saved_queries_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingFoldersLocationsSavedQueriesDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// DELETE v2/folders/{foldersId}/locations/{locationsId}/savedQueries/{savedQueriesId}
/// Deletes an existing SavedQuery that was created by the user making the request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_folders_locations_saved_queries_delete_builder()` + `logging_folders_locations_saved_queries_delete_execute()`.
/// For task-level control, use `logging_folders_locations_saved_queries_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_folders_locations_saved_queries_delete(
    client: &SimpleHttpClient,
    args: &LoggingFoldersLocationsSavedQueriesDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_folders_locations_saved_queries_delete_builder(client, &args.name)?;
    logging_folders_locations_saved_queries_delete_execute(builder)
}

/// GET v2/folders/{foldersId}/locations/{locationsId}/savedQueries/{savedQueriesId}
/// Returns all data associated with the requested query.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_folders_locations_saved_queries_get_execute()` to send, or `logging_folders_locations_saved_queries_get` for simplest API.

pub fn logging_folders_locations_saved_queries_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/folders/{}/locations/{locationsId}/savedQueries/{savedQueriesId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/folders/{foldersId}/locations/{locationsId}/savedQueries/{savedQueriesId}
/// Returns all data associated with the requested query.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_folders_locations_saved_queries_get_execute()` or `logging_folders_locations_saved_queries_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_folders_locations_saved_queries_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_folders_locations_saved_queries_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SavedQuery>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SavedQuery = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/folders/{foldersId}/locations/{locationsId}/savedQueries/{savedQueriesId}
/// Returns all data associated with the requested query.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_folders_locations_saved_queries_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_folders_locations_saved_queries_get_task()`.
/// For the simplest API, use `logging_folders_locations_saved_queries_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_folders_locations_saved_queries_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_folders_locations_saved_queries_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SavedQuery>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = logging_folders_locations_saved_queries_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_folders_locations_saved_queries_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingFoldersLocationsSavedQueriesGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v2/folders/{foldersId}/locations/{locationsId}/savedQueries/{savedQueriesId}
/// Returns all data associated with the requested query.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_folders_locations_saved_queries_get_builder()` + `logging_folders_locations_saved_queries_get_execute()`.
/// For task-level control, use `logging_folders_locations_saved_queries_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_folders_locations_saved_queries_get(
    client: &SimpleHttpClient,
    args: &LoggingFoldersLocationsSavedQueriesGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SavedQuery>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_folders_locations_saved_queries_get_builder(client, &args.name)?;
    logging_folders_locations_saved_queries_get_execute(builder)
}

/// GET v2/folders/{foldersId}/locations/{locationsId}/savedQueries
/// Lists the SavedQueries that were created by the user making the request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_folders_locations_saved_queries_list_execute()` to send, or `logging_folders_locations_saved_queries_list` for simplest API.

pub fn logging_folders_locations_saved_queries_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    filter: &Option<String>,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/folders/{}/locations/{locationsId}/savedQueries",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/folders/{foldersId}/locations/{locationsId}/savedQueries
/// Lists the SavedQueries that were created by the user making the request.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_folders_locations_saved_queries_list_execute()` or `logging_folders_locations_saved_queries_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_folders_locations_saved_queries_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_folders_locations_saved_queries_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListSavedQueriesResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListSavedQueriesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/folders/{foldersId}/locations/{locationsId}/savedQueries
/// Lists the SavedQueries that were created by the user making the request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_folders_locations_saved_queries_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_folders_locations_saved_queries_list_task()`.
/// For the simplest API, use `logging_folders_locations_saved_queries_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_folders_locations_saved_queries_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_folders_locations_saved_queries_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListSavedQueriesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = logging_folders_locations_saved_queries_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_folders_locations_saved_queries_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingFoldersLocationsSavedQueriesListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v2/folders/{foldersId}/locations/{locationsId}/savedQueries
/// Lists the SavedQueries that were created by the user making the request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_folders_locations_saved_queries_list_builder()` + `logging_folders_locations_saved_queries_list_execute()`.
/// For task-level control, use `logging_folders_locations_saved_queries_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_folders_locations_saved_queries_list(
    client: &SimpleHttpClient,
    args: &LoggingFoldersLocationsSavedQueriesListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListSavedQueriesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = logging_folders_locations_saved_queries_list_builder(
        client,
        &args.parent,
        &args.filter,
        &args.pageSize,
        &args.pageToken,
    )?;
    logging_folders_locations_saved_queries_list_execute(builder)
}

/// PATCH v2/folders/{foldersId}/locations/{locationsId}/savedQueries/{savedQueriesId}
/// Updates an existing SavedQuery.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_folders_locations_saved_queries_patch_execute()` to send, or `logging_folders_locations_saved_queries_patch` for simplest API.

pub fn logging_folders_locations_saved_queries_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    updateMask: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/folders/{}/locations/{locationsId}/savedQueries/{savedQueriesId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v2/folders/{foldersId}/locations/{locationsId}/savedQueries/{savedQueriesId}
/// Updates an existing SavedQuery.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_folders_locations_saved_queries_patch_execute()` or `logging_folders_locations_saved_queries_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_folders_locations_saved_queries_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_folders_locations_saved_queries_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SavedQuery>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SavedQuery = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v2/folders/{foldersId}/locations/{locationsId}/savedQueries/{savedQueriesId}
/// Updates an existing SavedQuery.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_folders_locations_saved_queries_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_folders_locations_saved_queries_patch_task()`.
/// For the simplest API, use `logging_folders_locations_saved_queries_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_folders_locations_saved_queries_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_folders_locations_saved_queries_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SavedQuery>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = logging_folders_locations_saved_queries_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_folders_locations_saved_queries_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingFoldersLocationsSavedQueriesPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
}

/// PATCH v2/folders/{foldersId}/locations/{locationsId}/savedQueries/{savedQueriesId}
/// Updates an existing SavedQuery.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_folders_locations_saved_queries_patch_builder()` + `logging_folders_locations_saved_queries_patch_execute()`.
/// For task-level control, use `logging_folders_locations_saved_queries_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_folders_locations_saved_queries_patch(
    client: &SimpleHttpClient,
    args: &LoggingFoldersLocationsSavedQueriesPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SavedQuery>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_folders_locations_saved_queries_patch_builder(
        client,
        &args.name,
        &args.updateMask,
    )?;
    logging_folders_locations_saved_queries_patch_execute(builder)
}

/// DELETE v2/folders/{foldersId}/logs/{logsId}
/// Deletes all the log entries in a log for the global _Default Log Bucket. The log reappears if it receives new entries. Log entries written shortly before the delete operation might not be deleted. Entries received after the delete operation with a timestamp before the operation will be deleted.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_folders_logs_delete_execute()` to send, or `logging_folders_logs_delete` for simplest API.

pub fn logging_folders_logs_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    logName: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/folders/{}/logs/{logsId}",
        logName,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v2/folders/{foldersId}/logs/{logsId}
/// Deletes all the log entries in a log for the global _Default Log Bucket. The log reappears if it receives new entries. Log entries written shortly before the delete operation might not be deleted. Entries received after the delete operation with a timestamp before the operation will be deleted.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_folders_logs_delete_execute()` or `logging_folders_logs_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_folders_logs_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_folders_logs_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v2/folders/{foldersId}/logs/{logsId}
/// Deletes all the log entries in a log for the global _Default Log Bucket. The log reappears if it receives new entries. Log entries written shortly before the delete operation might not be deleted. Entries received after the delete operation with a timestamp before the operation will be deleted.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_folders_logs_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_folders_logs_delete_task()`.
/// For the simplest API, use `logging_folders_logs_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_folders_logs_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_folders_logs_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = logging_folders_logs_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_folders_logs_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingFoldersLogsDeleteArgs {
    /// Path parameter: logName
    pub logName: String,
}

/// DELETE v2/folders/{foldersId}/logs/{logsId}
/// Deletes all the log entries in a log for the global _Default Log Bucket. The log reappears if it receives new entries. Log entries written shortly before the delete operation might not be deleted. Entries received after the delete operation with a timestamp before the operation will be deleted.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_folders_logs_delete_builder()` + `logging_folders_logs_delete_execute()`.
/// For task-level control, use `logging_folders_logs_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_folders_logs_delete(
    client: &SimpleHttpClient,
    args: &LoggingFoldersLogsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_folders_logs_delete_builder(client, &args.logName)?;
    logging_folders_logs_delete_execute(builder)
}

/// GET v2/folders/{foldersId}/logs
/// Lists the logs in projects, organizations, folders, or billing accounts. Only logs that have entries are listed.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_folders_logs_list_execute()` to send, or `logging_folders_logs_list` for simplest API.

pub fn logging_folders_logs_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
    resourceNames: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!("https://logging.googleapis.com/v2/folders/{}/logs", parent,);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = resourceNames.as_ref() {
        query_parts.push(format!("resourceNames={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/folders/{foldersId}/logs
/// Lists the logs in projects, organizations, folders, or billing accounts. Only logs that have entries are listed.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_folders_logs_list_execute()` or `logging_folders_logs_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_folders_logs_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_folders_logs_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListLogsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListLogsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/folders/{foldersId}/logs
/// Lists the logs in projects, organizations, folders, or billing accounts. Only logs that have entries are listed.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_folders_logs_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_folders_logs_list_task()`.
/// For the simplest API, use `logging_folders_logs_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_folders_logs_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_folders_logs_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListLogsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = logging_folders_logs_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_folders_logs_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingFoldersLogsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: resourceNames
    pub resourceNames: Option<String>,
}

/// GET v2/folders/{foldersId}/logs
/// Lists the logs in projects, organizations, folders, or billing accounts. Only logs that have entries are listed.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_folders_logs_list_builder()` + `logging_folders_logs_list_execute()`.
/// For task-level control, use `logging_folders_logs_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_folders_logs_list(
    client: &SimpleHttpClient,
    args: &LoggingFoldersLogsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListLogsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = logging_folders_logs_list_builder(
        client,
        &args.parent,
        &args.pageSize,
        &args.pageToken,
        &args.resourceNames,
    )?;
    logging_folders_logs_list_execute(builder)
}

/// POST v2/folders/{foldersId}/sinks
/// Creates a sink that exports specified log entries to a destination. The export begins upon ingress, unless the sink''s writer_identity is not permitted to write to the destination. A sink can export log entries only from the resource owning the sink.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_folders_sinks_create_execute()` to send, or `logging_folders_sinks_create` for simplest API.

pub fn logging_folders_sinks_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    customWriterIdentity: &Option<String>,
    uniqueWriterIdentity: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!("https://logging.googleapis.com/v2/folders/{}/sinks", parent,);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = customWriterIdentity.as_ref() {
        query_parts.push(format!("customWriterIdentity={}", val));
    }
    if let Some(val) = uniqueWriterIdentity.as_ref() {
        query_parts.push(format!("uniqueWriterIdentity={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v2/folders/{foldersId}/sinks
/// Creates a sink that exports specified log entries to a destination. The export begins upon ingress, unless the sink''s writer_identity is not permitted to write to the destination. A sink can export log entries only from the resource owning the sink.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_folders_sinks_create_execute()` or `logging_folders_sinks_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_folders_sinks_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_folders_sinks_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<LogSink>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LogSink = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v2/folders/{foldersId}/sinks
/// Creates a sink that exports specified log entries to a destination. The export begins upon ingress, unless the sink''s writer_identity is not permitted to write to the destination. A sink can export log entries only from the resource owning the sink.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_folders_sinks_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_folders_sinks_create_task()`.
/// For the simplest API, use `logging_folders_sinks_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_folders_sinks_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_folders_sinks_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogSink>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = logging_folders_sinks_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_folders_sinks_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingFoldersSinksCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: customWriterIdentity
    pub customWriterIdentity: Option<String>,
    /// Query parameter: uniqueWriterIdentity
    pub uniqueWriterIdentity: Option<String>,
}

/// POST v2/folders/{foldersId}/sinks
/// Creates a sink that exports specified log entries to a destination. The export begins upon ingress, unless the sink''s writer_identity is not permitted to write to the destination. A sink can export log entries only from the resource owning the sink.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_folders_sinks_create_builder()` + `logging_folders_sinks_create_execute()`.
/// For task-level control, use `logging_folders_sinks_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_folders_sinks_create(
    client: &SimpleHttpClient,
    args: &LoggingFoldersSinksCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogSink>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_folders_sinks_create_builder(
        client,
        &args.parent,
        &args.customWriterIdentity,
        &args.uniqueWriterIdentity,
    )?;
    logging_folders_sinks_create_execute(builder)
}

/// DELETE v2/folders/{foldersId}/sinks/{sinksId}
/// Deletes a sink. If the sink has a unique writer_identity, then that service account is also deleted.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_folders_sinks_delete_execute()` to send, or `logging_folders_sinks_delete` for simplest API.

pub fn logging_folders_sinks_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    sinkName: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/folders/{}/sinks/{sinksId}",
        sinkName,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v2/folders/{foldersId}/sinks/{sinksId}
/// Deletes a sink. If the sink has a unique writer_identity, then that service account is also deleted.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_folders_sinks_delete_execute()` or `logging_folders_sinks_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_folders_sinks_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_folders_sinks_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v2/folders/{foldersId}/sinks/{sinksId}
/// Deletes a sink. If the sink has a unique writer_identity, then that service account is also deleted.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_folders_sinks_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_folders_sinks_delete_task()`.
/// For the simplest API, use `logging_folders_sinks_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_folders_sinks_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_folders_sinks_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = logging_folders_sinks_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_folders_sinks_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingFoldersSinksDeleteArgs {
    /// Path parameter: sinkName
    pub sinkName: String,
}

/// DELETE v2/folders/{foldersId}/sinks/{sinksId}
/// Deletes a sink. If the sink has a unique writer_identity, then that service account is also deleted.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_folders_sinks_delete_builder()` + `logging_folders_sinks_delete_execute()`.
/// For task-level control, use `logging_folders_sinks_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_folders_sinks_delete(
    client: &SimpleHttpClient,
    args: &LoggingFoldersSinksDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_folders_sinks_delete_builder(client, &args.sinkName)?;
    logging_folders_sinks_delete_execute(builder)
}

/// GET v2/folders/{foldersId}/sinks/{sinksId}
/// Gets a sink.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_folders_sinks_get_execute()` to send, or `logging_folders_sinks_get` for simplest API.

pub fn logging_folders_sinks_get_builder<R>(
    client: &SimpleHttpClient<R>,
    sinkName: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/folders/{}/sinks/{sinksId}",
        sinkName,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/folders/{foldersId}/sinks/{sinksId}
/// Gets a sink.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_folders_sinks_get_execute()` or `logging_folders_sinks_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_folders_sinks_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_folders_sinks_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<LogSink>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LogSink = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/folders/{foldersId}/sinks/{sinksId}
/// Gets a sink.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_folders_sinks_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_folders_sinks_get_task()`.
/// For the simplest API, use `logging_folders_sinks_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_folders_sinks_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_folders_sinks_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogSink>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = logging_folders_sinks_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_folders_sinks_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingFoldersSinksGetArgs {
    /// Path parameter: sinkName
    pub sinkName: String,
}

/// GET v2/folders/{foldersId}/sinks/{sinksId}
/// Gets a sink.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_folders_sinks_get_builder()` + `logging_folders_sinks_get_execute()`.
/// For task-level control, use `logging_folders_sinks_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_folders_sinks_get(
    client: &SimpleHttpClient,
    args: &LoggingFoldersSinksGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogSink>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_folders_sinks_get_builder(client, &args.sinkName)?;
    logging_folders_sinks_get_execute(builder)
}

/// GET v2/folders/{foldersId}/sinks
/// Lists sinks.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_folders_sinks_list_execute()` to send, or `logging_folders_sinks_list` for simplest API.

pub fn logging_folders_sinks_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    filter: &Option<String>,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!("https://logging.googleapis.com/v2/folders/{}/sinks", parent,);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/folders/{foldersId}/sinks
/// Lists sinks.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_folders_sinks_list_execute()` or `logging_folders_sinks_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_folders_sinks_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_folders_sinks_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListSinksResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListSinksResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/folders/{foldersId}/sinks
/// Lists sinks.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_folders_sinks_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_folders_sinks_list_task()`.
/// For the simplest API, use `logging_folders_sinks_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_folders_sinks_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_folders_sinks_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListSinksResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = logging_folders_sinks_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_folders_sinks_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingFoldersSinksListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v2/folders/{foldersId}/sinks
/// Lists sinks.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_folders_sinks_list_builder()` + `logging_folders_sinks_list_execute()`.
/// For task-level control, use `logging_folders_sinks_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_folders_sinks_list(
    client: &SimpleHttpClient,
    args: &LoggingFoldersSinksListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListSinksResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = logging_folders_sinks_list_builder(
        client,
        &args.parent,
        &args.filter,
        &args.pageSize,
        &args.pageToken,
    )?;
    logging_folders_sinks_list_execute(builder)
}

/// PATCH v2/folders/{foldersId}/sinks/{sinksId}
/// Updates a sink. This method replaces the values of the destination and filter fields of the existing sink with the corresponding values from the new sink.The updated sink might also have a new writer_identity; see the unique_writer_identity field.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_folders_sinks_patch_execute()` to send, or `logging_folders_sinks_patch` for simplest API.

pub fn logging_folders_sinks_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    sinkName: &String,
    customWriterIdentity: &Option<String>,
    uniqueWriterIdentity: &Option<String>,
    updateMask: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/folders/{}/sinks/{sinksId}",
        sinkName,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = customWriterIdentity.as_ref() {
        query_parts.push(format!("customWriterIdentity={}", val));
    }
    if let Some(val) = uniqueWriterIdentity.as_ref() {
        query_parts.push(format!("uniqueWriterIdentity={}", val));
    }
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v2/folders/{foldersId}/sinks/{sinksId}
/// Updates a sink. This method replaces the values of the destination and filter fields of the existing sink with the corresponding values from the new sink.The updated sink might also have a new writer_identity; see the unique_writer_identity field.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_folders_sinks_patch_execute()` or `logging_folders_sinks_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_folders_sinks_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_folders_sinks_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<LogSink>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LogSink = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v2/folders/{foldersId}/sinks/{sinksId}
/// Updates a sink. This method replaces the values of the destination and filter fields of the existing sink with the corresponding values from the new sink.The updated sink might also have a new writer_identity; see the unique_writer_identity field.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_folders_sinks_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_folders_sinks_patch_task()`.
/// For the simplest API, use `logging_folders_sinks_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_folders_sinks_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_folders_sinks_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogSink>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = logging_folders_sinks_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_folders_sinks_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingFoldersSinksPatchArgs {
    /// Path parameter: sinkName
    pub sinkName: String,
    /// Query parameter: customWriterIdentity
    pub customWriterIdentity: Option<String>,
    /// Query parameter: uniqueWriterIdentity
    pub uniqueWriterIdentity: Option<String>,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
}

/// PATCH v2/folders/{foldersId}/sinks/{sinksId}
/// Updates a sink. This method replaces the values of the destination and filter fields of the existing sink with the corresponding values from the new sink.The updated sink might also have a new writer_identity; see the unique_writer_identity field.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_folders_sinks_patch_builder()` + `logging_folders_sinks_patch_execute()`.
/// For task-level control, use `logging_folders_sinks_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_folders_sinks_patch(
    client: &SimpleHttpClient,
    args: &LoggingFoldersSinksPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogSink>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_folders_sinks_patch_builder(
        client,
        &args.sinkName,
        &args.customWriterIdentity,
        &args.uniqueWriterIdentity,
        &args.updateMask,
    )?;
    logging_folders_sinks_patch_execute(builder)
}

/// PUT v2/folders/{foldersId}/sinks/{sinksId}
/// Updates a sink. This method replaces the values of the destination and filter fields of the existing sink with the corresponding values from the new sink.The updated sink might also have a new writer_identity; see the unique_writer_identity field.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_folders_sinks_update_execute()` to send, or `logging_folders_sinks_update` for simplest API.

pub fn logging_folders_sinks_update_builder<R>(
    client: &SimpleHttpClient<R>,
    sinkName: &String,
    customWriterIdentity: &Option<String>,
    uniqueWriterIdentity: &Option<String>,
    updateMask: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/folders/{}/sinks/{sinksId}",
        sinkName,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = customWriterIdentity.as_ref() {
        query_parts.push(format!("customWriterIdentity={}", val));
    }
    if let Some(val) = uniqueWriterIdentity.as_ref() {
        query_parts.push(format!("uniqueWriterIdentity={}", val));
    }
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .put(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PUT v2/folders/{foldersId}/sinks/{sinksId}
/// Updates a sink. This method replaces the values of the destination and filter fields of the existing sink with the corresponding values from the new sink.The updated sink might also have a new writer_identity; see the unique_writer_identity field.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_folders_sinks_update_execute()` or `logging_folders_sinks_update`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_folders_sinks_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_folders_sinks_update_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<LogSink>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LogSink = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PUT v2/folders/{foldersId}/sinks/{sinksId}
/// Updates a sink. This method replaces the values of the destination and filter fields of the existing sink with the corresponding values from the new sink.The updated sink might also have a new writer_identity; see the unique_writer_identity field.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_folders_sinks_update_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_folders_sinks_update_task()`.
/// For the simplest API, use `logging_folders_sinks_update()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_folders_sinks_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_folders_sinks_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogSink>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = logging_folders_sinks_update_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_folders_sinks_update`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingFoldersSinksUpdateArgs {
    /// Path parameter: sinkName
    pub sinkName: String,
    /// Query parameter: customWriterIdentity
    pub customWriterIdentity: Option<String>,
    /// Query parameter: uniqueWriterIdentity
    pub uniqueWriterIdentity: Option<String>,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
}

/// PUT v2/folders/{foldersId}/sinks/{sinksId}
/// Updates a sink. This method replaces the values of the destination and filter fields of the existing sink with the corresponding values from the new sink.The updated sink might also have a new writer_identity; see the unique_writer_identity field.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_folders_sinks_update_builder()` + `logging_folders_sinks_update_execute()`.
/// For task-level control, use `logging_folders_sinks_update_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_folders_sinks_update(
    client: &SimpleHttpClient,
    args: &LoggingFoldersSinksUpdateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogSink>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_folders_sinks_update_builder(
        client,
        &args.sinkName,
        &args.customWriterIdentity,
        &args.uniqueWriterIdentity,
        &args.updateMask,
    )?;
    logging_folders_sinks_update_execute(builder)
}

/// GET v2/{v2Id}/{v2Id1}/locations/{locationsId}
/// Gets information about a location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_locations_get_execute()` to send, or `logging_locations_get` for simplest API.

pub fn logging_locations_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/{}/{v2Id1}/locations/{locationsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/{v2Id}/{v2Id1}/locations/{locationsId}
/// Gets information about a location.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_locations_get_execute()` or `logging_locations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_locations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_locations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Location>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Location = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/{v2Id}/{v2Id1}/locations/{locationsId}
/// Gets information about a location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_locations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_locations_get_task()`.
/// For the simplest API, use `logging_locations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_locations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_locations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Location>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = logging_locations_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_locations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingLocationsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v2/{v2Id}/{v2Id1}/locations/{locationsId}
/// Gets information about a location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_locations_get_builder()` + `logging_locations_get_execute()`.
/// For task-level control, use `logging_locations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_locations_get(
    client: &SimpleHttpClient,
    args: &LoggingLocationsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Location>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_locations_get_builder(client, &args.name)?;
    logging_locations_get_execute(builder)
}

/// GET v2/{v2Id}/{v2Id1}/locations
/// Lists information about the supported locations for this service.This method lists locations based on the resource scope provided in the ListLocationsRequest.name field: Global locations: If name is empty, the method lists the public locations available to all projects. Project-specific locations: If name follows the format `projects/{project}`, the method lists locations visible to that specific project. This includes public, private, or other project-specific locations enabled for the project.For `gRPC` and client library implementations, the resource name is passed as the name field. For direct service calls, the resource name is incorporated into the request path based on the specific service implementation and version.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_locations_list_execute()` to send, or `logging_locations_list` for simplest API.

pub fn logging_locations_list_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    extraLocationTypes: &Option<String>,
    filter: &Option<String>,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/{}/{v2Id1}/locations",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = extraLocationTypes.as_ref() {
        query_parts.push(format!("extraLocationTypes={}", val));
    }
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/{v2Id}/{v2Id1}/locations
/// Lists information about the supported locations for this service.This method lists locations based on the resource scope provided in the ListLocationsRequest.name field: Global locations: If name is empty, the method lists the public locations available to all projects. Project-specific locations: If name follows the format `projects/{project}`, the method lists locations visible to that specific project. This includes public, private, or other project-specific locations enabled for the project.For `gRPC` and client library implementations, the resource name is passed as the name field. For direct service calls, the resource name is incorporated into the request path based on the specific service implementation and version.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_locations_list_execute()` or `logging_locations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_locations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_locations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListLocationsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListLocationsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/{v2Id}/{v2Id1}/locations
/// Lists information about the supported locations for this service.This method lists locations based on the resource scope provided in the ListLocationsRequest.name field: Global locations: If name is empty, the method lists the public locations available to all projects. Project-specific locations: If name follows the format `projects/{project}`, the method lists locations visible to that specific project. This includes public, private, or other project-specific locations enabled for the project.For `gRPC` and client library implementations, the resource name is passed as the name field. For direct service calls, the resource name is incorporated into the request path based on the specific service implementation and version.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_locations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_locations_list_task()`.
/// For the simplest API, use `logging_locations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_locations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_locations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListLocationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = logging_locations_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_locations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingLocationsListArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: extraLocationTypes
    pub extraLocationTypes: Option<String>,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v2/{v2Id}/{v2Id1}/locations
/// Lists information about the supported locations for this service.This method lists locations based on the resource scope provided in the ListLocationsRequest.name field: Global locations: If name is empty, the method lists the public locations available to all projects. Project-specific locations: If name follows the format `projects/{project}`, the method lists locations visible to that specific project. This includes public, private, or other project-specific locations enabled for the project.For `gRPC` and client library implementations, the resource name is passed as the name field. For direct service calls, the resource name is incorporated into the request path based on the specific service implementation and version.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_locations_list_builder()` + `logging_locations_list_execute()`.
/// For task-level control, use `logging_locations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_locations_list(
    client: &SimpleHttpClient,
    args: &LoggingLocationsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListLocationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = logging_locations_list_builder(
        client,
        &args.name,
        &args.extraLocationTypes,
        &args.filter,
        &args.pageSize,
        &args.pageToken,
    )?;
    logging_locations_list_execute(builder)
}

/// POST v2/{v2Id}/{v2Id1}/locations/{locationsId}/buckets
/// Creates a log bucket that can be used to store log entries. After a bucket has been created, the bucket''s location cannot be changed.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_locations_buckets_create_execute()` to send, or `logging_locations_buckets_create` for simplest API.

pub fn logging_locations_buckets_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    bucketId: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/{}/{v2Id1}/locations/{locationsId}/buckets",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = bucketId.as_ref() {
        query_parts.push(format!("bucketId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v2/{v2Id}/{v2Id1}/locations/{locationsId}/buckets
/// Creates a log bucket that can be used to store log entries. After a bucket has been created, the bucket''s location cannot be changed.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_locations_buckets_create_execute()` or `logging_locations_buckets_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_locations_buckets_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_locations_buckets_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<LogBucket>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LogBucket = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v2/{v2Id}/{v2Id1}/locations/{locationsId}/buckets
/// Creates a log bucket that can be used to store log entries. After a bucket has been created, the bucket''s location cannot be changed.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_locations_buckets_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_locations_buckets_create_task()`.
/// For the simplest API, use `logging_locations_buckets_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_locations_buckets_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_locations_buckets_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogBucket>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = logging_locations_buckets_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_locations_buckets_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingLocationsBucketsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: bucketId
    pub bucketId: Option<String>,
}

/// POST v2/{v2Id}/{v2Id1}/locations/{locationsId}/buckets
/// Creates a log bucket that can be used to store log entries. After a bucket has been created, the bucket''s location cannot be changed.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_locations_buckets_create_builder()` + `logging_locations_buckets_create_execute()`.
/// For task-level control, use `logging_locations_buckets_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_locations_buckets_create(
    client: &SimpleHttpClient,
    args: &LoggingLocationsBucketsCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogBucket>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_locations_buckets_create_builder(client, &args.parent, &args.bucketId)?;
    logging_locations_buckets_create_execute(builder)
}

/// POST v2/{v2Id}/{v2Id1}/locations/{locationsId}/buckets:createAsync
/// Creates a log bucket asynchronously that can be used to store log entries.After a bucket has been created, the bucket''s location cannot be changed.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_locations_buckets_create_async_execute()` to send, or `logging_locations_buckets_create_async` for simplest API.

pub fn logging_locations_buckets_create_async_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    bucketId: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/{}/{v2Id1}/locations/{locationsId}/buckets:createAsync",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = bucketId.as_ref() {
        query_parts.push(format!("bucketId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v2/{v2Id}/{v2Id1}/locations/{locationsId}/buckets:createAsync
/// Creates a log bucket asynchronously that can be used to store log entries.After a bucket has been created, the bucket''s location cannot be changed.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_locations_buckets_create_async_execute()` or `logging_locations_buckets_create_async`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_locations_buckets_create_async_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_locations_buckets_create_async_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v2/{v2Id}/{v2Id1}/locations/{locationsId}/buckets:createAsync
/// Creates a log bucket asynchronously that can be used to store log entries.After a bucket has been created, the bucket''s location cannot be changed.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_locations_buckets_create_async_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_locations_buckets_create_async_task()`.
/// For the simplest API, use `logging_locations_buckets_create_async()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_locations_buckets_create_async_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_locations_buckets_create_async_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = logging_locations_buckets_create_async_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_locations_buckets_create_async`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingLocationsBucketsCreateAsyncArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: bucketId
    pub bucketId: Option<String>,
}

/// POST v2/{v2Id}/{v2Id1}/locations/{locationsId}/buckets:createAsync
/// Creates a log bucket asynchronously that can be used to store log entries.After a bucket has been created, the bucket''s location cannot be changed.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_locations_buckets_create_async_builder()` + `logging_locations_buckets_create_async_execute()`.
/// For task-level control, use `logging_locations_buckets_create_async_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_locations_buckets_create_async(
    client: &SimpleHttpClient,
    args: &LoggingLocationsBucketsCreateAsyncArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        logging_locations_buckets_create_async_builder(client, &args.parent, &args.bucketId)?;
    logging_locations_buckets_create_async_execute(builder)
}

/// DELETE v2/{v2Id}/{v2Id1}/locations/{locationsId}/buckets/{bucketsId}
/// Deletes a log bucket.Changes the bucket''s lifecycle_state to the DELETE_REQUESTED state. After 7 days, the bucket will be purged and all log entries in the bucket will be permanently deleted.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_locations_buckets_delete_execute()` to send, or `logging_locations_buckets_delete` for simplest API.

pub fn logging_locations_buckets_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/{}/{v2Id1}/locations/{locationsId}/buckets/{bucketsId}",
        name,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v2/{v2Id}/{v2Id1}/locations/{locationsId}/buckets/{bucketsId}
/// Deletes a log bucket.Changes the bucket''s lifecycle_state to the DELETE_REQUESTED state. After 7 days, the bucket will be purged and all log entries in the bucket will be permanently deleted.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_locations_buckets_delete_execute()` or `logging_locations_buckets_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_locations_buckets_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_locations_buckets_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v2/{v2Id}/{v2Id1}/locations/{locationsId}/buckets/{bucketsId}
/// Deletes a log bucket.Changes the bucket''s lifecycle_state to the DELETE_REQUESTED state. After 7 days, the bucket will be purged and all log entries in the bucket will be permanently deleted.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_locations_buckets_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_locations_buckets_delete_task()`.
/// For the simplest API, use `logging_locations_buckets_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_locations_buckets_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_locations_buckets_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = logging_locations_buckets_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_locations_buckets_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingLocationsBucketsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// DELETE v2/{v2Id}/{v2Id1}/locations/{locationsId}/buckets/{bucketsId}
/// Deletes a log bucket.Changes the bucket''s lifecycle_state to the DELETE_REQUESTED state. After 7 days, the bucket will be purged and all log entries in the bucket will be permanently deleted.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_locations_buckets_delete_builder()` + `logging_locations_buckets_delete_execute()`.
/// For task-level control, use `logging_locations_buckets_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_locations_buckets_delete(
    client: &SimpleHttpClient,
    args: &LoggingLocationsBucketsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_locations_buckets_delete_builder(client, &args.name)?;
    logging_locations_buckets_delete_execute(builder)
}

/// GET v2/{v2Id}/{v2Id1}/locations/{locationsId}/buckets/{bucketsId}
/// Gets a log bucket.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_locations_buckets_get_execute()` to send, or `logging_locations_buckets_get` for simplest API.

pub fn logging_locations_buckets_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/{}/{v2Id1}/locations/{locationsId}/buckets/{bucketsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/{v2Id}/{v2Id1}/locations/{locationsId}/buckets/{bucketsId}
/// Gets a log bucket.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_locations_buckets_get_execute()` or `logging_locations_buckets_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_locations_buckets_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_locations_buckets_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<LogBucket>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LogBucket = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/{v2Id}/{v2Id1}/locations/{locationsId}/buckets/{bucketsId}
/// Gets a log bucket.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_locations_buckets_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_locations_buckets_get_task()`.
/// For the simplest API, use `logging_locations_buckets_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_locations_buckets_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_locations_buckets_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogBucket>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = logging_locations_buckets_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_locations_buckets_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingLocationsBucketsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v2/{v2Id}/{v2Id1}/locations/{locationsId}/buckets/{bucketsId}
/// Gets a log bucket.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_locations_buckets_get_builder()` + `logging_locations_buckets_get_execute()`.
/// For task-level control, use `logging_locations_buckets_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_locations_buckets_get(
    client: &SimpleHttpClient,
    args: &LoggingLocationsBucketsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogBucket>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_locations_buckets_get_builder(client, &args.name)?;
    logging_locations_buckets_get_execute(builder)
}

/// GET v2/{v2Id}/{v2Id1}/locations/{locationsId}/buckets
/// Lists log buckets.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_locations_buckets_list_execute()` to send, or `logging_locations_buckets_list` for simplest API.

pub fn logging_locations_buckets_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/{}/{v2Id1}/locations/{locationsId}/buckets",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/{v2Id}/{v2Id1}/locations/{locationsId}/buckets
/// Lists log buckets.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_locations_buckets_list_execute()` or `logging_locations_buckets_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_locations_buckets_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_locations_buckets_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListBucketsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListBucketsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/{v2Id}/{v2Id1}/locations/{locationsId}/buckets
/// Lists log buckets.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_locations_buckets_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_locations_buckets_list_task()`.
/// For the simplest API, use `logging_locations_buckets_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_locations_buckets_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_locations_buckets_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListBucketsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = logging_locations_buckets_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_locations_buckets_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingLocationsBucketsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v2/{v2Id}/{v2Id1}/locations/{locationsId}/buckets
/// Lists log buckets.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_locations_buckets_list_builder()` + `logging_locations_buckets_list_execute()`.
/// For task-level control, use `logging_locations_buckets_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_locations_buckets_list(
    client: &SimpleHttpClient,
    args: &LoggingLocationsBucketsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListBucketsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = logging_locations_buckets_list_builder(
        client,
        &args.parent,
        &args.pageSize,
        &args.pageToken,
    )?;
    logging_locations_buckets_list_execute(builder)
}

/// PATCH v2/{v2Id}/{v2Id1}/locations/{locationsId}/buckets/{bucketsId}
/// Updates a log bucket.If the bucket has a lifecycle_state of DELETE_REQUESTED, then FAILED_PRECONDITION will be returned.After a bucket has been created, the bucket''s location cannot be changed.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_locations_buckets_patch_execute()` to send, or `logging_locations_buckets_patch` for simplest API.

pub fn logging_locations_buckets_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    updateMask: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/{}/{v2Id1}/locations/{locationsId}/buckets/{bucketsId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v2/{v2Id}/{v2Id1}/locations/{locationsId}/buckets/{bucketsId}
/// Updates a log bucket.If the bucket has a lifecycle_state of DELETE_REQUESTED, then FAILED_PRECONDITION will be returned.After a bucket has been created, the bucket''s location cannot be changed.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_locations_buckets_patch_execute()` or `logging_locations_buckets_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_locations_buckets_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_locations_buckets_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<LogBucket>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LogBucket = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v2/{v2Id}/{v2Id1}/locations/{locationsId}/buckets/{bucketsId}
/// Updates a log bucket.If the bucket has a lifecycle_state of DELETE_REQUESTED, then FAILED_PRECONDITION will be returned.After a bucket has been created, the bucket''s location cannot be changed.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_locations_buckets_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_locations_buckets_patch_task()`.
/// For the simplest API, use `logging_locations_buckets_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_locations_buckets_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_locations_buckets_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogBucket>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = logging_locations_buckets_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_locations_buckets_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingLocationsBucketsPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
}

/// PATCH v2/{v2Id}/{v2Id1}/locations/{locationsId}/buckets/{bucketsId}
/// Updates a log bucket.If the bucket has a lifecycle_state of DELETE_REQUESTED, then FAILED_PRECONDITION will be returned.After a bucket has been created, the bucket''s location cannot be changed.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_locations_buckets_patch_builder()` + `logging_locations_buckets_patch_execute()`.
/// For task-level control, use `logging_locations_buckets_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_locations_buckets_patch(
    client: &SimpleHttpClient,
    args: &LoggingLocationsBucketsPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogBucket>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_locations_buckets_patch_builder(client, &args.name, &args.updateMask)?;
    logging_locations_buckets_patch_execute(builder)
}

/// POST v2/{v2Id}/{v2Id1}/locations/{locationsId}/buckets/{bucketsId}:undelete
/// Undeletes a log bucket. A bucket that has been deleted can be undeleted within the grace period of 7 days.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_locations_buckets_undelete_execute()` to send, or `logging_locations_buckets_undelete` for simplest API.

pub fn logging_locations_buckets_undelete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/{}/{v2Id1}/locations/{locationsId}/buckets/{bucketsId}:undelete",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v2/{v2Id}/{v2Id1}/locations/{locationsId}/buckets/{bucketsId}:undelete
/// Undeletes a log bucket. A bucket that has been deleted can be undeleted within the grace period of 7 days.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_locations_buckets_undelete_execute()` or `logging_locations_buckets_undelete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_locations_buckets_undelete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_locations_buckets_undelete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v2/{v2Id}/{v2Id1}/locations/{locationsId}/buckets/{bucketsId}:undelete
/// Undeletes a log bucket. A bucket that has been deleted can be undeleted within the grace period of 7 days.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_locations_buckets_undelete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_locations_buckets_undelete_task()`.
/// For the simplest API, use `logging_locations_buckets_undelete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_locations_buckets_undelete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_locations_buckets_undelete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = logging_locations_buckets_undelete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_locations_buckets_undelete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingLocationsBucketsUndeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v2/{v2Id}/{v2Id1}/locations/{locationsId}/buckets/{bucketsId}:undelete
/// Undeletes a log bucket. A bucket that has been deleted can be undeleted within the grace period of 7 days.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_locations_buckets_undelete_builder()` + `logging_locations_buckets_undelete_execute()`.
/// For task-level control, use `logging_locations_buckets_undelete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_locations_buckets_undelete(
    client: &SimpleHttpClient,
    args: &LoggingLocationsBucketsUndeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_locations_buckets_undelete_builder(client, &args.name)?;
    logging_locations_buckets_undelete_execute(builder)
}

/// POST v2/{v2Id}/{v2Id1}/locations/{locationsId}/buckets/{bucketsId}:updateAsync
/// Updates a log bucket asynchronously.If the bucket has a lifecycle_state of DELETE_REQUESTED, then FAILED_PRECONDITION will be returned.After a bucket has been created, the bucket''s location cannot be changed.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_locations_buckets_update_async_execute()` to send, or `logging_locations_buckets_update_async` for simplest API.

pub fn logging_locations_buckets_update_async_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    updateMask: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/{}/{v2Id1}/locations/{locationsId}/buckets/{bucketsId}:updateAsync",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v2/{v2Id}/{v2Id1}/locations/{locationsId}/buckets/{bucketsId}:updateAsync
/// Updates a log bucket asynchronously.If the bucket has a lifecycle_state of DELETE_REQUESTED, then FAILED_PRECONDITION will be returned.After a bucket has been created, the bucket''s location cannot be changed.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_locations_buckets_update_async_execute()` or `logging_locations_buckets_update_async`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_locations_buckets_update_async_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_locations_buckets_update_async_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v2/{v2Id}/{v2Id1}/locations/{locationsId}/buckets/{bucketsId}:updateAsync
/// Updates a log bucket asynchronously.If the bucket has a lifecycle_state of DELETE_REQUESTED, then FAILED_PRECONDITION will be returned.After a bucket has been created, the bucket''s location cannot be changed.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_locations_buckets_update_async_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_locations_buckets_update_async_task()`.
/// For the simplest API, use `logging_locations_buckets_update_async()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_locations_buckets_update_async_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_locations_buckets_update_async_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = logging_locations_buckets_update_async_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_locations_buckets_update_async`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingLocationsBucketsUpdateAsyncArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
}

/// POST v2/{v2Id}/{v2Id1}/locations/{locationsId}/buckets/{bucketsId}:updateAsync
/// Updates a log bucket asynchronously.If the bucket has a lifecycle_state of DELETE_REQUESTED, then FAILED_PRECONDITION will be returned.After a bucket has been created, the bucket''s location cannot be changed.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_locations_buckets_update_async_builder()` + `logging_locations_buckets_update_async_execute()`.
/// For task-level control, use `logging_locations_buckets_update_async_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_locations_buckets_update_async(
    client: &SimpleHttpClient,
    args: &LoggingLocationsBucketsUpdateAsyncArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        logging_locations_buckets_update_async_builder(client, &args.name, &args.updateMask)?;
    logging_locations_buckets_update_async_execute(builder)
}

/// POST v2/{v2Id}/{v2Id1}/locations/{locationsId}/buckets/{bucketsId}/links
/// Asynchronously creates a linked dataset in BigQuery which makes it possible to use BigQuery to read the logs stored in the log bucket. A log bucket may currently only contain one link.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_locations_buckets_links_create_execute()` to send, or `logging_locations_buckets_links_create` for simplest API.

pub fn logging_locations_buckets_links_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    linkId: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/{}/{v2Id1}/locations/{locationsId}/buckets/{bucketsId}/links",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = linkId.as_ref() {
        query_parts.push(format!("linkId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v2/{v2Id}/{v2Id1}/locations/{locationsId}/buckets/{bucketsId}/links
/// Asynchronously creates a linked dataset in BigQuery which makes it possible to use BigQuery to read the logs stored in the log bucket. A log bucket may currently only contain one link.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_locations_buckets_links_create_execute()` or `logging_locations_buckets_links_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_locations_buckets_links_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_locations_buckets_links_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v2/{v2Id}/{v2Id1}/locations/{locationsId}/buckets/{bucketsId}/links
/// Asynchronously creates a linked dataset in BigQuery which makes it possible to use BigQuery to read the logs stored in the log bucket. A log bucket may currently only contain one link.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_locations_buckets_links_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_locations_buckets_links_create_task()`.
/// For the simplest API, use `logging_locations_buckets_links_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_locations_buckets_links_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_locations_buckets_links_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = logging_locations_buckets_links_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_locations_buckets_links_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingLocationsBucketsLinksCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: linkId
    pub linkId: Option<String>,
}

/// POST v2/{v2Id}/{v2Id1}/locations/{locationsId}/buckets/{bucketsId}/links
/// Asynchronously creates a linked dataset in BigQuery which makes it possible to use BigQuery to read the logs stored in the log bucket. A log bucket may currently only contain one link.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_locations_buckets_links_create_builder()` + `logging_locations_buckets_links_create_execute()`.
/// For task-level control, use `logging_locations_buckets_links_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_locations_buckets_links_create(
    client: &SimpleHttpClient,
    args: &LoggingLocationsBucketsLinksCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        logging_locations_buckets_links_create_builder(client, &args.parent, &args.linkId)?;
    logging_locations_buckets_links_create_execute(builder)
}

/// DELETE v2/{v2Id}/{v2Id1}/locations/{locationsId}/buckets/{bucketsId}/links/{linksId}
/// Deletes a link. This will also delete the corresponding BigQuery linked dataset.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_locations_buckets_links_delete_execute()` to send, or `logging_locations_buckets_links_delete` for simplest API.

pub fn logging_locations_buckets_links_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/{}/{v2Id1}/locations/{locationsId}/buckets/{bucketsId}/links/{linksId}",
        name,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v2/{v2Id}/{v2Id1}/locations/{locationsId}/buckets/{bucketsId}/links/{linksId}
/// Deletes a link. This will also delete the corresponding BigQuery linked dataset.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_locations_buckets_links_delete_execute()` or `logging_locations_buckets_links_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_locations_buckets_links_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_locations_buckets_links_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v2/{v2Id}/{v2Id1}/locations/{locationsId}/buckets/{bucketsId}/links/{linksId}
/// Deletes a link. This will also delete the corresponding BigQuery linked dataset.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_locations_buckets_links_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_locations_buckets_links_delete_task()`.
/// For the simplest API, use `logging_locations_buckets_links_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_locations_buckets_links_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_locations_buckets_links_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = logging_locations_buckets_links_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_locations_buckets_links_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingLocationsBucketsLinksDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// DELETE v2/{v2Id}/{v2Id1}/locations/{locationsId}/buckets/{bucketsId}/links/{linksId}
/// Deletes a link. This will also delete the corresponding BigQuery linked dataset.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_locations_buckets_links_delete_builder()` + `logging_locations_buckets_links_delete_execute()`.
/// For task-level control, use `logging_locations_buckets_links_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_locations_buckets_links_delete(
    client: &SimpleHttpClient,
    args: &LoggingLocationsBucketsLinksDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_locations_buckets_links_delete_builder(client, &args.name)?;
    logging_locations_buckets_links_delete_execute(builder)
}

/// GET v2/{v2Id}/{v2Id1}/locations/{locationsId}/buckets/{bucketsId}/links/{linksId}
/// Gets a link.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_locations_buckets_links_get_execute()` to send, or `logging_locations_buckets_links_get` for simplest API.

pub fn logging_locations_buckets_links_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/{}/{v2Id1}/locations/{locationsId}/buckets/{bucketsId}/links/{linksId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/{v2Id}/{v2Id1}/locations/{locationsId}/buckets/{bucketsId}/links/{linksId}
/// Gets a link.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_locations_buckets_links_get_execute()` or `logging_locations_buckets_links_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_locations_buckets_links_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_locations_buckets_links_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Link>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Link = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/{v2Id}/{v2Id1}/locations/{locationsId}/buckets/{bucketsId}/links/{linksId}
/// Gets a link.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_locations_buckets_links_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_locations_buckets_links_get_task()`.
/// For the simplest API, use `logging_locations_buckets_links_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_locations_buckets_links_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_locations_buckets_links_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Link>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = logging_locations_buckets_links_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_locations_buckets_links_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingLocationsBucketsLinksGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v2/{v2Id}/{v2Id1}/locations/{locationsId}/buckets/{bucketsId}/links/{linksId}
/// Gets a link.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_locations_buckets_links_get_builder()` + `logging_locations_buckets_links_get_execute()`.
/// For task-level control, use `logging_locations_buckets_links_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_locations_buckets_links_get(
    client: &SimpleHttpClient,
    args: &LoggingLocationsBucketsLinksGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Link>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_locations_buckets_links_get_builder(client, &args.name)?;
    logging_locations_buckets_links_get_execute(builder)
}

/// GET v2/{v2Id}/{v2Id1}/locations/{locationsId}/buckets/{bucketsId}/links
/// Lists links.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_locations_buckets_links_list_execute()` to send, or `logging_locations_buckets_links_list` for simplest API.

pub fn logging_locations_buckets_links_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/{}/{v2Id1}/locations/{locationsId}/buckets/{bucketsId}/links",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/{v2Id}/{v2Id1}/locations/{locationsId}/buckets/{bucketsId}/links
/// Lists links.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_locations_buckets_links_list_execute()` or `logging_locations_buckets_links_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_locations_buckets_links_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_locations_buckets_links_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListLinksResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListLinksResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/{v2Id}/{v2Id1}/locations/{locationsId}/buckets/{bucketsId}/links
/// Lists links.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_locations_buckets_links_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_locations_buckets_links_list_task()`.
/// For the simplest API, use `logging_locations_buckets_links_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_locations_buckets_links_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_locations_buckets_links_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListLinksResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = logging_locations_buckets_links_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_locations_buckets_links_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingLocationsBucketsLinksListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v2/{v2Id}/{v2Id1}/locations/{locationsId}/buckets/{bucketsId}/links
/// Lists links.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_locations_buckets_links_list_builder()` + `logging_locations_buckets_links_list_execute()`.
/// For task-level control, use `logging_locations_buckets_links_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_locations_buckets_links_list(
    client: &SimpleHttpClient,
    args: &LoggingLocationsBucketsLinksListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListLinksResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = logging_locations_buckets_links_list_builder(
        client,
        &args.parent,
        &args.pageSize,
        &args.pageToken,
    )?;
    logging_locations_buckets_links_list_execute(builder)
}

/// POST v2/{v2Id}/{v2Id1}/locations/{locationsId}/buckets/{bucketsId}/views
/// Creates a view over log entries in a log bucket. A bucket may contain a maximum of 30 views.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_locations_buckets_views_create_execute()` to send, or `logging_locations_buckets_views_create` for simplest API.

pub fn logging_locations_buckets_views_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    viewId: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/{}/{v2Id1}/locations/{locationsId}/buckets/{bucketsId}/views",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = viewId.as_ref() {
        query_parts.push(format!("viewId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v2/{v2Id}/{v2Id1}/locations/{locationsId}/buckets/{bucketsId}/views
/// Creates a view over log entries in a log bucket. A bucket may contain a maximum of 30 views.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_locations_buckets_views_create_execute()` or `logging_locations_buckets_views_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_locations_buckets_views_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_locations_buckets_views_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<LogView>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LogView = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v2/{v2Id}/{v2Id1}/locations/{locationsId}/buckets/{bucketsId}/views
/// Creates a view over log entries in a log bucket. A bucket may contain a maximum of 30 views.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_locations_buckets_views_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_locations_buckets_views_create_task()`.
/// For the simplest API, use `logging_locations_buckets_views_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_locations_buckets_views_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_locations_buckets_views_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogView>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = logging_locations_buckets_views_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_locations_buckets_views_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingLocationsBucketsViewsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: viewId
    pub viewId: Option<String>,
}

/// POST v2/{v2Id}/{v2Id1}/locations/{locationsId}/buckets/{bucketsId}/views
/// Creates a view over log entries in a log bucket. A bucket may contain a maximum of 30 views.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_locations_buckets_views_create_builder()` + `logging_locations_buckets_views_create_execute()`.
/// For task-level control, use `logging_locations_buckets_views_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_locations_buckets_views_create(
    client: &SimpleHttpClient,
    args: &LoggingLocationsBucketsViewsCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogView>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        logging_locations_buckets_views_create_builder(client, &args.parent, &args.viewId)?;
    logging_locations_buckets_views_create_execute(builder)
}

/// DELETE v2/{v2Id}/{v2Id1}/locations/{locationsId}/buckets/{bucketsId}/views/{viewsId}
/// Deletes a view on a log bucket. If an UNAVAILABLE error is returned, this indicates that system is not in a state where it can delete the view. If this occurs, please try again in a few minutes.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_locations_buckets_views_delete_execute()` to send, or `logging_locations_buckets_views_delete` for simplest API.

pub fn logging_locations_buckets_views_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/{}/{v2Id1}/locations/{locationsId}/buckets/{bucketsId}/views/{viewsId}",
        name,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v2/{v2Id}/{v2Id1}/locations/{locationsId}/buckets/{bucketsId}/views/{viewsId}
/// Deletes a view on a log bucket. If an UNAVAILABLE error is returned, this indicates that system is not in a state where it can delete the view. If this occurs, please try again in a few minutes.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_locations_buckets_views_delete_execute()` or `logging_locations_buckets_views_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_locations_buckets_views_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_locations_buckets_views_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v2/{v2Id}/{v2Id1}/locations/{locationsId}/buckets/{bucketsId}/views/{viewsId}
/// Deletes a view on a log bucket. If an UNAVAILABLE error is returned, this indicates that system is not in a state where it can delete the view. If this occurs, please try again in a few minutes.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_locations_buckets_views_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_locations_buckets_views_delete_task()`.
/// For the simplest API, use `logging_locations_buckets_views_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_locations_buckets_views_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_locations_buckets_views_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = logging_locations_buckets_views_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_locations_buckets_views_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingLocationsBucketsViewsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// DELETE v2/{v2Id}/{v2Id1}/locations/{locationsId}/buckets/{bucketsId}/views/{viewsId}
/// Deletes a view on a log bucket. If an UNAVAILABLE error is returned, this indicates that system is not in a state where it can delete the view. If this occurs, please try again in a few minutes.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_locations_buckets_views_delete_builder()` + `logging_locations_buckets_views_delete_execute()`.
/// For task-level control, use `logging_locations_buckets_views_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_locations_buckets_views_delete(
    client: &SimpleHttpClient,
    args: &LoggingLocationsBucketsViewsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_locations_buckets_views_delete_builder(client, &args.name)?;
    logging_locations_buckets_views_delete_execute(builder)
}

/// GET v2/{v2Id}/{v2Id1}/locations/{locationsId}/buckets/{bucketsId}/views/{viewsId}
/// Gets a view on a log bucket.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_locations_buckets_views_get_execute()` to send, or `logging_locations_buckets_views_get` for simplest API.

pub fn logging_locations_buckets_views_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/{}/{v2Id1}/locations/{locationsId}/buckets/{bucketsId}/views/{viewsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/{v2Id}/{v2Id1}/locations/{locationsId}/buckets/{bucketsId}/views/{viewsId}
/// Gets a view on a log bucket.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_locations_buckets_views_get_execute()` or `logging_locations_buckets_views_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_locations_buckets_views_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_locations_buckets_views_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<LogView>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LogView = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/{v2Id}/{v2Id1}/locations/{locationsId}/buckets/{bucketsId}/views/{viewsId}
/// Gets a view on a log bucket.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_locations_buckets_views_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_locations_buckets_views_get_task()`.
/// For the simplest API, use `logging_locations_buckets_views_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_locations_buckets_views_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_locations_buckets_views_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogView>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = logging_locations_buckets_views_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_locations_buckets_views_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingLocationsBucketsViewsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v2/{v2Id}/{v2Id1}/locations/{locationsId}/buckets/{bucketsId}/views/{viewsId}
/// Gets a view on a log bucket.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_locations_buckets_views_get_builder()` + `logging_locations_buckets_views_get_execute()`.
/// For task-level control, use `logging_locations_buckets_views_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_locations_buckets_views_get(
    client: &SimpleHttpClient,
    args: &LoggingLocationsBucketsViewsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogView>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_locations_buckets_views_get_builder(client, &args.name)?;
    logging_locations_buckets_views_get_execute(builder)
}

/// POST v2/{v2Id}/{v2Id1}/locations/{locationsId}/buckets/{bucketsId}/views/{viewsId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_locations_buckets_views_get_iam_policy_execute()` to send, or `logging_locations_buckets_views_get_iam_policy` for simplest API.

pub fn logging_locations_buckets_views_get_iam_policy_builder<R>(
    client: &SimpleHttpClient<R>,
    resource: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/{}/{v2Id1}/locations/{locationsId}/buckets/{bucketsId}/views/{viewsId}:getIamPolicy",
        resource,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v2/{v2Id}/{v2Id1}/locations/{locationsId}/buckets/{bucketsId}/views/{viewsId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_locations_buckets_views_get_iam_policy_execute()` or `logging_locations_buckets_views_get_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_locations_buckets_views_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_locations_buckets_views_get_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v2/{v2Id}/{v2Id1}/locations/{locationsId}/buckets/{bucketsId}/views/{viewsId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_locations_buckets_views_get_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_locations_buckets_views_get_iam_policy_task()`.
/// For the simplest API, use `logging_locations_buckets_views_get_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_locations_buckets_views_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_locations_buckets_views_get_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = logging_locations_buckets_views_get_iam_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_locations_buckets_views_get_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingLocationsBucketsViewsGetIamPolicyArgs {
    /// Path parameter: resource
    pub resource: String,
}

/// POST v2/{v2Id}/{v2Id1}/locations/{locationsId}/buckets/{bucketsId}/views/{viewsId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_locations_buckets_views_get_iam_policy_builder()` + `logging_locations_buckets_views_get_iam_policy_execute()`.
/// For task-level control, use `logging_locations_buckets_views_get_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_locations_buckets_views_get_iam_policy(
    client: &SimpleHttpClient,
    args: &LoggingLocationsBucketsViewsGetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_locations_buckets_views_get_iam_policy_builder(client, &args.resource)?;
    logging_locations_buckets_views_get_iam_policy_execute(builder)
}

/// GET v2/{v2Id}/{v2Id1}/locations/{locationsId}/buckets/{bucketsId}/views
/// Lists views on a log bucket.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_locations_buckets_views_list_execute()` to send, or `logging_locations_buckets_views_list` for simplest API.

pub fn logging_locations_buckets_views_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/{}/{v2Id1}/locations/{locationsId}/buckets/{bucketsId}/views",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/{v2Id}/{v2Id1}/locations/{locationsId}/buckets/{bucketsId}/views
/// Lists views on a log bucket.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_locations_buckets_views_list_execute()` or `logging_locations_buckets_views_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_locations_buckets_views_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_locations_buckets_views_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListViewsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListViewsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/{v2Id}/{v2Id1}/locations/{locationsId}/buckets/{bucketsId}/views
/// Lists views on a log bucket.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_locations_buckets_views_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_locations_buckets_views_list_task()`.
/// For the simplest API, use `logging_locations_buckets_views_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_locations_buckets_views_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_locations_buckets_views_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListViewsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = logging_locations_buckets_views_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_locations_buckets_views_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingLocationsBucketsViewsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v2/{v2Id}/{v2Id1}/locations/{locationsId}/buckets/{bucketsId}/views
/// Lists views on a log bucket.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_locations_buckets_views_list_builder()` + `logging_locations_buckets_views_list_execute()`.
/// For task-level control, use `logging_locations_buckets_views_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_locations_buckets_views_list(
    client: &SimpleHttpClient,
    args: &LoggingLocationsBucketsViewsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListViewsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = logging_locations_buckets_views_list_builder(
        client,
        &args.parent,
        &args.pageSize,
        &args.pageToken,
    )?;
    logging_locations_buckets_views_list_execute(builder)
}

/// PATCH v2/{v2Id}/{v2Id1}/locations/{locationsId}/buckets/{bucketsId}/views/{viewsId}
/// Updates a view on a log bucket. This method replaces the value of the filter field from the existing view with the corresponding value from the new view. If an UNAVAILABLE error is returned, this indicates that system is not in a state where it can update the view. If this occurs, please try again in a few minutes.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_locations_buckets_views_patch_execute()` to send, or `logging_locations_buckets_views_patch` for simplest API.

pub fn logging_locations_buckets_views_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    updateMask: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/{}/{v2Id1}/locations/{locationsId}/buckets/{bucketsId}/views/{viewsId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v2/{v2Id}/{v2Id1}/locations/{locationsId}/buckets/{bucketsId}/views/{viewsId}
/// Updates a view on a log bucket. This method replaces the value of the filter field from the existing view with the corresponding value from the new view. If an UNAVAILABLE error is returned, this indicates that system is not in a state where it can update the view. If this occurs, please try again in a few minutes.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_locations_buckets_views_patch_execute()` or `logging_locations_buckets_views_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_locations_buckets_views_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_locations_buckets_views_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<LogView>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LogView = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v2/{v2Id}/{v2Id1}/locations/{locationsId}/buckets/{bucketsId}/views/{viewsId}
/// Updates a view on a log bucket. This method replaces the value of the filter field from the existing view with the corresponding value from the new view. If an UNAVAILABLE error is returned, this indicates that system is not in a state where it can update the view. If this occurs, please try again in a few minutes.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_locations_buckets_views_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_locations_buckets_views_patch_task()`.
/// For the simplest API, use `logging_locations_buckets_views_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_locations_buckets_views_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_locations_buckets_views_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogView>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = logging_locations_buckets_views_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_locations_buckets_views_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingLocationsBucketsViewsPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
}

/// PATCH v2/{v2Id}/{v2Id1}/locations/{locationsId}/buckets/{bucketsId}/views/{viewsId}
/// Updates a view on a log bucket. This method replaces the value of the filter field from the existing view with the corresponding value from the new view. If an UNAVAILABLE error is returned, this indicates that system is not in a state where it can update the view. If this occurs, please try again in a few minutes.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_locations_buckets_views_patch_builder()` + `logging_locations_buckets_views_patch_execute()`.
/// For task-level control, use `logging_locations_buckets_views_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_locations_buckets_views_patch(
    client: &SimpleHttpClient,
    args: &LoggingLocationsBucketsViewsPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogView>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        logging_locations_buckets_views_patch_builder(client, &args.name, &args.updateMask)?;
    logging_locations_buckets_views_patch_execute(builder)
}

/// POST v2/{v2Id}/{v2Id1}/locations/{locationsId}/buckets/{bucketsId}/views/{viewsId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_locations_buckets_views_set_iam_policy_execute()` to send, or `logging_locations_buckets_views_set_iam_policy` for simplest API.

pub fn logging_locations_buckets_views_set_iam_policy_builder<R>(
    client: &SimpleHttpClient<R>,
    resource: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/{}/{v2Id1}/locations/{locationsId}/buckets/{bucketsId}/views/{viewsId}:setIamPolicy",
        resource,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v2/{v2Id}/{v2Id1}/locations/{locationsId}/buckets/{bucketsId}/views/{viewsId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_locations_buckets_views_set_iam_policy_execute()` or `logging_locations_buckets_views_set_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_locations_buckets_views_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_locations_buckets_views_set_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v2/{v2Id}/{v2Id1}/locations/{locationsId}/buckets/{bucketsId}/views/{viewsId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_locations_buckets_views_set_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_locations_buckets_views_set_iam_policy_task()`.
/// For the simplest API, use `logging_locations_buckets_views_set_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_locations_buckets_views_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_locations_buckets_views_set_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = logging_locations_buckets_views_set_iam_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_locations_buckets_views_set_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingLocationsBucketsViewsSetIamPolicyArgs {
    /// Path parameter: resource
    pub resource: String,
}

/// POST v2/{v2Id}/{v2Id1}/locations/{locationsId}/buckets/{bucketsId}/views/{viewsId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_locations_buckets_views_set_iam_policy_builder()` + `logging_locations_buckets_views_set_iam_policy_execute()`.
/// For task-level control, use `logging_locations_buckets_views_set_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_locations_buckets_views_set_iam_policy(
    client: &SimpleHttpClient,
    args: &LoggingLocationsBucketsViewsSetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_locations_buckets_views_set_iam_policy_builder(client, &args.resource)?;
    logging_locations_buckets_views_set_iam_policy_execute(builder)
}

/// POST v2/{v2Id}/{v2Id1}/locations/{locationsId}/buckets/{bucketsId}/views/{viewsId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error.Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_locations_buckets_views_test_iam_permissions_execute()` to send, or `logging_locations_buckets_views_test_iam_permissions` for simplest API.

pub fn logging_locations_buckets_views_test_iam_permissions_builder<R>(
    client: &SimpleHttpClient<R>,
    resource: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/{}/{v2Id1}/locations/{locationsId}/buckets/{bucketsId}/views/{viewsId}:testIamPermissions",
        resource,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v2/{v2Id}/{v2Id1}/locations/{locationsId}/buckets/{bucketsId}/views/{viewsId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error.Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_locations_buckets_views_test_iam_permissions_execute()` or `logging_locations_buckets_views_test_iam_permissions`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_locations_buckets_views_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_locations_buckets_views_test_iam_permissions_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TestIamPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v2/{v2Id}/{v2Id1}/locations/{locationsId}/buckets/{bucketsId}/views/{viewsId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error.Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_locations_buckets_views_test_iam_permissions_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_locations_buckets_views_test_iam_permissions_task()`.
/// For the simplest API, use `logging_locations_buckets_views_test_iam_permissions()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_locations_buckets_views_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_locations_buckets_views_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = logging_locations_buckets_views_test_iam_permissions_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_locations_buckets_views_test_iam_permissions`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingLocationsBucketsViewsTestIamPermissionsArgs {
    /// Path parameter: resource
    pub resource: String,
}

/// POST v2/{v2Id}/{v2Id1}/locations/{locationsId}/buckets/{bucketsId}/views/{viewsId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error.Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_locations_buckets_views_test_iam_permissions_builder()` + `logging_locations_buckets_views_test_iam_permissions_execute()`.
/// For task-level control, use `logging_locations_buckets_views_test_iam_permissions_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_locations_buckets_views_test_iam_permissions(
    client: &SimpleHttpClient,
    args: &LoggingLocationsBucketsViewsTestIamPermissionsArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        logging_locations_buckets_views_test_iam_permissions_builder(client, &args.resource)?;
    logging_locations_buckets_views_test_iam_permissions_execute(builder)
}

/// POST v2/{v2Id}/{v2Id1}/locations/{locationsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn''t support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_locations_operations_cancel_execute()` to send, or `logging_locations_operations_cancel` for simplest API.

pub fn logging_locations_operations_cancel_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/{}/{v2Id1}/locations/{locationsId}/operations/{operationsId}:cancel",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v2/{v2Id}/{v2Id1}/locations/{locationsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn''t support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_locations_operations_cancel_execute()` or `logging_locations_operations_cancel`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_locations_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_locations_operations_cancel_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v2/{v2Id}/{v2Id1}/locations/{locationsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn''t support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_locations_operations_cancel_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_locations_operations_cancel_task()`.
/// For the simplest API, use `logging_locations_operations_cancel()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_locations_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_locations_operations_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = logging_locations_operations_cancel_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_locations_operations_cancel`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingLocationsOperationsCancelArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v2/{v2Id}/{v2Id1}/locations/{locationsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn''t support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_locations_operations_cancel_builder()` + `logging_locations_operations_cancel_execute()`.
/// For task-level control, use `logging_locations_operations_cancel_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_locations_operations_cancel(
    client: &SimpleHttpClient,
    args: &LoggingLocationsOperationsCancelArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_locations_operations_cancel_builder(client, &args.name)?;
    logging_locations_operations_cancel_execute(builder)
}

/// GET v2/{v2Id}/{v2Id1}/locations/{locationsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_locations_operations_get_execute()` to send, or `logging_locations_operations_get` for simplest API.

pub fn logging_locations_operations_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/{}/{v2Id1}/locations/{locationsId}/operations/{operationsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/{v2Id}/{v2Id1}/locations/{locationsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_locations_operations_get_execute()` or `logging_locations_operations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_locations_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_locations_operations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/{v2Id}/{v2Id1}/locations/{locationsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_locations_operations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_locations_operations_get_task()`.
/// For the simplest API, use `logging_locations_operations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_locations_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_locations_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = logging_locations_operations_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_locations_operations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingLocationsOperationsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v2/{v2Id}/{v2Id1}/locations/{locationsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_locations_operations_get_builder()` + `logging_locations_operations_get_execute()`.
/// For task-level control, use `logging_locations_operations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_locations_operations_get(
    client: &SimpleHttpClient,
    args: &LoggingLocationsOperationsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_locations_operations_get_builder(client, &args.name)?;
    logging_locations_operations_get_execute(builder)
}

/// GET v2/{v2Id}/{v2Id1}/locations/{locationsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn''t support this method, it returns UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_locations_operations_list_execute()` to send, or `logging_locations_operations_list` for simplest API.

pub fn logging_locations_operations_list_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    filter: &Option<String>,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
    returnPartialSuccess: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/{}/{v2Id1}/locations/{locationsId}/operations",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/{v2Id}/{v2Id1}/locations/{locationsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn''t support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_locations_operations_list_execute()` or `logging_locations_operations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_locations_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_locations_operations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListOperationsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListOperationsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/{v2Id}/{v2Id1}/locations/{locationsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn''t support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_locations_operations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_locations_operations_list_task()`.
/// For the simplest API, use `logging_locations_operations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_locations_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_locations_operations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListOperationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = logging_locations_operations_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_locations_operations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingLocationsOperationsListArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<String>,
}

/// GET v2/{v2Id}/{v2Id1}/locations/{locationsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn''t support this method, it returns UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_locations_operations_list_builder()` + `logging_locations_operations_list_execute()`.
/// For task-level control, use `logging_locations_operations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_locations_operations_list(
    client: &SimpleHttpClient,
    args: &LoggingLocationsOperationsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListOperationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = logging_locations_operations_list_builder(
        client,
        &args.name,
        &args.filter,
        &args.pageSize,
        &args.pageToken,
        &args.returnPartialSuccess,
    )?;
    logging_locations_operations_list_execute(builder)
}

/// DELETE v2/{v2Id}/{v2Id1}/logs/{logsId}
/// Deletes all the log entries in a log for the global _Default Log Bucket. The log reappears if it receives new entries. Log entries written shortly before the delete operation might not be deleted. Entries received after the delete operation with a timestamp before the operation will be deleted.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_logs_delete_execute()` to send, or `logging_logs_delete` for simplest API.

pub fn logging_logs_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    logName: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/{}/{v2Id1}/logs/{logsId}",
        logName,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v2/{v2Id}/{v2Id1}/logs/{logsId}
/// Deletes all the log entries in a log for the global _Default Log Bucket. The log reappears if it receives new entries. Log entries written shortly before the delete operation might not be deleted. Entries received after the delete operation with a timestamp before the operation will be deleted.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_logs_delete_execute()` or `logging_logs_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_logs_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_logs_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v2/{v2Id}/{v2Id1}/logs/{logsId}
/// Deletes all the log entries in a log for the global _Default Log Bucket. The log reappears if it receives new entries. Log entries written shortly before the delete operation might not be deleted. Entries received after the delete operation with a timestamp before the operation will be deleted.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_logs_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_logs_delete_task()`.
/// For the simplest API, use `logging_logs_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_logs_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_logs_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = logging_logs_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_logs_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingLogsDeleteArgs {
    /// Path parameter: logName
    pub logName: String,
}

/// DELETE v2/{v2Id}/{v2Id1}/logs/{logsId}
/// Deletes all the log entries in a log for the global _Default Log Bucket. The log reappears if it receives new entries. Log entries written shortly before the delete operation might not be deleted. Entries received after the delete operation with a timestamp before the operation will be deleted.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_logs_delete_builder()` + `logging_logs_delete_execute()`.
/// For task-level control, use `logging_logs_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_logs_delete(
    client: &SimpleHttpClient,
    args: &LoggingLogsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_logs_delete_builder(client, &args.logName)?;
    logging_logs_delete_execute(builder)
}

/// GET v2/{v2Id}/{v2Id1}/logs
/// Lists the logs in projects, organizations, folders, or billing accounts. Only logs that have entries are listed.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_logs_list_execute()` to send, or `logging_logs_list` for simplest API.

pub fn logging_logs_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
    resourceNames: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!("https://logging.googleapis.com/v2/{}/{v2Id1}/logs", parent,);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = resourceNames.as_ref() {
        query_parts.push(format!("resourceNames={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/{v2Id}/{v2Id1}/logs
/// Lists the logs in projects, organizations, folders, or billing accounts. Only logs that have entries are listed.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_logs_list_execute()` or `logging_logs_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_logs_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_logs_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListLogsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListLogsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/{v2Id}/{v2Id1}/logs
/// Lists the logs in projects, organizations, folders, or billing accounts. Only logs that have entries are listed.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_logs_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_logs_list_task()`.
/// For the simplest API, use `logging_logs_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_logs_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_logs_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListLogsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = logging_logs_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_logs_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingLogsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: resourceNames
    pub resourceNames: Option<String>,
}

/// GET v2/{v2Id}/{v2Id1}/logs
/// Lists the logs in projects, organizations, folders, or billing accounts. Only logs that have entries are listed.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_logs_list_builder()` + `logging_logs_list_execute()`.
/// For task-level control, use `logging_logs_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_logs_list(
    client: &SimpleHttpClient,
    args: &LoggingLogsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListLogsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = logging_logs_list_builder(
        client,
        &args.parent,
        &args.pageSize,
        &args.pageToken,
        &args.resourceNames,
    )?;
    logging_logs_list_execute(builder)
}

/// GET v2/monitoredResourceDescriptors
/// Lists the descriptors for monitored resource types used by Logging.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_monitored_resource_descriptors_list_execute()` to send, or `logging_monitored_resource_descriptors_list` for simplest API.

pub fn logging_monitored_resource_descriptors_list_builder<R>(
    client: &SimpleHttpClient<R>,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!("https://logging.googleapis.com/v2/monitoredResourceDescriptors",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/monitoredResourceDescriptors
/// Lists the descriptors for monitored resource types used by Logging.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_monitored_resource_descriptors_list_execute()` or `logging_monitored_resource_descriptors_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_monitored_resource_descriptors_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_monitored_resource_descriptors_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListMonitoredResourceDescriptorsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListMonitoredResourceDescriptorsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/monitoredResourceDescriptors
/// Lists the descriptors for monitored resource types used by Logging.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_monitored_resource_descriptors_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_monitored_resource_descriptors_list_task()`.
/// For the simplest API, use `logging_monitored_resource_descriptors_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_monitored_resource_descriptors_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_monitored_resource_descriptors_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListMonitoredResourceDescriptorsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = logging_monitored_resource_descriptors_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_monitored_resource_descriptors_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingMonitoredResourceDescriptorsListArgs {
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v2/monitoredResourceDescriptors
/// Lists the descriptors for monitored resource types used by Logging.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_monitored_resource_descriptors_list_builder()` + `logging_monitored_resource_descriptors_list_execute()`.
/// For task-level control, use `logging_monitored_resource_descriptors_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_monitored_resource_descriptors_list(
    client: &SimpleHttpClient,
    args: &LoggingMonitoredResourceDescriptorsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListMonitoredResourceDescriptorsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = logging_monitored_resource_descriptors_list_builder(
        client,
        &args.pageSize,
        &args.pageToken,
    )?;
    logging_monitored_resource_descriptors_list_execute(builder)
}

/// GET v2/organizations/{organizationsId}/cmekSettings
/// Gets the Logging CMEK settings for the given resource.Note: CMEK for the Log Router can be configured for Google Cloud projects, folders, organizations, and billing accounts. Once configured for an organization, it applies to all projects and folders in the Google Cloud organization.See Configure CMEK for Cloud Logging (<https://docs.cloud.google.`com/logging/docs/routing/managed-encryption`>) for more information.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_organizations_get_cmek_settings_execute()` to send, or `logging_organizations_get_cmek_settings` for simplest API.

pub fn logging_organizations_get_cmek_settings_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/organizations/{}/cmekSettings",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/organizations/{organizationsId}/cmekSettings
/// Gets the Logging CMEK settings for the given resource.Note: CMEK for the Log Router can be configured for Google Cloud projects, folders, organizations, and billing accounts. Once configured for an organization, it applies to all projects and folders in the Google Cloud organization.See Configure CMEK for Cloud Logging (<https://docs.cloud.google.`com/logging/docs/routing/managed-encryption`>) for more information.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_organizations_get_cmek_settings_execute()` or `logging_organizations_get_cmek_settings`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_organizations_get_cmek_settings_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_organizations_get_cmek_settings_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<CmekSettings>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: CmekSettings = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/organizations/{organizationsId}/cmekSettings
/// Gets the Logging CMEK settings for the given resource.Note: CMEK for the Log Router can be configured for Google Cloud projects, folders, organizations, and billing accounts. Once configured for an organization, it applies to all projects and folders in the Google Cloud organization.See Configure CMEK for Cloud Logging (<https://docs.cloud.google.`com/logging/docs/routing/managed-encryption`>) for more information.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_organizations_get_cmek_settings_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_organizations_get_cmek_settings_task()`.
/// For the simplest API, use `logging_organizations_get_cmek_settings()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_organizations_get_cmek_settings_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_organizations_get_cmek_settings_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CmekSettings>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = logging_organizations_get_cmek_settings_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_organizations_get_cmek_settings`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingOrganizationsGetCmekSettingsArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v2/organizations/{organizationsId}/cmekSettings
/// Gets the Logging CMEK settings for the given resource.Note: CMEK for the Log Router can be configured for Google Cloud projects, folders, organizations, and billing accounts. Once configured for an organization, it applies to all projects and folders in the Google Cloud organization.See Configure CMEK for Cloud Logging (<https://docs.cloud.google.`com/logging/docs/routing/managed-encryption`>) for more information.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_organizations_get_cmek_settings_builder()` + `logging_organizations_get_cmek_settings_execute()`.
/// For task-level control, use `logging_organizations_get_cmek_settings_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_organizations_get_cmek_settings(
    client: &SimpleHttpClient,
    args: &LoggingOrganizationsGetCmekSettingsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CmekSettings>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = logging_organizations_get_cmek_settings_builder(client, &args.name)?;
    logging_organizations_get_cmek_settings_execute(builder)
}

/// GET v2/organizations/{organizationsId}/settings
/// Gets the settings for the given resource.Note: Settings can be retrieved for Google Cloud projects, folders, organizations, and billing accounts.See View default resource settings for Logging (<https://docs.cloud.google.`com/logging/docs/default-settings`#view-org-settings>) for more information.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_organizations_get_settings_execute()` to send, or `logging_organizations_get_settings` for simplest API.

pub fn logging_organizations_get_settings_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/organizations/{}/settings",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/organizations/{organizationsId}/settings
/// Gets the settings for the given resource.Note: Settings can be retrieved for Google Cloud projects, folders, organizations, and billing accounts.See View default resource settings for Logging (<https://docs.cloud.google.`com/logging/docs/default-settings`#view-org-settings>) for more information.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_organizations_get_settings_execute()` or `logging_organizations_get_settings`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_organizations_get_settings_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_organizations_get_settings_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Settings>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Settings = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/organizations/{organizationsId}/settings
/// Gets the settings for the given resource.Note: Settings can be retrieved for Google Cloud projects, folders, organizations, and billing accounts.See View default resource settings for Logging (<https://docs.cloud.google.`com/logging/docs/default-settings`#view-org-settings>) for more information.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_organizations_get_settings_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_organizations_get_settings_task()`.
/// For the simplest API, use `logging_organizations_get_settings()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_organizations_get_settings_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_organizations_get_settings_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Settings>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = logging_organizations_get_settings_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_organizations_get_settings`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingOrganizationsGetSettingsArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v2/organizations/{organizationsId}/settings
/// Gets the settings for the given resource.Note: Settings can be retrieved for Google Cloud projects, folders, organizations, and billing accounts.See View default resource settings for Logging (<https://docs.cloud.google.`com/logging/docs/default-settings`#view-org-settings>) for more information.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_organizations_get_settings_builder()` + `logging_organizations_get_settings_execute()`.
/// For task-level control, use `logging_organizations_get_settings_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_organizations_get_settings(
    client: &SimpleHttpClient,
    args: &LoggingOrganizationsGetSettingsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Settings>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_organizations_get_settings_builder(client, &args.name)?;
    logging_organizations_get_settings_execute(builder)
}

/// PATCH v2/organizations/{organizationsId}/cmekSettings
/// Updates the Log Router CMEK settings for the given resource.Note: CMEK for the Log Router can currently only be configured for Google Cloud organizations. Once configured, it applies to all projects and folders in the Google Cloud organization.UpdateCmekSettings fails when any of the following are `true`: The value of kms_key_name is invalid. The associated service account doesn''t have the required `roles/cloudkms`.`cryptoKeyEncrypterDecrypter` role assigned for the key. Access to the key is disabled.See Configure CMEK for Cloud Logging (<https://docs.cloud.google.`com/logging/docs/routing/managed-encryption`>) for more information.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_organizations_update_cmek_settings_execute()` to send, or `logging_organizations_update_cmek_settings` for simplest API.

pub fn logging_organizations_update_cmek_settings_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    updateMask: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/organizations/{}/cmekSettings",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v2/organizations/{organizationsId}/cmekSettings
/// Updates the Log Router CMEK settings for the given resource.Note: CMEK for the Log Router can currently only be configured for Google Cloud organizations. Once configured, it applies to all projects and folders in the Google Cloud organization.UpdateCmekSettings fails when any of the following are `true`: The value of kms_key_name is invalid. The associated service account doesn''t have the required `roles/cloudkms`.`cryptoKeyEncrypterDecrypter` role assigned for the key. Access to the key is disabled.See Configure CMEK for Cloud Logging (<https://docs.cloud.google.`com/logging/docs/routing/managed-encryption`>) for more information.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_organizations_update_cmek_settings_execute()` or `logging_organizations_update_cmek_settings`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_organizations_update_cmek_settings_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_organizations_update_cmek_settings_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<CmekSettings>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: CmekSettings = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v2/organizations/{organizationsId}/cmekSettings
/// Updates the Log Router CMEK settings for the given resource.Note: CMEK for the Log Router can currently only be configured for Google Cloud organizations. Once configured, it applies to all projects and folders in the Google Cloud organization.UpdateCmekSettings fails when any of the following are `true`: The value of kms_key_name is invalid. The associated service account doesn''t have the required `roles/cloudkms`.`cryptoKeyEncrypterDecrypter` role assigned for the key. Access to the key is disabled.See Configure CMEK for Cloud Logging (<https://docs.cloud.google.`com/logging/docs/routing/managed-encryption`>) for more information.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_organizations_update_cmek_settings_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_organizations_update_cmek_settings_task()`.
/// For the simplest API, use `logging_organizations_update_cmek_settings()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_organizations_update_cmek_settings_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_organizations_update_cmek_settings_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CmekSettings>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = logging_organizations_update_cmek_settings_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_organizations_update_cmek_settings`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingOrganizationsUpdateCmekSettingsArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
}

/// PATCH v2/organizations/{organizationsId}/cmekSettings
/// Updates the Log Router CMEK settings for the given resource.Note: CMEK for the Log Router can currently only be configured for Google Cloud organizations. Once configured, it applies to all projects and folders in the Google Cloud organization.UpdateCmekSettings fails when any of the following are `true`: The value of kms_key_name is invalid. The associated service account doesn''t have the required `roles/cloudkms`.`cryptoKeyEncrypterDecrypter` role assigned for the key. Access to the key is disabled.See Configure CMEK for Cloud Logging (<https://docs.cloud.google.`com/logging/docs/routing/managed-encryption`>) for more information.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_organizations_update_cmek_settings_builder()` + `logging_organizations_update_cmek_settings_execute()`.
/// For task-level control, use `logging_organizations_update_cmek_settings_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_organizations_update_cmek_settings(
    client: &SimpleHttpClient,
    args: &LoggingOrganizationsUpdateCmekSettingsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CmekSettings>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        logging_organizations_update_cmek_settings_builder(client, &args.name, &args.updateMask)?;
    logging_organizations_update_cmek_settings_execute(builder)
}

/// PATCH v2/organizations/{organizationsId}/settings
/// Updates the settings for the given resource. This method applies to all feature configurations for organization and folders.UpdateSettings fails when any of the following are `true`: The value of storage_location either isn''t supported by Logging or violates the location OrgPolicy. The default_sink_config field is set, but it has an unspecified filter write mode. The value of kms_key_name is invalid. The associated service account doesn''t have the required `roles/cloudkms`.`cryptoKeyEncrypterDecrypter` role assigned for the key. Access to the key is disabled.See Configure default settings for organizations and folders (<https://docs.cloud.google.`com/logging/docs/default-settings`>) for more information.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_organizations_update_settings_execute()` to send, or `logging_organizations_update_settings` for simplest API.

pub fn logging_organizations_update_settings_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    updateMask: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/organizations/{}/settings",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v2/organizations/{organizationsId}/settings
/// Updates the settings for the given resource. This method applies to all feature configurations for organization and folders.UpdateSettings fails when any of the following are `true`: The value of storage_location either isn''t supported by Logging or violates the location OrgPolicy. The default_sink_config field is set, but it has an unspecified filter write mode. The value of kms_key_name is invalid. The associated service account doesn''t have the required `roles/cloudkms`.`cryptoKeyEncrypterDecrypter` role assigned for the key. Access to the key is disabled.See Configure default settings for organizations and folders (<https://docs.cloud.google.`com/logging/docs/default-settings`>) for more information.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_organizations_update_settings_execute()` or `logging_organizations_update_settings`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_organizations_update_settings_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_organizations_update_settings_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Settings>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Settings = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v2/organizations/{organizationsId}/settings
/// Updates the settings for the given resource. This method applies to all feature configurations for organization and folders.UpdateSettings fails when any of the following are `true`: The value of storage_location either isn''t supported by Logging or violates the location OrgPolicy. The default_sink_config field is set, but it has an unspecified filter write mode. The value of kms_key_name is invalid. The associated service account doesn''t have the required `roles/cloudkms`.`cryptoKeyEncrypterDecrypter` role assigned for the key. Access to the key is disabled.See Configure default settings for organizations and folders (<https://docs.cloud.google.`com/logging/docs/default-settings`>) for more information.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_organizations_update_settings_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_organizations_update_settings_task()`.
/// For the simplest API, use `logging_organizations_update_settings()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_organizations_update_settings_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_organizations_update_settings_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Settings>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = logging_organizations_update_settings_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_organizations_update_settings`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingOrganizationsUpdateSettingsArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
}

/// PATCH v2/organizations/{organizationsId}/settings
/// Updates the settings for the given resource. This method applies to all feature configurations for organization and folders.UpdateSettings fails when any of the following are `true`: The value of storage_location either isn''t supported by Logging or violates the location OrgPolicy. The default_sink_config field is set, but it has an unspecified filter write mode. The value of kms_key_name is invalid. The associated service account doesn''t have the required `roles/cloudkms`.`cryptoKeyEncrypterDecrypter` role assigned for the key. Access to the key is disabled.See Configure default settings for organizations and folders (<https://docs.cloud.google.`com/logging/docs/default-settings`>) for more information.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_organizations_update_settings_builder()` + `logging_organizations_update_settings_execute()`.
/// For task-level control, use `logging_organizations_update_settings_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_organizations_update_settings(
    client: &SimpleHttpClient,
    args: &LoggingOrganizationsUpdateSettingsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Settings>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        logging_organizations_update_settings_builder(client, &args.name, &args.updateMask)?;
    logging_organizations_update_settings_execute(builder)
}

/// POST v2/organizations/{organizationsId}/exclusions
/// Creates a new exclusion in the _Default sink in a specified parent resource. Only log entries belonging to that resource can be excluded. You can have up to 10 exclusions in a resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_organizations_exclusions_create_execute()` to send, or `logging_organizations_exclusions_create` for simplest API.

pub fn logging_organizations_exclusions_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/organizations/{}/exclusions",
        parent,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v2/organizations/{organizationsId}/exclusions
/// Creates a new exclusion in the _Default sink in a specified parent resource. Only log entries belonging to that resource can be excluded. You can have up to 10 exclusions in a resource.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_organizations_exclusions_create_execute()` or `logging_organizations_exclusions_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_organizations_exclusions_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_organizations_exclusions_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<LogExclusion>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LogExclusion = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v2/organizations/{organizationsId}/exclusions
/// Creates a new exclusion in the _Default sink in a specified parent resource. Only log entries belonging to that resource can be excluded. You can have up to 10 exclusions in a resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_organizations_exclusions_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_organizations_exclusions_create_task()`.
/// For the simplest API, use `logging_organizations_exclusions_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_organizations_exclusions_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_organizations_exclusions_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogExclusion>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = logging_organizations_exclusions_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_organizations_exclusions_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingOrganizationsExclusionsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// POST v2/organizations/{organizationsId}/exclusions
/// Creates a new exclusion in the _Default sink in a specified parent resource. Only log entries belonging to that resource can be excluded. You can have up to 10 exclusions in a resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_organizations_exclusions_create_builder()` + `logging_organizations_exclusions_create_execute()`.
/// For task-level control, use `logging_organizations_exclusions_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_organizations_exclusions_create(
    client: &SimpleHttpClient,
    args: &LoggingOrganizationsExclusionsCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogExclusion>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = logging_organizations_exclusions_create_builder(client, &args.parent)?;
    logging_organizations_exclusions_create_execute(builder)
}

/// DELETE v2/organizations/{organizationsId}/exclusions/{exclusionsId}
/// Deletes an exclusion in the _Default sink.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_organizations_exclusions_delete_execute()` to send, or `logging_organizations_exclusions_delete` for simplest API.

pub fn logging_organizations_exclusions_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/organizations/{}/exclusions/{exclusionsId}",
        name,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v2/organizations/{organizationsId}/exclusions/{exclusionsId}
/// Deletes an exclusion in the _Default sink.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_organizations_exclusions_delete_execute()` or `logging_organizations_exclusions_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_organizations_exclusions_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_organizations_exclusions_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v2/organizations/{organizationsId}/exclusions/{exclusionsId}
/// Deletes an exclusion in the _Default sink.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_organizations_exclusions_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_organizations_exclusions_delete_task()`.
/// For the simplest API, use `logging_organizations_exclusions_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_organizations_exclusions_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_organizations_exclusions_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = logging_organizations_exclusions_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_organizations_exclusions_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingOrganizationsExclusionsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// DELETE v2/organizations/{organizationsId}/exclusions/{exclusionsId}
/// Deletes an exclusion in the _Default sink.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_organizations_exclusions_delete_builder()` + `logging_organizations_exclusions_delete_execute()`.
/// For task-level control, use `logging_organizations_exclusions_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_organizations_exclusions_delete(
    client: &SimpleHttpClient,
    args: &LoggingOrganizationsExclusionsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_organizations_exclusions_delete_builder(client, &args.name)?;
    logging_organizations_exclusions_delete_execute(builder)
}

/// GET v2/organizations/{organizationsId}/exclusions/{exclusionsId}
/// Gets the description of an exclusion in the _Default sink.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_organizations_exclusions_get_execute()` to send, or `logging_organizations_exclusions_get` for simplest API.

pub fn logging_organizations_exclusions_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/organizations/{}/exclusions/{exclusionsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/organizations/{organizationsId}/exclusions/{exclusionsId}
/// Gets the description of an exclusion in the _Default sink.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_organizations_exclusions_get_execute()` or `logging_organizations_exclusions_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_organizations_exclusions_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_organizations_exclusions_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<LogExclusion>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LogExclusion = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/organizations/{organizationsId}/exclusions/{exclusionsId}
/// Gets the description of an exclusion in the _Default sink.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_organizations_exclusions_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_organizations_exclusions_get_task()`.
/// For the simplest API, use `logging_organizations_exclusions_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_organizations_exclusions_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_organizations_exclusions_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogExclusion>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = logging_organizations_exclusions_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_organizations_exclusions_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingOrganizationsExclusionsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v2/organizations/{organizationsId}/exclusions/{exclusionsId}
/// Gets the description of an exclusion in the _Default sink.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_organizations_exclusions_get_builder()` + `logging_organizations_exclusions_get_execute()`.
/// For task-level control, use `logging_organizations_exclusions_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_organizations_exclusions_get(
    client: &SimpleHttpClient,
    args: &LoggingOrganizationsExclusionsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogExclusion>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = logging_organizations_exclusions_get_builder(client, &args.name)?;
    logging_organizations_exclusions_get_execute(builder)
}

/// GET v2/organizations/{organizationsId}/exclusions
/// Lists all the exclusions on the _Default sink in a parent resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_organizations_exclusions_list_execute()` to send, or `logging_organizations_exclusions_list` for simplest API.

pub fn logging_organizations_exclusions_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/organizations/{}/exclusions",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/organizations/{organizationsId}/exclusions
/// Lists all the exclusions on the _Default sink in a parent resource.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_organizations_exclusions_list_execute()` or `logging_organizations_exclusions_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_organizations_exclusions_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_organizations_exclusions_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListExclusionsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListExclusionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/organizations/{organizationsId}/exclusions
/// Lists all the exclusions on the _Default sink in a parent resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_organizations_exclusions_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_organizations_exclusions_list_task()`.
/// For the simplest API, use `logging_organizations_exclusions_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_organizations_exclusions_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_organizations_exclusions_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListExclusionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = logging_organizations_exclusions_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_organizations_exclusions_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingOrganizationsExclusionsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v2/organizations/{organizationsId}/exclusions
/// Lists all the exclusions on the _Default sink in a parent resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_organizations_exclusions_list_builder()` + `logging_organizations_exclusions_list_execute()`.
/// For task-level control, use `logging_organizations_exclusions_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_organizations_exclusions_list(
    client: &SimpleHttpClient,
    args: &LoggingOrganizationsExclusionsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListExclusionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = logging_organizations_exclusions_list_builder(
        client,
        &args.parent,
        &args.pageSize,
        &args.pageToken,
    )?;
    logging_organizations_exclusions_list_execute(builder)
}

/// PATCH v2/organizations/{organizationsId}/exclusions/{exclusionsId}
/// Changes one or more properties of an existing exclusion in the _Default sink.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_organizations_exclusions_patch_execute()` to send, or `logging_organizations_exclusions_patch` for simplest API.

pub fn logging_organizations_exclusions_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    updateMask: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/organizations/{}/exclusions/{exclusionsId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v2/organizations/{organizationsId}/exclusions/{exclusionsId}
/// Changes one or more properties of an existing exclusion in the _Default sink.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_organizations_exclusions_patch_execute()` or `logging_organizations_exclusions_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_organizations_exclusions_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_organizations_exclusions_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<LogExclusion>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LogExclusion = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v2/organizations/{organizationsId}/exclusions/{exclusionsId}
/// Changes one or more properties of an existing exclusion in the _Default sink.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_organizations_exclusions_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_organizations_exclusions_patch_task()`.
/// For the simplest API, use `logging_organizations_exclusions_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_organizations_exclusions_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_organizations_exclusions_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogExclusion>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = logging_organizations_exclusions_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_organizations_exclusions_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingOrganizationsExclusionsPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
}

/// PATCH v2/organizations/{organizationsId}/exclusions/{exclusionsId}
/// Changes one or more properties of an existing exclusion in the _Default sink.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_organizations_exclusions_patch_builder()` + `logging_organizations_exclusions_patch_execute()`.
/// For task-level control, use `logging_organizations_exclusions_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_organizations_exclusions_patch(
    client: &SimpleHttpClient,
    args: &LoggingOrganizationsExclusionsPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogExclusion>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        logging_organizations_exclusions_patch_builder(client, &args.name, &args.updateMask)?;
    logging_organizations_exclusions_patch_execute(builder)
}

/// GET v2/organizations/{organizationsId}/locations/{locationsId}
/// Gets information about a location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_organizations_locations_get_execute()` to send, or `logging_organizations_locations_get` for simplest API.

pub fn logging_organizations_locations_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/organizations/{}/locations/{locationsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/organizations/{organizationsId}/locations/{locationsId}
/// Gets information about a location.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_organizations_locations_get_execute()` or `logging_organizations_locations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_organizations_locations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_organizations_locations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Location>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Location = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/organizations/{organizationsId}/locations/{locationsId}
/// Gets information about a location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_organizations_locations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_organizations_locations_get_task()`.
/// For the simplest API, use `logging_organizations_locations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_organizations_locations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_organizations_locations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Location>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = logging_organizations_locations_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_organizations_locations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingOrganizationsLocationsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v2/organizations/{organizationsId}/locations/{locationsId}
/// Gets information about a location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_organizations_locations_get_builder()` + `logging_organizations_locations_get_execute()`.
/// For task-level control, use `logging_organizations_locations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_organizations_locations_get(
    client: &SimpleHttpClient,
    args: &LoggingOrganizationsLocationsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Location>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_organizations_locations_get_builder(client, &args.name)?;
    logging_organizations_locations_get_execute(builder)
}

/// GET v2/organizations/{organizationsId}/locations
/// Lists information about the supported locations for this service.This method lists locations based on the resource scope provided in the ListLocationsRequest.name field: Global locations: If name is empty, the method lists the public locations available to all projects. Project-specific locations: If name follows the format `projects/{project}`, the method lists locations visible to that specific project. This includes public, private, or other project-specific locations enabled for the project.For `gRPC` and client library implementations, the resource name is passed as the name field. For direct service calls, the resource name is incorporated into the request path based on the specific service implementation and version.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_organizations_locations_list_execute()` to send, or `logging_organizations_locations_list` for simplest API.

pub fn logging_organizations_locations_list_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    extraLocationTypes: &Option<String>,
    filter: &Option<String>,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/organizations/{}/locations",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = extraLocationTypes.as_ref() {
        query_parts.push(format!("extraLocationTypes={}", val));
    }
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/organizations/{organizationsId}/locations
/// Lists information about the supported locations for this service.This method lists locations based on the resource scope provided in the ListLocationsRequest.name field: Global locations: If name is empty, the method lists the public locations available to all projects. Project-specific locations: If name follows the format `projects/{project}`, the method lists locations visible to that specific project. This includes public, private, or other project-specific locations enabled for the project.For `gRPC` and client library implementations, the resource name is passed as the name field. For direct service calls, the resource name is incorporated into the request path based on the specific service implementation and version.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_organizations_locations_list_execute()` or `logging_organizations_locations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_organizations_locations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_organizations_locations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListLocationsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListLocationsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/organizations/{organizationsId}/locations
/// Lists information about the supported locations for this service.This method lists locations based on the resource scope provided in the ListLocationsRequest.name field: Global locations: If name is empty, the method lists the public locations available to all projects. Project-specific locations: If name follows the format `projects/{project}`, the method lists locations visible to that specific project. This includes public, private, or other project-specific locations enabled for the project.For `gRPC` and client library implementations, the resource name is passed as the name field. For direct service calls, the resource name is incorporated into the request path based on the specific service implementation and version.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_organizations_locations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_organizations_locations_list_task()`.
/// For the simplest API, use `logging_organizations_locations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_organizations_locations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_organizations_locations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListLocationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = logging_organizations_locations_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_organizations_locations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingOrganizationsLocationsListArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: extraLocationTypes
    pub extraLocationTypes: Option<String>,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v2/organizations/{organizationsId}/locations
/// Lists information about the supported locations for this service.This method lists locations based on the resource scope provided in the ListLocationsRequest.name field: Global locations: If name is empty, the method lists the public locations available to all projects. Project-specific locations: If name follows the format `projects/{project}`, the method lists locations visible to that specific project. This includes public, private, or other project-specific locations enabled for the project.For `gRPC` and client library implementations, the resource name is passed as the name field. For direct service calls, the resource name is incorporated into the request path based on the specific service implementation and version.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_organizations_locations_list_builder()` + `logging_organizations_locations_list_execute()`.
/// For task-level control, use `logging_organizations_locations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_organizations_locations_list(
    client: &SimpleHttpClient,
    args: &LoggingOrganizationsLocationsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListLocationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = logging_organizations_locations_list_builder(
        client,
        &args.name,
        &args.extraLocationTypes,
        &args.filter,
        &args.pageSize,
        &args.pageToken,
    )?;
    logging_organizations_locations_list_execute(builder)
}

/// POST v2/organizations/{organizationsId}/locations/{locationsId}/buckets
/// Creates a log bucket that can be used to store log entries. After a bucket has been created, the bucket''s location cannot be changed.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_organizations_locations_buckets_create_execute()` to send, or `logging_organizations_locations_buckets_create` for simplest API.

pub fn logging_organizations_locations_buckets_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    bucketId: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/organizations/{}/locations/{locationsId}/buckets",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = bucketId.as_ref() {
        query_parts.push(format!("bucketId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v2/organizations/{organizationsId}/locations/{locationsId}/buckets
/// Creates a log bucket that can be used to store log entries. After a bucket has been created, the bucket''s location cannot be changed.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_organizations_locations_buckets_create_execute()` or `logging_organizations_locations_buckets_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_organizations_locations_buckets_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_organizations_locations_buckets_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<LogBucket>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LogBucket = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v2/organizations/{organizationsId}/locations/{locationsId}/buckets
/// Creates a log bucket that can be used to store log entries. After a bucket has been created, the bucket''s location cannot be changed.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_organizations_locations_buckets_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_organizations_locations_buckets_create_task()`.
/// For the simplest API, use `logging_organizations_locations_buckets_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_organizations_locations_buckets_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_organizations_locations_buckets_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogBucket>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = logging_organizations_locations_buckets_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_organizations_locations_buckets_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingOrganizationsLocationsBucketsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: bucketId
    pub bucketId: Option<String>,
}

/// POST v2/organizations/{organizationsId}/locations/{locationsId}/buckets
/// Creates a log bucket that can be used to store log entries. After a bucket has been created, the bucket''s location cannot be changed.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_organizations_locations_buckets_create_builder()` + `logging_organizations_locations_buckets_create_execute()`.
/// For task-level control, use `logging_organizations_locations_buckets_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_organizations_locations_buckets_create(
    client: &SimpleHttpClient,
    args: &LoggingOrganizationsLocationsBucketsCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogBucket>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_organizations_locations_buckets_create_builder(
        client,
        &args.parent,
        &args.bucketId,
    )?;
    logging_organizations_locations_buckets_create_execute(builder)
}

/// POST v2/organizations/{organizationsId}/locations/{locationsId}/buckets:createAsync
/// Creates a log bucket asynchronously that can be used to store log entries.After a bucket has been created, the bucket''s location cannot be changed.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_organizations_locations_buckets_create_async_execute()` to send, or `logging_organizations_locations_buckets_create_async` for simplest API.

pub fn logging_organizations_locations_buckets_create_async_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    bucketId: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/organizations/{}/locations/{locationsId}/buckets:createAsync",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = bucketId.as_ref() {
        query_parts.push(format!("bucketId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v2/organizations/{organizationsId}/locations/{locationsId}/buckets:createAsync
/// Creates a log bucket asynchronously that can be used to store log entries.After a bucket has been created, the bucket''s location cannot be changed.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_organizations_locations_buckets_create_async_execute()` or `logging_organizations_locations_buckets_create_async`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_organizations_locations_buckets_create_async_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_organizations_locations_buckets_create_async_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v2/organizations/{organizationsId}/locations/{locationsId}/buckets:createAsync
/// Creates a log bucket asynchronously that can be used to store log entries.After a bucket has been created, the bucket''s location cannot be changed.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_organizations_locations_buckets_create_async_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_organizations_locations_buckets_create_async_task()`.
/// For the simplest API, use `logging_organizations_locations_buckets_create_async()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_organizations_locations_buckets_create_async_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_organizations_locations_buckets_create_async_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = logging_organizations_locations_buckets_create_async_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_organizations_locations_buckets_create_async`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingOrganizationsLocationsBucketsCreateAsyncArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: bucketId
    pub bucketId: Option<String>,
}

/// POST v2/organizations/{organizationsId}/locations/{locationsId}/buckets:createAsync
/// Creates a log bucket asynchronously that can be used to store log entries.After a bucket has been created, the bucket''s location cannot be changed.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_organizations_locations_buckets_create_async_builder()` + `logging_organizations_locations_buckets_create_async_execute()`.
/// For task-level control, use `logging_organizations_locations_buckets_create_async_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_organizations_locations_buckets_create_async(
    client: &SimpleHttpClient,
    args: &LoggingOrganizationsLocationsBucketsCreateAsyncArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_organizations_locations_buckets_create_async_builder(
        client,
        &args.parent,
        &args.bucketId,
    )?;
    logging_organizations_locations_buckets_create_async_execute(builder)
}

/// DELETE v2/organizations/{organizationsId}/locations/{locationsId}/buckets/{bucketsId}
/// Deletes a log bucket.Changes the bucket''s lifecycle_state to the DELETE_REQUESTED state. After 7 days, the bucket will be purged and all log entries in the bucket will be permanently deleted.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_organizations_locations_buckets_delete_execute()` to send, or `logging_organizations_locations_buckets_delete` for simplest API.

pub fn logging_organizations_locations_buckets_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/organizations/{}/locations/{locationsId}/buckets/{bucketsId}",
        name,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v2/organizations/{organizationsId}/locations/{locationsId}/buckets/{bucketsId}
/// Deletes a log bucket.Changes the bucket''s lifecycle_state to the DELETE_REQUESTED state. After 7 days, the bucket will be purged and all log entries in the bucket will be permanently deleted.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_organizations_locations_buckets_delete_execute()` or `logging_organizations_locations_buckets_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_organizations_locations_buckets_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_organizations_locations_buckets_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v2/organizations/{organizationsId}/locations/{locationsId}/buckets/{bucketsId}
/// Deletes a log bucket.Changes the bucket''s lifecycle_state to the DELETE_REQUESTED state. After 7 days, the bucket will be purged and all log entries in the bucket will be permanently deleted.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_organizations_locations_buckets_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_organizations_locations_buckets_delete_task()`.
/// For the simplest API, use `logging_organizations_locations_buckets_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_organizations_locations_buckets_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_organizations_locations_buckets_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = logging_organizations_locations_buckets_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_organizations_locations_buckets_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingOrganizationsLocationsBucketsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// DELETE v2/organizations/{organizationsId}/locations/{locationsId}/buckets/{bucketsId}
/// Deletes a log bucket.Changes the bucket''s lifecycle_state to the DELETE_REQUESTED state. After 7 days, the bucket will be purged and all log entries in the bucket will be permanently deleted.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_organizations_locations_buckets_delete_builder()` + `logging_organizations_locations_buckets_delete_execute()`.
/// For task-level control, use `logging_organizations_locations_buckets_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_organizations_locations_buckets_delete(
    client: &SimpleHttpClient,
    args: &LoggingOrganizationsLocationsBucketsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_organizations_locations_buckets_delete_builder(client, &args.name)?;
    logging_organizations_locations_buckets_delete_execute(builder)
}

/// GET v2/organizations/{organizationsId}/locations/{locationsId}/buckets/{bucketsId}
/// Gets a log bucket.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_organizations_locations_buckets_get_execute()` to send, or `logging_organizations_locations_buckets_get` for simplest API.

pub fn logging_organizations_locations_buckets_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/organizations/{}/locations/{locationsId}/buckets/{bucketsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/organizations/{organizationsId}/locations/{locationsId}/buckets/{bucketsId}
/// Gets a log bucket.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_organizations_locations_buckets_get_execute()` or `logging_organizations_locations_buckets_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_organizations_locations_buckets_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_organizations_locations_buckets_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<LogBucket>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LogBucket = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/organizations/{organizationsId}/locations/{locationsId}/buckets/{bucketsId}
/// Gets a log bucket.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_organizations_locations_buckets_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_organizations_locations_buckets_get_task()`.
/// For the simplest API, use `logging_organizations_locations_buckets_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_organizations_locations_buckets_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_organizations_locations_buckets_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogBucket>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = logging_organizations_locations_buckets_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_organizations_locations_buckets_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingOrganizationsLocationsBucketsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v2/organizations/{organizationsId}/locations/{locationsId}/buckets/{bucketsId}
/// Gets a log bucket.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_organizations_locations_buckets_get_builder()` + `logging_organizations_locations_buckets_get_execute()`.
/// For task-level control, use `logging_organizations_locations_buckets_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_organizations_locations_buckets_get(
    client: &SimpleHttpClient,
    args: &LoggingOrganizationsLocationsBucketsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogBucket>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_organizations_locations_buckets_get_builder(client, &args.name)?;
    logging_organizations_locations_buckets_get_execute(builder)
}

/// GET v2/organizations/{organizationsId}/locations/{locationsId}/buckets
/// Lists log buckets.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_organizations_locations_buckets_list_execute()` to send, or `logging_organizations_locations_buckets_list` for simplest API.

pub fn logging_organizations_locations_buckets_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/organizations/{}/locations/{locationsId}/buckets",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/organizations/{organizationsId}/locations/{locationsId}/buckets
/// Lists log buckets.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_organizations_locations_buckets_list_execute()` or `logging_organizations_locations_buckets_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_organizations_locations_buckets_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_organizations_locations_buckets_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListBucketsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListBucketsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/organizations/{organizationsId}/locations/{locationsId}/buckets
/// Lists log buckets.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_organizations_locations_buckets_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_organizations_locations_buckets_list_task()`.
/// For the simplest API, use `logging_organizations_locations_buckets_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_organizations_locations_buckets_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_organizations_locations_buckets_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListBucketsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = logging_organizations_locations_buckets_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_organizations_locations_buckets_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingOrganizationsLocationsBucketsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v2/organizations/{organizationsId}/locations/{locationsId}/buckets
/// Lists log buckets.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_organizations_locations_buckets_list_builder()` + `logging_organizations_locations_buckets_list_execute()`.
/// For task-level control, use `logging_organizations_locations_buckets_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_organizations_locations_buckets_list(
    client: &SimpleHttpClient,
    args: &LoggingOrganizationsLocationsBucketsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListBucketsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = logging_organizations_locations_buckets_list_builder(
        client,
        &args.parent,
        &args.pageSize,
        &args.pageToken,
    )?;
    logging_organizations_locations_buckets_list_execute(builder)
}

/// PATCH v2/organizations/{organizationsId}/locations/{locationsId}/buckets/{bucketsId}
/// Updates a log bucket.If the bucket has a lifecycle_state of DELETE_REQUESTED, then FAILED_PRECONDITION will be returned.After a bucket has been created, the bucket''s location cannot be changed.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_organizations_locations_buckets_patch_execute()` to send, or `logging_organizations_locations_buckets_patch` for simplest API.

pub fn logging_organizations_locations_buckets_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    updateMask: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/organizations/{}/locations/{locationsId}/buckets/{bucketsId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v2/organizations/{organizationsId}/locations/{locationsId}/buckets/{bucketsId}
/// Updates a log bucket.If the bucket has a lifecycle_state of DELETE_REQUESTED, then FAILED_PRECONDITION will be returned.After a bucket has been created, the bucket''s location cannot be changed.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_organizations_locations_buckets_patch_execute()` or `logging_organizations_locations_buckets_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_organizations_locations_buckets_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_organizations_locations_buckets_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<LogBucket>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LogBucket = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v2/organizations/{organizationsId}/locations/{locationsId}/buckets/{bucketsId}
/// Updates a log bucket.If the bucket has a lifecycle_state of DELETE_REQUESTED, then FAILED_PRECONDITION will be returned.After a bucket has been created, the bucket''s location cannot be changed.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_organizations_locations_buckets_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_organizations_locations_buckets_patch_task()`.
/// For the simplest API, use `logging_organizations_locations_buckets_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_organizations_locations_buckets_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_organizations_locations_buckets_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogBucket>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = logging_organizations_locations_buckets_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_organizations_locations_buckets_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingOrganizationsLocationsBucketsPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
}

/// PATCH v2/organizations/{organizationsId}/locations/{locationsId}/buckets/{bucketsId}
/// Updates a log bucket.If the bucket has a lifecycle_state of DELETE_REQUESTED, then FAILED_PRECONDITION will be returned.After a bucket has been created, the bucket''s location cannot be changed.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_organizations_locations_buckets_patch_builder()` + `logging_organizations_locations_buckets_patch_execute()`.
/// For task-level control, use `logging_organizations_locations_buckets_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_organizations_locations_buckets_patch(
    client: &SimpleHttpClient,
    args: &LoggingOrganizationsLocationsBucketsPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogBucket>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_organizations_locations_buckets_patch_builder(
        client,
        &args.name,
        &args.updateMask,
    )?;
    logging_organizations_locations_buckets_patch_execute(builder)
}

/// POST v2/organizations/{organizationsId}/locations/{locationsId}/buckets/{bucketsId}:undelete
/// Undeletes a log bucket. A bucket that has been deleted can be undeleted within the grace period of 7 days.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_organizations_locations_buckets_undelete_execute()` to send, or `logging_organizations_locations_buckets_undelete` for simplest API.

pub fn logging_organizations_locations_buckets_undelete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/organizations/{}/locations/{locationsId}/buckets/{bucketsId}:undelete",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v2/organizations/{organizationsId}/locations/{locationsId}/buckets/{bucketsId}:undelete
/// Undeletes a log bucket. A bucket that has been deleted can be undeleted within the grace period of 7 days.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_organizations_locations_buckets_undelete_execute()` or `logging_organizations_locations_buckets_undelete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_organizations_locations_buckets_undelete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_organizations_locations_buckets_undelete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v2/organizations/{organizationsId}/locations/{locationsId}/buckets/{bucketsId}:undelete
/// Undeletes a log bucket. A bucket that has been deleted can be undeleted within the grace period of 7 days.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_organizations_locations_buckets_undelete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_organizations_locations_buckets_undelete_task()`.
/// For the simplest API, use `logging_organizations_locations_buckets_undelete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_organizations_locations_buckets_undelete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_organizations_locations_buckets_undelete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = logging_organizations_locations_buckets_undelete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_organizations_locations_buckets_undelete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingOrganizationsLocationsBucketsUndeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v2/organizations/{organizationsId}/locations/{locationsId}/buckets/{bucketsId}:undelete
/// Undeletes a log bucket. A bucket that has been deleted can be undeleted within the grace period of 7 days.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_organizations_locations_buckets_undelete_builder()` + `logging_organizations_locations_buckets_undelete_execute()`.
/// For task-level control, use `logging_organizations_locations_buckets_undelete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_organizations_locations_buckets_undelete(
    client: &SimpleHttpClient,
    args: &LoggingOrganizationsLocationsBucketsUndeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_organizations_locations_buckets_undelete_builder(client, &args.name)?;
    logging_organizations_locations_buckets_undelete_execute(builder)
}

/// POST v2/organizations/{organizationsId}/locations/{locationsId}/buckets/{bucketsId}:updateAsync
/// Updates a log bucket asynchronously.If the bucket has a lifecycle_state of DELETE_REQUESTED, then FAILED_PRECONDITION will be returned.After a bucket has been created, the bucket''s location cannot be changed.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_organizations_locations_buckets_update_async_execute()` to send, or `logging_organizations_locations_buckets_update_async` for simplest API.

pub fn logging_organizations_locations_buckets_update_async_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    updateMask: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/organizations/{}/locations/{locationsId}/buckets/{bucketsId}:updateAsync",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v2/organizations/{organizationsId}/locations/{locationsId}/buckets/{bucketsId}:updateAsync
/// Updates a log bucket asynchronously.If the bucket has a lifecycle_state of DELETE_REQUESTED, then FAILED_PRECONDITION will be returned.After a bucket has been created, the bucket''s location cannot be changed.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_organizations_locations_buckets_update_async_execute()` or `logging_organizations_locations_buckets_update_async`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_organizations_locations_buckets_update_async_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_organizations_locations_buckets_update_async_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v2/organizations/{organizationsId}/locations/{locationsId}/buckets/{bucketsId}:updateAsync
/// Updates a log bucket asynchronously.If the bucket has a lifecycle_state of DELETE_REQUESTED, then FAILED_PRECONDITION will be returned.After a bucket has been created, the bucket''s location cannot be changed.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_organizations_locations_buckets_update_async_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_organizations_locations_buckets_update_async_task()`.
/// For the simplest API, use `logging_organizations_locations_buckets_update_async()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_organizations_locations_buckets_update_async_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_organizations_locations_buckets_update_async_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = logging_organizations_locations_buckets_update_async_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_organizations_locations_buckets_update_async`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingOrganizationsLocationsBucketsUpdateAsyncArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
}

/// POST v2/organizations/{organizationsId}/locations/{locationsId}/buckets/{bucketsId}:updateAsync
/// Updates a log bucket asynchronously.If the bucket has a lifecycle_state of DELETE_REQUESTED, then FAILED_PRECONDITION will be returned.After a bucket has been created, the bucket''s location cannot be changed.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_organizations_locations_buckets_update_async_builder()` + `logging_organizations_locations_buckets_update_async_execute()`.
/// For task-level control, use `logging_organizations_locations_buckets_update_async_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_organizations_locations_buckets_update_async(
    client: &SimpleHttpClient,
    args: &LoggingOrganizationsLocationsBucketsUpdateAsyncArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_organizations_locations_buckets_update_async_builder(
        client,
        &args.name,
        &args.updateMask,
    )?;
    logging_organizations_locations_buckets_update_async_execute(builder)
}

/// POST v2/organizations/{organizationsId}/locations/{locationsId}/buckets/{bucketsId}/links
/// Asynchronously creates a linked dataset in BigQuery which makes it possible to use BigQuery to read the logs stored in the log bucket. A log bucket may currently only contain one link.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_organizations_locations_buckets_links_create_execute()` to send, or `logging_organizations_locations_buckets_links_create` for simplest API.

pub fn logging_organizations_locations_buckets_links_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    linkId: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/organizations/{}/locations/{locationsId}/buckets/{bucketsId}/links",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = linkId.as_ref() {
        query_parts.push(format!("linkId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v2/organizations/{organizationsId}/locations/{locationsId}/buckets/{bucketsId}/links
/// Asynchronously creates a linked dataset in BigQuery which makes it possible to use BigQuery to read the logs stored in the log bucket. A log bucket may currently only contain one link.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_organizations_locations_buckets_links_create_execute()` or `logging_organizations_locations_buckets_links_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_organizations_locations_buckets_links_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_organizations_locations_buckets_links_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v2/organizations/{organizationsId}/locations/{locationsId}/buckets/{bucketsId}/links
/// Asynchronously creates a linked dataset in BigQuery which makes it possible to use BigQuery to read the logs stored in the log bucket. A log bucket may currently only contain one link.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_organizations_locations_buckets_links_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_organizations_locations_buckets_links_create_task()`.
/// For the simplest API, use `logging_organizations_locations_buckets_links_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_organizations_locations_buckets_links_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_organizations_locations_buckets_links_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = logging_organizations_locations_buckets_links_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_organizations_locations_buckets_links_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingOrganizationsLocationsBucketsLinksCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: linkId
    pub linkId: Option<String>,
}

/// POST v2/organizations/{organizationsId}/locations/{locationsId}/buckets/{bucketsId}/links
/// Asynchronously creates a linked dataset in BigQuery which makes it possible to use BigQuery to read the logs stored in the log bucket. A log bucket may currently only contain one link.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_organizations_locations_buckets_links_create_builder()` + `logging_organizations_locations_buckets_links_create_execute()`.
/// For task-level control, use `logging_organizations_locations_buckets_links_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_organizations_locations_buckets_links_create(
    client: &SimpleHttpClient,
    args: &LoggingOrganizationsLocationsBucketsLinksCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_organizations_locations_buckets_links_create_builder(
        client,
        &args.parent,
        &args.linkId,
    )?;
    logging_organizations_locations_buckets_links_create_execute(builder)
}

/// DELETE v2/organizations/{organizationsId}/locations/{locationsId}/buckets/{bucketsId}/links/{linksId}
/// Deletes a link. This will also delete the corresponding BigQuery linked dataset.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_organizations_locations_buckets_links_delete_execute()` to send, or `logging_organizations_locations_buckets_links_delete` for simplest API.

pub fn logging_organizations_locations_buckets_links_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/organizations/{}/locations/{locationsId}/buckets/{bucketsId}/links/{linksId}",
        name,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v2/organizations/{organizationsId}/locations/{locationsId}/buckets/{bucketsId}/links/{linksId}
/// Deletes a link. This will also delete the corresponding BigQuery linked dataset.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_organizations_locations_buckets_links_delete_execute()` or `logging_organizations_locations_buckets_links_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_organizations_locations_buckets_links_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_organizations_locations_buckets_links_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v2/organizations/{organizationsId}/locations/{locationsId}/buckets/{bucketsId}/links/{linksId}
/// Deletes a link. This will also delete the corresponding BigQuery linked dataset.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_organizations_locations_buckets_links_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_organizations_locations_buckets_links_delete_task()`.
/// For the simplest API, use `logging_organizations_locations_buckets_links_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_organizations_locations_buckets_links_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_organizations_locations_buckets_links_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = logging_organizations_locations_buckets_links_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_organizations_locations_buckets_links_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingOrganizationsLocationsBucketsLinksDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// DELETE v2/organizations/{organizationsId}/locations/{locationsId}/buckets/{bucketsId}/links/{linksId}
/// Deletes a link. This will also delete the corresponding BigQuery linked dataset.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_organizations_locations_buckets_links_delete_builder()` + `logging_organizations_locations_buckets_links_delete_execute()`.
/// For task-level control, use `logging_organizations_locations_buckets_links_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_organizations_locations_buckets_links_delete(
    client: &SimpleHttpClient,
    args: &LoggingOrganizationsLocationsBucketsLinksDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_organizations_locations_buckets_links_delete_builder(client, &args.name)?;
    logging_organizations_locations_buckets_links_delete_execute(builder)
}

/// GET v2/organizations/{organizationsId}/locations/{locationsId}/buckets/{bucketsId}/links/{linksId}
/// Gets a link.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_organizations_locations_buckets_links_get_execute()` to send, or `logging_organizations_locations_buckets_links_get` for simplest API.

pub fn logging_organizations_locations_buckets_links_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/organizations/{}/locations/{locationsId}/buckets/{bucketsId}/links/{linksId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/organizations/{organizationsId}/locations/{locationsId}/buckets/{bucketsId}/links/{linksId}
/// Gets a link.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_organizations_locations_buckets_links_get_execute()` or `logging_organizations_locations_buckets_links_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_organizations_locations_buckets_links_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_organizations_locations_buckets_links_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Link>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Link = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/organizations/{organizationsId}/locations/{locationsId}/buckets/{bucketsId}/links/{linksId}
/// Gets a link.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_organizations_locations_buckets_links_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_organizations_locations_buckets_links_get_task()`.
/// For the simplest API, use `logging_organizations_locations_buckets_links_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_organizations_locations_buckets_links_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_organizations_locations_buckets_links_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Link>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = logging_organizations_locations_buckets_links_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_organizations_locations_buckets_links_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingOrganizationsLocationsBucketsLinksGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v2/organizations/{organizationsId}/locations/{locationsId}/buckets/{bucketsId}/links/{linksId}
/// Gets a link.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_organizations_locations_buckets_links_get_builder()` + `logging_organizations_locations_buckets_links_get_execute()`.
/// For task-level control, use `logging_organizations_locations_buckets_links_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_organizations_locations_buckets_links_get(
    client: &SimpleHttpClient,
    args: &LoggingOrganizationsLocationsBucketsLinksGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Link>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_organizations_locations_buckets_links_get_builder(client, &args.name)?;
    logging_organizations_locations_buckets_links_get_execute(builder)
}

/// GET v2/organizations/{organizationsId}/locations/{locationsId}/buckets/{bucketsId}/links
/// Lists links.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_organizations_locations_buckets_links_list_execute()` to send, or `logging_organizations_locations_buckets_links_list` for simplest API.

pub fn logging_organizations_locations_buckets_links_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/organizations/{}/locations/{locationsId}/buckets/{bucketsId}/links",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/organizations/{organizationsId}/locations/{locationsId}/buckets/{bucketsId}/links
/// Lists links.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_organizations_locations_buckets_links_list_execute()` or `logging_organizations_locations_buckets_links_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_organizations_locations_buckets_links_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_organizations_locations_buckets_links_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListLinksResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListLinksResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/organizations/{organizationsId}/locations/{locationsId}/buckets/{bucketsId}/links
/// Lists links.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_organizations_locations_buckets_links_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_organizations_locations_buckets_links_list_task()`.
/// For the simplest API, use `logging_organizations_locations_buckets_links_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_organizations_locations_buckets_links_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_organizations_locations_buckets_links_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListLinksResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = logging_organizations_locations_buckets_links_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_organizations_locations_buckets_links_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingOrganizationsLocationsBucketsLinksListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v2/organizations/{organizationsId}/locations/{locationsId}/buckets/{bucketsId}/links
/// Lists links.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_organizations_locations_buckets_links_list_builder()` + `logging_organizations_locations_buckets_links_list_execute()`.
/// For task-level control, use `logging_organizations_locations_buckets_links_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_organizations_locations_buckets_links_list(
    client: &SimpleHttpClient,
    args: &LoggingOrganizationsLocationsBucketsLinksListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListLinksResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = logging_organizations_locations_buckets_links_list_builder(
        client,
        &args.parent,
        &args.pageSize,
        &args.pageToken,
    )?;
    logging_organizations_locations_buckets_links_list_execute(builder)
}

/// POST v2/organizations/{organizationsId}/locations/{locationsId}/buckets/{bucketsId}/views
/// Creates a view over log entries in a log bucket. A bucket may contain a maximum of 30 views.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_organizations_locations_buckets_views_create_execute()` to send, or `logging_organizations_locations_buckets_views_create` for simplest API.

pub fn logging_organizations_locations_buckets_views_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    viewId: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/organizations/{}/locations/{locationsId}/buckets/{bucketsId}/views",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = viewId.as_ref() {
        query_parts.push(format!("viewId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v2/organizations/{organizationsId}/locations/{locationsId}/buckets/{bucketsId}/views
/// Creates a view over log entries in a log bucket. A bucket may contain a maximum of 30 views.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_organizations_locations_buckets_views_create_execute()` or `logging_organizations_locations_buckets_views_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_organizations_locations_buckets_views_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_organizations_locations_buckets_views_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<LogView>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LogView = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v2/organizations/{organizationsId}/locations/{locationsId}/buckets/{bucketsId}/views
/// Creates a view over log entries in a log bucket. A bucket may contain a maximum of 30 views.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_organizations_locations_buckets_views_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_organizations_locations_buckets_views_create_task()`.
/// For the simplest API, use `logging_organizations_locations_buckets_views_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_organizations_locations_buckets_views_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_organizations_locations_buckets_views_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogView>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = logging_organizations_locations_buckets_views_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_organizations_locations_buckets_views_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingOrganizationsLocationsBucketsViewsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: viewId
    pub viewId: Option<String>,
}

/// POST v2/organizations/{organizationsId}/locations/{locationsId}/buckets/{bucketsId}/views
/// Creates a view over log entries in a log bucket. A bucket may contain a maximum of 30 views.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_organizations_locations_buckets_views_create_builder()` + `logging_organizations_locations_buckets_views_create_execute()`.
/// For task-level control, use `logging_organizations_locations_buckets_views_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_organizations_locations_buckets_views_create(
    client: &SimpleHttpClient,
    args: &LoggingOrganizationsLocationsBucketsViewsCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogView>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_organizations_locations_buckets_views_create_builder(
        client,
        &args.parent,
        &args.viewId,
    )?;
    logging_organizations_locations_buckets_views_create_execute(builder)
}

/// DELETE v2/organizations/{organizationsId}/locations/{locationsId}/buckets/{bucketsId}/views/{viewsId}
/// Deletes a view on a log bucket. If an UNAVAILABLE error is returned, this indicates that system is not in a state where it can delete the view. If this occurs, please try again in a few minutes.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_organizations_locations_buckets_views_delete_execute()` to send, or `logging_organizations_locations_buckets_views_delete` for simplest API.

pub fn logging_organizations_locations_buckets_views_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/organizations/{}/locations/{locationsId}/buckets/{bucketsId}/views/{viewsId}",
        name,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v2/organizations/{organizationsId}/locations/{locationsId}/buckets/{bucketsId}/views/{viewsId}
/// Deletes a view on a log bucket. If an UNAVAILABLE error is returned, this indicates that system is not in a state where it can delete the view. If this occurs, please try again in a few minutes.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_organizations_locations_buckets_views_delete_execute()` or `logging_organizations_locations_buckets_views_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_organizations_locations_buckets_views_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_organizations_locations_buckets_views_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v2/organizations/{organizationsId}/locations/{locationsId}/buckets/{bucketsId}/views/{viewsId}
/// Deletes a view on a log bucket. If an UNAVAILABLE error is returned, this indicates that system is not in a state where it can delete the view. If this occurs, please try again in a few minutes.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_organizations_locations_buckets_views_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_organizations_locations_buckets_views_delete_task()`.
/// For the simplest API, use `logging_organizations_locations_buckets_views_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_organizations_locations_buckets_views_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_organizations_locations_buckets_views_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = logging_organizations_locations_buckets_views_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_organizations_locations_buckets_views_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingOrganizationsLocationsBucketsViewsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// DELETE v2/organizations/{organizationsId}/locations/{locationsId}/buckets/{bucketsId}/views/{viewsId}
/// Deletes a view on a log bucket. If an UNAVAILABLE error is returned, this indicates that system is not in a state where it can delete the view. If this occurs, please try again in a few minutes.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_organizations_locations_buckets_views_delete_builder()` + `logging_organizations_locations_buckets_views_delete_execute()`.
/// For task-level control, use `logging_organizations_locations_buckets_views_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_organizations_locations_buckets_views_delete(
    client: &SimpleHttpClient,
    args: &LoggingOrganizationsLocationsBucketsViewsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_organizations_locations_buckets_views_delete_builder(client, &args.name)?;
    logging_organizations_locations_buckets_views_delete_execute(builder)
}

/// GET v2/organizations/{organizationsId}/locations/{locationsId}/buckets/{bucketsId}/views/{viewsId}
/// Gets a view on a log bucket.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_organizations_locations_buckets_views_get_execute()` to send, or `logging_organizations_locations_buckets_views_get` for simplest API.

pub fn logging_organizations_locations_buckets_views_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/organizations/{}/locations/{locationsId}/buckets/{bucketsId}/views/{viewsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/organizations/{organizationsId}/locations/{locationsId}/buckets/{bucketsId}/views/{viewsId}
/// Gets a view on a log bucket.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_organizations_locations_buckets_views_get_execute()` or `logging_organizations_locations_buckets_views_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_organizations_locations_buckets_views_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_organizations_locations_buckets_views_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<LogView>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LogView = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/organizations/{organizationsId}/locations/{locationsId}/buckets/{bucketsId}/views/{viewsId}
/// Gets a view on a log bucket.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_organizations_locations_buckets_views_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_organizations_locations_buckets_views_get_task()`.
/// For the simplest API, use `logging_organizations_locations_buckets_views_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_organizations_locations_buckets_views_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_organizations_locations_buckets_views_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogView>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = logging_organizations_locations_buckets_views_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_organizations_locations_buckets_views_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingOrganizationsLocationsBucketsViewsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v2/organizations/{organizationsId}/locations/{locationsId}/buckets/{bucketsId}/views/{viewsId}
/// Gets a view on a log bucket.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_organizations_locations_buckets_views_get_builder()` + `logging_organizations_locations_buckets_views_get_execute()`.
/// For task-level control, use `logging_organizations_locations_buckets_views_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_organizations_locations_buckets_views_get(
    client: &SimpleHttpClient,
    args: &LoggingOrganizationsLocationsBucketsViewsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogView>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_organizations_locations_buckets_views_get_builder(client, &args.name)?;
    logging_organizations_locations_buckets_views_get_execute(builder)
}

/// POST v2/organizations/{organizationsId}/locations/{locationsId}/buckets/{bucketsId}/views/{viewsId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_organizations_locations_buckets_views_get_iam_policy_execute()` to send, or `logging_organizations_locations_buckets_views_get_iam_policy` for simplest API.

pub fn logging_organizations_locations_buckets_views_get_iam_policy_builder<R>(
    client: &SimpleHttpClient<R>,
    resource: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/organizations/{}/locations/{locationsId}/buckets/{bucketsId}/views/{viewsId}:getIamPolicy",
        resource,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v2/organizations/{organizationsId}/locations/{locationsId}/buckets/{bucketsId}/views/{viewsId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_organizations_locations_buckets_views_get_iam_policy_execute()` or `logging_organizations_locations_buckets_views_get_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_organizations_locations_buckets_views_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_organizations_locations_buckets_views_get_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v2/organizations/{organizationsId}/locations/{locationsId}/buckets/{bucketsId}/views/{viewsId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_organizations_locations_buckets_views_get_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_organizations_locations_buckets_views_get_iam_policy_task()`.
/// For the simplest API, use `logging_organizations_locations_buckets_views_get_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_organizations_locations_buckets_views_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_organizations_locations_buckets_views_get_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = logging_organizations_locations_buckets_views_get_iam_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_organizations_locations_buckets_views_get_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingOrganizationsLocationsBucketsViewsGetIamPolicyArgs {
    /// Path parameter: resource
    pub resource: String,
}

/// POST v2/organizations/{organizationsId}/locations/{locationsId}/buckets/{bucketsId}/views/{viewsId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_organizations_locations_buckets_views_get_iam_policy_builder()` + `logging_organizations_locations_buckets_views_get_iam_policy_execute()`.
/// For task-level control, use `logging_organizations_locations_buckets_views_get_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_organizations_locations_buckets_views_get_iam_policy(
    client: &SimpleHttpClient,
    args: &LoggingOrganizationsLocationsBucketsViewsGetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_organizations_locations_buckets_views_get_iam_policy_builder(
        client,
        &args.resource,
    )?;
    logging_organizations_locations_buckets_views_get_iam_policy_execute(builder)
}

/// GET v2/organizations/{organizationsId}/locations/{locationsId}/buckets/{bucketsId}/views
/// Lists views on a log bucket.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_organizations_locations_buckets_views_list_execute()` to send, or `logging_organizations_locations_buckets_views_list` for simplest API.

pub fn logging_organizations_locations_buckets_views_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/organizations/{}/locations/{locationsId}/buckets/{bucketsId}/views",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/organizations/{organizationsId}/locations/{locationsId}/buckets/{bucketsId}/views
/// Lists views on a log bucket.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_organizations_locations_buckets_views_list_execute()` or `logging_organizations_locations_buckets_views_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_organizations_locations_buckets_views_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_organizations_locations_buckets_views_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListViewsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListViewsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/organizations/{organizationsId}/locations/{locationsId}/buckets/{bucketsId}/views
/// Lists views on a log bucket.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_organizations_locations_buckets_views_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_organizations_locations_buckets_views_list_task()`.
/// For the simplest API, use `logging_organizations_locations_buckets_views_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_organizations_locations_buckets_views_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_organizations_locations_buckets_views_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListViewsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = logging_organizations_locations_buckets_views_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_organizations_locations_buckets_views_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingOrganizationsLocationsBucketsViewsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v2/organizations/{organizationsId}/locations/{locationsId}/buckets/{bucketsId}/views
/// Lists views on a log bucket.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_organizations_locations_buckets_views_list_builder()` + `logging_organizations_locations_buckets_views_list_execute()`.
/// For task-level control, use `logging_organizations_locations_buckets_views_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_organizations_locations_buckets_views_list(
    client: &SimpleHttpClient,
    args: &LoggingOrganizationsLocationsBucketsViewsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListViewsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = logging_organizations_locations_buckets_views_list_builder(
        client,
        &args.parent,
        &args.pageSize,
        &args.pageToken,
    )?;
    logging_organizations_locations_buckets_views_list_execute(builder)
}

/// PATCH v2/organizations/{organizationsId}/locations/{locationsId}/buckets/{bucketsId}/views/{viewsId}
/// Updates a view on a log bucket. This method replaces the value of the filter field from the existing view with the corresponding value from the new view. If an UNAVAILABLE error is returned, this indicates that system is not in a state where it can update the view. If this occurs, please try again in a few minutes.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_organizations_locations_buckets_views_patch_execute()` to send, or `logging_organizations_locations_buckets_views_patch` for simplest API.

pub fn logging_organizations_locations_buckets_views_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    updateMask: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/organizations/{}/locations/{locationsId}/buckets/{bucketsId}/views/{viewsId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v2/organizations/{organizationsId}/locations/{locationsId}/buckets/{bucketsId}/views/{viewsId}
/// Updates a view on a log bucket. This method replaces the value of the filter field from the existing view with the corresponding value from the new view. If an UNAVAILABLE error is returned, this indicates that system is not in a state where it can update the view. If this occurs, please try again in a few minutes.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_organizations_locations_buckets_views_patch_execute()` or `logging_organizations_locations_buckets_views_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_organizations_locations_buckets_views_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_organizations_locations_buckets_views_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<LogView>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LogView = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v2/organizations/{organizationsId}/locations/{locationsId}/buckets/{bucketsId}/views/{viewsId}
/// Updates a view on a log bucket. This method replaces the value of the filter field from the existing view with the corresponding value from the new view. If an UNAVAILABLE error is returned, this indicates that system is not in a state where it can update the view. If this occurs, please try again in a few minutes.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_organizations_locations_buckets_views_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_organizations_locations_buckets_views_patch_task()`.
/// For the simplest API, use `logging_organizations_locations_buckets_views_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_organizations_locations_buckets_views_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_organizations_locations_buckets_views_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogView>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = logging_organizations_locations_buckets_views_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_organizations_locations_buckets_views_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingOrganizationsLocationsBucketsViewsPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
}

/// PATCH v2/organizations/{organizationsId}/locations/{locationsId}/buckets/{bucketsId}/views/{viewsId}
/// Updates a view on a log bucket. This method replaces the value of the filter field from the existing view with the corresponding value from the new view. If an UNAVAILABLE error is returned, this indicates that system is not in a state where it can update the view. If this occurs, please try again in a few minutes.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_organizations_locations_buckets_views_patch_builder()` + `logging_organizations_locations_buckets_views_patch_execute()`.
/// For task-level control, use `logging_organizations_locations_buckets_views_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_organizations_locations_buckets_views_patch(
    client: &SimpleHttpClient,
    args: &LoggingOrganizationsLocationsBucketsViewsPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogView>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_organizations_locations_buckets_views_patch_builder(
        client,
        &args.name,
        &args.updateMask,
    )?;
    logging_organizations_locations_buckets_views_patch_execute(builder)
}

/// POST v2/organizations/{organizationsId}/locations/{locationsId}/buckets/{bucketsId}/views/{viewsId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_organizations_locations_buckets_views_set_iam_policy_execute()` to send, or `logging_organizations_locations_buckets_views_set_iam_policy` for simplest API.

pub fn logging_organizations_locations_buckets_views_set_iam_policy_builder<R>(
    client: &SimpleHttpClient<R>,
    resource: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/organizations/{}/locations/{locationsId}/buckets/{bucketsId}/views/{viewsId}:setIamPolicy",
        resource,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v2/organizations/{organizationsId}/locations/{locationsId}/buckets/{bucketsId}/views/{viewsId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_organizations_locations_buckets_views_set_iam_policy_execute()` or `logging_organizations_locations_buckets_views_set_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_organizations_locations_buckets_views_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_organizations_locations_buckets_views_set_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v2/organizations/{organizationsId}/locations/{locationsId}/buckets/{bucketsId}/views/{viewsId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_organizations_locations_buckets_views_set_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_organizations_locations_buckets_views_set_iam_policy_task()`.
/// For the simplest API, use `logging_organizations_locations_buckets_views_set_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_organizations_locations_buckets_views_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_organizations_locations_buckets_views_set_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = logging_organizations_locations_buckets_views_set_iam_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_organizations_locations_buckets_views_set_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingOrganizationsLocationsBucketsViewsSetIamPolicyArgs {
    /// Path parameter: resource
    pub resource: String,
}

/// POST v2/organizations/{organizationsId}/locations/{locationsId}/buckets/{bucketsId}/views/{viewsId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_organizations_locations_buckets_views_set_iam_policy_builder()` + `logging_organizations_locations_buckets_views_set_iam_policy_execute()`.
/// For task-level control, use `logging_organizations_locations_buckets_views_set_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_organizations_locations_buckets_views_set_iam_policy(
    client: &SimpleHttpClient,
    args: &LoggingOrganizationsLocationsBucketsViewsSetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_organizations_locations_buckets_views_set_iam_policy_builder(
        client,
        &args.resource,
    )?;
    logging_organizations_locations_buckets_views_set_iam_policy_execute(builder)
}

/// POST v2/organizations/{organizationsId}/locations/{locationsId}/buckets/{bucketsId}/views/{viewsId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error.Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_organizations_locations_buckets_views_test_iam_permissions_execute()` to send, or `logging_organizations_locations_buckets_views_test_iam_permissions` for simplest API.

pub fn logging_organizations_locations_buckets_views_test_iam_permissions_builder<R>(
    client: &SimpleHttpClient<R>,
    resource: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/organizations/{}/locations/{locationsId}/buckets/{bucketsId}/views/{viewsId}:testIamPermissions",
        resource,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v2/organizations/{organizationsId}/locations/{locationsId}/buckets/{bucketsId}/views/{viewsId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error.Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_organizations_locations_buckets_views_test_iam_permissions_execute()` or `logging_organizations_locations_buckets_views_test_iam_permissions`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_organizations_locations_buckets_views_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_organizations_locations_buckets_views_test_iam_permissions_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TestIamPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v2/organizations/{organizationsId}/locations/{locationsId}/buckets/{bucketsId}/views/{viewsId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error.Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_organizations_locations_buckets_views_test_iam_permissions_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_organizations_locations_buckets_views_test_iam_permissions_task()`.
/// For the simplest API, use `logging_organizations_locations_buckets_views_test_iam_permissions()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_organizations_locations_buckets_views_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_organizations_locations_buckets_views_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = logging_organizations_locations_buckets_views_test_iam_permissions_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_organizations_locations_buckets_views_test_iam_permissions`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingOrganizationsLocationsBucketsViewsTestIamPermissionsArgs {
    /// Path parameter: resource
    pub resource: String,
}

/// POST v2/organizations/{organizationsId}/locations/{locationsId}/buckets/{bucketsId}/views/{viewsId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error.Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_organizations_locations_buckets_views_test_iam_permissions_builder()` + `logging_organizations_locations_buckets_views_test_iam_permissions_execute()`.
/// For task-level control, use `logging_organizations_locations_buckets_views_test_iam_permissions_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_organizations_locations_buckets_views_test_iam_permissions(
    client: &SimpleHttpClient,
    args: &LoggingOrganizationsLocationsBucketsViewsTestIamPermissionsArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = logging_organizations_locations_buckets_views_test_iam_permissions_builder(
        client,
        &args.resource,
    )?;
    logging_organizations_locations_buckets_views_test_iam_permissions_execute(builder)
}

/// GET v2/organizations/{organizationsId}/locations/{locationsId}/buckets/{bucketsId}/views/{viewsId}/logs
/// Lists the logs in projects, organizations, folders, or billing accounts. Only logs that have entries are listed.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_organizations_locations_buckets_views_logs_list_execute()` to send, or `logging_organizations_locations_buckets_views_logs_list` for simplest API.

pub fn logging_organizations_locations_buckets_views_logs_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
    resourceNames: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/organizations/{}/locations/{locationsId}/buckets/{bucketsId}/views/{viewsId}/logs",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = resourceNames.as_ref() {
        query_parts.push(format!("resourceNames={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/organizations/{organizationsId}/locations/{locationsId}/buckets/{bucketsId}/views/{viewsId}/logs
/// Lists the logs in projects, organizations, folders, or billing accounts. Only logs that have entries are listed.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_organizations_locations_buckets_views_logs_list_execute()` or `logging_organizations_locations_buckets_views_logs_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_organizations_locations_buckets_views_logs_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_organizations_locations_buckets_views_logs_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListLogsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListLogsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/organizations/{organizationsId}/locations/{locationsId}/buckets/{bucketsId}/views/{viewsId}/logs
/// Lists the logs in projects, organizations, folders, or billing accounts. Only logs that have entries are listed.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_organizations_locations_buckets_views_logs_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_organizations_locations_buckets_views_logs_list_task()`.
/// For the simplest API, use `logging_organizations_locations_buckets_views_logs_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_organizations_locations_buckets_views_logs_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_organizations_locations_buckets_views_logs_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListLogsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = logging_organizations_locations_buckets_views_logs_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_organizations_locations_buckets_views_logs_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingOrganizationsLocationsBucketsViewsLogsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: resourceNames
    pub resourceNames: Option<String>,
}

/// GET v2/organizations/{organizationsId}/locations/{locationsId}/buckets/{bucketsId}/views/{viewsId}/logs
/// Lists the logs in projects, organizations, folders, or billing accounts. Only logs that have entries are listed.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_organizations_locations_buckets_views_logs_list_builder()` + `logging_organizations_locations_buckets_views_logs_list_execute()`.
/// For task-level control, use `logging_organizations_locations_buckets_views_logs_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_organizations_locations_buckets_views_logs_list(
    client: &SimpleHttpClient,
    args: &LoggingOrganizationsLocationsBucketsViewsLogsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListLogsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = logging_organizations_locations_buckets_views_logs_list_builder(
        client,
        &args.parent,
        &args.pageSize,
        &args.pageToken,
        &args.resourceNames,
    )?;
    logging_organizations_locations_buckets_views_logs_list_execute(builder)
}

/// POST v2/organizations/{organizationsId}/locations/{locationsId}/logScopes
/// Creates a log scope.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_organizations_locations_log_scopes_create_execute()` to send, or `logging_organizations_locations_log_scopes_create` for simplest API.

pub fn logging_organizations_locations_log_scopes_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    logScopeId: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/organizations/{}/locations/{locationsId}/logScopes",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = logScopeId.as_ref() {
        query_parts.push(format!("logScopeId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v2/organizations/{organizationsId}/locations/{locationsId}/logScopes
/// Creates a log scope.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_organizations_locations_log_scopes_create_execute()` or `logging_organizations_locations_log_scopes_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_organizations_locations_log_scopes_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_organizations_locations_log_scopes_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<LogScope>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LogScope = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v2/organizations/{organizationsId}/locations/{locationsId}/logScopes
/// Creates a log scope.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_organizations_locations_log_scopes_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_organizations_locations_log_scopes_create_task()`.
/// For the simplest API, use `logging_organizations_locations_log_scopes_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_organizations_locations_log_scopes_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_organizations_locations_log_scopes_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogScope>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = logging_organizations_locations_log_scopes_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_organizations_locations_log_scopes_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingOrganizationsLocationsLogScopesCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: logScopeId
    pub logScopeId: Option<String>,
}

/// POST v2/organizations/{organizationsId}/locations/{locationsId}/logScopes
/// Creates a log scope.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_organizations_locations_log_scopes_create_builder()` + `logging_organizations_locations_log_scopes_create_execute()`.
/// For task-level control, use `logging_organizations_locations_log_scopes_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_organizations_locations_log_scopes_create(
    client: &SimpleHttpClient,
    args: &LoggingOrganizationsLocationsLogScopesCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogScope>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_organizations_locations_log_scopes_create_builder(
        client,
        &args.parent,
        &args.logScopeId,
    )?;
    logging_organizations_locations_log_scopes_create_execute(builder)
}

/// DELETE v2/organizations/{organizationsId}/locations/{locationsId}/logScopes/{logScopesId}
/// Deletes a log scope.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_organizations_locations_log_scopes_delete_execute()` to send, or `logging_organizations_locations_log_scopes_delete` for simplest API.

pub fn logging_organizations_locations_log_scopes_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/organizations/{}/locations/{locationsId}/logScopes/{logScopesId}",
        name,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v2/organizations/{organizationsId}/locations/{locationsId}/logScopes/{logScopesId}
/// Deletes a log scope.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_organizations_locations_log_scopes_delete_execute()` or `logging_organizations_locations_log_scopes_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_organizations_locations_log_scopes_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_organizations_locations_log_scopes_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v2/organizations/{organizationsId}/locations/{locationsId}/logScopes/{logScopesId}
/// Deletes a log scope.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_organizations_locations_log_scopes_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_organizations_locations_log_scopes_delete_task()`.
/// For the simplest API, use `logging_organizations_locations_log_scopes_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_organizations_locations_log_scopes_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_organizations_locations_log_scopes_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = logging_organizations_locations_log_scopes_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_organizations_locations_log_scopes_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingOrganizationsLocationsLogScopesDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// DELETE v2/organizations/{organizationsId}/locations/{locationsId}/logScopes/{logScopesId}
/// Deletes a log scope.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_organizations_locations_log_scopes_delete_builder()` + `logging_organizations_locations_log_scopes_delete_execute()`.
/// For task-level control, use `logging_organizations_locations_log_scopes_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_organizations_locations_log_scopes_delete(
    client: &SimpleHttpClient,
    args: &LoggingOrganizationsLocationsLogScopesDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_organizations_locations_log_scopes_delete_builder(client, &args.name)?;
    logging_organizations_locations_log_scopes_delete_execute(builder)
}

/// GET v2/organizations/{organizationsId}/locations/{locationsId}/logScopes/{logScopesId}
/// Gets a log scope.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_organizations_locations_log_scopes_get_execute()` to send, or `logging_organizations_locations_log_scopes_get` for simplest API.

pub fn logging_organizations_locations_log_scopes_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/organizations/{}/locations/{locationsId}/logScopes/{logScopesId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/organizations/{organizationsId}/locations/{locationsId}/logScopes/{logScopesId}
/// Gets a log scope.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_organizations_locations_log_scopes_get_execute()` or `logging_organizations_locations_log_scopes_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_organizations_locations_log_scopes_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_organizations_locations_log_scopes_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<LogScope>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LogScope = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/organizations/{organizationsId}/locations/{locationsId}/logScopes/{logScopesId}
/// Gets a log scope.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_organizations_locations_log_scopes_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_organizations_locations_log_scopes_get_task()`.
/// For the simplest API, use `logging_organizations_locations_log_scopes_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_organizations_locations_log_scopes_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_organizations_locations_log_scopes_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogScope>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = logging_organizations_locations_log_scopes_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_organizations_locations_log_scopes_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingOrganizationsLocationsLogScopesGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v2/organizations/{organizationsId}/locations/{locationsId}/logScopes/{logScopesId}
/// Gets a log scope.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_organizations_locations_log_scopes_get_builder()` + `logging_organizations_locations_log_scopes_get_execute()`.
/// For task-level control, use `logging_organizations_locations_log_scopes_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_organizations_locations_log_scopes_get(
    client: &SimpleHttpClient,
    args: &LoggingOrganizationsLocationsLogScopesGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogScope>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_organizations_locations_log_scopes_get_builder(client, &args.name)?;
    logging_organizations_locations_log_scopes_get_execute(builder)
}

/// GET v2/organizations/{organizationsId}/locations/{locationsId}/logScopes
/// Lists log scopes.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_organizations_locations_log_scopes_list_execute()` to send, or `logging_organizations_locations_log_scopes_list` for simplest API.

pub fn logging_organizations_locations_log_scopes_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/organizations/{}/locations/{locationsId}/logScopes",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/organizations/{organizationsId}/locations/{locationsId}/logScopes
/// Lists log scopes.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_organizations_locations_log_scopes_list_execute()` or `logging_organizations_locations_log_scopes_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_organizations_locations_log_scopes_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_organizations_locations_log_scopes_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListLogScopesResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListLogScopesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/organizations/{organizationsId}/locations/{locationsId}/logScopes
/// Lists log scopes.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_organizations_locations_log_scopes_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_organizations_locations_log_scopes_list_task()`.
/// For the simplest API, use `logging_organizations_locations_log_scopes_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_organizations_locations_log_scopes_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_organizations_locations_log_scopes_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListLogScopesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = logging_organizations_locations_log_scopes_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_organizations_locations_log_scopes_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingOrganizationsLocationsLogScopesListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v2/organizations/{organizationsId}/locations/{locationsId}/logScopes
/// Lists log scopes.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_organizations_locations_log_scopes_list_builder()` + `logging_organizations_locations_log_scopes_list_execute()`.
/// For task-level control, use `logging_organizations_locations_log_scopes_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_organizations_locations_log_scopes_list(
    client: &SimpleHttpClient,
    args: &LoggingOrganizationsLocationsLogScopesListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListLogScopesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = logging_organizations_locations_log_scopes_list_builder(
        client,
        &args.parent,
        &args.pageSize,
        &args.pageToken,
    )?;
    logging_organizations_locations_log_scopes_list_execute(builder)
}

/// PATCH v2/organizations/{organizationsId}/locations/{locationsId}/logScopes/{logScopesId}
/// Updates a log scope.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_organizations_locations_log_scopes_patch_execute()` to send, or `logging_organizations_locations_log_scopes_patch` for simplest API.

pub fn logging_organizations_locations_log_scopes_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    updateMask: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/organizations/{}/locations/{locationsId}/logScopes/{logScopesId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v2/organizations/{organizationsId}/locations/{locationsId}/logScopes/{logScopesId}
/// Updates a log scope.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_organizations_locations_log_scopes_patch_execute()` or `logging_organizations_locations_log_scopes_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_organizations_locations_log_scopes_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_organizations_locations_log_scopes_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<LogScope>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LogScope = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v2/organizations/{organizationsId}/locations/{locationsId}/logScopes/{logScopesId}
/// Updates a log scope.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_organizations_locations_log_scopes_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_organizations_locations_log_scopes_patch_task()`.
/// For the simplest API, use `logging_organizations_locations_log_scopes_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_organizations_locations_log_scopes_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_organizations_locations_log_scopes_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogScope>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = logging_organizations_locations_log_scopes_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_organizations_locations_log_scopes_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingOrganizationsLocationsLogScopesPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
}

/// PATCH v2/organizations/{organizationsId}/locations/{locationsId}/logScopes/{logScopesId}
/// Updates a log scope.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_organizations_locations_log_scopes_patch_builder()` + `logging_organizations_locations_log_scopes_patch_execute()`.
/// For task-level control, use `logging_organizations_locations_log_scopes_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_organizations_locations_log_scopes_patch(
    client: &SimpleHttpClient,
    args: &LoggingOrganizationsLocationsLogScopesPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogScope>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_organizations_locations_log_scopes_patch_builder(
        client,
        &args.name,
        &args.updateMask,
    )?;
    logging_organizations_locations_log_scopes_patch_execute(builder)
}

/// POST v2/organizations/{organizationsId}/locations/{locationsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn''t support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_organizations_locations_operations_cancel_execute()` to send, or `logging_organizations_locations_operations_cancel` for simplest API.

pub fn logging_organizations_locations_operations_cancel_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/organizations/{}/locations/{locationsId}/operations/{operationsId}:cancel",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v2/organizations/{organizationsId}/locations/{locationsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn''t support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_organizations_locations_operations_cancel_execute()` or `logging_organizations_locations_operations_cancel`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_organizations_locations_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_organizations_locations_operations_cancel_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v2/organizations/{organizationsId}/locations/{locationsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn''t support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_organizations_locations_operations_cancel_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_organizations_locations_operations_cancel_task()`.
/// For the simplest API, use `logging_organizations_locations_operations_cancel()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_organizations_locations_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_organizations_locations_operations_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = logging_organizations_locations_operations_cancel_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_organizations_locations_operations_cancel`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingOrganizationsLocationsOperationsCancelArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v2/organizations/{organizationsId}/locations/{locationsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn''t support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_organizations_locations_operations_cancel_builder()` + `logging_organizations_locations_operations_cancel_execute()`.
/// For task-level control, use `logging_organizations_locations_operations_cancel_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_organizations_locations_operations_cancel(
    client: &SimpleHttpClient,
    args: &LoggingOrganizationsLocationsOperationsCancelArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_organizations_locations_operations_cancel_builder(client, &args.name)?;
    logging_organizations_locations_operations_cancel_execute(builder)
}

/// GET v2/organizations/{organizationsId}/locations/{locationsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_organizations_locations_operations_get_execute()` to send, or `logging_organizations_locations_operations_get` for simplest API.

pub fn logging_organizations_locations_operations_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/organizations/{}/locations/{locationsId}/operations/{operationsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/organizations/{organizationsId}/locations/{locationsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_organizations_locations_operations_get_execute()` or `logging_organizations_locations_operations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_organizations_locations_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_organizations_locations_operations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/organizations/{organizationsId}/locations/{locationsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_organizations_locations_operations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_organizations_locations_operations_get_task()`.
/// For the simplest API, use `logging_organizations_locations_operations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_organizations_locations_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_organizations_locations_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = logging_organizations_locations_operations_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_organizations_locations_operations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingOrganizationsLocationsOperationsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v2/organizations/{organizationsId}/locations/{locationsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_organizations_locations_operations_get_builder()` + `logging_organizations_locations_operations_get_execute()`.
/// For task-level control, use `logging_organizations_locations_operations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_organizations_locations_operations_get(
    client: &SimpleHttpClient,
    args: &LoggingOrganizationsLocationsOperationsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_organizations_locations_operations_get_builder(client, &args.name)?;
    logging_organizations_locations_operations_get_execute(builder)
}

/// GET v2/organizations/{organizationsId}/locations/{locationsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn''t support this method, it returns UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_organizations_locations_operations_list_execute()` to send, or `logging_organizations_locations_operations_list` for simplest API.

pub fn logging_organizations_locations_operations_list_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    filter: &Option<String>,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
    returnPartialSuccess: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/organizations/{}/locations/{locationsId}/operations",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/organizations/{organizationsId}/locations/{locationsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn''t support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_organizations_locations_operations_list_execute()` or `logging_organizations_locations_operations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_organizations_locations_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_organizations_locations_operations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListOperationsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListOperationsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/organizations/{organizationsId}/locations/{locationsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn''t support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_organizations_locations_operations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_organizations_locations_operations_list_task()`.
/// For the simplest API, use `logging_organizations_locations_operations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_organizations_locations_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_organizations_locations_operations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListOperationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = logging_organizations_locations_operations_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_organizations_locations_operations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingOrganizationsLocationsOperationsListArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<String>,
}

/// GET v2/organizations/{organizationsId}/locations/{locationsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn''t support this method, it returns UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_organizations_locations_operations_list_builder()` + `logging_organizations_locations_operations_list_execute()`.
/// For task-level control, use `logging_organizations_locations_operations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_organizations_locations_operations_list(
    client: &SimpleHttpClient,
    args: &LoggingOrganizationsLocationsOperationsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListOperationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = logging_organizations_locations_operations_list_builder(
        client,
        &args.name,
        &args.filter,
        &args.pageSize,
        &args.pageToken,
        &args.returnPartialSuccess,
    )?;
    logging_organizations_locations_operations_list_execute(builder)
}

/// GET v2/organizations/{organizationsId}/locations/{locationsId}/recentQueries
/// Lists the RecentQueries that were created by the user making the request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_organizations_locations_recent_queries_list_execute()` to send, or `logging_organizations_locations_recent_queries_list` for simplest API.

pub fn logging_organizations_locations_recent_queries_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    filter: &Option<String>,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/organizations/{}/locations/{locationsId}/recentQueries",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/organizations/{organizationsId}/locations/{locationsId}/recentQueries
/// Lists the RecentQueries that were created by the user making the request.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_organizations_locations_recent_queries_list_execute()` or `logging_organizations_locations_recent_queries_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_organizations_locations_recent_queries_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_organizations_locations_recent_queries_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListRecentQueriesResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListRecentQueriesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/organizations/{organizationsId}/locations/{locationsId}/recentQueries
/// Lists the RecentQueries that were created by the user making the request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_organizations_locations_recent_queries_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_organizations_locations_recent_queries_list_task()`.
/// For the simplest API, use `logging_organizations_locations_recent_queries_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_organizations_locations_recent_queries_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_organizations_locations_recent_queries_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListRecentQueriesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = logging_organizations_locations_recent_queries_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_organizations_locations_recent_queries_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingOrganizationsLocationsRecentQueriesListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v2/organizations/{organizationsId}/locations/{locationsId}/recentQueries
/// Lists the RecentQueries that were created by the user making the request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_organizations_locations_recent_queries_list_builder()` + `logging_organizations_locations_recent_queries_list_execute()`.
/// For task-level control, use `logging_organizations_locations_recent_queries_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_organizations_locations_recent_queries_list(
    client: &SimpleHttpClient,
    args: &LoggingOrganizationsLocationsRecentQueriesListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListRecentQueriesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = logging_organizations_locations_recent_queries_list_builder(
        client,
        &args.parent,
        &args.filter,
        &args.pageSize,
        &args.pageToken,
    )?;
    logging_organizations_locations_recent_queries_list_execute(builder)
}

/// POST v2/organizations/{organizationsId}/locations/{locationsId}/savedQueries
/// Creates a new SavedQuery for the user making the request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_organizations_locations_saved_queries_create_execute()` to send, or `logging_organizations_locations_saved_queries_create` for simplest API.

pub fn logging_organizations_locations_saved_queries_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    savedQueryId: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/organizations/{}/locations/{locationsId}/savedQueries",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = savedQueryId.as_ref() {
        query_parts.push(format!("savedQueryId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v2/organizations/{organizationsId}/locations/{locationsId}/savedQueries
/// Creates a new SavedQuery for the user making the request.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_organizations_locations_saved_queries_create_execute()` or `logging_organizations_locations_saved_queries_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_organizations_locations_saved_queries_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_organizations_locations_saved_queries_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SavedQuery>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SavedQuery = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v2/organizations/{organizationsId}/locations/{locationsId}/savedQueries
/// Creates a new SavedQuery for the user making the request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_organizations_locations_saved_queries_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_organizations_locations_saved_queries_create_task()`.
/// For the simplest API, use `logging_organizations_locations_saved_queries_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_organizations_locations_saved_queries_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_organizations_locations_saved_queries_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SavedQuery>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = logging_organizations_locations_saved_queries_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_organizations_locations_saved_queries_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingOrganizationsLocationsSavedQueriesCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: savedQueryId
    pub savedQueryId: Option<String>,
}

/// POST v2/organizations/{organizationsId}/locations/{locationsId}/savedQueries
/// Creates a new SavedQuery for the user making the request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_organizations_locations_saved_queries_create_builder()` + `logging_organizations_locations_saved_queries_create_execute()`.
/// For task-level control, use `logging_organizations_locations_saved_queries_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_organizations_locations_saved_queries_create(
    client: &SimpleHttpClient,
    args: &LoggingOrganizationsLocationsSavedQueriesCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SavedQuery>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_organizations_locations_saved_queries_create_builder(
        client,
        &args.parent,
        &args.savedQueryId,
    )?;
    logging_organizations_locations_saved_queries_create_execute(builder)
}

/// DELETE v2/organizations/{organizationsId}/locations/{locationsId}/savedQueries/{savedQueriesId}
/// Deletes an existing SavedQuery that was created by the user making the request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_organizations_locations_saved_queries_delete_execute()` to send, or `logging_organizations_locations_saved_queries_delete` for simplest API.

pub fn logging_organizations_locations_saved_queries_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/organizations/{}/locations/{locationsId}/savedQueries/{savedQueriesId}",
        name,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v2/organizations/{organizationsId}/locations/{locationsId}/savedQueries/{savedQueriesId}
/// Deletes an existing SavedQuery that was created by the user making the request.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_organizations_locations_saved_queries_delete_execute()` or `logging_organizations_locations_saved_queries_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_organizations_locations_saved_queries_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_organizations_locations_saved_queries_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v2/organizations/{organizationsId}/locations/{locationsId}/savedQueries/{savedQueriesId}
/// Deletes an existing SavedQuery that was created by the user making the request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_organizations_locations_saved_queries_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_organizations_locations_saved_queries_delete_task()`.
/// For the simplest API, use `logging_organizations_locations_saved_queries_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_organizations_locations_saved_queries_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_organizations_locations_saved_queries_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = logging_organizations_locations_saved_queries_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_organizations_locations_saved_queries_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingOrganizationsLocationsSavedQueriesDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// DELETE v2/organizations/{organizationsId}/locations/{locationsId}/savedQueries/{savedQueriesId}
/// Deletes an existing SavedQuery that was created by the user making the request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_organizations_locations_saved_queries_delete_builder()` + `logging_organizations_locations_saved_queries_delete_execute()`.
/// For task-level control, use `logging_organizations_locations_saved_queries_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_organizations_locations_saved_queries_delete(
    client: &SimpleHttpClient,
    args: &LoggingOrganizationsLocationsSavedQueriesDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_organizations_locations_saved_queries_delete_builder(client, &args.name)?;
    logging_organizations_locations_saved_queries_delete_execute(builder)
}

/// GET v2/organizations/{organizationsId}/locations/{locationsId}/savedQueries/{savedQueriesId}
/// Returns all data associated with the requested query.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_organizations_locations_saved_queries_get_execute()` to send, or `logging_organizations_locations_saved_queries_get` for simplest API.

pub fn logging_organizations_locations_saved_queries_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/organizations/{}/locations/{locationsId}/savedQueries/{savedQueriesId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/organizations/{organizationsId}/locations/{locationsId}/savedQueries/{savedQueriesId}
/// Returns all data associated with the requested query.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_organizations_locations_saved_queries_get_execute()` or `logging_organizations_locations_saved_queries_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_organizations_locations_saved_queries_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_organizations_locations_saved_queries_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SavedQuery>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SavedQuery = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/organizations/{organizationsId}/locations/{locationsId}/savedQueries/{savedQueriesId}
/// Returns all data associated with the requested query.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_organizations_locations_saved_queries_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_organizations_locations_saved_queries_get_task()`.
/// For the simplest API, use `logging_organizations_locations_saved_queries_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_organizations_locations_saved_queries_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_organizations_locations_saved_queries_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SavedQuery>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = logging_organizations_locations_saved_queries_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_organizations_locations_saved_queries_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingOrganizationsLocationsSavedQueriesGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v2/organizations/{organizationsId}/locations/{locationsId}/savedQueries/{savedQueriesId}
/// Returns all data associated with the requested query.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_organizations_locations_saved_queries_get_builder()` + `logging_organizations_locations_saved_queries_get_execute()`.
/// For task-level control, use `logging_organizations_locations_saved_queries_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_organizations_locations_saved_queries_get(
    client: &SimpleHttpClient,
    args: &LoggingOrganizationsLocationsSavedQueriesGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SavedQuery>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_organizations_locations_saved_queries_get_builder(client, &args.name)?;
    logging_organizations_locations_saved_queries_get_execute(builder)
}

/// GET v2/organizations/{organizationsId}/locations/{locationsId}/savedQueries
/// Lists the SavedQueries that were created by the user making the request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_organizations_locations_saved_queries_list_execute()` to send, or `logging_organizations_locations_saved_queries_list` for simplest API.

pub fn logging_organizations_locations_saved_queries_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    filter: &Option<String>,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/organizations/{}/locations/{locationsId}/savedQueries",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/organizations/{organizationsId}/locations/{locationsId}/savedQueries
/// Lists the SavedQueries that were created by the user making the request.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_organizations_locations_saved_queries_list_execute()` or `logging_organizations_locations_saved_queries_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_organizations_locations_saved_queries_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_organizations_locations_saved_queries_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListSavedQueriesResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListSavedQueriesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/organizations/{organizationsId}/locations/{locationsId}/savedQueries
/// Lists the SavedQueries that were created by the user making the request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_organizations_locations_saved_queries_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_organizations_locations_saved_queries_list_task()`.
/// For the simplest API, use `logging_organizations_locations_saved_queries_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_organizations_locations_saved_queries_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_organizations_locations_saved_queries_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListSavedQueriesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = logging_organizations_locations_saved_queries_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_organizations_locations_saved_queries_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingOrganizationsLocationsSavedQueriesListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v2/organizations/{organizationsId}/locations/{locationsId}/savedQueries
/// Lists the SavedQueries that were created by the user making the request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_organizations_locations_saved_queries_list_builder()` + `logging_organizations_locations_saved_queries_list_execute()`.
/// For task-level control, use `logging_organizations_locations_saved_queries_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_organizations_locations_saved_queries_list(
    client: &SimpleHttpClient,
    args: &LoggingOrganizationsLocationsSavedQueriesListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListSavedQueriesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = logging_organizations_locations_saved_queries_list_builder(
        client,
        &args.parent,
        &args.filter,
        &args.pageSize,
        &args.pageToken,
    )?;
    logging_organizations_locations_saved_queries_list_execute(builder)
}

/// PATCH v2/organizations/{organizationsId}/locations/{locationsId}/savedQueries/{savedQueriesId}
/// Updates an existing SavedQuery.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_organizations_locations_saved_queries_patch_execute()` to send, or `logging_organizations_locations_saved_queries_patch` for simplest API.

pub fn logging_organizations_locations_saved_queries_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    updateMask: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/organizations/{}/locations/{locationsId}/savedQueries/{savedQueriesId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v2/organizations/{organizationsId}/locations/{locationsId}/savedQueries/{savedQueriesId}
/// Updates an existing SavedQuery.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_organizations_locations_saved_queries_patch_execute()` or `logging_organizations_locations_saved_queries_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_organizations_locations_saved_queries_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_organizations_locations_saved_queries_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SavedQuery>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SavedQuery = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v2/organizations/{organizationsId}/locations/{locationsId}/savedQueries/{savedQueriesId}
/// Updates an existing SavedQuery.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_organizations_locations_saved_queries_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_organizations_locations_saved_queries_patch_task()`.
/// For the simplest API, use `logging_organizations_locations_saved_queries_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_organizations_locations_saved_queries_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_organizations_locations_saved_queries_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SavedQuery>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = logging_organizations_locations_saved_queries_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_organizations_locations_saved_queries_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingOrganizationsLocationsSavedQueriesPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
}

/// PATCH v2/organizations/{organizationsId}/locations/{locationsId}/savedQueries/{savedQueriesId}
/// Updates an existing SavedQuery.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_organizations_locations_saved_queries_patch_builder()` + `logging_organizations_locations_saved_queries_patch_execute()`.
/// For task-level control, use `logging_organizations_locations_saved_queries_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_organizations_locations_saved_queries_patch(
    client: &SimpleHttpClient,
    args: &LoggingOrganizationsLocationsSavedQueriesPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SavedQuery>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_organizations_locations_saved_queries_patch_builder(
        client,
        &args.name,
        &args.updateMask,
    )?;
    logging_organizations_locations_saved_queries_patch_execute(builder)
}

/// DELETE v2/organizations/{organizationsId}/logs/{logsId}
/// Deletes all the log entries in a log for the global _Default Log Bucket. The log reappears if it receives new entries. Log entries written shortly before the delete operation might not be deleted. Entries received after the delete operation with a timestamp before the operation will be deleted.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_organizations_logs_delete_execute()` to send, or `logging_organizations_logs_delete` for simplest API.

pub fn logging_organizations_logs_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    logName: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/organizations/{}/logs/{logsId}",
        logName,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v2/organizations/{organizationsId}/logs/{logsId}
/// Deletes all the log entries in a log for the global _Default Log Bucket. The log reappears if it receives new entries. Log entries written shortly before the delete operation might not be deleted. Entries received after the delete operation with a timestamp before the operation will be deleted.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_organizations_logs_delete_execute()` or `logging_organizations_logs_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_organizations_logs_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_organizations_logs_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v2/organizations/{organizationsId}/logs/{logsId}
/// Deletes all the log entries in a log for the global _Default Log Bucket. The log reappears if it receives new entries. Log entries written shortly before the delete operation might not be deleted. Entries received after the delete operation with a timestamp before the operation will be deleted.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_organizations_logs_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_organizations_logs_delete_task()`.
/// For the simplest API, use `logging_organizations_logs_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_organizations_logs_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_organizations_logs_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = logging_organizations_logs_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_organizations_logs_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingOrganizationsLogsDeleteArgs {
    /// Path parameter: logName
    pub logName: String,
}

/// DELETE v2/organizations/{organizationsId}/logs/{logsId}
/// Deletes all the log entries in a log for the global _Default Log Bucket. The log reappears if it receives new entries. Log entries written shortly before the delete operation might not be deleted. Entries received after the delete operation with a timestamp before the operation will be deleted.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_organizations_logs_delete_builder()` + `logging_organizations_logs_delete_execute()`.
/// For task-level control, use `logging_organizations_logs_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_organizations_logs_delete(
    client: &SimpleHttpClient,
    args: &LoggingOrganizationsLogsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_organizations_logs_delete_builder(client, &args.logName)?;
    logging_organizations_logs_delete_execute(builder)
}

/// GET v2/organizations/{organizationsId}/logs
/// Lists the logs in projects, organizations, folders, or billing accounts. Only logs that have entries are listed.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_organizations_logs_list_execute()` to send, or `logging_organizations_logs_list` for simplest API.

pub fn logging_organizations_logs_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
    resourceNames: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/organizations/{}/logs",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = resourceNames.as_ref() {
        query_parts.push(format!("resourceNames={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/organizations/{organizationsId}/logs
/// Lists the logs in projects, organizations, folders, or billing accounts. Only logs that have entries are listed.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_organizations_logs_list_execute()` or `logging_organizations_logs_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_organizations_logs_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_organizations_logs_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListLogsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListLogsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/organizations/{organizationsId}/logs
/// Lists the logs in projects, organizations, folders, or billing accounts. Only logs that have entries are listed.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_organizations_logs_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_organizations_logs_list_task()`.
/// For the simplest API, use `logging_organizations_logs_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_organizations_logs_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_organizations_logs_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListLogsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = logging_organizations_logs_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_organizations_logs_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingOrganizationsLogsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: resourceNames
    pub resourceNames: Option<String>,
}

/// GET v2/organizations/{organizationsId}/logs
/// Lists the logs in projects, organizations, folders, or billing accounts. Only logs that have entries are listed.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_organizations_logs_list_builder()` + `logging_organizations_logs_list_execute()`.
/// For task-level control, use `logging_organizations_logs_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_organizations_logs_list(
    client: &SimpleHttpClient,
    args: &LoggingOrganizationsLogsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListLogsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = logging_organizations_logs_list_builder(
        client,
        &args.parent,
        &args.pageSize,
        &args.pageToken,
        &args.resourceNames,
    )?;
    logging_organizations_logs_list_execute(builder)
}

/// POST v2/organizations/{organizationsId}/sinks
/// Creates a sink that exports specified log entries to a destination. The export begins upon ingress, unless the sink''s writer_identity is not permitted to write to the destination. A sink can export log entries only from the resource owning the sink.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_organizations_sinks_create_execute()` to send, or `logging_organizations_sinks_create` for simplest API.

pub fn logging_organizations_sinks_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    customWriterIdentity: &Option<String>,
    uniqueWriterIdentity: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/organizations/{}/sinks",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = customWriterIdentity.as_ref() {
        query_parts.push(format!("customWriterIdentity={}", val));
    }
    if let Some(val) = uniqueWriterIdentity.as_ref() {
        query_parts.push(format!("uniqueWriterIdentity={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v2/organizations/{organizationsId}/sinks
/// Creates a sink that exports specified log entries to a destination. The export begins upon ingress, unless the sink''s writer_identity is not permitted to write to the destination. A sink can export log entries only from the resource owning the sink.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_organizations_sinks_create_execute()` or `logging_organizations_sinks_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_organizations_sinks_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_organizations_sinks_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<LogSink>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LogSink = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v2/organizations/{organizationsId}/sinks
/// Creates a sink that exports specified log entries to a destination. The export begins upon ingress, unless the sink''s writer_identity is not permitted to write to the destination. A sink can export log entries only from the resource owning the sink.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_organizations_sinks_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_organizations_sinks_create_task()`.
/// For the simplest API, use `logging_organizations_sinks_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_organizations_sinks_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_organizations_sinks_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogSink>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = logging_organizations_sinks_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_organizations_sinks_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingOrganizationsSinksCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: customWriterIdentity
    pub customWriterIdentity: Option<String>,
    /// Query parameter: uniqueWriterIdentity
    pub uniqueWriterIdentity: Option<String>,
}

/// POST v2/organizations/{organizationsId}/sinks
/// Creates a sink that exports specified log entries to a destination. The export begins upon ingress, unless the sink''s writer_identity is not permitted to write to the destination. A sink can export log entries only from the resource owning the sink.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_organizations_sinks_create_builder()` + `logging_organizations_sinks_create_execute()`.
/// For task-level control, use `logging_organizations_sinks_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_organizations_sinks_create(
    client: &SimpleHttpClient,
    args: &LoggingOrganizationsSinksCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogSink>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_organizations_sinks_create_builder(
        client,
        &args.parent,
        &args.customWriterIdentity,
        &args.uniqueWriterIdentity,
    )?;
    logging_organizations_sinks_create_execute(builder)
}

/// DELETE v2/organizations/{organizationsId}/sinks/{sinksId}
/// Deletes a sink. If the sink has a unique writer_identity, then that service account is also deleted.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_organizations_sinks_delete_execute()` to send, or `logging_organizations_sinks_delete` for simplest API.

pub fn logging_organizations_sinks_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    sinkName: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/organizations/{}/sinks/{sinksId}",
        sinkName,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v2/organizations/{organizationsId}/sinks/{sinksId}
/// Deletes a sink. If the sink has a unique writer_identity, then that service account is also deleted.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_organizations_sinks_delete_execute()` or `logging_organizations_sinks_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_organizations_sinks_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_organizations_sinks_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v2/organizations/{organizationsId}/sinks/{sinksId}
/// Deletes a sink. If the sink has a unique writer_identity, then that service account is also deleted.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_organizations_sinks_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_organizations_sinks_delete_task()`.
/// For the simplest API, use `logging_organizations_sinks_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_organizations_sinks_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_organizations_sinks_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = logging_organizations_sinks_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_organizations_sinks_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingOrganizationsSinksDeleteArgs {
    /// Path parameter: sinkName
    pub sinkName: String,
}

/// DELETE v2/organizations/{organizationsId}/sinks/{sinksId}
/// Deletes a sink. If the sink has a unique writer_identity, then that service account is also deleted.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_organizations_sinks_delete_builder()` + `logging_organizations_sinks_delete_execute()`.
/// For task-level control, use `logging_organizations_sinks_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_organizations_sinks_delete(
    client: &SimpleHttpClient,
    args: &LoggingOrganizationsSinksDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_organizations_sinks_delete_builder(client, &args.sinkName)?;
    logging_organizations_sinks_delete_execute(builder)
}

/// GET v2/organizations/{organizationsId}/sinks/{sinksId}
/// Gets a sink.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_organizations_sinks_get_execute()` to send, or `logging_organizations_sinks_get` for simplest API.

pub fn logging_organizations_sinks_get_builder<R>(
    client: &SimpleHttpClient<R>,
    sinkName: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/organizations/{}/sinks/{sinksId}",
        sinkName,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/organizations/{organizationsId}/sinks/{sinksId}
/// Gets a sink.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_organizations_sinks_get_execute()` or `logging_organizations_sinks_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_organizations_sinks_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_organizations_sinks_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<LogSink>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LogSink = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/organizations/{organizationsId}/sinks/{sinksId}
/// Gets a sink.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_organizations_sinks_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_organizations_sinks_get_task()`.
/// For the simplest API, use `logging_organizations_sinks_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_organizations_sinks_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_organizations_sinks_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogSink>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = logging_organizations_sinks_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_organizations_sinks_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingOrganizationsSinksGetArgs {
    /// Path parameter: sinkName
    pub sinkName: String,
}

/// GET v2/organizations/{organizationsId}/sinks/{sinksId}
/// Gets a sink.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_organizations_sinks_get_builder()` + `logging_organizations_sinks_get_execute()`.
/// For task-level control, use `logging_organizations_sinks_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_organizations_sinks_get(
    client: &SimpleHttpClient,
    args: &LoggingOrganizationsSinksGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogSink>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_organizations_sinks_get_builder(client, &args.sinkName)?;
    logging_organizations_sinks_get_execute(builder)
}

/// GET v2/organizations/{organizationsId}/sinks
/// Lists sinks.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_organizations_sinks_list_execute()` to send, or `logging_organizations_sinks_list` for simplest API.

pub fn logging_organizations_sinks_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    filter: &Option<String>,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/organizations/{}/sinks",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/organizations/{organizationsId}/sinks
/// Lists sinks.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_organizations_sinks_list_execute()` or `logging_organizations_sinks_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_organizations_sinks_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_organizations_sinks_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListSinksResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListSinksResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/organizations/{organizationsId}/sinks
/// Lists sinks.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_organizations_sinks_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_organizations_sinks_list_task()`.
/// For the simplest API, use `logging_organizations_sinks_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_organizations_sinks_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_organizations_sinks_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListSinksResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = logging_organizations_sinks_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_organizations_sinks_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingOrganizationsSinksListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v2/organizations/{organizationsId}/sinks
/// Lists sinks.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_organizations_sinks_list_builder()` + `logging_organizations_sinks_list_execute()`.
/// For task-level control, use `logging_organizations_sinks_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_organizations_sinks_list(
    client: &SimpleHttpClient,
    args: &LoggingOrganizationsSinksListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListSinksResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = logging_organizations_sinks_list_builder(
        client,
        &args.parent,
        &args.filter,
        &args.pageSize,
        &args.pageToken,
    )?;
    logging_organizations_sinks_list_execute(builder)
}

/// PATCH v2/organizations/{organizationsId}/sinks/{sinksId}
/// Updates a sink. This method replaces the values of the destination and filter fields of the existing sink with the corresponding values from the new sink.The updated sink might also have a new writer_identity; see the unique_writer_identity field.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_organizations_sinks_patch_execute()` to send, or `logging_organizations_sinks_patch` for simplest API.

pub fn logging_organizations_sinks_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    sinkName: &String,
    customWriterIdentity: &Option<String>,
    uniqueWriterIdentity: &Option<String>,
    updateMask: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/organizations/{}/sinks/{sinksId}",
        sinkName,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = customWriterIdentity.as_ref() {
        query_parts.push(format!("customWriterIdentity={}", val));
    }
    if let Some(val) = uniqueWriterIdentity.as_ref() {
        query_parts.push(format!("uniqueWriterIdentity={}", val));
    }
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v2/organizations/{organizationsId}/sinks/{sinksId}
/// Updates a sink. This method replaces the values of the destination and filter fields of the existing sink with the corresponding values from the new sink.The updated sink might also have a new writer_identity; see the unique_writer_identity field.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_organizations_sinks_patch_execute()` or `logging_organizations_sinks_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_organizations_sinks_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_organizations_sinks_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<LogSink>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LogSink = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v2/organizations/{organizationsId}/sinks/{sinksId}
/// Updates a sink. This method replaces the values of the destination and filter fields of the existing sink with the corresponding values from the new sink.The updated sink might also have a new writer_identity; see the unique_writer_identity field.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_organizations_sinks_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_organizations_sinks_patch_task()`.
/// For the simplest API, use `logging_organizations_sinks_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_organizations_sinks_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_organizations_sinks_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogSink>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = logging_organizations_sinks_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_organizations_sinks_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingOrganizationsSinksPatchArgs {
    /// Path parameter: sinkName
    pub sinkName: String,
    /// Query parameter: customWriterIdentity
    pub customWriterIdentity: Option<String>,
    /// Query parameter: uniqueWriterIdentity
    pub uniqueWriterIdentity: Option<String>,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
}

/// PATCH v2/organizations/{organizationsId}/sinks/{sinksId}
/// Updates a sink. This method replaces the values of the destination and filter fields of the existing sink with the corresponding values from the new sink.The updated sink might also have a new writer_identity; see the unique_writer_identity field.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_organizations_sinks_patch_builder()` + `logging_organizations_sinks_patch_execute()`.
/// For task-level control, use `logging_organizations_sinks_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_organizations_sinks_patch(
    client: &SimpleHttpClient,
    args: &LoggingOrganizationsSinksPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogSink>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_organizations_sinks_patch_builder(
        client,
        &args.sinkName,
        &args.customWriterIdentity,
        &args.uniqueWriterIdentity,
        &args.updateMask,
    )?;
    logging_organizations_sinks_patch_execute(builder)
}

/// PUT v2/organizations/{organizationsId}/sinks/{sinksId}
/// Updates a sink. This method replaces the values of the destination and filter fields of the existing sink with the corresponding values from the new sink.The updated sink might also have a new writer_identity; see the unique_writer_identity field.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_organizations_sinks_update_execute()` to send, or `logging_organizations_sinks_update` for simplest API.

pub fn logging_organizations_sinks_update_builder<R>(
    client: &SimpleHttpClient<R>,
    sinkName: &String,
    customWriterIdentity: &Option<String>,
    uniqueWriterIdentity: &Option<String>,
    updateMask: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/organizations/{}/sinks/{sinksId}",
        sinkName,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = customWriterIdentity.as_ref() {
        query_parts.push(format!("customWriterIdentity={}", val));
    }
    if let Some(val) = uniqueWriterIdentity.as_ref() {
        query_parts.push(format!("uniqueWriterIdentity={}", val));
    }
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .put(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PUT v2/organizations/{organizationsId}/sinks/{sinksId}
/// Updates a sink. This method replaces the values of the destination and filter fields of the existing sink with the corresponding values from the new sink.The updated sink might also have a new writer_identity; see the unique_writer_identity field.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_organizations_sinks_update_execute()` or `logging_organizations_sinks_update`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_organizations_sinks_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_organizations_sinks_update_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<LogSink>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LogSink = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PUT v2/organizations/{organizationsId}/sinks/{sinksId}
/// Updates a sink. This method replaces the values of the destination and filter fields of the existing sink with the corresponding values from the new sink.The updated sink might also have a new writer_identity; see the unique_writer_identity field.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_organizations_sinks_update_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_organizations_sinks_update_task()`.
/// For the simplest API, use `logging_organizations_sinks_update()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_organizations_sinks_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_organizations_sinks_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogSink>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = logging_organizations_sinks_update_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_organizations_sinks_update`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingOrganizationsSinksUpdateArgs {
    /// Path parameter: sinkName
    pub sinkName: String,
    /// Query parameter: customWriterIdentity
    pub customWriterIdentity: Option<String>,
    /// Query parameter: uniqueWriterIdentity
    pub uniqueWriterIdentity: Option<String>,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
}

/// PUT v2/organizations/{organizationsId}/sinks/{sinksId}
/// Updates a sink. This method replaces the values of the destination and filter fields of the existing sink with the corresponding values from the new sink.The updated sink might also have a new writer_identity; see the unique_writer_identity field.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_organizations_sinks_update_builder()` + `logging_organizations_sinks_update_execute()`.
/// For task-level control, use `logging_organizations_sinks_update_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_organizations_sinks_update(
    client: &SimpleHttpClient,
    args: &LoggingOrganizationsSinksUpdateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogSink>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_organizations_sinks_update_builder(
        client,
        &args.sinkName,
        &args.customWriterIdentity,
        &args.uniqueWriterIdentity,
        &args.updateMask,
    )?;
    logging_organizations_sinks_update_execute(builder)
}

/// GET v2/projects/{projectsId}/cmekSettings
/// Gets the Logging CMEK settings for the given resource.Note: CMEK for the Log Router can be configured for Google Cloud projects, folders, organizations, and billing accounts. Once configured for an organization, it applies to all projects and folders in the Google Cloud organization.See Configure CMEK for Cloud Logging (<https://docs.cloud.google.`com/logging/docs/routing/managed-encryption`>) for more information.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_projects_get_cmek_settings_execute()` to send, or `logging_projects_get_cmek_settings` for simplest API.

pub fn logging_projects_get_cmek_settings_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/projects/{}/cmekSettings",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/projects/{projectsId}/cmekSettings
/// Gets the Logging CMEK settings for the given resource.Note: CMEK for the Log Router can be configured for Google Cloud projects, folders, organizations, and billing accounts. Once configured for an organization, it applies to all projects and folders in the Google Cloud organization.See Configure CMEK for Cloud Logging (<https://docs.cloud.google.`com/logging/docs/routing/managed-encryption`>) for more information.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_projects_get_cmek_settings_execute()` or `logging_projects_get_cmek_settings`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_projects_get_cmek_settings_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_projects_get_cmek_settings_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<CmekSettings>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: CmekSettings = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/projects/{projectsId}/cmekSettings
/// Gets the Logging CMEK settings for the given resource.Note: CMEK for the Log Router can be configured for Google Cloud projects, folders, organizations, and billing accounts. Once configured for an organization, it applies to all projects and folders in the Google Cloud organization.See Configure CMEK for Cloud Logging (<https://docs.cloud.google.`com/logging/docs/routing/managed-encryption`>) for more information.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_projects_get_cmek_settings_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_projects_get_cmek_settings_task()`.
/// For the simplest API, use `logging_projects_get_cmek_settings()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_projects_get_cmek_settings_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_projects_get_cmek_settings_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CmekSettings>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = logging_projects_get_cmek_settings_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_projects_get_cmek_settings`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingProjectsGetCmekSettingsArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v2/projects/{projectsId}/cmekSettings
/// Gets the Logging CMEK settings for the given resource.Note: CMEK for the Log Router can be configured for Google Cloud projects, folders, organizations, and billing accounts. Once configured for an organization, it applies to all projects and folders in the Google Cloud organization.See Configure CMEK for Cloud Logging (<https://docs.cloud.google.`com/logging/docs/routing/managed-encryption`>) for more information.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_projects_get_cmek_settings_builder()` + `logging_projects_get_cmek_settings_execute()`.
/// For task-level control, use `logging_projects_get_cmek_settings_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_projects_get_cmek_settings(
    client: &SimpleHttpClient,
    args: &LoggingProjectsGetCmekSettingsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CmekSettings>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = logging_projects_get_cmek_settings_builder(client, &args.name)?;
    logging_projects_get_cmek_settings_execute(builder)
}

/// GET v2/projects/{projectsId}/settings
/// Gets the settings for the given resource.Note: Settings can be retrieved for Google Cloud projects, folders, organizations, and billing accounts.See View default resource settings for Logging (<https://docs.cloud.google.`com/logging/docs/default-settings`#view-org-settings>) for more information.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_projects_get_settings_execute()` to send, or `logging_projects_get_settings` for simplest API.

pub fn logging_projects_get_settings_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/projects/{}/settings",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/projects/{projectsId}/settings
/// Gets the settings for the given resource.Note: Settings can be retrieved for Google Cloud projects, folders, organizations, and billing accounts.See View default resource settings for Logging (<https://docs.cloud.google.`com/logging/docs/default-settings`#view-org-settings>) for more information.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_projects_get_settings_execute()` or `logging_projects_get_settings`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_projects_get_settings_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_projects_get_settings_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Settings>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Settings = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/projects/{projectsId}/settings
/// Gets the settings for the given resource.Note: Settings can be retrieved for Google Cloud projects, folders, organizations, and billing accounts.See View default resource settings for Logging (<https://docs.cloud.google.`com/logging/docs/default-settings`#view-org-settings>) for more information.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_projects_get_settings_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_projects_get_settings_task()`.
/// For the simplest API, use `logging_projects_get_settings()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_projects_get_settings_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_projects_get_settings_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Settings>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = logging_projects_get_settings_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_projects_get_settings`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingProjectsGetSettingsArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v2/projects/{projectsId}/settings
/// Gets the settings for the given resource.Note: Settings can be retrieved for Google Cloud projects, folders, organizations, and billing accounts.See View default resource settings for Logging (<https://docs.cloud.google.`com/logging/docs/default-settings`#view-org-settings>) for more information.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_projects_get_settings_builder()` + `logging_projects_get_settings_execute()`.
/// For task-level control, use `logging_projects_get_settings_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_projects_get_settings(
    client: &SimpleHttpClient,
    args: &LoggingProjectsGetSettingsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Settings>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_projects_get_settings_builder(client, &args.name)?;
    logging_projects_get_settings_execute(builder)
}

/// POST v2/projects/{projectsId}/exclusions
/// Creates a new exclusion in the _Default sink in a specified parent resource. Only log entries belonging to that resource can be excluded. You can have up to 10 exclusions in a resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_projects_exclusions_create_execute()` to send, or `logging_projects_exclusions_create` for simplest API.

pub fn logging_projects_exclusions_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/projects/{}/exclusions",
        parent,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v2/projects/{projectsId}/exclusions
/// Creates a new exclusion in the _Default sink in a specified parent resource. Only log entries belonging to that resource can be excluded. You can have up to 10 exclusions in a resource.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_projects_exclusions_create_execute()` or `logging_projects_exclusions_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_projects_exclusions_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_projects_exclusions_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<LogExclusion>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LogExclusion = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v2/projects/{projectsId}/exclusions
/// Creates a new exclusion in the _Default sink in a specified parent resource. Only log entries belonging to that resource can be excluded. You can have up to 10 exclusions in a resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_projects_exclusions_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_projects_exclusions_create_task()`.
/// For the simplest API, use `logging_projects_exclusions_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_projects_exclusions_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_projects_exclusions_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogExclusion>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = logging_projects_exclusions_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_projects_exclusions_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingProjectsExclusionsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// POST v2/projects/{projectsId}/exclusions
/// Creates a new exclusion in the _Default sink in a specified parent resource. Only log entries belonging to that resource can be excluded. You can have up to 10 exclusions in a resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_projects_exclusions_create_builder()` + `logging_projects_exclusions_create_execute()`.
/// For task-level control, use `logging_projects_exclusions_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_projects_exclusions_create(
    client: &SimpleHttpClient,
    args: &LoggingProjectsExclusionsCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogExclusion>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = logging_projects_exclusions_create_builder(client, &args.parent)?;
    logging_projects_exclusions_create_execute(builder)
}

/// DELETE v2/projects/{projectsId}/exclusions/{exclusionsId}
/// Deletes an exclusion in the _Default sink.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_projects_exclusions_delete_execute()` to send, or `logging_projects_exclusions_delete` for simplest API.

pub fn logging_projects_exclusions_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/projects/{}/exclusions/{exclusionsId}",
        name,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v2/projects/{projectsId}/exclusions/{exclusionsId}
/// Deletes an exclusion in the _Default sink.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_projects_exclusions_delete_execute()` or `logging_projects_exclusions_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_projects_exclusions_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_projects_exclusions_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v2/projects/{projectsId}/exclusions/{exclusionsId}
/// Deletes an exclusion in the _Default sink.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_projects_exclusions_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_projects_exclusions_delete_task()`.
/// For the simplest API, use `logging_projects_exclusions_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_projects_exclusions_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_projects_exclusions_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = logging_projects_exclusions_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_projects_exclusions_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingProjectsExclusionsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// DELETE v2/projects/{projectsId}/exclusions/{exclusionsId}
/// Deletes an exclusion in the _Default sink.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_projects_exclusions_delete_builder()` + `logging_projects_exclusions_delete_execute()`.
/// For task-level control, use `logging_projects_exclusions_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_projects_exclusions_delete(
    client: &SimpleHttpClient,
    args: &LoggingProjectsExclusionsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_projects_exclusions_delete_builder(client, &args.name)?;
    logging_projects_exclusions_delete_execute(builder)
}

/// GET v2/projects/{projectsId}/exclusions/{exclusionsId}
/// Gets the description of an exclusion in the _Default sink.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_projects_exclusions_get_execute()` to send, or `logging_projects_exclusions_get` for simplest API.

pub fn logging_projects_exclusions_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/projects/{}/exclusions/{exclusionsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/projects/{projectsId}/exclusions/{exclusionsId}
/// Gets the description of an exclusion in the _Default sink.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_projects_exclusions_get_execute()` or `logging_projects_exclusions_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_projects_exclusions_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_projects_exclusions_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<LogExclusion>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LogExclusion = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/projects/{projectsId}/exclusions/{exclusionsId}
/// Gets the description of an exclusion in the _Default sink.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_projects_exclusions_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_projects_exclusions_get_task()`.
/// For the simplest API, use `logging_projects_exclusions_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_projects_exclusions_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_projects_exclusions_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogExclusion>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = logging_projects_exclusions_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_projects_exclusions_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingProjectsExclusionsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v2/projects/{projectsId}/exclusions/{exclusionsId}
/// Gets the description of an exclusion in the _Default sink.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_projects_exclusions_get_builder()` + `logging_projects_exclusions_get_execute()`.
/// For task-level control, use `logging_projects_exclusions_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_projects_exclusions_get(
    client: &SimpleHttpClient,
    args: &LoggingProjectsExclusionsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogExclusion>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = logging_projects_exclusions_get_builder(client, &args.name)?;
    logging_projects_exclusions_get_execute(builder)
}

/// GET v2/projects/{projectsId}/exclusions
/// Lists all the exclusions on the _Default sink in a parent resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_projects_exclusions_list_execute()` to send, or `logging_projects_exclusions_list` for simplest API.

pub fn logging_projects_exclusions_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/projects/{}/exclusions",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/projects/{projectsId}/exclusions
/// Lists all the exclusions on the _Default sink in a parent resource.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_projects_exclusions_list_execute()` or `logging_projects_exclusions_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_projects_exclusions_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_projects_exclusions_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListExclusionsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListExclusionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/projects/{projectsId}/exclusions
/// Lists all the exclusions on the _Default sink in a parent resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_projects_exclusions_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_projects_exclusions_list_task()`.
/// For the simplest API, use `logging_projects_exclusions_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_projects_exclusions_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_projects_exclusions_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListExclusionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = logging_projects_exclusions_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_projects_exclusions_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingProjectsExclusionsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v2/projects/{projectsId}/exclusions
/// Lists all the exclusions on the _Default sink in a parent resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_projects_exclusions_list_builder()` + `logging_projects_exclusions_list_execute()`.
/// For task-level control, use `logging_projects_exclusions_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_projects_exclusions_list(
    client: &SimpleHttpClient,
    args: &LoggingProjectsExclusionsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListExclusionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = logging_projects_exclusions_list_builder(
        client,
        &args.parent,
        &args.pageSize,
        &args.pageToken,
    )?;
    logging_projects_exclusions_list_execute(builder)
}

/// PATCH v2/projects/{projectsId}/exclusions/{exclusionsId}
/// Changes one or more properties of an existing exclusion in the _Default sink.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_projects_exclusions_patch_execute()` to send, or `logging_projects_exclusions_patch` for simplest API.

pub fn logging_projects_exclusions_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    updateMask: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/projects/{}/exclusions/{exclusionsId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v2/projects/{projectsId}/exclusions/{exclusionsId}
/// Changes one or more properties of an existing exclusion in the _Default sink.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_projects_exclusions_patch_execute()` or `logging_projects_exclusions_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_projects_exclusions_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_projects_exclusions_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<LogExclusion>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LogExclusion = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v2/projects/{projectsId}/exclusions/{exclusionsId}
/// Changes one or more properties of an existing exclusion in the _Default sink.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_projects_exclusions_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_projects_exclusions_patch_task()`.
/// For the simplest API, use `logging_projects_exclusions_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_projects_exclusions_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_projects_exclusions_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogExclusion>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = logging_projects_exclusions_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_projects_exclusions_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingProjectsExclusionsPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
}

/// PATCH v2/projects/{projectsId}/exclusions/{exclusionsId}
/// Changes one or more properties of an existing exclusion in the _Default sink.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_projects_exclusions_patch_builder()` + `logging_projects_exclusions_patch_execute()`.
/// For task-level control, use `logging_projects_exclusions_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_projects_exclusions_patch(
    client: &SimpleHttpClient,
    args: &LoggingProjectsExclusionsPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogExclusion>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = logging_projects_exclusions_patch_builder(client, &args.name, &args.updateMask)?;
    logging_projects_exclusions_patch_execute(builder)
}

/// GET v2/projects/{projectsId}/locations/{locationsId}
/// Gets information about a location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_projects_locations_get_execute()` to send, or `logging_projects_locations_get` for simplest API.

pub fn logging_projects_locations_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/projects/{}/locations/{locationsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/projects/{projectsId}/locations/{locationsId}
/// Gets information about a location.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_projects_locations_get_execute()` or `logging_projects_locations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_projects_locations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_projects_locations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Location>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Location = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/projects/{projectsId}/locations/{locationsId}
/// Gets information about a location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_projects_locations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_projects_locations_get_task()`.
/// For the simplest API, use `logging_projects_locations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_projects_locations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_projects_locations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Location>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = logging_projects_locations_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_projects_locations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingProjectsLocationsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v2/projects/{projectsId}/locations/{locationsId}
/// Gets information about a location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_projects_locations_get_builder()` + `logging_projects_locations_get_execute()`.
/// For task-level control, use `logging_projects_locations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_projects_locations_get(
    client: &SimpleHttpClient,
    args: &LoggingProjectsLocationsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Location>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_projects_locations_get_builder(client, &args.name)?;
    logging_projects_locations_get_execute(builder)
}

/// GET v2/projects/{projectsId}/locations
/// Lists information about the supported locations for this service.This method lists locations based on the resource scope provided in the ListLocationsRequest.name field: Global locations: If name is empty, the method lists the public locations available to all projects. Project-specific locations: If name follows the format `projects/{project}`, the method lists locations visible to that specific project. This includes public, private, or other project-specific locations enabled for the project.For `gRPC` and client library implementations, the resource name is passed as the name field. For direct service calls, the resource name is incorporated into the request path based on the specific service implementation and version.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_projects_locations_list_execute()` to send, or `logging_projects_locations_list` for simplest API.

pub fn logging_projects_locations_list_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    extraLocationTypes: &Option<String>,
    filter: &Option<String>,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/projects/{}/locations",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = extraLocationTypes.as_ref() {
        query_parts.push(format!("extraLocationTypes={}", val));
    }
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/projects/{projectsId}/locations
/// Lists information about the supported locations for this service.This method lists locations based on the resource scope provided in the ListLocationsRequest.name field: Global locations: If name is empty, the method lists the public locations available to all projects. Project-specific locations: If name follows the format `projects/{project}`, the method lists locations visible to that specific project. This includes public, private, or other project-specific locations enabled for the project.For `gRPC` and client library implementations, the resource name is passed as the name field. For direct service calls, the resource name is incorporated into the request path based on the specific service implementation and version.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_projects_locations_list_execute()` or `logging_projects_locations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_projects_locations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_projects_locations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListLocationsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListLocationsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/projects/{projectsId}/locations
/// Lists information about the supported locations for this service.This method lists locations based on the resource scope provided in the ListLocationsRequest.name field: Global locations: If name is empty, the method lists the public locations available to all projects. Project-specific locations: If name follows the format `projects/{project}`, the method lists locations visible to that specific project. This includes public, private, or other project-specific locations enabled for the project.For `gRPC` and client library implementations, the resource name is passed as the name field. For direct service calls, the resource name is incorporated into the request path based on the specific service implementation and version.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_projects_locations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_projects_locations_list_task()`.
/// For the simplest API, use `logging_projects_locations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_projects_locations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_projects_locations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListLocationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = logging_projects_locations_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_projects_locations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingProjectsLocationsListArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: extraLocationTypes
    pub extraLocationTypes: Option<String>,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v2/projects/{projectsId}/locations
/// Lists information about the supported locations for this service.This method lists locations based on the resource scope provided in the ListLocationsRequest.name field: Global locations: If name is empty, the method lists the public locations available to all projects. Project-specific locations: If name follows the format `projects/{project}`, the method lists locations visible to that specific project. This includes public, private, or other project-specific locations enabled for the project.For `gRPC` and client library implementations, the resource name is passed as the name field. For direct service calls, the resource name is incorporated into the request path based on the specific service implementation and version.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_projects_locations_list_builder()` + `logging_projects_locations_list_execute()`.
/// For task-level control, use `logging_projects_locations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_projects_locations_list(
    client: &SimpleHttpClient,
    args: &LoggingProjectsLocationsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListLocationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = logging_projects_locations_list_builder(
        client,
        &args.name,
        &args.extraLocationTypes,
        &args.filter,
        &args.pageSize,
        &args.pageToken,
    )?;
    logging_projects_locations_list_execute(builder)
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/buckets
/// Creates a log bucket that can be used to store log entries. After a bucket has been created, the bucket''s location cannot be changed.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_projects_locations_buckets_create_execute()` to send, or `logging_projects_locations_buckets_create` for simplest API.

pub fn logging_projects_locations_buckets_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    bucketId: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/projects/{}/locations/{locationsId}/buckets",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = bucketId.as_ref() {
        query_parts.push(format!("bucketId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/buckets
/// Creates a log bucket that can be used to store log entries. After a bucket has been created, the bucket''s location cannot be changed.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_projects_locations_buckets_create_execute()` or `logging_projects_locations_buckets_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_projects_locations_buckets_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_projects_locations_buckets_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<LogBucket>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LogBucket = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/buckets
/// Creates a log bucket that can be used to store log entries. After a bucket has been created, the bucket''s location cannot be changed.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_projects_locations_buckets_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_projects_locations_buckets_create_task()`.
/// For the simplest API, use `logging_projects_locations_buckets_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_projects_locations_buckets_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_projects_locations_buckets_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogBucket>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = logging_projects_locations_buckets_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_projects_locations_buckets_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingProjectsLocationsBucketsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: bucketId
    pub bucketId: Option<String>,
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/buckets
/// Creates a log bucket that can be used to store log entries. After a bucket has been created, the bucket''s location cannot be changed.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_projects_locations_buckets_create_builder()` + `logging_projects_locations_buckets_create_execute()`.
/// For task-level control, use `logging_projects_locations_buckets_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_projects_locations_buckets_create(
    client: &SimpleHttpClient,
    args: &LoggingProjectsLocationsBucketsCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogBucket>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        logging_projects_locations_buckets_create_builder(client, &args.parent, &args.bucketId)?;
    logging_projects_locations_buckets_create_execute(builder)
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/buckets:createAsync
/// Creates a log bucket asynchronously that can be used to store log entries.After a bucket has been created, the bucket''s location cannot be changed.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_projects_locations_buckets_create_async_execute()` to send, or `logging_projects_locations_buckets_create_async` for simplest API.

pub fn logging_projects_locations_buckets_create_async_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    bucketId: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/projects/{}/locations/{locationsId}/buckets:createAsync",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = bucketId.as_ref() {
        query_parts.push(format!("bucketId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/buckets:createAsync
/// Creates a log bucket asynchronously that can be used to store log entries.After a bucket has been created, the bucket''s location cannot be changed.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_projects_locations_buckets_create_async_execute()` or `logging_projects_locations_buckets_create_async`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_projects_locations_buckets_create_async_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_projects_locations_buckets_create_async_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/buckets:createAsync
/// Creates a log bucket asynchronously that can be used to store log entries.After a bucket has been created, the bucket''s location cannot be changed.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_projects_locations_buckets_create_async_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_projects_locations_buckets_create_async_task()`.
/// For the simplest API, use `logging_projects_locations_buckets_create_async()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_projects_locations_buckets_create_async_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_projects_locations_buckets_create_async_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = logging_projects_locations_buckets_create_async_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_projects_locations_buckets_create_async`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingProjectsLocationsBucketsCreateAsyncArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: bucketId
    pub bucketId: Option<String>,
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/buckets:createAsync
/// Creates a log bucket asynchronously that can be used to store log entries.After a bucket has been created, the bucket''s location cannot be changed.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_projects_locations_buckets_create_async_builder()` + `logging_projects_locations_buckets_create_async_execute()`.
/// For task-level control, use `logging_projects_locations_buckets_create_async_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_projects_locations_buckets_create_async(
    client: &SimpleHttpClient,
    args: &LoggingProjectsLocationsBucketsCreateAsyncArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_projects_locations_buckets_create_async_builder(
        client,
        &args.parent,
        &args.bucketId,
    )?;
    logging_projects_locations_buckets_create_async_execute(builder)
}

/// DELETE v2/projects/{projectsId}/locations/{locationsId}/buckets/{bucketsId}
/// Deletes a log bucket.Changes the bucket''s lifecycle_state to the DELETE_REQUESTED state. After 7 days, the bucket will be purged and all log entries in the bucket will be permanently deleted.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_projects_locations_buckets_delete_execute()` to send, or `logging_projects_locations_buckets_delete` for simplest API.

pub fn logging_projects_locations_buckets_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/projects/{}/locations/{locationsId}/buckets/{bucketsId}",
        name,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v2/projects/{projectsId}/locations/{locationsId}/buckets/{bucketsId}
/// Deletes a log bucket.Changes the bucket''s lifecycle_state to the DELETE_REQUESTED state. After 7 days, the bucket will be purged and all log entries in the bucket will be permanently deleted.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_projects_locations_buckets_delete_execute()` or `logging_projects_locations_buckets_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_projects_locations_buckets_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_projects_locations_buckets_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v2/projects/{projectsId}/locations/{locationsId}/buckets/{bucketsId}
/// Deletes a log bucket.Changes the bucket''s lifecycle_state to the DELETE_REQUESTED state. After 7 days, the bucket will be purged and all log entries in the bucket will be permanently deleted.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_projects_locations_buckets_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_projects_locations_buckets_delete_task()`.
/// For the simplest API, use `logging_projects_locations_buckets_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_projects_locations_buckets_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_projects_locations_buckets_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = logging_projects_locations_buckets_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_projects_locations_buckets_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingProjectsLocationsBucketsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// DELETE v2/projects/{projectsId}/locations/{locationsId}/buckets/{bucketsId}
/// Deletes a log bucket.Changes the bucket''s lifecycle_state to the DELETE_REQUESTED state. After 7 days, the bucket will be purged and all log entries in the bucket will be permanently deleted.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_projects_locations_buckets_delete_builder()` + `logging_projects_locations_buckets_delete_execute()`.
/// For task-level control, use `logging_projects_locations_buckets_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_projects_locations_buckets_delete(
    client: &SimpleHttpClient,
    args: &LoggingProjectsLocationsBucketsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_projects_locations_buckets_delete_builder(client, &args.name)?;
    logging_projects_locations_buckets_delete_execute(builder)
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/buckets/{bucketsId}
/// Gets a log bucket.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_projects_locations_buckets_get_execute()` to send, or `logging_projects_locations_buckets_get` for simplest API.

pub fn logging_projects_locations_buckets_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/projects/{}/locations/{locationsId}/buckets/{bucketsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/buckets/{bucketsId}
/// Gets a log bucket.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_projects_locations_buckets_get_execute()` or `logging_projects_locations_buckets_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_projects_locations_buckets_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_projects_locations_buckets_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<LogBucket>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LogBucket = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/buckets/{bucketsId}
/// Gets a log bucket.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_projects_locations_buckets_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_projects_locations_buckets_get_task()`.
/// For the simplest API, use `logging_projects_locations_buckets_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_projects_locations_buckets_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_projects_locations_buckets_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogBucket>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = logging_projects_locations_buckets_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_projects_locations_buckets_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingProjectsLocationsBucketsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/buckets/{bucketsId}
/// Gets a log bucket.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_projects_locations_buckets_get_builder()` + `logging_projects_locations_buckets_get_execute()`.
/// For task-level control, use `logging_projects_locations_buckets_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_projects_locations_buckets_get(
    client: &SimpleHttpClient,
    args: &LoggingProjectsLocationsBucketsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogBucket>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_projects_locations_buckets_get_builder(client, &args.name)?;
    logging_projects_locations_buckets_get_execute(builder)
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/buckets
/// Lists log buckets.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_projects_locations_buckets_list_execute()` to send, or `logging_projects_locations_buckets_list` for simplest API.

pub fn logging_projects_locations_buckets_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/projects/{}/locations/{locationsId}/buckets",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/buckets
/// Lists log buckets.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_projects_locations_buckets_list_execute()` or `logging_projects_locations_buckets_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_projects_locations_buckets_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_projects_locations_buckets_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListBucketsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListBucketsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/buckets
/// Lists log buckets.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_projects_locations_buckets_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_projects_locations_buckets_list_task()`.
/// For the simplest API, use `logging_projects_locations_buckets_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_projects_locations_buckets_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_projects_locations_buckets_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListBucketsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = logging_projects_locations_buckets_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_projects_locations_buckets_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingProjectsLocationsBucketsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/buckets
/// Lists log buckets.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_projects_locations_buckets_list_builder()` + `logging_projects_locations_buckets_list_execute()`.
/// For task-level control, use `logging_projects_locations_buckets_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_projects_locations_buckets_list(
    client: &SimpleHttpClient,
    args: &LoggingProjectsLocationsBucketsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListBucketsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = logging_projects_locations_buckets_list_builder(
        client,
        &args.parent,
        &args.pageSize,
        &args.pageToken,
    )?;
    logging_projects_locations_buckets_list_execute(builder)
}

/// PATCH v2/projects/{projectsId}/locations/{locationsId}/buckets/{bucketsId}
/// Updates a log bucket.If the bucket has a lifecycle_state of DELETE_REQUESTED, then FAILED_PRECONDITION will be returned.After a bucket has been created, the bucket''s location cannot be changed.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_projects_locations_buckets_patch_execute()` to send, or `logging_projects_locations_buckets_patch` for simplest API.

pub fn logging_projects_locations_buckets_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    updateMask: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/projects/{}/locations/{locationsId}/buckets/{bucketsId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v2/projects/{projectsId}/locations/{locationsId}/buckets/{bucketsId}
/// Updates a log bucket.If the bucket has a lifecycle_state of DELETE_REQUESTED, then FAILED_PRECONDITION will be returned.After a bucket has been created, the bucket''s location cannot be changed.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_projects_locations_buckets_patch_execute()` or `logging_projects_locations_buckets_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_projects_locations_buckets_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_projects_locations_buckets_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<LogBucket>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LogBucket = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v2/projects/{projectsId}/locations/{locationsId}/buckets/{bucketsId}
/// Updates a log bucket.If the bucket has a lifecycle_state of DELETE_REQUESTED, then FAILED_PRECONDITION will be returned.After a bucket has been created, the bucket''s location cannot be changed.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_projects_locations_buckets_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_projects_locations_buckets_patch_task()`.
/// For the simplest API, use `logging_projects_locations_buckets_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_projects_locations_buckets_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_projects_locations_buckets_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogBucket>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = logging_projects_locations_buckets_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_projects_locations_buckets_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingProjectsLocationsBucketsPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
}

/// PATCH v2/projects/{projectsId}/locations/{locationsId}/buckets/{bucketsId}
/// Updates a log bucket.If the bucket has a lifecycle_state of DELETE_REQUESTED, then FAILED_PRECONDITION will be returned.After a bucket has been created, the bucket''s location cannot be changed.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_projects_locations_buckets_patch_builder()` + `logging_projects_locations_buckets_patch_execute()`.
/// For task-level control, use `logging_projects_locations_buckets_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_projects_locations_buckets_patch(
    client: &SimpleHttpClient,
    args: &LoggingProjectsLocationsBucketsPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogBucket>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        logging_projects_locations_buckets_patch_builder(client, &args.name, &args.updateMask)?;
    logging_projects_locations_buckets_patch_execute(builder)
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/buckets/{bucketsId}:undelete
/// Undeletes a log bucket. A bucket that has been deleted can be undeleted within the grace period of 7 days.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_projects_locations_buckets_undelete_execute()` to send, or `logging_projects_locations_buckets_undelete` for simplest API.

pub fn logging_projects_locations_buckets_undelete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/projects/{}/locations/{locationsId}/buckets/{bucketsId}:undelete",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/buckets/{bucketsId}:undelete
/// Undeletes a log bucket. A bucket that has been deleted can be undeleted within the grace period of 7 days.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_projects_locations_buckets_undelete_execute()` or `logging_projects_locations_buckets_undelete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_projects_locations_buckets_undelete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_projects_locations_buckets_undelete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/buckets/{bucketsId}:undelete
/// Undeletes a log bucket. A bucket that has been deleted can be undeleted within the grace period of 7 days.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_projects_locations_buckets_undelete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_projects_locations_buckets_undelete_task()`.
/// For the simplest API, use `logging_projects_locations_buckets_undelete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_projects_locations_buckets_undelete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_projects_locations_buckets_undelete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = logging_projects_locations_buckets_undelete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_projects_locations_buckets_undelete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingProjectsLocationsBucketsUndeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/buckets/{bucketsId}:undelete
/// Undeletes a log bucket. A bucket that has been deleted can be undeleted within the grace period of 7 days.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_projects_locations_buckets_undelete_builder()` + `logging_projects_locations_buckets_undelete_execute()`.
/// For task-level control, use `logging_projects_locations_buckets_undelete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_projects_locations_buckets_undelete(
    client: &SimpleHttpClient,
    args: &LoggingProjectsLocationsBucketsUndeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_projects_locations_buckets_undelete_builder(client, &args.name)?;
    logging_projects_locations_buckets_undelete_execute(builder)
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/buckets/{bucketsId}:updateAsync
/// Updates a log bucket asynchronously.If the bucket has a lifecycle_state of DELETE_REQUESTED, then FAILED_PRECONDITION will be returned.After a bucket has been created, the bucket''s location cannot be changed.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_projects_locations_buckets_update_async_execute()` to send, or `logging_projects_locations_buckets_update_async` for simplest API.

pub fn logging_projects_locations_buckets_update_async_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    updateMask: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/projects/{}/locations/{locationsId}/buckets/{bucketsId}:updateAsync",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/buckets/{bucketsId}:updateAsync
/// Updates a log bucket asynchronously.If the bucket has a lifecycle_state of DELETE_REQUESTED, then FAILED_PRECONDITION will be returned.After a bucket has been created, the bucket''s location cannot be changed.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_projects_locations_buckets_update_async_execute()` or `logging_projects_locations_buckets_update_async`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_projects_locations_buckets_update_async_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_projects_locations_buckets_update_async_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/buckets/{bucketsId}:updateAsync
/// Updates a log bucket asynchronously.If the bucket has a lifecycle_state of DELETE_REQUESTED, then FAILED_PRECONDITION will be returned.After a bucket has been created, the bucket''s location cannot be changed.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_projects_locations_buckets_update_async_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_projects_locations_buckets_update_async_task()`.
/// For the simplest API, use `logging_projects_locations_buckets_update_async()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_projects_locations_buckets_update_async_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_projects_locations_buckets_update_async_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = logging_projects_locations_buckets_update_async_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_projects_locations_buckets_update_async`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingProjectsLocationsBucketsUpdateAsyncArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/buckets/{bucketsId}:updateAsync
/// Updates a log bucket asynchronously.If the bucket has a lifecycle_state of DELETE_REQUESTED, then FAILED_PRECONDITION will be returned.After a bucket has been created, the bucket''s location cannot be changed.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_projects_locations_buckets_update_async_builder()` + `logging_projects_locations_buckets_update_async_execute()`.
/// For task-level control, use `logging_projects_locations_buckets_update_async_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_projects_locations_buckets_update_async(
    client: &SimpleHttpClient,
    args: &LoggingProjectsLocationsBucketsUpdateAsyncArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_projects_locations_buckets_update_async_builder(
        client,
        &args.name,
        &args.updateMask,
    )?;
    logging_projects_locations_buckets_update_async_execute(builder)
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/buckets/{bucketsId}/links
/// Asynchronously creates a linked dataset in BigQuery which makes it possible to use BigQuery to read the logs stored in the log bucket. A log bucket may currently only contain one link.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_projects_locations_buckets_links_create_execute()` to send, or `logging_projects_locations_buckets_links_create` for simplest API.

pub fn logging_projects_locations_buckets_links_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    linkId: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/projects/{}/locations/{locationsId}/buckets/{bucketsId}/links",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = linkId.as_ref() {
        query_parts.push(format!("linkId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/buckets/{bucketsId}/links
/// Asynchronously creates a linked dataset in BigQuery which makes it possible to use BigQuery to read the logs stored in the log bucket. A log bucket may currently only contain one link.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_projects_locations_buckets_links_create_execute()` or `logging_projects_locations_buckets_links_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_projects_locations_buckets_links_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_projects_locations_buckets_links_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/buckets/{bucketsId}/links
/// Asynchronously creates a linked dataset in BigQuery which makes it possible to use BigQuery to read the logs stored in the log bucket. A log bucket may currently only contain one link.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_projects_locations_buckets_links_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_projects_locations_buckets_links_create_task()`.
/// For the simplest API, use `logging_projects_locations_buckets_links_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_projects_locations_buckets_links_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_projects_locations_buckets_links_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = logging_projects_locations_buckets_links_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_projects_locations_buckets_links_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingProjectsLocationsBucketsLinksCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: linkId
    pub linkId: Option<String>,
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/buckets/{bucketsId}/links
/// Asynchronously creates a linked dataset in BigQuery which makes it possible to use BigQuery to read the logs stored in the log bucket. A log bucket may currently only contain one link.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_projects_locations_buckets_links_create_builder()` + `logging_projects_locations_buckets_links_create_execute()`.
/// For task-level control, use `logging_projects_locations_buckets_links_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_projects_locations_buckets_links_create(
    client: &SimpleHttpClient,
    args: &LoggingProjectsLocationsBucketsLinksCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_projects_locations_buckets_links_create_builder(
        client,
        &args.parent,
        &args.linkId,
    )?;
    logging_projects_locations_buckets_links_create_execute(builder)
}

/// DELETE v2/projects/{projectsId}/locations/{locationsId}/buckets/{bucketsId}/links/{linksId}
/// Deletes a link. This will also delete the corresponding BigQuery linked dataset.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_projects_locations_buckets_links_delete_execute()` to send, or `logging_projects_locations_buckets_links_delete` for simplest API.

pub fn logging_projects_locations_buckets_links_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/projects/{}/locations/{locationsId}/buckets/{bucketsId}/links/{linksId}",
        name,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v2/projects/{projectsId}/locations/{locationsId}/buckets/{bucketsId}/links/{linksId}
/// Deletes a link. This will also delete the corresponding BigQuery linked dataset.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_projects_locations_buckets_links_delete_execute()` or `logging_projects_locations_buckets_links_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_projects_locations_buckets_links_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_projects_locations_buckets_links_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v2/projects/{projectsId}/locations/{locationsId}/buckets/{bucketsId}/links/{linksId}
/// Deletes a link. This will also delete the corresponding BigQuery linked dataset.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_projects_locations_buckets_links_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_projects_locations_buckets_links_delete_task()`.
/// For the simplest API, use `logging_projects_locations_buckets_links_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_projects_locations_buckets_links_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_projects_locations_buckets_links_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = logging_projects_locations_buckets_links_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_projects_locations_buckets_links_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingProjectsLocationsBucketsLinksDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// DELETE v2/projects/{projectsId}/locations/{locationsId}/buckets/{bucketsId}/links/{linksId}
/// Deletes a link. This will also delete the corresponding BigQuery linked dataset.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_projects_locations_buckets_links_delete_builder()` + `logging_projects_locations_buckets_links_delete_execute()`.
/// For task-level control, use `logging_projects_locations_buckets_links_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_projects_locations_buckets_links_delete(
    client: &SimpleHttpClient,
    args: &LoggingProjectsLocationsBucketsLinksDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_projects_locations_buckets_links_delete_builder(client, &args.name)?;
    logging_projects_locations_buckets_links_delete_execute(builder)
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/buckets/{bucketsId}/links/{linksId}
/// Gets a link.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_projects_locations_buckets_links_get_execute()` to send, or `logging_projects_locations_buckets_links_get` for simplest API.

pub fn logging_projects_locations_buckets_links_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/projects/{}/locations/{locationsId}/buckets/{bucketsId}/links/{linksId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/buckets/{bucketsId}/links/{linksId}
/// Gets a link.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_projects_locations_buckets_links_get_execute()` or `logging_projects_locations_buckets_links_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_projects_locations_buckets_links_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_projects_locations_buckets_links_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Link>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Link = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/buckets/{bucketsId}/links/{linksId}
/// Gets a link.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_projects_locations_buckets_links_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_projects_locations_buckets_links_get_task()`.
/// For the simplest API, use `logging_projects_locations_buckets_links_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_projects_locations_buckets_links_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_projects_locations_buckets_links_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Link>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = logging_projects_locations_buckets_links_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_projects_locations_buckets_links_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingProjectsLocationsBucketsLinksGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/buckets/{bucketsId}/links/{linksId}
/// Gets a link.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_projects_locations_buckets_links_get_builder()` + `logging_projects_locations_buckets_links_get_execute()`.
/// For task-level control, use `logging_projects_locations_buckets_links_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_projects_locations_buckets_links_get(
    client: &SimpleHttpClient,
    args: &LoggingProjectsLocationsBucketsLinksGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Link>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_projects_locations_buckets_links_get_builder(client, &args.name)?;
    logging_projects_locations_buckets_links_get_execute(builder)
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/buckets/{bucketsId}/links
/// Lists links.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_projects_locations_buckets_links_list_execute()` to send, or `logging_projects_locations_buckets_links_list` for simplest API.

pub fn logging_projects_locations_buckets_links_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/projects/{}/locations/{locationsId}/buckets/{bucketsId}/links",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/buckets/{bucketsId}/links
/// Lists links.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_projects_locations_buckets_links_list_execute()` or `logging_projects_locations_buckets_links_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_projects_locations_buckets_links_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_projects_locations_buckets_links_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListLinksResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListLinksResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/buckets/{bucketsId}/links
/// Lists links.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_projects_locations_buckets_links_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_projects_locations_buckets_links_list_task()`.
/// For the simplest API, use `logging_projects_locations_buckets_links_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_projects_locations_buckets_links_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_projects_locations_buckets_links_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListLinksResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = logging_projects_locations_buckets_links_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_projects_locations_buckets_links_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingProjectsLocationsBucketsLinksListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/buckets/{bucketsId}/links
/// Lists links.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_projects_locations_buckets_links_list_builder()` + `logging_projects_locations_buckets_links_list_execute()`.
/// For task-level control, use `logging_projects_locations_buckets_links_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_projects_locations_buckets_links_list(
    client: &SimpleHttpClient,
    args: &LoggingProjectsLocationsBucketsLinksListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListLinksResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = logging_projects_locations_buckets_links_list_builder(
        client,
        &args.parent,
        &args.pageSize,
        &args.pageToken,
    )?;
    logging_projects_locations_buckets_links_list_execute(builder)
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/buckets/{bucketsId}/views
/// Creates a view over log entries in a log bucket. A bucket may contain a maximum of 30 views.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_projects_locations_buckets_views_create_execute()` to send, or `logging_projects_locations_buckets_views_create` for simplest API.

pub fn logging_projects_locations_buckets_views_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    viewId: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/projects/{}/locations/{locationsId}/buckets/{bucketsId}/views",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = viewId.as_ref() {
        query_parts.push(format!("viewId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/buckets/{bucketsId}/views
/// Creates a view over log entries in a log bucket. A bucket may contain a maximum of 30 views.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_projects_locations_buckets_views_create_execute()` or `logging_projects_locations_buckets_views_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_projects_locations_buckets_views_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_projects_locations_buckets_views_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<LogView>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LogView = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/buckets/{bucketsId}/views
/// Creates a view over log entries in a log bucket. A bucket may contain a maximum of 30 views.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_projects_locations_buckets_views_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_projects_locations_buckets_views_create_task()`.
/// For the simplest API, use `logging_projects_locations_buckets_views_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_projects_locations_buckets_views_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_projects_locations_buckets_views_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogView>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = logging_projects_locations_buckets_views_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_projects_locations_buckets_views_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingProjectsLocationsBucketsViewsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: viewId
    pub viewId: Option<String>,
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/buckets/{bucketsId}/views
/// Creates a view over log entries in a log bucket. A bucket may contain a maximum of 30 views.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_projects_locations_buckets_views_create_builder()` + `logging_projects_locations_buckets_views_create_execute()`.
/// For task-level control, use `logging_projects_locations_buckets_views_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_projects_locations_buckets_views_create(
    client: &SimpleHttpClient,
    args: &LoggingProjectsLocationsBucketsViewsCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogView>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_projects_locations_buckets_views_create_builder(
        client,
        &args.parent,
        &args.viewId,
    )?;
    logging_projects_locations_buckets_views_create_execute(builder)
}

/// DELETE v2/projects/{projectsId}/locations/{locationsId}/buckets/{bucketsId}/views/{viewsId}
/// Deletes a view on a log bucket. If an UNAVAILABLE error is returned, this indicates that system is not in a state where it can delete the view. If this occurs, please try again in a few minutes.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_projects_locations_buckets_views_delete_execute()` to send, or `logging_projects_locations_buckets_views_delete` for simplest API.

pub fn logging_projects_locations_buckets_views_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/projects/{}/locations/{locationsId}/buckets/{bucketsId}/views/{viewsId}",
        name,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v2/projects/{projectsId}/locations/{locationsId}/buckets/{bucketsId}/views/{viewsId}
/// Deletes a view on a log bucket. If an UNAVAILABLE error is returned, this indicates that system is not in a state where it can delete the view. If this occurs, please try again in a few minutes.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_projects_locations_buckets_views_delete_execute()` or `logging_projects_locations_buckets_views_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_projects_locations_buckets_views_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_projects_locations_buckets_views_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v2/projects/{projectsId}/locations/{locationsId}/buckets/{bucketsId}/views/{viewsId}
/// Deletes a view on a log bucket. If an UNAVAILABLE error is returned, this indicates that system is not in a state where it can delete the view. If this occurs, please try again in a few minutes.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_projects_locations_buckets_views_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_projects_locations_buckets_views_delete_task()`.
/// For the simplest API, use `logging_projects_locations_buckets_views_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_projects_locations_buckets_views_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_projects_locations_buckets_views_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = logging_projects_locations_buckets_views_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_projects_locations_buckets_views_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingProjectsLocationsBucketsViewsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// DELETE v2/projects/{projectsId}/locations/{locationsId}/buckets/{bucketsId}/views/{viewsId}
/// Deletes a view on a log bucket. If an UNAVAILABLE error is returned, this indicates that system is not in a state where it can delete the view. If this occurs, please try again in a few minutes.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_projects_locations_buckets_views_delete_builder()` + `logging_projects_locations_buckets_views_delete_execute()`.
/// For task-level control, use `logging_projects_locations_buckets_views_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_projects_locations_buckets_views_delete(
    client: &SimpleHttpClient,
    args: &LoggingProjectsLocationsBucketsViewsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_projects_locations_buckets_views_delete_builder(client, &args.name)?;
    logging_projects_locations_buckets_views_delete_execute(builder)
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/buckets/{bucketsId}/views/{viewsId}
/// Gets a view on a log bucket.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_projects_locations_buckets_views_get_execute()` to send, or `logging_projects_locations_buckets_views_get` for simplest API.

pub fn logging_projects_locations_buckets_views_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/projects/{}/locations/{locationsId}/buckets/{bucketsId}/views/{viewsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/buckets/{bucketsId}/views/{viewsId}
/// Gets a view on a log bucket.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_projects_locations_buckets_views_get_execute()` or `logging_projects_locations_buckets_views_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_projects_locations_buckets_views_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_projects_locations_buckets_views_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<LogView>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LogView = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/buckets/{bucketsId}/views/{viewsId}
/// Gets a view on a log bucket.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_projects_locations_buckets_views_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_projects_locations_buckets_views_get_task()`.
/// For the simplest API, use `logging_projects_locations_buckets_views_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_projects_locations_buckets_views_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_projects_locations_buckets_views_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogView>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = logging_projects_locations_buckets_views_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_projects_locations_buckets_views_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingProjectsLocationsBucketsViewsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/buckets/{bucketsId}/views/{viewsId}
/// Gets a view on a log bucket.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_projects_locations_buckets_views_get_builder()` + `logging_projects_locations_buckets_views_get_execute()`.
/// For task-level control, use `logging_projects_locations_buckets_views_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_projects_locations_buckets_views_get(
    client: &SimpleHttpClient,
    args: &LoggingProjectsLocationsBucketsViewsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogView>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_projects_locations_buckets_views_get_builder(client, &args.name)?;
    logging_projects_locations_buckets_views_get_execute(builder)
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/buckets/{bucketsId}/views/{viewsId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_projects_locations_buckets_views_get_iam_policy_execute()` to send, or `logging_projects_locations_buckets_views_get_iam_policy` for simplest API.

pub fn logging_projects_locations_buckets_views_get_iam_policy_builder<R>(
    client: &SimpleHttpClient<R>,
    resource: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/projects/{}/locations/{locationsId}/buckets/{bucketsId}/views/{viewsId}:getIamPolicy",
        resource,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/buckets/{bucketsId}/views/{viewsId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_projects_locations_buckets_views_get_iam_policy_execute()` or `logging_projects_locations_buckets_views_get_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_projects_locations_buckets_views_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_projects_locations_buckets_views_get_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/buckets/{bucketsId}/views/{viewsId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_projects_locations_buckets_views_get_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_projects_locations_buckets_views_get_iam_policy_task()`.
/// For the simplest API, use `logging_projects_locations_buckets_views_get_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_projects_locations_buckets_views_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_projects_locations_buckets_views_get_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = logging_projects_locations_buckets_views_get_iam_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_projects_locations_buckets_views_get_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingProjectsLocationsBucketsViewsGetIamPolicyArgs {
    /// Path parameter: resource
    pub resource: String,
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/buckets/{bucketsId}/views/{viewsId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_projects_locations_buckets_views_get_iam_policy_builder()` + `logging_projects_locations_buckets_views_get_iam_policy_execute()`.
/// For task-level control, use `logging_projects_locations_buckets_views_get_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_projects_locations_buckets_views_get_iam_policy(
    client: &SimpleHttpClient,
    args: &LoggingProjectsLocationsBucketsViewsGetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        logging_projects_locations_buckets_views_get_iam_policy_builder(client, &args.resource)?;
    logging_projects_locations_buckets_views_get_iam_policy_execute(builder)
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/buckets/{bucketsId}/views
/// Lists views on a log bucket.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_projects_locations_buckets_views_list_execute()` to send, or `logging_projects_locations_buckets_views_list` for simplest API.

pub fn logging_projects_locations_buckets_views_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/projects/{}/locations/{locationsId}/buckets/{bucketsId}/views",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/buckets/{bucketsId}/views
/// Lists views on a log bucket.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_projects_locations_buckets_views_list_execute()` or `logging_projects_locations_buckets_views_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_projects_locations_buckets_views_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_projects_locations_buckets_views_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListViewsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListViewsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/buckets/{bucketsId}/views
/// Lists views on a log bucket.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_projects_locations_buckets_views_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_projects_locations_buckets_views_list_task()`.
/// For the simplest API, use `logging_projects_locations_buckets_views_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_projects_locations_buckets_views_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_projects_locations_buckets_views_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListViewsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = logging_projects_locations_buckets_views_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_projects_locations_buckets_views_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingProjectsLocationsBucketsViewsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/buckets/{bucketsId}/views
/// Lists views on a log bucket.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_projects_locations_buckets_views_list_builder()` + `logging_projects_locations_buckets_views_list_execute()`.
/// For task-level control, use `logging_projects_locations_buckets_views_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_projects_locations_buckets_views_list(
    client: &SimpleHttpClient,
    args: &LoggingProjectsLocationsBucketsViewsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListViewsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = logging_projects_locations_buckets_views_list_builder(
        client,
        &args.parent,
        &args.pageSize,
        &args.pageToken,
    )?;
    logging_projects_locations_buckets_views_list_execute(builder)
}

/// PATCH v2/projects/{projectsId}/locations/{locationsId}/buckets/{bucketsId}/views/{viewsId}
/// Updates a view on a log bucket. This method replaces the value of the filter field from the existing view with the corresponding value from the new view. If an UNAVAILABLE error is returned, this indicates that system is not in a state where it can update the view. If this occurs, please try again in a few minutes.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_projects_locations_buckets_views_patch_execute()` to send, or `logging_projects_locations_buckets_views_patch` for simplest API.

pub fn logging_projects_locations_buckets_views_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    updateMask: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/projects/{}/locations/{locationsId}/buckets/{bucketsId}/views/{viewsId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v2/projects/{projectsId}/locations/{locationsId}/buckets/{bucketsId}/views/{viewsId}
/// Updates a view on a log bucket. This method replaces the value of the filter field from the existing view with the corresponding value from the new view. If an UNAVAILABLE error is returned, this indicates that system is not in a state where it can update the view. If this occurs, please try again in a few minutes.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_projects_locations_buckets_views_patch_execute()` or `logging_projects_locations_buckets_views_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_projects_locations_buckets_views_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_projects_locations_buckets_views_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<LogView>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LogView = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v2/projects/{projectsId}/locations/{locationsId}/buckets/{bucketsId}/views/{viewsId}
/// Updates a view on a log bucket. This method replaces the value of the filter field from the existing view with the corresponding value from the new view. If an UNAVAILABLE error is returned, this indicates that system is not in a state where it can update the view. If this occurs, please try again in a few minutes.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_projects_locations_buckets_views_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_projects_locations_buckets_views_patch_task()`.
/// For the simplest API, use `logging_projects_locations_buckets_views_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_projects_locations_buckets_views_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_projects_locations_buckets_views_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogView>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = logging_projects_locations_buckets_views_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_projects_locations_buckets_views_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingProjectsLocationsBucketsViewsPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
}

/// PATCH v2/projects/{projectsId}/locations/{locationsId}/buckets/{bucketsId}/views/{viewsId}
/// Updates a view on a log bucket. This method replaces the value of the filter field from the existing view with the corresponding value from the new view. If an UNAVAILABLE error is returned, this indicates that system is not in a state where it can update the view. If this occurs, please try again in a few minutes.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_projects_locations_buckets_views_patch_builder()` + `logging_projects_locations_buckets_views_patch_execute()`.
/// For task-level control, use `logging_projects_locations_buckets_views_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_projects_locations_buckets_views_patch(
    client: &SimpleHttpClient,
    args: &LoggingProjectsLocationsBucketsViewsPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogView>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_projects_locations_buckets_views_patch_builder(
        client,
        &args.name,
        &args.updateMask,
    )?;
    logging_projects_locations_buckets_views_patch_execute(builder)
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/buckets/{bucketsId}/views/{viewsId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_projects_locations_buckets_views_set_iam_policy_execute()` to send, or `logging_projects_locations_buckets_views_set_iam_policy` for simplest API.

pub fn logging_projects_locations_buckets_views_set_iam_policy_builder<R>(
    client: &SimpleHttpClient<R>,
    resource: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/projects/{}/locations/{locationsId}/buckets/{bucketsId}/views/{viewsId}:setIamPolicy",
        resource,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/buckets/{bucketsId}/views/{viewsId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_projects_locations_buckets_views_set_iam_policy_execute()` or `logging_projects_locations_buckets_views_set_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_projects_locations_buckets_views_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_projects_locations_buckets_views_set_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/buckets/{bucketsId}/views/{viewsId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_projects_locations_buckets_views_set_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_projects_locations_buckets_views_set_iam_policy_task()`.
/// For the simplest API, use `logging_projects_locations_buckets_views_set_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_projects_locations_buckets_views_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_projects_locations_buckets_views_set_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = logging_projects_locations_buckets_views_set_iam_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_projects_locations_buckets_views_set_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingProjectsLocationsBucketsViewsSetIamPolicyArgs {
    /// Path parameter: resource
    pub resource: String,
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/buckets/{bucketsId}/views/{viewsId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy.Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_projects_locations_buckets_views_set_iam_policy_builder()` + `logging_projects_locations_buckets_views_set_iam_policy_execute()`.
/// For task-level control, use `logging_projects_locations_buckets_views_set_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_projects_locations_buckets_views_set_iam_policy(
    client: &SimpleHttpClient,
    args: &LoggingProjectsLocationsBucketsViewsSetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        logging_projects_locations_buckets_views_set_iam_policy_builder(client, &args.resource)?;
    logging_projects_locations_buckets_views_set_iam_policy_execute(builder)
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/buckets/{bucketsId}/views/{viewsId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error.Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_projects_locations_buckets_views_test_iam_permissions_execute()` to send, or `logging_projects_locations_buckets_views_test_iam_permissions` for simplest API.

pub fn logging_projects_locations_buckets_views_test_iam_permissions_builder<R>(
    client: &SimpleHttpClient<R>,
    resource: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/projects/{}/locations/{locationsId}/buckets/{bucketsId}/views/{viewsId}:testIamPermissions",
        resource,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/buckets/{bucketsId}/views/{viewsId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error.Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_projects_locations_buckets_views_test_iam_permissions_execute()` or `logging_projects_locations_buckets_views_test_iam_permissions`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_projects_locations_buckets_views_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_projects_locations_buckets_views_test_iam_permissions_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TestIamPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/buckets/{bucketsId}/views/{viewsId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error.Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_projects_locations_buckets_views_test_iam_permissions_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_projects_locations_buckets_views_test_iam_permissions_task()`.
/// For the simplest API, use `logging_projects_locations_buckets_views_test_iam_permissions()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_projects_locations_buckets_views_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_projects_locations_buckets_views_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = logging_projects_locations_buckets_views_test_iam_permissions_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_projects_locations_buckets_views_test_iam_permissions`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingProjectsLocationsBucketsViewsTestIamPermissionsArgs {
    /// Path parameter: resource
    pub resource: String,
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/buckets/{bucketsId}/views/{viewsId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error.Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_projects_locations_buckets_views_test_iam_permissions_builder()` + `logging_projects_locations_buckets_views_test_iam_permissions_execute()`.
/// For task-level control, use `logging_projects_locations_buckets_views_test_iam_permissions_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_projects_locations_buckets_views_test_iam_permissions(
    client: &SimpleHttpClient,
    args: &LoggingProjectsLocationsBucketsViewsTestIamPermissionsArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = logging_projects_locations_buckets_views_test_iam_permissions_builder(
        client,
        &args.resource,
    )?;
    logging_projects_locations_buckets_views_test_iam_permissions_execute(builder)
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/buckets/{bucketsId}/views/{viewsId}/logs
/// Lists the logs in projects, organizations, folders, or billing accounts. Only logs that have entries are listed.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_projects_locations_buckets_views_logs_list_execute()` to send, or `logging_projects_locations_buckets_views_logs_list` for simplest API.

pub fn logging_projects_locations_buckets_views_logs_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
    resourceNames: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/projects/{}/locations/{locationsId}/buckets/{bucketsId}/views/{viewsId}/logs",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = resourceNames.as_ref() {
        query_parts.push(format!("resourceNames={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/buckets/{bucketsId}/views/{viewsId}/logs
/// Lists the logs in projects, organizations, folders, or billing accounts. Only logs that have entries are listed.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_projects_locations_buckets_views_logs_list_execute()` or `logging_projects_locations_buckets_views_logs_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_projects_locations_buckets_views_logs_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_projects_locations_buckets_views_logs_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListLogsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListLogsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/buckets/{bucketsId}/views/{viewsId}/logs
/// Lists the logs in projects, organizations, folders, or billing accounts. Only logs that have entries are listed.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_projects_locations_buckets_views_logs_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_projects_locations_buckets_views_logs_list_task()`.
/// For the simplest API, use `logging_projects_locations_buckets_views_logs_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_projects_locations_buckets_views_logs_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_projects_locations_buckets_views_logs_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListLogsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = logging_projects_locations_buckets_views_logs_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_projects_locations_buckets_views_logs_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingProjectsLocationsBucketsViewsLogsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: resourceNames
    pub resourceNames: Option<String>,
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/buckets/{bucketsId}/views/{viewsId}/logs
/// Lists the logs in projects, organizations, folders, or billing accounts. Only logs that have entries are listed.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_projects_locations_buckets_views_logs_list_builder()` + `logging_projects_locations_buckets_views_logs_list_execute()`.
/// For task-level control, use `logging_projects_locations_buckets_views_logs_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_projects_locations_buckets_views_logs_list(
    client: &SimpleHttpClient,
    args: &LoggingProjectsLocationsBucketsViewsLogsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListLogsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = logging_projects_locations_buckets_views_logs_list_builder(
        client,
        &args.parent,
        &args.pageSize,
        &args.pageToken,
        &args.resourceNames,
    )?;
    logging_projects_locations_buckets_views_logs_list_execute(builder)
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/logScopes
/// Creates a log scope.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_projects_locations_log_scopes_create_execute()` to send, or `logging_projects_locations_log_scopes_create` for simplest API.

pub fn logging_projects_locations_log_scopes_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    logScopeId: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/projects/{}/locations/{locationsId}/logScopes",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = logScopeId.as_ref() {
        query_parts.push(format!("logScopeId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/logScopes
/// Creates a log scope.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_projects_locations_log_scopes_create_execute()` or `logging_projects_locations_log_scopes_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_projects_locations_log_scopes_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_projects_locations_log_scopes_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<LogScope>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LogScope = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/logScopes
/// Creates a log scope.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_projects_locations_log_scopes_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_projects_locations_log_scopes_create_task()`.
/// For the simplest API, use `logging_projects_locations_log_scopes_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_projects_locations_log_scopes_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_projects_locations_log_scopes_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogScope>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = logging_projects_locations_log_scopes_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_projects_locations_log_scopes_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingProjectsLocationsLogScopesCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: logScopeId
    pub logScopeId: Option<String>,
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/logScopes
/// Creates a log scope.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_projects_locations_log_scopes_create_builder()` + `logging_projects_locations_log_scopes_create_execute()`.
/// For task-level control, use `logging_projects_locations_log_scopes_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_projects_locations_log_scopes_create(
    client: &SimpleHttpClient,
    args: &LoggingProjectsLocationsLogScopesCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogScope>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_projects_locations_log_scopes_create_builder(
        client,
        &args.parent,
        &args.logScopeId,
    )?;
    logging_projects_locations_log_scopes_create_execute(builder)
}

/// DELETE v2/projects/{projectsId}/locations/{locationsId}/logScopes/{logScopesId}
/// Deletes a log scope.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_projects_locations_log_scopes_delete_execute()` to send, or `logging_projects_locations_log_scopes_delete` for simplest API.

pub fn logging_projects_locations_log_scopes_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/projects/{}/locations/{locationsId}/logScopes/{logScopesId}",
        name,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v2/projects/{projectsId}/locations/{locationsId}/logScopes/{logScopesId}
/// Deletes a log scope.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_projects_locations_log_scopes_delete_execute()` or `logging_projects_locations_log_scopes_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_projects_locations_log_scopes_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_projects_locations_log_scopes_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v2/projects/{projectsId}/locations/{locationsId}/logScopes/{logScopesId}
/// Deletes a log scope.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_projects_locations_log_scopes_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_projects_locations_log_scopes_delete_task()`.
/// For the simplest API, use `logging_projects_locations_log_scopes_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_projects_locations_log_scopes_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_projects_locations_log_scopes_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = logging_projects_locations_log_scopes_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_projects_locations_log_scopes_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingProjectsLocationsLogScopesDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// DELETE v2/projects/{projectsId}/locations/{locationsId}/logScopes/{logScopesId}
/// Deletes a log scope.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_projects_locations_log_scopes_delete_builder()` + `logging_projects_locations_log_scopes_delete_execute()`.
/// For task-level control, use `logging_projects_locations_log_scopes_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_projects_locations_log_scopes_delete(
    client: &SimpleHttpClient,
    args: &LoggingProjectsLocationsLogScopesDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_projects_locations_log_scopes_delete_builder(client, &args.name)?;
    logging_projects_locations_log_scopes_delete_execute(builder)
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/logScopes/{logScopesId}
/// Gets a log scope.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_projects_locations_log_scopes_get_execute()` to send, or `logging_projects_locations_log_scopes_get` for simplest API.

pub fn logging_projects_locations_log_scopes_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/projects/{}/locations/{locationsId}/logScopes/{logScopesId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/logScopes/{logScopesId}
/// Gets a log scope.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_projects_locations_log_scopes_get_execute()` or `logging_projects_locations_log_scopes_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_projects_locations_log_scopes_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_projects_locations_log_scopes_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<LogScope>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LogScope = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/logScopes/{logScopesId}
/// Gets a log scope.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_projects_locations_log_scopes_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_projects_locations_log_scopes_get_task()`.
/// For the simplest API, use `logging_projects_locations_log_scopes_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_projects_locations_log_scopes_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_projects_locations_log_scopes_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogScope>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = logging_projects_locations_log_scopes_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_projects_locations_log_scopes_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingProjectsLocationsLogScopesGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/logScopes/{logScopesId}
/// Gets a log scope.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_projects_locations_log_scopes_get_builder()` + `logging_projects_locations_log_scopes_get_execute()`.
/// For task-level control, use `logging_projects_locations_log_scopes_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_projects_locations_log_scopes_get(
    client: &SimpleHttpClient,
    args: &LoggingProjectsLocationsLogScopesGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogScope>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_projects_locations_log_scopes_get_builder(client, &args.name)?;
    logging_projects_locations_log_scopes_get_execute(builder)
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/logScopes
/// Lists log scopes.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_projects_locations_log_scopes_list_execute()` to send, or `logging_projects_locations_log_scopes_list` for simplest API.

pub fn logging_projects_locations_log_scopes_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/projects/{}/locations/{locationsId}/logScopes",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/logScopes
/// Lists log scopes.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_projects_locations_log_scopes_list_execute()` or `logging_projects_locations_log_scopes_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_projects_locations_log_scopes_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_projects_locations_log_scopes_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListLogScopesResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListLogScopesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/logScopes
/// Lists log scopes.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_projects_locations_log_scopes_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_projects_locations_log_scopes_list_task()`.
/// For the simplest API, use `logging_projects_locations_log_scopes_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_projects_locations_log_scopes_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_projects_locations_log_scopes_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListLogScopesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = logging_projects_locations_log_scopes_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_projects_locations_log_scopes_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingProjectsLocationsLogScopesListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/logScopes
/// Lists log scopes.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_projects_locations_log_scopes_list_builder()` + `logging_projects_locations_log_scopes_list_execute()`.
/// For task-level control, use `logging_projects_locations_log_scopes_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_projects_locations_log_scopes_list(
    client: &SimpleHttpClient,
    args: &LoggingProjectsLocationsLogScopesListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListLogScopesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = logging_projects_locations_log_scopes_list_builder(
        client,
        &args.parent,
        &args.pageSize,
        &args.pageToken,
    )?;
    logging_projects_locations_log_scopes_list_execute(builder)
}

/// PATCH v2/projects/{projectsId}/locations/{locationsId}/logScopes/{logScopesId}
/// Updates a log scope.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_projects_locations_log_scopes_patch_execute()` to send, or `logging_projects_locations_log_scopes_patch` for simplest API.

pub fn logging_projects_locations_log_scopes_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    updateMask: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/projects/{}/locations/{locationsId}/logScopes/{logScopesId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v2/projects/{projectsId}/locations/{locationsId}/logScopes/{logScopesId}
/// Updates a log scope.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_projects_locations_log_scopes_patch_execute()` or `logging_projects_locations_log_scopes_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_projects_locations_log_scopes_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_projects_locations_log_scopes_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<LogScope>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LogScope = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v2/projects/{projectsId}/locations/{locationsId}/logScopes/{logScopesId}
/// Updates a log scope.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_projects_locations_log_scopes_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_projects_locations_log_scopes_patch_task()`.
/// For the simplest API, use `logging_projects_locations_log_scopes_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_projects_locations_log_scopes_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_projects_locations_log_scopes_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogScope>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = logging_projects_locations_log_scopes_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_projects_locations_log_scopes_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingProjectsLocationsLogScopesPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
}

/// PATCH v2/projects/{projectsId}/locations/{locationsId}/logScopes/{logScopesId}
/// Updates a log scope.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_projects_locations_log_scopes_patch_builder()` + `logging_projects_locations_log_scopes_patch_execute()`.
/// For task-level control, use `logging_projects_locations_log_scopes_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_projects_locations_log_scopes_patch(
    client: &SimpleHttpClient,
    args: &LoggingProjectsLocationsLogScopesPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogScope>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        logging_projects_locations_log_scopes_patch_builder(client, &args.name, &args.updateMask)?;
    logging_projects_locations_log_scopes_patch_execute(builder)
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn''t support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_projects_locations_operations_cancel_execute()` to send, or `logging_projects_locations_operations_cancel` for simplest API.

pub fn logging_projects_locations_operations_cancel_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/projects/{}/locations/{locationsId}/operations/{operationsId}:cancel",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn''t support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_projects_locations_operations_cancel_execute()` or `logging_projects_locations_operations_cancel`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_projects_locations_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_projects_locations_operations_cancel_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn''t support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_projects_locations_operations_cancel_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_projects_locations_operations_cancel_task()`.
/// For the simplest API, use `logging_projects_locations_operations_cancel()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_projects_locations_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_projects_locations_operations_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = logging_projects_locations_operations_cancel_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_projects_locations_operations_cancel`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingProjectsLocationsOperationsCancelArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn''t support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_projects_locations_operations_cancel_builder()` + `logging_projects_locations_operations_cancel_execute()`.
/// For task-level control, use `logging_projects_locations_operations_cancel_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_projects_locations_operations_cancel(
    client: &SimpleHttpClient,
    args: &LoggingProjectsLocationsOperationsCancelArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_projects_locations_operations_cancel_builder(client, &args.name)?;
    logging_projects_locations_operations_cancel_execute(builder)
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_projects_locations_operations_get_execute()` to send, or `logging_projects_locations_operations_get` for simplest API.

pub fn logging_projects_locations_operations_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/projects/{}/locations/{locationsId}/operations/{operationsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_projects_locations_operations_get_execute()` or `logging_projects_locations_operations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_projects_locations_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_projects_locations_operations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_projects_locations_operations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_projects_locations_operations_get_task()`.
/// For the simplest API, use `logging_projects_locations_operations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_projects_locations_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_projects_locations_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = logging_projects_locations_operations_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_projects_locations_operations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingProjectsLocationsOperationsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_projects_locations_operations_get_builder()` + `logging_projects_locations_operations_get_execute()`.
/// For task-level control, use `logging_projects_locations_operations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_projects_locations_operations_get(
    client: &SimpleHttpClient,
    args: &LoggingProjectsLocationsOperationsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_projects_locations_operations_get_builder(client, &args.name)?;
    logging_projects_locations_operations_get_execute(builder)
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn''t support this method, it returns UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_projects_locations_operations_list_execute()` to send, or `logging_projects_locations_operations_list` for simplest API.

pub fn logging_projects_locations_operations_list_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    filter: &Option<String>,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
    returnPartialSuccess: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/projects/{}/locations/{locationsId}/operations",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn''t support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_projects_locations_operations_list_execute()` or `logging_projects_locations_operations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_projects_locations_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_projects_locations_operations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListOperationsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListOperationsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn''t support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_projects_locations_operations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_projects_locations_operations_list_task()`.
/// For the simplest API, use `logging_projects_locations_operations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_projects_locations_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_projects_locations_operations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListOperationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = logging_projects_locations_operations_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_projects_locations_operations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingProjectsLocationsOperationsListArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<String>,
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn''t support this method, it returns UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_projects_locations_operations_list_builder()` + `logging_projects_locations_operations_list_execute()`.
/// For task-level control, use `logging_projects_locations_operations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_projects_locations_operations_list(
    client: &SimpleHttpClient,
    args: &LoggingProjectsLocationsOperationsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListOperationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = logging_projects_locations_operations_list_builder(
        client,
        &args.name,
        &args.filter,
        &args.pageSize,
        &args.pageToken,
        &args.returnPartialSuccess,
    )?;
    logging_projects_locations_operations_list_execute(builder)
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/recentQueries
/// Lists the RecentQueries that were created by the user making the request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_projects_locations_recent_queries_list_execute()` to send, or `logging_projects_locations_recent_queries_list` for simplest API.

pub fn logging_projects_locations_recent_queries_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    filter: &Option<String>,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/projects/{}/locations/{locationsId}/recentQueries",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/recentQueries
/// Lists the RecentQueries that were created by the user making the request.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_projects_locations_recent_queries_list_execute()` or `logging_projects_locations_recent_queries_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_projects_locations_recent_queries_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_projects_locations_recent_queries_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListRecentQueriesResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListRecentQueriesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/recentQueries
/// Lists the RecentQueries that were created by the user making the request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_projects_locations_recent_queries_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_projects_locations_recent_queries_list_task()`.
/// For the simplest API, use `logging_projects_locations_recent_queries_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_projects_locations_recent_queries_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_projects_locations_recent_queries_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListRecentQueriesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = logging_projects_locations_recent_queries_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_projects_locations_recent_queries_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingProjectsLocationsRecentQueriesListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/recentQueries
/// Lists the RecentQueries that were created by the user making the request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_projects_locations_recent_queries_list_builder()` + `logging_projects_locations_recent_queries_list_execute()`.
/// For task-level control, use `logging_projects_locations_recent_queries_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_projects_locations_recent_queries_list(
    client: &SimpleHttpClient,
    args: &LoggingProjectsLocationsRecentQueriesListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListRecentQueriesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = logging_projects_locations_recent_queries_list_builder(
        client,
        &args.parent,
        &args.filter,
        &args.pageSize,
        &args.pageToken,
    )?;
    logging_projects_locations_recent_queries_list_execute(builder)
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/savedQueries
/// Creates a new SavedQuery for the user making the request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_projects_locations_saved_queries_create_execute()` to send, or `logging_projects_locations_saved_queries_create` for simplest API.

pub fn logging_projects_locations_saved_queries_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    savedQueryId: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/projects/{}/locations/{locationsId}/savedQueries",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = savedQueryId.as_ref() {
        query_parts.push(format!("savedQueryId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/savedQueries
/// Creates a new SavedQuery for the user making the request.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_projects_locations_saved_queries_create_execute()` or `logging_projects_locations_saved_queries_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_projects_locations_saved_queries_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_projects_locations_saved_queries_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SavedQuery>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SavedQuery = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/savedQueries
/// Creates a new SavedQuery for the user making the request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_projects_locations_saved_queries_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_projects_locations_saved_queries_create_task()`.
/// For the simplest API, use `logging_projects_locations_saved_queries_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_projects_locations_saved_queries_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_projects_locations_saved_queries_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SavedQuery>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = logging_projects_locations_saved_queries_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_projects_locations_saved_queries_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingProjectsLocationsSavedQueriesCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: savedQueryId
    pub savedQueryId: Option<String>,
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/savedQueries
/// Creates a new SavedQuery for the user making the request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_projects_locations_saved_queries_create_builder()` + `logging_projects_locations_saved_queries_create_execute()`.
/// For task-level control, use `logging_projects_locations_saved_queries_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_projects_locations_saved_queries_create(
    client: &SimpleHttpClient,
    args: &LoggingProjectsLocationsSavedQueriesCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SavedQuery>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_projects_locations_saved_queries_create_builder(
        client,
        &args.parent,
        &args.savedQueryId,
    )?;
    logging_projects_locations_saved_queries_create_execute(builder)
}

/// DELETE v2/projects/{projectsId}/locations/{locationsId}/savedQueries/{savedQueriesId}
/// Deletes an existing SavedQuery that was created by the user making the request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_projects_locations_saved_queries_delete_execute()` to send, or `logging_projects_locations_saved_queries_delete` for simplest API.

pub fn logging_projects_locations_saved_queries_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/projects/{}/locations/{locationsId}/savedQueries/{savedQueriesId}",
        name,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v2/projects/{projectsId}/locations/{locationsId}/savedQueries/{savedQueriesId}
/// Deletes an existing SavedQuery that was created by the user making the request.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_projects_locations_saved_queries_delete_execute()` or `logging_projects_locations_saved_queries_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_projects_locations_saved_queries_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_projects_locations_saved_queries_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v2/projects/{projectsId}/locations/{locationsId}/savedQueries/{savedQueriesId}
/// Deletes an existing SavedQuery that was created by the user making the request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_projects_locations_saved_queries_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_projects_locations_saved_queries_delete_task()`.
/// For the simplest API, use `logging_projects_locations_saved_queries_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_projects_locations_saved_queries_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_projects_locations_saved_queries_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = logging_projects_locations_saved_queries_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_projects_locations_saved_queries_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingProjectsLocationsSavedQueriesDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// DELETE v2/projects/{projectsId}/locations/{locationsId}/savedQueries/{savedQueriesId}
/// Deletes an existing SavedQuery that was created by the user making the request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_projects_locations_saved_queries_delete_builder()` + `logging_projects_locations_saved_queries_delete_execute()`.
/// For task-level control, use `logging_projects_locations_saved_queries_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_projects_locations_saved_queries_delete(
    client: &SimpleHttpClient,
    args: &LoggingProjectsLocationsSavedQueriesDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_projects_locations_saved_queries_delete_builder(client, &args.name)?;
    logging_projects_locations_saved_queries_delete_execute(builder)
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/savedQueries/{savedQueriesId}
/// Returns all data associated with the requested query.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_projects_locations_saved_queries_get_execute()` to send, or `logging_projects_locations_saved_queries_get` for simplest API.

pub fn logging_projects_locations_saved_queries_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/projects/{}/locations/{locationsId}/savedQueries/{savedQueriesId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/savedQueries/{savedQueriesId}
/// Returns all data associated with the requested query.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_projects_locations_saved_queries_get_execute()` or `logging_projects_locations_saved_queries_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_projects_locations_saved_queries_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_projects_locations_saved_queries_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SavedQuery>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SavedQuery = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/savedQueries/{savedQueriesId}
/// Returns all data associated with the requested query.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_projects_locations_saved_queries_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_projects_locations_saved_queries_get_task()`.
/// For the simplest API, use `logging_projects_locations_saved_queries_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_projects_locations_saved_queries_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_projects_locations_saved_queries_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SavedQuery>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = logging_projects_locations_saved_queries_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_projects_locations_saved_queries_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingProjectsLocationsSavedQueriesGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/savedQueries/{savedQueriesId}
/// Returns all data associated with the requested query.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_projects_locations_saved_queries_get_builder()` + `logging_projects_locations_saved_queries_get_execute()`.
/// For task-level control, use `logging_projects_locations_saved_queries_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_projects_locations_saved_queries_get(
    client: &SimpleHttpClient,
    args: &LoggingProjectsLocationsSavedQueriesGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SavedQuery>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_projects_locations_saved_queries_get_builder(client, &args.name)?;
    logging_projects_locations_saved_queries_get_execute(builder)
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/savedQueries
/// Lists the SavedQueries that were created by the user making the request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_projects_locations_saved_queries_list_execute()` to send, or `logging_projects_locations_saved_queries_list` for simplest API.

pub fn logging_projects_locations_saved_queries_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    filter: &Option<String>,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/projects/{}/locations/{locationsId}/savedQueries",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/savedQueries
/// Lists the SavedQueries that were created by the user making the request.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_projects_locations_saved_queries_list_execute()` or `logging_projects_locations_saved_queries_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_projects_locations_saved_queries_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_projects_locations_saved_queries_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListSavedQueriesResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListSavedQueriesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/savedQueries
/// Lists the SavedQueries that were created by the user making the request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_projects_locations_saved_queries_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_projects_locations_saved_queries_list_task()`.
/// For the simplest API, use `logging_projects_locations_saved_queries_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_projects_locations_saved_queries_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_projects_locations_saved_queries_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListSavedQueriesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = logging_projects_locations_saved_queries_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_projects_locations_saved_queries_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingProjectsLocationsSavedQueriesListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/savedQueries
/// Lists the SavedQueries that were created by the user making the request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_projects_locations_saved_queries_list_builder()` + `logging_projects_locations_saved_queries_list_execute()`.
/// For task-level control, use `logging_projects_locations_saved_queries_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_projects_locations_saved_queries_list(
    client: &SimpleHttpClient,
    args: &LoggingProjectsLocationsSavedQueriesListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListSavedQueriesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = logging_projects_locations_saved_queries_list_builder(
        client,
        &args.parent,
        &args.filter,
        &args.pageSize,
        &args.pageToken,
    )?;
    logging_projects_locations_saved_queries_list_execute(builder)
}

/// PATCH v2/projects/{projectsId}/locations/{locationsId}/savedQueries/{savedQueriesId}
/// Updates an existing SavedQuery.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_projects_locations_saved_queries_patch_execute()` to send, or `logging_projects_locations_saved_queries_patch` for simplest API.

pub fn logging_projects_locations_saved_queries_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    updateMask: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/projects/{}/locations/{locationsId}/savedQueries/{savedQueriesId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v2/projects/{projectsId}/locations/{locationsId}/savedQueries/{savedQueriesId}
/// Updates an existing SavedQuery.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_projects_locations_saved_queries_patch_execute()` or `logging_projects_locations_saved_queries_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_projects_locations_saved_queries_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_projects_locations_saved_queries_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SavedQuery>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SavedQuery = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v2/projects/{projectsId}/locations/{locationsId}/savedQueries/{savedQueriesId}
/// Updates an existing SavedQuery.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_projects_locations_saved_queries_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_projects_locations_saved_queries_patch_task()`.
/// For the simplest API, use `logging_projects_locations_saved_queries_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_projects_locations_saved_queries_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_projects_locations_saved_queries_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SavedQuery>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = logging_projects_locations_saved_queries_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_projects_locations_saved_queries_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingProjectsLocationsSavedQueriesPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
}

/// PATCH v2/projects/{projectsId}/locations/{locationsId}/savedQueries/{savedQueriesId}
/// Updates an existing SavedQuery.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_projects_locations_saved_queries_patch_builder()` + `logging_projects_locations_saved_queries_patch_execute()`.
/// For task-level control, use `logging_projects_locations_saved_queries_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_projects_locations_saved_queries_patch(
    client: &SimpleHttpClient,
    args: &LoggingProjectsLocationsSavedQueriesPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SavedQuery>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_projects_locations_saved_queries_patch_builder(
        client,
        &args.name,
        &args.updateMask,
    )?;
    logging_projects_locations_saved_queries_patch_execute(builder)
}

/// DELETE v2/projects/{projectsId}/logs/{logsId}
/// Deletes all the log entries in a log for the global _Default Log Bucket. The log reappears if it receives new entries. Log entries written shortly before the delete operation might not be deleted. Entries received after the delete operation with a timestamp before the operation will be deleted.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_projects_logs_delete_execute()` to send, or `logging_projects_logs_delete` for simplest API.

pub fn logging_projects_logs_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    logName: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/projects/{}/logs/{logsId}",
        logName,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v2/projects/{projectsId}/logs/{logsId}
/// Deletes all the log entries in a log for the global _Default Log Bucket. The log reappears if it receives new entries. Log entries written shortly before the delete operation might not be deleted. Entries received after the delete operation with a timestamp before the operation will be deleted.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_projects_logs_delete_execute()` or `logging_projects_logs_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_projects_logs_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_projects_logs_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v2/projects/{projectsId}/logs/{logsId}
/// Deletes all the log entries in a log for the global _Default Log Bucket. The log reappears if it receives new entries. Log entries written shortly before the delete operation might not be deleted. Entries received after the delete operation with a timestamp before the operation will be deleted.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_projects_logs_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_projects_logs_delete_task()`.
/// For the simplest API, use `logging_projects_logs_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_projects_logs_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_projects_logs_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = logging_projects_logs_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_projects_logs_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingProjectsLogsDeleteArgs {
    /// Path parameter: logName
    pub logName: String,
}

/// DELETE v2/projects/{projectsId}/logs/{logsId}
/// Deletes all the log entries in a log for the global _Default Log Bucket. The log reappears if it receives new entries. Log entries written shortly before the delete operation might not be deleted. Entries received after the delete operation with a timestamp before the operation will be deleted.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_projects_logs_delete_builder()` + `logging_projects_logs_delete_execute()`.
/// For task-level control, use `logging_projects_logs_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_projects_logs_delete(
    client: &SimpleHttpClient,
    args: &LoggingProjectsLogsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_projects_logs_delete_builder(client, &args.logName)?;
    logging_projects_logs_delete_execute(builder)
}

/// GET v2/projects/{projectsId}/logs
/// Lists the logs in projects, organizations, folders, or billing accounts. Only logs that have entries are listed.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_projects_logs_list_execute()` to send, or `logging_projects_logs_list` for simplest API.

pub fn logging_projects_logs_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
    resourceNames: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!("https://logging.googleapis.com/v2/projects/{}/logs", parent,);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = resourceNames.as_ref() {
        query_parts.push(format!("resourceNames={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/projects/{projectsId}/logs
/// Lists the logs in projects, organizations, folders, or billing accounts. Only logs that have entries are listed.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_projects_logs_list_execute()` or `logging_projects_logs_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_projects_logs_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_projects_logs_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListLogsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListLogsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/projects/{projectsId}/logs
/// Lists the logs in projects, organizations, folders, or billing accounts. Only logs that have entries are listed.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_projects_logs_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_projects_logs_list_task()`.
/// For the simplest API, use `logging_projects_logs_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_projects_logs_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_projects_logs_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListLogsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = logging_projects_logs_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_projects_logs_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingProjectsLogsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: resourceNames
    pub resourceNames: Option<String>,
}

/// GET v2/projects/{projectsId}/logs
/// Lists the logs in projects, organizations, folders, or billing accounts. Only logs that have entries are listed.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_projects_logs_list_builder()` + `logging_projects_logs_list_execute()`.
/// For task-level control, use `logging_projects_logs_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_projects_logs_list(
    client: &SimpleHttpClient,
    args: &LoggingProjectsLogsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListLogsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = logging_projects_logs_list_builder(
        client,
        &args.parent,
        &args.pageSize,
        &args.pageToken,
        &args.resourceNames,
    )?;
    logging_projects_logs_list_execute(builder)
}

/// POST v2/projects/{projectsId}/metrics
/// Creates a logs-based metric.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_projects_metrics_create_execute()` to send, or `logging_projects_metrics_create` for simplest API.

pub fn logging_projects_metrics_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/projects/{}/metrics",
        parent,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v2/projects/{projectsId}/metrics
/// Creates a logs-based metric.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_projects_metrics_create_execute()` or `logging_projects_metrics_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_projects_metrics_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_projects_metrics_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<LogMetric>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LogMetric = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v2/projects/{projectsId}/metrics
/// Creates a logs-based metric.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_projects_metrics_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_projects_metrics_create_task()`.
/// For the simplest API, use `logging_projects_metrics_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_projects_metrics_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_projects_metrics_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogMetric>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = logging_projects_metrics_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_projects_metrics_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingProjectsMetricsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// POST v2/projects/{projectsId}/metrics
/// Creates a logs-based metric.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_projects_metrics_create_builder()` + `logging_projects_metrics_create_execute()`.
/// For task-level control, use `logging_projects_metrics_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_projects_metrics_create(
    client: &SimpleHttpClient,
    args: &LoggingProjectsMetricsCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogMetric>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_projects_metrics_create_builder(client, &args.parent)?;
    logging_projects_metrics_create_execute(builder)
}

/// DELETE v2/projects/{projectsId}/metrics/{metricsId}
/// Deletes a logs-based metric.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_projects_metrics_delete_execute()` to send, or `logging_projects_metrics_delete` for simplest API.

pub fn logging_projects_metrics_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    metricName: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/projects/{}/metrics/{metricsId}",
        metricName,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v2/projects/{projectsId}/metrics/{metricsId}
/// Deletes a logs-based metric.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_projects_metrics_delete_execute()` or `logging_projects_metrics_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_projects_metrics_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_projects_metrics_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v2/projects/{projectsId}/metrics/{metricsId}
/// Deletes a logs-based metric.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_projects_metrics_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_projects_metrics_delete_task()`.
/// For the simplest API, use `logging_projects_metrics_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_projects_metrics_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_projects_metrics_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = logging_projects_metrics_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_projects_metrics_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingProjectsMetricsDeleteArgs {
    /// Path parameter: metricName
    pub metricName: String,
}

/// DELETE v2/projects/{projectsId}/metrics/{metricsId}
/// Deletes a logs-based metric.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_projects_metrics_delete_builder()` + `logging_projects_metrics_delete_execute()`.
/// For task-level control, use `logging_projects_metrics_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_projects_metrics_delete(
    client: &SimpleHttpClient,
    args: &LoggingProjectsMetricsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_projects_metrics_delete_builder(client, &args.metricName)?;
    logging_projects_metrics_delete_execute(builder)
}

/// GET v2/projects/{projectsId}/metrics/{metricsId}
/// Gets a logs-based metric.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_projects_metrics_get_execute()` to send, or `logging_projects_metrics_get` for simplest API.

pub fn logging_projects_metrics_get_builder<R>(
    client: &SimpleHttpClient<R>,
    metricName: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/projects/{}/metrics/{metricsId}",
        metricName,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/projects/{projectsId}/metrics/{metricsId}
/// Gets a logs-based metric.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_projects_metrics_get_execute()` or `logging_projects_metrics_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_projects_metrics_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_projects_metrics_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<LogMetric>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LogMetric = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/projects/{projectsId}/metrics/{metricsId}
/// Gets a logs-based metric.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_projects_metrics_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_projects_metrics_get_task()`.
/// For the simplest API, use `logging_projects_metrics_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_projects_metrics_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_projects_metrics_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogMetric>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = logging_projects_metrics_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_projects_metrics_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingProjectsMetricsGetArgs {
    /// Path parameter: metricName
    pub metricName: String,
}

/// GET v2/projects/{projectsId}/metrics/{metricsId}
/// Gets a logs-based metric.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_projects_metrics_get_builder()` + `logging_projects_metrics_get_execute()`.
/// For task-level control, use `logging_projects_metrics_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_projects_metrics_get(
    client: &SimpleHttpClient,
    args: &LoggingProjectsMetricsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogMetric>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_projects_metrics_get_builder(client, &args.metricName)?;
    logging_projects_metrics_get_execute(builder)
}

/// GET v2/projects/{projectsId}/metrics
/// Lists logs-based metrics.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_projects_metrics_list_execute()` to send, or `logging_projects_metrics_list` for simplest API.

pub fn logging_projects_metrics_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/projects/{}/metrics",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/projects/{projectsId}/metrics
/// Lists logs-based metrics.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_projects_metrics_list_execute()` or `logging_projects_metrics_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_projects_metrics_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_projects_metrics_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListLogMetricsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListLogMetricsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/projects/{projectsId}/metrics
/// Lists logs-based metrics.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_projects_metrics_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_projects_metrics_list_task()`.
/// For the simplest API, use `logging_projects_metrics_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_projects_metrics_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_projects_metrics_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListLogMetricsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = logging_projects_metrics_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_projects_metrics_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingProjectsMetricsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v2/projects/{projectsId}/metrics
/// Lists logs-based metrics.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_projects_metrics_list_builder()` + `logging_projects_metrics_list_execute()`.
/// For task-level control, use `logging_projects_metrics_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_projects_metrics_list(
    client: &SimpleHttpClient,
    args: &LoggingProjectsMetricsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListLogMetricsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = logging_projects_metrics_list_builder(
        client,
        &args.parent,
        &args.pageSize,
        &args.pageToken,
    )?;
    logging_projects_metrics_list_execute(builder)
}

/// PUT v2/projects/{projectsId}/metrics/{metricsId}
/// Creates or updates a logs-based metric.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_projects_metrics_update_execute()` to send, or `logging_projects_metrics_update` for simplest API.

pub fn logging_projects_metrics_update_builder<R>(
    client: &SimpleHttpClient<R>,
    metricName: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/projects/{}/metrics/{metricsId}",
        metricName,
    );

    // Build request
    let builder = client
        .put(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PUT v2/projects/{projectsId}/metrics/{metricsId}
/// Creates or updates a logs-based metric.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_projects_metrics_update_execute()` or `logging_projects_metrics_update`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_projects_metrics_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_projects_metrics_update_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<LogMetric>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LogMetric = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PUT v2/projects/{projectsId}/metrics/{metricsId}
/// Creates or updates a logs-based metric.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_projects_metrics_update_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_projects_metrics_update_task()`.
/// For the simplest API, use `logging_projects_metrics_update()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_projects_metrics_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_projects_metrics_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogMetric>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = logging_projects_metrics_update_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_projects_metrics_update`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingProjectsMetricsUpdateArgs {
    /// Path parameter: metricName
    pub metricName: String,
}

/// PUT v2/projects/{projectsId}/metrics/{metricsId}
/// Creates or updates a logs-based metric.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_projects_metrics_update_builder()` + `logging_projects_metrics_update_execute()`.
/// For task-level control, use `logging_projects_metrics_update_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_projects_metrics_update(
    client: &SimpleHttpClient,
    args: &LoggingProjectsMetricsUpdateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogMetric>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_projects_metrics_update_builder(client, &args.metricName)?;
    logging_projects_metrics_update_execute(builder)
}

/// POST v2/projects/{projectsId}/sinks
/// Creates a sink that exports specified log entries to a destination. The export begins upon ingress, unless the sink''s writer_identity is not permitted to write to the destination. A sink can export log entries only from the resource owning the sink.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_projects_sinks_create_execute()` to send, or `logging_projects_sinks_create` for simplest API.

pub fn logging_projects_sinks_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    customWriterIdentity: &Option<String>,
    uniqueWriterIdentity: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/projects/{}/sinks",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = customWriterIdentity.as_ref() {
        query_parts.push(format!("customWriterIdentity={}", val));
    }
    if let Some(val) = uniqueWriterIdentity.as_ref() {
        query_parts.push(format!("uniqueWriterIdentity={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v2/projects/{projectsId}/sinks
/// Creates a sink that exports specified log entries to a destination. The export begins upon ingress, unless the sink''s writer_identity is not permitted to write to the destination. A sink can export log entries only from the resource owning the sink.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_projects_sinks_create_execute()` or `logging_projects_sinks_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_projects_sinks_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_projects_sinks_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<LogSink>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LogSink = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v2/projects/{projectsId}/sinks
/// Creates a sink that exports specified log entries to a destination. The export begins upon ingress, unless the sink''s writer_identity is not permitted to write to the destination. A sink can export log entries only from the resource owning the sink.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_projects_sinks_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_projects_sinks_create_task()`.
/// For the simplest API, use `logging_projects_sinks_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_projects_sinks_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_projects_sinks_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogSink>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = logging_projects_sinks_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_projects_sinks_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingProjectsSinksCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: customWriterIdentity
    pub customWriterIdentity: Option<String>,
    /// Query parameter: uniqueWriterIdentity
    pub uniqueWriterIdentity: Option<String>,
}

/// POST v2/projects/{projectsId}/sinks
/// Creates a sink that exports specified log entries to a destination. The export begins upon ingress, unless the sink''s writer_identity is not permitted to write to the destination. A sink can export log entries only from the resource owning the sink.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_projects_sinks_create_builder()` + `logging_projects_sinks_create_execute()`.
/// For task-level control, use `logging_projects_sinks_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_projects_sinks_create(
    client: &SimpleHttpClient,
    args: &LoggingProjectsSinksCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogSink>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_projects_sinks_create_builder(
        client,
        &args.parent,
        &args.customWriterIdentity,
        &args.uniqueWriterIdentity,
    )?;
    logging_projects_sinks_create_execute(builder)
}

/// DELETE v2/projects/{projectsId}/sinks/{sinksId}
/// Deletes a sink. If the sink has a unique writer_identity, then that service account is also deleted.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_projects_sinks_delete_execute()` to send, or `logging_projects_sinks_delete` for simplest API.

pub fn logging_projects_sinks_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    sinkName: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/projects/{}/sinks/{sinksId}",
        sinkName,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v2/projects/{projectsId}/sinks/{sinksId}
/// Deletes a sink. If the sink has a unique writer_identity, then that service account is also deleted.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_projects_sinks_delete_execute()` or `logging_projects_sinks_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_projects_sinks_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_projects_sinks_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v2/projects/{projectsId}/sinks/{sinksId}
/// Deletes a sink. If the sink has a unique writer_identity, then that service account is also deleted.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_projects_sinks_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_projects_sinks_delete_task()`.
/// For the simplest API, use `logging_projects_sinks_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_projects_sinks_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_projects_sinks_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = logging_projects_sinks_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_projects_sinks_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingProjectsSinksDeleteArgs {
    /// Path parameter: sinkName
    pub sinkName: String,
}

/// DELETE v2/projects/{projectsId}/sinks/{sinksId}
/// Deletes a sink. If the sink has a unique writer_identity, then that service account is also deleted.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_projects_sinks_delete_builder()` + `logging_projects_sinks_delete_execute()`.
/// For task-level control, use `logging_projects_sinks_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_projects_sinks_delete(
    client: &SimpleHttpClient,
    args: &LoggingProjectsSinksDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_projects_sinks_delete_builder(client, &args.sinkName)?;
    logging_projects_sinks_delete_execute(builder)
}

/// GET v2/projects/{projectsId}/sinks/{sinksId}
/// Gets a sink.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_projects_sinks_get_execute()` to send, or `logging_projects_sinks_get` for simplest API.

pub fn logging_projects_sinks_get_builder<R>(
    client: &SimpleHttpClient<R>,
    sinkName: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/projects/{}/sinks/{sinksId}",
        sinkName,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/projects/{projectsId}/sinks/{sinksId}
/// Gets a sink.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_projects_sinks_get_execute()` or `logging_projects_sinks_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_projects_sinks_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_projects_sinks_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<LogSink>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LogSink = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/projects/{projectsId}/sinks/{sinksId}
/// Gets a sink.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_projects_sinks_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_projects_sinks_get_task()`.
/// For the simplest API, use `logging_projects_sinks_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_projects_sinks_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_projects_sinks_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogSink>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = logging_projects_sinks_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_projects_sinks_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingProjectsSinksGetArgs {
    /// Path parameter: sinkName
    pub sinkName: String,
}

/// GET v2/projects/{projectsId}/sinks/{sinksId}
/// Gets a sink.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_projects_sinks_get_builder()` + `logging_projects_sinks_get_execute()`.
/// For task-level control, use `logging_projects_sinks_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_projects_sinks_get(
    client: &SimpleHttpClient,
    args: &LoggingProjectsSinksGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogSink>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_projects_sinks_get_builder(client, &args.sinkName)?;
    logging_projects_sinks_get_execute(builder)
}

/// GET v2/projects/{projectsId}/sinks
/// Lists sinks.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_projects_sinks_list_execute()` to send, or `logging_projects_sinks_list` for simplest API.

pub fn logging_projects_sinks_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    filter: &Option<String>,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/projects/{}/sinks",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/projects/{projectsId}/sinks
/// Lists sinks.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_projects_sinks_list_execute()` or `logging_projects_sinks_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_projects_sinks_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_projects_sinks_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListSinksResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListSinksResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/projects/{projectsId}/sinks
/// Lists sinks.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_projects_sinks_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_projects_sinks_list_task()`.
/// For the simplest API, use `logging_projects_sinks_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_projects_sinks_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_projects_sinks_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListSinksResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = logging_projects_sinks_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_projects_sinks_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingProjectsSinksListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v2/projects/{projectsId}/sinks
/// Lists sinks.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_projects_sinks_list_builder()` + `logging_projects_sinks_list_execute()`.
/// For task-level control, use `logging_projects_sinks_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_projects_sinks_list(
    client: &SimpleHttpClient,
    args: &LoggingProjectsSinksListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListSinksResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = logging_projects_sinks_list_builder(
        client,
        &args.parent,
        &args.filter,
        &args.pageSize,
        &args.pageToken,
    )?;
    logging_projects_sinks_list_execute(builder)
}

/// PATCH v2/projects/{projectsId}/sinks/{sinksId}
/// Updates a sink. This method replaces the values of the destination and filter fields of the existing sink with the corresponding values from the new sink.The updated sink might also have a new writer_identity; see the unique_writer_identity field.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_projects_sinks_patch_execute()` to send, or `logging_projects_sinks_patch` for simplest API.

pub fn logging_projects_sinks_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    sinkName: &String,
    customWriterIdentity: &Option<String>,
    uniqueWriterIdentity: &Option<String>,
    updateMask: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/projects/{}/sinks/{sinksId}",
        sinkName,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = customWriterIdentity.as_ref() {
        query_parts.push(format!("customWriterIdentity={}", val));
    }
    if let Some(val) = uniqueWriterIdentity.as_ref() {
        query_parts.push(format!("uniqueWriterIdentity={}", val));
    }
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v2/projects/{projectsId}/sinks/{sinksId}
/// Updates a sink. This method replaces the values of the destination and filter fields of the existing sink with the corresponding values from the new sink.The updated sink might also have a new writer_identity; see the unique_writer_identity field.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_projects_sinks_patch_execute()` or `logging_projects_sinks_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_projects_sinks_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_projects_sinks_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<LogSink>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LogSink = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v2/projects/{projectsId}/sinks/{sinksId}
/// Updates a sink. This method replaces the values of the destination and filter fields of the existing sink with the corresponding values from the new sink.The updated sink might also have a new writer_identity; see the unique_writer_identity field.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_projects_sinks_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_projects_sinks_patch_task()`.
/// For the simplest API, use `logging_projects_sinks_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_projects_sinks_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_projects_sinks_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogSink>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = logging_projects_sinks_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_projects_sinks_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingProjectsSinksPatchArgs {
    /// Path parameter: sinkName
    pub sinkName: String,
    /// Query parameter: customWriterIdentity
    pub customWriterIdentity: Option<String>,
    /// Query parameter: uniqueWriterIdentity
    pub uniqueWriterIdentity: Option<String>,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
}

/// PATCH v2/projects/{projectsId}/sinks/{sinksId}
/// Updates a sink. This method replaces the values of the destination and filter fields of the existing sink with the corresponding values from the new sink.The updated sink might also have a new writer_identity; see the unique_writer_identity field.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_projects_sinks_patch_builder()` + `logging_projects_sinks_patch_execute()`.
/// For task-level control, use `logging_projects_sinks_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_projects_sinks_patch(
    client: &SimpleHttpClient,
    args: &LoggingProjectsSinksPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogSink>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_projects_sinks_patch_builder(
        client,
        &args.sinkName,
        &args.customWriterIdentity,
        &args.uniqueWriterIdentity,
        &args.updateMask,
    )?;
    logging_projects_sinks_patch_execute(builder)
}

/// PUT v2/projects/{projectsId}/sinks/{sinksId}
/// Updates a sink. This method replaces the values of the destination and filter fields of the existing sink with the corresponding values from the new sink.The updated sink might also have a new writer_identity; see the unique_writer_identity field.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_projects_sinks_update_execute()` to send, or `logging_projects_sinks_update` for simplest API.

pub fn logging_projects_sinks_update_builder<R>(
    client: &SimpleHttpClient<R>,
    sinkName: &String,
    customWriterIdentity: &Option<String>,
    uniqueWriterIdentity: &Option<String>,
    updateMask: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/projects/{}/sinks/{sinksId}",
        sinkName,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = customWriterIdentity.as_ref() {
        query_parts.push(format!("customWriterIdentity={}", val));
    }
    if let Some(val) = uniqueWriterIdentity.as_ref() {
        query_parts.push(format!("uniqueWriterIdentity={}", val));
    }
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .put(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PUT v2/projects/{projectsId}/sinks/{sinksId}
/// Updates a sink. This method replaces the values of the destination and filter fields of the existing sink with the corresponding values from the new sink.The updated sink might also have a new writer_identity; see the unique_writer_identity field.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_projects_sinks_update_execute()` or `logging_projects_sinks_update`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_projects_sinks_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_projects_sinks_update_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<LogSink>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LogSink = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PUT v2/projects/{projectsId}/sinks/{sinksId}
/// Updates a sink. This method replaces the values of the destination and filter fields of the existing sink with the corresponding values from the new sink.The updated sink might also have a new writer_identity; see the unique_writer_identity field.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_projects_sinks_update_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_projects_sinks_update_task()`.
/// For the simplest API, use `logging_projects_sinks_update()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_projects_sinks_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_projects_sinks_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogSink>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = logging_projects_sinks_update_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_projects_sinks_update`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingProjectsSinksUpdateArgs {
    /// Path parameter: sinkName
    pub sinkName: String,
    /// Query parameter: customWriterIdentity
    pub customWriterIdentity: Option<String>,
    /// Query parameter: uniqueWriterIdentity
    pub uniqueWriterIdentity: Option<String>,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
}

/// PUT v2/projects/{projectsId}/sinks/{sinksId}
/// Updates a sink. This method replaces the values of the destination and filter fields of the existing sink with the corresponding values from the new sink.The updated sink might also have a new writer_identity; see the unique_writer_identity field.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_projects_sinks_update_builder()` + `logging_projects_sinks_update_execute()`.
/// For task-level control, use `logging_projects_sinks_update_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_projects_sinks_update(
    client: &SimpleHttpClient,
    args: &LoggingProjectsSinksUpdateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogSink>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_projects_sinks_update_builder(
        client,
        &args.sinkName,
        &args.customWriterIdentity,
        &args.uniqueWriterIdentity,
        &args.updateMask,
    )?;
    logging_projects_sinks_update_execute(builder)
}

/// POST v2/{v2Id}/{v2Id1}/sinks
/// Creates a sink that exports specified log entries to a destination. The export begins upon ingress, unless the sink''s writer_identity is not permitted to write to the destination. A sink can export log entries only from the resource owning the sink.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_sinks_create_execute()` to send, or `logging_sinks_create` for simplest API.

pub fn logging_sinks_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    customWriterIdentity: &Option<String>,
    uniqueWriterIdentity: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!("https://logging.googleapis.com/v2/{}/{v2Id1}/sinks", parent,);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = customWriterIdentity.as_ref() {
        query_parts.push(format!("customWriterIdentity={}", val));
    }
    if let Some(val) = uniqueWriterIdentity.as_ref() {
        query_parts.push(format!("uniqueWriterIdentity={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v2/{v2Id}/{v2Id1}/sinks
/// Creates a sink that exports specified log entries to a destination. The export begins upon ingress, unless the sink''s writer_identity is not permitted to write to the destination. A sink can export log entries only from the resource owning the sink.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_sinks_create_execute()` or `logging_sinks_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_sinks_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_sinks_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<LogSink>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LogSink = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v2/{v2Id}/{v2Id1}/sinks
/// Creates a sink that exports specified log entries to a destination. The export begins upon ingress, unless the sink''s writer_identity is not permitted to write to the destination. A sink can export log entries only from the resource owning the sink.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_sinks_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_sinks_create_task()`.
/// For the simplest API, use `logging_sinks_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_sinks_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_sinks_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogSink>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = logging_sinks_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_sinks_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingSinksCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: customWriterIdentity
    pub customWriterIdentity: Option<String>,
    /// Query parameter: uniqueWriterIdentity
    pub uniqueWriterIdentity: Option<String>,
}

/// POST v2/{v2Id}/{v2Id1}/sinks
/// Creates a sink that exports specified log entries to a destination. The export begins upon ingress, unless the sink''s writer_identity is not permitted to write to the destination. A sink can export log entries only from the resource owning the sink.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_sinks_create_builder()` + `logging_sinks_create_execute()`.
/// For task-level control, use `logging_sinks_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_sinks_create(
    client: &SimpleHttpClient,
    args: &LoggingSinksCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogSink>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_sinks_create_builder(
        client,
        &args.parent,
        &args.customWriterIdentity,
        &args.uniqueWriterIdentity,
    )?;
    logging_sinks_create_execute(builder)
}

/// DELETE v2/{v2Id}/{v2Id1}/sinks/{sinksId}
/// Deletes a sink. If the sink has a unique writer_identity, then that service account is also deleted.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_sinks_delete_execute()` to send, or `logging_sinks_delete` for simplest API.

pub fn logging_sinks_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    sinkName: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/{}/{v2Id1}/sinks/{sinksId}",
        sinkName,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v2/{v2Id}/{v2Id1}/sinks/{sinksId}
/// Deletes a sink. If the sink has a unique writer_identity, then that service account is also deleted.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_sinks_delete_execute()` or `logging_sinks_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_sinks_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_sinks_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v2/{v2Id}/{v2Id1}/sinks/{sinksId}
/// Deletes a sink. If the sink has a unique writer_identity, then that service account is also deleted.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_sinks_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_sinks_delete_task()`.
/// For the simplest API, use `logging_sinks_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_sinks_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_sinks_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = logging_sinks_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_sinks_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingSinksDeleteArgs {
    /// Path parameter: sinkName
    pub sinkName: String,
}

/// DELETE v2/{v2Id}/{v2Id1}/sinks/{sinksId}
/// Deletes a sink. If the sink has a unique writer_identity, then that service account is also deleted.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_sinks_delete_builder()` + `logging_sinks_delete_execute()`.
/// For task-level control, use `logging_sinks_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_sinks_delete(
    client: &SimpleHttpClient,
    args: &LoggingSinksDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_sinks_delete_builder(client, &args.sinkName)?;
    logging_sinks_delete_execute(builder)
}

/// GET v2/{v2Id}/{v2Id1}/sinks/{sinksId}
/// Gets a sink.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_sinks_get_execute()` to send, or `logging_sinks_get` for simplest API.

pub fn logging_sinks_get_builder<R>(
    client: &SimpleHttpClient<R>,
    sinkName: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/{}/{v2Id1}/sinks/{sinksId}",
        sinkName,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/{v2Id}/{v2Id1}/sinks/{sinksId}
/// Gets a sink.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_sinks_get_execute()` or `logging_sinks_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_sinks_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_sinks_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<LogSink>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LogSink = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/{v2Id}/{v2Id1}/sinks/{sinksId}
/// Gets a sink.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_sinks_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_sinks_get_task()`.
/// For the simplest API, use `logging_sinks_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_sinks_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_sinks_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogSink>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = logging_sinks_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_sinks_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingSinksGetArgs {
    /// Path parameter: sinkName
    pub sinkName: String,
}

/// GET v2/{v2Id}/{v2Id1}/sinks/{sinksId}
/// Gets a sink.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_sinks_get_builder()` + `logging_sinks_get_execute()`.
/// For task-level control, use `logging_sinks_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_sinks_get(
    client: &SimpleHttpClient,
    args: &LoggingSinksGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogSink>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_sinks_get_builder(client, &args.sinkName)?;
    logging_sinks_get_execute(builder)
}

/// GET v2/{v2Id}/{v2Id1}/sinks
/// Lists sinks.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_sinks_list_execute()` to send, or `logging_sinks_list` for simplest API.

pub fn logging_sinks_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    filter: &Option<String>,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!("https://logging.googleapis.com/v2/{}/{v2Id1}/sinks", parent,);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/{v2Id}/{v2Id1}/sinks
/// Lists sinks.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_sinks_list_execute()` or `logging_sinks_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_sinks_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_sinks_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListSinksResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListSinksResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/{v2Id}/{v2Id1}/sinks
/// Lists sinks.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_sinks_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_sinks_list_task()`.
/// For the simplest API, use `logging_sinks_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_sinks_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_sinks_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListSinksResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = logging_sinks_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_sinks_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingSinksListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v2/{v2Id}/{v2Id1}/sinks
/// Lists sinks.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_sinks_list_builder()` + `logging_sinks_list_execute()`.
/// For task-level control, use `logging_sinks_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_sinks_list(
    client: &SimpleHttpClient,
    args: &LoggingSinksListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListSinksResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = logging_sinks_list_builder(
        client,
        &args.parent,
        &args.filter,
        &args.pageSize,
        &args.pageToken,
    )?;
    logging_sinks_list_execute(builder)
}

/// PUT v2/{v2Id}/{v2Id1}/sinks/{sinksId}
/// Updates a sink. This method replaces the values of the destination and filter fields of the existing sink with the corresponding values from the new sink.The updated sink might also have a new writer_identity; see the unique_writer_identity field.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_sinks_update_execute()` to send, or `logging_sinks_update` for simplest API.

pub fn logging_sinks_update_builder<R>(
    client: &SimpleHttpClient<R>,
    sinkName: &String,
    customWriterIdentity: &Option<String>,
    uniqueWriterIdentity: &Option<String>,
    updateMask: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/{}/{v2Id1}/sinks/{sinksId}",
        sinkName,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = customWriterIdentity.as_ref() {
        query_parts.push(format!("customWriterIdentity={}", val));
    }
    if let Some(val) = uniqueWriterIdentity.as_ref() {
        query_parts.push(format!("uniqueWriterIdentity={}", val));
    }
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .put(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PUT v2/{v2Id}/{v2Id1}/sinks/{sinksId}
/// Updates a sink. This method replaces the values of the destination and filter fields of the existing sink with the corresponding values from the new sink.The updated sink might also have a new writer_identity; see the unique_writer_identity field.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_sinks_update_execute()` or `logging_sinks_update`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_sinks_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_sinks_update_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<LogSink>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LogSink = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PUT v2/{v2Id}/{v2Id1}/sinks/{sinksId}
/// Updates a sink. This method replaces the values of the destination and filter fields of the existing sink with the corresponding values from the new sink.The updated sink might also have a new writer_identity; see the unique_writer_identity field.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_sinks_update_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_sinks_update_task()`.
/// For the simplest API, use `logging_sinks_update()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_sinks_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_sinks_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogSink>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = logging_sinks_update_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_sinks_update`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingSinksUpdateArgs {
    /// Path parameter: sinkName
    pub sinkName: String,
    /// Query parameter: customWriterIdentity
    pub customWriterIdentity: Option<String>,
    /// Query parameter: uniqueWriterIdentity
    pub uniqueWriterIdentity: Option<String>,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
}

/// PUT v2/{v2Id}/{v2Id1}/sinks/{sinksId}
/// Updates a sink. This method replaces the values of the destination and filter fields of the existing sink with the corresponding values from the new sink.The updated sink might also have a new writer_identity; see the unique_writer_identity field.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_sinks_update_builder()` + `logging_sinks_update_execute()`.
/// For task-level control, use `logging_sinks_update_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_sinks_update(
    client: &SimpleHttpClient,
    args: &LoggingSinksUpdateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LogSink>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_sinks_update_builder(
        client,
        &args.sinkName,
        &args.customWriterIdentity,
        &args.uniqueWriterIdentity,
        &args.updateMask,
    )?;
    logging_sinks_update_execute(builder)
}

/// GET v2/{v2Id}/{v2Id1}/cmekSettings
/// Gets the Logging CMEK settings for the given resource.Note: CMEK for the Log Router can be configured for Google Cloud projects, folders, organizations, and billing accounts. Once configured for an organization, it applies to all projects and folders in the Google Cloud organization.See Configure CMEK for Cloud Logging (<https://docs.cloud.google.`com/logging/docs/routing/managed-encryption`>) for more information.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_get_cmek_settings_execute()` to send, or `logging_get_cmek_settings` for simplest API.

pub fn logging_get_cmek_settings_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/{}/{v2Id1}/cmekSettings",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/{v2Id}/{v2Id1}/cmekSettings
/// Gets the Logging CMEK settings for the given resource.Note: CMEK for the Log Router can be configured for Google Cloud projects, folders, organizations, and billing accounts. Once configured for an organization, it applies to all projects and folders in the Google Cloud organization.See Configure CMEK for Cloud Logging (<https://docs.cloud.google.`com/logging/docs/routing/managed-encryption`>) for more information.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_get_cmek_settings_execute()` or `logging_get_cmek_settings`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_get_cmek_settings_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_get_cmek_settings_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<CmekSettings>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: CmekSettings = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/{v2Id}/{v2Id1}/cmekSettings
/// Gets the Logging CMEK settings for the given resource.Note: CMEK for the Log Router can be configured for Google Cloud projects, folders, organizations, and billing accounts. Once configured for an organization, it applies to all projects and folders in the Google Cloud organization.See Configure CMEK for Cloud Logging (<https://docs.cloud.google.`com/logging/docs/routing/managed-encryption`>) for more information.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_get_cmek_settings_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_get_cmek_settings_task()`.
/// For the simplest API, use `logging_get_cmek_settings()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_get_cmek_settings_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_get_cmek_settings_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CmekSettings>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = logging_get_cmek_settings_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_get_cmek_settings`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingGetCmekSettingsArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v2/{v2Id}/{v2Id1}/cmekSettings
/// Gets the Logging CMEK settings for the given resource.Note: CMEK for the Log Router can be configured for Google Cloud projects, folders, organizations, and billing accounts. Once configured for an organization, it applies to all projects and folders in the Google Cloud organization.See Configure CMEK for Cloud Logging (<https://docs.cloud.google.`com/logging/docs/routing/managed-encryption`>) for more information.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_get_cmek_settings_builder()` + `logging_get_cmek_settings_execute()`.
/// For task-level control, use `logging_get_cmek_settings_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_get_cmek_settings(
    client: &SimpleHttpClient,
    args: &LoggingGetCmekSettingsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CmekSettings>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = logging_get_cmek_settings_builder(client, &args.name)?;
    logging_get_cmek_settings_execute(builder)
}

/// GET v2/{v2Id}/{v2Id1}/settings
/// Gets the settings for the given resource.Note: Settings can be retrieved for Google Cloud projects, folders, organizations, and billing accounts.See View default resource settings for Logging (<https://docs.cloud.google.`com/logging/docs/default-settings`#view-org-settings>) for more information.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_get_settings_execute()` to send, or `logging_get_settings` for simplest API.

pub fn logging_get_settings_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/{}/{v2Id1}/settings",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/{v2Id}/{v2Id1}/settings
/// Gets the settings for the given resource.Note: Settings can be retrieved for Google Cloud projects, folders, organizations, and billing accounts.See View default resource settings for Logging (<https://docs.cloud.google.`com/logging/docs/default-settings`#view-org-settings>) for more information.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_get_settings_execute()` or `logging_get_settings`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_get_settings_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_get_settings_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Settings>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Settings = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/{v2Id}/{v2Id1}/settings
/// Gets the settings for the given resource.Note: Settings can be retrieved for Google Cloud projects, folders, organizations, and billing accounts.See View default resource settings for Logging (<https://docs.cloud.google.`com/logging/docs/default-settings`#view-org-settings>) for more information.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_get_settings_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_get_settings_task()`.
/// For the simplest API, use `logging_get_settings()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_get_settings_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_get_settings_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Settings>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = logging_get_settings_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_get_settings`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingGetSettingsArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v2/{v2Id}/{v2Id1}/settings
/// Gets the settings for the given resource.Note: Settings can be retrieved for Google Cloud projects, folders, organizations, and billing accounts.See View default resource settings for Logging (<https://docs.cloud.google.`com/logging/docs/default-settings`#view-org-settings>) for more information.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_get_settings_builder()` + `logging_get_settings_execute()`.
/// For task-level control, use `logging_get_settings_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_get_settings(
    client: &SimpleHttpClient,
    args: &LoggingGetSettingsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Settings>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_get_settings_builder(client, &args.name)?;
    logging_get_settings_execute(builder)
}

/// PATCH v2/{v2Id}/{v2Id1}/cmekSettings
/// Updates the Log Router CMEK settings for the given resource.Note: CMEK for the Log Router can currently only be configured for Google Cloud organizations. Once configured, it applies to all projects and folders in the Google Cloud organization.UpdateCmekSettings fails when any of the following are `true`: The value of kms_key_name is invalid. The associated service account doesn''t have the required `roles/cloudkms`.`cryptoKeyEncrypterDecrypter` role assigned for the key. Access to the key is disabled.See Configure CMEK for Cloud Logging (<https://docs.cloud.google.`com/logging/docs/routing/managed-encryption`>) for more information.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_update_cmek_settings_execute()` to send, or `logging_update_cmek_settings` for simplest API.

pub fn logging_update_cmek_settings_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    updateMask: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/{}/{v2Id1}/cmekSettings",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v2/{v2Id}/{v2Id1}/cmekSettings
/// Updates the Log Router CMEK settings for the given resource.Note: CMEK for the Log Router can currently only be configured for Google Cloud organizations. Once configured, it applies to all projects and folders in the Google Cloud organization.UpdateCmekSettings fails when any of the following are `true`: The value of kms_key_name is invalid. The associated service account doesn''t have the required `roles/cloudkms`.`cryptoKeyEncrypterDecrypter` role assigned for the key. Access to the key is disabled.See Configure CMEK for Cloud Logging (<https://docs.cloud.google.`com/logging/docs/routing/managed-encryption`>) for more information.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_update_cmek_settings_execute()` or `logging_update_cmek_settings`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_update_cmek_settings_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_update_cmek_settings_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<CmekSettings>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: CmekSettings = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v2/{v2Id}/{v2Id1}/cmekSettings
/// Updates the Log Router CMEK settings for the given resource.Note: CMEK for the Log Router can currently only be configured for Google Cloud organizations. Once configured, it applies to all projects and folders in the Google Cloud organization.UpdateCmekSettings fails when any of the following are `true`: The value of kms_key_name is invalid. The associated service account doesn''t have the required `roles/cloudkms`.`cryptoKeyEncrypterDecrypter` role assigned for the key. Access to the key is disabled.See Configure CMEK for Cloud Logging (<https://docs.cloud.google.`com/logging/docs/routing/managed-encryption`>) for more information.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_update_cmek_settings_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_update_cmek_settings_task()`.
/// For the simplest API, use `logging_update_cmek_settings()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_update_cmek_settings_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_update_cmek_settings_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CmekSettings>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = logging_update_cmek_settings_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_update_cmek_settings`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingUpdateCmekSettingsArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
}

/// PATCH v2/{v2Id}/{v2Id1}/cmekSettings
/// Updates the Log Router CMEK settings for the given resource.Note: CMEK for the Log Router can currently only be configured for Google Cloud organizations. Once configured, it applies to all projects and folders in the Google Cloud organization.UpdateCmekSettings fails when any of the following are `true`: The value of kms_key_name is invalid. The associated service account doesn''t have the required `roles/cloudkms`.`cryptoKeyEncrypterDecrypter` role assigned for the key. Access to the key is disabled.See Configure CMEK for Cloud Logging (<https://docs.cloud.google.`com/logging/docs/routing/managed-encryption`>) for more information.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_update_cmek_settings_builder()` + `logging_update_cmek_settings_execute()`.
/// For task-level control, use `logging_update_cmek_settings_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_update_cmek_settings(
    client: &SimpleHttpClient,
    args: &LoggingUpdateCmekSettingsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CmekSettings>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = logging_update_cmek_settings_builder(client, &args.name, &args.updateMask)?;
    logging_update_cmek_settings_execute(builder)
}

/// PATCH v2/{v2Id}/{v2Id1}/settings
/// Updates the settings for the given resource. This method applies to all feature configurations for organization and folders.UpdateSettings fails when any of the following are `true`: The value of storage_location either isn''t supported by Logging or violates the location OrgPolicy. The default_sink_config field is set, but it has an unspecified filter write mode. The value of kms_key_name is invalid. The associated service account doesn''t have the required `roles/cloudkms`.`cryptoKeyEncrypterDecrypter` role assigned for the key. Access to the key is disabled.See Configure default settings for organizations and folders (<https://docs.cloud.google.`com/logging/docs/default-settings`>) for more information.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `logging_update_settings_execute()` to send, or `logging_update_settings` for simplest API.

pub fn logging_update_settings_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    updateMask: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://logging.googleapis.com/v2/{}/{v2Id1}/settings",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v2/{v2Id}/{v2Id1}/settings
/// Updates the settings for the given resource. This method applies to all feature configurations for organization and folders.UpdateSettings fails when any of the following are `true`: The value of storage_location either isn''t supported by Logging or violates the location OrgPolicy. The default_sink_config field is set, but it has an unspecified filter write mode. The value of kms_key_name is invalid. The associated service account doesn''t have the required `roles/cloudkms`.`cryptoKeyEncrypterDecrypter` role assigned for the key. Access to the key is disabled.See Configure default settings for organizations and folders (<https://docs.cloud.google.`com/logging/docs/default-settings`>) for more information.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `logging_update_settings_execute()` or `logging_update_settings`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_update_settings_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_update_settings_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Settings>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Settings = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v2/{v2Id}/{v2Id1}/settings
/// Updates the settings for the given resource. This method applies to all feature configurations for organization and folders.UpdateSettings fails when any of the following are `true`: The value of storage_location either isn''t supported by Logging or violates the location OrgPolicy. The default_sink_config field is set, but it has an unspecified filter write mode. The value of kms_key_name is invalid. The associated service account doesn''t have the required `roles/cloudkms`.`cryptoKeyEncrypterDecrypter` role assigned for the key. Access to the key is disabled.See Configure default settings for organizations and folders (<https://docs.cloud.google.`com/logging/docs/default-settings`>) for more information.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `logging_update_settings_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `logging_update_settings_task()`.
/// For the simplest API, use `logging_update_settings()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `logging_update_settings_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn logging_update_settings_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Settings>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = logging_update_settings_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`logging_update_settings`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct LoggingUpdateSettingsArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
}

/// PATCH v2/{v2Id}/{v2Id1}/settings
/// Updates the settings for the given resource. This method applies to all feature configurations for organization and folders.UpdateSettings fails when any of the following are `true`: The value of storage_location either isn''t supported by Logging or violates the location OrgPolicy. The default_sink_config field is set, but it has an unspecified filter write mode. The value of kms_key_name is invalid. The associated service account doesn''t have the required `roles/cloudkms`.`cryptoKeyEncrypterDecrypter` role assigned for the key. Access to the key is disabled.See Configure default settings for organizations and folders (<https://docs.cloud.google.`com/logging/docs/default-settings`>) for more information.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `logging_update_settings_builder()` + `logging_update_settings_execute()`.
/// For task-level control, use `logging_update_settings_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn logging_update_settings(
    client: &SimpleHttpClient,
    args: &LoggingUpdateSettingsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Settings>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = logging_update_settings_builder(client, &args.name, &args.updateMask)?;
    logging_update_settings_execute(builder)
}

// =============================================================================
// ResourceIdentifier implementation for CmekSettings
// =============================================================================

/// ResourceIdentifier implementation for CmekSettings with LoggingBillingAccountsGetCmekSettingsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingBillingAccountsGetCmekSettingsArgs> for CmekSettings {
    fn generate_resource_id(&self, input: &LoggingBillingAccountsGetCmekSettingsArgs) -> String {
        format!("gcp::logging::CmekSettings/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::CmekSettings"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Settings
// =============================================================================

/// ResourceIdentifier implementation for Settings with LoggingBillingAccountsGetSettingsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingBillingAccountsGetSettingsArgs> for Settings {
    fn generate_resource_id(&self, input: &LoggingBillingAccountsGetSettingsArgs) -> String {
        format!("gcp::logging::Settings/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::Settings"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for LogExclusion
// =============================================================================

/// ResourceIdentifier implementation for LogExclusion with LoggingBillingAccountsExclusionsCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingBillingAccountsExclusionsCreateArgs> for LogExclusion {
    fn generate_resource_id(&self, input: &LoggingBillingAccountsExclusionsCreateArgs) -> String {
        format!("gcp::logging::LogExclusion/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::LogExclusion"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Empty
// =============================================================================

/// ResourceIdentifier implementation for Empty with LoggingBillingAccountsExclusionsDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingBillingAccountsExclusionsDeleteArgs> for Empty {
    fn generate_resource_id(&self, input: &LoggingBillingAccountsExclusionsDeleteArgs) -> String {
        format!("gcp::logging::Empty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::Empty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for LogExclusion
// =============================================================================

/// ResourceIdentifier implementation for LogExclusion with LoggingBillingAccountsExclusionsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingBillingAccountsExclusionsGetArgs> for LogExclusion {
    fn generate_resource_id(&self, input: &LoggingBillingAccountsExclusionsGetArgs) -> String {
        format!("gcp::logging::LogExclusion/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::LogExclusion"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListExclusionsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListExclusionsResponse with LoggingBillingAccountsExclusionsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingBillingAccountsExclusionsListArgs> for ListExclusionsResponse {
    fn generate_resource_id(&self, input: &LoggingBillingAccountsExclusionsListArgs) -> String {
        format!("gcp::logging::ListExclusionsResponse/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::ListExclusionsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for LogExclusion
// =============================================================================

/// ResourceIdentifier implementation for LogExclusion with LoggingBillingAccountsExclusionsPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingBillingAccountsExclusionsPatchArgs> for LogExclusion {
    fn generate_resource_id(&self, input: &LoggingBillingAccountsExclusionsPatchArgs) -> String {
        format!("gcp::logging::LogExclusion/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::LogExclusion"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Location
// =============================================================================

/// ResourceIdentifier implementation for Location with LoggingBillingAccountsLocationsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingBillingAccountsLocationsGetArgs> for Location {
    fn generate_resource_id(&self, input: &LoggingBillingAccountsLocationsGetArgs) -> String {
        format!("gcp::logging::Location/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::Location"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListLocationsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListLocationsResponse with LoggingBillingAccountsLocationsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingBillingAccountsLocationsListArgs> for ListLocationsResponse {
    fn generate_resource_id(&self, input: &LoggingBillingAccountsLocationsListArgs) -> String {
        format!("gcp::logging::ListLocationsResponse/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::ListLocationsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for LogBucket
// =============================================================================

/// ResourceIdentifier implementation for LogBucket with LoggingBillingAccountsLocationsBucketsCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingBillingAccountsLocationsBucketsCreateArgs> for LogBucket {
    fn generate_resource_id(
        &self,
        input: &LoggingBillingAccountsLocationsBucketsCreateArgs,
    ) -> String {
        format!("gcp::logging::LogBucket/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::LogBucket"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with LoggingBillingAccountsLocationsBucketsCreateAsyncArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingBillingAccountsLocationsBucketsCreateAsyncArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &LoggingBillingAccountsLocationsBucketsCreateAsyncArgs,
    ) -> String {
        format!("gcp::logging::Operation/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Empty
// =============================================================================

/// ResourceIdentifier implementation for Empty with LoggingBillingAccountsLocationsBucketsDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingBillingAccountsLocationsBucketsDeleteArgs> for Empty {
    fn generate_resource_id(
        &self,
        input: &LoggingBillingAccountsLocationsBucketsDeleteArgs,
    ) -> String {
        format!("gcp::logging::Empty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::Empty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for LogBucket
// =============================================================================

/// ResourceIdentifier implementation for LogBucket with LoggingBillingAccountsLocationsBucketsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingBillingAccountsLocationsBucketsGetArgs> for LogBucket {
    fn generate_resource_id(
        &self,
        input: &LoggingBillingAccountsLocationsBucketsGetArgs,
    ) -> String {
        format!("gcp::logging::LogBucket/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::LogBucket"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListBucketsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListBucketsResponse with LoggingBillingAccountsLocationsBucketsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingBillingAccountsLocationsBucketsListArgs> for ListBucketsResponse {
    fn generate_resource_id(
        &self,
        input: &LoggingBillingAccountsLocationsBucketsListArgs,
    ) -> String {
        format!("gcp::logging::ListBucketsResponse/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::ListBucketsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for LogBucket
// =============================================================================

/// ResourceIdentifier implementation for LogBucket with LoggingBillingAccountsLocationsBucketsPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingBillingAccountsLocationsBucketsPatchArgs> for LogBucket {
    fn generate_resource_id(
        &self,
        input: &LoggingBillingAccountsLocationsBucketsPatchArgs,
    ) -> String {
        format!("gcp::logging::LogBucket/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::LogBucket"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Empty
// =============================================================================

/// ResourceIdentifier implementation for Empty with LoggingBillingAccountsLocationsBucketsUndeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingBillingAccountsLocationsBucketsUndeleteArgs> for Empty {
    fn generate_resource_id(
        &self,
        input: &LoggingBillingAccountsLocationsBucketsUndeleteArgs,
    ) -> String {
        format!("gcp::logging::Empty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::Empty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with LoggingBillingAccountsLocationsBucketsUpdateAsyncArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingBillingAccountsLocationsBucketsUpdateAsyncArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &LoggingBillingAccountsLocationsBucketsUpdateAsyncArgs,
    ) -> String {
        format!("gcp::logging::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with LoggingBillingAccountsLocationsBucketsLinksCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingBillingAccountsLocationsBucketsLinksCreateArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &LoggingBillingAccountsLocationsBucketsLinksCreateArgs,
    ) -> String {
        format!("gcp::logging::Operation/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with LoggingBillingAccountsLocationsBucketsLinksDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingBillingAccountsLocationsBucketsLinksDeleteArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &LoggingBillingAccountsLocationsBucketsLinksDeleteArgs,
    ) -> String {
        format!("gcp::logging::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Link
// =============================================================================

/// ResourceIdentifier implementation for Link with LoggingBillingAccountsLocationsBucketsLinksGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingBillingAccountsLocationsBucketsLinksGetArgs> for Link {
    fn generate_resource_id(
        &self,
        input: &LoggingBillingAccountsLocationsBucketsLinksGetArgs,
    ) -> String {
        format!("gcp::logging::Link/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::Link"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListLinksResponse
// =============================================================================

/// ResourceIdentifier implementation for ListLinksResponse with LoggingBillingAccountsLocationsBucketsLinksListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingBillingAccountsLocationsBucketsLinksListArgs> for ListLinksResponse {
    fn generate_resource_id(
        &self,
        input: &LoggingBillingAccountsLocationsBucketsLinksListArgs,
    ) -> String {
        format!("gcp::logging::ListLinksResponse/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::ListLinksResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for LogView
// =============================================================================

/// ResourceIdentifier implementation for LogView with LoggingBillingAccountsLocationsBucketsViewsCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingBillingAccountsLocationsBucketsViewsCreateArgs> for LogView {
    fn generate_resource_id(
        &self,
        input: &LoggingBillingAccountsLocationsBucketsViewsCreateArgs,
    ) -> String {
        format!("gcp::logging::LogView/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::LogView"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Empty
// =============================================================================

/// ResourceIdentifier implementation for Empty with LoggingBillingAccountsLocationsBucketsViewsDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingBillingAccountsLocationsBucketsViewsDeleteArgs> for Empty {
    fn generate_resource_id(
        &self,
        input: &LoggingBillingAccountsLocationsBucketsViewsDeleteArgs,
    ) -> String {
        format!("gcp::logging::Empty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::Empty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for LogView
// =============================================================================

/// ResourceIdentifier implementation for LogView with LoggingBillingAccountsLocationsBucketsViewsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingBillingAccountsLocationsBucketsViewsGetArgs> for LogView {
    fn generate_resource_id(
        &self,
        input: &LoggingBillingAccountsLocationsBucketsViewsGetArgs,
    ) -> String {
        format!("gcp::logging::LogView/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::LogView"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListViewsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListViewsResponse with LoggingBillingAccountsLocationsBucketsViewsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingBillingAccountsLocationsBucketsViewsListArgs> for ListViewsResponse {
    fn generate_resource_id(
        &self,
        input: &LoggingBillingAccountsLocationsBucketsViewsListArgs,
    ) -> String {
        format!("gcp::logging::ListViewsResponse/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::ListViewsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for LogView
// =============================================================================

/// ResourceIdentifier implementation for LogView with LoggingBillingAccountsLocationsBucketsViewsPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingBillingAccountsLocationsBucketsViewsPatchArgs> for LogView {
    fn generate_resource_id(
        &self,
        input: &LoggingBillingAccountsLocationsBucketsViewsPatchArgs,
    ) -> String {
        format!("gcp::logging::LogView/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::LogView"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListLogsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListLogsResponse with LoggingBillingAccountsLocationsBucketsViewsLogsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingBillingAccountsLocationsBucketsViewsLogsListArgs>
    for ListLogsResponse
{
    fn generate_resource_id(
        &self,
        input: &LoggingBillingAccountsLocationsBucketsViewsLogsListArgs,
    ) -> String {
        format!("gcp::logging::ListLogsResponse/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::ListLogsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Empty
// =============================================================================

/// ResourceIdentifier implementation for Empty with LoggingBillingAccountsLocationsOperationsCancelArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingBillingAccountsLocationsOperationsCancelArgs> for Empty {
    fn generate_resource_id(
        &self,
        input: &LoggingBillingAccountsLocationsOperationsCancelArgs,
    ) -> String {
        format!("gcp::logging::Empty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::Empty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with LoggingBillingAccountsLocationsOperationsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingBillingAccountsLocationsOperationsGetArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &LoggingBillingAccountsLocationsOperationsGetArgs,
    ) -> String {
        format!("gcp::logging::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListOperationsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListOperationsResponse with LoggingBillingAccountsLocationsOperationsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingBillingAccountsLocationsOperationsListArgs>
    for ListOperationsResponse
{
    fn generate_resource_id(
        &self,
        input: &LoggingBillingAccountsLocationsOperationsListArgs,
    ) -> String {
        format!("gcp::logging::ListOperationsResponse/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::ListOperationsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListRecentQueriesResponse
// =============================================================================

/// ResourceIdentifier implementation for ListRecentQueriesResponse with LoggingBillingAccountsLocationsRecentQueriesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingBillingAccountsLocationsRecentQueriesListArgs>
    for ListRecentQueriesResponse
{
    fn generate_resource_id(
        &self,
        input: &LoggingBillingAccountsLocationsRecentQueriesListArgs,
    ) -> String {
        format!("gcp::logging::ListRecentQueriesResponse/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::ListRecentQueriesResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for SavedQuery
// =============================================================================

/// ResourceIdentifier implementation for SavedQuery with LoggingBillingAccountsLocationsSavedQueriesCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingBillingAccountsLocationsSavedQueriesCreateArgs> for SavedQuery {
    fn generate_resource_id(
        &self,
        input: &LoggingBillingAccountsLocationsSavedQueriesCreateArgs,
    ) -> String {
        format!("gcp::logging::SavedQuery/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::SavedQuery"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Empty
// =============================================================================

/// ResourceIdentifier implementation for Empty with LoggingBillingAccountsLocationsSavedQueriesDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingBillingAccountsLocationsSavedQueriesDeleteArgs> for Empty {
    fn generate_resource_id(
        &self,
        input: &LoggingBillingAccountsLocationsSavedQueriesDeleteArgs,
    ) -> String {
        format!("gcp::logging::Empty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::Empty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for SavedQuery
// =============================================================================

/// ResourceIdentifier implementation for SavedQuery with LoggingBillingAccountsLocationsSavedQueriesGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingBillingAccountsLocationsSavedQueriesGetArgs> for SavedQuery {
    fn generate_resource_id(
        &self,
        input: &LoggingBillingAccountsLocationsSavedQueriesGetArgs,
    ) -> String {
        format!("gcp::logging::SavedQuery/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::SavedQuery"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListSavedQueriesResponse
// =============================================================================

/// ResourceIdentifier implementation for ListSavedQueriesResponse with LoggingBillingAccountsLocationsSavedQueriesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingBillingAccountsLocationsSavedQueriesListArgs>
    for ListSavedQueriesResponse
{
    fn generate_resource_id(
        &self,
        input: &LoggingBillingAccountsLocationsSavedQueriesListArgs,
    ) -> String {
        format!("gcp::logging::ListSavedQueriesResponse/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::ListSavedQueriesResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for SavedQuery
// =============================================================================

/// ResourceIdentifier implementation for SavedQuery with LoggingBillingAccountsLocationsSavedQueriesPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingBillingAccountsLocationsSavedQueriesPatchArgs> for SavedQuery {
    fn generate_resource_id(
        &self,
        input: &LoggingBillingAccountsLocationsSavedQueriesPatchArgs,
    ) -> String {
        format!("gcp::logging::SavedQuery/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::SavedQuery"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Empty
// =============================================================================

/// ResourceIdentifier implementation for Empty with LoggingBillingAccountsLogsDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingBillingAccountsLogsDeleteArgs> for Empty {
    fn generate_resource_id(&self, input: &LoggingBillingAccountsLogsDeleteArgs) -> String {
        format!("gcp::logging::Empty/{}", input.logName)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::Empty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListLogsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListLogsResponse with LoggingBillingAccountsLogsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingBillingAccountsLogsListArgs> for ListLogsResponse {
    fn generate_resource_id(&self, input: &LoggingBillingAccountsLogsListArgs) -> String {
        format!("gcp::logging::ListLogsResponse/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::ListLogsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for LogSink
// =============================================================================

/// ResourceIdentifier implementation for LogSink with LoggingBillingAccountsSinksCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingBillingAccountsSinksCreateArgs> for LogSink {
    fn generate_resource_id(&self, input: &LoggingBillingAccountsSinksCreateArgs) -> String {
        format!("gcp::logging::LogSink/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::LogSink"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Empty
// =============================================================================

/// ResourceIdentifier implementation for Empty with LoggingBillingAccountsSinksDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingBillingAccountsSinksDeleteArgs> for Empty {
    fn generate_resource_id(&self, input: &LoggingBillingAccountsSinksDeleteArgs) -> String {
        format!("gcp::logging::Empty/{}", input.sinkName)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::Empty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for LogSink
// =============================================================================

/// ResourceIdentifier implementation for LogSink with LoggingBillingAccountsSinksGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingBillingAccountsSinksGetArgs> for LogSink {
    fn generate_resource_id(&self, input: &LoggingBillingAccountsSinksGetArgs) -> String {
        format!("gcp::logging::LogSink/{}", input.sinkName)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::LogSink"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListSinksResponse
// =============================================================================

/// ResourceIdentifier implementation for ListSinksResponse with LoggingBillingAccountsSinksListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingBillingAccountsSinksListArgs> for ListSinksResponse {
    fn generate_resource_id(&self, input: &LoggingBillingAccountsSinksListArgs) -> String {
        format!("gcp::logging::ListSinksResponse/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::ListSinksResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for LogSink
// =============================================================================

/// ResourceIdentifier implementation for LogSink with LoggingBillingAccountsSinksPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingBillingAccountsSinksPatchArgs> for LogSink {
    fn generate_resource_id(&self, input: &LoggingBillingAccountsSinksPatchArgs) -> String {
        format!("gcp::logging::LogSink/{}", input.sinkName)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::LogSink"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for LogSink
// =============================================================================

/// ResourceIdentifier implementation for LogSink with LoggingBillingAccountsSinksUpdateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingBillingAccountsSinksUpdateArgs> for LogSink {
    fn generate_resource_id(&self, input: &LoggingBillingAccountsSinksUpdateArgs) -> String {
        format!("gcp::logging::LogSink/{}", input.sinkName)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::LogSink"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for LogExclusion
// =============================================================================

/// ResourceIdentifier implementation for LogExclusion with LoggingExclusionsCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingExclusionsCreateArgs> for LogExclusion {
    fn generate_resource_id(&self, input: &LoggingExclusionsCreateArgs) -> String {
        format!("gcp::logging::LogExclusion/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::LogExclusion"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Empty
// =============================================================================

/// ResourceIdentifier implementation for Empty with LoggingExclusionsDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingExclusionsDeleteArgs> for Empty {
    fn generate_resource_id(&self, input: &LoggingExclusionsDeleteArgs) -> String {
        format!("gcp::logging::Empty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::Empty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for LogExclusion
// =============================================================================

/// ResourceIdentifier implementation for LogExclusion with LoggingExclusionsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingExclusionsGetArgs> for LogExclusion {
    fn generate_resource_id(&self, input: &LoggingExclusionsGetArgs) -> String {
        format!("gcp::logging::LogExclusion/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::LogExclusion"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListExclusionsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListExclusionsResponse with LoggingExclusionsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingExclusionsListArgs> for ListExclusionsResponse {
    fn generate_resource_id(&self, input: &LoggingExclusionsListArgs) -> String {
        format!("gcp::logging::ListExclusionsResponse/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::ListExclusionsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for LogExclusion
// =============================================================================

/// ResourceIdentifier implementation for LogExclusion with LoggingExclusionsPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingExclusionsPatchArgs> for LogExclusion {
    fn generate_resource_id(&self, input: &LoggingExclusionsPatchArgs) -> String {
        format!("gcp::logging::LogExclusion/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::LogExclusion"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for CmekSettings
// =============================================================================

/// ResourceIdentifier implementation for CmekSettings with LoggingFoldersGetCmekSettingsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingFoldersGetCmekSettingsArgs> for CmekSettings {
    fn generate_resource_id(&self, input: &LoggingFoldersGetCmekSettingsArgs) -> String {
        format!("gcp::logging::CmekSettings/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::CmekSettings"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Settings
// =============================================================================

/// ResourceIdentifier implementation for Settings with LoggingFoldersGetSettingsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingFoldersGetSettingsArgs> for Settings {
    fn generate_resource_id(&self, input: &LoggingFoldersGetSettingsArgs) -> String {
        format!("gcp::logging::Settings/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::Settings"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Settings
// =============================================================================

/// ResourceIdentifier implementation for Settings with LoggingFoldersUpdateSettingsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingFoldersUpdateSettingsArgs> for Settings {
    fn generate_resource_id(&self, input: &LoggingFoldersUpdateSettingsArgs) -> String {
        format!("gcp::logging::Settings/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::Settings"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for LogExclusion
// =============================================================================

/// ResourceIdentifier implementation for LogExclusion with LoggingFoldersExclusionsCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingFoldersExclusionsCreateArgs> for LogExclusion {
    fn generate_resource_id(&self, input: &LoggingFoldersExclusionsCreateArgs) -> String {
        format!("gcp::logging::LogExclusion/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::LogExclusion"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Empty
// =============================================================================

/// ResourceIdentifier implementation for Empty with LoggingFoldersExclusionsDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingFoldersExclusionsDeleteArgs> for Empty {
    fn generate_resource_id(&self, input: &LoggingFoldersExclusionsDeleteArgs) -> String {
        format!("gcp::logging::Empty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::Empty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for LogExclusion
// =============================================================================

/// ResourceIdentifier implementation for LogExclusion with LoggingFoldersExclusionsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingFoldersExclusionsGetArgs> for LogExclusion {
    fn generate_resource_id(&self, input: &LoggingFoldersExclusionsGetArgs) -> String {
        format!("gcp::logging::LogExclusion/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::LogExclusion"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListExclusionsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListExclusionsResponse with LoggingFoldersExclusionsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingFoldersExclusionsListArgs> for ListExclusionsResponse {
    fn generate_resource_id(&self, input: &LoggingFoldersExclusionsListArgs) -> String {
        format!("gcp::logging::ListExclusionsResponse/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::ListExclusionsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for LogExclusion
// =============================================================================

/// ResourceIdentifier implementation for LogExclusion with LoggingFoldersExclusionsPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingFoldersExclusionsPatchArgs> for LogExclusion {
    fn generate_resource_id(&self, input: &LoggingFoldersExclusionsPatchArgs) -> String {
        format!("gcp::logging::LogExclusion/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::LogExclusion"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Location
// =============================================================================

/// ResourceIdentifier implementation for Location with LoggingFoldersLocationsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingFoldersLocationsGetArgs> for Location {
    fn generate_resource_id(&self, input: &LoggingFoldersLocationsGetArgs) -> String {
        format!("gcp::logging::Location/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::Location"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListLocationsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListLocationsResponse with LoggingFoldersLocationsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingFoldersLocationsListArgs> for ListLocationsResponse {
    fn generate_resource_id(&self, input: &LoggingFoldersLocationsListArgs) -> String {
        format!("gcp::logging::ListLocationsResponse/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::ListLocationsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for LogBucket
// =============================================================================

/// ResourceIdentifier implementation for LogBucket with LoggingFoldersLocationsBucketsCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingFoldersLocationsBucketsCreateArgs> for LogBucket {
    fn generate_resource_id(&self, input: &LoggingFoldersLocationsBucketsCreateArgs) -> String {
        format!("gcp::logging::LogBucket/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::LogBucket"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with LoggingFoldersLocationsBucketsCreateAsyncArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingFoldersLocationsBucketsCreateAsyncArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &LoggingFoldersLocationsBucketsCreateAsyncArgs,
    ) -> String {
        format!("gcp::logging::Operation/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Empty
// =============================================================================

/// ResourceIdentifier implementation for Empty with LoggingFoldersLocationsBucketsDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingFoldersLocationsBucketsDeleteArgs> for Empty {
    fn generate_resource_id(&self, input: &LoggingFoldersLocationsBucketsDeleteArgs) -> String {
        format!("gcp::logging::Empty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::Empty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for LogBucket
// =============================================================================

/// ResourceIdentifier implementation for LogBucket with LoggingFoldersLocationsBucketsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingFoldersLocationsBucketsGetArgs> for LogBucket {
    fn generate_resource_id(&self, input: &LoggingFoldersLocationsBucketsGetArgs) -> String {
        format!("gcp::logging::LogBucket/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::LogBucket"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListBucketsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListBucketsResponse with LoggingFoldersLocationsBucketsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingFoldersLocationsBucketsListArgs> for ListBucketsResponse {
    fn generate_resource_id(&self, input: &LoggingFoldersLocationsBucketsListArgs) -> String {
        format!("gcp::logging::ListBucketsResponse/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::ListBucketsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for LogBucket
// =============================================================================

/// ResourceIdentifier implementation for LogBucket with LoggingFoldersLocationsBucketsPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingFoldersLocationsBucketsPatchArgs> for LogBucket {
    fn generate_resource_id(&self, input: &LoggingFoldersLocationsBucketsPatchArgs) -> String {
        format!("gcp::logging::LogBucket/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::LogBucket"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Empty
// =============================================================================

/// ResourceIdentifier implementation for Empty with LoggingFoldersLocationsBucketsUndeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingFoldersLocationsBucketsUndeleteArgs> for Empty {
    fn generate_resource_id(&self, input: &LoggingFoldersLocationsBucketsUndeleteArgs) -> String {
        format!("gcp::logging::Empty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::Empty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with LoggingFoldersLocationsBucketsUpdateAsyncArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingFoldersLocationsBucketsUpdateAsyncArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &LoggingFoldersLocationsBucketsUpdateAsyncArgs,
    ) -> String {
        format!("gcp::logging::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with LoggingFoldersLocationsBucketsLinksCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingFoldersLocationsBucketsLinksCreateArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &LoggingFoldersLocationsBucketsLinksCreateArgs,
    ) -> String {
        format!("gcp::logging::Operation/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with LoggingFoldersLocationsBucketsLinksDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingFoldersLocationsBucketsLinksDeleteArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &LoggingFoldersLocationsBucketsLinksDeleteArgs,
    ) -> String {
        format!("gcp::logging::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Link
// =============================================================================

/// ResourceIdentifier implementation for Link with LoggingFoldersLocationsBucketsLinksGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingFoldersLocationsBucketsLinksGetArgs> for Link {
    fn generate_resource_id(&self, input: &LoggingFoldersLocationsBucketsLinksGetArgs) -> String {
        format!("gcp::logging::Link/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::Link"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListLinksResponse
// =============================================================================

/// ResourceIdentifier implementation for ListLinksResponse with LoggingFoldersLocationsBucketsLinksListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingFoldersLocationsBucketsLinksListArgs> for ListLinksResponse {
    fn generate_resource_id(&self, input: &LoggingFoldersLocationsBucketsLinksListArgs) -> String {
        format!("gcp::logging::ListLinksResponse/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::ListLinksResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for LogView
// =============================================================================

/// ResourceIdentifier implementation for LogView with LoggingFoldersLocationsBucketsViewsCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingFoldersLocationsBucketsViewsCreateArgs> for LogView {
    fn generate_resource_id(
        &self,
        input: &LoggingFoldersLocationsBucketsViewsCreateArgs,
    ) -> String {
        format!("gcp::logging::LogView/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::LogView"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Empty
// =============================================================================

/// ResourceIdentifier implementation for Empty with LoggingFoldersLocationsBucketsViewsDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingFoldersLocationsBucketsViewsDeleteArgs> for Empty {
    fn generate_resource_id(
        &self,
        input: &LoggingFoldersLocationsBucketsViewsDeleteArgs,
    ) -> String {
        format!("gcp::logging::Empty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::Empty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for LogView
// =============================================================================

/// ResourceIdentifier implementation for LogView with LoggingFoldersLocationsBucketsViewsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingFoldersLocationsBucketsViewsGetArgs> for LogView {
    fn generate_resource_id(&self, input: &LoggingFoldersLocationsBucketsViewsGetArgs) -> String {
        format!("gcp::logging::LogView/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::LogView"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Policy
// =============================================================================

/// ResourceIdentifier implementation for Policy with LoggingFoldersLocationsBucketsViewsGetIamPolicyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingFoldersLocationsBucketsViewsGetIamPolicyArgs> for Policy {
    fn generate_resource_id(
        &self,
        input: &LoggingFoldersLocationsBucketsViewsGetIamPolicyArgs,
    ) -> String {
        format!("gcp::logging::Policy/{}", input.resource)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::Policy"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListViewsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListViewsResponse with LoggingFoldersLocationsBucketsViewsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingFoldersLocationsBucketsViewsListArgs> for ListViewsResponse {
    fn generate_resource_id(&self, input: &LoggingFoldersLocationsBucketsViewsListArgs) -> String {
        format!("gcp::logging::ListViewsResponse/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::ListViewsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for LogView
// =============================================================================

/// ResourceIdentifier implementation for LogView with LoggingFoldersLocationsBucketsViewsPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingFoldersLocationsBucketsViewsPatchArgs> for LogView {
    fn generate_resource_id(&self, input: &LoggingFoldersLocationsBucketsViewsPatchArgs) -> String {
        format!("gcp::logging::LogView/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::LogView"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Policy
// =============================================================================

/// ResourceIdentifier implementation for Policy with LoggingFoldersLocationsBucketsViewsSetIamPolicyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingFoldersLocationsBucketsViewsSetIamPolicyArgs> for Policy {
    fn generate_resource_id(
        &self,
        input: &LoggingFoldersLocationsBucketsViewsSetIamPolicyArgs,
    ) -> String {
        format!("gcp::logging::Policy/{}", input.resource)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::Policy"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for TestIamPermissionsResponse
// =============================================================================

/// ResourceIdentifier implementation for TestIamPermissionsResponse with LoggingFoldersLocationsBucketsViewsTestIamPermissionsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingFoldersLocationsBucketsViewsTestIamPermissionsArgs>
    for TestIamPermissionsResponse
{
    fn generate_resource_id(
        &self,
        input: &LoggingFoldersLocationsBucketsViewsTestIamPermissionsArgs,
    ) -> String {
        format!(
            "gcp::logging::TestIamPermissionsResponse/{}",
            input.resource
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::TestIamPermissionsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListLogsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListLogsResponse with LoggingFoldersLocationsBucketsViewsLogsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingFoldersLocationsBucketsViewsLogsListArgs> for ListLogsResponse {
    fn generate_resource_id(
        &self,
        input: &LoggingFoldersLocationsBucketsViewsLogsListArgs,
    ) -> String {
        format!("gcp::logging::ListLogsResponse/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::ListLogsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for LogScope
// =============================================================================

/// ResourceIdentifier implementation for LogScope with LoggingFoldersLocationsLogScopesCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingFoldersLocationsLogScopesCreateArgs> for LogScope {
    fn generate_resource_id(&self, input: &LoggingFoldersLocationsLogScopesCreateArgs) -> String {
        format!("gcp::logging::LogScope/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::LogScope"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Empty
// =============================================================================

/// ResourceIdentifier implementation for Empty with LoggingFoldersLocationsLogScopesDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingFoldersLocationsLogScopesDeleteArgs> for Empty {
    fn generate_resource_id(&self, input: &LoggingFoldersLocationsLogScopesDeleteArgs) -> String {
        format!("gcp::logging::Empty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::Empty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for LogScope
// =============================================================================

/// ResourceIdentifier implementation for LogScope with LoggingFoldersLocationsLogScopesGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingFoldersLocationsLogScopesGetArgs> for LogScope {
    fn generate_resource_id(&self, input: &LoggingFoldersLocationsLogScopesGetArgs) -> String {
        format!("gcp::logging::LogScope/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::LogScope"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListLogScopesResponse
// =============================================================================

/// ResourceIdentifier implementation for ListLogScopesResponse with LoggingFoldersLocationsLogScopesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingFoldersLocationsLogScopesListArgs> for ListLogScopesResponse {
    fn generate_resource_id(&self, input: &LoggingFoldersLocationsLogScopesListArgs) -> String {
        format!("gcp::logging::ListLogScopesResponse/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::ListLogScopesResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for LogScope
// =============================================================================

/// ResourceIdentifier implementation for LogScope with LoggingFoldersLocationsLogScopesPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingFoldersLocationsLogScopesPatchArgs> for LogScope {
    fn generate_resource_id(&self, input: &LoggingFoldersLocationsLogScopesPatchArgs) -> String {
        format!("gcp::logging::LogScope/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::LogScope"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Empty
// =============================================================================

/// ResourceIdentifier implementation for Empty with LoggingFoldersLocationsOperationsCancelArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingFoldersLocationsOperationsCancelArgs> for Empty {
    fn generate_resource_id(&self, input: &LoggingFoldersLocationsOperationsCancelArgs) -> String {
        format!("gcp::logging::Empty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::Empty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with LoggingFoldersLocationsOperationsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingFoldersLocationsOperationsGetArgs> for Operation {
    fn generate_resource_id(&self, input: &LoggingFoldersLocationsOperationsGetArgs) -> String {
        format!("gcp::logging::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListOperationsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListOperationsResponse with LoggingFoldersLocationsOperationsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingFoldersLocationsOperationsListArgs> for ListOperationsResponse {
    fn generate_resource_id(&self, input: &LoggingFoldersLocationsOperationsListArgs) -> String {
        format!("gcp::logging::ListOperationsResponse/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::ListOperationsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListRecentQueriesResponse
// =============================================================================

/// ResourceIdentifier implementation for ListRecentQueriesResponse with LoggingFoldersLocationsRecentQueriesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingFoldersLocationsRecentQueriesListArgs>
    for ListRecentQueriesResponse
{
    fn generate_resource_id(&self, input: &LoggingFoldersLocationsRecentQueriesListArgs) -> String {
        format!("gcp::logging::ListRecentQueriesResponse/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::ListRecentQueriesResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for SavedQuery
// =============================================================================

/// ResourceIdentifier implementation for SavedQuery with LoggingFoldersLocationsSavedQueriesCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingFoldersLocationsSavedQueriesCreateArgs> for SavedQuery {
    fn generate_resource_id(
        &self,
        input: &LoggingFoldersLocationsSavedQueriesCreateArgs,
    ) -> String {
        format!("gcp::logging::SavedQuery/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::SavedQuery"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Empty
// =============================================================================

/// ResourceIdentifier implementation for Empty with LoggingFoldersLocationsSavedQueriesDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingFoldersLocationsSavedQueriesDeleteArgs> for Empty {
    fn generate_resource_id(
        &self,
        input: &LoggingFoldersLocationsSavedQueriesDeleteArgs,
    ) -> String {
        format!("gcp::logging::Empty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::Empty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for SavedQuery
// =============================================================================

/// ResourceIdentifier implementation for SavedQuery with LoggingFoldersLocationsSavedQueriesGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingFoldersLocationsSavedQueriesGetArgs> for SavedQuery {
    fn generate_resource_id(&self, input: &LoggingFoldersLocationsSavedQueriesGetArgs) -> String {
        format!("gcp::logging::SavedQuery/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::SavedQuery"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListSavedQueriesResponse
// =============================================================================

/// ResourceIdentifier implementation for ListSavedQueriesResponse with LoggingFoldersLocationsSavedQueriesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingFoldersLocationsSavedQueriesListArgs> for ListSavedQueriesResponse {
    fn generate_resource_id(&self, input: &LoggingFoldersLocationsSavedQueriesListArgs) -> String {
        format!("gcp::logging::ListSavedQueriesResponse/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::ListSavedQueriesResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for SavedQuery
// =============================================================================

/// ResourceIdentifier implementation for SavedQuery with LoggingFoldersLocationsSavedQueriesPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingFoldersLocationsSavedQueriesPatchArgs> for SavedQuery {
    fn generate_resource_id(&self, input: &LoggingFoldersLocationsSavedQueriesPatchArgs) -> String {
        format!("gcp::logging::SavedQuery/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::SavedQuery"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Empty
// =============================================================================

/// ResourceIdentifier implementation for Empty with LoggingFoldersLogsDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingFoldersLogsDeleteArgs> for Empty {
    fn generate_resource_id(&self, input: &LoggingFoldersLogsDeleteArgs) -> String {
        format!("gcp::logging::Empty/{}", input.logName)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::Empty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListLogsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListLogsResponse with LoggingFoldersLogsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingFoldersLogsListArgs> for ListLogsResponse {
    fn generate_resource_id(&self, input: &LoggingFoldersLogsListArgs) -> String {
        format!("gcp::logging::ListLogsResponse/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::ListLogsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for LogSink
// =============================================================================

/// ResourceIdentifier implementation for LogSink with LoggingFoldersSinksCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingFoldersSinksCreateArgs> for LogSink {
    fn generate_resource_id(&self, input: &LoggingFoldersSinksCreateArgs) -> String {
        format!("gcp::logging::LogSink/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::LogSink"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Empty
// =============================================================================

/// ResourceIdentifier implementation for Empty with LoggingFoldersSinksDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingFoldersSinksDeleteArgs> for Empty {
    fn generate_resource_id(&self, input: &LoggingFoldersSinksDeleteArgs) -> String {
        format!("gcp::logging::Empty/{}", input.sinkName)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::Empty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for LogSink
// =============================================================================

/// ResourceIdentifier implementation for LogSink with LoggingFoldersSinksGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingFoldersSinksGetArgs> for LogSink {
    fn generate_resource_id(&self, input: &LoggingFoldersSinksGetArgs) -> String {
        format!("gcp::logging::LogSink/{}", input.sinkName)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::LogSink"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListSinksResponse
// =============================================================================

/// ResourceIdentifier implementation for ListSinksResponse with LoggingFoldersSinksListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingFoldersSinksListArgs> for ListSinksResponse {
    fn generate_resource_id(&self, input: &LoggingFoldersSinksListArgs) -> String {
        format!("gcp::logging::ListSinksResponse/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::ListSinksResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for LogSink
// =============================================================================

/// ResourceIdentifier implementation for LogSink with LoggingFoldersSinksPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingFoldersSinksPatchArgs> for LogSink {
    fn generate_resource_id(&self, input: &LoggingFoldersSinksPatchArgs) -> String {
        format!("gcp::logging::LogSink/{}", input.sinkName)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::LogSink"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for LogSink
// =============================================================================

/// ResourceIdentifier implementation for LogSink with LoggingFoldersSinksUpdateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingFoldersSinksUpdateArgs> for LogSink {
    fn generate_resource_id(&self, input: &LoggingFoldersSinksUpdateArgs) -> String {
        format!("gcp::logging::LogSink/{}", input.sinkName)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::LogSink"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Location
// =============================================================================

/// ResourceIdentifier implementation for Location with LoggingLocationsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingLocationsGetArgs> for Location {
    fn generate_resource_id(&self, input: &LoggingLocationsGetArgs) -> String {
        format!("gcp::logging::Location/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::Location"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListLocationsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListLocationsResponse with LoggingLocationsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingLocationsListArgs> for ListLocationsResponse {
    fn generate_resource_id(&self, input: &LoggingLocationsListArgs) -> String {
        format!("gcp::logging::ListLocationsResponse/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::ListLocationsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for LogBucket
// =============================================================================

/// ResourceIdentifier implementation for LogBucket with LoggingLocationsBucketsCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingLocationsBucketsCreateArgs> for LogBucket {
    fn generate_resource_id(&self, input: &LoggingLocationsBucketsCreateArgs) -> String {
        format!("gcp::logging::LogBucket/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::LogBucket"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with LoggingLocationsBucketsCreateAsyncArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingLocationsBucketsCreateAsyncArgs> for Operation {
    fn generate_resource_id(&self, input: &LoggingLocationsBucketsCreateAsyncArgs) -> String {
        format!("gcp::logging::Operation/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Empty
// =============================================================================

/// ResourceIdentifier implementation for Empty with LoggingLocationsBucketsDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingLocationsBucketsDeleteArgs> for Empty {
    fn generate_resource_id(&self, input: &LoggingLocationsBucketsDeleteArgs) -> String {
        format!("gcp::logging::Empty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::Empty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for LogBucket
// =============================================================================

/// ResourceIdentifier implementation for LogBucket with LoggingLocationsBucketsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingLocationsBucketsGetArgs> for LogBucket {
    fn generate_resource_id(&self, input: &LoggingLocationsBucketsGetArgs) -> String {
        format!("gcp::logging::LogBucket/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::LogBucket"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListBucketsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListBucketsResponse with LoggingLocationsBucketsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingLocationsBucketsListArgs> for ListBucketsResponse {
    fn generate_resource_id(&self, input: &LoggingLocationsBucketsListArgs) -> String {
        format!("gcp::logging::ListBucketsResponse/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::ListBucketsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for LogBucket
// =============================================================================

/// ResourceIdentifier implementation for LogBucket with LoggingLocationsBucketsPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingLocationsBucketsPatchArgs> for LogBucket {
    fn generate_resource_id(&self, input: &LoggingLocationsBucketsPatchArgs) -> String {
        format!("gcp::logging::LogBucket/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::LogBucket"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Empty
// =============================================================================

/// ResourceIdentifier implementation for Empty with LoggingLocationsBucketsUndeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingLocationsBucketsUndeleteArgs> for Empty {
    fn generate_resource_id(&self, input: &LoggingLocationsBucketsUndeleteArgs) -> String {
        format!("gcp::logging::Empty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::Empty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with LoggingLocationsBucketsUpdateAsyncArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingLocationsBucketsUpdateAsyncArgs> for Operation {
    fn generate_resource_id(&self, input: &LoggingLocationsBucketsUpdateAsyncArgs) -> String {
        format!("gcp::logging::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with LoggingLocationsBucketsLinksCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingLocationsBucketsLinksCreateArgs> for Operation {
    fn generate_resource_id(&self, input: &LoggingLocationsBucketsLinksCreateArgs) -> String {
        format!("gcp::logging::Operation/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with LoggingLocationsBucketsLinksDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingLocationsBucketsLinksDeleteArgs> for Operation {
    fn generate_resource_id(&self, input: &LoggingLocationsBucketsLinksDeleteArgs) -> String {
        format!("gcp::logging::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Link
// =============================================================================

/// ResourceIdentifier implementation for Link with LoggingLocationsBucketsLinksGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingLocationsBucketsLinksGetArgs> for Link {
    fn generate_resource_id(&self, input: &LoggingLocationsBucketsLinksGetArgs) -> String {
        format!("gcp::logging::Link/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::Link"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListLinksResponse
// =============================================================================

/// ResourceIdentifier implementation for ListLinksResponse with LoggingLocationsBucketsLinksListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingLocationsBucketsLinksListArgs> for ListLinksResponse {
    fn generate_resource_id(&self, input: &LoggingLocationsBucketsLinksListArgs) -> String {
        format!("gcp::logging::ListLinksResponse/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::ListLinksResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for LogView
// =============================================================================

/// ResourceIdentifier implementation for LogView with LoggingLocationsBucketsViewsCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingLocationsBucketsViewsCreateArgs> for LogView {
    fn generate_resource_id(&self, input: &LoggingLocationsBucketsViewsCreateArgs) -> String {
        format!("gcp::logging::LogView/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::LogView"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Empty
// =============================================================================

/// ResourceIdentifier implementation for Empty with LoggingLocationsBucketsViewsDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingLocationsBucketsViewsDeleteArgs> for Empty {
    fn generate_resource_id(&self, input: &LoggingLocationsBucketsViewsDeleteArgs) -> String {
        format!("gcp::logging::Empty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::Empty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for LogView
// =============================================================================

/// ResourceIdentifier implementation for LogView with LoggingLocationsBucketsViewsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingLocationsBucketsViewsGetArgs> for LogView {
    fn generate_resource_id(&self, input: &LoggingLocationsBucketsViewsGetArgs) -> String {
        format!("gcp::logging::LogView/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::LogView"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Policy
// =============================================================================

/// ResourceIdentifier implementation for Policy with LoggingLocationsBucketsViewsGetIamPolicyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingLocationsBucketsViewsGetIamPolicyArgs> for Policy {
    fn generate_resource_id(&self, input: &LoggingLocationsBucketsViewsGetIamPolicyArgs) -> String {
        format!("gcp::logging::Policy/{}", input.resource)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::Policy"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListViewsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListViewsResponse with LoggingLocationsBucketsViewsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingLocationsBucketsViewsListArgs> for ListViewsResponse {
    fn generate_resource_id(&self, input: &LoggingLocationsBucketsViewsListArgs) -> String {
        format!("gcp::logging::ListViewsResponse/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::ListViewsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for LogView
// =============================================================================

/// ResourceIdentifier implementation for LogView with LoggingLocationsBucketsViewsPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingLocationsBucketsViewsPatchArgs> for LogView {
    fn generate_resource_id(&self, input: &LoggingLocationsBucketsViewsPatchArgs) -> String {
        format!("gcp::logging::LogView/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::LogView"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Policy
// =============================================================================

/// ResourceIdentifier implementation for Policy with LoggingLocationsBucketsViewsSetIamPolicyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingLocationsBucketsViewsSetIamPolicyArgs> for Policy {
    fn generate_resource_id(&self, input: &LoggingLocationsBucketsViewsSetIamPolicyArgs) -> String {
        format!("gcp::logging::Policy/{}", input.resource)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::Policy"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for TestIamPermissionsResponse
// =============================================================================

/// ResourceIdentifier implementation for TestIamPermissionsResponse with LoggingLocationsBucketsViewsTestIamPermissionsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingLocationsBucketsViewsTestIamPermissionsArgs>
    for TestIamPermissionsResponse
{
    fn generate_resource_id(
        &self,
        input: &LoggingLocationsBucketsViewsTestIamPermissionsArgs,
    ) -> String {
        format!(
            "gcp::logging::TestIamPermissionsResponse/{}",
            input.resource
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::TestIamPermissionsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Empty
// =============================================================================

/// ResourceIdentifier implementation for Empty with LoggingLocationsOperationsCancelArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingLocationsOperationsCancelArgs> for Empty {
    fn generate_resource_id(&self, input: &LoggingLocationsOperationsCancelArgs) -> String {
        format!("gcp::logging::Empty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::Empty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with LoggingLocationsOperationsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingLocationsOperationsGetArgs> for Operation {
    fn generate_resource_id(&self, input: &LoggingLocationsOperationsGetArgs) -> String {
        format!("gcp::logging::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListOperationsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListOperationsResponse with LoggingLocationsOperationsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingLocationsOperationsListArgs> for ListOperationsResponse {
    fn generate_resource_id(&self, input: &LoggingLocationsOperationsListArgs) -> String {
        format!("gcp::logging::ListOperationsResponse/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::ListOperationsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Empty
// =============================================================================

/// ResourceIdentifier implementation for Empty with LoggingLogsDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingLogsDeleteArgs> for Empty {
    fn generate_resource_id(&self, input: &LoggingLogsDeleteArgs) -> String {
        format!("gcp::logging::Empty/{}", input.logName)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::Empty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListLogsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListLogsResponse with LoggingLogsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingLogsListArgs> for ListLogsResponse {
    fn generate_resource_id(&self, input: &LoggingLogsListArgs) -> String {
        format!("gcp::logging::ListLogsResponse/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::ListLogsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListMonitoredResourceDescriptorsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListMonitoredResourceDescriptorsResponse with LoggingMonitoredResourceDescriptorsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingMonitoredResourceDescriptorsListArgs>
    for ListMonitoredResourceDescriptorsResponse
{
    fn generate_resource_id(&self, input: &LoggingMonitoredResourceDescriptorsListArgs) -> String {
        "gcp::logging::ListMonitoredResourceDescriptorsResponse".to_string()
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::ListMonitoredResourceDescriptorsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for CmekSettings
// =============================================================================

/// ResourceIdentifier implementation for CmekSettings with LoggingOrganizationsGetCmekSettingsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingOrganizationsGetCmekSettingsArgs> for CmekSettings {
    fn generate_resource_id(&self, input: &LoggingOrganizationsGetCmekSettingsArgs) -> String {
        format!("gcp::logging::CmekSettings/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::CmekSettings"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Settings
// =============================================================================

/// ResourceIdentifier implementation for Settings with LoggingOrganizationsGetSettingsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingOrganizationsGetSettingsArgs> for Settings {
    fn generate_resource_id(&self, input: &LoggingOrganizationsGetSettingsArgs) -> String {
        format!("gcp::logging::Settings/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::Settings"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for CmekSettings
// =============================================================================

/// ResourceIdentifier implementation for CmekSettings with LoggingOrganizationsUpdateCmekSettingsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingOrganizationsUpdateCmekSettingsArgs> for CmekSettings {
    fn generate_resource_id(&self, input: &LoggingOrganizationsUpdateCmekSettingsArgs) -> String {
        format!("gcp::logging::CmekSettings/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::CmekSettings"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Settings
// =============================================================================

/// ResourceIdentifier implementation for Settings with LoggingOrganizationsUpdateSettingsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingOrganizationsUpdateSettingsArgs> for Settings {
    fn generate_resource_id(&self, input: &LoggingOrganizationsUpdateSettingsArgs) -> String {
        format!("gcp::logging::Settings/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::Settings"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for LogExclusion
// =============================================================================

/// ResourceIdentifier implementation for LogExclusion with LoggingOrganizationsExclusionsCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingOrganizationsExclusionsCreateArgs> for LogExclusion {
    fn generate_resource_id(&self, input: &LoggingOrganizationsExclusionsCreateArgs) -> String {
        format!("gcp::logging::LogExclusion/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::LogExclusion"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Empty
// =============================================================================

/// ResourceIdentifier implementation for Empty with LoggingOrganizationsExclusionsDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingOrganizationsExclusionsDeleteArgs> for Empty {
    fn generate_resource_id(&self, input: &LoggingOrganizationsExclusionsDeleteArgs) -> String {
        format!("gcp::logging::Empty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::Empty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for LogExclusion
// =============================================================================

/// ResourceIdentifier implementation for LogExclusion with LoggingOrganizationsExclusionsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingOrganizationsExclusionsGetArgs> for LogExclusion {
    fn generate_resource_id(&self, input: &LoggingOrganizationsExclusionsGetArgs) -> String {
        format!("gcp::logging::LogExclusion/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::LogExclusion"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListExclusionsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListExclusionsResponse with LoggingOrganizationsExclusionsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingOrganizationsExclusionsListArgs> for ListExclusionsResponse {
    fn generate_resource_id(&self, input: &LoggingOrganizationsExclusionsListArgs) -> String {
        format!("gcp::logging::ListExclusionsResponse/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::ListExclusionsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for LogExclusion
// =============================================================================

/// ResourceIdentifier implementation for LogExclusion with LoggingOrganizationsExclusionsPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingOrganizationsExclusionsPatchArgs> for LogExclusion {
    fn generate_resource_id(&self, input: &LoggingOrganizationsExclusionsPatchArgs) -> String {
        format!("gcp::logging::LogExclusion/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::LogExclusion"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Location
// =============================================================================

/// ResourceIdentifier implementation for Location with LoggingOrganizationsLocationsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingOrganizationsLocationsGetArgs> for Location {
    fn generate_resource_id(&self, input: &LoggingOrganizationsLocationsGetArgs) -> String {
        format!("gcp::logging::Location/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::Location"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListLocationsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListLocationsResponse with LoggingOrganizationsLocationsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingOrganizationsLocationsListArgs> for ListLocationsResponse {
    fn generate_resource_id(&self, input: &LoggingOrganizationsLocationsListArgs) -> String {
        format!("gcp::logging::ListLocationsResponse/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::ListLocationsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for LogBucket
// =============================================================================

/// ResourceIdentifier implementation for LogBucket with LoggingOrganizationsLocationsBucketsCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingOrganizationsLocationsBucketsCreateArgs> for LogBucket {
    fn generate_resource_id(
        &self,
        input: &LoggingOrganizationsLocationsBucketsCreateArgs,
    ) -> String {
        format!("gcp::logging::LogBucket/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::LogBucket"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with LoggingOrganizationsLocationsBucketsCreateAsyncArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingOrganizationsLocationsBucketsCreateAsyncArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &LoggingOrganizationsLocationsBucketsCreateAsyncArgs,
    ) -> String {
        format!("gcp::logging::Operation/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Empty
// =============================================================================

/// ResourceIdentifier implementation for Empty with LoggingOrganizationsLocationsBucketsDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingOrganizationsLocationsBucketsDeleteArgs> for Empty {
    fn generate_resource_id(
        &self,
        input: &LoggingOrganizationsLocationsBucketsDeleteArgs,
    ) -> String {
        format!("gcp::logging::Empty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::Empty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for LogBucket
// =============================================================================

/// ResourceIdentifier implementation for LogBucket with LoggingOrganizationsLocationsBucketsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingOrganizationsLocationsBucketsGetArgs> for LogBucket {
    fn generate_resource_id(&self, input: &LoggingOrganizationsLocationsBucketsGetArgs) -> String {
        format!("gcp::logging::LogBucket/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::LogBucket"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListBucketsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListBucketsResponse with LoggingOrganizationsLocationsBucketsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingOrganizationsLocationsBucketsListArgs> for ListBucketsResponse {
    fn generate_resource_id(&self, input: &LoggingOrganizationsLocationsBucketsListArgs) -> String {
        format!("gcp::logging::ListBucketsResponse/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::ListBucketsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for LogBucket
// =============================================================================

/// ResourceIdentifier implementation for LogBucket with LoggingOrganizationsLocationsBucketsPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingOrganizationsLocationsBucketsPatchArgs> for LogBucket {
    fn generate_resource_id(
        &self,
        input: &LoggingOrganizationsLocationsBucketsPatchArgs,
    ) -> String {
        format!("gcp::logging::LogBucket/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::LogBucket"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Empty
// =============================================================================

/// ResourceIdentifier implementation for Empty with LoggingOrganizationsLocationsBucketsUndeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingOrganizationsLocationsBucketsUndeleteArgs> for Empty {
    fn generate_resource_id(
        &self,
        input: &LoggingOrganizationsLocationsBucketsUndeleteArgs,
    ) -> String {
        format!("gcp::logging::Empty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::Empty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with LoggingOrganizationsLocationsBucketsUpdateAsyncArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingOrganizationsLocationsBucketsUpdateAsyncArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &LoggingOrganizationsLocationsBucketsUpdateAsyncArgs,
    ) -> String {
        format!("gcp::logging::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with LoggingOrganizationsLocationsBucketsLinksCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingOrganizationsLocationsBucketsLinksCreateArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &LoggingOrganizationsLocationsBucketsLinksCreateArgs,
    ) -> String {
        format!("gcp::logging::Operation/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with LoggingOrganizationsLocationsBucketsLinksDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingOrganizationsLocationsBucketsLinksDeleteArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &LoggingOrganizationsLocationsBucketsLinksDeleteArgs,
    ) -> String {
        format!("gcp::logging::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Link
// =============================================================================

/// ResourceIdentifier implementation for Link with LoggingOrganizationsLocationsBucketsLinksGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingOrganizationsLocationsBucketsLinksGetArgs> for Link {
    fn generate_resource_id(
        &self,
        input: &LoggingOrganizationsLocationsBucketsLinksGetArgs,
    ) -> String {
        format!("gcp::logging::Link/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::Link"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListLinksResponse
// =============================================================================

/// ResourceIdentifier implementation for ListLinksResponse with LoggingOrganizationsLocationsBucketsLinksListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingOrganizationsLocationsBucketsLinksListArgs> for ListLinksResponse {
    fn generate_resource_id(
        &self,
        input: &LoggingOrganizationsLocationsBucketsLinksListArgs,
    ) -> String {
        format!("gcp::logging::ListLinksResponse/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::ListLinksResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for LogView
// =============================================================================

/// ResourceIdentifier implementation for LogView with LoggingOrganizationsLocationsBucketsViewsCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingOrganizationsLocationsBucketsViewsCreateArgs> for LogView {
    fn generate_resource_id(
        &self,
        input: &LoggingOrganizationsLocationsBucketsViewsCreateArgs,
    ) -> String {
        format!("gcp::logging::LogView/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::LogView"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Empty
// =============================================================================

/// ResourceIdentifier implementation for Empty with LoggingOrganizationsLocationsBucketsViewsDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingOrganizationsLocationsBucketsViewsDeleteArgs> for Empty {
    fn generate_resource_id(
        &self,
        input: &LoggingOrganizationsLocationsBucketsViewsDeleteArgs,
    ) -> String {
        format!("gcp::logging::Empty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::Empty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for LogView
// =============================================================================

/// ResourceIdentifier implementation for LogView with LoggingOrganizationsLocationsBucketsViewsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingOrganizationsLocationsBucketsViewsGetArgs> for LogView {
    fn generate_resource_id(
        &self,
        input: &LoggingOrganizationsLocationsBucketsViewsGetArgs,
    ) -> String {
        format!("gcp::logging::LogView/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::LogView"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Policy
// =============================================================================

/// ResourceIdentifier implementation for Policy with LoggingOrganizationsLocationsBucketsViewsGetIamPolicyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingOrganizationsLocationsBucketsViewsGetIamPolicyArgs> for Policy {
    fn generate_resource_id(
        &self,
        input: &LoggingOrganizationsLocationsBucketsViewsGetIamPolicyArgs,
    ) -> String {
        format!("gcp::logging::Policy/{}", input.resource)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::Policy"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListViewsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListViewsResponse with LoggingOrganizationsLocationsBucketsViewsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingOrganizationsLocationsBucketsViewsListArgs> for ListViewsResponse {
    fn generate_resource_id(
        &self,
        input: &LoggingOrganizationsLocationsBucketsViewsListArgs,
    ) -> String {
        format!("gcp::logging::ListViewsResponse/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::ListViewsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for LogView
// =============================================================================

/// ResourceIdentifier implementation for LogView with LoggingOrganizationsLocationsBucketsViewsPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingOrganizationsLocationsBucketsViewsPatchArgs> for LogView {
    fn generate_resource_id(
        &self,
        input: &LoggingOrganizationsLocationsBucketsViewsPatchArgs,
    ) -> String {
        format!("gcp::logging::LogView/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::LogView"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Policy
// =============================================================================

/// ResourceIdentifier implementation for Policy with LoggingOrganizationsLocationsBucketsViewsSetIamPolicyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingOrganizationsLocationsBucketsViewsSetIamPolicyArgs> for Policy {
    fn generate_resource_id(
        &self,
        input: &LoggingOrganizationsLocationsBucketsViewsSetIamPolicyArgs,
    ) -> String {
        format!("gcp::logging::Policy/{}", input.resource)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::Policy"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for TestIamPermissionsResponse
// =============================================================================

/// ResourceIdentifier implementation for TestIamPermissionsResponse with LoggingOrganizationsLocationsBucketsViewsTestIamPermissionsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingOrganizationsLocationsBucketsViewsTestIamPermissionsArgs>
    for TestIamPermissionsResponse
{
    fn generate_resource_id(
        &self,
        input: &LoggingOrganizationsLocationsBucketsViewsTestIamPermissionsArgs,
    ) -> String {
        format!(
            "gcp::logging::TestIamPermissionsResponse/{}",
            input.resource
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::TestIamPermissionsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListLogsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListLogsResponse with LoggingOrganizationsLocationsBucketsViewsLogsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingOrganizationsLocationsBucketsViewsLogsListArgs>
    for ListLogsResponse
{
    fn generate_resource_id(
        &self,
        input: &LoggingOrganizationsLocationsBucketsViewsLogsListArgs,
    ) -> String {
        format!("gcp::logging::ListLogsResponse/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::ListLogsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for LogScope
// =============================================================================

/// ResourceIdentifier implementation for LogScope with LoggingOrganizationsLocationsLogScopesCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingOrganizationsLocationsLogScopesCreateArgs> for LogScope {
    fn generate_resource_id(
        &self,
        input: &LoggingOrganizationsLocationsLogScopesCreateArgs,
    ) -> String {
        format!("gcp::logging::LogScope/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::LogScope"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Empty
// =============================================================================

/// ResourceIdentifier implementation for Empty with LoggingOrganizationsLocationsLogScopesDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingOrganizationsLocationsLogScopesDeleteArgs> for Empty {
    fn generate_resource_id(
        &self,
        input: &LoggingOrganizationsLocationsLogScopesDeleteArgs,
    ) -> String {
        format!("gcp::logging::Empty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::Empty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for LogScope
// =============================================================================

/// ResourceIdentifier implementation for LogScope with LoggingOrganizationsLocationsLogScopesGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingOrganizationsLocationsLogScopesGetArgs> for LogScope {
    fn generate_resource_id(
        &self,
        input: &LoggingOrganizationsLocationsLogScopesGetArgs,
    ) -> String {
        format!("gcp::logging::LogScope/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::LogScope"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListLogScopesResponse
// =============================================================================

/// ResourceIdentifier implementation for ListLogScopesResponse with LoggingOrganizationsLocationsLogScopesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingOrganizationsLocationsLogScopesListArgs> for ListLogScopesResponse {
    fn generate_resource_id(
        &self,
        input: &LoggingOrganizationsLocationsLogScopesListArgs,
    ) -> String {
        format!("gcp::logging::ListLogScopesResponse/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::ListLogScopesResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for LogScope
// =============================================================================

/// ResourceIdentifier implementation for LogScope with LoggingOrganizationsLocationsLogScopesPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingOrganizationsLocationsLogScopesPatchArgs> for LogScope {
    fn generate_resource_id(
        &self,
        input: &LoggingOrganizationsLocationsLogScopesPatchArgs,
    ) -> String {
        format!("gcp::logging::LogScope/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::LogScope"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Empty
// =============================================================================

/// ResourceIdentifier implementation for Empty with LoggingOrganizationsLocationsOperationsCancelArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingOrganizationsLocationsOperationsCancelArgs> for Empty {
    fn generate_resource_id(
        &self,
        input: &LoggingOrganizationsLocationsOperationsCancelArgs,
    ) -> String {
        format!("gcp::logging::Empty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::Empty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with LoggingOrganizationsLocationsOperationsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingOrganizationsLocationsOperationsGetArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &LoggingOrganizationsLocationsOperationsGetArgs,
    ) -> String {
        format!("gcp::logging::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListOperationsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListOperationsResponse with LoggingOrganizationsLocationsOperationsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingOrganizationsLocationsOperationsListArgs>
    for ListOperationsResponse
{
    fn generate_resource_id(
        &self,
        input: &LoggingOrganizationsLocationsOperationsListArgs,
    ) -> String {
        format!("gcp::logging::ListOperationsResponse/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::ListOperationsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListRecentQueriesResponse
// =============================================================================

/// ResourceIdentifier implementation for ListRecentQueriesResponse with LoggingOrganizationsLocationsRecentQueriesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingOrganizationsLocationsRecentQueriesListArgs>
    for ListRecentQueriesResponse
{
    fn generate_resource_id(
        &self,
        input: &LoggingOrganizationsLocationsRecentQueriesListArgs,
    ) -> String {
        format!("gcp::logging::ListRecentQueriesResponse/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::ListRecentQueriesResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for SavedQuery
// =============================================================================

/// ResourceIdentifier implementation for SavedQuery with LoggingOrganizationsLocationsSavedQueriesCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingOrganizationsLocationsSavedQueriesCreateArgs> for SavedQuery {
    fn generate_resource_id(
        &self,
        input: &LoggingOrganizationsLocationsSavedQueriesCreateArgs,
    ) -> String {
        format!("gcp::logging::SavedQuery/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::SavedQuery"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Empty
// =============================================================================

/// ResourceIdentifier implementation for Empty with LoggingOrganizationsLocationsSavedQueriesDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingOrganizationsLocationsSavedQueriesDeleteArgs> for Empty {
    fn generate_resource_id(
        &self,
        input: &LoggingOrganizationsLocationsSavedQueriesDeleteArgs,
    ) -> String {
        format!("gcp::logging::Empty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::Empty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for SavedQuery
// =============================================================================

/// ResourceIdentifier implementation for SavedQuery with LoggingOrganizationsLocationsSavedQueriesGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingOrganizationsLocationsSavedQueriesGetArgs> for SavedQuery {
    fn generate_resource_id(
        &self,
        input: &LoggingOrganizationsLocationsSavedQueriesGetArgs,
    ) -> String {
        format!("gcp::logging::SavedQuery/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::SavedQuery"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListSavedQueriesResponse
// =============================================================================

/// ResourceIdentifier implementation for ListSavedQueriesResponse with LoggingOrganizationsLocationsSavedQueriesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingOrganizationsLocationsSavedQueriesListArgs>
    for ListSavedQueriesResponse
{
    fn generate_resource_id(
        &self,
        input: &LoggingOrganizationsLocationsSavedQueriesListArgs,
    ) -> String {
        format!("gcp::logging::ListSavedQueriesResponse/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::ListSavedQueriesResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for SavedQuery
// =============================================================================

/// ResourceIdentifier implementation for SavedQuery with LoggingOrganizationsLocationsSavedQueriesPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingOrganizationsLocationsSavedQueriesPatchArgs> for SavedQuery {
    fn generate_resource_id(
        &self,
        input: &LoggingOrganizationsLocationsSavedQueriesPatchArgs,
    ) -> String {
        format!("gcp::logging::SavedQuery/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::SavedQuery"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Empty
// =============================================================================

/// ResourceIdentifier implementation for Empty with LoggingOrganizationsLogsDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingOrganizationsLogsDeleteArgs> for Empty {
    fn generate_resource_id(&self, input: &LoggingOrganizationsLogsDeleteArgs) -> String {
        format!("gcp::logging::Empty/{}", input.logName)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::Empty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListLogsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListLogsResponse with LoggingOrganizationsLogsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingOrganizationsLogsListArgs> for ListLogsResponse {
    fn generate_resource_id(&self, input: &LoggingOrganizationsLogsListArgs) -> String {
        format!("gcp::logging::ListLogsResponse/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::ListLogsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for LogSink
// =============================================================================

/// ResourceIdentifier implementation for LogSink with LoggingOrganizationsSinksCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingOrganizationsSinksCreateArgs> for LogSink {
    fn generate_resource_id(&self, input: &LoggingOrganizationsSinksCreateArgs) -> String {
        format!("gcp::logging::LogSink/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::LogSink"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Empty
// =============================================================================

/// ResourceIdentifier implementation for Empty with LoggingOrganizationsSinksDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingOrganizationsSinksDeleteArgs> for Empty {
    fn generate_resource_id(&self, input: &LoggingOrganizationsSinksDeleteArgs) -> String {
        format!("gcp::logging::Empty/{}", input.sinkName)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::Empty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for LogSink
// =============================================================================

/// ResourceIdentifier implementation for LogSink with LoggingOrganizationsSinksGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingOrganizationsSinksGetArgs> for LogSink {
    fn generate_resource_id(&self, input: &LoggingOrganizationsSinksGetArgs) -> String {
        format!("gcp::logging::LogSink/{}", input.sinkName)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::LogSink"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListSinksResponse
// =============================================================================

/// ResourceIdentifier implementation for ListSinksResponse with LoggingOrganizationsSinksListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingOrganizationsSinksListArgs> for ListSinksResponse {
    fn generate_resource_id(&self, input: &LoggingOrganizationsSinksListArgs) -> String {
        format!("gcp::logging::ListSinksResponse/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::ListSinksResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for LogSink
// =============================================================================

/// ResourceIdentifier implementation for LogSink with LoggingOrganizationsSinksPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingOrganizationsSinksPatchArgs> for LogSink {
    fn generate_resource_id(&self, input: &LoggingOrganizationsSinksPatchArgs) -> String {
        format!("gcp::logging::LogSink/{}", input.sinkName)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::LogSink"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for LogSink
// =============================================================================

/// ResourceIdentifier implementation for LogSink with LoggingOrganizationsSinksUpdateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingOrganizationsSinksUpdateArgs> for LogSink {
    fn generate_resource_id(&self, input: &LoggingOrganizationsSinksUpdateArgs) -> String {
        format!("gcp::logging::LogSink/{}", input.sinkName)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::LogSink"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for CmekSettings
// =============================================================================

/// ResourceIdentifier implementation for CmekSettings with LoggingProjectsGetCmekSettingsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingProjectsGetCmekSettingsArgs> for CmekSettings {
    fn generate_resource_id(&self, input: &LoggingProjectsGetCmekSettingsArgs) -> String {
        format!("gcp::logging::CmekSettings/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::CmekSettings"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Settings
// =============================================================================

/// ResourceIdentifier implementation for Settings with LoggingProjectsGetSettingsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingProjectsGetSettingsArgs> for Settings {
    fn generate_resource_id(&self, input: &LoggingProjectsGetSettingsArgs) -> String {
        format!("gcp::logging::Settings/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::Settings"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for LogExclusion
// =============================================================================

/// ResourceIdentifier implementation for LogExclusion with LoggingProjectsExclusionsCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingProjectsExclusionsCreateArgs> for LogExclusion {
    fn generate_resource_id(&self, input: &LoggingProjectsExclusionsCreateArgs) -> String {
        format!("gcp::logging::LogExclusion/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::LogExclusion"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Empty
// =============================================================================

/// ResourceIdentifier implementation for Empty with LoggingProjectsExclusionsDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingProjectsExclusionsDeleteArgs> for Empty {
    fn generate_resource_id(&self, input: &LoggingProjectsExclusionsDeleteArgs) -> String {
        format!("gcp::logging::Empty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::Empty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for LogExclusion
// =============================================================================

/// ResourceIdentifier implementation for LogExclusion with LoggingProjectsExclusionsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingProjectsExclusionsGetArgs> for LogExclusion {
    fn generate_resource_id(&self, input: &LoggingProjectsExclusionsGetArgs) -> String {
        format!("gcp::logging::LogExclusion/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::LogExclusion"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListExclusionsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListExclusionsResponse with LoggingProjectsExclusionsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingProjectsExclusionsListArgs> for ListExclusionsResponse {
    fn generate_resource_id(&self, input: &LoggingProjectsExclusionsListArgs) -> String {
        format!("gcp::logging::ListExclusionsResponse/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::ListExclusionsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for LogExclusion
// =============================================================================

/// ResourceIdentifier implementation for LogExclusion with LoggingProjectsExclusionsPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingProjectsExclusionsPatchArgs> for LogExclusion {
    fn generate_resource_id(&self, input: &LoggingProjectsExclusionsPatchArgs) -> String {
        format!("gcp::logging::LogExclusion/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::LogExclusion"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Location
// =============================================================================

/// ResourceIdentifier implementation for Location with LoggingProjectsLocationsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingProjectsLocationsGetArgs> for Location {
    fn generate_resource_id(&self, input: &LoggingProjectsLocationsGetArgs) -> String {
        format!("gcp::logging::Location/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::Location"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListLocationsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListLocationsResponse with LoggingProjectsLocationsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingProjectsLocationsListArgs> for ListLocationsResponse {
    fn generate_resource_id(&self, input: &LoggingProjectsLocationsListArgs) -> String {
        format!("gcp::logging::ListLocationsResponse/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::ListLocationsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for LogBucket
// =============================================================================

/// ResourceIdentifier implementation for LogBucket with LoggingProjectsLocationsBucketsCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingProjectsLocationsBucketsCreateArgs> for LogBucket {
    fn generate_resource_id(&self, input: &LoggingProjectsLocationsBucketsCreateArgs) -> String {
        format!("gcp::logging::LogBucket/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::LogBucket"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with LoggingProjectsLocationsBucketsCreateAsyncArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingProjectsLocationsBucketsCreateAsyncArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &LoggingProjectsLocationsBucketsCreateAsyncArgs,
    ) -> String {
        format!("gcp::logging::Operation/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Empty
// =============================================================================

/// ResourceIdentifier implementation for Empty with LoggingProjectsLocationsBucketsDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingProjectsLocationsBucketsDeleteArgs> for Empty {
    fn generate_resource_id(&self, input: &LoggingProjectsLocationsBucketsDeleteArgs) -> String {
        format!("gcp::logging::Empty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::Empty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for LogBucket
// =============================================================================

/// ResourceIdentifier implementation for LogBucket with LoggingProjectsLocationsBucketsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingProjectsLocationsBucketsGetArgs> for LogBucket {
    fn generate_resource_id(&self, input: &LoggingProjectsLocationsBucketsGetArgs) -> String {
        format!("gcp::logging::LogBucket/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::LogBucket"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListBucketsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListBucketsResponse with LoggingProjectsLocationsBucketsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingProjectsLocationsBucketsListArgs> for ListBucketsResponse {
    fn generate_resource_id(&self, input: &LoggingProjectsLocationsBucketsListArgs) -> String {
        format!("gcp::logging::ListBucketsResponse/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::ListBucketsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for LogBucket
// =============================================================================

/// ResourceIdentifier implementation for LogBucket with LoggingProjectsLocationsBucketsPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingProjectsLocationsBucketsPatchArgs> for LogBucket {
    fn generate_resource_id(&self, input: &LoggingProjectsLocationsBucketsPatchArgs) -> String {
        format!("gcp::logging::LogBucket/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::LogBucket"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Empty
// =============================================================================

/// ResourceIdentifier implementation for Empty with LoggingProjectsLocationsBucketsUndeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingProjectsLocationsBucketsUndeleteArgs> for Empty {
    fn generate_resource_id(&self, input: &LoggingProjectsLocationsBucketsUndeleteArgs) -> String {
        format!("gcp::logging::Empty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::Empty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with LoggingProjectsLocationsBucketsUpdateAsyncArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingProjectsLocationsBucketsUpdateAsyncArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &LoggingProjectsLocationsBucketsUpdateAsyncArgs,
    ) -> String {
        format!("gcp::logging::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with LoggingProjectsLocationsBucketsLinksCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingProjectsLocationsBucketsLinksCreateArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &LoggingProjectsLocationsBucketsLinksCreateArgs,
    ) -> String {
        format!("gcp::logging::Operation/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with LoggingProjectsLocationsBucketsLinksDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingProjectsLocationsBucketsLinksDeleteArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &LoggingProjectsLocationsBucketsLinksDeleteArgs,
    ) -> String {
        format!("gcp::logging::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Link
// =============================================================================

/// ResourceIdentifier implementation for Link with LoggingProjectsLocationsBucketsLinksGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingProjectsLocationsBucketsLinksGetArgs> for Link {
    fn generate_resource_id(&self, input: &LoggingProjectsLocationsBucketsLinksGetArgs) -> String {
        format!("gcp::logging::Link/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::Link"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListLinksResponse
// =============================================================================

/// ResourceIdentifier implementation for ListLinksResponse with LoggingProjectsLocationsBucketsLinksListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingProjectsLocationsBucketsLinksListArgs> for ListLinksResponse {
    fn generate_resource_id(&self, input: &LoggingProjectsLocationsBucketsLinksListArgs) -> String {
        format!("gcp::logging::ListLinksResponse/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::ListLinksResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for LogView
// =============================================================================

/// ResourceIdentifier implementation for LogView with LoggingProjectsLocationsBucketsViewsCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingProjectsLocationsBucketsViewsCreateArgs> for LogView {
    fn generate_resource_id(
        &self,
        input: &LoggingProjectsLocationsBucketsViewsCreateArgs,
    ) -> String {
        format!("gcp::logging::LogView/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::LogView"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Empty
// =============================================================================

/// ResourceIdentifier implementation for Empty with LoggingProjectsLocationsBucketsViewsDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingProjectsLocationsBucketsViewsDeleteArgs> for Empty {
    fn generate_resource_id(
        &self,
        input: &LoggingProjectsLocationsBucketsViewsDeleteArgs,
    ) -> String {
        format!("gcp::logging::Empty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::Empty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for LogView
// =============================================================================

/// ResourceIdentifier implementation for LogView with LoggingProjectsLocationsBucketsViewsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingProjectsLocationsBucketsViewsGetArgs> for LogView {
    fn generate_resource_id(&self, input: &LoggingProjectsLocationsBucketsViewsGetArgs) -> String {
        format!("gcp::logging::LogView/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::LogView"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Policy
// =============================================================================

/// ResourceIdentifier implementation for Policy with LoggingProjectsLocationsBucketsViewsGetIamPolicyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingProjectsLocationsBucketsViewsGetIamPolicyArgs> for Policy {
    fn generate_resource_id(
        &self,
        input: &LoggingProjectsLocationsBucketsViewsGetIamPolicyArgs,
    ) -> String {
        format!("gcp::logging::Policy/{}", input.resource)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::Policy"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListViewsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListViewsResponse with LoggingProjectsLocationsBucketsViewsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingProjectsLocationsBucketsViewsListArgs> for ListViewsResponse {
    fn generate_resource_id(&self, input: &LoggingProjectsLocationsBucketsViewsListArgs) -> String {
        format!("gcp::logging::ListViewsResponse/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::ListViewsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for LogView
// =============================================================================

/// ResourceIdentifier implementation for LogView with LoggingProjectsLocationsBucketsViewsPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingProjectsLocationsBucketsViewsPatchArgs> for LogView {
    fn generate_resource_id(
        &self,
        input: &LoggingProjectsLocationsBucketsViewsPatchArgs,
    ) -> String {
        format!("gcp::logging::LogView/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::LogView"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Policy
// =============================================================================

/// ResourceIdentifier implementation for Policy with LoggingProjectsLocationsBucketsViewsSetIamPolicyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingProjectsLocationsBucketsViewsSetIamPolicyArgs> for Policy {
    fn generate_resource_id(
        &self,
        input: &LoggingProjectsLocationsBucketsViewsSetIamPolicyArgs,
    ) -> String {
        format!("gcp::logging::Policy/{}", input.resource)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::Policy"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for TestIamPermissionsResponse
// =============================================================================

/// ResourceIdentifier implementation for TestIamPermissionsResponse with LoggingProjectsLocationsBucketsViewsTestIamPermissionsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingProjectsLocationsBucketsViewsTestIamPermissionsArgs>
    for TestIamPermissionsResponse
{
    fn generate_resource_id(
        &self,
        input: &LoggingProjectsLocationsBucketsViewsTestIamPermissionsArgs,
    ) -> String {
        format!(
            "gcp::logging::TestIamPermissionsResponse/{}",
            input.resource
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::TestIamPermissionsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListLogsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListLogsResponse with LoggingProjectsLocationsBucketsViewsLogsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingProjectsLocationsBucketsViewsLogsListArgs> for ListLogsResponse {
    fn generate_resource_id(
        &self,
        input: &LoggingProjectsLocationsBucketsViewsLogsListArgs,
    ) -> String {
        format!("gcp::logging::ListLogsResponse/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::ListLogsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for LogScope
// =============================================================================

/// ResourceIdentifier implementation for LogScope with LoggingProjectsLocationsLogScopesCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingProjectsLocationsLogScopesCreateArgs> for LogScope {
    fn generate_resource_id(&self, input: &LoggingProjectsLocationsLogScopesCreateArgs) -> String {
        format!("gcp::logging::LogScope/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::LogScope"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Empty
// =============================================================================

/// ResourceIdentifier implementation for Empty with LoggingProjectsLocationsLogScopesDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingProjectsLocationsLogScopesDeleteArgs> for Empty {
    fn generate_resource_id(&self, input: &LoggingProjectsLocationsLogScopesDeleteArgs) -> String {
        format!("gcp::logging::Empty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::Empty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for LogScope
// =============================================================================

/// ResourceIdentifier implementation for LogScope with LoggingProjectsLocationsLogScopesGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingProjectsLocationsLogScopesGetArgs> for LogScope {
    fn generate_resource_id(&self, input: &LoggingProjectsLocationsLogScopesGetArgs) -> String {
        format!("gcp::logging::LogScope/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::LogScope"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListLogScopesResponse
// =============================================================================

/// ResourceIdentifier implementation for ListLogScopesResponse with LoggingProjectsLocationsLogScopesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingProjectsLocationsLogScopesListArgs> for ListLogScopesResponse {
    fn generate_resource_id(&self, input: &LoggingProjectsLocationsLogScopesListArgs) -> String {
        format!("gcp::logging::ListLogScopesResponse/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::ListLogScopesResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for LogScope
// =============================================================================

/// ResourceIdentifier implementation for LogScope with LoggingProjectsLocationsLogScopesPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingProjectsLocationsLogScopesPatchArgs> for LogScope {
    fn generate_resource_id(&self, input: &LoggingProjectsLocationsLogScopesPatchArgs) -> String {
        format!("gcp::logging::LogScope/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::LogScope"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Empty
// =============================================================================

/// ResourceIdentifier implementation for Empty with LoggingProjectsLocationsOperationsCancelArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingProjectsLocationsOperationsCancelArgs> for Empty {
    fn generate_resource_id(&self, input: &LoggingProjectsLocationsOperationsCancelArgs) -> String {
        format!("gcp::logging::Empty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::Empty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with LoggingProjectsLocationsOperationsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingProjectsLocationsOperationsGetArgs> for Operation {
    fn generate_resource_id(&self, input: &LoggingProjectsLocationsOperationsGetArgs) -> String {
        format!("gcp::logging::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListOperationsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListOperationsResponse with LoggingProjectsLocationsOperationsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingProjectsLocationsOperationsListArgs> for ListOperationsResponse {
    fn generate_resource_id(&self, input: &LoggingProjectsLocationsOperationsListArgs) -> String {
        format!("gcp::logging::ListOperationsResponse/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::ListOperationsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListRecentQueriesResponse
// =============================================================================

/// ResourceIdentifier implementation for ListRecentQueriesResponse with LoggingProjectsLocationsRecentQueriesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingProjectsLocationsRecentQueriesListArgs>
    for ListRecentQueriesResponse
{
    fn generate_resource_id(
        &self,
        input: &LoggingProjectsLocationsRecentQueriesListArgs,
    ) -> String {
        format!("gcp::logging::ListRecentQueriesResponse/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::ListRecentQueriesResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for SavedQuery
// =============================================================================

/// ResourceIdentifier implementation for SavedQuery with LoggingProjectsLocationsSavedQueriesCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingProjectsLocationsSavedQueriesCreateArgs> for SavedQuery {
    fn generate_resource_id(
        &self,
        input: &LoggingProjectsLocationsSavedQueriesCreateArgs,
    ) -> String {
        format!("gcp::logging::SavedQuery/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::SavedQuery"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Empty
// =============================================================================

/// ResourceIdentifier implementation for Empty with LoggingProjectsLocationsSavedQueriesDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingProjectsLocationsSavedQueriesDeleteArgs> for Empty {
    fn generate_resource_id(
        &self,
        input: &LoggingProjectsLocationsSavedQueriesDeleteArgs,
    ) -> String {
        format!("gcp::logging::Empty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::Empty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for SavedQuery
// =============================================================================

/// ResourceIdentifier implementation for SavedQuery with LoggingProjectsLocationsSavedQueriesGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingProjectsLocationsSavedQueriesGetArgs> for SavedQuery {
    fn generate_resource_id(&self, input: &LoggingProjectsLocationsSavedQueriesGetArgs) -> String {
        format!("gcp::logging::SavedQuery/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::SavedQuery"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListSavedQueriesResponse
// =============================================================================

/// ResourceIdentifier implementation for ListSavedQueriesResponse with LoggingProjectsLocationsSavedQueriesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingProjectsLocationsSavedQueriesListArgs> for ListSavedQueriesResponse {
    fn generate_resource_id(&self, input: &LoggingProjectsLocationsSavedQueriesListArgs) -> String {
        format!("gcp::logging::ListSavedQueriesResponse/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::ListSavedQueriesResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for SavedQuery
// =============================================================================

/// ResourceIdentifier implementation for SavedQuery with LoggingProjectsLocationsSavedQueriesPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingProjectsLocationsSavedQueriesPatchArgs> for SavedQuery {
    fn generate_resource_id(
        &self,
        input: &LoggingProjectsLocationsSavedQueriesPatchArgs,
    ) -> String {
        format!("gcp::logging::SavedQuery/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::SavedQuery"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Empty
// =============================================================================

/// ResourceIdentifier implementation for Empty with LoggingProjectsLogsDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingProjectsLogsDeleteArgs> for Empty {
    fn generate_resource_id(&self, input: &LoggingProjectsLogsDeleteArgs) -> String {
        format!("gcp::logging::Empty/{}", input.logName)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::Empty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListLogsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListLogsResponse with LoggingProjectsLogsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingProjectsLogsListArgs> for ListLogsResponse {
    fn generate_resource_id(&self, input: &LoggingProjectsLogsListArgs) -> String {
        format!("gcp::logging::ListLogsResponse/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::ListLogsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for LogMetric
// =============================================================================

/// ResourceIdentifier implementation for LogMetric with LoggingProjectsMetricsCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingProjectsMetricsCreateArgs> for LogMetric {
    fn generate_resource_id(&self, input: &LoggingProjectsMetricsCreateArgs) -> String {
        format!("gcp::logging::LogMetric/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::LogMetric"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Empty
// =============================================================================

/// ResourceIdentifier implementation for Empty with LoggingProjectsMetricsDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingProjectsMetricsDeleteArgs> for Empty {
    fn generate_resource_id(&self, input: &LoggingProjectsMetricsDeleteArgs) -> String {
        format!("gcp::logging::Empty/{}", input.metricName)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::Empty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for LogMetric
// =============================================================================

/// ResourceIdentifier implementation for LogMetric with LoggingProjectsMetricsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingProjectsMetricsGetArgs> for LogMetric {
    fn generate_resource_id(&self, input: &LoggingProjectsMetricsGetArgs) -> String {
        format!("gcp::logging::LogMetric/{}", input.metricName)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::LogMetric"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListLogMetricsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListLogMetricsResponse with LoggingProjectsMetricsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingProjectsMetricsListArgs> for ListLogMetricsResponse {
    fn generate_resource_id(&self, input: &LoggingProjectsMetricsListArgs) -> String {
        format!("gcp::logging::ListLogMetricsResponse/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::ListLogMetricsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for LogMetric
// =============================================================================

/// ResourceIdentifier implementation for LogMetric with LoggingProjectsMetricsUpdateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingProjectsMetricsUpdateArgs> for LogMetric {
    fn generate_resource_id(&self, input: &LoggingProjectsMetricsUpdateArgs) -> String {
        format!("gcp::logging::LogMetric/{}", input.metricName)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::LogMetric"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for LogSink
// =============================================================================

/// ResourceIdentifier implementation for LogSink with LoggingProjectsSinksCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingProjectsSinksCreateArgs> for LogSink {
    fn generate_resource_id(&self, input: &LoggingProjectsSinksCreateArgs) -> String {
        format!("gcp::logging::LogSink/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::LogSink"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Empty
// =============================================================================

/// ResourceIdentifier implementation for Empty with LoggingProjectsSinksDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingProjectsSinksDeleteArgs> for Empty {
    fn generate_resource_id(&self, input: &LoggingProjectsSinksDeleteArgs) -> String {
        format!("gcp::logging::Empty/{}", input.sinkName)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::Empty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for LogSink
// =============================================================================

/// ResourceIdentifier implementation for LogSink with LoggingProjectsSinksGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingProjectsSinksGetArgs> for LogSink {
    fn generate_resource_id(&self, input: &LoggingProjectsSinksGetArgs) -> String {
        format!("gcp::logging::LogSink/{}", input.sinkName)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::LogSink"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListSinksResponse
// =============================================================================

/// ResourceIdentifier implementation for ListSinksResponse with LoggingProjectsSinksListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingProjectsSinksListArgs> for ListSinksResponse {
    fn generate_resource_id(&self, input: &LoggingProjectsSinksListArgs) -> String {
        format!("gcp::logging::ListSinksResponse/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::ListSinksResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for LogSink
// =============================================================================

/// ResourceIdentifier implementation for LogSink with LoggingProjectsSinksPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingProjectsSinksPatchArgs> for LogSink {
    fn generate_resource_id(&self, input: &LoggingProjectsSinksPatchArgs) -> String {
        format!("gcp::logging::LogSink/{}", input.sinkName)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::LogSink"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for LogSink
// =============================================================================

/// ResourceIdentifier implementation for LogSink with LoggingProjectsSinksUpdateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingProjectsSinksUpdateArgs> for LogSink {
    fn generate_resource_id(&self, input: &LoggingProjectsSinksUpdateArgs) -> String {
        format!("gcp::logging::LogSink/{}", input.sinkName)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::LogSink"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for LogSink
// =============================================================================

/// ResourceIdentifier implementation for LogSink with LoggingSinksCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingSinksCreateArgs> for LogSink {
    fn generate_resource_id(&self, input: &LoggingSinksCreateArgs) -> String {
        format!("gcp::logging::LogSink/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::LogSink"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Empty
// =============================================================================

/// ResourceIdentifier implementation for Empty with LoggingSinksDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingSinksDeleteArgs> for Empty {
    fn generate_resource_id(&self, input: &LoggingSinksDeleteArgs) -> String {
        format!("gcp::logging::Empty/{}", input.sinkName)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::Empty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for LogSink
// =============================================================================

/// ResourceIdentifier implementation for LogSink with LoggingSinksGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingSinksGetArgs> for LogSink {
    fn generate_resource_id(&self, input: &LoggingSinksGetArgs) -> String {
        format!("gcp::logging::LogSink/{}", input.sinkName)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::LogSink"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListSinksResponse
// =============================================================================

/// ResourceIdentifier implementation for ListSinksResponse with LoggingSinksListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingSinksListArgs> for ListSinksResponse {
    fn generate_resource_id(&self, input: &LoggingSinksListArgs) -> String {
        format!("gcp::logging::ListSinksResponse/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::ListSinksResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for LogSink
// =============================================================================

/// ResourceIdentifier implementation for LogSink with LoggingSinksUpdateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingSinksUpdateArgs> for LogSink {
    fn generate_resource_id(&self, input: &LoggingSinksUpdateArgs) -> String {
        format!("gcp::logging::LogSink/{}", input.sinkName)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::LogSink"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for CmekSettings
// =============================================================================

/// ResourceIdentifier implementation for CmekSettings with LoggingGetCmekSettingsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingGetCmekSettingsArgs> for CmekSettings {
    fn generate_resource_id(&self, input: &LoggingGetCmekSettingsArgs) -> String {
        format!("gcp::logging::CmekSettings/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::CmekSettings"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Settings
// =============================================================================

/// ResourceIdentifier implementation for Settings with LoggingGetSettingsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingGetSettingsArgs> for Settings {
    fn generate_resource_id(&self, input: &LoggingGetSettingsArgs) -> String {
        format!("gcp::logging::Settings/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::Settings"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for CmekSettings
// =============================================================================

/// ResourceIdentifier implementation for CmekSettings with LoggingUpdateCmekSettingsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingUpdateCmekSettingsArgs> for CmekSettings {
    fn generate_resource_id(&self, input: &LoggingUpdateCmekSettingsArgs) -> String {
        format!("gcp::logging::CmekSettings/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::CmekSettings"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Settings
// =============================================================================

/// ResourceIdentifier implementation for Settings with LoggingUpdateSettingsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<LoggingUpdateSettingsArgs> for Settings {
    fn generate_resource_id(&self, input: &LoggingUpdateSettingsArgs) -> String {
        format!("gcp::logging::Settings/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::logging::Settings"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}
